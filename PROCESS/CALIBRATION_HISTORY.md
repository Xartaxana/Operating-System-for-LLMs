# Calibration History (архив)

Назначение: известные поправки счёта / разрывы базы / прежние
редакции норм калибровки — данные, нужные при СЧЁТЕ (интерпретации
конкретных прошлых чисел, сверке с прежней формулировкой нормы), не
при каждом прогоне. Живая норма и её сторож остаются в
`PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md`; сюда уезжает только
история, на которую протокол ставит именованный указатель.

Владелец: Lead.

Правило ведения: append по датам (новые записи добавляются в конец,
существующие не переписываются и не переставляются); сюда уезжает
ИСТОРИЯ — прежние редакции норм, разрывы базы сравнения, разовые
известные поправки счёта — а не действующие нормы; переименование
файла и заголовков секций запрещено (на них ссылаются по имени файла
из протокола). Формат — LF, UTF-8 без BOM.

---

## 2026-07-09 — известный дубль task_id (учтён в чеке 13(д))

Известный дубль — t-008 (2026-07-09, F-23: golden-set прогон
12:42/12:55 и task-pipeline recon 13:20/13:28) — во всех счётах
чеков 3/13 идёт как ДВЕ задачи.

## 2026-07-10 — известный случай ts-нечестности (учтён в чеке 13(е))

Известный случай — события t-023..t-026 (2026-07-10, defect_found
08:12, реальные границы в notes): НЕ переписаны, во всех
таймлайновых счётах брать реальные времена из notes defect_found.

## 2026-07-19 — прежний скоуп v1-триггера hygiene_gate (учтён в чеке 26(а))

Формулировка чека 26(а), действовавшая с этой даты: на 2026-07-19
(калибровка №3, по чтению кода) v1-триггер ТРЕБОВАЛ подстроку
routing-log + редирект/printf-echo, то есть только класс «журнал
мимо Edit/Write», а cd-префикс/2>&1/python -c хук НЕ ловил — их
находки аудита при молчащем хуке НЕ были отказом постановки.

## 2026-07-13 — ретро-контур: правило 6 без исполняемого чека (учтено в чеке 21)

Заголовок чека 21 до диеты W2c-2: ретро-контур 2026-07-13 — до
этого правило 6 ССЫЛАЛОСЬ на калибровку, но исполняемого чека не
было, класс F-15.

## 2026-08-05 — прежняя редакция про permissionDecision hygiene_gate (учтена в чеке 26(а))

Поправка калибровки №6, 2026-08-05 — прежняя редакция «hygiene_gate
НЕ несёт permissionDecision, появление ключа = регресс» была верна
для v1 t-177 и ПРОТУХЛА после VG-5 2026-07-23, осознанно
промотировавшего класс (г) в БЛОК через permissionDecisionReason;
протокол противоречил коду и завёл бы ложный регресс.

## 2026-08-10 — отмена фильтра «обкатано у нас» (учтена в чеке 12(в))

Фильтр «обкатано у нас» отменён словом оператора 2026-08-10,
обкатка у добровольцев.

## 2026-08-09 — снимок «скрипты хуков вне невода» (учтён в чеке 26(а))

На 2026-08-09 невод MECHANISM_PREFIXES держал .claude/settings.json
целиком, но НЕ держал по отдельности ни один из tools/*.py-скриптов
хуков — правка скрипта без правки settings.json проходила мимо
осевого блока. Ремедиация F-56 2026-08-10 внесла скрипты поимённо;
строка чека переведена на текущее состояние диетой W2c-2
(2026-08-19).

## 2026-08-10..19 — флейк LIVE-STATE-TOUCHED пробы живости (учтён в чеке 26(д))

Прежняя инструкция чека 26(д): «ИЗВЕСТНЫЙ ДОБРОКАЧЕСТВЕННЫЙ ФЛЕЙК:
LIVE-STATE-TOUCHED на леджер-файле ТЕКУЩЕЙ сессии при длинном
прогоне может быть тайминг-совпадением записи собственного
PostToolUse-хука сессии — повторить прогон; устойчивое повторение =
настоящая находка». Класс «сторож, которого приучают игнорировать»;
снят посадкой q503/N2 2026-08-19 — атрибуция по маркеру
liveness-probe-, немаркированный дифф = LIVE-STATE-AMBIENT без
влияния на exit code.

## 2026-08-13 — исправление формулировки «ПРОПУЩЕННЫЕ ОКНА» (учтено в чеке 18(д))

ИСПРАВЛЕНИЕ (2026-08-13, критик t-428): формулировка ДО этой правки
утверждала, что секция ловит пропуск «даже если про него забыли
написать в notes» НЕЗАВИСИМО от прогона к прогону — ложно: main()
отчёта ВСЕГДА пересчитывал window_series непосредственно ПЕРЕД
построением этой секции, так что она структурно НИКОГДА не
срабатывала (к моменту печати гэп был уже сам собой закрыт тем же
прогоном).

## 2026-08-16 — прежняя редакция границы деградации (учтена в чеке 5)

Прежняя редакция дословно: «Lead-активность на модели ниже Fable
вне заявленного окна» (binding-aware формулировка 2026-08-16 по
кросс-пункту AO3_tests 08-15 заменила её приведением к привязке
`roles.lead` того деплоя, чей журнал читается).

## 2026-08-16 — прежняя редакция оснований приёмки сверху (учтена в чеке 6)

Прежняя редакция знала только (i) и (ii) — она отстала от D-0087
(2026-07-21) и D-0099 (2026-08-04) и до 2026-08-16 метила
нарушением обе законные формы; при разборе окон ДО этой даты
держать поправку в уме.

## 2026-08-16 — разрыв базы контрфакта savings_report (учтён в чеке 18(б))

СОСТОЯНИЕ НА 2026-08-16, знать при чтении числа: носитель расчёта
`tools/savings_report.py:52` всё ещё держит `FABLE_MODEL`
константой, приведение к привязке — задача t-443/t-444; пока она не
закрыта, число (б) считается по Fable и НЕСОПОСТАВИМО с точками
после закрытия — пометить разрыв базы в notes прогона.

ЗАТРОНУТА РОВНО ОДНА ПРОШЛАЯ ТОЧКА — сверено по окнам событий
`calibrated`, вопросом оператора 2026-08-16, поправившим завышение
вдвое в первой редакции этой строки: №6 шла в окне
07-29T18:42..08-05T12:34, перепривязка случилась ВНУТРИ него
(08-04), Lead фактически был Fable почти всё окно — контрфакт по
Fable для №6 ВЕРЕН; №7 шла в окне 08-05T12:34..08-14T12:12 целиком
после перепривязки, Lead = Opus — завышена именно она. НАПРАВЛЕНИЕ
ошибки называть обязательно: Fable $10/$50 против Opus $5/$25,
контрфакт считался ВДВОЕ ДОРОЖЕ истины, значит брутто-экономия №7
ЗАВЫШЕНА, а не занижена.

## 2026-08-19 — ретирование K1: witness-норма builder-приёмки (учтён в чеке 13(а))

Полнотекст ретированного 2026-08-19 (W2c-3, гибрид-нормализация
Р12(C) + ретирование): «Каждый accepted по builder-диспатчу несёт
поле `witness` — фактический вывод проверочного прогона (команда +
результат); accepted(builder) без witness = нарушение.» Сторож —
`tools/journal_validator.py:763-766` (обязательность непустого
`witness` для accepted+agent=builder); живость —
`python -m pytest tools/test_journal_validator.py -q`. Остаточный
риск (строка, не дошедшая до коммита, гейта не встречает) второй
слой ловит НЕ чек 13(л) (он проверяет качество доковых витнессов и
приёмку без поля `witness` не ловит) — реальный второй слой это
WITNESS-warn `tools/journal_echo.py` в момент записи строки
(`docs/RULE_COVERAGE.md:39`); чек 13(л) остаётся дополнительным
указателем именно для доковых витнессов, адресатом остаточного
риска НЕ является (критик W2c-3, 2026-08-19, фикс 4).

## 2026-08-19 — ретирование K3 (фрагмент): «"judge" в whitelist» чека 30(а) (учтён в чеке 30(а))

Полнотекст ретированного фрагмента 2026-08-19 (W2c-3) — ТОЛЬКО
клауза «"judge" в whitelist»; соседние клаузы «R13 в ядре» и
«judge-калибровка жива (13/13, D-0031)» ретированию НЕ подверглись
и остаются ЖИВЫМ текстом в чеке 30(а) (уточнение критика W2c-3,
2026-08-19, фикс 3 — первая редакция этой записи ошибочно относила
к ретированному всё предложение целиком, нарушая инвариант 1: у
живой нормы не может быть второго адреса со статусом «ретировано»).
Сторож — `JUDGE_BASIS_VALUE` (`tools/journal_validator.py:305`) +
лист-класс гейт (`tools/journal_validator.py:599-611`, требует
category ∈ LEAF_CATEGORIES и agent ∈ {scout, builder} при
basis="judge"); живость — `python -m pytest
tools/test_journal_validator.py -q` (сторожа покрыты
test_rejected_invalid_attempt_and_failure_class_fail:187 и
judge-basis группой тестов :809-1294). ДОСЛОВНОСТЬ судейского
промпта (`docs/RULE_COVERAGE.md:104` [спека W2c-3 §1 инв.5 цитирует
«:103» — факт: строка 104 на момент сверки 2026-08-19, замер, не
якорь], предложение «ОБЕ формы судьи —
... точка эквивалентности t-254 жива в журнале») этим срезом не
тронута — держится дословно в чеке 30(а) как прежде; ретирование
дословности промпта — НЕ этот срез.

## 2026-08-19 — ретирование K5 (фрагмент): первая фраза чека 13(в) про failure_class (учтён в чеке 13(в))

Полнотекст ретированного 2026-08-19 (W2c-3): «Каждый `rejected`
несёт `failure_class` (spec / capability / recon / tooling); без
класса = нарушение (смотреть вместе с чеком 3).» Сторож — enum
`FAILURE_CLASSES` (`tools/journal_validator.py:276`) + проверка
(`tools/journal_validator.py:757-761`) для event="rejected"; живость
— `python -m pytest tools/test_journal_validator.py -q` (сторож
покрыт test_rejected_invalid_attempt_and_failure_class_fail:187).
Остаток чека 13(в) (детектор отказа `tools/calibration_counts.py`)
остаётся живым текстом без изменений — расщепление K5 сняло только
первую фразу. ПРЕДИКАТ ПОДПУНКТА (критик W2c-3, 2026-08-19, БЛОКЕР
фикс 1): `pred:journal.event:rejected` гасил подпункт ПУСТ в любом
окне без rejected-событий, молча снимая из «к чтению» живые нормы,
не относящиеся к rejected (хвост подпункта — детектор отказа
calibration_counts); предикат заменён на `journal.any`
(наследование родителя чека 13, инвариант A3 — необходимое условие
непустоты — держится).

## 2026-08-20 — прежняя редакция триггера Lead-экзамена v2 (учтена в чеке 14(ж))

Период действия: 2026-08-18..2026-08-20. Прежняя формулировка
дословно: «Каждая такая правка сопровождается в Runs log v2 экзамена
ЛИБО строкой прогона-наследника, ЛИБО строкой «наследник
построен/в очереди с триггером» тем же коммитом; правка без строки =
находка (набор одноразовый, сжигается после серии —
«перепрогнать сожжённый» невозможно, триггер требует ПОСТРОИТЬ
наследника по методу).» Триггером было ЛЮБОЕ касание
`delegation.config.yaml` (поля `roles.lead`/`roles.reserve`) или
названных Lead-секций CLAUDE.md — без различения содержательной и
формосохраняющей правки. Основание сужения: два решения оператора
2026-08-20 (находка F6 калибровки №8) — предметом триггера остаётся
только СОДЕРЖАТЕЛЬНАЯ правка Lead-политики; формосохраняющая
переработка требует ОДНОГО из двух доказательств формы («форма: …» в
сообщении коммита) вместо наследника. Что перестало ловиться:
содержательная дельта в словах вне ключей `norm_keys.json` и вне
внимания критика формы; правка прозаических секций («Role ≠ tier»,
«Lead degradation», матрица приёмки, R11a), где машинного пина нет
вовсе. Компенсация: безусловный триггер роста `failure_class=spec`;
АУДИТ ЧЕСТНОСТИ строк «форма:»; fail-closed умолчание (недоказанная
формосохраняемость есть содержательная правка); закрытие пробела
«прозаические секции без ключей» — в очередь (Р1-Г). Для окон ДО
2026-08-20 читать эту редакцию.

## 2026-08-20 — прежняя редакция абзаца «РЕЦИДИВ ВНУТРИ СЕССИИ» (учтена в чеке 22)

Период действия: 2026-08-09..2026-08-20. Прежняя формулировка несла
ДВЕ клаузы дословно. Первая: «ВТОРОЕ и последующие события ОДНОГО
класса в ОДНОЙ сессии = НАРУШЕНИЕ САМО ПО СЕБЕ, независимо от того,
добрал ли класс порог >=3 ЗА ОКНО (существующий порог по окну —
ОТДЕЛЬНЫЙ сигнал «правило не соответствует практике», им НЕ
заменяется и НЕ переписывается).» Вторая — обязанность маркера:
«любое skip-событие, начиная СО ВТОРОГО в своём классе внутри
сессии, ОБЯЗАНО нести в notes маркер повтора («повтор класса»,
«второй ход класса» — любая явная строка), делающий нарушение
видимым грепом БЕЗ обращения к транскрипту. Оба слоя фиксируются:
скан транскрипта — окончательный арбитр, маркер в notes — дешёвый
первичный проход; отсутствие маркера у второго-и-далее skip-события
класса в сессии = нарушение само по себе (самодекларация не
сработала).» Правило было классово-нейтральным: ОБЕ клаузы (включая
обязанность маркера) применялись к повтору ЛЮБОГО skip-класса внутри
одной сессии, без различения класса-льготы (норма даёт послабление
один раз — эталон R8(c)) и стоячей концессии (норма требует события
на каждом применении — R1-самочтение, designer-самодрафт). Именно
ОБЛАСТЬ ДЕЙСТВИЯ второй клаузы (обязанность маркера) сузилась вместе
с первой — обе теперь применяются ТОЛЬКО к классу-льготе, и на этой
букве стоит находка окна №8. Основание сужения: два решения
оператора 2026-08-20 (находка F6 калибровки №8) — сужен ДО МОТИВА:
рецидивом само по себе считается только повтор КЛАССА-ЛЬГОТЫ; повтор
СТОЯЧЕЙ КОНЦЕССИИ нарушением не является, но считается и называется
числом (кормит порог >=3 и чеки-владельцы 1/1(б)). Что перестало
ловиться: повтор класса, формально остающегося стоячей концессией, но
фактически ставшего поглощением функции. Компенсация: порог >=3 за
окно не тронут; обязанность назвать числом повторы концессий;
чеки-владельцы 1 и 1(б); встроенный лимит R1 «до ~4 целей»; явная
процедура перевода класса в льготу записанной находкой калибрующего
Lead. Для окон ДО 2026-08-20 читать эту редакцию.

## 2026-08-05 — прецедент-эталон небезопасного отката порчи и первый замер пилота (учтён в чеке 25(в))

Прецедент-эталон: 2026-08-05, критик при R3-гейте портил БОЕВОЙ
logs/token_usage.xlsx и откатывал checkout'ом; потерь не было (файл
был чист, бэкап снят, md5 сверен Lead), но идиома ровно та, что
стоила инцидента AO3 08-02. Это и стало основанием регистрации
10(в)-детектора секции «командная гигиена» CLAUDE.md: без него
нарушения формы вызова не имели отдельного детектора отказа. Первый
замер — пилот t-106 на живой сессии: cd-префикс = 76% подозрительных
вызовов, 2 heredoc-аппенда журнала, 3 MASKED-правила.

## 2026-08-16 — базлайн посадки NOTES LEN (учтён в чеке 11(б))

Замер 2026-08-16 по всему журналу, 1239 событий: медиана 550, p75
768, p90 1047, p99 2040, max 13352; сверх 600 — 43%; последние 30
событий на момент посадки — медиана 780 и 22 из 30 сверх 600.

## 2026-08-05 — прецедент дубля разбора 32 чеков в очереди (учтён в чеке 24)

Прецедент 2026-08-05: разбор 32 чеков был написан в журнал И
продублирован прозой в очередь — 12059 Б, крупнейший пункт файла;
шаги 2 и 5 диеты его не ловили, потому что он не был ни закрытым, ни
дублем другого boot-файла.

## 2026-07-17 — рецидивы класса F-41 на стороне AO3 и прецедент-эталон прогона (учтены в чеке 24)

Рецидив класса F-41 на непокрытой стороне: AO3 docs/04-roadmap.md
содержательно стоял с 07-04 при действующем чеке — D-0048 п.2,
находка о самом чеке; второй экземпляр того же класса —
PROJECT.md/BUG-001. Прецедент-эталон прогона обоих проходов одним
scout-диспатчем: прогон fresh-docs-ao3 2026-07-17 (журнал AO3;
4 протухания найдено и закрыто тем же заходом).

## 2026-08-09 — провенанс абзаца «РЕЦИДИВ ВНУТРИ СЕССИИ» и эталон skip умолчания (учтён в чеке 22)

Абзац заведён 2026-08-09 как детектор нормы R8 «первый ход класса
бесплатно»; источник — ретро от калибровки №6, чек 22 счёт 3.
Сужение до мотива — 2026-08-20 словом оператора; прежняя редакция
самого абзаца лежит отдельной секцией «2026-08-20 — прежняя редакция
абзаца «РЕЦИДИВ ВНУТРИ СЕССИИ»» этого файла. Эталон skip класса
УМОЛЧАНИЕ (Lead-ярусная работа по Tiers table, R8 core «needs no
skip events») — событие журнала 2026-08-16T16:15:52.

## 2026-07-23 — регистрация кросс-репо handoff среза карты осей AO3 (учтена в чеке 12(в))

Регистрация сверки их state/sibling-map.snapshot.md — кросс-репо
handoff gate-map-anchor-0723, исполнен калибровкой №4 (событие
`calibrated` 2026-07-23T14:44:42: «детектор среза зарегистрирован в
чеке 12 этим же коммитом»).

## 2026-08-09 — что значит «ИНЦИДЕНТНЫЙ детектор» в чеке 25(г) (учтено в чеке 25(г))

Базовый детектор класса «усечение окна реестра → статусное
утверждение» назван ИНЦИДЕНТНЫМ в том смысле, что вопрос оператора
вскрывает расхождение по факту, не по правилу.
