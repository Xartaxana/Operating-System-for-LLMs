# Weekly Calibration Protocol (D-0047)

Исполняемый чек-лист еженедельной калибровки (ROADMAP Phase 1.5,
шаг 3). Загружается на время прогона, НЕ при boot (D-0038). До
D-0047 проверки были рассеяны по текстам решений (D-0042/44/45/46,
правила 1–10) без единого списка — калибратору пришлось бы
пере-выводить их каждый раз; этот файл — их канонический дом.
Новый механизм с обещанием «калибровка заметит X» обязан тем же
коммитом добавить сюда чек (иначе обещание пустое — F-15).

Входные данные: logs/routing-log.jsonl обоих деплоев (OS-репо и
D:\AO3_tests), cc_usage через tools/usage_report.py, git-история
обоих репо за период, диффы DECISIONS.md.

> ФОРМА ЗАПИСИ (Phase 5, W2a). Каждый чек несёт машиночитаемую шапку
> <!--CHK …--> первой строкой ПОСЛЕ строки с ЗАКРЫВАЮЩИМ `**` жирного
> заголовка чека (заголовок может быть многострочным); подпункт со
> своим предикатом или статусом получает СВОЮ шапку ТОЛЬКО когда его
> маркер "(x)" начинает строку — вплетённый в абзац подпункт под-шапки
> не получает и наследует предикат/статус родителя (поля и лимиты —
> tools/test_calibration_prepass.py). НОВЫЙ ЧЕК РОЖДАЕТСЯ СРАЗУ В ЭТОЙ
> ФОРМЕ: чек без шапки считается заведённым, но всегда живым
> (fail-closed), и помечается пре-пассом как дефект формы (только при
> --require-all). Номера НЕ переиспользуются: новый чек — max+1;
> ретированный остаётся СТАБОМ со своим номером, именем сторожа и
> командой его живости. Чек, чей источник пре-пассу недоступен,
> объявляет pred:manual:<почему> и жив всегда — молчаливое угасание
> чека запрещено.
>
> ПРОГОН НАЧИНАЕТСЯ С ПРЕ-ПАССА:
> python tools/calibration_prepass.py --window-start <ts прошлого
> calibrated>. Его вывод целиком идёт в notes calibrated разделом
> «ПРЕ-ПАСС»; строки «ПУСТ» — обязательные явные отметки пустых окон.
> Пре-пасс решает ВСТРЕЧУ, НЕ смысл (D-0063). ЕСЛИ ПРЕ-ПАСС НЕ
> ОТРАБОТАЛ — чек-лист читается ЦЕЛИКОМ + строка в notes. Пропуск чека
> без строки причины — нарушение этого правила.

## Чек-лист (механизм → нарушение → как проверить)

Порядок прогона (= порядок секций, A2, Phase 5 W2a): §0 РЕТРО-КОНТУР
→ §1 ЖУРНАЛ (оба деплоя) → §2 GIT-ДИФФЫ (оба репо) → §3 ФАЙЛЫ И
ЯКОРЯ → §4 ТРАНСКРИПТЫ/cc_usage/ДЕНЬГИ (самый дорогой, один раз) →
§5 ВНЕШНИЙ ДЕПЛОЙ AO3 (целиком пропускается, если дерева нет).
Номера чеков ниже НЕ переставлены — только физическое положение
тела чека; все внешние ссылки по номерам остаются валидными
(A8.4).

### §0 РЕТРО-КОНТУР

0. **Закрытие предыдущего прогона (ретро-контур, 2026-07-13).**
<!--CHK 0|src:журнал,файлы|pred:always|rules:RC§3/D-0050|status:живой|since:2026-07-13-->
   Открыть notes прошлого события `calibrated` и находки того
   прогона: каждая закрыта механизмом/решением ЛИБО стоит в очереди
   с именованным триггером, и триггер ещё не выстрелил. Пункт
   очереди (CURRENT_CONTEXT + бэклог docs/RETRO_PATTERNS.md), чей
   триггер выстрелил, а работа не начата и не перерешена, = находка
   текущего прогона. Источники: журнал (calibrated), FINDINGS за
   окно, очередь CURRENT_CONTEXT, docs/RETRO_PATTERNS.md.

### §1 ЖУРНАЛ (оба деплоя)

3. **Правило 6 / D-0045 — эскалация.** Два `rejected` по одной
<!--CHK 3|src:журнал|pred:journal.event:rejected|rules:RC§1/R6|status:живой-->
   задаче на одном ярусе без последующего `escalated` = нарушение.
   `escalated` без следа `rejected` = вопрос к самодекларации.
   С D-0053 считается механически: события одной задачи связаны
   `task_id`, попытки — поле `attempt`; post-D-0053 событие из
   task_id-обязательного набора без task_id = нарушение само по себе.

6. **D-0037/D-0046/D-0058 — приёмка.** Каждый завершённый delegated
<!--CHK 6|src:журнал|pred:journal.event:accepted|rules:RC§6/D-0058|status:живой-->
   имеет accepted или rejected. Accepted по scout-диспатчам содержит
   отметку точечной сверки (негативные утверждения — обязательно).
   Приёмка сверху (D-0058): для каждого accepted сверить ярус
   принимающей сессии (cc_usage, main chain на момент события) с
   ярусом agent — принимающий строго выше: ок; равный/ниже законен
   ТОЛЬКО по одному из ЧЕТЫРЁХ оснований, иначе нарушение
   (самосертификация сессии, F-22):
   (i) вход яруса выше — `basis` "critic";
   (ii) пометка «в очередь полного Lead» — `basis` "queued-to-lead";
   (iii) `basis` "judge" на ЛИСТ-КЛАССОВОМ диспатче (R13/D-0087 —
   разведка либо реализация по написанной спеке). На механизме,
   правке политики или интеграционном целом judge-основание
   НЕЗАКОННО и остаётся нарушением — смотреть вместе с чеком 30;
   (iv) КЛАУЗУЛА НЕЗАВИСИМОГО КОНТЕКСТА (D-0099, добавлена
   2026-08-16 тем же ходом, что binding-aware чек 5): принимающий
   ЕСТЬ полный Lead — его ярус РАВЕН `roles.lead` деплоя — и
   принимает вывод critic/designer, произведённый в ОТДЕЛЬНОЙ
   фоновой сессии; на вершине лестницы независимость контекста
   замещает строгое превосходство модели. Такая строка законна БЕЗ
   basis, и требование basis к ней = ложное срабатывание.
   Основания (iii)/(iv) читаются по привязке ТОГО деплоя, чей журнал
   читается, а не по имени модели: у AO3_tests с 2026-08-15
   финальные приёмки идут `by=opus` без basis и несут поле
   `lead_binding` — по ИХ конфигу это (iv). Прежняя редакция знала
   только (i) и (ii); для окон ДО 2026-08-16 — поправка в
   PROCESS/CALIBRATION_HISTORY.md.
   Льгота «critic: skipped» легальна только у принимающего выше
   исполнителя; смотреть вместе с чеком 5 (окна деградации).
   Env-негатив: негативные утверждения О СРЕДЕ в отчётах/вердиктах
   окна (нет сервиса/команды/файла) валидны только с позитивной
   сверкой канонической формой (командная гигиена п.6; порт AO3
   fb41872).

13. **D-0052/D-0053 — доказательная приёмка (обе стороны), типизи-
    рованные поля.**
<!--CHK 13|src:журнал,журнал-AO3|pred:journal.any|rules:RC§1/D-0052,RC§2/D-0053|status:живой-->
<!--CHK 13(а)|src:журнал,журнал-AO3|pred:journal.event:accepted,agent=builder|rules:RC§1/D-0052|status:ретирован:2026-08-19;сторож:journal_validator;живость:python -m pytest tools/test_journal_validator.py -q-->
    (а) СТАБ (ретирован 2026-08-19, K1): witness-норма builder-
    приёмки держится сторожем journal_validator (:763-766, непустой
    `witness` для accepted+agent=builder); остаточный риск — строка,
    не дошедшая до коммита, гейта не встречает — второй слой ловит
    WITNESS-warn tools/journal_echo.py в момент записи строки
    (RULE_COVERAGE.md:39); 13(л) — дополнительный указатель именно
    для доковых витнессов, не адресат этого риска.
    Полнотекст — PROCESS/CALIBRATION_HISTORY.md.
<!--CHK 13(б)|src:журнал,журнал-AO3|pred:journal.event:defect_found|rules:RC§2/D-0053|status:живой-->
    (б) События `defect_found` ссылаются полем `ref` на task_id
    исходного accepted; false-accept rate по ярусам (defect_found /
    accepted за окно) считается и пишется в notes события
    `calibrated`; систематический false-accept по ярусу — evidence
    для движения статуса таблицы ВНИЗ (Update Rule 1).
<!--CHK 13(в)|src:журнал,журнал-AO3|pred:journal.any|rules:RC§2/D-0053|status:живой-->
    (в) `failure_class`-обязательность `rejected` — СТАБ K5
    (2026-08-19): сторож FAILURE_CLASSES (journal_validator.py:276);
    живость: python -m pytest tools/test_journal_validator.py -q;
    полнотекст — PROCESS/CALIBRATION_HISTORY.md. Поля —
    D-0053; на AO3 их отсутствие отсекает scripts/log_append.py, в
    OS-репо журнал пишется Edit-тулом и проверяется этим чеком.
    События до 2026-07-08 читаются вручную (append-only, не
    переписаны). Счёт чеков 3/13 и A4 исполняется скриптом
    tools/calibration_counts.py (t-040, 2026-07-11): скрипт печатает
    КАНДИДАТОВ, вердикты за калибрующим Lead (D-0063). Детектор
    отказа САМОГО счётчика: (а) тесты в каноническом прогоне suite;
    (б) baseline-кросс — числа первого ручного прогона зафиксированы
    в notes `calibrated` 2026-07-11 и воспроизведены скриптом; (в)
    при каждом прогоне Lead выборочно сверяет 1–2 счётчика с
    журналом вручную (слой 2); (г) дрейф схемы от journal_validator
    ловит тест синхронизации констант
    (test_schema_constants_match_journal_validator).
<!--CHK 13(г)|src:журнал|pred:journal.field:failure_class=spec|rules:RC§1/R11|status:живой-->
    (г) D-0054/D-0073: систематический `failure_class=spec` по ярусу
    — сигнал диспатчей без DoD; проверить последние спеки Lead на
    критерии приёмки + проверочный прогон И на манифест контекста
    (пишущий диспатч без «дано»/«owns» — утечка D-0073);
    систематический `failure_class=recon` у builder — сигнал
    недостаточной корзины «дано»; строки отчётов «понадобилось
    сверх манифеста» — прямая телеметрия того же (копятся →
    корзины Lead системно бедны).
<!--CHK 13(д)|src:журнал,журнал-AO3|pred:journal.any|rules:RC§2/D-0060|status:живой-->
    (д) Целостность task_id (D-0060): дублей task_id между
    несвязанными задачами нет — в ОБОИХ журналах (OS: t-NNN;
    AO3: описательные id). Этот же скан — residual-детектор
    TOCTOU-окна кодового гарда AO3 log_append.py (гард best-effort,
    read-then-append без лока — принято Lead 2026-07-09, t-009,
    critic finding F-A); известные поправки счёта —
    PROCESS/CALIBRATION_HISTORY.md.
<!--CHK 13(е)|src:журнал,git,cc_usage|pred:journal.any|rules:RC§2/F-29|status:живой-->
    (е) ts-честность (F-29): выборочная сверка ts событий окна с
    внешними часами — requests.db / cc_usage / git log (все три
    пишутся кодом); расхождение порядка часов или немонотонность
    внутри сессии = нарушение (правило — CLAUDE.md, журнальная
    секция; ts пишется с часов, не из повествования). Промоция
    в код — ts-sanity в объёме B1-валидатора (очередь).
<!--CHK 13(ж)|src:транскрипты|pred:manual:живость-хука|rules:RC§3/D-0103|status:живой-->
    (ж) Живость SessionStart-хука (t-027, критик B1): хук
    tools/session_context.py fail-open — сломавшись, он молчит
    (warning + exit 0), сессии продолжают жить без «реальности
    фоном». Проверка: в 1-2 транскриптах окна строка «NOW: ...
    (local system clock)» присутствует в начале сессии; её
    отсутствие = хук сломан/разрегистрирован = нарушение. Тот же
    чек ловит отказ preflight-правила запуска: НОВЫЙ
    rejected/failure_class=tooling с Groq/Gemini 429 при
    СУЩЕСТВУЮЩЕМ tools/preflight_quota.py = запуск мимо скрипта
    (правило запуска в gateway/PI_HARNESS.md) либо утечка его
    математики — разобрать, какой из двух. Дополнение F-37/D-0075
    (2026-07-12): MODEL-строка хука — ДЕКЛАРАЦИЯ harness-payload'а,
    не измерение; живость включает маркер «declared by harness, not
    measured» в строке (маркер пропал после правки хука = регресс
    F-37); сверку ФАКТА яруса делает чек 5 — этот подпункт проверяет
    только честность формулировки. Дополнение D-0103 (2026-08-18,
    авто-бут): на FRESH-START транскрипте окна (source=startup/clear)
    строка «AUTO-BOOT (D-0103…)» присутствует; её отсутствие при
    наличии «NOW:» = source-гейт или autoboot_lines сломаны / директива
    снята с main(). На compact/resume-транскрипте директивы быть НЕ
    должно (её появление там = гейт инвертирован). Логика source-гейта
    закрыта юниттестами tools/test_session_context_autoboot.py; этот
    подпункт — внешняя сверка, что живой хук реально её печатает.
    Дополнение W2c-1 (2026-08-19, перенос раскрытия из ядра CLAUDE.md
    по вердикту критика t-516): у journal_echo есть ФОЛБЭК-БАЗА
    (tool вне Edit/Write, кривой originalFile) — предупреждающие
    слои там молчат либо метят чужие строки, а фолбэк печатает
    собственный маркер «echo base: HEAD-diff fallback»; отсутствие
    warn при видимом маркере ≠ отсутствие нарушения, вердикты слоя
    в фолбэке читаются с этой поправкой. Сужение ИСПОЛНЕНО посадкой
    q503/N3 (2026-08-19): метка несёт код причины «(reason: <код>)»,
    TS DRIFT в фолбэке молчит (второй корректностно-ложный слой),
    TIER/WITNESS/ESCALATION остаются на кумулятивной базе.
<!--CHK 13(з)|src:скрипт:requests|pred:script:requests|rules:RC§1/R7|status:живой-->
    (з) traffic_kind-честность (D-0075): спот-скан requests за окно
    на сигнатуру немаркированного генератора — мульти-алиасная пачка
    в один тик (несколько моделей с одинаковым ts до миллисекунд)
    И/ИЛИ крошечные токены (total < ~100) с traffic_kind='real'.
    Найдено → найти генератор, добавить самотег (класс D-0075),
    ретро-коррекция строк решением Lead громко (id + обоснование);
    сама коррекция задним числом без записи в коммите/журнале — тоже
    нарушение этого чека. Образец сигнатуры — смок-пачка id 512–526
    (2026-07-12 22:19:05, ретегирована при принятии D-0075).
<!--CHK 13(и)|src:журнал|pred:journal.event:delegated|rules:RC§2/D-0060|status:живой-->
    (и) Фантомные диспатчи (D-0076/F-44): открытые delegated окна
    (последнее lifecycle-событие task_id — delegated) старше границы
    сессии, их породившей, = кандидаты-фантомы — сверить worker_ref
    с транскриптами/таск-листом (воркер существовал?); delegated без
    worker_ref после 2026-07-14 = нарушение (валидатор должен был
    отсечь — значит утёк мимо коммит-пути); спот-сверка 1–2
    worker_ref окна с реальным воркером (транскрипт сайдчейна,
    job-лог). Прецедент класса — инцидент AO3 2026-07-14 (F-44).
<!--CHK 13(к)|src:журнал|pred:journal.parallel_groups|rules:RC§1/R4|status:живой|since:2026-08-09-->
    (к) ОБЛАСТЬ ВИТНЕСС-ПРОГОНА ПАРАЛЛЕЛЬНОГО УЗЛА (норма R4,
    2026-08-09). Для каждой группы delegated окна, работавших
    ОДНОВРЕМЕННО (пересекающиеся окна delegated…accepted с разными
    worker_ref), проверяется, не требовал ли DoD ДВУХ И БОЛЕЕ
    воркеров группы ОДНОГО ПОЛНОГО прогона
    `python -m pytest tools/ gateway/ -q`. Требовал = spec-дефект
    ДИСПЕТЧЕРА (атрибуция чека 23), даже если в тот раз прогоны
    сошлись без конфликта. ПРИЗНАК В ДАННЫХ: поле witness ДВУХ И
    БОЛЕЕ ОДНОВРЕМЕННЫХ builder-приёмок несёт вывод ПОЛНОГО канона
    вместо суженного таргета. Полный канон в окне обязан быть РОВНО
    ОДИН — координаторский, ПОСЛЕ схождения ветвей, и он опознаётся
    пометкой «канон батча»/«BATCH CANON» в witness ПОСЛЕДНЕГО
    accepted батча (R4 назначила именно этот носитель); второй и
    далее полный прогон в witness той же группы = нарушение. ВТОРОЙ
    ПРИЗНАК: witness параллельного узла, НЕ покрывающий тестовые
    наборы ВСЕХ путей его owns (норма говорит «сужен ПО OWNS», а не
    «по своим файлам») — тот же класс spec-дефекта, даже если
    пропущенный путь не задет диффом узла. Пустое окно (нет
    параллельных delegated-групп) отмечается явно — «параллельных
    групп нет».
<!--CHK 13(л)|src:журнал|pred:journal.event:accepted,agent=builder|rules:RC§1/D-0052|status:живой-->
    (л) ВИТНЕСС ДОКОВОЙ ДИСПЕТЧЕРИЗАЦИИ (норма R11, 2026-08-10,
    вердикт критика t-407 по прецеденту B6 v0.8.0). Для каждой
    builder-приёмки окна, чей witness — НЕ pytest-прогон, а
    детерминированный скрипт присутствия ключей (owns —
    markdown/config без тест-сета): проверить три свойства нормы —
    (i) ключи процитированы из DoD, написанного ДО прогона; (ii)
    скрипт закоммичен тестом либо приложен полным исходником; (iii)
    прогон нёс негативный контроль (намеренно отсутствующий ключ →
    сообщённый провал). Любое отсутствующее свойство = находка
    (витнесс-пересказ, класс D-0052); скрипт-витнесс без негативного
    контроля — находка ВСЕГДА, даже при живых (i)-(ii). Пустое окно
    (доковых витнессов нет) — явная строка.

20. **Gate walk по defect_found (ретро-контур 2026-07-13).** На
<!--CHK 20|src:журнал|pred:journal.event:defect_found|rules:RC§1/D-0064|status:живой-->
    каждый defect_found окна: перечислить ворота, стоявшие на пути
    дефекта (witness, critic-вход, приёмка по следу, код-гейты), и
    назвать, какие промолчали и почему (утечка дисциплины / слепое
    пятно чека / новый класс). Промолчавшие ворота = находка и
    кандидат промоции в код (D-0063); строка разбора — в notes
    `calibrated`. Пустое окно (0 defect_found) отмечается явно.

21. **Стоимостной кроссовер эскалаций (Update Rule 4 таблицы,
    исполняемая).** По каждой эскалированной задаче окна (escalated +
<!--CHK 21|src:журнал,cc_usage|pred:journal.event:escalated|rules:RC§1/R6|status:живой-->
    цепочка task_id): фактическая полная стоимость всех попыток
    (сайдчейны попыток по cc_usage + Lead-оверхед приёмок) против
    оценки прямого диспатча на ярус, где задача принята.
    Систематический минус по категории (дешёвый ярус с ретраями
    дороже) = evidence для правки границы делегирования (строка
    таблицы / правило 6). Пустое окно (0 эскалаций) — явно.

22. **Повторяющиеся причины dispatch_skipped (ретро-контур
    2026-07-13).** Причины skip-событий окна (оба журнала)
<!--CHK 22|src:журнал,журнал-AO3|pred:journal.event:dispatch_skipped|rules:RC§1/D-0081|status:живой-->
    группируются; причина, повторившаяся >=3 раз, = сигнал: либо
    правило не соответствует практике (править политику решением),
    либо льгота стала лазейкой (находка). Дешёвый grep журналов.
    С D-0081 (2026-07-18) — детектор батчинга мелочей: счёт
    skip-класса «правка меньше диспатча» сравнивается с baseline 16
    (окно калибровки №2); отсутствие падения при введённом правиле =
    утечка (skip-причины обязаны называть БЛОКИРОВКУ хода); рядом
    считаются пакетные builder-диспатчи (маркер «батч мелочей» в
    notes) и их реджекты — экономика пакетов по факту; правило
    откатывается по данным, не по ощущению.
    РЕЦИДИВ ВНУТРИ СЕССИИ (2026-08-09, детектор нормы R8 «первый ход
    класса бесплатно», ретро от калибровки №6, чек 22 счёт 3):
    skip-события ОДНОЙ сессии группируются по классу причины В
    ПОРЯДКЕ ts; ВТОРОЕ и последующие события ОДНОГО класса в ОДНОЙ
    сессии = НАРУШЕНИЕ САМО ПО СЕБЕ, независимо от того, добрал ли
    класс порог >=3 ЗА ОКНО (существующий порог по окну — ОТДЕЛЬНЫЙ
    сигнал «правило не соответствует практике», им НЕ заменяется и
    НЕ переписывается). Класс-эталон — «оператор ждёт сейчас» /
    «интерактивный запрос блокирует ход» (окно №6, счёт 3). ГРАНИЦА
    СЕССИИ: арбитр — транскрипт сессии (`~/.claude/projects/**` /
    данные cc_usage), удешевление проверки — самодекларация: любое
    skip-событие, начиная СО ВТОРОГО в своём классе внутри сессии,
    ОБЯЗАНО нести в notes маркер повтора («повтор класса», «второй
    ход класса» — любая явная строка), делающий нарушение видимым
    грепом БЕЗ обращения к транскрипту. Оба слоя фиксируются: скан
    транскрипта — окончательный арбитр, маркер в notes — дешёвый
    первичный проход; отсутствие маркера у второго-и-далее
    skip-события класса в сессии = нарушение само по себе
    (самодекларация не сработала). Пустое окно (skip-событий нет ИЛИ
    ни один класс не повторился внутри одной сессии) отмечается
    явно — «рецидивов внутри сессии нет».

23. **Повторы находок critic → кодификация (ретро-контур
    2026-07-13; pattern capture Лопополо).** Вердикты critic окна:
<!--CHK 23|src:транскрипты|pred:manual:вердикты-критиков|rules:RC§1/D-0096|status:живой-->
    класс замечания, встретившийся >=2 раз (включая не дошедшие до
    дефекта), кодифицируется — строка в спеку/шаблон builder, ось
    карты или правило (D-0043, распространённый на ревью-замечания);
    повтор класса, кодифицированного ранее, = находка об утечке
    кодификации. С 2026-07-28 (слово оператора, разбор
    инсайт-отчёта харнесса) шаг ОБЯЗАТЕЛЕН и АТРИБУТИВЕН: каждый
    вердикт окна с ДОРАБОТАТЬ/blocker и каждый rejected (оба
    журнала + вердикт-блоки отчётов) относится к источнику —
    spec-дефект ДИСПЕТЧЕРА (пробел/противоречие спеки, неназванные
    данные корзины) | ошибка исполнителя | внешнее; причины
    кластеризуются, счёт кластеров — в notes прогона.
    Диспетчерская причина с >=2 повторами обязана тем же прогоном
    осесть пунктом/уточнением пятипунктового пре-диспатч чеклиста
    D-0096 (ядро R11; отдельного файла-чеклиста НЕТ — записанное
    решение против расходящегося дубля пары оси 4) — либо явной
    строкой «не кодифицируется: <почему>». Цель измерима: класс,
    осевший в чеклист, в следующем окне не даёт повторных
    доработок; его повтор = утечка кодификации (как и было).

30. **Leaf-routing D-0087 (механизм 2026-07-21).** Механизм: правило
<!--CHK 30|src:файлы,журнал|pred:always|rules:RC§1/R13|status:живой-->
    R13 ядра + "judge" в BASIS_VALUES валидатора + judge_accept-класс
    инструментов. Проверка окна:
<!--CHK 30(а)|src:файлы,журнал|pred:always|rules:RC§1/R13|status:живой-->
    (а) живость — R13 в ядре, judge-whitelist — СТАБ K3 (2026-08-19,
    JUDGE_BASIS_VALUE, живость: python -m pytest
    tools/test_journal_validator.py -q, PROCESS/CALIBRATION_HISTORY.md);
    judge-калибровка жива (13/13, D-0031); ОБЕ формы судьи: шлюзовой
    алиас И подписочный судья-субагент — промпт последнего ДОСЛОВНО
    равен JUDGE_SYSTEM_PROMPT из gateway/shadow_eval.py (дрейф =
    находка), точка эквивалентности t-254 (13/13) жива в журнале;
    (б)
    по-событийно: каждый accepted с basis "judge" — действительно
    лист-класс (recon/реализация по спеке; механизм/политика/
    интеграция под judge-basis = самосертификация класса F-22,
    находка); выборочная сверка судейских reject/accept-оснований на
    галлюцинацию (класс t-247: несуществующие символы/файлы в
    основании) — систематика → пересмотр judge-привязки решением;
    (в) экономика: суды окна ~копейки против снятой Lead-приёмки
    (Rule #1); рост доли failed-после-эскалации → находка
    конструкции; (г) сиблинги: порт-очереди кита (R13+judge_accept)
    и AO3-пункта не протухли.

31. **Durable-персистентность принятого деливерабла (F-50, механизм
    2026-07-22).** Класс «деливерабл-дрейф»: принятое по рабочему
<!--CHK 31|src:журнал,журнал-AO3|pred:journal.event:accepted,agent=builder|rules:RC§3/D-0050|status:живой-->
    дереву (witness реален) смыто широким checkout/reset до коммита —
    ни witness, ни критик персистентность не сверяют. Проверка окна:
    (а) по каждому accepted(builder) обоих журналов — несущая
    сущность witness (тест/функция/файл, названные в witness или
    notes) существует в ЗАКОММИЧЕННОМ дереве (git show/grep по HEAD;
    при сомнении git log -S по имени: коммит-носитель есть и не
    откачен). Витнесс реален + сущности нет в HEAD = дрейф — находка
    + `defect_found` (ref на исходный accepted); (б) живость вечерней
    стороны — шаг 2а session-handoff на месте (точечный lookup);
    систематические дрейфы при живом шаге 2а = утечка дисциплины,
    evidence промоции в код-гейт (D-0063); (в) спор «существовал ли
    артефакт» решается форензик-приёмом F-50 (runtime-свидетели вне
    git: pytest nodeids-кэш, .pyc mtime, треки/базы процессов).
    AO3-сторона закрыта их фиксом d4b612d (пост-коммит сверка в
    /qa-loop) — их экземпляр этот чек смотрит только на рецидив.

### §2 GIT-ДИФФЫ (оба репо)

2. **Правило 3 — critic на крупных диффах.** Коммиты периода
<!--CHK 2|src:git|pred:git.diff_lines:>100|rules:RC§1/R3|status:живой-->
   со >100 изменённых строк, принятые без delegated(critic) и без
   пометки «critic: skipped, <причина>» в accepted-событии.
   Источник: git log --stat + журнал. С 2026-07-16 тот же чек
   покрывает КРИТИК-НА-ПЛАН (правило 3, t-159 п.5): принятый
   recon-деливерабл, ставший спекой реализации дороже ~30 мин
   (признак: последующие delegated(builder) ссылаются на него как
   на спеку), без критик-входа на план до первого builder-диспатча
   и без skip-пометки = нарушение того же класса.

8. **Правило 10 — четыре вопроса.** Новые записи DECISIONS.md за период
<!--CHK 8|src:git|pred:git.any|rules:RC§1/R10|status:живой-->
   содержат ответы (а)/(б)/(в)/(г), причём (в)-ответ ЗАРЕГИСТРИРОВАН
   (D-0049): чек в этом протоколе тем же коммитом либо явный внешний
   детектор в тексте решения. Незарегистрированный детектор =
   механизм-пожелание, оформляется находкой. С D-0064: (г)-ответ —
   слой enforcement НАЗВАН: чем механизм триггерится и какой код
   стоит на пути исполнения, либо явная строка «на дисциплине» с
   названным детектором утечки; отсутствие (г)-строки или
   дисциплинный ответ без детектора = находка (класс F-24).
   С D-0065 — аудит РАСПОЗНАВАНИЯ (F-25): коммиты периода ОБОИХ
   репо просматриваются на механизмо-образные правки без rule-10
   блока и без решения (добавлена обязанность/машинная проверка вне
   DECISIONS = «нераспознанный механизм»); вердикт семантический —
   за калибрующим Lead (D-0063, слой 2), невод гейта даёт только
   кандидатов; найденное = находка + ретро-оформление четырёх
   вопросов. С D-0055: (б)-ответ —
   осевым ПЕРЕЧИСЛЕНИЕМ по текущей карте (проза «оси покрыты» = нет
   ответа); живость гейта — `git config core.hooksPath` равен
   .githooks в ОБОИХ репо; механизмные коммиты периода выборочно
   проверяются на осевой блок (обойти гейт можно было только
   --no-verify или строкой отказа); строки «оси: не-механизм» за
   период аудируются на честность — отказ на содержательном
   механизме = нарушение. С D-0072 (батч 5б): tier-строки
   механизменных коммитов периода сверяются с фактической моделью
   сессии по транскриптам (cc_usage, тот же эталон, что чек 5) —
   декларация lead-яруса сессией ниже lead = нарушение класса
   F-36/F-29 (гейт заставляет декларировать, истинность judge'ит
   этот чек); строки «оси: не-механизм» заодно аудируются как
   потенциальный обход tier-требования (машинная правка,
   переименованная в не-механизм, чтобы не декларировать ярус).

14. **D-0057 — golden set разведки и regression-правило правок
    агент-промптов.** (а) `git log` окна по `.claude/agents/*.md`
<!--CHK 14|src:git,git-AO3|pred:git.paths:.claude/agents/*.md@OS|rules:RC§5/D-0071|status:живой-->
    обоих деплоев: каждая правка role-файла яруса, имеющего набор
    (scout — PROCESS/SCOUT_GOLDEN_SET.md; critic —
    PROCESS/CRITIC_EXAM.md, D-0071), и каждая смена `model:` во
    frontmatter сопровождаются строкой прогона в Runs log
    соответствующего набора тем же коммитом; правка без прогона
    = нарушение. (б) Живость ключа: у >=2 вопросов набора прогнать
    verify-команды; уехавший ключ — баг eval'а, чинится ДО выводов
    о деградации scout (CORE-Bench, docs/RELATED_WORK.md «Evals»).
    (в) Рост failure_class=recon за окно (смотреть с чеком 3) без
    внепланового прогона набора -> назначить прогон. (г) Правки
    role-файлов ярусов БЕЗ набора (builder — н-п по решению
    D-0057: его выход проверяется witness-приёмкой на каждой
    задаче) отмечаются в notes события calibrated.
    (д) Guard фабрикации Pi-прогонов (t-017, D-0063 слой «код
    гарантирует встречу»): каждый экзамен/вступительный прогон окна
    через Pi-обвязку несёт guard-вердикт (строка Runs log /
    журнальное событие) ДО оценки по ключу; принятый или оценённый
    прогон без guard-строки = утечка дисциплинного триггера
    (evidence для продвижения в код-гейт, D-0063). Живость
    детектора: ретро-кейс t-016 (`python tools/pi_run_guard.py --db
    gateway/requests.db --model intern --since 2026-07-09T15:45:00
    --until 2026-07-09T15:46:00`) обязан давать REJECTED exit 1.
<!--CHK 14(е)|src:git|pred:git.paths:toolkit|rules:RC§7/D-0074|status:живой-->
    (е) Экзамен экономии деплоя (PROCESS/DEPLOYMENT_ECONOMY_EXAM.md,
    2026-07-13): релизный снимок тулкита за окно (ось 7, батчи
    D-0074) без строки БОЛЬШОГО прогона в Runs log экзамена =
    находка (релизная регрессия деплоя не прогнана). С 2026-07-15
    (слово оператора, два класса экзамена): калибровка сама ГОНЯЕТ
    МАЛЫЙ экзамен (задачи класса набора №1, Sonnet-координатор,
    раннер) и вносит его строку в Runs log — статистика копится
    точками калибровок; большой (многосессионный) экзамен —
    периодически, не чаще раза на релиз, НЕ на каждой калибровке.
<!--CHK 14(ж)|src:git|pred:git.paths:delegation.config.yaml,CLAUDE.md|rules:RC§2/D-0099|status:живой-->
    (ж) **Lead-экзамен v2 (PROCESS/LEAD_RANKING_EXAM.md, секция v2,
    2026-08-18).** У Lead-яруса НЕТ role-файла, поэтому подпункт (а)
    его не покрывает — отдельный триггер: `git log` окна по
    `delegation.config.yaml` (поля `roles.lead`/`roles.reserve`) и по
    названным Lead-секциям CLAUDE.md (Routing rules R11/R11a,
    «Role ≠ tier», «Lead degradation», матрица приёмки). Каждая такая
    правка сопровождается в Runs log v2 экзамена ЛИБО строкой прогона-
    наследника, ЛИБО строкой «наследник построен/в очереди с
    триггером» тем же коммитом; правка без строки = находка (набор
    одноразовый, сжигается после серии — «перепрогнать сожжённый»
    невозможно, триггер требует ПОСТРОИТЬ наследника по методу).
    Также: рост `failure_class=spec` на Lead-ярусе за окно (с чеком 3)
    без внепланового наследника → назначить. Живость: у >=1 элемента
    последнего набора прогнать перепин-команду ключа; уехавший ключ —
    баг инструмента, чинится ДО выводов о ярусе.

16. **D-0066 — внешняя разведка двухпроходная.** Новые секции
<!--CHK 16|src:git|pred:git.paths:docs/RELATED_WORK.md|rules:RC§1/R1|status:живой-->
    docs/RELATED_WORK.md за период (и записи очереди, ссылающиеся на
    внешние обзоры): каждая ли называет след второго прохода Lead —
    какие файлы внешнего репо Lead читал сам, помимо дайджеста
    scout. Секция/план только на дайджесте (след второго прохода
    отсутствует) = находка класса F-26; механизмы из такого плана
    замораживаются в очереди до дочитывания.

32. **Апгрейд-поставка дельтой-по-ревизии (D-0091, механизм
    2026-07-22).** Механизм: порт-батч к деплою шаблона собирается
<!--CHK 32|src:git|pred:git.paths:toolkit|rules:RC§7/D-0093|status:живой-->
    как дельта от записанной у деплоя снимок-ревизии кита;
    роль-файлы — диффуемый КОНТЕНТ поставки. Нарушение: батч ушёл,
    а новые/изменённые с той ревизии кит-механизмы не получили
    строк леджер-дельты в носителе-хэндоффе цели (класс F-52:
    роль-файлы невидимы поставке — слепое пятно Dog прожило 10
    дней). Проверка окна: по каждому порт-батчу окна
    (журнал/CURRENT_CONTEXT) — (а) записанная у деплоя
    снимок-ревизия существует; (б) дельта кита от неё (git diff по
    toolkit/) сопоставлена строкам леджер-дельты в носителе цели —
    непокрытая позиция дельты = находка; (в) принятые правки ролей
    у цели сопровождены их экзаменационным гейтом; (г) запечатанная
    поставка цепочки контроля (D-0093): позиция дельты, меняющая
    ИСПОЛНЯЕМЫЙ файл цепочки (git-хук, hook-скрипт), несёт полное
    целевое содержимое (не дельту-строку), приёмка батча несёт
    witness пробы живости (невалидный вход отклонён → откачен), и
    моды обязательных хуков цели в git-индексе — 100755 (F-53:
    носитель довёз опасность без страховки; 100644 = молча мёртвый
    гейт на Linux). Хост-сторона
    пары — чек 22 кит-копии протокола (полнота/дрейф леджера против
    шаблона).

### §3 ФАЙЛЫ И ЯКОРЯ

9. **D-0043/SIBLING_MAP — классовая полнота.** Коммиты, закрывающие
<!--CHK 9|src:файлы,git|pred:always|rules:RC§1/D-0043|status:живой-->
   findings, называют оси; строки «новая ось для SIBLING_MAP» из
   AO3 HANDOFF/журнала перенесены в карту.
   (б) ПОДМЕТАНИЕ ВНУТРИ АРТЕФАКТА (D-0100, 2026-08-09). Каждая
   находка окна, называющая КЛАСС (docs/FINDINGS.md + вердикты
   критика + notes журнальных событий), несёт перечисление соседей
   своего артефакта с вердиктом на каждого ЛИБО строку «соседей
   нет: <почему>». Отсутствие обеих форм = находка окна. Дешёвый
   первичный проход — греп маркера перечисления с позитивным
   контролем той же формы; окончательный арбитр — чтение самой
   записи. ДЕТЕКТОР ФОРМАЛЬНОСТИ (2026-08-09, по образцу чека 22):
   перечисление, где ВСЕ соседи получили вердикт «не применим» без
   указания, ЧЕМ сосед отличается от найденного экземпляра, =
   отписка и находка сама по себе. Пустое окно (находок с названным
   классом нет) отмечается явно.

10. **F-8 / D-0038 — рост живых файлов.** Число строк файлов
<!--CHK 10|src:файлы,файлы-AO3|pred:always|rules:RC§3/session_context|status:живой-->
    boot-пути ОБОИХ деплоев (OS: список BOOT.md + CLAUDE.md +
    CURRENT_CONTEXT.md; AO3: CLAUDE.md + docs/HANDOFF.md +
    docs/09-improvement-plan.md, в буте с 2026-07-17 — статусный
    [X]-срез, нарративы обязаны жить в docs/09-history.md — ось 1:
    автозагружаемый контекст пилота такой же платный) плюс
    docs/SIBLING_MAP.md (D-0048: карта обязана оставаться малой);
    сравнить с прошлым прогоном (числа — в notes события
    `calibrated`). Монотонный рост живого файла = нарушение D-0038
    (закрытое не архивируется).

12. **D-0048 — живость SIBLING_MAP.** Каждый конкретный путь,
<!--CHK 12|src:файлы|pred:always|rules:RC§1/R9|status:живой-->
    названный в карте, существует (обе стороны осей, оба репо);
    упомянутые правила/механизмы действуют. Мёртвый путь =
    нарушение same-commit-правила карты.
<!--CHK 12(а)|src:файлы|pred:always|rules:RC§1/R9|status:ретирован:2026-08-18;сторож:test_sibling_map_liveness;живость:python -m pytest tools/test_sibling_map_liveness.py -q-->
    (а) Целость нумерации осей
    (непрерывность 1..N живой карты) держит тест
    tools/test_sibling_map_liveness.py; ручная сверка заголовков не
    требуется. Рецидивы за период
    (дефект уже чинившегося класса вне указанных картой мест)
    оформлены как находки о карте, ось поправлена.
<!--CHK 12(б)|src:файлы|pred:always|rules:RC§1/R9|status:ретирован:2026-08-18;сторож:norm_retire(I3,I4);живость:python tools/norm_retire.py --check-->
    (б) Паритет записей
    D-XXXX индекс↔полнотекст (D-0051), включая ретированные стабы
    (Phase 5 W1), держит tools/test_norm_retire.py (инварианты
    I3/I4) на каноническом прогоне; ручной пересчёт не требуется —
    прогон читает вывод `python tools/norm_retire.py --check`.
<!--CHK 12(в)|src:файлы|pred:always|rules:RC§7/D-0074|status:живой-->
    (в) Ретирование как ПРОЦЕСС (исполнялось ли где было должно,
    полезна ли диета) — зона стоячего чека W3, не этого. Код-зеркало таблицы статусов
    (critic t-090): VALIDATED_DELEGABLE_CATEGORIES в
    gateway/metrics.py равно множеству категорий строк
    provisionally_validated/production_validated
    DELEGATION_TABLE.md; статусы двигает ТОЛЬКО калибровка
    (Update Rule 1), поэтому ЛЮБОЙ статусный ход текущего прогона
    обновляет frozenset тем же коммитом — расхождение на момент
    чека = нарушение. Срез карты осей у AO3 (их
    state/sibling-map.snapshot.md — третье звено их mechanism_gate
    для сред без D:\; регистрация — кросс-репо handoff
    gate-map-anchor-0723, исполнен калибровкой №4): номера осей
    среза сверяются с живой картой; отставший/ужатый срез =
    находка, обновление среза — тем же ходом (их файл под
    самозащитой их гейта: осевой блок + tier). ПОЛНОТА ЗЕРКАЛА
    ОСИ 7 (D-0101): прогнать `python tools/parity_check.py --check`
    и сверить файловые списки штаб↔toolkit (tools/, PROCESS/,
    gateway/, .claude/) — каждый hq-drift и каждый файл штаба без
    кит-твина ЛЕГАЛЕН только если его класс явно помечен «кухня»
    (kit-release, parity-инструментарий, живые конфиги, корпуса
    реального трафика, история штаба); прочий разрыв = НЕДОПОСТАВКА,
    находка калибровки — фильтр «обкатано у нас» отменён, обкатка у
    добровольцев.

24. **Свежесть навигационных документов (F-41; заказ оператора
    2026-07-13 — прецедент протухшего ROADMAP).** Прямой проход:
<!--CHK 24|src:файлы,файлы-AO3|pred:always|rules:RC§8/F-41|status:живой-->
    события окна (закрытия фаз/задач, решения, статусные ходы — из
    журнала и git log) отражены в документах-владельцах: ROADMAP
    (фазы/гейты), CURRENT_CONTEXT (живая задача/очередь),
    DECISIONS+FULL (решения), DELEGATION_TABLE (статусы), README
    (навигация), docs/RULE_COVERAGE.md (карта «правило → сторож»:
    новые механизмы/смены режимов окна отражены строками; с
    2026-07-28). ПРОХОД ВЛАДЕНИЯ (заведён 2026-08-05 по вопросу
    оператора; зарегистрированный детектор шага 5а скилла boot-diet):
    тот же перечень владельцев читается в ОБРАТНУЮ сторону — по
    блокам boot-файлов задаётся вопрос «у СВОЕГО ли владельца этот
    факт?». Блок, чей владелец другой документ, = находка того же
    класса F-41, даже если он свеж и не дублирован: он платится
    каждой сессией, а живёт не там. Отдельно проверяется, что итоги
    прогона калибровки лежат в notes события `calibrated`, а в
    CURRENT_CONTEXT осталось только НЕИСПОЛНЕННОЕ решение строкой на
    пункт (прецедент 2026-08-05: разбор 32 чеков был написан в журнал
    И продублирован прозой в очередь — 12059 Б, крупнейший пункт
    файла; шаги 2 и 5 диеты его не ловили, потому что он не был ни
    закрытым, ни дублем другого boot-файла). Обратный проход:
    несущие утверждения boot-пути
    («ждёт X», «следующее — Y», даты) выборочно сверяются с фактом;
    протухшее утверждение = находка класса F-41. Вердикт
    семантический — за калибрующим Lead (D-0063); еженедельной
    частоты достаточно (слово оператора). Отличие от чека 10: тот
    смотрит РАЗМЕР живых файлов, этот — СООТВЕТСТВИЕ содержимого
    фактам и ЕГО МЕСТО. ИСПОЛНЕНИЕ (промоция в scout по слову оператора
    2026-07-13, без ожидания триггера): оба прохода собирает
    scout-диспатч (обход документов = разведка, правило 1; DoD —
    построчный вердикт «свеж / кандидаты протухания» на каждый
    документ со следом, «протуханий нет» — валидный итог со следом);
    приёмка дайджеста — D-0046; вердикт «протухло/свежо» и находки —
    за калибрующим Lead. Внеплановый прогон чека легален в любой
    момент по слову оператора. С 2026-07-17 чек покрывает ОБА деплоя
    (рецидив класса F-41 на непокрытой стороне: AO3
    docs/04-roadmap.md содержательно стоял с 07-04 при действующем
    чеке — D-0048 п.2, находка о самом чеке; второй экземпляр того
    же класса — PROJECT.md/BUG-001). AO3-перечень
    документов-владельцев: docs/01 (test-strategist), docs/03,
    docs/05, docs/06, docs/07, docs/09 (ЕДИНЫЙ план развития со
    слияния 2026-07-17: Часть I — фазовая карта, бывший
    docs/04-roadmap, удалён; Часть II — этапы улучшений; контент
    ведёт Lead при закрытии этапов/смене фазы, приоритеты/скоуп
    пунктов — решения оператора), docs/environment-setup,
    README. Владелец по умолчанию для дока без пометки — Lead-сессия
    (вопрос оператора 2026-07-17: перечень без владельца = находки
    без того, кто чинит). Исключены: docs/HANDOFF.md
    (живость держит session-handoff + Session Start-детектор),
    генерируемые state/factory-status.md и state/coverage-map.md
    (свежесть держат генераторы + A4), docs/feature-registry.yaml
    (свой детектор протухания в coverage_map),
    app-under-test/PROJECT.md (чужой репозиторий, известно-мёртв —
    BUG-001, правило «истина = код»). Оба прохода AO3 — тем же
    scout-диспатчем (можно одним с OS-проходом); прецедент-эталон:
    прогон fresh-docs-ao3 2026-07-17 (журнал AO3; 4 протухания
    найдено и закрыто тем же заходом).

26. **Policy-as-code гейты штаба (порт t-159, активация 2026-07-16;
    hygiene_gate добавлен 2026-07-18, t-177/D-0080).**
<!--CHK 26|src:скрипт:hook_liveness_probe,файлы|pred:always|rules:RC§1/dispatch_gate,RC§4/hygiene_gate|status:живой|since:2026-07-16-->
    Зарегистрированный 10(в)-детектор отказа — ПО ФАКТУ ПОКРЫВАЕТ
    ЧАСТЬ из 12 command-хуков `.claude/settings.json` (состав файла —
    см. (а)), НЕ ВСЕ (переформулировано 2026-08-09, F-56, находка
    досылки B11 к t-367): обязанность прогона — для КАЖДОГО хука
    установить, назван ли он ГДЕ-НИБУДЬ в этом протоколе (в этом
    чеке или в любом другом) как предмет проверки отказа/живости —
    грепом имени скрипта по ВСЕМУ файлу протокола, с положительным
    контролем той же формы вызова на каждый нулевой результат.
    КРИТЕРИЙ ЗАСЧИТЫВАНИЯ (2026-08-09, N5; ред. 2026-08-19: после
    диеты W2c-2 сводки-дубликата в файле больше нет — нулевой
    результат грепа ДОСТИЖИМ и валиден при положительном контроле
    формы). Греп-попадание на имя гейта бывает ТРЁХ родов, засчитывается
    (полностью или частично — см. РЕЖИМ ОТКАЗА ниже) только средний:
    (1) ПРОСТО УПОМЯНУТ — имя встретилось в прозе или в снимке у
    владельца (docs/RULE_COVERAGE.md) — НЕ
    засчитывается; (2) НАЗВАН ПРЕДМЕТОМ ПРОВЕРКИ
    ЕГО ОТКАЗА/ЖИВОСТИ — подпункт (или другой чек протокола)
    прескрайбит: что именно проверяется У ЭТОГО гейта и что считается
    его отказом, ЛИБО задаёт механизм, который по факту данных
    прогона однозначно резолвится в проверку именно этого гейта
    (резолюция по данным — законная форма опознания предмета, пример
    — (в) ниже: «событие gate_log с ключом gate» относится к
    КОНКРЕТНОМУ гейту, чьё имя стоит в поле `gate` самой записи, даже
    если текст подпункта не называет его буквенной строкой).
    РЕЖИМ ОТКАЗА, НЕ ФОРМА УПОМИНАНИЯ (2026-08-09, находка критика
    по (в) БЛОКИ — тот же класс, что уже назван прецедентом в (б) выше:
    «fail-open проглотил отказ БЕЗ СЛЕДА, снимок ... потерян молча» —
    второй экземпляр класса, не новый): категория (2) засчитывается
    ПОЛНОСТЬЮ только если предписанная проверка способна обнаружить
    МОЛЧАНИЕ гейта (его тихую неработу, ноль срабатываний), А НЕ
    ТОЛЬКО его ошибочное срабатывание — проверка, которая сверяет
    ЛИШЬ СУЩЕСТВУЮЩИЕ записи/события (без утверждения вида «в окне с
    такими-то фактами запись ОБЯЗАНА существовать») не отличает
    «нарушений не было» от «гейт мёртв, записей нет вообще» и
    засчитывается лишь ЧАСТИЧНО (группа «ПОКРЫТ ЧАСТИЧНО» снимка у
    владельца; не путать с (1)); (3) НАЗВАН КАК ИНСТРУМЕНТ ЧУЖОЙ ПРОВЕРКИ — гейт
    упомянут потому, что от него ОДАЛЖИВАЕТСЯ что-то (файл, строка,
    якорь) для проверки ДРУГОГО предмета (пример: dod_gate как
    носитель строки BLOCK_MESSAGE в чеке 29 — предмет ТОЙ проверки —
    полнота финал-сообщений воркеров, а не отказ dod_gate САМОГО) —
    НЕ засчитывается. Источник истины для категории (2) на каждом
    прогоне — САМИ подпункты (б)/(в)/(г) ниже и тексты других чеков,
    ПЕРЕЧИТАННЫЕ ЗАНОВО, а не снимок применения у владельца
    (docs/RULE_COVERAGE.md, «Справочник упомянутых чеков», запись
    «чек 26»): снимок может протухнуть — тогда
    прогон пересчитывает категорию (2) по факту текста.
    Трёхгрупповой снимок применения критерия на 2026-08-09 (ПОКРЫТ /
    ПОКРЫТ ЧАСТИЧНО / НЕ ПОКРЫТ по каждому из проверяемых гейтов) —
    docs/RULE_COVERAGE.md, «Справочник упомянутых чеков», запись
    «чек 26» (навигация, не источник истины — см. критерий выше).
    (а) ПОСТАНОВКА (переформулирована 2026-08-09, F-56 — перечисление
    протухло: жёсткий список «шесть хуков» разошёлся с фактом
    ДВЕНАДЦАТИ command-хуков живого файла) — обязанность чека: сверить
    состав `.claude/settings.json` ПО ФАКТУ ФАЙЛА НА МОМЕНТ ПРОГОНА
    (все точки — SessionStart/PreToolUse/PostToolUse/SubagentStop/
    Stop, полный список `command`-строк), а НЕ по тексту протокола;
    расхождение перечисления протокола с фактом = находка САМА ПО
    СЕБЕ, даже если новый/пропавший гейт не задет остальными
    подпунктами. СВЯЗЬ С F-56 (невод механизмов): множество
    command-хуков `.claude/settings.json` обязано покрываться и этой
    проверкой живости, И неводом `MECHANISM_PREFIXES` в
    `tools/mechanism_gate.py` — невод держит и сам путь
    `.claude/settings.json` ЦЕЛИКОМ, и (с 2026-08-10, ремедиация
    F-56) поимённо все tools/*.py-скрипты хуков; прежнее состояние
    «скрипты вне невода» (снимок 2026-08-09: правка скрипта БЕЗ
    правки settings.json проходит мимо осевого блока) —
    PROCESS/CALIBRATION_HISTORY.md; расхождение
    ЛЮБОГО из двух покрытий (эта сверка / невод) с фактическим составом
    файла = находка; связь фиксируется, чтобы оба слоя не
    промахивались по одному множеству независимо друг от друга. Ноль
    расхождений (состав по обоим покрытиям совпал с фактом) отмечается
    явно — «состав сходится». ПО КЛАССАМ ВЫВОДА
    hygiene_gate: блокирующий класс «журнал мимо Edit/Write»
    permissionDecision НЕСЁТ ПО ЗАМЫСЛУ — его ИСЧЕЗНОВЕНИЕ есть
    регресс; все ОСТАЛЬНЫЕ классы хука — warn-only, появление
    permissionDecision у любого из них = регресс в обратную сторону
    (тихая эскалация предупреждения в блок). Живость
    hygiene_gate сверяется ЧЕКОМ 25 ПО ЕГО ФАКТИЧЕСКОМУ СКОУПУ НА
    МОМЕНТ ПРОГОНА (скоуп расширяется решением по evidence, D-0063,
    и тогда же расширяется эта сверка): пример дрейфа скоупа:
    v1-триггер t-177 ловил только класс «журнал мимо Edit/Write».
    Правило сверки при любой редакции скоупа одно: находки аудита
    класса, ВХОДЯЩЕГО в текущий скоуп хука, при ПУСТЫХ warn-строках
    хука в тех же транскриптах = хук молчит/слетел, отказ
    постановки; (б) ЖИВОСТЬ — при живых
    диспатчах окна .claude/dod_track/<session>.json непусты и
    critic_snapshot.json обновлялся; молчание ВСЕХ гейтов при живых
    диспатчах = отказ постановки (хук слетел/переименован), не
    «нарушений не было». ПОГЕЙТОВАЯ ФОРМА (заведена калибровкой №6,
    2026-08-05, по собственной находке — прежняя редакция ловила
    только молчание ВСЕХ и этот случай пропускала): молчание ОДНОГО
    гейта при живых остальных — тоже отказ, и ищется адресно.
    Механическая сверка, дешёвая: ts последней записи
    critic_snapshot.json против ВРЕМЕНИ последнего критик-диспатча
    окна с subagent_type=critic (журнал + сайдчейны cc_usage,
    поправка UTC→local); снимок старше диспатча = хук не отработал.
    Прецедент-эталон: 2026-08-04 16:32 критик-диспатч t-350 при
    снимке от 07-30 01:49 — fail-open проглотил отказ БЕЗ СЛЕДА,
    снимок ревью крупнейшего диффа окна потерян молча. Ту же форму
    применять к любому гейту, чей след — файл с меткой времени;
    (в) БЛОКИ — события gate_log
    (blocked/skipped_after_2_blocks, ключ gate) сверяются с фактами
    сессий: ложный блок = находка о гейте (экзамены №9–№10б: 0
    ложных блоков / 4 прогона — baseline), скип-предохранитель без
    последующего зелёного прогона = находка о сессии;
    (г) ЭХО-БАЗА journal_echo (t-277/t-279, ось 10 карты) — детектор
    деградации payload-scoped базы: строки «echo base: HEAD-diff
    fallback» в транскриптах окна. Единичные — легитимный fail-open
    (не-хвостовая правка, no-op); РЕГУЛЯРНЫЕ на рутинных
    журнальных аппендах = originalFile исчез из payload харнесса
    (обновление Claude Code сменило схему) — хук тихо вернулся на
    кумулятивную базу, класс t-277 воскрес: находка + фикс-диспатч.
    Live-baseline 2026-07-22: originalFile в рантайме подтверждён
    (запись приёмки t-279 при незакоммиченных строках >45 мин —
    ноль TS DRIFT, ноль fallback-меток).
    (д) ПРОБА ЖИВОСТИ КОДА хук-гейтов (D-0100-семья, заведена
    2026-08-10, t-387; закрывает находку правила 10(в) этого чека —
    пять гейтов без детектора и три с детектором без утверждения
    присутствия): прогнать `python tools/hook_liveness_probe.py` из
    корня и приложить вывод ДОСЛОВНО. Проба подаёт каждому из
    command-хуков `.claude/settings.json` заведомо триггерящий
    stdin-payload в изолированном временном дереве (плюс доп. кейс
    owns_gate на OVERLAP — 13 кейсов) и утверждает ожидаемый след:
    печатный ответ с якорем и объявленным exit-кодом либо
    файл-артефакт для гейтов, молчащих по замыслу. НЕЗАВИСИМЫЙ
    ФАКТ-ТРИГГЕР — сам прогон: след ОБЯЗАН быть, молчание гейта
    отличимо от «нарушений не было». Любой вердикт кроме OK
    (DEAD/MISMATCH/CRASH/HUNG/MISSING/CASE-MISSING/STALE-CASE/
    LIVE-STATE-TOUCHED/SETTINGS-UNREADABLE) — находка постановки
    адресно по имени гейта. Отсутствие приложенного вывода = чек НЕ
    выполнен. АТРИБУЦИЯ ЖИВОГО СОСТОЯНИЯ (посадка q503/N2,
    2026-08-19, снята прежняя инструкция «повторить прогон»): дифф
    живого состояния с маркером liveness-probe- (имя или содержимое)
    = утечка изоляции, LIVE-STATE-TOUCHED, FAIL и внутри сессии;
    дифф БЕЗ маркера = записи окружающей сессии, отдельный вердикт
    LIVE-STATE-AMBIENT, на exit code не влияет — ложного FAIL от
    собственных PostToolUse-хуков больше нет (прежний текст флейка —
    PROCESS/CALIBRATION_HISTORY.md).
    ОГРАНИЧЕНИЕ явной строкой: проба доказывает живость КОДА гейта,
    НЕ живость ПРОВОДКИ (регистрация в settings.json, matcher,
    core.hooksPath) — проводка остаётся за (а) и
    tools/wiring_check.py; засчитывать (а) через (д) запрещено.

29. **Полнота финал-сообщений воркеров (F-49, механизм 2026-07-21).**
<!--CHK 29|src:журнал,cc_usage|pred:always|rules:RC§5/F-49|status:живой-->
    Механизм: правило «финальное сообщение = полный отчёт/дайджест/
    вердикт целиком, при пересдаче — заново» в role-файлах
    (.claude/agents/builder.md п.8, scout.md п.9, critic.md п.15) +
    подсказка пересдачи в BLOCK_MESSAGE tools/dod_gate.py (подстрока
    «финальный отчёт ЦЕЛИКОМ»). Проверка окна: (а) живость — 4 якоря
    на месте (точечный lookup); (б) отказ — события окна с пометкой
    дозапроса содержимого («повтори отчёт», «дозапрошен», «недоставка
    предыдущих сообщений») в notes журнала И/ИЛИ сайдчейн cc_usage с
    крупным объёмом работы, чьё финальное сообщение пустое/усечено
    (класс «пустые финал-сообщения ~200K+», очередь №4 07-19) =
    утечка правила — находка; рост против baseline 5 прецедентов/день
    (07-20: t-225, t-226, t-231, t-234) → пересмотр формы механизма
    (кандидат: код-слой на SubagentStop), падение к ~0 — норма;
    (в) сиблинги — кит (ось 7, порт-очередь) и AO3-агенты (ось 1/3,
    их носитель) закрыты или стоят в очереди с триггером.

34. **Рост нормативного корпуса — раскладка, диеты, пороги (Phase 5
    W3, слово Архитектора 2026-08-18).**
<!--CHK 34|src:скрипт:corpus_growth,git|pred:script:corpus_growth|rules:RC§1/corpus_growth|status:живой|since:2026-08-19-->
    Вход чека — вывод машинного слоя: `python tools/corpus_growth.py`
    (сайдкар logs/corpus_growth.jsonl хранит замеры; дельты — к
    предыдущей записи). Предмет — НЕ-бут корпус: DECISIONS_FULL,
    FINDINGS, этот чек-лист, POLICY_FULL, SIBLING_MAP, RULE_COVERAGE,
    индекс DECISIONS.md, OPERATIONAL_NOTES, RETRO_PATTERNS и оба
    архива; единица — БАЙТ НА ЗАПИСЬ. Бут-путь и CLAUDE.md держат
    чек 10 (строки) и бут-бюджет SessionStart (байты, храповик
    CLAUDE_WARN/BREACH) — их числа здесь не переписываются, CLAUDE.md
    печатается зеркалом без собственного порога. ОТСУТСТВИЕ записи
    сайдкара за окно = машинный слой не прогонялся = чек НЕ ВЫПОЛНЕН
    (а не «нет данных»).
<!--CHK 34(а)|src:git|pred:git.paths:docs/*.md,CLAUDE.md,DECISIONS.md,PROCESS/*.md|rules:RC§1/corpus_growth|status:живой|since:2026-08-19-->
    (а) ТЕМАТИЧЕСКАЯ РАСКЛАДКА ДЕРЖИТСЯ. По диффам корпусных
    артефактов окна: каждая НОВАЯ запись легла в реестровую форму
    своего носителя и РЯДОМ СО СХОЖИМ (класс/ось/секция), а не прозой
    в хвост файла. Нарушения: запись без типизированных полей
    носителя; второй дом для существующего класса; правило, осевшее
    амендментом внутри чужого правила вместо своей строки реестра.
    Пустое окно — строка «н-п: корпусные артефакты в окне не
    менялись» С ПОЗИТИВНЫМ КОНТРОЛЕМ ТОЙ ЖЕ ФОРМЫ (тот же git log по
    тем же путям за более широкое окно показывает известный коммит);
    молчание неотличимо от пропуска.
<!--CHK 34(б)|src:git,журнал,файлы|pred:git.any|rules:RC§1/corpus_growth|status:живой|since:2026-08-19-->
    (б) ДИЕТЫ ИСПОЛНЯЮТСЯ И ПОЛЕЗНЫ — четыре вопроса подряд.
    (1) ПРИМЕНЯЛИСЬ ГДЕ ДОЛЖНЫ: запись окна, чей урок стал правилом с
    НАЗВАННЫМ детектором, ретирована стабом либо несёт явную строку
    «диета не нужна: <почему>»; машинная половина — `python
    tools/norm_retire.py --check` (вывод в notes).
    (2) НОРМЫ НЕ ПОТЕРЯНЫ: каждый ретированный полнотекст оставил
    индексную строку с указателем на архивный дом (инварианты I1/I2
    того же прогона); ноль стабов при живом архиве = находка.
    (3) СТАТУСЫ НЕ ПРОТУХЛИ: каждый пункт очереди и каждая
    статус-строка записи, чья работа закрыта в окне, сняты или
    обновлены. Процедура: взять из журнала окна все `accepted` и
    токены закрытия и сверить с носителями очередей —
    CURRENT_CONTEXT.md, статусные строки docs/FINDINGS.md и индекса
    DECISIONS.md, docs/tasks/*; статус читать ЧЕРЕЗ статусную строку
    записи, а не по заголовку в окне чтения (F-55). ЖИВОЙ МАТЕРИАЛ
    ПОСАДКИ — три экземпляра класса F-41 за сутки 2026-08-19: хвост
    Phase 4 «SQL LIKE exam_runner» пережил закрытие t-449 (08-16) и
    диету 08-18, вскрыт повторным диспатчем t-501 («ДУБЛИКАТ»);
    статусы F-57 и F-59 стояли «Открыта» при исполненной в дереве
    ремедиации (доклад свежести t-502). Ноль найденных протуханий при
    непустом окне закрытий записывается явно, с числом сверенных.
    (4) ПОЛЬЗА: диета, формально исполненная, но не снизившая
    стоимость чтения (числа машинного слоя не упали), — находка
    ПРОТИВ диеты (Rule #1), а не успех. Обратное верно по рамке
    Архитектора: снятие байт ценой исполнимости правила без второго
    хопа — находка против среза, а не экономия.
<!--CHK 34(в)|src:скрипт:corpus_growth|pred:script:corpus_growth|rules:RC§1/corpus_growth|status:живой|since:2026-08-19-->
    (в) ФАЙЛЫ НЕ РАЗРОСЛИСЬ ПРОТИВ СОБСТВЕННЫХ ПРАВИЛ ВЕДЕНИЯ. Числа
    машинного слоя против порогов: каждый WARN/BREACH объясняется ИЛИ
    становится находкой; байт/запись, выросший при неизменном либо
    уменьшившемся числе записей, — рост ВГЛУБЬ (диагноз фазы),
    объяснение обязательно. Артефакт без назначенного порога
    («порогов назначено k/N») — сам по себе находка: класс «растёт
    без владельца числа». В notes события `calibrated`, рядом со
    счётчиками чека 10: сумма корпуса, дельта к прошлому замеру, все
    WARN/BREACH и три худших артефакта по байт/запись. ОТКАЗ САМОГО
    ИНСТРУМЕНТА ловится здесь же: строки ДЕФЕКТ его вывода (артефакт
    MISSING; ноль записей при непустом файле — регекс протух; сайдкар
    не записан) и `python tools/corpus_growth.py --check` в
    каноническом прогоне; тихий ноль записей не принимается как
    «записей нет».

### §4 ТРАНСКРИПТЫ/cc_usage/ДЕНЬГИ

1. **Правила 1/2/8 — молчаливое самоизъятие.** Main-сессии в
<!--CHK 1|src:cc_usage,журнал|pred:manual:cc_usage|rules:RC§1/R1,RC§1/R8|status:живой-->
   cc_usage с крупным объёмом чтения/правок, у которых за тот же
   период нет ни delegated, ни dispatch_skipped. Источник:
   usage_report + журнал.
<!--CHK 1(б)|src:журнал|pred:journal.event:delegated|rules:RC§1/R2|status:живой-->
   (б) ДИСПЕТЧЕРСКИЙ ПОРОГ ДИЗАЙНЕРА (заведён 2026-08-05, слово
   оператора; детектор нормы R2 «драфт → designer по умолчанию»):
   каждый ПИШУЩИЙ delegated окна, чья спека несёт ≥3 нумерованных
   пункта ИЛИ трогает ≥3 файла (считается по owns-строке диспатча и
   по диффу принятой работы), обязан иметь ЛИБО парный
   delegated(designer) до себя, ЛИБО dispatch_skipped(agent=designer)
   с названной причиной. Ни того, ни другого = молчаливое поглощение
   функции координатором — нарушение того же класса F-9, что и
   самочтение вместо scout. Мера рядом (для тренда, не нарушение):
   число designer-диспатчей окна против числа пишущих диспатчей выше
   порога — baseline замера калибровки №6: 5 designer-диспатчей ЗА
   ВСЮ ИСТОРИЮ против 480 builder / 340 scout, из них с фактически
   загруженным роль-файлом — ОДИН (t-345; четыре пилотных шли
   general-purpose, роль-файл создан позже, см. строку evidence
   DELEGATION_TABLE). Рост доли — цель нормы; отсутствие роста при
   живой норме = утечка, находка.
<!--CHK 1(в)|src:журнал|pred:journal.event:rejected|rules:RC§1/R2|status:живой-->
   (в) ИСКЛЮЧЕНИЕ «ПЕРЕСДАЧА = ПРОДОЛЖЕНИЕ СПЕКИ» (норма R2, слово
   оператора 2026-08-09) — детектор отказа: `delegated` с
   `attempt`>=2 под ТЕМ ЖЕ `task_id` после `rejected` — продолжение
   уже написанной спеки; designer-диспатч и
   `dispatch_skipped(agent=designer)` для такого `delegated` НЕ
   требуются, порог (б) на него не проецируется. УТЕЧКА ИСКЛЮЧЕНИЯ:
   переоформление продолжения под НОВЫМ `task_id` — задача окна, чей
   предмет и набор owns совпадают с только что отклонённой работой
   (соседние ts, тот же владелец путей), без парного
   `delegated(designer)` и без `dispatch_skipped(agent=designer)` =
   утечка исключения — нарушение (норма обойдена сменой task_id).
   ОБРАТНАЯ УТЕЧКА: skip-событие с причиной класса «пересдача» датой
   после 2026-08-09 значит, что норма не понята — либо это была
   новая задача (нужен designer-диспатч, событие подано неверно),
   либо событие лишнее (норма уже освобождает пересдачу без
   событий); любой из двух случаев = находка. Пустое окно (нет
   rejected-пересдач под порогом (б), нет переоформлений под смежным
   task_id, нет skip-событий класса «пересдача») отмечается явно —
   «пересдач нет».

4. **D-0042 — самодекларация модели.** Поле model в
<!--CHK 4|src:cc_usage,журнал|pred:journal.event:delegated|rules:RC§1/D-0083|status:живой-->
   delegated/escalated/accepted/rejected против фактических моделей
   исполнения в cc_usage; расхождение — само по себе событие
   калибровки. ТИП наравне с моделью (2026-08-09, разрыв калибровки
   №6, t-348): тем же проходом сверяется поле `agent` журнальной
   строки против ФАКТИЧЕСКОГО `subagent_type` транскрипта воркера;
   совпадение model при расхождении agent/subagent_type (прецедент
   t-348: agent=critic записан, реальный subagent_type=general-purpose
   при model=opus-5) значит, что роль-файл яруса НЕ ГРУЗИЛСЯ, а
   замер D-0083 молчал ЗАКОННО (модель совпала) — нарушение того же
   класса, что расхождение модели. Ручная сверка по транскриптам
   ОСТАЁТСЯ ОСНОВОЙ чека В ОБОИХ МИРАХ — до и после появления
   машинного слоя; PreToolUse WARN-слой `tools/dispatch_gate.py`
   (role-type warn) — ДОПОЛНИТЕЛЬНЫЙ, не замена: формулировка
   остаётся верной, если этот слой выключен, снят или ещё не заведён
   в клоне-цели. Пустое окно (нет расхождений agent/subagent_type за
   период) отмечается явно — «расхождений типа нет».

5. **D-0039/D-0044/D-0056 — деградация.** Каждое `lead_restored`
<!--CHK 5|src:транскрипты,файлы|pred:manual:транскрипты|rules:RC§6/D-0056|status:живой-->
   содержит в notes разбор окна (что ревьюлось, вердикт; пустое окно
   отмечено). МУЛЬТИРЕПО-ОКНО (калибровка №2, прецедент d951844):
   «диффы окна» = коммиты ВСЕХ репо, которых касалась сессия, не
   только домашнего — restored, перечисляющий только домашние
   коммиты при кросс-репо диффах окна (git log обоих репо по
   границам окна), = неполная приёмка, находка. `lead_degraded` без парного `lead_restored` к границе
   сессии — сверить с последним событием журнала. С D-0056 (F-21):
   фактические модели Lead-сессий периода (main-цепочки cc_usage,
   НЕ сайдчейны) против покрытия окон парами
   lead_degraded/lead_restored обоих журналов. ГРАНИЦА ДЕГРАДАЦИИ —
   ПРИВЯЗКА, А НЕ ИМЯ МОДЕЛИ (binding-aware формулировка 2026-08-16
   по кросс-пункту AO3_tests 08-15; прежняя редакция —
   PROCESS/CALIBRATION_HISTORY.md):
   Lead-активность на модели НИЖЕ `roles.lead` ТОГО деплоя, чей
   журнал читается (его delegation.config.yaml; при спорной строке —
   привязка на МОМЕНТ СОБЫТИЯ, `git log delegation.config.yaml`, а не
   на момент прогона), вне заявленного окна = молчаливая деградация.
   Модель, РАВНАЯ привязке, — штатный полный Lead, не деградация;
   модель ВЫШЕ привязки (`roles.reserve`) — резерв по слову
   оператора, тоже не деградация и окна не требует. Кросс-чтение:
   у AO3_tests с 2026-08-15 roles.lead = claude-opus-5,
   roles.reserve = claude-fable-5 — их Opus-сессии штатны, метить их
   деградацией = ложное срабатывание. Далее без изменений: это отказ
   обеих in-session точек п.4 CLAUDE.md, вкл. сессию, умершую
   деградированной (расширение чека 4 / D-0042 с воркеров на Lead;
   ретроактивные пары ищутся по пометке в notes).
   ДЕТЕКТОР ОТКАЗА САМОЙ ЭТОЙ ФОРМУЛИРОВКИ (заведён тем же ходом,
   правило 10в — иначе binding-aware правка есть пожелание): каждый
   прогон — греп по ЭТОМУ файлу `-i -E "fable|opus|sonnet|haiku"`,
   каждое попадание классифицируется вслух как ИСТОРИЯ (описывает
   прошлый факт, прецедент, дату, цену baseline) или как КРИТЕРИЙ
   (задаёт проверку на будущее). Хоть одно попадание класса КРИТЕРИЙ
   = чек прибит к имени модели и переживёт следующую перепривязку
   молча — находка. Стоимость: один греп; платит прогон калибровки.
   Основание цены: собственная перепривязка D-0099 (2026-08-04)
   прожила в этом файле 12 дней незамеченной, и нашли её не мы, а
   встречный деплой.

7. **Журнал ↔ транскрипты, обе стороны.** Сайдчейн в cc_usage без
<!--CHK 7|src:cc_usage,журнал|pred:manual:cc_usage|rules:RC§1/R8|status:живой-->
   delegated-события = незадокументированный диспатч; delegated без
   сайдчейна = фантомное событие.

11. **Rule #1 к самой машинерии (вопрос 10а ретроспективно).**
<!--CHK 11|src:cc_usage,журнал,скрипт:session_context|pred:always|rules:RC§3/session_context|status:живой-->
    Накладные расходы роутинга по данным, а не по оценкам: доля
    Lead-tier токенов, ушедшая на журнал/приёмки/сверки/спеки,
    против стоимости делегированной работы (cc_usage: main-сессия
    vs сайдчейны за период). Если оверхед механизма систематически
    сопоставим с работой, которую он маршрутизирует, — это evidence
    для упрощения механизма (решением, не молчаливым забиванием).
    Сюда же — детектор D-0040 (зарегистрирован ретро-свипом
    2026-07-11, закрыл единственный найденный D-0049-разрыв):
    синхронные диспатчи окна выборочно сверяются с обоснованием в
    notes delegated-события (правило 7: синхронно только если
    следующий шаг зависит от результата И другой работы нет);
    sync-запуск без обоснования = находка. Сюда же — размер вывода
    SessionStart-хука (заказ оператора 2026-07-11, Rule #1 к
    «реальности фоном»): `echo '{}' | python tools/session_context.py`
    через wc -c / wc -l, оба числа в notes события `calibrated`
    рядом со счётчиками чека 10 (baseline 2026-07-11: 528 байт /
    11 строк); монотонный рост от прогона к прогону без решения =
    evidence для упрощения — тот же класс, что оверхед выше.
    11(в) ДОЛЯ СРАБАТЫВАНИЙ КАЖДОГО WARN-СЛОЯ (расширение чека с
    размера вывода ОДНОГО хука на нагрузку ВСЕХ, решение по F-54
    2026-08-16). По каждому живому warn-слою (NOTES LEN, TIER,
    WITNESS, TS DRIFT, ESCALATION, given-path, owns) — какую ДОЛЮ
    своего потока он пометил за окно. Слой, стабильно метящий десятки
    процентов, = находка: не «строгая настройка», а дефект механизма,
    потому что сторож такой плотности воспитывает привычку себя
    игнорировать, и дальше молчит уже не он, а читатель. Это тот же
    класс «элементы корректны, дефект в агрегате», что F-54, и та же
    порода, что монотонный рост вывода в 11(а). Порог доли не
    назначается заранее — он предмет суждения прогона; назначается
    ОБЯЗАННОСТЬ назвать долю числом.
    11(б) ДЛИНА NOTES ЖУРНАЛА И ДЕТЕКТОР ОТКАЗА ЕЁ СЛОЯ (заведено
    2026-08-16, механизм t-447 — норма бюджета в ядре + WARN-слой
    NOTES LEN в tools/journal_echo.py). Каждый прогон: медиана и p90
    длины `notes` событий окна, доля событий сверх бюджета (800 для
    диспатчевого цикла), в notes события `calibrated` рядом со
    счётчиками чека 10. БАЗЛАЙН ПОСАДКИ (замер 2026-08-16 по всему
    журналу, 1239 событий): медиана 550, p75 768, p90 1047, p99 2040,
    max 13352; сверх 600 — 43%; последние 30 событий на момент
    посадки — медиана 780 и 22 из 30 сверх 600. Ожидание: доля падает.
    ДЕТЕКТОР ОТКАЗА САМОГО СЛОЯ (правило 10в — иначе механизм есть
    пожелание): в окне, где журнал прирос событиями, обязано быть
    ЛИБО хотя бы одно срабатывание NOTES LEN в транскриптах, ЛИБО
    явная строка «бюджет не превышался» с числом; молчание обоих =
    слой мёртв либо не проведён — находка. Причина, по которой
    детектор нужен именно такой: проверка, читающая только
    собственный след хука, тихую смерть не отличает от «писать было
    нечего» (тот же структурный критерий, что у чека 26).

18. **Экономический тренд (Rule #1 итогом; заказ оператора
    2026-07-11) — «экономим ли мы, и куда идёт тренд».** Каждый
<!--CHK 18|src:cc_usage,скрипт:savings_report|pred:script:savings_report|rules:RC§1/R1|status:живой-->
    прогон: `python tools/savings_report.py --until <конец окна>`
    (полные API-цены из usage_report.py, кэш-скидки учтены, батча
    нет). В notes события `calibrated` — четыре числа против
    предыдущей точки: (а) $/день ROUTED-окна; (б) брутто-экономия
    контрфакта делегирования ($ и %: сайдчейны фактом против того же
    токен-профиля по ценам ПРИВЯЗКИ `roles.lead` НА МОМЕНТ ОКНА —
    вопрос контрфакта звучит «а если бы это делал Lead», и имя
    модели в нём не константа: до 2026-08-04 привязка Fable, с
    D-0099 — Opus 5. База контрфакта = привязка `roles.lead` с
    2026-08-16 (t-444); точки ДО — по Fable, несопоставимы; разрыв
    базы помечается в notes с НАПРАВЛЕНИЕМ ошибки
    (завышена/занижена); известные разрывы —
    PROCESS/CALIBRATION_HISTORY.md); (в) цена за принятую единицу
    делегированной работы (факт сайдчейнов / accepted обоих журналов
    за окно — счётчик даёт чек 13); (г) API-контур: учётный итог по
    traffic_kind + real-доля. Первая точка сверки (baseline,
    2026-07-11): $265.53/день (4 дня стройки), брутто-экономия
    $286.99 = 69%, $1.49/единицу, API $0.30/вся история — числа и
    ОГОВОРКИ МЕТОДА (цензурированный baseline, нераздельность премии
    координации от неделегируемой Lead-работы) в
    docs/task_reports/2026-07-11_savings-analysis.md; тренд читается
    только с этими оговорками, пока не накоплены недели без стройки
    механизмов. Детектор отказа самого чека: отсутствие
    экономических чисел в notes `calibrated` видно следующему
    прогону и оператору (тот же класс, что счёт N из N, F-32);
    отказ скрипта — тесты в каноническом прогоне suite.
    18(д) SPEND-ОТЧЁТ (2026-08-13, добавление по задаче
    docs/tasks/2026-08-13_spend-analytics.md, раздел 2 «единственная
    новая обязанность», узел B5 t-426; ссылаться на этот подпункт как
    на «18(д)»). Каждый прогон дополнительно исполняет
    `python tools/spend_report.py --window last --compare prev`
    (ленивый импорт транскриптов внутри самого прогона,
    идемпотентный — Rule #1, отдельного шага импорта не заводим). В
    notes события `calibrated` — минимум: M4 (доля координатора за
    окно), M10 по ярусам (haiku/sonnet/opus/fable), топ-3 стоков окна
    (секция R4.2 отчёта), счёт строк секции здоровья измерения (R4.3);
    при наличии реестра прогонов (run_units непуст) — также M6/M8 по
    прогонам окна, при пустом реестре — явное «н/д (реестра нет)»
    вместо молчания. Секция отчёта «ПРОПУЩЕННЫЕ ОКНА» (calibrated-окна,
    не имевшие строк window_series ДО пересчёта ЭТИМ прогоном отчёта —
    снимок берётся ДО rebuild()/compute_window_series(), t-428
    fix-batch item 3) обязана быть пустой ЛИБО объяснённой в notes.
    Секция «ПРОПУЩЕННЫЕ ОКНА» — аудит СОСТОЯНИЯ НА СТАРТЕ прогона, не
    постоянный сторож текущего состояния БД независимо от notes;
    детектор пропуска — тот же класс, что у (а)–(г): отсутствие чисел
    в notes `calibrated` видно следующему прогону и оператору (F-32).
    Калибровка ЗАПИСЫВАЕТ и
    свой собственный прогон в реестр run_units
    (`run_kind='calibration'`, `unit_kind='events'`, unit_count =
    `in_window_count` из `python tools/calibration_counts.py --journal
    logs/routing-log.jsonl --json`) — без этой строки калибровка не
    попадает в собственные ряды M6/M8. Это ЕДИНСТВЕННАЯ новая
    обязанность, которую вводит spend-мониторинг (раздел 2 носителя
    задачи); формулы M1–M14/D1–D9 и структура таблиц — там же, здесь
    не дублируются.

19. **Opportunity-скан делегирования (Update Rule 2 таблицы,
    исполняемая; ретро-контур 2026-07-13).** Крупнейшие блоки
<!--CHK 19|src:cc_usage,журнал|pred:manual:cc_usage|rules:RC§1/R8|status:живой-->
    САМОСТОЯТЕЛЬНОЙ Lead-работы окна (cc_usage: main-цепочки без
    сайдчейнов; git-диффы Lead-сессий): для каждого — вопрос
    «отображается ли на существующий ярус (тогда это чек 1) или это
    НОВАЯ категория-кандидат для таблицы (строка estimated)». Выход:
    0..N новых строк таблицы; «кандидатов нет» — валидный итог
    явной строкой в notes. Дополняет чеки-нарушения: единственное
    место, где калибровка ищет ВОЗМОЖНОСТИ, а не отказы.

25. **Permission/hygiene-аудит окна (порт AO3, оценка t-106 / порт
    t-107).** Прогон `python tools/permission_audit.py --minutes <окно>`
<!--CHK 25|src:транскрипты,скрипт:permission_audit|pred:script:permission_audit|rules:RC§4/permission_audit|status:живой-->
    (или `--all`) по транскриптам периода; шаги разбора — скилл
    `.claude/skills/permission-audit/SKILL.md`. `--summary` печатает
    блок «По классам гигиены» — счёт по ЧЕТЫРЁМ классам (`2>&1`,
    `cd/Set-Location`, `python -c/heredoc`, «журнал мимо Edit/Write»)
    по ВСЕМ просканированным вызовам, в скобках рядом — сколько из
    них suspects, и строку «команд с >=1 классом»; baseline
    сравнения — числа окна калибровки №6: 290 вызовов / 210 вне
    allowlist, классы 2>&1 x10, cd/Set-Location x8, python -c x7,
    журнал мимо Edit/Write 0. Замер сделан ПРИ НЕИЗМЕННОМ скоупе
    hygiene_gate и служит входом ОТЛОЖЕННОГО решения warn->блок —
    само это решение ЭТИМ ЧЕКОМ НЕ ПРИНИМАЕТСЯ, оно калибровки №7
    (или следующей по слову оператора). Две категории:
    (а) находки-нарушения гигиены (правая колонка таблицы шага 2
    скилла: cd-префикс/2>&1, ручная сборка журнальных строк мимо
    Edit/Write, ad-hoc чтение мимо Read/Grep, обход канона pytest,
    poll-циклы, ОТНОСИТЕЛЬНОЕ ЧИСЛО БЕЗ НАЗВАННОЙ БАЗЫ (подача
    «−14% от собственного пика» вместо честных −2.7% от базы),
    СТАТУС ЗАПИСИ РЕЕСТРА ИЗ ЧАСТИЧНОГО ЧТЕНИЯ (командная гигиена
    п.6: статус структурированной записи заявлен по окну, не
    дошедшему до статус-строки самой записи)) — чинить у источника,
    число и класс — в отчёт калибровки; (б) кураторство
    settings.local.json (MASKED-широкие
    правила, дубликаты, разовые записи) — по шагам 3–5 скилла;
    сужение широких правил — решение оператора.
    (в) НЕБЕЗОПАСНЫЙ ОТКАТ ПОРЧИ (заведён 2026-08-05 как
    зарегистрированный детектор п.7 командной гигиены): в
    транскриптах окна ищутся `git checkout --` / `git restore` РЯДОМ
    с мутационной пробой (порча файла, red-probe, «испорчу и верну»).
    Каждое попадание сверяется ПО ТРЁМ ВЕТКАМ (третья заведена
    2026-08-19, F-58): (1) снималась ли байтовая копия ДО порчи и
    восстановление — из неё; (2) был ли файл чист по
    `git status --porcelain` ДО порчи, замеренной НЕПОСРЕДСТВЕННО
    перед откатом (замер на базлайне хода в зачёт не идёт —
    межвременной зазор); (3) входит ли путь в owns исполнителя
    пробы, причём правка — той же сессии (ветка (д) п.7 CLAUDE.md) —
    свидетель: ОБА артефакта обязательны — декларация владения И
    дословный пустой дифф после отката (плюс дифф ДО отбрасывания,
    что именно отбрасывается); отсутствие ЛЮБОГО из двух — находка,
    не «зачтено по смыслу». Ни одна из трёх не верна — нарушение п.7
    и находка НЕЗАВИСИМО от того, что в тот раз обошлось; БЕЗ
    третьей ветки детектор метил бы легальный откат СВОЕЙ работы как
    нарушение (класс «сторож, которого приучают игнорировать»).
    ЗЕРКАЛЬНО: непустой `git status --porcelain` на пути, НЕ
    входящем в owns исполнителя пробы, — НЕ основание для отката, а
    легитимный параллельный писатель (не сторонняя порча; сшивка с
    R4(d) «never touch another session's uncommitted paths») — сам
    откат такого пути этим же (в) ловится как нарушение, независимо
    от исхода. Прецедент-эталон: 2026-08-05, критик при R3-гейте
    портил БОЕВОЙ logs/token_usage.xlsx и откатывал checkout'ом;
    потерь не было (файл был чист, бэкап снят, md5 сверен Lead), но
    идиома ровно та, что стоила инцидента AO3 08-02. Контроль формы
    поиска обязателен (гигиена п.6). Это зарегистрированный 10(в)-
    детектор секции «командная гигиена» CLAUDE.md: без него
    нарушения формы вызова не имели отдельного детектора отказа
    (первый замер — пилот t-106 на живой сессии: cd-префикс = 76%
    подозрительных вызовов, 2 heredoc-аппенда журнала, 3
    MASKED-правила).
    (г) СВЯЗКА «УСЕЧЕНИЕ ОКНА РЕЕСТРА → СТАТУСНОЕ УТВЕРЖДЕНИЕ»
    (2026-08-09, структурно по образцу (в), детектор подкласса
    командной гигиены п.6 «статус записи реестра»): адресный скан
    транскриптов окна на связку чтение реестра (эскалации/решения/
    задачи/леджеры) с усечённым/ограниченным окном (Read с
    limit/offset, головной grep без выхода на статус-строку записи)
    → следующее статусное утверждение об этой записи в сводке/
    отчёте БЕЗ повторного чтения статус-поля. Найденное = находка +
    напоминание правила гигиены п.6. ЧЕСТНАЯ ОГОВОРКА: базовый
    детектор этого класса — ИНЦИДЕНТНЫЙ (вопрос оператора вскрывает
    расхождение по факту, не по правилу); скан транскриптов этим
    подпунктом ЛИШЬ ДОПОЛНЯЕТ инцидентный слой, не заменяет его
    машинным — не выдавать одно за другое. Пустое окно (связка не
    встретилась) отмечается явно — «усечённых статусных утверждений
    нет».

### §5 ВНЕШНИЙ ДЕПЛОЙ AO3

15. **Ось 6 / R14-guard AO3 — «блокер → test_debt-баг у источника».**
<!--CHK 15|src:файлы-AO3|pred:deploy.exists:ao3|rules:RC§7/D-0074|status:живой-->
    Класс AT-BUG-004/005/006 (знание прозой в теле артефакта —
    конвейер его не видит) закрыт в AO3 2026-07-09 двумя механизмами:
    обязанность test-designer (D:\AO3_tests
    .claude/agents/test-designer.md, шаг 4 воркфлоу) и входной guard
    правила «Автоматизировать Approved-кейс» (state/rules.yaml,
    guard-комментарий при правиле). Guard сам — детектор отказа
    обязанности test-designer; детектором отказа САМОГО guard'а его
    текст называет реактивный триаж и ЭТОТ чек. Проверка окна:
    (а) живость — оба якоря на месте (точечный lookup: шаг 4 в
    промпте test-designer; when-условие «НЕ покрыт Open|Reopened
    test_debt-багом» + guard-комментарий в rules.yaml);
    (б) отказ — кейсы окна, продиспатченные в автоматизацию (журнал
    AO3), у которых «Заметки для автоматизации» содержат маркеры
    блокера (список маркеров — в guard-тексте rules.yaml), а
    покрывающего Open|Reopened test_debt-бага в bugs/
    (`type: test_debt`, `test_cases` с TC-id кейса) нет = холостой
    диспатч мимо guard'а: находка + `defect_found` (agent = исходный
    ярус диспатча) в журнал AO3. Якоря сверены разведкой t-014
    (2026-07-09, оба несущих утверждения подтверждены точечным
    чтением).
    (в) очередь фабрики — только через qa-loop (фидбек оператора
    AO3 2026-07-10, CLAUDE.md AO3 «Ярусы»): delegated-события окна
    по QA-агентам конвейера AO3 (test-automator, test-maintainer,
    test-designer, test-runner, fix-verifier и пр.), у которых нет
    парной записи orchestrator-log (диспатч вне прохода qa-loop) и
    нет пометки не-конвейерной работы в notes = нарушение; ручные
    диспатчи до 2026-07-10T17:00 — до введения правила, не
    считаются.

17. **Таймаут-гейт AO3 (AT-BUG-007) — «висящий Appium-вызов = fail
    за конечное время, не клин suite».** Механизм (AO3 2026-07-10,
<!--CHK 17|src:файлы-AO3|pred:deploy.exists:ao3|rules:RC§7/D-0074|status:живой-->
    коммит be5ec05; регистрация детектора — вопрос 10(в) в его
    сообщении): client read-timeout на command_executor
    (D:\AO3_tests framework/config/settings.py APPIUM_HTTP_TIMEOUT;
    framework/core/driver_factory.py client_config c retries=False)
    + таргетный rerun (framework/pytest.ini `--reruns 1 --only-rerun
    ReadTimeoutError|MaxRetryError`). Проверка окна:
    (а) живость — три якоря на месте (точечный lookup:
    APPIUM_HTTP_TIMEOUT в settings.py; retries=False в
    driver_factory.py; --only-rerun-строка в pytest.ini);
    (б) отказ — прогоны окна (runs/ AO3, журнал, заметки оператора):
    зависание suite заметно дольше 2×APPIUM_HTTP_TIMEOUT (~8 мин при
    дефолте 120с), потребовавшее ручного вмешательства = отказ гейта:
    находка + `defect_found` (ref=at-bug-007) в журнал AO3;
    (в) ложные срабатывания — таймаут-fail легитимного вызова в
    триаже окна (известный риск: холодная докачка chromedriver на
    SET_CONTEXT, задокументирован в settings.py) = пересмотр
    величины (env AO3_APPIUM_HTTP_TIMEOUT), не снятие гейта.

27. **Гранулярность реестра фич AO3 vs покрытие режимов (урок
    hide/dim; внедрение AO3 2026-07-17, коммит 8ccf8a6).** Механизм:
<!--CHK 27|src:файлы-AO3|pred:deploy.exists:ao3|rules:RC§7/D-0074|status:живой-->
    правило «одна запись реестра = одно наблюдаемо различимое
    поведение» в шапке D:\AO3_tests\docs\feature-registry.yaml +
    промпты test-strategist (владелец реестра) и test-designer (не
    зачитывать фичу одним кейсом). Дыра класса: запись, склеившая
    несколько наблюдаемых режимов, выглядит «покрытой» в
    coverage-map кейсами на один режим — машинного детектора нет
    (поле features при этом честно заполнено и валидно). Проверка
    окна: (а) живость — правило на месте (точечный lookup: шапка
    реестра; правки реестра/промптов за окно не снесли
    формулировку); (б) отказ — по записям реестра, затронутым за
    окно (новые/правленые записи ИЛИ записи, чьи кейсы добавлялись),
    выборочно 3–5: формулировка объединяет несколько наблюдаемо
    различимых поведений (режимы, состояния, ветки UI), а кейсы
    features-тега покрывают только часть = ложное «покрыто» —
    находка, дробление записи в очередь test-strategist (вердикт
    семантический — за калибрующим Lead, D-0063). Прецедент-эталон:
    hide и dim — разные наблюдаемые исходы; dim-фильтрация жила
    непокрытой внутри «покрытой» записи (AO3 2026-07-17). Ось —
    под-ось «фичевая трассабилити» оси 6 SIBLING_MAP.

28. **Fail-fast device-воркеров AO3 (слово оператора 2026-07-16,
    внедрение 2026-07-17, AO3 коммит 89c0b1e).** Механизм: канон в
<!--CHK 28|src:файлы-AO3|pred:deploy.exists:ao3|rules:RC§7/D-0074|status:живой-->
    D:\AO3_tests\docs\06-dark-factory.md §5 «Самовосстановление» +
    якорь-блок «Fail-fast среды» в промптах 5 device-воркеров
    (fix-verifier, test-runner, test-reviewer, test-automator,
    test-maintainer): 2 идентичных env-класс фейла
    (ReadTimeoutError/TimeoutError на одном вызове/шаге) → стоп
    штатного протокола → диагностический мини-прогон → Blocked +
    эскалация с диагнозом. Проверка окна: (а) живость — 6 якорей на
    месте (точечный lookup: буллет в docs/06 §5 + секция «Fail-fast
    среды» в каждом из 5 промптов); (б) отказ — длительности сессий
    device-воркеров окна в cc_usage (сайдчейны QA-агентов): сессия
    заметно дольше типовой для своего класса, чей отчёт/артефакты
    несут серию однотипных Timeout-фейлов БЕЗ раннего
    blocked-вердикта = утечка правила — находка + defect_found в
    журнал AO3 (эталон прецедента: 65 мин fix-verifier
    at-bug-006-verify-d1, ~52 из них — ожидание 120s-таймаутов);
    (в) ложный fail-fast — blocked при среде, которую диагностика
    воркера битой не подтвердила (следующий прогон прошёл без
    починки среды) = пересмотр формулировки триггера решением, не
    молчаливое снятие. Смотреть вместе с чеком 17 (код-гейт
    AT-BUG-007: клин → быстрый fail; этот чек — промпт-слой над ним:
    воркер не должен накапливать быстрые fail'ы полным протоколом).

33. **Отказ детектора heartbeat-fastdeath AO3_tests (named-чек по
    кросс-пункту D-0082 от 2026-08-15; посажен в протокол
    2026-08-16 — до этой даты пункт жил только строкой
    CURRENT_CONTEXT, то есть по F-48 НЕ был передан механизму
    калибровки).**
<!--CHK 33|src:файлы-AO3|pred:deploy.exists:ao3|rules:RC§7/D-0074|status:живой|since:2026-08-16-->
    Механизм у них: их фабрика 2026-08-15 стояла
    молча 8+ тиков — ребёнок heartbeat умирал за ~2 с на истёкшем
    OAuth, а обёртка считала проход состоявшимся; посажен
    spec-heartbeat-fastdeath v2 — серия из 3+ быстрых смертей
    подряд обязана поднять singleton-эскалацию
    HEARTBEAT-CHILD-DEATH, бюджет прогонов за быстрые смерти
    возвращается. НАРУШЕНИЕ (формулировка их стороны, принята
    дословно): по их `state/orchestrator-log.md` — серия >=3 ПОДРЯД
    heartbeat-строк с нездоровым исходом (`exit=<не 0>` /
    `spawn-failed` / `exit=error:`), в которой НИ ОДНА строка не
    несёт суффикс ` fastdeath=`, И при этом в их
    `state/escalations.md` нет ОТКРЫТОЙ HEARTBEAT-CHILD-DEATH =
    детектор отказал, механизм выродился в пожелание (правило 10в).
    Оба их носителя сверены существующими 2026-08-16 при посадке
    чека. Негатив «серий нет» валиден только с позитивным контролем
    (гигиена п.6): тем же грепом показать заведомо присутствующую
    heartbeat-строку окна. Статус их эскалации читать ДО статусной
    строки записи, а не по заголовку (F-55): их реестр append-only,
    вердикт стоит в КОНЦЕ многосекционной записи. Наша сторона
    класса: heartbeat-контуров у нас нет (греп tools/ и .claude/ на
    2026-08-16 пуст при пройденном позитивном контроле той же формы)
    — при пустом окне писать «н-п: heartbeat-контура у нас нет»,
    молчание неотличимо от пропуска.

35. **Дрейф среза карты осей у AO3 — «отставший срез молча сужает
    их механизменный гейт» (регистрация кросс-пункта их
    state/sibling-map.snapshot.md, 2026-08-19).**
<!--CHK 35|src:файлы-AO3,файлы|pred:deploy.exists:ao3|rules:RC§7/D-0074|status:живой|since:2026-08-19-->
    Механизм на их стороне: `D:\AO3_tests
    state/sibling-map.snapshot.md` — закоммиченный СРЕЗ номеров осей
    ЖИВОЙ docs/SIBLING_MAP.md этого репо; их `scripts/
    mechanism_gate.py` резолвит карту цепочкой env → живая карта →
    срез (облачная сессия без `D:\` видит только срез). Отставший
    срез = механизменный коммит AO3 из облака проходит гейт без
    строк на новые оси — молчаливое сужение гейта, зеркало класса
    7bd21cc. Их частичные детекторы (stderr-строка гейта,
    анти-дрейф unit «не меньше 9 осей», snapshot_shrink_guard на
    ужатие) РОСТ живой карты при отставшем срезе не ловят — этот
    чек закрывает именно эту дыру. Проверка окна: (а) живость —
    срез существует и его шапка объявляет цепочку резолюции и
    правило «Windows-сессия обновляет тем же ходом» (точечный
    lookup); (б) дрейф — сверка множеств заголовков `## Ось N`
    среза и живой карты (греп обеих): расхождение номеров в ЛЮБУЮ
    сторону (ось живой карты без строки в срезе ИЛИ ось среза,
    которой нет в живой) = находка + синк-обязанность первой
    Windows-сессии AO3; изъятая ось живой карты (правило 4 карты:
    заголовок остаётся с пометкой «изъята») обязана стоять в срезе
    той же пометкой. Негатив «дрейфа нет» валиден только с
    позитивным контролем той же формы (тем же грепом показать
    заведомо присутствующую ось в ОБОИХ файлах, гигиена п.6).

## Завершение прогона

Статусы DELEGATION_TABLE.md двигаются только по этим данным
(Update Rule 1; полная стоимость с ретраями — Update Rule 4). По
завершении — событие `calibrated` в журнал OS-репо: notes содержат
период, число разобранных событий, изменения статусов таблицы,
счётчики строк из чека 10, размер вывода хука из чека 11,
false-accept rate по ярусам из чека 13, экономические числа
чека 18 (против предыдущей точки) и итоги ретро-контура — чеков 0
и 19–24 (пустые окна — явно; см. docs/RETRO_PATTERNS.md). Отсутствие `calibrated` дольше 7 дней
при живом routed-трафике — нарушение, видимое строкой Last
Calibration в Boot Report каждой сессии (детектор вынесен наружу
сознательно: калибровка не может быть детектором собственного
отсутствия).
