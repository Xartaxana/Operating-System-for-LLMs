__pycache__/
*.pyc
gateway/*.db
gateway/*.db-journal
gateway/*.log
.env
gateway/.env
.pytest_cache/
# рабочие данные policy-as-code гейтов (t-161): пер-сессионный трек
# правка/прогон и снимок критика - эфемерные, не для истории
.claude/dod_track/
.claude/critic_snapshot.json
# sidecar owns_gate.py (слой 1 защиты от коллизий параллельных
# диспатчей) -- живой трек, эфемерный, не для истории
logs/owns_registry.jsonl
# синк Dog 07-29 (D-0082): сессионный леджер search_control_gate_next.py
# половины A -- живой per-session трек, эфемерный, не для истории
logs/.search-ledger/
# handoff 07-30: Office-lock открытых у оператора xlsx (docs/~$EXAM_RESULTS.xlsx)
~$*.xlsx
