{"task": "e1", "commit": "REPLACE-WITH-A-COMMIT-HASH-FROM-YOUR-REPO", "kind": "config", "prompt": "Add a comment block to gateway/config.template.yaml explaining the GATEWAY_LOG_RAW_TEXT env var (default disabled, masks prompt/response, accounting fields unaffected) and pointing to the README's raw-text warning.", "paths": ["gateway/config.template.yaml"]}
{"task": "e2", "commit": "REPLACE-WITH-A-COMMIT-HASH-FROM-YOUR-REPO", "kind": "docs-edit", "prompt": "Add a corpus-slicing-rule paragraph to gateway/regression_runner.py's module docstring: a candidate whose expected output alone exceeds ~25K tokens must be sliced into parts before being added as a regression-set row.", "paths": ["gateway/regression_runner.py"]}
