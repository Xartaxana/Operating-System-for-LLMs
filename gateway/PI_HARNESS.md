# Pi-обвязка для gateway-воркеров (прототип 2026-07-09, t-011/t-012)

Состояние и рецепт; решения/вердикты — журнал t-011/t-012 и Runs log
PROCESS/SCOUT_GOLDEN_SET.md; обзор кандидатов — docs/RELATED_WORK.md
«Agent tool harnesses».

## Установлено

- `npm install -g @earendil-works/pi-coding-agent` (v0.80.3, MIT).
- Провайдер: `C:\Users\user\.pi\agent\models.json` — `os-gateway`
  (baseUrl `http://localhost:4000/v1`, api `openai-completions`),
  модели: `intern`, `middle-groq`, `builder-groq`. Схема ТРЕБУЕТ в
  cost все четыре поля: input/output/cacheRead/cacheWrite.
- Прокси перед запуском: из gateway/, ключи из .env, PYTHONUTF8=1
  (README «Run»).

## Рабочая форма вызова (проверена)

    cmd /c "pi --provider os-gateway --model os-gateway/<id> -p
      --mode json -nc -ns -ne --no-session --offline
      -t read,bash
      --append-system-prompt <файл-роли>
      @<файл-вопросов> "<сообщение>" < NUL > <вывод.json> 2>&1"

Критично: `< NUL` — в `-p`-режиме Pi ждёт EOF на открытом stdin и
висит вечно; `--mode json` — text-режим рендерит только в TTY,
редирект пуст; `-nc` — не подхватывать CLAUDE.md (роль подаётся
файлом); разбор ответа — по `message_end`-событиям JSON-потока.
Из Git Bash (t-063): cmd-обёртку НЕ использовать — `< NUL` внутри
кавычек перехватывает сам bash и cmd повисает интерактивно; звать
pi напрямую с `< /dev/null` (форма проверена, работает).

## Профиль скаута (ужесточённый, после t-012 attempt 1)

    Ты — scout, разведчик репозитория. Правила:
    0. ГЛАВНОЕ: отвечай ТОЛЬКО фактами из файлов, которые ты РЕАЛЬНО
       прочитал инструментами (read/bash) в этом прогоне. Отвечать по
       памяти/догадке запрещено. Указывать в ответе или в «Следе»
       файл, строку или команду, которые ты не читал/не выполнял, —
       запрещено. Не нашёл ответа — так и напиши: «не нашёл», с
       перечнем реально выполненных поисков.
    1. Режим строго READ-ONLY: не создавай, не изменяй и не удаляй
       файлы; bash — только поиск и чтение (grep, ls, cat).
    2. Отвечай ДАЙДЖЕСТОМ: к каждому ответу — file:line.
    3. Негативное утверждение валидно только со следом.
    4. Вопрос-суждение не решай: факты + «требуется решение уровня
       выше».
    5. Заверши блоком «След»: поиски и прочитанные файлы.

## Пост-прогонный guard (t-017, обязателен для экзаменов)

ДО чтения/оценки отчёта Pi-воркера координатором — детерминированный
детектор фабрикации F-14 (ноль структурных tool-вызовов при
содержательном ответе):

    python tools/pi_run_guard.py --json <вывод.json>
    python tools/pi_run_guard.py --db gateway/requests.db --model <alias> --since <ts> --until <ts>

Exit: 0 PASS / 1 REJECTED (штамп rejected без оценки) / 2 INCONCLUSIVE
(ops-абор, не вердикт). Окно --db обязано начинаться И заканчиваться
на оцениваемом ответе (см. KNOWN GOTCHA в docstring guard'а).
Детектор отказа самого guard'а — чек 14(д) еженедельной калибровки.

## Известные разрывы (очередь CURRENT_CONTEXT)

1. ЗАКРЫТ КАК НЕ ВОСПРОИЗВЕДЁННЫЙ (t-013, 2026-07-09; builder +
   critic + Lead, независимые прогоны каждого): стриминговые
   tool-call-дельты groq ДОЖИВАЮТ до клиента через прокси — оба
   groq-алиаса (middle-groq, builder-groq) дают structured tool_calls
   в стриме, Pi e2e исполняет реальные вызовы (single-turn read +
   мультитёрн 3 вызова). Версия litellm при t-012 и t-013 ОДНА
   (1.90.2, установлена 2026-07-03) — конфаунда апгрейда нет;
   наблюдение t-012 attempt 2 — вероятный транзиент на стороне Groq
   (гипотеза, не доказана; сегодняшние 429 выглядят иначе — громкая
   HTTP-ошибка, не тихий обрыв). Персистентный изолятор:
   gateway/tools_stream_check.py (--stream/--no-stream, --model;
   exit 0/1) — при рецидиве гонять его ПЕРЕД выводами о моделях.
2. litellm-префикс `ollama/` НЕ пробрасывает tools — intern переведён
   на `ollama_chat/` (config.yaml, комментарий на месте).
3. РАЗБЛОКИРОВАН (t-037, 2026-07-10): builder-Pi на builder-groq
   работает по валидированному рецепту — урезанный тулсет
   `-t read,bash,write,edit -nc` (= 1,659 prompt-ток. у скаута,
   1,531 у builder-набора — ИЗМЕРЕНО, write/edit-набор легче) +
   ЯВНЫЙ колпак `maxTokens: 1500` в models.json (admission Groq =
   промпт + completion-надбавка; дефолтные 8192 надбавки пробивали
   TPM 8000 в одиночку) + reasoning_format:hidden на шлюзе (разрыв
   №5 ниже). Живой прогон: мультитёрн write→edit→bash, 5 success-
   ходов, артефакт корректен (журнал t-037). Остаточная known
   quirk: на бёрсте быстрых ходов возможен транзиентный 429 TPM
   (Groq считает admission-вес, стена t-018 — фактические токены;
   t-037 row 404) — litellm-ретрай поглощает его сам, вреда нет;
   admission-математика в стене — кандидат по evidence калибровки.
   Историческая диагностика TPM-потолка: gpt-oss-120b на Groq free tier имеет
   8000 токенов/мин, а дефолтный системный промпт Pi + один tool
   read ≈ 8879 токенов → 429 «Request too large» ещё до генерации
   (t013-лог, rows 36-51). Класс: «вес дефолтного Pi-промпта не
   помещается в TPM free-tier модели». Кандидаты: урезанный системный
   промпт Pi, платный tier, другая модель-носитель builder-функции.
   Дополнение по TPD (t-015 attempt 2, 2026-07-09 22:21): окно TPD у
   Groq СКОЛЬЗЯЩЕЕ (24 часа), а не сброс в фиксированный час —
   «Used 96661 ... try again in 3h38m» через час после ожидавшегося
   «сброса». Следствие (t-015): квотный прогон, запущенный после
   других Groq-задач дня, умирает об их расход — attempt 3 поставлен
   первым Groq-трафиком свежего окна. Факт веса: с
   `-t read,bash -nc` первый запрос = 1659 prompt-токенов (сильно
   легче дефолтных ~8.9k), но лимитер Groq считает «Requested»
   ~18.5k — включает completion-надбавку. Кодовая стена квот —
   t-018 (A2).
   Дополнение по планированию (t-015 attempt 3, 2026-07-10): каждый
   прогар выпадает из скользящего окна ровно через 24ч ПОСЛЕ себя —
   «try again in X» из 429 освобождает место под ОДИН запрос, а
   многоходовый экзамен требует запаса ~70k (история растёт,
   admission-вес каждого хода ~17–18.5k). Считать момент запуска =
   когда выпадут вчерашние куски, по СУММЕ requests.db И боковых баз:
   стена t-018 видит только requests.db, а Groq считает всё — трафик
   через GATEWAY_DB_PATH (t013.db: 68,054 tok) для стены невидим
   (F-27; сверка с provider-заголовками — в очереди, OpenClaw item 2).
   ПРАВИЛО ЗАПУСКА (t-027, F-30 слой 2; заменяет ручную оконную
   математику выше): любой квотный прогон стартует ТОЛЬКО после
   `python tools/preflight_quota.py --alias <alias> --need <оценка>`
   с вердиктом GO; NO-GO не переторговывается — ждать названного
   скриптом времени или менять план. Скрипт делает F-27-математику
   сам (все gateway/*.db, группировка по provider-модели, расписание
   освобождения); `--probe` даёт истину провайдера из 429. Прогноз
   go_at считается по локальным базам — при расхождении probe>локаль
   он оптимистичен (нижняя граница; критик t-027 N3). Детектор
   обхода правила — чек 13(ж) калибровки.
5. ЗАКРЫТ В КОНФИГЕ (t-037 attempts 1-2, 2026-07-10): reasoning-эхо
   Pi↔litellm↔Groq — gpt-oss возвращает reasoning в ответе, Pi
   сохраняет его thinking-блоком (thinkingSignature=reasoning_content)
   и эхом шлёт в следующем запросе, а Groq НА ВХОДЕ свойство
   reasoning_content не принимает → 400 на КАЖДОМ мультитёрне.
   models.json reasoning:false НЕ лечит (модель шлёт reasoning
   независимо от Pi). Фикс: `reasoning_format: hidden` в
   litellm_params алиаса builder-groq (config.yaml, пробой сверено:
   ответ без reasoning-поля). judge-groq не тронут: судья
   одноходовый, эха нет.
6. НОВЫЙ разрыв (t-063, 2026-07-11, не закрыт — фикс не строился,
   Rule #1: вскрылся на закрытом операционным вердиктом экзамене):
   когда модель эмитит некорректный tool-вызов и Groq возвращает
   ошибку типа `tool_use_failed`, litellm падает, рендеря её
   (`invalid literal for int(): 'tool_use_failed'` — код ошибки
   нечисловой), и Pi видит крэш вместо внятного отказа. Диагностика:
   грепать транскрипт по `tool_use_failed`. Задокументирован после
   двух проявлений (t-063 att.2 s1/s4, gpt-oss-120b).
   Сопутствующий факт того же прогона: TPM 8000 free-tier gpt-oss
   даёт потолок ~3 tool-хода на сессию ЛЮБОЙ ширины (админ-вес
   истории; сплит по 1-2 вопроса не спасает) — scout-функция на
   этом тарифе операционно невозможна (вердикт t-063).
4. Экзамены после t-013: ОБА ЗАКРЫТЫ. qwen3:4b — FAIL (t-016);
   llama-70B — FAIL (t-015 attempt 4, 2026-07-10): при чистой квоте
   (preflight GO) и доказанной в том же окне трубе
   (tools_stream_check PASS) модель эмитила вызовы текстом
   (<function/bash ...>, ноль структурных tool_calls) и сфабриковала
   «След» — guard t-017 REJECTED, вердикт по Runs log
   SCOUT_GOLDEN_SET. Псевдо-синтаксис вызовов текстом — известная
   мода отказа llama-семейства под многовопросной нагрузкой;
   бистабильность (реальные вызовы в attempts 2/3) вердикта не
   спасает: тихая фабрикация несовместима с scout-функцией.
