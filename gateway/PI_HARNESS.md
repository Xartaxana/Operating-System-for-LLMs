# Pi-обвязка для gateway-воркеров (прототип 2026-07-09, t-011/t-012)

Состояние и рецепт; решения/вердикты — журнал t-011/t-012 и Runs log
PROCESS/SCOUT_GOLDEN_SET.md; обзор кандидатов — docs/RELATED_WORK.md
«Agent tool harnesses».

## Установлено

- `npm install -g @earendil-works/pi-coding-agent` (v0.80.3, MIT).
- Провайдер: `C:\Users\user\.pi\agent\models.json` — `os-gateway`
  (baseUrl `http://localhost:4000/v1`, api `openai-completions`),
  модели: `intern`, `middle-groq`, `builder-groq`. Схема ТРЕБУЕТ в
  cost все четыре поля: input/output/cacheRead/cacheWrite.
- Прокси перед запуском: из gateway/, ключи из .env, PYTHONUTF8=1
  (README «Run»).

## Рабочая форма вызова (проверена)

    cmd /c "pi --provider os-gateway --model os-gateway/<id> -p
      --mode json -nc -ns -ne --no-session --offline
      -t read,bash
      --append-system-prompt <файл-роли>
      @<файл-вопросов> "<сообщение>" < NUL > <вывод.json> 2>&1"

Критично: `< NUL` — в `-p`-режиме Pi ждёт EOF на открытом stdin и
висит вечно; `--mode json` — text-режим рендерит только в TTY,
редирект пуст; `-nc` — не подхватывать CLAUDE.md (роль подаётся
файлом); разбор ответа — по `message_end`-событиям JSON-потока.

## Профиль скаута (ужесточённый, после t-012 attempt 1)

    Ты — scout, разведчик репозитория. Правила:
    0. ГЛАВНОЕ: отвечай ТОЛЬКО фактами из файлов, которые ты РЕАЛЬНО
       прочитал инструментами (read/bash) в этом прогоне. Отвечать по
       памяти/догадке запрещено. Указывать в ответе или в «Следе»
       файл, строку или команду, которые ты не читал/не выполнял, —
       запрещено. Не нашёл ответа — так и напиши: «не нашёл», с
       перечнем реально выполненных поисков.
    1. Режим строго READ-ONLY: не создавай, не изменяй и не удаляй
       файлы; bash — только поиск и чтение (grep, ls, cat).
    2. Отвечай ДАЙДЖЕСТОМ: к каждому ответу — file:line.
    3. Негативное утверждение валидно только со следом.
    4. Вопрос-суждение не решай: факты + «требуется решение уровня
       выше».
    5. Заверши блоком «След»: поиски и прочитанные файлы.

## Известные разрывы (очередь CURRENT_CONTEXT)

1. ЗАКРЫТ КАК НЕ ВОСПРОИЗВЕДЁННЫЙ (t-013, 2026-07-09; builder +
   critic + Lead, независимые прогоны каждого): стриминговые
   tool-call-дельты groq ДОЖИВАЮТ до клиента через прокси — оба
   groq-алиаса (middle-groq, builder-groq) дают structured tool_calls
   в стриме, Pi e2e исполняет реальные вызовы (single-turn read +
   мультитёрн 3 вызова). Версия litellm при t-012 и t-013 ОДНА
   (1.90.2, установлена 2026-07-03) — конфаунда апгрейда нет;
   наблюдение t-012 attempt 2 — вероятный транзиент на стороне Groq
   (гипотеза, не доказана; сегодняшние 429 выглядят иначе — громкая
   HTTP-ошибка, не тихий обрыв). Персистентный изолятор:
   gateway/tools_stream_check.py (--stream/--no-stream, --model;
   exit 0/1) — при рецидиве гонять его ПЕРЕД выводами о моделях.
2. litellm-префикс `ollama/` НЕ пробрасывает tools — intern переведён
   на `ollama_chat/` (config.yaml, комментарий на месте).
3. Pi-builder на builder-groq заблокирован НЕ разрывом №1 (опровергнуто
   t-013), а TPM-ПОТОЛКОМ: gpt-oss-120b на Groq free tier имеет
   8000 токенов/мин, а дефолтный системный промпт Pi + один tool
   read ≈ 8879 токенов → 429 «Request too large» ещё до генерации
   (t013-лог, rows 36-51). Класс: «вес дефолтного Pi-промпта не
   помещается в TPM free-tier модели». Кандидаты: урезанный системный
   промпт Pi, платный tier, другая модель-носитель builder-функции.
4. Экзамены после t-013: пере-экзамен llama-70B (вердикт не вынесен)
   и qwen3:4b (FAIL стоит) на ужесточённом профиле — путь свободен,
   разрыва нет.
