# 2026-07-12 — API-окно: подготовка, смоки, включение (t-074/t-075)

Закрытая часть нарратива API-окна, перенесена ДОСЛОВНО из
CURRENT_CONTEXT.md вечерним handoff-boot-diet'ом (D-0068; живой
остаток — статус окна и процедура закрытия — остался в Current
Task). Журнал: t-074 (разведка), t-075 (builder + critic-вход,
принят 21:16). Коммиты: 989c81c (шлюз), b449891 (лаунчер+смок),
41d29f0 (включение), поправка 22:05 — коммитом параллельной нити.

## Перенесённый блок (дословно)

API-ОКНО (приказ оператора 2026-07-12 вечер): лимиты подписки
исчерпаны, оператор и так платит по API-ценам — 2–3 дня РЕАЛЬНОЙ
работы через прокси на ANTHROPIC_API_KEY как тест. Даёт requests.db
real-трафик С промпт-контентом → R2/R3 гейта становятся вычислимы
(cc_usage этого не даёт by design). ПОДГОТОВКА ГОТОВА (t-075 принят
с critic-входом 2026-07-12): passthrough-алиасы 5 имён, кэш-колонки
requests + cache-aware cost_usd (litellm 1.90.2 сам считает скидку —
воспроизведено критиком), капы daily_usd (fable 150 / sonnet-5 50 /
opus 50 / haiku 10 / sonnet-4-6 20 — оператор может поправить).
СМОК НА ЖИВОМ ПРОКСИ 2026-07-12 21:30+ (synthetic, ~$0.05): полный
кэш-цикл подтверждён — call1 write 16804 ток./$0.0421, call2 READ
16804/$0.0034 (12x дешевле, cost_usd cache-aware); обе строки в
requests с кэш-колонками; /v1/messages (формат Claude Code)
отвечает; пробы перепомечены synthetic. Прокси запущен независимым
окном (gateway/run_proxy.ps1). ОКНО ВКЛЮЧЕНО 2026-07-12 ~21:45:
env-блок (ANTHROPIC_BASE_URL=localhost:4000 + ключ) вписан в
ПОЛЬЗОВАТЕЛЬСКИЙ C:\Users\user\.claude\settings.json (бэкап
settings.json.bak-apiwindow рядом). ПОПРАВКА 22:05 (сверка живой
сессии после перезапуска приложения): ДЕСКТОП через шлюз НЕ ходит —
приложение переопределяет ANTHROPIC_BASE_URL на api.anthropic.com
и остаётся на ПОДПИСКЕ (OAuth активен, ключ для биллинга не одобрен:
customApiKeyResponses.approved пуст; live-соединения claude →
160.79.104.10 напрямую, к :4000 — ни одного). API-контур =
ТЕРМИНАЛЬНЫЕ сессии claude. Headless-смок через шлюз 22:02
(claude -p, haiku, BASE_URL форсирован в env): 2 строки real в
requests; СТРИМИНГОВЫЙ чек (критик №1) ЗАКРЫТ —
cache_read_input_tokens=4400 non-NULL, cost_usd cache-aware
($0.000663 на кэш-чтении).

## Финальная сверка на рабочем трафике (закрытие сессии, 22:20)

Оператор переключилась, интерактивная терминальная сессия пошла
через шлюз: requests id 527–531 (claude-opus-4-8, traffic_kind=real,
22:19–22:20) — cache_read заполнен на ВСЕХ строках (пример: 61541
из 63423 токенов промпта из кэша, ~97%; cost $0.089 cache-aware).
Все условия приёмки t-075 закрыты. Урок PYTHONUTF8: litellm-баннер
падает на cp1251-консоли — канонический запуск прокси только через
gateway/run_proxy.ps1. Попутная находка: пробы SessionStart-хука
(gemini/qwen, 22:19) пишутся traffic_kind='real' — кандидат тегать
synthetic, строка в очереди (ось 2).
