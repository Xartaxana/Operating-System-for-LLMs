# Манифест намеренных расхождений корень ↔ toolkit — релиз v0.2.0 (2026-07-14)

DoD релизного catch-up батча (порт-очередь п.13; слово оператора
2026-07-14 «делаем новый релиз в тулкит» открыло синк D-0074 и
сдвинуло его же триггер «после закрытия Phase 2»). Батч: спека
дизайнера t-101 (пилот, прогон 4), пакеты A/B/C = t-102/t-103/t-104,
зачистка t-105; critic-вход одним ревью на батч. Файл живёт в docs/
КОРНЯ намеренно — пользователи шаблона нашу кухню не видят (D-0070).

## Что догнало корень (порт выполнен)

| Пара | Портировано | Пункты очереди |
|---|---|---|
| gateway/sqlite_logger.py | кэш-колонки + _cache_tokens (t-075), колонка category (t-085), идемпотентные миграции | 3, 5 |
| gateway/shadow_eval.py | 4-статусный словарь вердиктов + --record-evidence (t-095), авто-max_tokens + truncated (F-39), приоритет хранимой category, --pace | 5, 7 |
| gateway/metrics.py | readiness R2/C3 вычисляемые (t-090), cache_read_share (формула F-38), shadow_log_path | 6 |
| gateway/regression_runner.py + regression_set_coding.jsonl | раннер + НОВЫЙ генерический набор (см. ниже) | 5 |
| tools/usage_report.py | рекурсивный глоб субагентов (F-42), MAX-wins дедуп (F-43/t-094) | 12, 4 |
| tools/session_context.py | F-37-маркер MODEL-строки, OPEN DISPATCH скан с законом accepted (D-0076) | 4, 14 |
| tools/journal_validator.py | worker_ref-проверка новых delegated (D-0076), хирургически | 14 |
| CLAUDE.md (EN) | UI-witness (правило 2), лейбл-конвенция (правило 7), секция журнала D-0076 (пример/абзац/перечень полей) | 8, 11, 14 |
| PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md | подпункты 13 (g-доп F-37 / h traffic_kind / i phantom dispatches) | 14, 4 |
| docs/SHADOW_EVALUATION_LOG.md | создан пустым-с-заголовком (D-0067-сплит) | 5 |

## Намеренные расхождения (не дефекты синка)

1. **Язык/референсы**: toolkit — EN; RU-дрейф корня НЕ портирован
   (preflight_quota: RU-комментарии живут в КОРНЕ, toolkit-версия
   корректнее — обратное направление дрейфа, находка дизайнера).
   Наши task-id (t-NNN), даты инцидентов, форензика вычищены;
   короткие D-#### токены — по конвенции конкретного файла
   (journal_validator де-референсирован полностью).
2. **VALIDATED_DELEGABLE_CATEGORIES = frozenset()** (пусто): свежий
   деплой ничего не валидировал; наш набор сломал бы их чек-зеркало
   в день установки (вердикт B-1).
3. **regression_set_coding.jsonl**: наш набор (15 промптов = реверс
   нашего кода + наши task-id) НЕ шипается (утечка D-0070); в шаблоне
   3 генерических промпта (ISO-date parser / LRU cache / todo-CLI),
   source_task=sample-1..3.
4. **DECISIONS.md — пустой шаблон** (дом решений пользователя);
   строка D-0076 внесена пакетом C и РЕВЕРТНУТА при приёмке
   (вердикт G перевёрнут на evidence: toolkit де-референсирован по
   построению, единственная D-0076-ссылка — сама строка).
5. **config.template.yaml** vs наш config.yaml: алиасы-примеры
   (lead/critic/builder/scout/judge/analyst/mock) без наших ключей.
6. **calibration_counts: миграционные cutover'ы = эпоха**
   (1970-01-01) с комментарием «выставь свои при миграции» — наши
   даты схемы журнала не шипаются; тест переведён на
   monkeypatch-cutoff.
7. **savings_report: --routed-start стал required** — дата деплоя
   роутинга у каждого своя, нашего дефолта нет.
8. **CATEGORY_TO_TASK_TYPE** адаптирован под wording ИХ таблицы
   (строки Formatting у них нет — нейтральная метка); пост-t-095
   метки не несущие (кодового пути записи в таблицу нет).
9. **Ссылки на оси SIBLING_MAP генерализованы** (их карта — 2 другие
   оси); нумерация чеков: корневой чек 5 = их чек 4 (сверено).
10. **Принятый остаточный провенанс** (решение Lead, не чувствителен):
    ISO-таймстемпы-иллюстрации формата в preflight_quota; даты
    D-0030-оценок в guard.py; имя фикстуры t013.db; функциональные
    model-id ключи прайс-словаря usage_report (публичные id;
    удаление = регрессия прайсинга).

## Не портировано (свои гейты, не забыто)

- п.9 очереди — ретро-контур калибровки (чеки 0/19–24): после >=1
  прожитого прогона (~07-18).
- п.13 — privacy-режимы sqlite_logger: не порт, а разработка;
  дефолт — решение оператора.
- п.10 — экзамен экономии деплоя: прогоняется ПОСЛЕ этого снимка
  (чек 14е).
- permission-audit (t-106/t-107, порт из AO3 сегодня же): в toolkit —
  после проживания в корне (новый пункт порт-очереди).

## Верификация

- Свиты: toolkit 393 passed; корень 419 passed (обе — канонической
  формой, отдельными инвокациями).
- Утечки: греп-сканы критика и t-105 с позитивными контролями;
  остаточный список закрыт Lead при приёмке (conftest, cutover'ы,
  savings-дефолт) либо принят п.10 выше.
- CLI-смоки: shadow_eval/metrics — новые флаги есть, старых нет;
  раннер собирает payload из генерического набора.
