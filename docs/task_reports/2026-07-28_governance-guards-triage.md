# Триаж 2026-07-28: «Governance → Executable Guards» (инсайт-промпт) против стоящего enforcement

Вход: шестипунктовый промпт «Governance Rules As Executable Guards»
из инсайт-отчёта харнесса 07-28, подан оператором к исполнению.
Метод: сверка каждого пункта против стоящих механизмов (прецедент —
триаж ревью Codex, 2026-07-28_codex-review-triage.md); исполняются
ДЕЛЬТЫ, дубли исполненного не строятся (D-0025, Rule #1). Промпт
сгенерирован без знания Phase 4 — его ядро и есть наша Phase 4
(D-0098), входная оценка которой (enforcement-gap аудит 07-24,
t-312..t-315) уже произвела требуемую матрицу покрытия.

## Пункт 1 — RULE_COVERAGE.md (матрица правило → enforcement)

УЖЕ СТОИТ: enforcement-gap аудит
docs/task_reports/2026-07-24_enforcement-gap-audit.md + ре-аудит
t-319..t-321 (свип леджера 19/19 строк с файлами за ними, все 157
док-ссылок резолвятся, каждая E-позиция с named-детектором); гейт
(б) Phase 4 «нет неклассифицированных дыр» — ПРОЙДЕН 07-24 (ROADMAP,
Gate status).

РЕШЕНИЕ (записанное): второй ЖИВОЙ файл-матрица НЕ создаётся —
поддерживаемый дубль аудита = класс расходящейся пары (ось 4);
владельцы покрытия — аудит-отчёт (снимок) + гейт (г) (ре-прогон
аудита) + чеки калибровки (непрерывный слой). Reopen-триггер: гейт
(г) финального закрытия Phase 4 и любой будущий аудит-прогон.
ПЕРЕВЁРНУТО ТЕМ ЖЕ ДНЁМ СЛОВОМ ОПЕРАТОРА (вечер 07-28): живой
docs/RULE_COVERAGE.md заведён по явному заказу — с обязанностью
поддержания той же парой оси 4 и строкой чека 24 (риск дрейфа,
ради которого матрица отвергалась утром, закрыт именно этими двумя
сторожами; решение оператора — вершина иерархии, R11a).

## Пункт 2 — классификация UNENFORCED (a/b/c)

УЖЕ СТОИТ дословно как D-0063/D-0064: (a) «механизируемо сейчас» =
машинный слой на пути; (b) «нужен артефакт» = промоция/строительство
по evidence; (c) «судейское по природе» = «held by discipline» с
НАЗВАННЫМ детектором (без детектора — не механизм, D-0049).
Классификация исполнена аудитом по всем позициям слоя E.

## Пункт 3 — пять именованных гвардов промпта

| Гвард из промпта | Состояние |
|---|---|
| Сессия не закрывается с непринятыми delegated | СТОИТ: session-handoff (D-0050) вечером + SessionStart «OPEN DISPATCH» утром (closes:-токены, t-197) + Boot Report правило 6 (DIRTY/UNPUSHED = находка) |
| Драфт не держит open questions, закрытые решением | ЧАСТИЧНО, записанное решение: чек 24 (F-41, прямой+обратный проход) еженедельно; staleness-метод доказан чувствительным (t-321, 2 исторических кейса). Пер-коммитного хука НЕТ сознательно: для ШТАБНЫХ доков дефолт D-0063 (промоция по лику) в силе — D-0098-переворот касается продукта; детектор назван (чек 24) |
| Приёмка не цитирует неверный basis | СТОИТ КОДОМ: journal_validator (пре-коммит) + journal_echo (момент записи) — матрица D-0058, пары-легальности B7 (t-322), unknown-by отказ (t-323, порт семантики AO3 30e79c8) |
| Handoff не заявляет зелёные тесты без свежего прогона | СТОИТ КОДОМ: main_gate (Stop-хук) — блок завершения main-хода при правках без зелёного прогона ПОСЛЕ последней правки (свежесть структурная, не заявительная); + witness = дословный вывод (D-0052), критик перепрогоняет дешёвое (правило 12) |
| Смена модели/яруса детектится и журналируется | СТОИТ: D-0056 (оба конца + внешняя сеть чек 5), journal_echo TIER-слой, D-0083 (замер фактических моделей транскриптов воркеров на worker_ref), hook MODEL-строка = декларация (D-0075) |

## Пункт 4 — (b)-класс: недостающие артефакты

Единственная реальная (b)-дельта промпта — коллизии параллельных
owns и post-batch сверка факта: строится СЕГОДНЯ слоем 1 (t-332:
tools/owns_gate.py warn-first + tools/owns_verify.py + общая сверка
+ тесты; критик-гейт t-333; норма R4-owns на пишущих узлах DAG —
коммит 93047f8). Полный планировщик непересекающихся батчей —
сознательно НЕ строится (Rule #1), reopen-триггер Phase 2 стоит.

## Пункт 5 — мета-гвард «решение без enforcement-плана не коммитится»

УЖЕ СТОИТ: commit-msg гейт (.githooks/ + tools/mechanism_gate.py)
отвергает механизм-коммит без осевого блока и tier-строки
(D-0055/D-0072); вопрос (в) «где зарегистрирован детектор отказа» и
(г) «что стоит на пути исполнения» обязательны В ТЕКСТЕ (D-0064) —
это и есть enforcement-план; смысл заполнения аудирует чек 8
(двухслойка D-0063: код гарантирует встречу, AI судит смысл).
Новая запись DECISIONS без плана не проходит гейт.

## Пункт 6 — прогон гвард-свиты по HEAD (этим ходом, D-0095)

- tools/journal_validator.py logs/routing-log.jsonl → OK (exit 0).
- tools/parity_check.py --check → 50 пар: 40 CLEAN; 7 HQ-DRIFT —
  все ОЖИДАЕМЫ (записанные строки порт-очереди минора, легальны по
  D-0074); 0 KIT-DRIFT (подозрительных); 3 BOTH-DRIFT — все
  записанные диспозиции (journal_validator-пара t-323 07-28 после
  порт-базлайна 07-24; CLAUDE.md-пара — KIT-DRIFT-диспозиция
  07-28); ERRORS 0.
- Штабной wiring — OK строкой SessionStart этой сессии (git hooks
  pre-commit+commit-msg, 9 harness-хуков импортируемы). Примечание
  метода: вызов tools/wiring_check.py напрямую — промах вызова
  (файл — кит-поставка t-326; штабная проверка живёт SessionStart-
  хуком), не отсутствие проверки (гигиена п.6).
- Канонический python -m pytest tools/ gateway/ -q → 1625 passed,
  34 warnings in 206.90s (дословный хвост прогона).

НАРУШЕНИЙ СВЕРХ ЗАПИСАННЫХ ПОРТ-СТРОК НЕ НАЙДЕНО → «чинить
делегированными билдерами с критик-ревью» (п.6 промпта) нечего;
делегированный билдер этого хода занят единственной реальной
дельтой (слой 1, t-332).

## Итог

Промпт: 4 пункта из 6 — уже стоящие механизмы Phase 4 (с
доказательствами выше), 1 пункт — записанное решение с named-
детектором (пер-коммитный staleness-хук не строится до лика), 1
пункт — реальная дельта, исполняемая сегодняшним батчем слоя 1.
Материал чека 23 №5: инсайт-промпты полезны как внешняя линза, но
исполняются ТРИАЖЕМ против стоящего, не буквально.
