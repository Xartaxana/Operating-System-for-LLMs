# Превентивный аудит: актуальность документов + карта enforcement (2026-07-24)

Заказ оператора (сессия 07-24): «все ли документы актуальны; все ли
утверждения имеют проверку кодом — не ждём утечки, ищем где бы она
могла произойти в принципе». Осознанное отступление от дефолта D-0063
(промоция по утечкам) словом оператора: деливерабл — карта, решения о
промоции — по пунктам, за оператором/Lead.

Материал: t-312 (ядро политики), t-313 (актуальность), t-314
(роли/скиллы/PROCESS + хвост чеков 23–32), t-315 (кит) — приёмки в
журнале с спот-сверками Lead. Сверено с живым состоянием после батча
t-308..t-316 этой же сессии.

## 1. Актуальность документов

**Верифицировано живым:** счёт решений 97/97 (DECISIONS.md ↔
DECISIONS_FULL.md); все конкретные пути осей SIBLING_MAP существуют
(включая D:\AO3_tests и exam_fullgates_kit); ROADMAP/фазы
консистентны; внешние якоря (теги, коммиты) живы; WHITE_PAPER v0.2.2
согласован календарно.

**Протухания (все — CURRENT_CONTEXT; перепись батчем на handoff
этой сессии):**
1. строка «(3) gate_log без ts/agent_id» — оба поля пишутся с t-278
   (dod_gate.py:363–376);
2. «(6) journal_echo ts-drift замер завышен ~+31 мин» — атрибуция
   t-263 была ошибочной, класс закрыт t-277/t-279 (доказательство:
   77/77 в test_journal_echo_tsdrift.py, подтверждено B2);
3. строка порт-очереди «critic-verdict (t-259)» — порт исполнен
   t-274 (49a828b), сегодняшний parity-«дрейф» был ложным
   CRLF/LF-классом;
4. блок «ОЧЕРЕДЬ ГЕЙТ-КАСАНИЯ» (1)–(5) читается открытой работой —
   закрыт t-278, доказано регресс-батареей t-308 (163 passed);
   пункт про битый config.yaml тоже был закрыт t-278 (локальный
   try/except в quota_lines — подтверждено B2).

**Кит:** обещание «documented separately» (Two Vocabularies) закрыто
сегодня (B4, toolkit/docs/TWO_VOCABULARIES.md); wiring-механизм
D-0092 был обещан леджером БЕЗ кода в поставке — закрывается B3
(toolkit/tools/wiring_check.py); INSTALL.md не несёт шага
инстанцирования escape_allowlist.json из template (fail-closed
удержит, но хост встретит его без подсказки в INSTALL — мини-пробел);
ссылки на генерируемые SCOUT_GOLDEN_SET/CRITIC_EXAM — живые по
дизайну, битых ссылок нет.

## 2. Карта enforcement (сводка; полные таблицы — в notes приёмок t-312/t-314/t-315)

**Слой A — gate-блоки (код на пути, не обойти):** манифест owns/given
и DoD-маркеры и модель-в-лейбле диспатча (dispatch_gate, exit 2);
осевой блок + tier-строка механизменного коммита (mechanism_gate,
commit-msg); зелёный прогон после последней правки (dod_gate
SubagentStop / main_gate Stop); шелл-запись в журнал = deny
(hygiene_gate класс (г), VG-5).

**Слой B — валидатор на коммите (pre-commit, exit 1):** весь
журнальный контракт — типизированные поля, witness у
builder-accepted, worker_ref, attempt/failure_class, append-only,
ts≤now+10мин (F-29), task_id-новизна/lifecycle, матрица D-0058,
basis judge только leaf (R13); escape_check (пины решений +
JUDGE_SYSTEM_PROMPT sha256).

**Слой C — WARN-обходимое (сигнал есть, реакция не форсится):**
гигиена пп.3–5 (cd/2>&1/python -c); negative_lint (негатив без
контроля, активирован 07-24); journal_echo эхо-слои
(TIER/WITNESS/ts-drift); MODEL/BOOT BUDGET/OPEN DISPATCH/WIRING
строки SessionStart.

**Слой D — недельные чеки калибровки (постфактум, окно):** R3-порог
критика (чек 2); R6-эскалация (чек 3); MISMATCH моделей (чеки 4/5/7);
приёмка сверху (чек 6); гигиена/permissions (чек 25); живость гейтов
(чек 26); F-49 финал-сообщения (чек 29); leaf-дисциплина и
галлюцинации судьи (чек 30); F-50 персистентность деливерабла
(чек 31); порт-дельта ↔ леджер (чек 32); правки роль-файлов без
прогона наборов (чек 14); свежесть навигационных доков (чек 24 — сам
этот аудит есть его ручная широкая форма).

**Слой E — дисциплина без машинного слоя (искомые «где могло бы
протечь»; ранжировано оценкой Lead):**

| # | Норма | Сегодняшний держатель | Возможная машина (цена) |
|---|---|---|---|
| E1 | Эскалация ПОСЛЕ 2 rejected — ничто не мешает 3-му ретраю тем же ярусом | чек 3 (неделя) | ДЁШЕВО: journal_echo/validator — attempt≥3 без escalated выше = WARN/блок на записи; детерминированно |
| E2 | Пересечение owns ПАРАЛЛЕЛЬНЫХ диспатчей никем не сверяется | дисциплина Lead (R4) | СРЕДНЕ: dispatch_gate копит owns сессии в state, WARN при пересечении |
| E3 | R1/R8: самоисполнение без dispatch_skipped — событие никто не требует | чеки 1/22 (неделя) | суждение «это была задача яруса» машине недоступно (D-0063-инверсия); осознанно оставить слою D |
| E4 | R5 плоскость: builder-роль имеет All tools — технически может спавнить | дисциплина + ревью | СРЕДНЕ-ДЁШЕВО: сузить tools билдера в role-файле (убрать Task/Agent) — конфигурационный фикс, не код |
| E5 | Критик-на-плане (R3) до старта кода — только постфактум чек | дисциплина | суждение; оставить + чек 2 |
| E6 | closes:-обязанность закрытия — открытые видны SessionStart'ом, но закрытие не форсится | WARN-слой + handoff | оставить: форсить закрытие механически = ложные закрытия |
| E7 | Пререгистрация экзам-ключей ДО диспатча | дисциплина протоколов оси 8 | оставить (чеки 14/30 ловят следствия) |
| E8 | D-0089 retro-пары / D-0079 каденция журнального батча | дисциплина | частично держит journal_echo (F-29, staleness); достаточен |
| E9 | Аналоги D-0043 у воркеров («докладывай замеченное») | ревью-слой | не машинизируемо; критик-чек классовой полноты остаётся |
| E10 | «По слову оператора» (kit-release, boot-diet, батчи D-0074) | дисциплина | НЕ машинизировать: авторизация оператора — не машинный факт |
| E11 | Хост-сторона кита: проба живости D-0093, hook-конфиг своей рукой, строки леджера | INSTALL/онбординг-текст + кит-чек 22 | wiring_check (B3) закрывает врезку/моды/adopt-строки; проба остаётся ручной по существу |

**Замечание метода:** сегодняшняя сессия сама дала живые примеры
работы слоёв C/D: F-29-будущее в ts поймано journal_echo на записи,
WITNESS ECHO staleness — предупреждением, дефект спеки диспетчера —
возвратом билдера (чек-23-класс), ложные негативы разведки — судьёй и
спот-сверками. Карта сходится с наблюдаемым поведением.

## 3. Решения, ожидающие слова (кандидаты промоции из E)

Рекомендация Lead: E1 (эскалационный WARN на attempt≥3) и E4 (сузить
tools builder-роли) — дёшевы и детерминированны, стоят исполнения;
E2 — по первому реальному инциденту коллизии (reopen-триггер (а)
Phase 2 уже стоит в очереди); E3/E5–E10 — осознанно «held by
discipline» с НАЗВАННЫМИ детекторами слоя D (форма D-0064
удовлетворена). E11 закрыт частично B3 (wiring_check), остаток —
текстовые шаги INSTALL (escape-шаг) в следующий кит-минор.
