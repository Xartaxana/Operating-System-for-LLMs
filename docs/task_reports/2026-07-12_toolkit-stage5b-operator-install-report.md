# Toolkit стадия 5б — установка в существующий проект оператора (D:\Dog)

**Дата:** 2026-07-12 (установка 00:26–01:49, приёмка той же ночью).
**Исполнитель установки:** оператор сам, по собственному решению
(«я сама поставлю, так будет реалистичнее») — аналог протокола
незнакомца 5а, вторая реалистичная точка валидации D-0070 стадии 5.
**Приёмка:** Lead (Fable), эта сессия. **Вердикт: ACCEPTED.**

## Спека стадии (зафиксирована до установки, коммит e93feb8)

Вход: github.com/Xartaxana/Supervised-Delegation (снимок f91fb31),
INSTALL.md Path B + onboarding-скилл. Lead в установку не
вмешивается. Критерии приёмки:

1. Path B на существующем проекте;
2. onboarding пройден — привязка моделей + вступительные экзамены;
3. первый делегированный цикл;
4. список находок/спотыканий и отступлений от INSTALL.md.

## Исполнение (по транскрипту установки, дайджест t-067 + точечные
сверки Lead)

Проект: D:\Dog — существующий (obsidian-vault, telegram-скрипты,
command-center), не полигон. Сессия установки: claude-sonnet-5
(вся, 154 assistant-записи) — т.е. заодно живой тест матрицы «Роль
≠ ярус» на Mid-tier координаторе.

Хронология: чтение INSTALL/README с GitHub → копирование Path B
(полный список путей) → замена {SET_AT_INSTALL} реальным временем
(00:56:14) → `git config core.hooksPath .githooks` → onboarding:
контур subscription, привязки lead=claude-fable-5 /
critic=claude-opus-4-8 / builder=claude-sonnet-5 /
scout=claude-haiku-4-5 → scout-exam-gen + critic-exam-gen
(сгенерированы под проект) → экзамены → Boot Report → штатный STOP
по протоколу.

Экзамены: scout — PASS 7/7. critic — run 1 FAIL (пропустил
обязательную ловушку T-K3 «непроверенный негатив» в отчёте
сдающего), run 2 с добавленной в диспатч подсказкой «verify
negative claims independently» — PASS; решение «оставить opus-4-8»
записано в их DECISIONS.md как D-0001 с образцовой честностью:
hinted-pass явно помечен как не доказывающий unprompted-поведение.
Экзаменационный инструмент отработал по назначению на чужом репо:
ловушка поймала реальную слабость, а не проштамповала PASS.

Первый делегированный цикл (критерий 3): состоялся ПОСЛЕ Boot
Report, по реальной задаче оператора (обзор симметрий проекта —
живой прогон дизайна F-35-фикса): диспатч scout 23:41Z → дайджест
парных структур → точечная сверка сессией несущих утверждений по
конфигам (поведение приёмки по следу) → использование результата
(оси 3–7 карты). Цикл доказан транскриптом (первоклассная
телеметрия, D-0034), НЕ журналом — события delegated/accepted не
записаны (находка (б) ниже). Решение приёмки: критерий закрыт по
транскрипт-evidence; требование повторить цикл «с журналом» дало бы
подсказанное событие — тот же класс, что hinted-pass критика, — а
неподсказанное поведение уже измерено и записано находкой.

Чек-лист артефактов (сверен t-067 + Lead): delegation.config.yaml
заполнен; placeholder журнала заменён; hooksPath настроен; оба
экзаменационных файла сгенерированы; journal_created валиден.
Установка на момент приёмки не закоммичена в их репо — код-гейты
шаблона (пути коммита) ещё не встречались с работой.

## Находки стадии

- **F-35** (до установки, вопрос оператора): onboarding не собирает
  оси симметрии принимающего проекта; дизайн фикса скорректирован
  вторым словом оператора (scout-обзор по умолчанию, оператор —
  судья; вопрос оператору — только про невидимое изнутри репо).
- **F-36** (наблюдение оператора за сессией): Sonnet-координатор
  назвал решение об осях «Lead-tier judgment» и исполнил его сам —
  распознавание сработало, ПЕРЕДАЧА отказала: у Lead-очереди в
  шаблоне нет носителя/события/кода. Оси 3–7 вписаны Mid-tier'ом;
  правка карты — вдобавок механизм.
- **(а)** Ретрай экзамена не регламентирован: ретрай с подсказкой
  засчитан как PASS (честность их D-0001 спасла данные, но шаблон
  правила ретрая не даёт).
- **(б)** Обязанность журнала утекает в свежей установке (класс
  F-9, три экземпляра): экзаменационные диспатчи и scout-обзор
  прошли без delegated/accepted; журнал держит одну строку.
- Мелкое, не в счёт шаблону: gh CLI без аутентификации (инструкция
  его не требует); один сбой Edit-тула при записи результата
  экзамена (сессия восстановилась сама).
- Отступлений от INSTALL.md по существу нет.

## Итог и хвосты

Стадия 5 D-0070 (валидация ДВУМЯ установками) закрыта: 5а — пустой
проект (2026-07-11, t-055 + повтор t-064), 5б — существующий проект
(этот отчёт). Батч фиксов находок 5б (F-35 + F-36 + (а) + (б)) — в
очереди CURRENT_CONTEXT; рекомендация Lead: прогнать батч и третий
релизный снимок ДО раздачи шаблона желающим (стадия 6). Следующая
задача по приказу оператора 2026-07-11 — A3 (манифест контекста
диспатча) выделенным проходом.
