# Триаж внешнего ревью 2026-07-13 (Lead, 2026-07-14)

Источник: docs/task_reports/2026-07-13_external_reviewer_report.md
(12 находок + 7 продуктовых идей). Приёмка по дисциплине D-0046:
внешнее ревью = информационная работа, несущие утверждения сверены
точечно, не на доверии.

## Сверка несущих утверждений (4/4 подтверждены)

1. `gateway/shadow_eval.py` пишет легаси-статус `validated`
   (строки 366–367, 453) и имеет `--update-table`, способный внести
   его в DELEGATION_TABLE.md — словарь вне 4-статусной модели
   (D-0035). ПОДТВЕРЖДЕНО grep'ом.
2. `gateway/guard.py` не резервирует текущий запрос (нет
   max_tokens/reserve в admission-логике). ПОДТВЕРЖДЕНО негативным
   grep'ом с позитивным контролем трубы (budget: 40 вхождений тем же
   вызовом; F-30).
3. `gateway/metrics.py` держит `VALIDATED_DELEGABLE_CATEGORIES`
   frozenset'ом — ручное зеркало таблицы. ПОДТВЕРЖДЕНО (строка 62).
4. Дрейф toolkit/ от корня — известен и записан у нас же
   (порт-очередь CURRENT_CONTEXT, 12 пунктов; чек 12 калибровки).

## Вердикты

### БЕРЁМ СЕЙЧАС (builder-класс, дёшево, защищает экономические
### утверждения — «маленькие скучные механизмы» ревьюера)

- **Находка 2 (словарь `validated`)** — единственный немедленный
  кандидат. Рекомендация Lead: НЕ чинить `--update-table`, а УБРАТЬ
  запись в таблицу из shadow_eval вовсе (вердикт остаётся в отчёте
  прогона): по Update Rule 1 и практике статусы двигает ТОЛЬКО
  калибровка с evidence — кодовый путь записи в таблицу это
  дисциплинарная дыра класса D-0063 (детерминированно решаемое
  «кто пишет статус» должно быть закрыто кодом). Заодно закрывает
  находку 11 (--update-table без привязки к калибровке судьи) без
  единой новой строки конфигурации. Ждёт слова оператора на диспатч.

### УЖЕ СДЕЛАНО / УЖЕ В ОЧЕРЕДИ (ревью подтверждает наш бэклог;
### деталями обогащаем существующие пункты, новых задач не открываем)

- **Находка 1 (guard не резервирует текущий запрос)** — пункт
  очереди «t-018 wall admission-math» (evidence-gated). Спека
  ревьюера (резерв в SQLite транзакцией, quota groups, fail-closed
  для экзаменного трафика) — приложить к пункту как готовый черновик
  спеки при срабатывании триггера. Частично покрыто: preflight-гейт
  экзаменов после F-27 уже меряет все базы и спрашивает провайдера.
- **Находка 3 + идея «release discipline» (дрейф toolkit)** — D-0074
  мораторий + порт-очередь + чек 12 уже есть. НОВОЕ берём в DoD
  следующего порт-батча: (а) hash-сверка корень↔toolkit по core-
  файлам + манифест намеренных расхождений с причинами; (б) правило
  «не публиковать снимок при непортированных экономических фиксах» —
  уже наша практика (батч после Phase 2), теперь явной строкой.
- **Находка 4 + идея «delegation opportunity report» (позднее
  обнаружение самоисключения Lead)** — совпадает с кандидатом
  повестки калибровки ~07-18 (скип-порог числом, чеки 19/21/22).
  Добавить на тот же разбор: сессионный отчёт-кандидат (диспатчи/
  скипы/подозрения на пропущенное делегирование) и строку-детектор
  в SessionStart/Boot Report. Решение по Rule #1 — на данных чеков.
- **Находка 5 + идея «API-worker harness» (API-контур не продукт)** —
  осознанно отложено (D-0029/D-0030, гейт Router закрыт R1).
  Порядок постройки ревьюера совпадает с нашим (scout read-only →
  builder по спеке → witness wrapper A5 → журнал → приёмка → только
  потом Router). Расхождений нет, новых задач не открываем.
- **Находка 9 (квоты по ключу, не по алиасу)** — класс F-27;
  экзаменный preflight уже суммирует. Runtime quota groups в guard —
  триггер: первый 429 от разделённого ключа на живом прогоне.
- **Идея «retry-loop accounting»** — уже живёт: Update Rule 4,
  поля attempt/failure_class, тренд цены за принятую единицу
  (чеки 10/11), reject-тренд coding→Middle (4 подряд) — статус
  двинет калибровка. Совпадение, не новая работа.
- **Идея «confidence and sample quality»** — большей частью уже в
  stage-2: distinct-промпты (оговорка 11/19 в логе), truncated-
  счётчик (t-091), unjudged-наблюдение (F-40/C3). Остаток
  (minimum-n / pass^k, CI) — уже в очереди «Eval plan stage 2»,
  пороги с данных калибровки.

### КАНДИДАТЫ ПОВЕСТКИ КАЛИБРОВКИ ~07-18 (решение по Rule #1 там)

- **Находка 8 (реестр цен захардкожен)**: staleness-чек цен +
  версионированный реестр с датой/источником. Дёшево, защищает
  D-0032; провести через 4 вопроса на разборе.
- **Идея «economic routing margin»**: формула
  expected_savings ≥ coordination+judge+retry+margin на КАТЕГОРИЮ —
  у нас уже есть входные данные (экзамен экономии деплоя, прогон №1:
  $0.96/$2.60/$3.01 за принятую). Кандидат в Update Rules таблицы.

### В ПОРТ-ОЧЕРЕДЬ ТУЛКИТА (D-0074; только батчем по слову оператора)

- **Находка 6 + идея «privacy modes» (сырые prompt/response в
  SQLite)** — реальный барьер адопции; для нашего dogfooding-репо
  риск мал, для внешних пользователей — нет. В порт-очередь п. (13):
  режим GATEWAY_LOG_RAW_TEXT=false + fingerprint для метрик повторов
  + TTL/purge + предупреждение при старте. Дефолт (Safe telemetry?)
  — продуктовое решение ОПЕРАТОРА (класс В-решений пакетирования).
- **Находка 12 (onboarding-пробы моделей)** — детерминированная
  валидация при онбординге (пробный запрос, матрица способностей,
  отказ завершать онбординг с пустыми api-полями). Хорошая спека
  для onboarding v2 тем же батчем.

### НЕ БЕРЁМ СЕЙЧАС (записано, чтобы не перетирать)

- **Находка 7 (delegation_table.yaml как машинный источник)** —
  Rule #1 против: frozenset уже под детектором (чек 12), генерация
  markdown из yaml добавляет сборочный шаг в артефакт, который
  читают люди и политика. Триггер пересмотра: первый ЗАПИСАННЫЙ
  инцидент дрейфа frozenset↔таблица, пропущенный чеком.
- **Находка 10 (config_check.py на старте)** — часть проверок уже в
  тестах/калибровке; отдельный стартовый валидатор — на evidence
  первого инцидента конфиг-дрейфа (D-0063: продвижение в код по
  утечкам, не для симметрии).

## Замечание к контексту ревью

Ревью датировано 2026-07-13 (372 теста); к 2026-07-14 корень уже
ушёл вперёд: F-39 (max_tokens/truncated в shadow_eval, t-091),
F-42/F-43 (usage_report глоб+дедуп, t-093/t-094) — примеры дрейфа
из находки 3 частично описывают именно эти непортированные фиксы,
что подтверждает нашу порт-очередь, а не опровергает её.
