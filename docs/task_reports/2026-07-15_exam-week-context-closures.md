# Архив закрытий экзаменационной недели (boot-diet раунд 10, 2026-07-15)

VERBATIM-перенос закрытого экзаменационного повествования из
CURRENT_CONTEXT.md (D-0038/D-0068; условие оператора «сначала закроем
всё по экзамену» наступило — решения приняты, t-126/t-127 приняты).
Полные разборы: docs/tasks/2026-07-14_economy-exam-run2.md,
docs/tasks/2026-07-15_economy-exam-runs3-4.md,
docs/QUALITY_MEASUREMENT_PROPOSALS_2026-07-15.md; Runs log — 6 строк
в PROCESS/DEPLOYMENT_ECONOMY_EXAM.md. Живые остатки (очереди
window_load/calibration_counts/AO3, порт-очередь, задача набора №2)
НЕ переносились — остались в CURRENT_CONTEXT.

---

  ОСТАЮТСЯ (свои гейты): п.9 ретро-контур (после >=1 прожитого
  прогона ~07-18); п.10 экзамен экономии — прогон №2 ЗАКРЫТ 07-14
  вечером: все 9 вердиктов финальны (A 3/3 $0.93 / B 3/3 $5.19 /
  C 3/3 $3.26; A-T1 и A-T2 — вердиктами оператора), строка Runs log
  внесена, чек 14е закрыт для v0.2.0; итоги —
  docs/tasks/2026-07-14_economy-exam-run2.md. КОНТРОЛЬНОЕ B0
  ЗАКРЫТО 07-14 22:22 (3/3, $4.94/принятую) — КОНФАУНД РАЗРЕЗАН:
  дизайнер денежно нейтрален (B−B0 = +5%, шум; метрика 2 пилота
  получает точку «не вдвое дороже»), удвоение B к №1 воспроизвелось
  БЕЗ дизайнера. ДЕКОМПОЗИЦИЯ удвоения (разбор по ходам, t-116):
  фикс-налог релиза ≈ +16% (t2-контроль; CLAUDE.md +9.5%), основное
  ~+$5 — два reject-цикла ходами Fable-координатора (вариативность
  воркеров, не релиз); повтор прогона разделит окончательно (медиана
  2–3). НОВЫЙ ПУНКТ ПОВЕСТКИ калибровки (вопрос оператора ~00:05
  «реджекты съедают все деньги?»): стоимость ретраев как ДОЛЯ
  расхода — по ярусам и новизне среды (журнал корня: rejected 27/169
  всей истории, 16%: capability 10 / tooling 9 / spec 6 / recon 2;
  экзамен: B0 2/8 при 0/10 у B той же ночью — дисперсия; cc_usage
  атрибутирует цену циклов). Гипотеза на проверку: реджект-риск
  коррелирует с ХОЛОДНЫМ СТАРТОМ (незнакомая среда, greenfield-код),
  не со сложностью задачи — вход в тезис «выигрыш растёт с
  размером». Правило «отчёт — финальная сдача, недоделанное не
  сдаётся» ВНЕДРЕНО в корне словом оператора 07-14 (~23:55):
  builder.md п.7 (прецедент version race B0-t3). ОЧЕРЕДИ класса (оси 1/3/6):
  AO3 — их builder.md + 11 пишущих QA-агентов конвейера (внести их
  сессией или Lead при следующем касании AO3, строка обязательна);
  тулкит — порт-очередь п.16 (мораторий D-0074). Детектор утечки —
  приёмка D-0052 (независимый перепрогон witness) + чек 13. На разбор калибровки ~07-18 также: ПОПРАВКА
  СКОРОСТИ («дрейф базлайна» отозван — смешение методов замера;
  одним методом A/C без изменений, B ×2.1 с троттлинг-долей — дыры
  >60s, B №2: 16 мин на плечо при параллельной Lead-нагрузке);
  кандидат правки протокола — пин метода метрики 4 + параллельность
  окна ЗАМЕРОМ cc_usage, не декларацией (прецедент: два ложных
  «чистых окна» за вечер — «ночь свободна» и «B0 без нагрузки», оба
  вскрыты оператором, класс F-30 у Lead; в окне B0 реально работала
  фабрика AO3 — 38 ходов); кандидат смены каденции экзамена (слово
  оператора 07-15 ~00:10, формулировка оператора): сессии плеч —
  SONNET-КООРДИНАТОР (первый живой тест штатного режима матрицы
  D-0058, до сих пор бумажного), ЛИД ОСТАЁТСЯ FABLE — спорные
  эскалации/Lead-класс диспатчатся агентом model=fable по мере
  нужды («пользователи никогда не поставят лидом модель ниже
  исполнителей»); переход — через мостовой прогон обеих моделей на
  одном релизе (раннер t-117 делает это дёшево), Fable-прогон
  остаётся якорем мажорных релизов; решает разбор.
  МЕХАНИЗМ-КАНДИДАТ на разбор (дизайн оператора 07-15 ~02:35-02:50,
  формулировка уточнена его словом): ВОПРОСЫ ХОДЯТ ВВЕРХ, РАБОТА —
  ВНИЗ; СКИП-ЛЬГОТА НАПРАВЛЕННАЯ — ТОЛЬКО ВНИЗ. Вершина иерархии —
  ПОЛЬЗОВАТЕЛЬ (выше лида): интерпретация недоопределённого
  намерения — вопрос пользовательского уровня, решать его сам не
  вправе НИКАКОЙ ярус, включая Fable («правильно спросить, какой
  калькулятор хочет пользователь, а не какой выдумает Fable»;
  прогон №1 — прецедент: оба инфраструктурных плеча решили за
  пользователя). Пропустить диспатч НИЖЕ своего яруса можно (с
  событием); вопрос ВЫШЕ своего уровня решить/поглотить нельзя
  никогда — только эскалация. ДЛЯ ЭКЗАМЕНА (headless, пользователя
  нет): вопрос искусственно перенаправляется Fable как прокси
  пользователя — экзаменационный протез, фиксируется вариацией
  условий, в общую политику НЕ входит. ПРОТЕЗ ИСПЫТАН 07-15 (оба
  B-t1: эскалация исполнена, каскад дошёл до конца). ЧЕРНОВИК
  ПРОД-ТЕКСТА к разбору (по схеме оператора 07-15 ~04:40): (1)
  неясные ТРЕБОВАНИЯ — вопрос ПОЛЬЗОВАТЕЛЮ, работа участка стоит до
  ответа, решать за него запрещено любому ярусу; (2) два rejected на
  ярусе — эскалация исполнения ярусом выше (правило 6, уже работает
  — прецедент B-t1 №3: critic доделал), ярусы исчерпаны — задача в
  ОЧЕРЕДЬ ЛИДА событием escalated; (3) только для headless, явной
  строкой условий среды: пользователь недоступен — вопросы п.1
  эскалируются агентом лид-модели как ПРОКСИ, вердикт-прокси
  помечается в журнале (не выдаётся за слово пользователя).
  Данные серии к гипотезе «матрица рождает скромность»:
  B-t1 №3 (sonnet+v0.2.0) спросил пользователя — первый за все
  прогоны; A-t1 №3/№4 (sonnet без политики) и B-t1 №4
  (sonnet+v0.1.0) решили молча — связка направленная, но
  недетерминированная, нужен n. Внедрение — ПОСЛЕ серии (не менять
  измеряемый объект посреди замера); дом — политика корня +
  порт-очередь тулкита. ТУДА ЖЕ — ДЫРА, вскрытая вопросом оператора
  07-15 ~02:40 («мы вроде запретили скип сонету — проверь»): запрет
  скип-льготы Sonnet есть в ОБЕИХ версиях шаблона дословно, но
  прецедент B-t1 №4 обошёл его нулём — dispatch_skipped(builder) +
  самоисполнение + сдача пользователю ВООБЩЕ без приёмки (нет ни
  accepted, ни критика: матрица гейтит приёмку чужой работы, а
  самоисполненная координатором в приёмку не попадает). Кандидат-
  правило: самоисполненное после dispatch_skipped проходит ту же
  приёмку, что builder-дифф (Sonnet — critic-вход или очередь лида;
  сдача непринятого — нарушение). СЛОВО ОПЕРАТОРА
  07-15: ПРОГОНЫ №3/№4 ВЫПОЛНЕНЫ И ЗАКРЫТЫ (~04:25) раннером на
  Sonnet-координаторе; обе строки Runs log внесены; сводный лист с
  таблицами/рубрикой/матрицей 2×2 —
  docs/tasks/2026-07-15_economy-exam-runs3-4.md. ГЛАВНОЕ ЧИСЛО: B на
  Sonnet×v0.2.0 = $2.38/принятую — дешевле всех B-клеток при полном
  процессе (Fable×v0.2.0 = $4.94). Вариации: C-клетки — перезапуски
  после 2-го дефекта t-117 (cmd.exe резал \n-аргумент шима — C
  получали только префикс; фикс stdin + реальный смок); B-t1 —
  перезапуски с протезом эскалации fable-прокси (каскад прожит
  целиком: 2 reject → доработка критиком по правилу 6 → приёмка
  fable; ходы fable видны в учёте). ОТКРЫТЫ 2 условных вердикта
  оператора: A-t1 №3/№4 — артефакт-ссылки живы только из аккаунта
  (публично 404); при отказе — пересчёт строк A. Бэклог раннера:
  полный stdout в run_log, скоуп test-discovery, LIKE-слаги
  (под-проекты вида -click), предупреждение про артефакты.
  ГЛАВНЫЙ ВЫВОД СЕРИИ К РАЗБОРУ (вопрос оператора 05:00, ВЫЗОВ
  ТЕЗИСУ): на ~10-мин задачах заборы качества B по ИЗМЕРИМОЙ рубрике
  6а не превосходят workflow-плечо C (№2/№3 — ничья, №1/№4 — B чуть
  выше), но стоят в 1.3-1.5× дороже — Rule #1 не выполняется на этом
  размере. Оговорки: рубрика груба + тест C асимметрично мягкий
  (нужен инструмент качества тоньше); C выигрывает частью меньшей
  амбицией (кнопочный UI vs CLI-парсер); заборы покупают неоценённое
  (evidence-in-git — у C отчёты умерли 3×, caught-before-ship —
  реальные дефекты). СЛЕДУЮЩИЙ ЭКЗАМЕН — на КРУПНОЙ многосессионной
  задаче (кроссовер окупаемости выше 10 мин). Разбор —
  docs/tasks/2026-07-15_economy-exam-runs3-4.md «ГЛАВНЫЙ ВЫВОД».
  ПРЕДЛОЖЕНИЕ тонкого измерения качества (заказ оператора 07-15
  ~05:15) — docs/QUALITY_MEASUREMENT_PROPOSALS_2026-07-15.md:
  многоосевой ТАБЕЛЬ вместо шкалы 6а (корректность / полнота-против-
  интента / качество-тестов-мутационно / персистентность-evidence /
  аудируемость / стоимость-на-качество); ключевое — разнести
  «способный-но-хрупкий» и «простой-но-безопасный» по РАЗНЫМ осям
  (6а схлопнула в 0); слепая оценка + предрегистрация батарей +
  судейская панель против асимметрии грейдера; ярусная стоимость
  инструмента. К разбору — механизм (ось 8), четыре вопроса при
  принятии.
  Класс «результат в чате» повторился и у
  нашего деплоя (B0-t2 план — в чате, файл-отчёта нет).
  Окно экзамена (18:53..19:14 UTC, замер cc_usage) — в notes
  калибровки (D-0075, тренды 11/18 без exam_release2); п.13
  privacy-режимы (дефолт — решение оператора); п.15
  permission-audit (t-107) — после проживания в корне; п.16
  builder-правило финальной сдачи (builder.md п.7, слово оператора
  07-14: недоделанное не сдаётся, после отчёта в owns не писать —
  прецедент version race B0-t3); п.17 правило 11а маршрутизации
  вопросов (D-0077). Мораторий
  D-0074 снова в силе: новых правок toolkit/ не делать — ось 7 в
  осевых блоках отвечается «в очередь порта» сюда.
- ЭКЗАМЕН — РЕШЕНИЯ ПРИНЯТЫ 07-15 БЕЗ ОТСРОЧКИ (слово оператора:
  экзамен НЕ привязан к срокам калибровки, оттачивается своим
  темпом; калибровка — запускатель готового): (1) табель 6б
  нормативно в протоколе (веса оператора A/B/C=1, D/E=0.5; 6а —
  историческая); (2) метод метрики 4 запинован + нагрузка окна
  замером; (3) headless-протез — штатная строка протокола;
  (4) каденция: раннер + Sonnet-координатор, Fable — якорь мажорных;
  (5) D-0077 внедрено (правило 11а); (6) бэклог раннера — t-126
  ПРИНЯТ 07-15 14:34 (critic: код чист; witness закрыт собственным
  haiku-смоком Lead). ОЧЕРЕДЬ от t-126 (ось 2, вердикт критика — не
  блокер): window_load — LIKE-исключение под-слагов (сейчас под-слаг
  песочницы попадает в «чужую нагрузку» посторонним проектом);
  инвариант «id задач/плеч без дефисов» (LIKE-граница) — ассерт в
  validate_manifest при следующем касании раннера;
  (7) валидатор-ветка замены воркера — t-127 ПРИНЯТ 07-15
  14:31 (critic ACCEPT, гейт не ослаблен адверсариально). ОЧЕРЕДЬ от
  миноров t-127 (ось 1, вердикт критика — не блокер):
  calibration_counts блок 5 — ветка «replacement» по маркеру
  replaces_worker (сейчас замена падает в «other» и шумит в отчёте
  калибровки); гайданс-строка в секцию журнала CLAUDE.md «маркер =
  голый ref без хвостовой пунктуации» (при касании политики/диетой);
  опционально — регресс-локи closed-task+маркер и self-reference.
  ОСТАЛОСЬ ПО ЭКЗАМЕНУ: батареи оси A + чек-листы оси B для
  СЛЕДУЮЩЕГО НАБОРА (пинуются при пиновке набора №2); НАБОР №2 —
  крупная многосессионная задача (кандидатов предложить оператору);
  ретро-иллюстрация табеля прогонов №3/№4 — в чате 07-15, в
  proposal-док по желанию. НА КАЛИБРОВКЕ от экзамена остаётся
  только: окна прогонов в notes (D-0075) + аналитика ретраев/
  холодного старта (копится журналом).