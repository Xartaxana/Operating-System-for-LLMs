# 2026-07-21 — день leaf routing: гейт-отчёт → серии D → открытие Router-гейта → D-0087/D-0088 (архив D-0038/D-0068)

Закрытые блоки CURRENT_CONTEXT, перенесены VERBATIM развёрткой
boot-diet на handoff 2026-07-21 (пробой 110.0К → развёртка).
Хронология дня — журнал t-236..t-255 и коммиты ef83b16..88408ba.

## Блок «ГЕЙТ-ОТЧЁТ ROUTER 2026-07-20» (superseded открытием гейта; решение — ROADMAP «Gate decision 2026-07-21»)

- ГЕЙТ-ОТЧЁТ ROUTER 2026-07-20 (слово «делай все 4, не жди
  калибровки»; НА ПОДПИСЬ АРХИТЕКТОРА). Числа против порогов:
  R1 ВЫПОЛНЕН: coding->Sonnet ~34 пар-инстанса / 5 прогонов (>=30/2);
  оговорка F-39 на 6 из 14 пар Fable->Sonnet. R2 (как записан) НЕ
  ВЫПОЛНЕН: validated-категории 0.01% real-спенда (порог 25%);
  структурно неисчислим на подписочном контуре (cc_usage без
  category/prompt), «coding 99.9%» — артефакт классификатора
  turn!=task. R3 формально met (0.02 п.п. < 10), вырожден
  (насыщение 99.9%). R4 (как записан) НЕ ВЫПОЛНИМ (следствие R2:
  экономия ~0). R5 ГОТОВ к подписи: платный Lead жив (ANTHROPIC-ключ,
  ~$185+ фактических трат по ценам листа), учётно-ценовое
  обоснование D-0032. ВЕРДИКТ-РЕКОМЕНДАЦИЯ: гейт В ЗАПИСАННОМ ВИДЕ
  остаётся ЗАКРЫТ (R2/R4 красные честно — «validated-денег на
  столе» нет, политика+привязки D-0085 уже сняли эту экономию);
  НАСТОЯЩИЙ приз — координационный блок (~$400/день, 85% спенда;
  чек 11: 1.27-1.7x к стоимости работ) — гейт его НЕ МЕРИТ.
  РЕВИЗИЯ КРИТЕРИЕВ ОФОРМЛЕНА (слово оператора «сделай п2» —
  D-0086, ROADMAP правлен, прежние формулировки VERBATIM в
  DECISIONS_FULL): R2' координационные накладные >=25% (замер ~85%
  — ВЫПОЛНЕН); R4' — предрегистрированные D-ключи экономэкзамена.
  Статус гейта после ревизии: R1+, R2'+, R3+, R5 на подписи; R4'
  ждёт прогонов D-плеча (смета ниже) — открытие гейта = подпись
  R5 + слово на D-прогоны. Инструментальные вердикты
  дня: RouteLLM-bert ОТКЛОНЁН (AUC 0.60 на чистой ground truth,
  слеп к супервизионным рефлексам); кандидат D-плеча №1 —
  статическая allocate-таблица + judge-приёмка (кандидат №2
  haiku-классификатор — очередь следующей сессии). Смета D-плеча:
  3 прогона батареи ~$15-30 + грейдинг; запуск — по слову.
  Данные дня: экзамен №18/18b (A-sonnet 0.792 vs A-opus 0.875;
  конструкция > модель: +0.43 против +0.125), эскалационный корпус
  финально: Sonnet 3/3 eq на измеримых, Opus 2 eq + 2 worse, Sonnet
  дешевле везде; t-097 немерим одноходовым методом (>64K вывода).

## Блок «ЗАВТРА» (исполнен целиком 07-21)

- ЗАВТРА (авторизовано словом оператора 07-20 вечером: «Принято...
  гонять новое плечо будем завтра» + «значит будем проверять эти
  три варианта» с поправкой Lead «лестница с ранним выходом»):
  D-ПРОГОНЫ ЭКОНОМЭКЗАМЕНА. Порядок завтрашней сессии: (0) скамейка
  кандидатов №2/№3 на labeled_v2 ($0, до прогонов; см. пункт 2
  ниже); (1) D-плечо с кандидатом №1 (статическая allocate-таблица
  лестницы D-0085 + judge-приёмка) — 3 прогона батареи №1, медианы
  против предрегистрированных ключей R4' (D-0086); смета $15-30;
  (2) кандидаты №2/№3 в D-прогоны ТОЛЬКО по evidence-триггерам
  (статика провалила ключи качеством/misroute ИЛИ трафик шире
  батареи). ПОДПИСЬ R5 — снять с оператора одним словом при
  запуске (формально ещё не дана; «Принято» вечера = принятие
  плана). Гейт открывается: подпись R5 + взятые ключи R4'.

## Пункты дня 1–2 и скамейки (исполнены)

- ПУНКТ 1 СЛЕДУЮЩЕЙ СЕССИИ — ИСПОЛНЕН 2026-07-21: класс F-49
  «финал-сообщение воркера теряет содержательную часть» закрыт
  механизмом (правило «финал = отчёт ЦЕЛИКОМ, пересдача = заново» в
  builder.md п.8 / scout.md п.9 / critic.md п.15 + подсказка в
  BLOCK_MESSAGE dod_gate; находка docs/FINDINGS.md F-49; детектор —
  чек 29 калибровки; регрессии пар: SCOUT_GOLDEN_SET 7/7 PASS +
  CRITIC_EXAM PASS, t-237/t-238). Смежный пункт очереди №4 «пустые
  финал-сообщения воркеров ~200K+» закрыт тем же классом (чек 29
  считает и его). Сиблинги: AO3 — пункт в их HANDOFF (D-0082),
  кит — порт-очередь.
- ПУНКТ 2 — ИСПОЛНЕН 2026-07-21 (t-236, ~$0.04): causal_llm
  НЕПРОГОНЯЕМ средой (gated-токенизатор 401 + CUDA-хардкод; Rule #1
  — закрыт), haiku-классификатор ОТКЛОНЁН (AUC 0.5255 полный — хуже
  bert; ties 7/20 на экзамен-срезе). Волна-1 кандидатов закрыта →
  D-плечо со статикой (кандидат №1). Вердикты VERBATIM — план оценки
  (docs/tasks/2026-07-20_routellm-evaluation-plan.md).
- СЮРВЕЙ №2 + СКАМЕЙКА ВОЛНЫ-2 — ЗАКРЫТЫ 07-21 (t-240/t-242, $0):
  ВСЕ ТРИ кандидата волны-2 отклонены (semantic-router AUC 0.51 /
  misroute 28.6%; KNN/LogReg хуже случайности — n=21 мало;
  Arch-Router misroute 25% + 7/21 «other» + недетерминизм GGUF на
  temp=0). Итог обеих волн: 6/6 кандидатов закрыты — D-плечо со
  статикой БЕЗ LLM-резерва подтверждено дважды. Триггер
  переоткрытия: labeled-корпус >=100 судимых пар → повторная
  скамейка KNN-класса. Вердикты VERBATIM — план оценки.

## Подпись R5, серии D, открытие гейта, D-0087 (исполнены)

- R5 ПОДПИСАН ОПЕРАТОРОМ 2026-07-21 («подписываю R5, запускай
  D-прогоны») — платный Lead + учётно-ценовое обоснование D-0032
  приняты Архитектором. Статус Router-гейта: R1+, R2'+, R3+, R5+
  ПОДПИСАНЫ; открытие гейта = взятые ключи R4' (D-прогоны).
- D-ПРОГОНЫ ИСПОЛНЕНЫ 2026-07-21 (серия №1–№3 + грейдинги
  t-244/246/247; Runs log DEPLOYMENT_ECONOMY_EXAM + DAG в
  Darm-plan.md; реестр xlsx — t-248). ВЕРДИКТ ПРОТИВ КЛЮЧЕЙ R4'
  (НА СЛОВО АРХИТЕКТОРА): (а) ВЗЯТ — медиана D $2.08/прин <= C
  $2.60 (по-прогонно 2/3); (б) ВЗЯТ — медиана качества D 0.975 >=
  0.87, ВЫШЕ B_ref 0.92 при цене вдвое ниже B ($2.08 vs $3.89);
  (в) НЕ ВЗЯТ сырым замером — медиана 32 мин > 18 (последовательный
  сплит Dsn→Dhk = артефакт лаунчера; параллельная форма
  max(Dsn,Dhk) даёт медиану 17.5 <= 18, но это гипотеза, не замер).
  Развилка решена словом «делай 1»: МИНИ-СЕРИЯ D№4–№6 ИСПОЛНЕНА
  тем же днём (параллельный лаунч t-249, рамка предрегистрирована) —
  wall-clock {17.1, 17.3, 15.8}, МЕДИАНА 17.1 ≤ 18: (в) ВЗЯТ
  честным замером. ИТОГ: R4' ВЗЯТ ЦЕЛИКОМ ((а) $2.08<=C$2.60;
  (б) 0.975>=0.87; (в) 17.1<=18). ROUTER-ГЕЙТ ОТКРЫТ 2026-07-21 —
  ПОДПИСЬ АРХИТЕКТОРА в сессии («подписываю открытие Router-гейта»);
  блок решения — ROADMAP.md «Gate decision 2026-07-21». Первая
  задача гейта по D-0086 исполнена той же серией; ADOPT ИСПОЛНЕН ТЕМ
  ЖЕ ДНЁМ: D-0087 «Leaf routing» принят словом оператора — правило
  R13 в ядре, basis "judge" в валидаторе, детектор чек 30,
  порт-очереди кита/AO3 заряжены; гибрид-экзамен H+C разобран (пин
  2026-07-21_hybrid-exam-set2H-pin.md). Открытые слова оператора:
  вердикт дубля мая C (0.63↔0.60, ключей не двигает); переигровка
  C-календаря (~$3; Lead считает необязательной). Эксплуатационная
  точка D-0087 — калибровка №4 (~07-25, чек 30, первое окно
  judge-basis). Содержательный
  итог серий: судейский гейт трижды купил корректность t1, лестница
  стабильна, судейские циклы дешевеют (№5/№6: t2 принят haiku с
  первой подачи), C терял evidence; находки — галлюцинация судьи
  (кандидат judge-детектора, калибровке №4), прокси-аудит D-0083
  замером чист. Синтетика окон — пометка D-0075 калибровке №4.
  [Дозакрытие тем же днём после переноса: дубль оставлен как
  заграйжено, переигровка C-tb снята словом «оставь всё как есть»;
  D-0087 дополнен второй формой судьи (подписочный субагент, 13/13
  t-254), D-0088 — однопутная архитектура.]

## Блок «ВХОДЯЩЕЕ ОТ AO3 (07-20)» (закрыт портом v2)

- ВХОДЯЩЕЕ ОТ AO3 (07-20) — ЗАКРЫТО 07-21 ПОРТОМ v2 (t-255, слово
  оператора «почини уже этот хук»): _strip_commit_messages (регекс
  AO3 дословно) + _mask_git_statements (наше расширение — CLI-канона
  журнала у нас нет, их ветка (в) неприменима сознательно); FP-класс
  git-команд с путём журнала убит (регресс дословной формой дня),
  писатели echo/printf/sed живы, 53 теста. Остаток класса:
  toolkit-сиблинг hygiene_gate (v1, FP жив) — порт-очередь.
