# Ночь 2026-07-22 — закрытия валидационной волны (архив D-0038, диета boot-бюджета)

Индекс ночи (журнал t-260..t-263; коммиты 7af43c7..5dd90a7):
экзамен №2 критика → промоушен critic.md (rule 16 + read-only,
t-260, аннулирование T-K3 как бага ключа — аудит №4); F-50
«деливерабл-дрейф» кодифицирован (чек 31 + handoff 2а + ось 9);
батч мелочей 07-20 исполнен целиком (t-261, критик fit_with_fixes);
N4 escape-allowlist (t-262, самовскрытие висячей метки D-0056b →
чеканка D-0089); ts-drift в journal_echo (t-263, F-29 в код);
White Paper §6.5/§6.6 + §7 до 07-22; ROADMAP-диет (критерии
решённых гейтов — в roadmap-closed-phases); режим лист-роутинга
активирован словом оператора; allowlist: Get-Date/date-формы
добавлены словом оператора.

Ниже — блоки, перенесённые VERBATIM из CURRENT_CONTEXT.md этой же
диетой (указатели остались на месте).

## ВАЛИДАЦИОННЫЙ СЛОЙ (перенос из Remaining Lead-tier Queue)

- ВАЛИДАЦИОННЫЙ СЛОЙ ЗАКРЫТ ЦЕЛИКОМ 07-22: DAG
  docs/tasks/2026-07-21_validation-import.md — N1..N4 done. N3:
  промоушен critic.md по экзамену №2 t-260 (rule 16 активен —
  чекер вердикта на каждой приёмке; первые живые срабатывания:
  отклонение v1-вердикта t-261 с пересдачей). N4 (t-262):
  escape-allowlist на pre-commit; НА СОБСТВЕННЫХ СИДАХ вскрыл
  висячую метку D-0056b → отчеканено D-0089.

## ВХОДЯЩЕЕ ОТ AO3 (перенос)

- ВХОДЯЩЕЕ ОТ AO3 «деливерабл-дрейф» ЗАКРЫТО КОДИФИКАЦИЕЙ 07-22:
  F-50 (FINDINGS, вкл. форензик-методичку runtime-свидетелей) +
  чек 31 калибровки (сверка accepted-witness с HEAD) + шаг 2а
  session-handoff (вечерняя точка) + ось 9 SIBLING_MAP. Промоция в
  код-гейт и D-номер — по первому OS-рецидиву (D-0063). Первый
  прогон чека 31 — калибровка №4.

## БАТЧ МЕЛОЧЕЙ 07-20 (перенос; исполнен целиком t-261, коммит c0278d1)

- БАТЧ МЕЛОЧЕЙ (07-20, к следующему builder-батчу, D-0081):
  (е) штабной tools/judge_accept.py + judge_client.py — снимок из
  D:\Improving_AI\exam_hybrid_kit\tools (операционная деталь R13:
  координатору живой сессии нечем позвать судью; тест-минимум как в
  ките); (ж) informational-запрос requests.db в darm_orchestrator
  даёт 0 (окно ts, дефект известен с прогона D№1);
  (а) shadow_eval SUMMARY печатает mean вместо sum cost при n>1;
  (б) calibrate() отбрасывает judge_cost; (в) дубль source_task=
  t-027 в regression_set_coding.jsonl; (г) ЗАКРЫТ 07-22 словом
  оператора «оставляем 20»: строка lead-sonnet: 20.00 стояла с
  топапа 07-20, размер утверждён постоянным, правка не потребовалась;
  (д) кандидаты корпусов «файлы целиком» крупнее ~25К токенов вывода
  непрогоняемы одноходовым нестриминговым реплеем — правило нарезки
  в док корпуса.
  Исходы: (а) закрыт невоспроизведённым + тест разрыва покрытия;
  (б)/(д)/(е) исполнены; (в) закрыт — t-027 одна задача с двумя
  артефактами, source_task провенанс; (ж) исполнен с доказанной
  корневой причиной (+2ч разрыв ts-клоков — подкласс оси 2 карты).

## Habr/LinkedIn (перенос)

- Habr/LinkedIn (t-036) ЗАКРЫТО 2026-07-22 словом оператора: статья
  давно опубликована (факт вне сессий, оператор-источник). Материалы
  остаются в docs/ARTICLE_BRIEF_2026-07-10.md.
