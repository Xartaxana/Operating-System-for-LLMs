# Триаж внешнего ревью Codex (07-24) — Lead, 2026-07-28

Вход: docs/task_reports/2026-07-24_codex_external_review.md (сторонний
read-only ревью, 2×P0 + 5×P1 + 2×P2 + 10 продуктовых предложений).
Прецедент формы: триаж внешнего ревью 07-13
(2026-07-14_external-review-triage.md).

Находка приёмки входа (D-0050): файл ревью остался НЕЗАКОММИЧЕННЫМ
сессией 07-24 — пропуск session-handoff; зафиксировано в очереди
калибровки №5 (класс гигиена-промахов), файл коммитится этим триажем.

Спот-проверка несущих утверждений (R1/D-0046, выполнена 07-28):

- P0-1 ПОДТВЕРЖДЁН: gateway/guard.py:219 `if spent >= budget` и :264
  `if spent >= limit` — обе стены смотрят только УЖЕ записанный
  расход; проекции текущего запроса нет.
- P0-2 ПОДТВЕРЖДЁН: .claude/agents/scout.md:5
  `tools: Read, Glob, Grep, Bash` — Bash есть при декларации
  «Read-only, ничего не пишет».

## Вердикты по находкам

### Принято в работу

1. **P0-1 Guard: проекция расхода** (+ предложение №5 «пулы квот по
   ключу провайдера» — тот же файл, один пакет). Один gateway-пакет:
   спека Lead → builder; оценка prompt-токенов + max_output до вызова,
   `spent + projected >= limit` для жёстких стен, fail-closed на
   дорогих алиасах при недоступной проекции, отдельный лог
   projected-отказов; пулы `quota_pool` по ключу поверх алиасных стен.
   Очередь: ближайший рабочий батч (НЕ гейт Phase 4 — прокси-контур).
2. **P0-2 scout: технический read-only.** Механизм-пункт: сужение
   tools (кандидат — убрать Bash; если ломает легитимную разведку по
   git-истории — узкая read-only обёртка). ОБЯЗАТЕЛЕН прогон golden
   set D-0057 ДО любой правки scout.md. Сиблинги оси E4 (builder уже
   сужен 2105d4f): critic (Bash легален — механический слой R3),
   кит-твины ролей. Очередь: механизм-батч.
3. **P1-2 main_gate: break-glass не должен выглядеть успехом.**
   `skipped_after_2_blocks` (CONSECUTIVE_BLOCK_LIMIT=2) сегодня даёт
   exit 0; сменить семантику — маркер unsafe в carrier + жёсткий
   блокер приёмки в следующем слое. Малый механизм-пункт, тот же
   механизм-батч.
4. **P2-2 wiring_check из исходного репо.** CLI `--host-root` /
   `--kit-root` / `--mode source|installed` + честный вывод при
   неверном корне. Кит-порт-очередь ближайшего минора (D-0074).
5. **P1-3 (продуктовая часть) + №10: режимы телеметрии.** Для штаба
   default raw-логирования СТОИТ ОСОЗНАННО (Shadow Evaluation и
   context-метрики требуют raw; research-деплой). Для продукта —
   принято направлением: режимы accounting-only / redacted /
   full-replay(opt-in) / synthetic-only + retention. Кит-бэклог
   (дизайн-пункт, не гейт (в)).

### Кандидаты калибровки №5 (решение там)

6. **P1-1 route-intent слой против самоисполнения Lead.** Класс уже
   держат чеки 1/2/8 + F-9/D-0096 (пост-фактум — признаём). Машинный
   слой (main_gate сверяет наблюдаемые правки с delegated/
   dispatch_skipped) — staff-internal механизм: промоция по D-0063 на
   рецидиве из статистики чеков, не проактивно.
7. **P1-5 basis=judge: category самодекларирована.** Детектор стоит
   (чек 30 — аудит judge-приёмок); предложенная привязка к
   типизированному leaf-артефакту (leaf_id, intent-ключи, вердикт
   судьи) — кандидат ПРОАКТИВНОГО закрытия для кита (D-0098);
   взвесить против Rule #1 на №5.
8. **P2-1 liveness-пробы хуков в еженедельной калибровке.** Пробы в
   delivery уже норма (D-0093); расширение на калибровочный цикл
   (инвалидный диспатч блокируется, валидный проходит, синтетическая
   грязная строка журнала режется) — кандидат правки протокола
   калибровки.

### Отклонено-пока (evidence-gated, reopen-триггеры явные)

9. **P1-4 структурированная DELEGATION_TABLE** (yaml → генерация MD).
   Rule #1: дрейф MD↔код ловится калибровкой, инцидентов дрейфа ноль.
   Reopen: первый реальный инцидент дрейфа
   VALIDATED_DELEGABLE_CATEGORIES ↔ таблица.
10. **№8 атомарность dod_track-состояния.** Reopen: первый
    lost-update инцидент под параллельными хуками.
11. **№9 acceptance packages.** Журнал (typed fields + witness) +
    task_reports уже несут состав; отдельный артефакт на каждую
    приёмку — расход без показанного спроса. Reopen: аудит, реально
    упёршийся в отсутствие пакета.

### Уже покрыто / заметки

12. №6 «установка как транзакция» ≈ D-0090 (онбординг-режимы) +
    D-0093 (sealed delivery + пробы + откат). №7 «роль-файлы в
    контракте апгрейда» = D-0091 дословно (принято 07-22). №2 дашборд
    cost-per-accepted ≈ savings_report/usage_report + чеки 10/11
    (идея «метрика на витрину» — заметка, не работа). №3 batch
    economics — чек 22 уже мерит батчинг; расширение метрик —
    заметка к №5. №1 route-plan для графов ≈ D-0080 (DAG-носитель);
    для лифов — п.6 выше. №4 capability registry — кит-бэклог рядом
    с п.5.
13. Их прогон `python -m pytest tools/ gateway/ -q` упёрся в их
    120s-таймаут — известно (witness t-322: 1548 passed за 122.53s);
    не дефект, заметка: полный прогон требует таймаут >120s.

## Итог

Ревью принято как вход высокого качества: оба P0 подтверждены кодом и
конвертированы в работу; 3 кандидата — на решение калибровки №5;
3 отклонения — с явными reopen-триггерами; остальное покрыто
действующими решениями (D-0090/D-0091/D-0093/D-0080). Исполнение
принятого — очереди CURRENT_CONTEXT (носитель приоритетов).
