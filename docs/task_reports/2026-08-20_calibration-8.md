# Калибровка №8 — полный разбор прогона (2026-08-20)

Владелец итогов — notes события `calibrated` 2026-08-20 в
logs/routing-log.jsonl (бюджет 15000). Здесь — перечисления, трейлы и
числа, которые в бюджет не влезают; notes несёт вердикты и указатель
сюда. Прошлый прогон (№7, 08-14) свой бюджет ПРОБИЛ (21553 симв.) —
это отдельная находка, см. §F14.

## Рамка прогона

- Окно: 2026-08-14T12:12:34 .. 2026-08-20T13:51:07 (6.06 дн).
- Разобрано: OS 244 события журнала (t-437..t-543), 137 коммитов OS,
  cc_usage 7 дн, 3535 вызовов Bash/PowerShell, транскрипты 132 файла
  окна (7 сессий штаба) + AO3 339 файлов + Dog 48.
- Ярус входа: claude-opus-5 == `roles.lead` → полный Lead, окно
  деградации не открывалось.
- Пре-пасс: `python tools/calibration_prepass.py --window-start
  2026-08-14T12:12:34 --calibration` — РЕЖИМ КОНТРОЛЬНЫЙ, живых 34/36,
  ПУСТЫХ 3 (16, 32, 14(е)), перевзведений 0, гейт (а) ВЗЯТ
  (139316 ≤ 146000, запас 6684 Б). Контрольный прочит: 3 ПУСТ-вердикта,
  расхождений 0.
- Разведка: t-544 (чек 24, оба деплоя), t-545 (чеки 25в/25г,
  транскрипты), t-546 (чеки 13ж/26г/11б/11в, счётный скан). Все три —
  scout/sonnet; приёмка по R1(b) (экзаменационное доверие D-0102 живо:
  scout.md не правился с прогона 08-16, пересборки набора и
  defect_found по принятым дайджестам не было).

## Машинный слой (прогоны, вывод дословно — в notes)

| скрипт | вердикт |
|---|---|
| `python -m pytest tools/ gateway/ -q` | **1 failed, 3939 passed, 29 skipped** (444.68s) — красный от окружения, см. F1 |
| `tools/calibration_counts.py` | непарсящихся 0, rule-6 кандидатов 0, пропусков полей 0, открытых delegated 0 |
| `tools/corpus_growth.py` | сумма 952216 (+8065), порогов 14/15, WARN 0, BREACH 0 |
| `tools/norm_retire.py --check` | I1..I8 OK |
| `tools/parity_check.py --check` | 97 пар: 56 CLEAN / 41 hq-drift (легальны D-0074) / 0 kit-drift / 0 both-drift |
| `tools/hook_liveness_probe.py` | 13/13 живы, ноль молчаний, OVERALL OK |
| `tools/savings_report.py --until 2026-08-20T13:51:07` | $489.13/день ROUTED, брутто-экономия $2472.80 = 30% |
| `tools/spend_report.py --window last --compare prev` | окно `shtab:calibrated:7` (см. F15) |
| `tools/permission_audit.py --minutes 8740 --summary` | 3535 вызовов / 2399 подозрительных |
| `echo '{}' \| python tools/session_context.py` | 1180 Б / 17 строк |

## Находки прогона

### F1. Канон недетерминирован от окружающей сессии (класс «сторож/тест не отличает записи окружения от нарушения изоляции»)

`tools/test_search_control_gate.py::test_ledger_dir_honoured_from_env_var_not_real_logs`
снимает срез РЕАЛЬНОГО каталога леджера до и после и требует равенства.
Живая сессия писала в тот же леджер своим PostToolUse-хуком, пока канон
шёл фоном → красный. Изолированный перепрогон: `46 passed in 18.01s`.

Класс закрыт 2026-08-19 в hook_liveness_probe (q503/N2: LIVE-STATE-TOUCHED
против LIVE-STATE-AMBIENT) — сиблинг в тестах не подметён. Третий носитель
того же класса за две недели: (1) t-493/ref t-491 —
`test_control_mode_periodic_trigger` тянул живой REPO_ROOT; (2) мутация
живого PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md прогоном канона (t-545, хиты
4-5, 08-19; частично снято O2 в a0e9c29); (3) настоящий случай.

Ось 13 карты («ресурсы с глобальным побочным эффектом») заведена 08-19
именно под этот класс — и экземпляр возник ПОСЛЕ её заведения. Ось есть,
подметания тестов не было.

Цена: фоновый прогон канона — дефолт R7; значит витнесс R2 может краснеть
ложно на торном пути.

### F2. Батч 2 и батч 3 калибровки №7 приняты драфтами и НЕ ИСПОЛНЕНЫ, в очереди их нет

Драфт батча 2 принят 08-14T12:12:34 (t-438), «батч 2 разложен исполнителем»
— 1258011 (08-14 16:24); драфт батча 3 принят — b1a7139 (08-14 16:31,
«шесть кодификаций ядра готовы к диспатчу»). Дальше окно ушло во входящее
DOG, экзамены, D-0102, boot-diet и Phase 5. Проверено:

- пункт (1) «точный model-id вместо подстроки семейства» — НЕ посажен
  (доказательство — F3);
- пункт (2) маркер `reserve_engaged/released` — в журнале 2 вхождения, оба
  в ПРОЗЕ notes драфта и самой калибровки №7; событий такого типа нет
  (позитивный контроль формы: `lead_degraded` — 19 вхождений);
- пункт (3) «python -c → БЛОК (решение оператора 08-14)» — НЕ посажен:
  слой по-прежнему WARN (замерено на себе в этом прогоне: вызов `python -c`
  дал «Командная гигиена (WARN, не блокирует)»), и класс дал 507
  срабатываний за окно (F13);
- батч 3 (кодификации чека 23) — НЕ посажен, см. F4.

Носителя нет: `docs/tasks/` не содержит файла батча (позитивный контроль
формы: 33 файла `2026-08*` в каталоге), CURRENT_CONTEXT.md не содержит
строки «батч 2/батча 2» (позитивный контроль: 15 вхождений слова «батч»).
То есть работа, назначенная словом оператора, выпала из всех носителей
очереди — ровно класс F-48 («пункт, живущий только в одном тексте, не
передан»), применённый к собственной калибровке.

### F3. Понижение ПОКОЛЕНИЯ внутри семейства по-прежнему невидимо — рецидив находки №7 5(ii) в этом же окне

Сессия `52b33a11-3d8d-46d6-93d1-d157b3a6c889` (08-18, до 18:40): 723 хода
на `claude-opus-4-8` + 113 на `claude-fable-5` при привязке
`roles.lead = claude-opus-5`. Окна деградации в журнале нет (скрипт: «нет
событий деградации в окне»). В тот же интервал посажены ТРИ механизменных
коммита с декларацией `tier: opus`: 8f8ff0e (17:16), f2504e7 (17:41),
5195e04 (17:53).

Формально: `opus` — семейство привязки, гейт пропустил; фактически
исполняла модель поколением ниже привязанной. Чек 5 знает только
«ниже/равно/выше привязки» и слота «то же семейство, другое поколение» не
имеет; именно об этом №7 писал «не поймал НИКТО». Фикс был назначен
батчем 2 — см. F2.

Остальные сессии окна штатны: opus-5 (31ef6dbb, a5e20211, c199b28e,
текущая f7bd02f0), fable-5 = `roles.reserve` (8d56849e, e16fecc8,
bfc65eae, e60aeefa, 91162c9a).

### F4. Обе кодификации, ОБЯЗАННЫЕ чеком 23 калибровки №7, не исполнены — и обе тут же дали новый экземпляр

№7 назвал два кластера ≥2 и записал «кодификация обязана тем же прогоном»:

- (i) «норма/спека рождает артефакт или случай, который сама не называет»
  ×4 → ТРЕТИЙ подкласс края в R11 (ГЕНЕРАТИВНЫЙ) рядом с TEMPORAL/POSITIONAL.
  Проверка: `grep -i "генератив"` по CLAUDE.md и docs/POLICY_FULL.md — 0
  (позитивный контроль той же формы: `TEMPORAL|POSITIONAL` — 2 вхождения в
  CLAUDE.md). НЕ ПОСАЖЕН.
- (ii) «спека требует от яруса инструмент, которого у него нет» ×3 → п.3
  пятипунктового чеклиста D-0096 расширить с достаточности ДАННЫХ на
  достаточность ИНСТРУМЕНТОВ. Проверка: `grep "инструмент"` по CLAUDE.md —
  0 (позитивный контроль: `fixtures, paths NAMED` — 1). НЕ ПОСАЖЕН.

НОВЫЙ ЭКЗЕМПЛЯР (ii), мой, в этом прогоне: DoD диспатча t-544 требовал от
scout источник `git log --oneline` — у роли scout в этом контуре нет Bash
(сужение 2026-07-28). Исполнитель честно объявил границу и отработал по
журналу. Счёт кластера (ii) = 4.

НОВЫЙ ЭКЗЕМПЛЯР (i): чек 11(в) перечисляет warn-слои именами
(`ESCALATION`), которых нет в фактическом выводе хука — реальный литерал
`R6-ЗЕРКАЛО` (`journal_echo.py:1494`); норма назвала предмет замера так,
что машинный замер по её же словам даёт 0.

### F5. Чек 30 обещан реестром как детектор отклонений от лист-маршрута, шага сверки в нём нет

Зафиксировано ещё 08-14 в b1a7139 с пометкой «войдёт в чек 0 следующего
прогона». Проверено сегодня по тексту чека 30: подпункты (а) живость,
(б) лист-классовость judge-приёмок + галлюцинации, (в) экономика,
(г) сиблинги. Шага «отклонение координатора от D-конструкции без
записанной причины» нет. R13(a) ядра при этом прямо говорит «оконный
детектор — чек 30». Класс F-41 (реестр обещает несуществующего сторожа),
живой.

### F6. Чек 14(ж) — первый же его триггер прошёл молча

Механизм Lead-экзамена v2 посажен 08-18T14:53 (555dcc0) вместе с чеком
14(ж). После этого CLAUDE.md правился по названным Lead-секциям: f5c1bba
(08-18 23:45, конверсия R11 — триггер 2 дословно), 95b27a5 (R8), 98f78bd
(R2/R4/R1), 34968b0 (R13), ee1df9f (диета ядра), 2a4e5c6. Ни один коммит
не тронул PROCESS/LEAD_RANKING_EXAM.md (позитивный контроль: файл имеет
историю — 555dcc0, 42e0f20, c248e26), строки «наследник построен/в очереди
с триггером» нет.

Смягчающее обстоятельство и предмет решения: конверсии W2b были
ФОРМОСОХРАНЯЮЩИМИ (W2c-V: «потерь норм нет»), а триггер написан ПО СЕКЦИИ,
не по содержательной дельте. Возможен ремонт триггера вместо постройки
наследника — решение оператора.

### F7. R3 протёк: 11 builder-приёмок окна без критик-входа и без пометки «critic: skipped»

Метод: по каждому `accepted(agent=builder)` окна искался
`delegated(agent=critic)` с тем же task_id ЛИБО упоминание критика в notes
(греп латиницей И кириллицей — первый проход латиницей был промахом формы,
исправлен). Позитивный контроль: у остальных 33 task_id совпадения есть.

Без обоих: t-437, t-444, t-447, t-449, t-453, t-492, t-496, t-501, t-503,
t-504, t-537. Из них подтверждены как >100 строк и/или механизм:

| task | коммит | объём |
|---|---|---|
| t-503 | 44ef907 | 11 файлов, 362+/2092− (механизм: четыре писателя трека) |
| t-447 | b5fcfb5 | 6 файлов, 985+/65− (механизм: NOTES LEN) |
| t-453 | 14eddab | 17 файлов, 481+/94− |
| t-449 | 842db30 | 2 файла, 236+/14− |
| — | 4813b6c | 6 файлов, 2922+/1− (узел A F-61) |

t-501 («ДУБЛИКАТ», объёма нет) и t-537 («замер без правок») под чек 2 не
подпадают. R3 не даёт льготы «critic: skipped» для крупных диффов вовсе —
она только для малых.

Течь ЛОКАЛИЗОВАНА во времени: с 08-19 критик-вход стоит систематически
(t-509, t-516, t-525, t-529, t-532, t-535, t-536, t-538 — восемь входов),
провал приходится на 08-14..08-18.

### F8. Свежесть навигационных документов (чек 24) — четыре протухания, из них два в носителях-владельцах

Разведка t-544, все кандидаты сверены мной точечно:

1. **ROADMAP.md:77** — `## Phase 5 — Norm Corpus — OPEN 2026-08-18`, при
   том что фаза ЗАКРЫТА 2026-08-20 (CURRENT_CONTEXT.md:17, журнал t-541).
   ROADMAP — единственный владелец фаз/гейтов; статус записан мимо
   владельца.
2. **docs/FINDINGS.md, F-61, статусная строка** — «Открыта; ось карты
   заведена» при посаженных узлах A (44ef907, t-503) и B (1e15980, t-509)
   в этом же окне.
3. **CURRENT_CONTEXT.md:121-124** — пять из шести «живых» хвостов handoff
   t-503 закрыты в том же окне (N1 t-521/t-531, N2 t-522, N3 t-523, П4
   t-535); живым остался только «кит-твины».
4. **docs/RULE_COVERAGE.md** — нет строк на четыре механизма окна: W4
   (храповик гейта (а), GATE_A_HISTORY, skip-каунтер тел), партия 2
   сканера (WRITE-QUOTED WARN, QUOTED_OWNS_WARN, SKIP_RE/TIER_LINE_RE),
   батч П4 (stdin-дедлайн 13 хуков), сторож d15
   (test_claude_binding_guard). Позитивный контроль формы: тот же греп
   находит `corpus_growth` на строке 50.

Кросс-деплойное (AO3, ТОЛЬКО ЧТЕНИЕ, не передано — их репо не трогаем):
README.md:47 называет «9 агентов» при 15 роль-файлах на диске и «сейчас 14»
в их же docs/09; блок «Мелкое хозяйство» docs/09-improvement-plan.md:201-283
(~85 строк нарратива) живёт в БУТ-документе при собственном правиле файла
«нарратив — в 09-history.md».

Это шестой–девятый экземпляры класса F-41 за неделю (три были 08-19).

### F9. Чек 29 — собственный пин номера протух в этом же окне

Чек 29 называет якорь «critic.md п.15». Факт: правило «Финальное сообщение
= ПОЛНЫЙ вердикт» стоит в **п.16** (перенумерация 14→15/16/17 посажена
c3f8585, 08-19, тем же окном). Остальные три якоря на месте: builder.md
п.8 ✔, scout.md п.9 ✔, `tools/dod_gate.py:267` подстрока «финальный отчёт
ЦЕЛИКОМ» ✔. Класс — тот самый «протухший пин номера», который мы нашли У
AO3 08-16; дома он живёт тоже.

### F10. Warn-слои метят десятки процентов своего потока (чек 11(в))

Замер t-546 по 132 файлам транскриптов окна; базы названы, где их удалось
назвать честно (proxy помечены):

| слой | срабатываний | база | доля |
|---|---|---|---|
| ROLE-TYPE WARN | 183 | ≈224 диспатча (`subagent_type`) | ≈82% |
| OWNS OVERLAP | 172 | ≈224 диспатча | ≈77% |
| GIVEN-PATH WARN | 134 | ≈224 диспатча | ≈60% |
| Search returned nothing | 39 | ≈80 прямых Grep/Glob координатора | ≈49% |
| NOTES LEN | 62 | 244 события журнала окна | ≈25% |
| WITNESS ECHO | 63 | ≈918 упоминаний routing-log (proxy) | — |
| TS DRIFT | 34 | то же | — |
| R6-ЗЕРКАЛО | 17 | то же | — |
| TIER ECHO | 8 | то же | — |
| JOURNAL ECHO | 138 | то же | — |
| NEGATIVE LINT / Negative claim | 23 / 56 | — | — |

Три верхних слоя метят большинство своего потока. Это ровно то, что чек
11(в) объявил находкой: «сторож такой плотности воспитывает привычку себя
игнорировать, и дальше молчит уже не он, а читатель». Порог чек назначать
запрещает — назначает обязанность назвать долю числом; названа.

**ПОПРАВКА К ЧИСЛАМ, внесена 2026-08-20 тем же днём (драфт узла D
ремедиации, t-551; прошлое не переписываю — поправка идёт вперёд,
D-0089).** Числа 183/172/134 и доли 82/77/60% ЗАВЫШЕНЫ на неизвестный
множитель. Причина замерена: разведка считала ИМЕНА слоёв, а имя слоя
живёт не только в срабатывании — оно есть в исходниках, тестах,
отчётах, спеках и в самих брифах; сверх того ОДНО реальное
срабатывание попадает в транскрипт минимум ДВАЖДЫ (сырой stdout хука и
запись `attachment.type=="hook_additional_context"`). Счёт по ТЕЛАМ
сообщений на БОЛЬШЕМ множестве (вся история, не окно) даёт
`ROLE-TYPE` 92, `OWNS OVERLAP` 65, `GIVEN-PATH` 156 — у двух слоёв из
трёх НИЖЕ оконных чисел выше.

Находка при этом СТОИТ: ложные срабатывания реальны и воспроизведены
поимённо (ниже и в узле D), а обязанность чека 11(в) — назвать долю
ЧИСЛОМ — сегодня выполнима только неякорным счётчиком. Правильный
вывод не «слои шумят на 80%», а «мы не умеем воспроизводимо мерить
собственный warn-поток»; инструмент замера — предмет очереди.
Диагнозы трёх слоёв ОКАЗАЛИСЬ РАЗНЫМИ: у GIVEN-PATH масса — временной
край (пути, которые диспатч создаёт; норма R2(b) сама гарантирует
несуществующий путь в owns); у OWNS OVERLAP — фантомные «пути» из
прозы канонической фразы `owns/non-goals/handoff` плюс окно живости
24 ч без сигнала завершения воркера; у ROLE-TYPE ложности НЕТ вовсе —
слой говорил правду о протухшем `model: haiku` в scout.md, причина
уже устранена перепривязкой D-0102, а шум был ПОВТОРОМ истины.

ДВА СОБСТВЕННЫХ ЭКЗЕМПЛЯРА ЛОЖНЫХ СРАБАТЫВАНИЙ В ЭТОМ ПРОГОНЕ:
- GIVEN-PATH WARN на диспатче t-544 объявил несуществующими
  `docs/HANDOFF.md` и `docs/feature-registry.yaml` — это ПУТИ ВНЕШНЕГО
  ДЕПЛОЯ (D:\AO3_tests\...), резолвленные от нашего корня. Подкласс
  «чужекорневой контекст» найден ещё калибровкой №7 (3 попадания),
  триггер «по рецидиву» ВЫСТРЕЛИЛ.
- QUOTED_OWNS_WARN на диспатче t-546 потребовал «перенести owns-строку в
  прозу» у READ-ONLY разведдиспатча, который owns не объявляет вовсе —
  токен `OWNS OVERLAP` стоял в инлайн-коде как ИМЯ ИСКОМОГО МАРКЕРА.
  Подкласс owns №2 «owns как ПРЕДМЕТ текста» — тоже находка №7 (2
  попадания), счёт ≥3.

ТРЕТИЙ — ЛОЖНЫЙ БЛОК (чек 26(в)): `hygiene_gate` отклонил READ-ONLY
команду `grep -c ... logs/routing-log.jsonl ... 2>/dev/null` как «шелл-запись
в журнал». Блокирующий класс сработал на чтении.

### F11. Норма длины notes не работает: доля сверх бюджета выросла вдвое

Замер по журналу окна (264 события): медиана **869**, p75 1086, p90 1258,
max 21553; сверх 800 (бюджет диспатчевого цикла) — **155 = 58.7%**, сверх
600 — 210 = 79.5%.

Базлайн посадки слоя (2026-08-16, весь журнал, 1239 событий): медиана 550,
p75 768, p90 1047, сверх 600 — 43%. Ожидание нормы было «доля падает».
Она ВЫРОСЛА (43% → 79.5%), при 62 фактических срабатываниях WARN-слоя в
окне. Детектор отказа самого слоя (чек 11(б)) отработал: срабатывания
есть, значит слой жив — молчит не он, а читатель. Прямое подтверждение
тезиса F10.

### F12. Эхо-база journal_echo в вырожденном режиме — 201 срабатывание фолбэка

Чек 26(г): «единичная легитимна, РЕГУЛЯРНАЯ на рутинных журнальных
аппендах = деградация payload-scoped базы, находка + фикс-диспатч».
Замер t-546: маркер `echo base: HEAD-diff fallback` — **201 вхождение в 41
файле** окна (для сравнения: `JOURNAL ECHO` — 138 в 31). Оба маркера
встречаются в ОДНОМ хук-вызове PostToolUse:Edit (образцы
`e16fecc8:607/614`, `a5e20211:814-815`).

Поправка к вердикту: узел N3 (1e4fb84, 08-19) сделал фолбэк
ДИАГНОСТИЧЕСКИМ — маркер теперь несёт причину (`reason: no tool_response`
видно в выводе пробы). Поэтому корректный следующий шаг — не «чинить
вслепую», а разложить 201 срабатывание ПО ПРИЧИНАМ и решать по раскладке.

### F13. Гигиена: класс `python -c`/heredoc вырос в 6 раз по ставке, решение №7 не исполнено

`permission_audit --minutes 8740 --summary`: 3535 вызовов, 2399
подозрительных. Классы (в скобках — suspects):

| класс | окно №8 | ставка на 100 вызовов | окно №6 (базлайн) | ставка №6 |
|---|---|---|---|---|
| `2>&1` | 90 (80) | 2.5 | 10 | 3.4 |
| `cd`/`Set-Location` | 137 (110) | 3.9 | 8 | 2.8 |
| `python -c`/heredoc | **507 (507)** | **14.3** | 7 | 2.4 |
| журнал мимо Edit/Write | 7 (5) | 0.20 | 0 | 0 |
| команд с ≥1 классом | 691 | — | — | — |

База названа явно: 3535 просканированных вызовов окна против 290 вызовов
окна №6 — сравнивать можно только ставки, абсолютные числа несопоставимы.
`2>&1` продолжает затухать (блок работает). `python -c`/heredoc —
единственный класс с РОСТОМ ставки (2.4 → 14.3, ×6), и это ровно тот
класс, по которому оператор 08-14 сказал «переводим в блок»; решение не
исполнено (F2, пункт 3).

### F14. Чек 25(в): четыре попадания небезопасного отката порчи в штабе

Скан t-545 (структурный поиск по полю `command`, а не по сырому тексту —
сырой давал 98 файлов цитат нормы против 5 реальных вызовов):

1. `e16fecc8/agent-a281856392d955143:203` (builder, 08-19T12:58) —
   `git restore -- CLAUDE.md docs/POLICY_FULL.md`. Своя работа в своих
   owns (ветка (д)), но свидетели ветки (д) неполны: байтокопии нет,
   `porcelain` замерен раньше с межвременным зазором (строка 167 против
   отката на 203), дословного пустого диффа после отката нет.
   **Нарушение формы свидетеля п.7(д).**
2. `e16fecc8/agent-a807daa155dcb7488:188` (builder, 08-20T11:07) —
   `git checkout -- logs/calibration_prepass.jsonl`. Путь ВНЕ owns этой
   сессии. **Нарушение п.7(е).**
3. `8d56849e/agent-a880de83bf0438475:202` и `:220` (builder, 08-19T00:06 и
   00:07) — `git checkout -- PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md
   logs/calibration_prepass.jsonl`, дважды. Путь ВНЕ owns (исполнитель сам
   это записал). `porcelain` свежий оба раза, байтокопии нет.
   **Два нарушения п.7(е)** — при том что исполнитель цитировал п.7 и
   считал себя действующим по ветке (б).
4. `e16fecc8:1567` — `git restore --staged ...`: индекс, рабочее дерево не
   тронуто, три ветви неприменимы. Не нарушение.

Причина попаданий 2-3 — F1: файлы мутировал САМ прогон канона, исполнители
принимали это за собственную грязь.

AO3-сторона (глубоко разобран один случай, dd0a1d97/agent-a6307eea5de296d91,
08-19T23:34): **эталонное исполнение** — `cp` до порчи, `git status
--porcelain` в той же команде, восстановление ЧЕРЕЗ байтокопию, `diff -q`
до и после с дословным `RESTORE_OK identical`. Их сторона нормы держит.
Три оставшихся попадания AO3 и весь вопрос 25(г) по AO3/Dog — незакрытый
хвост разведки, объявлен честно.

Чек 25(г) (штаб): явной пары «усечённое чтение реестра → статусное
утверждение без повторного чтения статуса» не найдено; покрытие ЧАСТИЧНОЕ
(проверено <10 из ~80 кандидатов) — объявлено как частичный негатив, не
как чистый проход.

### F15. Саморевизия протокола

- **Бюджет notes `calibrated`.** №7 записал 21553 симв. при бюджете 15000
  (+44%). Норма бюджета введена 08-16 (t-447), то есть ПОСЛЕ той записи —
  ретроактивно это не нарушение, но факт показывает: собственный формат
  прогона в бюджет не влезает. Настоящий прогон вынес разбор в этот файл,
  notes несёт вердикты и указатель. Кандидат в норму: у калибровки
  ОБЪЯВЛЕННЫЙ носитель — отчёт, notes — сводка.
- **Чек 18(д) `--window last`.** №7 записал: команда резолвится в
  ПРЕДЫДУЩЕЕ закрытое окно. Не исправлено: сегодня `--window last` снова
  напечатал `shtab:calibrated:7` (08-05..08-14), то есть числа НЕ своего
  окна. Курица и яйцо: своё окно появляется только после записи события
  `calibrated`. Формулировку чека надо чинить (например: прогон печатает
  предыдущее окно ДО записи и своё — следующим прогоном; либо калибровка
  пишет `calibrated` первым и считает вторым).
- **Чек 19.** Пятый пустой прогон подряд. №7 постановил
  «ПЕРЕФОРМУЛИРОВАТЬ ВОПРОС» — текст чека не менялся. Новых
  категорий-кандидатов снова нет.
- **Чек 13(з).** Четвёртый пустой прогон: API-окно пусто (real по-прежнему
  987 запросов за всю историю), контур не поднимали. Решение №7
  «ОСТАВИТЬ КАК ЕСТЬ» держится.
- **Дельты corpus_growth.** Сайдкар сообщил «БАЗА ОТ ДРУГОГО МАНИФЕСТА»:
  Δ считаются к предыдущей ЗАПИСИ САЙДКАРА, а не к границе окна
  калибровки. Для чека 34 это значит, что «Δ окна» из машинного слоя
  читать нельзя без поправки — кандидат в уточнение чека.
- **Чек 11(в) называет слой `ESCALATION`,** которого нет в выводе (реальный
  литерал `R6-ЗЕРКАЛО`). См. F4, новый экземпляр кластера (i).

## Чеки, прошедшие чисто (с явными отметками пустых окон)

- **0** — предыдущие находки разобраны; выстрелившие триггеры вынесены в
  F2/F3/F4/F5/F10/F13/F15.
- **1** — у каждой сессии окна есть delegated или skip.
- **1(б)** — норма R2 работает с запасом: **32 designer-диспатча** за окно
  (31 принят) против 6 в №7 и 5 за всю историю до №7; skip-событий
  designer 2, оба с названной причиной.
- **1(в)** — пересдач attempt≥2 под тем же task_id: 3 (t-491, t-536,
  t-538); переоформлений под новым id нет; skip-причин класса «пересдача»
  нет.
- **3** — rule-6 кандидатов 0 (скрипт); 9 rejected, все с `failure_class`.
- **4** — расхождений agent/subagent_type не найдено; модельная сторона —
  см. F3 (расхождение поколения, не типа).
- **5** — событий деградации 0; сессии окна сверены пофайлово по
  транскриптам (см. F3).
- **6** — приёмка сверху: `by=fable` над builder законно (резерв выше
  привязки), равноярусные critic/designer законны клаузулой независимого
  контекста D-0099; defect_found против них 0.
- **7** — открытых delegated 0, все несут `worker_ref`.
- **8** — механизменные коммиты окна несут осевые блоки ПЕРЕЧИСЛЕНИЕМ и
  строки `tier:`; коммиты после заведения оси 13 (47a5a73, 08-19 18:33)
  перечисляют «ось 13» — проверено на 6 из них; строки «оси: не-механизм»
  честны (714ce91, 9765870, 7d108af — ни один не трогает пути невода);
  коммиты без обеих строк невода не касались (проверено по --stat).
- **9** — 2 defect_found окна, каждый называет класс.
- **10** — строки boot-пути OS: README 71, CHARTER 24, ANTI_GOALS 9,
  PHILOSOPHY 22, ARCHITECTURE_BOOT 91, CURRENT_CONTEXT 353, BOOT 57,
  CLAUDE.md 503, SIBLING_MAP 899. AO3: CLAUDE 370 / HANDOFF 852 / план
  283 (№7: 690/984/247 — их CLAUDE и HANDOFF заметно ужаты, план вырос).
  Boot-бюджет по SessionStart: 73143/100000 при 112751 на 08-17 — диета
  08-16 и реструктура 08-18 держат.
- **12** — пути карты живы, нумерация осей 1..13 непрерывна (сторож
  test_sibling_map_liveness в каноне), паритет D-записей держит
  `norm_retire --check` (I1..I8 OK), `VALIDATED_DELEGABLE_CATEGORIES` =
  {summarization, extraction, formatting} сходится со строками
  provisionally_validated таблицы; parity 56/41/0/0.
- **13(а)** — стаб K1, сторож в каноне зелён.
- **13(б)** — false-accept: builder 2/44 = 4.5% (№7: 8.7%), critic 0/12,
  designer 0/31, scout 0/11, lead 0/3.
- **13(в)** — все 9 rejected несут failure_class: recon 4 (scout/haiku),
  capability 3, spec 1, tooling 1.
- **13(г)** — `failure_class=spec` один (t-536); систематики нет.
- **13(д)** — дублей task_id нет (скрипт).
- **13(е)** — ts-аномалий скрипт не нашёл.
- **13(ж)** — маркер `local system clock` есть во ВСЕХ 7 главных
  транскриптах окна; `AUTO-BOOT (D-0103` — в 4 из 7 (механизм посажен
  08-18, более ранние сессии его законно не несут); отказов
  preflight_quota не найдено (позитивный контроль формы пройден).
- **13(з)** — API-окно ПУСТО (0 запросов за окно; real 987 за всю историю
  — позитивный контроль).
- **13(и)** — фантомных delegated нет.
- **13(к)** — область витнесс-прогона параллельных узлов: полный канон в
  окне помечен BATCH CANON у координатора (9e61433).
- **13(л)** — доковые витнессы окна (W2c-1, W2c-2, W4-2) несут все три
  свойства, включая негативный контроль (`w2c1-keyrun.py`,
  `w2c2-keyrun.py` закоммичены).
- **14(а)/(г)** — правки роль-файлов окна: c3f8585 (critic.md + builder.md)
  — CRITIC_EXAM №4 PASS ТЕМ ЖЕ коммитом ✔; 9a857ce (scout.md, смена
  `model:` haiku→sonnet) — прогон экзамена v2 в Runs log есть (строка
  2026-08-16 ПЕРЕПРИВЯЗКА, `SCOUT_GOLDEN_SET.md:467`) ✔; builder — набора
  нет по D-0057, отмечено.
- **14(е)/16/32** — ПУСТЫЕ ОКНА явно: коммитов по `toolkit/` — 0 из 137,
  по `docs/RELATED_WORK.md` — 0 из 137 (пре-пасс, предикатный счёт).
- **15/17/27/28** — все якоря AO3 живы: test-designer `test_debt` 4 +
  rules.yaml guard 6; APPIUM_HTTP_TIMEOUT 6 / `retries=False` 2 /
  `--only-rerun` 1; правило реестра фич 10; «Fail-fast» в docs/06 и всех
  пяти device-промптах (1/1/1/1/1/1).
- **20** — gate walk по 2 defect_found: t-493/ref t-491 — негерметичный
  тест (см. F1, промолчал витнесс: тест был зелёным, пока живое дерево
  случайно удовлетворяло ожиданию); t-521 — карантин `_load_track` на
  канале «путь занят каталогом» (промолчала батарея узла A: канал не
  входил в её перечень; поймала верификация посадки соседнего узла).
- **21** — эскалация одна (t-450, scout→critic по явному сигналу яруса, не
  по счётчику неудач); цепочки ретраев нет, систематического минуса нет.
- **22** — 8 skip-событий (базлайн окна №2 — 16, снижение держится).
  Классы: scout-точечное-чтение ×4 (**порог ≥3 сработал**, см. решение
  ниже), designer-самодрафт ×2, builder-блокирующая-правка ×2.
  РЕЦИДИВ ВНУТРИ СЕССИИ: события 08-16T17:18 и 08-16T18:06 — одна сессия
  (fable), один класс, у второго НЕТ маркера повтора → по букве чека это
  нарушение самодекларации. Но см. решение о самом чеке ниже.
- **23** — атрибуция 9 rejected: ДИСПЕТЧЕР 1 (t-536, failure_class=spec),
  ИСПОЛНИТЕЛЬ 5, ЭКЗАМЕНАЦИОННОЕ 3 (t-455/t-456/t-459 — замеры, не работа).
  Кластеров ≥2 диспетчерской причины В ЖУРНАЛЕ нет; но кластеры из №7
  живы и пополнились — F4.
- **26(а)** — состав сходится ОБОИМИ покрытиями: 12 command-хуков
  `.claude/settings.json` по факту файла; невод `MECHANISM_PREFIXES` держит
  `.claude/settings.json` и все 12 скриптов поимённо (+ md_regions,
  journal_validator, escape_check). Трёхгрупповой критерий пересчитан
  заново по текстам чеков: ПОКРЫТЫ — session_context, critic_snapshot,
  dod_track, hygiene_gate; ЧАСТИЧНО — dod_gate, main_gate, journal_echo;
  НЕ ПОКРЫТЫ — dispatch_gate, owns_gate, claim_control_gate,
  search_control_gate, negative_lint. Совпадает со снимком
  docs/RULE_COVERAGE.md (4/3/5) — дрейфа снимка нет.
- **26(б)** — `critic_snapshot.json` обновлён 08-20 12:51 против последнего
  критик-диспатча окна 08-20T12:51:16 — не старше; треки живых сессий
  непусты.
- **26(в)** — событие `gate_log` в окне ОДНО (`main`, `no-green-run`,
  08-16T18:50:19), `skipped_after_2_blocks` нет. Плюс ложный блок
  hygiene_gate на чтении — F10.
- **26(д)** — проба 13/13, ноль молчаний, OVERALL OK (вывод дословно — в
  notes).
- **29** — дозапросов содержимого в окне 1 (t-439) против базлайна 5/день;
  якорей 3 из 4 на месте, четвёртый — F9.
- **30(а)** — R13 в ядре; стаб K3 зелён каноном; промпт подписочного судьи
  ДОСЛОВНО равен `JUDGE_SYSTEM_PROMPT` (`gateway/shadow_eval.py:85-99` ↔
  `.claude/agents/judge.md:49`) — дрейфа нет.
- **30(б)/(в)** — judge-приёмок в окне **0** (позитивный контроль поля:
  12 judge / 85 critic / 4 queued-to-lead за всю историю). Причина
  структурная и законная: всё окно — Phase 5, то есть механизмы и правки
  политики, где judge-основание НЕЗАКОННО по R13(d). Экономику
  конструкции этим окном измерить нельзя.
- **31** — дрейфа нет: все 55 тест-файлов, названных витнессами журнала,
  существуют в HEAD (контроль пробы: заведомо отсутствующий путь даёт
  exit 128, известный — 0); шаг 2а скилла session-handoff на месте
  (SKILL.md:28).
- **33** — детектор heartbeat-fastdeath AO3 РАБОТАЕТ: в их
  orchestrator-log 19 нездоровых исходов, 6 несут суффикс `fastdeath=`,
  серия 08-17 доведена до `fastdeath=3 escalated`; в их escalations.md
  запись HEARTBEAT-CHILD-DEATH есть. Наша сторона — н-п: heartbeat-контура
  у нас нет.
- **34(а)** — раскладка держится: чеки 34/35 легли в реестровую форму с
  шапками `<!--CHK-->`, ось 13 — секцией карты, F-61/F-62 — записями
  FINDINGS (форму поймал test_findings_form: f28ef5b дописал обязательный
  «Вопрос применимости»), D-0102/D-0103 — парой индекс+полнотекст.
- **34(б)** — (1) `norm_retire --check` I1..I8 OK; (2) стабы K1/K3/K5 несут
  индексные строки и архивный дом; (3) статусы — **три протухания**, см.
  F8; (4) польза диет измерима: boot 112751 → 73143 Б, пол пустого окна
  80083 → 67258 Б, CLAUDE.md 36102 при пороге 36700.
- **34(в)** — WARN 0, BREACH 0, порогов 14/15 (CLAUDE.md — зеркало без
  собственного порога, по замыслу). Рост ВГЛУБЬ у семи артефактов при 0
  новых записей объясняется содержанием Phase 5 (тела чеков вынесены,
  ось 13 заведена, RULE_COVERAGE дополнен). Три худших по байт/запись:
  CHK-26.md 16490, POLICY_FULL 7608, CHK-18.md 5889.
- **35** — дрейфа среза карты AO3 нет: `## Ось 1..13` в обоих файлах,
  13 = 13 (позитивный контроль формы на обоих).

## Экономика (чек 18)

| число | №8 | №7 | №6 |
|---|---|---|---|
| $/день ROUTED | **489.13** | 482.22 | 472.79 |
| брутто-экономия контрфакта | **$2472.80 = 30%** | $8685.18 = 65% | 65% |
| цена за принятую единицу | **$56.10** ($5666.27 / 101 accepted) | $6.30 | $5.48 |
| API-контур | real 987 / $174.91, окно пусто | то же | — |

ВАЖНО о сопоставимости: `savings_report` печатает контрфакт по ВСЕЙ
routed-истории (с 07-08), а не по окну — поэтому «брутто-экономия 30%»
и «$56.10 за единицу» НЕ сопоставимы с №7 напрямую: у №7 те же строки
считались после смены базы контрфакта 08-16 (`roles.lead` вместо
FABLE_MODEL), и знаменатель «принятых единиц» у меня — accepted ЭТОГО
окна против стоимости ВСЕЙ истории сайдчейнов. Это дефект моего чтения
инструмента, а не падение экономики; корректная форма — оконный срез, и
его у `savings_report` нет. **Кандидат в очередь: оконные аргументы для
savings_report** (тот же класс, что дефект 18(д) `--window last`).

18(д) `spend_report --window last --compare prev` (окно
`shtab:calibrated:7`, не своё — см. F15): M4 0.5459 (доля координатора),
M10 haiku 0.2762 / opus 4.2346 / sonnet 9.5188, топ-3 стока t-372 15.9% /
t-390 6.8% / t-423 5.8% (rework). Здоровье измерения: неатрибутированные
$171.65 / 2958 ходов, стадий без формы agent 151, мёртвых диспатчей 20,
ходов без цены 0, непарсящихся строк нет, ПРОПУЩЕННЫЕ ОКНА чисто,
RUNS_WITHOUT_UNITS — одна строка (session-handoff 08-18, нет строк
cc_usage в границах прогона), стадий делящих ключ 74/34 ключей/$352.81,
инвариант agent_id~session чист. M6 по прогонам: calibration-7 34.37,
session-handoff 2.92.

Чек 11 (Rule #1 к машинерии): вывод SessionStart-хука **1180 Б / 17 строк**
против 773/13 (№7), 802/13 (№6), базлайна 528/11 (07-11). Рост +52% за
окно объясняется посадкой авто-бута D-0103 (08-18) — то есть объяснён, но
за пять точек хук вырос в 2.2 раза. Синхронных диспатчей окна нет.

## Статусы DELEGATION_TABLE.md

Статусных ходов этим прогоном НЕТ. Основание: false-accept builder
снизился (8.7% → 4.5%), движения вниз не заслуживает; движения вверх
требуют прогонов РОЛИ, которых окно не дало (designer — 32 диспатча, но
это evidence функции, не смена критерия статуса).

## Реестровая строка прогона

`logs/run_units.jsonl`: run_id `calibration-8-2026-08-20`, run_kind
`calibration`, name `weekly-calibration-8`, unit_kind `events`,
unit_count 244.
