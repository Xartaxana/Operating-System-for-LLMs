# Спека W2c — Смысловая диета гигантов (драфт designer t-512, принят Lead 08-19)

Статус: принята; решения Lead по развилкам — ниже; Р1 — ОТКРЫТА, у
Архитектора. Ход: W2c-0 (замер) — builder t-513; W2c-S (свип) —
scout t-514; W2c-1..4 — после чисел W2c-0 (и слова Архитектора по
Р1 — блокирует только вердикт гейта, не диету).

## РЕШЕНИЯ LEAD ПО РАЗВИЛКАМ (08-19)

Р1 → Архитектору (факт Ф2: гейт (а) на busy-окне недостижим
смысловой диетой по построению; рекомендация дизайнера — (A):
переформулировать как «пол пустого окна ≤57.5 КБ»). Р2 →
ПЕРЕФОРМУЛИРОВАН Lead: «≥500 Б» было круглым числом Lead;
фактическая дельта узла C: 438 Б (ревизия 1) → 872 Б (ревизия 2
после фиксов критика, замер t-511 08-19). К4: освободить ≥841 Б
(= 872 − 31 запаса), размер CLAUDE.md после W2c-1 ≤ 35 243 Б.
ВНИМАНИЕ: оценка frame-safe C1+C2+C5+C7 ≈490 Б + C6 ≈350 = ~840 Б —
НА ГРАНИ порога; решают числа W2c-0. Недобор → СТОП узла и возврат
Архитектору (Р2); Lead-опция до возврата — сжатие реестровой строки
п.7 пакета узла C при посадке (обязательные ключи-фразы сохраняются,
дельта уменьшается). Р3 → (A)
носитель очереди ремедиации чека 26 — CURRENT_CONTEXT.md. Р4 → (A)
PROCESS/CALIBRATION_HISTORY.md; создаётся ТЕМ ЖЕ коммитом, что
первый указатель (темпоральный край 7.2); шапка — владелец + правило
ведения (гейт (б) фазы). Р5 → (A) K5 в скоуп W2c-3. Р6 → (B)
колонку «Model here» оставить; класс прибитых имён (C9) — отдельным
решением, уже в очереди d15. Р7 → (A) разовый скрипт полным
исходником + негативный контроль. Р8 → (A) сначала садится узел C,
потом перевывод порогов (W2c-4). Р9 → (A′), УТОЧНЁН по замеру
t-513: «сегодняшняя дата» даёт НЕпустое окно (в_окне=39 — ночная
активность); пустое окно = --window-start ТЕКУЩЕГО МОМЕНТА запуска.

## ЗАМЕР W2c-0 (builder t-513, 08-19) — числа вместо оценок §4

Файлы: протокол 130 961 Б (LF, дифф диск/индекс 0); CLAUDE.md
36 084 Б. Чеки: 13 = 12 878; 26 = 20 420; 18 = 7 674; 30 = 1 792
(кросс-подтверждено выводом пре-пасса). Окна: реальное
(--window-start 2026-08-14T12:12:34) ИТОГ: живых 33/35, к чтению
124 592 Б (95.1%), гейт (а) НЕ ВЗЯТ (+67 092); ИСТИННО ПУСТОЕ
(timestamp момента запуска) ИТОГ: живых 22/35, к чтению 83 067 Б
(63.4%), гейт (а) НЕ ВЗЯТ (+25 567). --check-form: FORM OK, 35
чеков, 53 шапки; --require-all opt-in флаг (:1590), на живом файле
0 дефектов.

СЛЕДСТВИЯ (решения Lead):
1. **Гейт (а) НЕ берётся диетой ни на каком окне**: недостача
   пустого окна 25 567 Б при frame-safe срезах протокола 7 253 Б
   ((а)-сумма) / 8 786 Б ((б)-сумма БЕЗ задвоения: замер вскрыл
   D7 ⊆ D1, диапазон 695-702 внутри 668-706 — минус 851 Б от
   формального сложения). Развилка Р1 у Архитектора обострена —
   см. её обновление ниже.
2. **К4 меряется НЕТТО** (removed − added), не gross: по текстам
   замен самой спеки C1 = −30 Б (УКАЗАТЕЛЬ ДЛИННЕЕ СНИМАЕМОГО,
   файл растёт), C2 = +90, C5 = +159; сумма 219 Б при пороге
   841 Б. Решение C1 ПЕРЕСМОТРЕНО: клауза `; the pilot is
   D:\AO3_tests` снимается БЕЗ указателя-замены (нетто +27 Б) —
   deploys: в delegation.config.yaml уже единственный машинный
   носитель (половина «README называет пилота прозой» ОПРОВЕРГНУТА
   критиком t-516: README пилота не называет; срез верен без неё).
   План W2c-1: C1-removal
   + C2 + C5 + C6 (перепись петли — разрешена рамкой) + C7
   (агрессивное сжатие до клаузы) + Lead-опция сжатия реестровой
   строки узла C (872 Б → меньше при сохранении ключей-фраз);
   недобор нетто-841 после этого → СТОП + Р2 Архитектору.
3. Точечные: D11 — маркер <!--CHK 13--> НЕ входит в срез (проза
   265 Б, не 380); D12 фактом 114 Б (не 250); D4 — граница
   «сжатого примера» назначается в диспатче W2c-2, не замером;
   ключ chek30-2 §6 исправлен транскрипцией (пробел на месте
   переноса строки исходника — норма жива, найдена 1 раз).
4. Ключи §6: все остальные найдены ровно по 1 разу в своих
   носителях, оба негативных контроля формы красные штатно.

---

Ниже — драфт designer t-512 дословно (read-only роль, файл записан
Lead при приёмке).

## 0. ЧЕТЫРЕ ФАКТА, КОТОРЫЕ МЕНЯЮТ ПОСТАНОВКУ

**Ф1. Носитель фазы ПРОТУХ по W2a; живой носитель говорит другое.**
`docs/tasks/2026-08-18_phase5-norm-corpus.md:126` — «ОСТАТОК W2a: M2
(шапки остальных 29 + перестановка секций + --require-all)».
`CURRENT_CONTEXT.md:20-21` (08-19) — «W2a (35 чеков в реестровой
форме + пре-пасс + require-all)» как ЗАКРЫТЫЙ узел. Сверено по
самому носителю нормы: в `PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md`
54 маркера `<!--CHK `, секции §0–§5 физически расставлены (заголовки
на строках 48/60/354/484/900/1207/1339), чеков 35 (0…34, пересчитаны
поимённо по заголовкам `^\s*\d+\. \*\*`). Следствия для W2c: (i) мир
ПОСЛЕ M2 — новая под-шапка обязана проходить `--check-form`
немедленно, смешанный мир больше не легален; (ii) все числа AM-6
(34.5 %, пол 45.9 %, 3.2 % пустоты, 115.1 КБ базы) сняты ДО M2 и до
чека 34 — перемерить первым ходом, а не цитировать. Замечено, скоуп
не расширяю (D-0043): строку узла W2a в носителе фазы правит Lead.
[Правка исполнена Lead 08-19 тем же ходом, что приёмка драфта.]

**Ф2. Байтовый рычаг чек-листа берётся БЕЗ спорных срезов — но гейт
(а) на busy-окне не берётся ВООБЩЕ.** Требование AM-6 «≥6.3 КБ
всегда-живого пола» закрывается срезами класса «дублирование /
история прежней редакции / протухшее» (§4, D1–D4, D5′, D14, D16
≈ 7.6 КБ оценкой) — ни один из них не уводит мотив. А требование
«~50 КБ для busy» не берётся смысловой диетой по построению: три
гиганта целиком = 39.7 КБ (число AM-6), файл после M2 вырос, и даже
полное удаление гигантов оставляет чтение busy-окна выше 57 500 Б.
Гейт (а) на busy-окне требует структурного хода (тела чеков вне
всегда-читаемого файла / более узкие предикаты / переформулировка
гейта) — это НЕ W2c. Развилка Р1, Архитектору.

**Ф3. Рычаг CLAUDE.md тесен, и требование ≥500 Б конфликтует с
рамкой Архитектора.** Диета ядра 08-16 уже сняла из
не-правило-секций всю историю (`docs/POLICY_FULL.md:844-960`), а
Role ≠ tier получила отдельное решение о РАЗНОМ лечении частям
(`POLICY_FULL:962-994`). Остаток не-правило-прозы преимущественно
нормативен. Честный счёт frame-safe срезов (§4, C1/C2/C5) ≈ 430 Б —
меньше 500. Порог берётся только с одним срезом уровня развилки
(C6: мермейд деградации с переписью петли прозой, ≈ +350 Б). Это
конфликт пары собственных требований спеки; поведение назначено в
§7 (P1): рамка сильнее числа, недобор → СТОП узла и возврат
развилки, а не срез мотива. [См. переформулировку Р2 в шапке:
порог теперь 407 Б по замеру узла C.]

**Ф4. Ретирование K1/K3 даёт байты только вместе с
гибрид-нормализацией — проверено кодом.**
`tools/calibration_prepass.py:1269-1349`: под-диапазоны считаются
только для маркеров, начинающих строку, а «к чтению» вычитает байты
подпункта лишь при его СОБСТВЕННОМ вердикте. У чека 13 маркеры
(а)/(б)/(в) вплетены в абзац, у чека 30 — (а) вплетён; своих шапок у
них нет (шапки 13 стоят у 13(г)…13(л), у чека 30 шапка одна, строка
316). Значит: без Р12(C)-нормализации ретирование K1/K3 не снимает
ни одного байта с ИТОГ — оно остаётся формальным. Порядок узлов §3
из этого выведен.

## 1. Предмет, рамки, инварианты

**Предмет.** Смысловая диета двух артефактов: (A)
`PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md` — гиганты 26/13/18
(+ ретирование фрагментов K1/K3 и гибрид-нормализация Р12(C) внутри
13/26/30); (B) `CLAUDE.md` — не-правило-секции из очереди §6 спеки
W2b (журнальная проза, гигиена п.6/п.7, деградация, шапка/ярусы).

**Рамка Архитектора (стоячая, поверх всего).** Режется ДУБЛИРОВАНИЕ
и ПРОТУХШЕЕ, не объяснения-с-примерами. Критерий — исполнимость
нормы без второго хопа. Каждый срез, уводящий мотив, несёт
строку-вердикт «норма остаётся исполнимой без него: да/нет»;
сомнение = мотив остаётся (сжатым). Цель 30 000 Б (Р3(C)) подчинена
рамке.

**Инварианты (не пересматриваются исполнителем).**
1. Ни одна норма не теряется: у каждого снятого утверждения ровно
   ОДИН адрес (остаётся дословно / уезжает в НАЗВАННЫЙ носитель).
   Адреса «выброшено» не существует (гейт (в) фазы).
2. Четыре `carrier_anchor` из `tools/escape_allowlist.json` дословно
   неизменны; два живут в тронутой W2c журнальной секции CLAUDE.md —
   `a missed event is fixed by a retro pair NOW` (CLAUDE.md:327-328)
   и `or "judge" on a leaf-class dispatch` (CLAUDE.md:318); ещё два —
   в правилах (R3:93, R8.b:151). Перепин — только отдельным решением
   Lead тем же коммитом.
3. Номера чеков не переиспользуются; ретированный фрагмент остаётся
   СТАБОМ со сторожем и командой живости (правило ведения A7, шапка
   протокола, строки 15–36).
4. Предикат шапки — необходимое условие непустоты (инвариант A3);
   нормализация маркеров не меняет предикат родителя без явной
   строки.
5. Внешние ссылки по номерам чеков остаются валидными:
   `docs/RULE_COVERAGE.md:103` держит «ЧЕК 30(а)» сторожем
   дословности судейского промпта, `:105` — «ЧЕК 13(г)», `:71` —
   «чек 13», `:148-190` — справочник чеков. `CLAUDE.md` R11.m
   ссылается на «check 13(л)», R13.a — на «check 30».
6. Оба owns-пути — в `MECHANISM_PREFIXES` (`tools/mechanism_gate.py:98`
   CLAUDE.md, `:102` PROCESS/): коммит с осевым блоком и `tier:` —
   акт Lead, builder отдаёт рабочее дерево отчётом (форма Р9(A) W2a).

## 2. Дано (манифест контекста; всё перечисленное прочитано дизайнером)

- `PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md` — шапка и правило ведения
  1–36; порядок секций 40–46; чек 13 — строки 106–235; чек 30 —
  315–333; чек 26 — 617–812; чек 18 — 1059–1134; заголовки секций —
  48/60/354/484/900/1207/1339. (Номера — ЗАМЕР 2026-08-19, не якорь:
  AM-10 запрещает фиксировать диапазоны; владелец диапазонов —
  пре-пасс на момент прогона.)
- `CLAUDE.md` — 504 строки; шапка 1–18 (бюджетная строка 13–18);
  ярусы 20–39; правила 41–281; Journal 283–368; Role ≠ tier 370–423;
  деградация 425–452; гигиена 454–503.
- `docs/POLICY_FULL.md` — журнал 383–450; Роль≠ярус 452–507;
  деградация 509–542; гигиена 544–606 (п.6 — 559–584, п.7 —
  585–606); «Диета ядра 2026-08-16» 844–960; «Role ≠ tier: разное
  лечение частям секции» 962–994.
- `docs/RULE_COVERAGE.md` — строки 103/105 (сторожа-чеки), 148–190
  (справочник чеков; 164–187 — трёхгрупповой снимок чека 26).
- `tools/calibration_prepass.py` — `GATE_A_THRESHOLD_BYTES = 57_500`
  (:80); статусы/`STATUS_RETIRED_RE` (:108, :435-457); под-диапазоны
  и «к чтению» (:1269-1349); строка ИТОГ и вердикт гейта
  (:1522-1532).
- `tools/session_context.py` — `CLAUDE_BREACH = 36115` (:178),
  `CLAUDE_WARN = 35815` (:179), сравнение строго `>` (:899-900);
  `tools/test_session_context.py:908/927/951/966/975-997` — четыре
  сверки строки на ТОЧНОЕ равенство + границы WARN−1/WARN/WARN+1.
- `tools/test_norm_form.py` — `CONVERTED_RULE_IDS` (:65),
  `ROW_BYTE_CAP = 523` (:70), ассерт полноты (:459-465).
- `tools/norm_keys.json`; `tools/escape_allowlist.json` (4 записи);
  `tools/mechanism_gate.py:97-109`; `tools/journal_validator.py:276,
  758-766` (сторож K1); `tools/savings_report.py:13-17, 39, 118-136`
  (резолюция привязки).
- `.claude/settings.json` — 12 command-строк.
- `delegation.config.yaml` — `deploys: ao3:` (19–24), `roles.lead`.
- `logs/routing-log.jsonl` — последний `calibrated`
  2026-08-14T12:12:34; строки 1235–1241 (t-443/t-444,
  `closes:t-444`).
- Носители задач: 2026-08-18_phase5-norm-corpus.md,
  2026-08-18_w2a-checklist-form-spec.md (A4, K1–K5; AM-1 П2; AM-4;
  AM-6), 2026-08-18_w2b-claude-md-form-spec.md (§6),
  2026-08-18_w2b-transfer-maps.md, CURRENT_CONTEXT.md:18-31, 73-76.

## 3. Узлы, порядок, ярусность

Узлы строго последовательны (единственный владелец каждого файла),
R4-сужение не применяется, канон полный.

| Узел | Содержание | Ярус | owns (абсолютные пути) |
|---|---|---|---|
| **W2c-0** | ЗАМЕР без правок: байты файлов; байты каждого из 26/13/18 и каждого фрагмента-кандидата; прогон пре-пасса на ДВУХ окнах (реальное `--window-start 2026-08-14T12:12:34`; пустое `--window-start <сегодня>`) + `--check-form`; факт включения `--require-all`; уникальность каждого ключа §6 в дожатом тексте | builder | — (scratch, коммита нет) |
| **W2c-S** | Свип класса «прежняя редакция и её опровержение живут в теле чека» по ВСЕМУ протоколу (R9/D-0100: единица >150 строк и >5 соседей → ОДИН scout-диспатч с вопросом применимости как интент-ключом) | scout | — |
| **W2c-1** | Диета CLAUDE.md (срезы C-серии §4.2) + перенос уводимого в POLICY_FULL тем же коммитом | builder → посадка Lead | D:\Improving_AI\Operating-System-for-LLMs\CLAUDE.md; D:\Improving_AI\Operating-System-for-LLMs\docs\POLICY_FULL.md |
| **W2c-2** | Диета гигантов 26/13/18 (срезы D-серии) + носитель архива (Р4(A)) | builder → посадка Lead | D:\Improving_AI\Operating-System-for-LLMs\PROCESS\WEEKLY_CALIBRATION_PROTOCOL.md + D:\Improving_AI\Operating-System-for-LLMs\PROCESS\CALIBRATION_HISTORY.md |
| **W2c-3** | Гибрид-нормализация Р12(C) вплетённых маркеров 13(а)/13(б)/13(в)/30(а) + под-шапки + ретирование K1/K3 + K5 (Р5(A)) | builder → посадка Lead | тот же путь протокола |
| **W2c-4** | Перевывод порогов CLAUDE_WARN/BREACH + четыре теста строки + строки `docs/RULE_COVERAGE.md` + финальный замер и вердикт гейта (а) | builder → посадка Lead | tools/session_context.py; tools/test_session_context.py; docs/RULE_COVERAGE.md |
| **W2c-V** | ГИТ-СВЕРКА после посадок (слово Архитектора 08-19): дифф до-диетной версии из истории гита против посаженной для КАЖДОГО порезанного файла; перечисление каждого снятого/изменённого нормативного утверждения с вердиктом «уехало по адресу X / дубликат / ПОТЕРЯНО»; правила держатся так же крепко (исполнимость без второго хопа); любая ПОТЕРЯ = откат среза до посадки итога. Ретро-скоуп: W1 партия 08-18 (FINDINGS −12.5 КБ, DECISIONS_FULL) и boot-diet 08-16 (CLAUDE.md→POLICY_FULL) | critic → приёмка Lead | — (read-only; вердикт-отчёт) |

**critic обязателен** (слово Архитектора; R3) на W2c-1, W2c-2, W2c-3
отдельными вердиктами: эквивалентность смысла, полнота класса по
SIBLING_MAP, конфликтные пары P1–P8, вердикт-строка исполнимости по
КАЖДОМУ срезу. Судейская приёмка (basis judge) нелегальна (R13.d).
Коммиты обоих owns-путей — только Lead (инвариант 6).

## 4. Карта срезов с вердиктами исполнимости

Форма строки: ID | что снимается (якорная фраза) | адрес | вердикт
«норма исполнима без него» | байты (ОЦЕНКА — W2c-0 заменяет
замером).

### 4.1 Протокол калибровки

| ID | Снимается (якорь) | Адрес | Вердикт | ≈Б |
|---|---|---|---|---|
| **D1** | Чек 26, блок `СПРАВОЧНЫЙ СНИМОК ПРИМЕНЕНИЯ на 2026-08-09` … до перечня трёх групп поимённо | указатель в 1 строку → `docs/RULE_COVERAGE.md` §7 (:164-187 несут ТОТ ЖЕ снимок — сверено чтением обоих носителей) | **ДА.** Обязанность прогона (греп с положительным контролем) и КРИТЕРИЙ остаются дословно; сам чек запрещает верить снимку («Источник истины … — САМИ подпункты … ПЕРЕЧИТАННЫЕ ЗАНОВО»). Второго хопа в ИСПОЛНЕНИИ не возникает | 4 000 |
| **D2** | Чек 26(а), `СПРАВОЧНЫЙ СНИМОК на 2026-08-09 … 12 command-хуков` | снять; обязанность «сверить состав `.claude/settings.json` ПО ФАКТУ ФАЙЛА НА МОМЕНТ ПРОГОНА» остаётся | **ДА.** Состав файла сегодня ровно тот (12 строк, имена совпадают) — срез снимает ДУБЛИКАТ; обязанность по построению читает файл, не снимок | 1 000 |
| **D3** | Чек 26(а), `поправка калибровки №6, 2026-08-05 — прежняя редакция …` | история редакции → архив (Р4) либо POLICY_FULL | **ДА.** Норма (permissionDecision ПО ЗАМЫСЛУ; исчезновение = регресс; появление у остальных = регресс в обратную сторону) остаётся дословно | 550 |
| **D4** | Чек 26(а), датированный снимок скоупа `на 2026-07-19 …` — кроме одного предложения-примера | архив; в чеке — правило сверки при ЛЮБОЙ редакции скоупа + сжатый пример дрейфа | **ДА при сохранении сжатого примера** (рамка: пример защищён). Голое удаление всего абзаца — НЕТ | 600 |
| **D5′** | Чек 26, буквальный повтор внутри критерия (скобка о сводке как своём контроле) | снять скобку | **ДА** — дублирование внутри одного абзаца | 200 |
| **D6** | Чек 26(б), `Прецедент-эталон: … t-350 при снимке от 07-30 …` | — | **НЕТ, не режем.** Пример задаёт ФОРМУ механической сверки; рамка защищает | 0 |
| **D7** | Чек 26, блок `ТРИ ЭКЗЕМПЛЯРА ОДНОГО КЛАССА …` | требует НАЗВАННОГО носителя очереди | **УСЛОВНО** — только после носителя очереди (Р3; решено: CURRENT_CONTEXT). Замечен дефект: очередь без носителя | 800 |
| **D14** | Чек 18, `СОСТОЯНИЕ НА 2026-08-16 … FABLE_MODEL константой … t-443/t-444` | одна строка: «база контрфакта = привязка `roles.lead` с 2026-08-16 (t-444); точки ДО — по Fable, несопоставимы; разрыв базы помечается в notes» | **ДА — ПРОТУХЛО, проверено двумя носителями** (журнал: accepted t-444 08-16, closes:t-444; код: savings_report резолвит привязку, FABLE_MODEL не найден при позитивном контроле той же формы) | 650 |
| **D15** | Чек 18, `ЗАТРОНУТА РОВНО ОДНА ПРОШЛАЯ ТОЧКА …` | архив «известные разрывы базы» (Р4) + строка-указатель | **УСЛОВНО ДА** (данные, не норма); Р4 решён (A) → применяется | 800 |
| **D16** | Чек 18(д), `ИСПРАВЛЕНИЕ (2026-08-13, критик t-428) …` | архив/POLICY_FULL; в чеке — действующая норма («ПРОПУЩЕННЫЕ ОКНА — аудит состояния на старте, не сторож; детектор — отсутствие чисел в notes, F-32») | **ДА.** Тот же класс, что D3 | 850 |
| **D9** | Чек 13(д) `Известный дубль — t-008 …`, 13(е) `события t-023..t-026 …` | архив «известные поправки счёта» + ОДНА строка-указатель | **УСЛОВНО ДА** — Р4 решён (A) → применяется | 500 |
| **D10** | Чек 13(ж), дополнения F-37/D-0075 и D-0103 | — | **НЕТ.** Обе — живые нормы живости маркеров хука | 0 |
| **D11 (K1)** | Чек 13(а) `Каждый accepted по builder-диспатчу несёт поле witness …` → СТАБ со сторожем journal_validator и командой живости | стаб на месте | **ДА, сторож проверен** (:764-766: непустой witness для accepted+builder). Остаточный риск покрыт 13(л) — фраза об этом обязана уцелеть в стабе | 400* |
| **D12 (K3)** | Чек 30(а), фрагмент `"judge" в whitelist` | стаб-фрагмент; сторож `JUDGE_BASIS_VALUE` + энум валидатора | **ДА, но ТОЛЬКО фрагмент.** `RULE_COVERAGE:103` «дословность судейского промпта» обязан остаться правдой; ретирование дословности промпта — НЕ этот срез | 250* |
| **D13 (K5)** | Чек 13(в), первая фраза (`failure_class`, сторож `FAILURE_CLASSES:276`) | расщепление; остаток 13(в) — детектор отказа `calibration_counts` | Р5 решён (A) → в скоуп W2c-3 | 200* |

\* байты уходят из «к чтению» (ИТОГ), не из размера файла, и только
после нормализации маркера (Ф4).

**Дополнение по свипу W2c-S (scout t-514, принят 08-19): класс
«история редакции» шире гигантов.** Четыре новых экземпляра + один
доп. фрагмент — тот же класс, что D3/D16 (история → носитель Р4(A),
в чеке остаётся действующая норма); входят в скоуп W2c-2 тем же
коммитом (решение Lead — полнота класса R9 дешевле отдельного
захода; адреса-строки = замер t-514, перечитать при исполнении):

| ID | Экземпляр | Адрес (замер t-514) | Вердикт |
|---|---|---|---|
| **D17** | чек 5: прежняя редакция границы деградации (дословная цитата, заменена binding-aware 08-16) | L977 | ДА — история редакции |
| **D18** | чек 6: «Прежняя редакция знала только (i) и (ii) — отстала от D-0087/D-0099…» | L95-98 | ДА — история редакции |
| **D19** | чек 12(в): отменённый фильтр «обкатано у нас» (слово оператора 08-10) | L555 | ДА — отменённый компонент критерия; действующая норма («обкатка у добровольцев») остаётся |
| **D20** | чек 21: «ретро-контур 2026-07-13 — до этого… класс F-15» | L247-248 | ДА — состояние «до» посадки |
| **D9b** | чек 13: «Известный случай — t-023..t-026…» | L154-158 | тот же подкласс, что D9 — вместе с ним (Р4(A)) |

Граница класса, подтверждённая Lead по докладу t-514:
founding-прецеденты новых пунктов (чеки 17/24/27/28/29/33/34) и
baseline-точки трендов — НЕ история редакции, не режутся (рамка
защищает мотив и вход критерия).

**Итог протокола (оценка):** frame-safe без развилок ≈ 7 850 Б; с
решёнными Р3/Р4 — до ≈ 9 900 Б + D17-D20/D9b ≈ 1.4 КБ; ретирование
≈ 850 Б к «к чтению». AM-6 «≥6.3 КБ» берётся без единого спорного
среза.

### 4.2 CLAUDE.md

| ID | Снимается | Адрес | Вердикт | ≈Б |
|---|---|---|---|---|
| **C1** | Шапка, хвост `; the pilot is D:\AO3_tests` → `(external deploys — deploys: in delegation.config.yaml)` | конфиг уже носитель (:23-24) | **ДА** — ось 2 «один носитель на факт»; путь резолвится машинно | 50 |
| **C2** | Ярусы, хвост про grades → одна клауза `grades (API contour) are accounting vocabulary, never rule words (ARCHITECTURE.md "Two Vocabularies")` | сжатие на месте | **ДА** — норма стоит предложением выше; мотив остаётся сжатым | 150 |
| **C5** | Journal, абзац о валидаторе → `Enforced at TWO points: the pre-commit validator and the journal_echo write-time warn (tools/journal_validator.py).` | сжатие; имя сторожа сохраняется | **ДА** — обязанности не несёт, называет сторожа | 230 |
| **C4** | Journal, мермейд жизненного цикла | — | **НЕТ.** Парен с абзацем `Closure is reader-specific …`; снятие рвёт связку | 0 |
| **C6** | Деградация, мермейд 431–436 → норма петли Degraded переписывается ДВУМЯ строками прозы | перепись | **ДА, ТОЛЬКО с переписью; голое удаление — НЕТ** (петля Degraded в прозе ниже отсутствует — проверено чтением 438–452). Резерв на случай недобора К4 (Р2) | 350 |
| **C7** | Гигиена п.6, хвост F-55 | сжатие до клаузы | **ДА**, выигрыш мал; рамка допускает сжатый мотив | 60 |
| **C3** | Ярусы, колонка «Model here» | — | Р6 решён (B) — не трогать | 0 |
| **C9** | Role ≠ tier, `The standing mode "operator coordinates with Sonnet …"` | — | **НЕТ, не режем.** ДОКЛАД (D-0043): третье прибитое имя модели в нормативном тексте — класс d15, лечение отдельным решением | 0 |

**Итог CLAUDE.md (оценка):** frame-safe C1+C2+C5+C7 ≈ 490 Б; с C6 ≈
840 Б. Порог К4 (407 Б) по оценке берётся frame-safe срезами;
недобор по замеру → СТОП, Р2 к Архитектору (C6).

## 5. Критерии приёмки (акцептанс-ключи)

**К1 (замер).** W2c-0 отдаёт числа ДО: размер обоих файлов; байты
чеков 26/13/18 и каждого фрагмента D-серии; вывод пре-пасса на
реальном и на пустом окне (строка ИТОГ дословно); `--check-form`
зелен; факт `--require-all`. Оценки §4 заменены измерением;
расхождение >15 % объясняется строкой.

**К2 (пол чек-листа).** ИТОГ пре-пасса на ПУСТОМ окне после
W2c-2/3 ниже, чем до, не менее чем на 6 300 Б; оба вывода дословно.

**К3 (гейт (а), пустое окно).** Строка ИТОГ на пустом окне печатает
`гейт (а) ≤57500 Б: ВЗЯТ`. Не взят — отчёт называет недостачу
числом; узел не провален (гейт подписывает Архитектор), результат
идёт развилкой Р1.

**К4 (запас CLAUDE.md) — ВЗЯТ 08-19.** Итог исполнения (t-516):
нетто диеты 517 Б, файл 36 084 → 35 567 Б. Порог рев.3 (526 Б) дал
недобор 9 Б — узел честно встал по P1; закрыто Lead-опцией: блок
узла C дожат до 535 Б (рев.4, замер measure_row_rev3), порог стал
504 Б ≤ 517 → К4 ВЗЯТ. После посадки узла C: 35 567 + 535 =
36 102 ≤ BREACH 36 115 (запас 13 Б). Развилка Р2 Архитектору НЕ
понадобилась.

**К5 (ни одна норма не потеряна).** Ключ-прогон (§6) зелен на всех
ключах §4 + негативный контроль; каждая строка карты переноса несёт
вердикт исполнителя; утверждений без адреса нет; вердикт «выброшено»
отсутствует как класс.

**К6 (вердикт-строка на каждый срез).** В отчёте — таблица §4 с
фактическим вердиктом ПО КАЖДОМУ ID, включая «НЕТ» (D6, D10, C4,
C9): невыполненный срез с обоснованием — законный результат,
молчание о нём — дефект.

**К7 (форма и сторожа целы).** `--check-form` зелен (в т.ч. новые
под-шапки: ≤300 Б, поля в порядке, `status:ретирован` со сторожем И
командой живости, id совпадает с текстом, покрытие всех строк
диапазонами); `escape_check` — 4 записи OK; канон зелен;
`parity_check --check` — дрейф ТОЛЬКО на парах CLAUDE.md и
протокола.

**К8 (внешние ссылки).** После срезов остаются правдой:
`RULE_COVERAGE:103` (чек 30(а)), `:105` (чек 13(г)), `:71`,
справочник 148–190 (строки чеков 13/26/18/30 обновлены ТЕМ ЖЕ
коммитом), `CLAUDE.md` R11.m «check 13(л)», R13.a «check 30».
Проверка — перечислением в отчёте.

**К9 (пороги).** W2c-4: `CLAUDE_WARN`/`CLAUDE_BREACH` перевыведены
по правилу W2b (`WARN = ceil(measured×1.015/100)×100`, клампы) ЛИБО
оставлены с явной строкой почему; храповик не поднимается никогда;
четыре теста точного равенства строки
(`test_session_context.py:908/927/951/966`) и границы
WARN−1/WARN/WARN+1 обновлены тем же ходом.

**К10 (гит-сверка, слово Архитектора 08-19).** После посадок W2c-1..3
узел W2c-V (critic) диффует до-диетные версии из истории гита против
посаженных по каждому файлу; отчёт — перечисление КАЖДОГО снятого
утверждения с вердиктом «уехало по адресу / дубликат / ПОТЕРЯНО»;
вердикт «правила держатся так же крепко» по каждому файлу; ПОТЕРЯНО
→ срез откатывается. Ретро-скоуп: W1 партия 08-18 и boot-diet
08-16. Фаза не закрывается без зелёного W2c-V.

## 6. DoD — проверочный прогон (вывод = witness)

Все команды — из корня репо, без `cd`, без ` 2>&1`; исполняет
builder на незакоммиченном дереве, вывод дословно в отчёт; `witness`
события `accepted` заполняет Lead при посадке, добавляя вывод
pre-commit.

```
python tools/calibration_prepass.py --check-form
python tools/calibration_prepass.py --window-start 2026-08-14T12:12:34
python tools/calibration_prepass.py --window-start <ISO сегодняшнего дня>
python tools/escape_check.py
python -m pytest tools/ gateway/ -q
python tools/parity_check.py --check
python -m pytest tools/test_journal_validator.py -q      # живость сторожа K1
python -m pytest tools/test_session_context.py -q        # только W2c-4
```

**Ключ-прогон (R11(l), все три свойства обязательны).**
(i) Ключи — дословные фразы норм, ОБЯЗАННЫХ уцелеть, зафиксированы
designer'ом ДО прогона и ДО диспатча (полный список — досылка
designer к каждому узлу; диспатч без списка не уходит). Стартовый
набор:
- чек 26: `грепом имени скрипта по ВСЕМУ файлу протокола, с положительным контролем той же формы вызова на каждый нулевой результат`; `категория (2) засчитывается ПОЛНОСТЬЮ только если предписанная проверка способна обнаружить МОЛЧАНИЕ гейта`; `сверить состав `.claude/settings.json` ПО ФАКТУ ФАЙЛА НА МОМЕНТ ПРОГОНА`; `блокирующий класс «журнал мимо Edit/Write» permissionDecision НЕСЁТ ПО ЗАМЫСЛУ`; `находки аудита класса, ВХОДЯЩЕГО в текущий скоуп хука, при ПУСТЫХ warn-строках`; `Отсутствие приложенного вывода = чек НЕ выполнен`; `засчитывать (а) через (д) запрещено`;
- чек 13: `витнесс-пересказ, класс D-0052`; `скрипт-витнесс без негативного контроля — находка ВСЕГДА`; `сужен ПО OWNS`; `Полный канон в окне обязан быть РОВНО ОДИН`;
- чек 18: `четыре числа против предыдущей точки`; `отсутствие экономических чисел в notes `calibrated` видно следующему прогону`; `при пустом реестре — явное «н/д (реестра нет)» вместо молчания`;
- чек 30: `дрейф = находка`; `механизм/политика/ интеграция под judge-basis = самосертификация класса F-22` (пробел после второго `/` — фолд переноса строки исходника, транскрипция выверена замером t-513);
- CLAUDE.md: `a missed event is fixed by a retro pair NOW`; `or "judge" on a leaf-class dispatch`; `never invent project state`; `never used in rules` (ключ сжатой редакции C2 перепинывается designer'ом ДО прогона); `check your actual model by the last visible signal`; `coordination and authorized tasks — yes` (перепись петли C6, если Р2 активирует C6).

(ii) Скрипт — разовый, приложенный ПОЛНЫМ ИСХОДНИКОМ к witness
(Р7(A)). Обязателен фолд пробелов по контракту `_fold_whitespace`
({пробел, таб, CR, LF}+ → один пробел; `|` НЕ фолдится) — правило
ключ-прогона, кодифицированное 08-19 по находке t-496.
(iii) Негативный контроль обязателен: один ключ намеренно снят →
скрипт сообщает провал; вывод обоих прогонов в отчёт.

**Адверсариальная мини-батарея** (поверхность интерактивна: CLI
пре-пасса + `--check-form`):
(а) под-шапка перед вплетённым маркером → ошибка формы (AM-1 П3);
(б) под-шапка ровно 300 Б / 301 Б;
(в) `status:ретирован` без `живость:` → ошибка;
(г) id под-шапки ≠ маркеру в тексте → ошибка;
(д) строка тела вне всех диапазонов после нормализации → ошибка
покрытия (A8.3);
(е) пустое окно: все не-always/manual ПУСТ, ИТОГ печатается, строки
ПУСТ несут знаменатель (AM-2);
(ж) окно, где ретированный фрагмент имел бы материал → фрагмент всё
равно не читается (статус сильнее), строка сторожа печатается;
(з) якорь `escape_allowlist`, разорванный правкой — красная проба
на КОПИИ дерева (гигиена п.7(г)), `escape_check` обязан упасть;
(и) `CLAUDE.md` размером ровно WARN → без пометки; WARN+1 → OVER;
отсутствует → `missing`.
Потолок объёма (R11.f): ключи + батарея + границы; полный регресс
сверх — не требуется, канон покрывает.

## 7. Края поведения

**7.1 Лимиты — на границе и за ней**

| Лимит | На границе | За границей |
|---|---|---|
| Гейт (а) 57 500 Б | ИТОГ ровно 57 500 → `ВЗЯТ` (`to_read <= GATE_A_THRESHOLD_BYTES`, :1526) | 57 501 → `НЕ ВЗЯТ (+1 Б)`; узел не падает, число идёт Архитектору (Р1) |
| Запас CLAUDE.md ≥841 Б (К4, редакция Lead по ревизии 2 узла C) | ровно 841 → К4 взят | 840 → К4 НЕ взят → СТОП узла, поведение P1 |
| `CLAUDE_WARN` | ровно WARN → без пометки (строго `>`) | WARN+1 → `OVER -> boot-diet (CLAUDE layer)` |
| `CLAUDE_BREACH` (храповик) | ровно 36 115 → легально | 36 116 → пробой; поднимать число запрещено, легальный выход — снять байты |
| Строка под-шапки 300 Б | 300 → PASS | 301 → FORM-ошибка, `--check-form` exit 1 |
| Снятие ≥6.3 КБ пола | ровно 6 300 → К2 взят | 6 299 → К2 не взят, отчёт называет недостачу и список НЕ применённых кандидатов с причиной |

**7.2 Временные края (R11.c)**
- `PROCESS/CALIBRATION_HISTORY.md` — артефакта СЕГОДНЯ НЕТ; создаёт
  его ТОТ ЖЕ коммит, который ставит первый указатель. До создания
  указатель ставить запрещено (иначе адрес пустой — потеря нормы).
  Прерванная миграция не оставляет висячих указателей: указатель и
  носитель — один коммит.
- Под-шапки 13(а)/13(б)/13(в)/30(а) — до W2c-3 их нет, ретирование
  K1/K3 байт не даёт (Ф4); ПОСЛЕ W2c-3 существуют и обязаны
  проходить `--check-form` немедленно (мир после M2; `--require-all`
  — проверить фактом в W2c-0).
- Перевывод `CLAUDE_WARN/BREACH` — миры «до» (файл над WARN,
  SessionStart печатает OVER — задуманное давление) и «после».
  Порядок: Р8(A) — сначала узел C, потом перевывод.

**7.3 Позиционные края (R11.d)**
- Под-шапка стоит первой строкой ПОСЛЕ строки с закрывающим `**`
  (AM-1 П1) либо перед маркером, начинающим строку (П2). Инвариант:
  ни одна строка тела не остаётся вне диапазонов (`--check-form`
  проверяет покрытие) — иначе адресное чтение молча теряет норму.
- Стаб ретированного фрагмента стоит НА МЕСТЕ фрагмента, не в конце
  чека. Инвариант: читатель, дошедший до места, где норма была,
  видит её статус и сторожа — а не пустоту.
- Указатель вместо снятого снимка (D1/D2) стоит там же, где стоял
  снимок. Инвариант: порядок чтения обязанности не меняется;
  читатель критерия не может прийти к более РАЗРЕШИТЕЛЬНОМУ выводу,
  чем читатель критерия+снимка.

**7.4 Пустой/отсутствующий вход**
- Пустое окно — штатный режим замера К2/К3 (не ошибка); все
  не-always ПУСТ, строки ПУСТ несут знаменатель.
- Отсутствующий журнал/недоступный внешний деплой — чеки живы
  (fail-closed), причина печатается; замер «до/после» делается при
  ОДИНАКОВОЙ доступности источников — иначе числа несравнимы.
- Пустой список ключей ключ-прогона = скрипт обязан упасть.
- Отсутствующий `CLAUDE.md` → `boot_budget_lines` печатает
  `missing`, не «под бюджетом» (уже реализовано).

**7.5 Конфликтующие пары собственных требований**
- **P1.** «запас К4» × «сомнение = мотив остаётся». РАМКА СИЛЬНЕЕ.
  Недобор frame-safe срезами → узел останавливается, печатает число
  и список отвергнутых кандидатов с вердиктами, возвращает развилку
  Архитектору (Р2/C6). Молчаливый добор — дефект.
- **P2.** «≥6.3 КБ пола» × гейт (в). Адрес обязателен для каждого
  утверждения; при отсутствии носителя срез не применяется, а
  становится развилкой.
- **P3.** «Снимок дублируется в RULE_COVERAGE» × «RULE_COVERAGE не
  источник истины протокола». Владелец снимка — ОДИН (RULE_COVERAGE
  §7); в протоколе указатель + строка «источник истины — подпункты,
  перечитанные заново». Свежесть владельца сторожит чек 24.
- **P4.** Ретирование × «номера не переиспользуются». Стаб остаётся;
  байты уходят из «к чтению», не из файла — К2 берётся по ИТОГ.
- **P5.** Нормализация Р12(C) × ревьюабельность диффа. Нормализуются
  ТОЛЬКО маркеры, получающие под-шапку, и только в 13/26/30.
- **P6.** Диета CLAUDE.md × якоря. Якорь выигрывает всегда; перед
  W2c-1 положение сверяется, после — `escape_check`.
- **P7.** Диета CLAUDE.md × машинные читатели. Проверено:
  `tools/norm_keys.json` пришпиливает фразы только конвертированных
  правил — греп `hygiene|Journal|retro pair|check 13` пуст при
  позитивном контроле той же формы (`BATCH CANON`:62, `judge`:109/114,
  `leaf-class`:110 найдены). Ассерт полноты не задет.
- **P8.** Понижение порогов × узел C. Р8(A): сначала садится узел C,
  потом перевывод.

## 8. Не-цели

Смысл норм не меняется (снимаются только дубликаты, история редакций
и протухшее); номера чеков и правил; схема журнала и словари
валидатора; правила R1–R13 (скоуп W2b, Р12(B)); секция Role ≠ tier
CLAUDE.md (отдельное решение, `POLICY_FULL:962-994`);
реструктуризация POLICY_FULL; ремедиация непокрытых гейтов чека 26;
порт в `toolkit/` и в пилот (D-0074, релизный батч); бюджеты прочих
артефактов корпуса (W3); заведение новых чеков; коммит, осевой блок,
журнал, статусы узлов (акты Lead).

## 9. Handoff

(1) Архитектор — развилка Р1 (гейт (а) busy) до подписи гейта; (2)
designer — досылка полного списка ключей на каждый узел до диспатча
(карту чинит designer, исполнитель её не дописывает); (3) critic —
вердикт на W2c-1/2/3; (4) Lead — коммиты обоих owns-путей (оси R10 +
`tier:`), строки `docs/RULE_COVERAGE.md`, журнал, статусы узлов; (5)
порт-очередь D-0074 — `parity_check` покажет дрейф на парах
CLAUDE.md и протокола: законно, обязано быть объявлено; (6) узел C
(F-58) — освобождённый запас передаётся числом, не «есть место»; (7)
носитель фазы — правка протухшей строки W2a (Ф1; исполнено 08-19) и
запись результата гейта (а); (8) W0/прогон №8 — контрольный замер
после диеты.

## 10. Развилки (исходный список дизайнера; решения — в шапке)

РЕШЕНИЯ LEAD ПО РАЗВИЛКАМ ДОСЫЛКИ ДИЗАЙНЕРА (08-19, t-512b):
Р10 → (A) исполнено Lead: реестровый блок узла C сжат 872→557 Б
(ревизия 3 пакета, семантика критика сохранена); порог К4 = 526 Б
нетто, план C1-removal+C2+C5+C6+C7+C10+C13 ≈ 541 Б — берётся с
запасом ~15 Б; недобор по замеру → P1/Р2. Р11 → (A) D19: снять
только атрибуцию отмены, контраст «не "обкатано у нас"» остаётся
(рамка). Р12 → (A) текст сжатого примера D4 дизайнера принят
дословно. Р13 → (A) все тексты замен дизайнера (C6/C7/D14/D15/D17/
D18/D9-указатель, d1-11, D2-фикс «перечисленных выше») приняты
дословно — ключи A′ машинно-проверяемы; правка при посадке = перепин
ключа ДО прогона. Р14 → (A) два красных негативных контроля (классы
A и B). Р15 → (A) C10 (60 Б) и C13 (55 Б) включены в W2c-1.
Фолд-обязательность ключей (второй живой экземпляр класса chek30-2:
«never invent project state» через перенос строки) — строкой в оба
диспатча.

Р1 — РЕШЕНА СЛОВОМ АРХИТЕКТОРА 08-19: вариант (B′), заведён узлом
W4 фазы (носитель фазы, спека — designer t-517). Состав (B′):
(1) структурный вынос тел гигантов в отдельные файлы, чтение по
вердикту пре-пасса, узкие предикаты; (2) SKIP-КАУНТЕР: чек,
пропущенный 3 калибровки подряд, принудительно живой — машинно в
пре-пассе (сайдкар-счётчик; слепота триггера ограничена ≤3 окнами
детерминированно); (3) гейт (а) ПЕРЕФОРМУЛИРОВАН из константы в
ХРАПОВИК ОТ ЗАМЕРА: после посадки W4 порог = фактическое «к чтению»
типового окна × малый запас, НИКОГДА не растёт молча (пробой —
явное слово), снижение легально всегда; 57 500 — ставка до-M2-мира,
уходит в историю гейта. Мотив гейта: не цель-число, а детектор
«рост петли надзора не пройдёт незамеченным» (Rule #1 на самой
петле). Исполнение W4 — ПОСЛЕ W2c-2/3 (выносить похудевшие тела).
Исходные варианты — история: (A) снят замером; (C)/(D) поглощены
(B′)-храповиком.
Р2: решение Lead в шапке (порог 407 Б; недобор → СТОП + C6(A)).
Р3: (A). Р4: (A). Р5: (A). Р6: (B). Р7: (A). Р8: (A). Р9: (A).

## 11. Мелочи, закрытые дизайнером

Язык срезов — язык носителя; номера строк в спеке — замер, не якорь
(AM-10), якоря — дословные фразы; порядок узлов последовательный;
замер W2c-0 — scratch, без коммита; сравнение порогов строго `>`;
стаб ретирования сохраняет номер и место; указатели — одной строкой;
вердикт-строка пишется и для несостоявшегося среза; окно замера
«до/после» одно (`2026-08-14T12:12:34`).

## 12. След чтения дизайнера (и позитивные контроли негативов)

Целиком: носитель фазы; спеки W2a/W2b/transfer-maps; CLAUDE.md (504
строки); escape_allowlist.json; delegation.config.yaml. Точечно:
протокол (шапка, чеки 13/26/18/30, заголовки секций, греп `<!--CHK `
= 54); POLICY_FULL 383–612, 923–995; RULE_COVERAGE 140–194;
calibration_prepass 1264–1373, 1505–1549; session_context /
test_session_context (пороги, четыре сверки); journal_validator
(:276, :758-766); savings_report; mechanism_gate; settings.json;
журнал (греп `"event":"calibrated"` — 7 событий, последнее
2026-08-14T12:12:34; t-443/t-444). Позитивные контроли негативов:
(i) первый греп заголовков чеков вернул пусто — промах вызова,
контроль `^#{1,4} ` нашёл 8 заголовков, форма установлена
`^\s*\d+\. \*\*`; (ii) отсутствие `hygiene|Journal|retro pair|check
13` в norm_keys.json — контроль той же формы: `BATCH
CANON`/`judge`/`leaf-class` найдены; (iii) отсутствие `FABLE_MODEL`
в savings_report.py — контроль тем же грепом: `roles.lead` найден в
том же файле 8 раз. НЕ ПЕРЕМЕРЕНО (оценки): байтовые числа §4 и §0;
36 084 Б CLAUDE.md; 39.7 КБ гигантов; 34.5 % — К1 требует
фактических чисел от builder первым ходом.
