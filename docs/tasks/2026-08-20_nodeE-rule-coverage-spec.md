# Спека узла E — карта «правило → сторож» (F8 калибровки №8)

**Происхождение:** дизайнер-диспатч t-570, 2026-08-20. **Статус:** ПРИНЯТА
Lead с решениями по четырнадцати развилкам (§5) и ответами на запросы
фактов (§7). Персистирована — жила только в транскрипте (класс F-50).

**Носитель правки:** `docs/RULE_COVERAGE.md` (236 строк, 8 секций +
справочник чеков + §8 «Сторож этого документа»; правило ведения — :8-11).

---

## 0. ЧТО РАСХОДИТСЯ С БРИФОМ Lead (принято)

**Р-1. «Четыре строки протухли» — неверно, строк НЕТ ВОВСЕ.** Отчёт F8
говорит буквально «нет строк». Греп по карте: `GATE_A`, `md_regions`,
`регион`, `stdin`, `binding_guard`, `Model here`, `QUOTED` — 0 вхождений;
позитивный контроль той же формы: `corpus_growth` найден 8 раз. Значит
все четыре пункта Д-7 — **НОВЫЕ строки**, а не правки.

**Р-2. Д-3 — ПРАВКА, а не новая строка.** Пороги ядра уже стоят в
строке 60 с текстом «WARN=36700/BREACH=37000». Это и есть протухшая
строка класса F-41; вторая запись о том же носителе сделала бы карту
двусмысленной.

**Р-3. Найден ВОСЬМОЙ долг (Д-8), брифом не названный** — строка 88.

---

## 1. РЕШЕНИЯ Lead ПО РАЗВИЛКАМ

- **Р1** (третий пин чека 23, «промоция чека 23 №5»): принят **(б)** —
  переписать в «промоция пункта 5 пятипунктового чеклиста D-0096 чеком
  23», с ЯВНОЙ пометкой в отчёте, что это интерпретация драфтера.
- **Р2** (Д-4 отдельной строкой или врезкой): принят **(а)** — отдельная
  строка. Предмет — не поведение гейта, а решение оператора, у которого
  ВПЕРВЫЕ появился детектор; врезка спрячет именно это.
- **Р3** (где живёт якорь чека 29): принят **(а)** — только справочник.
  Дубль в двух местах разошёлся бы, это класс «одна норма — один дом».
- **Р4** (процедурное правило 18(д) в карте): принят **(а)**, одной
  фразой. Основание: это единственное место, где неверная форма прогона
  печатает ЧУЖИЕ числа, и это случилось дважды.
- **Р5** (строка 38 и расширение STOP-регекса): принят **(б)** — добавить
  фразу с ИЗМЕРЕННОЙ exit-code-нейтральностью. Для единственного
  блокирующего гейта системы «трогали, но нейтрально» ценнее молчания.
- **Р6** (класс «гарантия шире фактической» и класс узла C): принят
  **(а)** — не в эту карту, их дом «Режимы отказа» SIBLING_MAP.
- **Р7** (принимать ли Д-8): принят **(а)** — да, этим узлом. Класс F-41
  в этом окне дал девять экземпляров; оставлять десятый нельзя.
- **Р8** (байтовый конфликт): принят **(а) с ужесточением** — см. §2
  ниже, это главный край узла.
- **Р9** (партия 2 сканера, одна строка или три): принят **(а)** — одна.
  Предмет здесь именно КЛАСС «одна дыра в трёх сторожах».
- **Р10** (место строки П4): принят **(а)** — секция 3. Вариант (в)
  (новая секция) ОТВЕРГНУТ ЯВНО: сломал бы нумерацию `RC§N`, на которую
  ссылаются шапки чеков, и потребовал бы правки протокола (чужой owns).
- **Р11** (легенда режимов): принят **(б)** сейчас — добавить `ГЕЙТ
  ФОРМЫ`; **(в)** (все девять недостающих) — строкой очереди.
- **Р12** (литерал `R6-ЗЕРКАЛО` в строку 44): принят **(а)**.
- **Р13** (пробел D-0102): принят **(б)** — строкой очереди с триггером
  «ближайшая правка секции 5». Механизм не из окна ремедиации.
- **Р14** (§8 не называет машинного сторожа документа): принят **(а)** —
  добавить пункт (в). Это ровно предмет документа.

---

## 2. ГЛАВНЫЙ КРАЙ УЗЛА — БАЙТЫ (решение Р8 с ужесточением)

**ЗАМЕР Lead (ответ на запрос З-1), не оценка:**
```
docs/RULE_COVERAGE.md [corpus, строка реестра]: WARN | bytes=58210
records=56 bpr=1039 | Δbytes=4157 Δrecords=1 Δbpr=56 | в полосе (95%..1058]
```

**Карта УЖЕ в WARN.** Пять новых строк по ~1700 Б каждая дадут
bpr ≈ 1094 — то есть **пробой 1058**.

**Норма исполнения:** каждая новая строка несёт ТРИ вещи и не более —
(i) саму норму, (ii) сторожа и его РЕЖИМ, (iii) где сторож стоит.
Детали, история и прецеденты идут УКАЗАТЕЛЕМ на их владельца, а не
пересказом в карте. Целевой размер новой строки — до ~1050 Б.

**Запрет:** ужимать НЕЛЬЗЯ ценой несущего факта. Если строка не влезает
без потери факта — исполнитель ОСТАНАВЛИВАЕТСЯ и возвращает вопросом,
как это правильно сделал узел B 08-20. Молчаливое сокращение нормы ради
числа — то, против чего стоит вся эта карта.

**Если после ужатия замер всё равно даёт BREACH** — работа сдаётся с
красным замером и явным вопросом ко мне. Подгонять число, резать старые
строки или трогать `bpr_max` исполнителю ЗАПРЕЩЕНО.

---

## 3. СВОДКА ПРАВОК

| | число |
|---|---|
| НОВЫХ строк | **5** — Д-1 храповик, Д-4 окно 8 ч, Д-7.2 партия 2 сканера, Д-7.3 П4/F-62, Д-7.4 сторож d15 |
| ПРАВОК строк | **5** — 41 (owns_gate), 54 (R13/чек 30(д)), 55 (Д-2 два вхождения + Д-6), 60 (Д-3), 88 (Д-8) |
| ПРАВОК справочника | **2** — чек 18 (18(д)), чек 29 (якорь по тексту) |
| Плюс | легенда (+`ГЕЙТ ФОРМЫ`), §8 пункт (в), строка 44 (литерал `R6-ЗЕРКАЛО`) |
| Удаляется | **0** |

---

## 4. ТЕКСТЫ ПРАВОК

### Д-1. Храповик гейта (а) — НОВАЯ строка, секция 1, после строки 51

Три колонки: политика | сторож и режим | как реализовано.

**Политика:** решение Р-D (2026-08-20): порог гейта (а) пре-пасса —
ХРАПОВИК, не константа: снижение и плато легальны молча, ПОДЪЁМ легален
только объявленный — слово оператора + замер, ПРОБИВШИЙ прежний порог
(подъём про запас запрещён) + та же формула `ceil(measured×1.05/100)×100`
+ названная причина; прежняя запись не переписывается и не аннулируется
(подъём ≠ аннулирование).

**Режим:** ГЕЙТ ФОРМЫ + ВСПЛЫТИЕ + ГЕЙТ КОММИТА + чек 34.

**Реализация:** носитель истории — `GATE_A_HISTORY` в
`tools/calibration_prepass.py` (подъём — вложенный блок `raised` с
from/word/measured_breach/reason); сторож —
`check_gate_a_history_monotonic()`, боевой вызов в `run_check_form()`,
то есть `--check-form` печатает «ХРАПОВИК ГЕЙТА (а): …» и завершается
кодом 1, плюс синтетические истории в каноне; всплытие — `ИТОГ` печатает
«ПОДЪЁМ №N словом оператора …» КАЖДЫМ прогоном, мир без подъёма даёт
прежнюю строку байт-в-байт; коммит-гейт — файл в `MECHANISM_PREFIXES`;
норма-близнец — блок «ГЕЙТ (а) — ХРАПОВИК, НЕ КОНСТАНТА» шапки протокола
(подъём без причины в notes `calibrated` = находка чека 34, два подъёма
за окно = находка независимо от причин). ИСТОРИЯ: 57500 (аннулирован),
146000 (база 139042), 154000 (подъём №1, база 146664). Сиблинг без
сторожа — `CLAUDE_WARN`/`CLAUDE_BREACH`, ось 14 SIBLING_MAP.

### Д-2. Протухший пин, ПРАВКА строки 55 (ДВА вхождения)

Позитивный контроль драфтера: чек 23 прочитан целиком (:289-309) — одна
шапка, НИ ОДНОГО подпункта; контроль формы — соседний чек 30 подпункты
(а)…(д) НЕСЁТ. Якорь существует дословно на ОДНОЙ строке (:296-297).

- **Вхождение 1 ДО:** `класс (б) чека 23, кейсы t-324/325/332/343; предложение оператора`
  **ПОСЛЕ:** `чек 23, абзац «шаг ОБЯЗАТЕЛЕН и АТРИБУТИВЕН» (якорь ПО ТЕКСТУ: подпунктов у чека 23 нет вовсе — прежний пин «класс (б)» протух, снят узлом E 2026-08-20), кейсы t-324/325/332/343; предложение оператора`
- **Вхождение 2 ДО:** `детектор — атрибутивный счёт класса (б) чеком 23 + возвраты-вопросами исполнителей`
  **ПОСЛЕ:** `детектор — атрибутивный счёт того же абзаца чеком 23 + возвраты-вопросами исполнителей`
- **Третье вхождение** (`промоция чека 23 №5 по рецидиву t-338`) — по
  решению Р1(б): `промоция пункта 5 пятипунктового чеклиста D-0096 чеком 23 по рецидиву t-338`.

Побочная польза: форма `чек 23` (строчная, с пробелом) попадает под
`validate_rules_backward` и машинно резолвится; прежняя форма `чека 23`
регексом не ловилась вовсе.

### Д-3. Пороги ядра, ПРАВКА строки 60

К существующему тексту про W2c-4 дописать: **ВТОРОЙ ПЕРЕВЫВОД ТОГО ЖЕ
ДНЯ** — решение Р-О4, слово оператора (узел G ремедиации): две
кодификации, которых потребовал чек 23 калибровки №7, подняли файл до
замера 36900 Б при WARN=36700; четыре диеты ядра подряд исчерпали
кандидатов на срез, поэтому порог перевыведен ПО ФАКТУ:
`WARN = ceil(36900*1.015/100)*100 = 37500`, `BREACH = WARN + 300 = 37800`
(носитель — `tools/session_context.py`, провенанс — комментарий там же);
рост порога — только словом оператора, **но машинного сторожа у этой
дисциплины НЕТ** — ось 14 SIBLING_MAP. Дополнительно: `CLAUDE_BREACH` —
число БЕЗ кодового пути, ни одна ветка не читает его; порог OVER
считается только по `CLAUDE_WARN` (проверено грепом с позитивным
контролем: живые чтения `CLAUDE_WARN` найдены).

### Д-4. Окно живости owns-реестра — НОВАЯ строка, секция 1, после 41

**Политика:** Р-О3 (решение оператора 2026-08-20): окно живости
owns-реестра — 8 часов вместо 24; компромисс назван в решении — фоновый
воркер, запущенный раньше окна, ловиться перестанет, настоящее лечение
(сигнал завершения воркера) остаётся в очереди.
**Режим:** WARN-ХУК + ЛИТЕРАЛЬНЫЙ ПИН В КАНОНЕ.
**Реализация:** `tools/owns_gate.py`: `WINDOW_SECONDS = 8 * 60 * 60`,
граница ВКЛЮЧИТЕЛЬНА; одну константу делят проверка живости и компакция
сайдкара; детектор САМОГО РЕШЕНИЯ —
`tools/test_owns_gate.py::test_window_seconds_literal_pin_8_hours_ро3`,
ЛИТЕРАЛЬНЫЙ ассерт в каноне: до фикса D-4 оба граничных теста
ссылались на константу СИМВОЛИЧЕСКИ, и возврат на 24 ч прошёл бы с
зелёным каноном — у решения оператора не было детектора вовсе (класс
R10(в)); граничные тесты остаются символическими сознательно.

### Д-5.1. R13(a) → подпункт (д) чека 30, ПРАВКА строки 54

**ДО:** `чек 30 метёт отклонения (форма t-286) и дословность судейского промпта (30(а), judge_prompt_pin)`
**ПОСЛЕ:** оконный детектор отклонений — подпункт (д) чека 30, абзац
«ОТКЛОНЕНИЯ ОТ ЛИСТ-МАРШРУТА» (заведён 2026-08-20 узлом B по находке F5:
до него R13(a) ядра называл чек 30 своим оконным детектором, а шага
сверки в чеке не было вовсе — экземпляр F-41); критерий: каждый
ЛИСТ-КЛАССОВЫЙ диспатч окна, чей `accepted` не несёт `basis "judge"`,
обязан иметь ЗАПИСАННУЮ ПРИЧИНУ в notes события той же задачи, голое
`basis "critic"/"queued-to-lead"` причиной не считается, пустое окно
отмечается ЯВНО с позитивным контролем; дословность судейского промпта —
30(а), judge_prompt_pin.

### Д-5.2. F-49 → чек 29, ПРАВКА справочника (строка 217)

**ДО:** `- чек 29 — F-49: полнота финал-сообщений воркеров.`
**ПОСЛЕ:** `- чек 29 — F-49: полнота финал-сообщений воркеров; ЖИВОСТЬ
ПРОВЕРЯЕТСЯ ПО ТЕКСТУ, не по номерам: четыре якоря — пункт, начинающийся
словами «Финальное сообщение = ПОЛНЫЙ», в builder.md, scout.md, critic.md
и подстрока «финальный отчёт ЦЕЛИКОМ» в BLOCK_MESSAGE tools/dod_gate.py;
точечный lookup с позитивным контролем на каждый нулевой результат.
Номера пунктов роль-файлов НЕ пинуются сознательно — прежний пин
«critic.md п.15» протух перенумерацией c3f8585 и снят узлом B 2026-08-20
(находка F9).`

### Д-5.3. 18(д) оконные аргументы, ПРАВКА справочника (строки 161-166)

**ДО:** `18(д) — spend-отчёт (M4/M10/топ-стоков/run_units); база контрфакта`
**ПОСЛЕ:** `18(д) — spend-отчёт (M4/M10/топ-стоков/run_units), прогон
ТОЛЬКО оконными аргументами `--window-start <ts прошлого calibrated>
--window-end <ISO момента прогона> --compare prev`: форма `--window last`
ЗАПРЕЩЕНА как форма прогона калибровки — она резолвится в ПОСЛЕДНЕЕ
ЗАКРЫТОЕ окно, а своё закрывается лишь записью `calibrated` в конце
прогона, поэтому печатает числа ЧУЖОГО окна (назван №7, сработал повторно
на №8, починен узлом B); `custom:`-окно и строка «н/д (окно пусто)» —
НОРМА, не дефект; база контрфакта`

### Д-6. Режимы узлов C и D — ПРАВКИ строк 55 и 41

**Строка 55, к given-path слою дописать:** с 2026-08-20 (узел D +
вердикт t-554) у слоя ДВА СУЖЕНИЯ МОЛЧАНИЯ и одно расширение
распознавания: (1) относительный путь освобождается маркером ВНЕШНЕГО
деплоя только ПО ЛОКАЛЬНОСТИ — когда КАЖДОЕ его вхождение лежит в том же
АБЗАЦЕ, что и маркер; хотя бы одно вне — путь проверяется как свой
(прежняя глобальная форма гасила 21% потока, включая пути СВОЕГО корня);
(2) путь, все вхождения которого в owns-СЕКЦИИ, не проверяется — диспатч
его СОЗДАЁТ (временной край R11(c)); (3) секция манифеста распознаётся
markdown-ЗАГОЛОВКОМ и БУЛЛЕТОМ (14 и 15 из 237 боевых промптов), и
owns-секция получила ВЕРХНЮЮ ГРАНИЦУ — прозаическая строка со словом owns
больше не гасит проверку до конца промпта.

**Строка 41, к owns_gate дописать:** с 2026-08-19/20 у разбора декларации
ТРИ уточнения режима: (а) декларация признаётся, только когда САМ МАРКЕР
в ПРОЗЕ — не в фенсе, не в цитате, не в инлайн-коде (полярность
СОЗНАТЕЛЬНО строже, чем у dispatch_gate); (б) третий исход
`QUOTED_OWNS_WARN` — маркер найден, но ВСЕ вхождения цитированы: в
сайдкар НЕ пишет; (в) заголовок и буллет распознаются наравне с жирным
маркером. Окно живости — 8 ч, отдельной строкой.

### Д-7.2. Партия 2 сканера регионов — НОВАЯ строка, секция 1

**Политика:** партия 2 (2026-08-19; класс SIBLING_MAP «сторож не
отличает утверждение автора от цитируемого содержимого»): у трёх сторожей
совпадение маркера засчитывается ПО РЕГИОНУ позиции, а не голым regex;
решение Lead — **СТРОЖЕ ТОЛЬКО ЧЕРЕЗ WARN**: в блокирующем гейте
регион-фильтр двигает exit_code исключительно в сторону СНЯТИЯ блока.
**Режим:** WARN-ХУК ×2 + ГЕЙТ КОММИТА.
**Реализация:** сканер `tools/md_regions.py`; потребители — dispatch_gate
(цитируемый write-глагол не включает проверку 2; новые слои WRITE-QUOTED
WARN и DoD-цитаты), owns_gate (маркер только в прозе, `QUOTED_OWNS_WARN`),
mechanism_gate (`SKIP_RE`/`TIER_LINE_RE` только когда позиция — проза,
то есть цитата синтаксиса отказа в фенсе больше не глушит коммит-гейт).
И-0: ЛЮБОЙ отказ сканера возвращает до-регионное поведение БАЙТ-В-БАЙТ.
ИЗВЕСТНОЕ ОГРАНИЧЕНИЕ (вердикт t-554, C-4, очередь уровня Lead):
физических сиблингов `*_md.py` в дереве НЕТ, обе ветки
`MODULE_UNDER_TEST` резолвятся в живой файл — зелень доказывает поведение
живого гейта, но НЕ дискриминацию, как обещает шапка.

### Д-7.3. Батч П4 — stdin-дедлайн — НОВАЯ строка, СЕКЦИЯ 3

**Политика:** F-62 (батч П4, 2026-08-19): ни один хук не висит вечно на
неинтерактивном stdin без EOF — `isatty()`-guard охраняет ТОЛЬКО
TTY-режим и самим присутствием маскирует неинтерактивный случай (для
SessionStart это было зависание старта КАЖДОЙ сессии).
**Режим:** ДИСЦИПЛИНА+ДЕТЕКТОР (батарея в каноне).
**Реализация:** локальная копия `_read_stdin_bytes_deadline` (поток +
`join(timeout)`, дефолт 10.0 с, env `OSLLM_STDIN_TIMEOUT` до 600 с, формы
«0 = ждать вечно» намеренно НЕТ) в 14 носителях штаба (13 хуков батча +
`critic_verdict_check`). ПРИНЯТАЯ ДЫРА (В1, решение Lead 08-19, владелец
текста — `docs/FINDINGS.md` F-62): таймаут FAIL-OPEN, payload
отбрасывается и проверка пропускается; дыра пред-существует (пустой или
битый payload дают тот же пропуск), fail-closed на Stop-классе рискует
петлёй, цена принята, замер доставки — в очереди (В2). Детектор —
`tools/test_p4_stdin_deadline.py` в каноне: K2 сверяет sha256 региона
хелпера у всех копий с пином, K5/K7 — subprocess-пробы с реально
незакрытым пайпом. Кит-твины дыру НЕСУТ — порт-очередь В11.

### Д-7.4. Сторож d15 — НОВАЯ строка, СЕКЦИЯ 6

**Политика:** d15 (решение Lead 2026-08-20, t-543; класс SIBLING_MAP
«имя модели прибито как КРИТЕРИЙ вместо резолюции привязки»): колонка
«Model here» таблицы ярусов CLAUDE.md обязана сходиться с `roles.*`
живого `delegation.config.yaml` — перепривязка обязана быть видна
КАНОНОМ, а не молча соврать правдоподобным неверным ответом (цена класса
замерена: прошлая перепривязка прожила в протоколе 12 дней, и нашёл её
встречный деплой, а не свой чек).
**Режим:** ГЕЙТ ФОРМЫ (канон).
**Реализация:** `tools/test_claude_binding_guard.py`: `parse_tiers_table()`
собирает строки ПО ФОРМЕ; `family_of()` резолвит семейство подстрокой
регистронезависимо, сверка двоичная; МОЛЧАЛИВЫЙ НОЛЬ ЗАПРЕЩЁН —
`require_nonempty` падает, если таблица не найдена или распознано ноль
строк; литеральный em-dash — ЕДИНСТВЕННОЕ значение, легально
пропускающее строку, обычный дефис и пустая ячейка ПАДАЮТ.

### Д-8. Смена режима класса (г) гигиены — ПРАВКА строки 88

К существующему «журнал мимо Edit/Write (с 07-23)» дописать: **СУЖЕНО
2026-08-20 узлом F.3 по ЖИВОМУ ЛОЖНОМУ БЛОКУ на ЧИТАЮЩЕМ вызове** — у
statement'а с read-only головой (grep/rg/cat/head/tail/wc/sed,
`_READ_ONLY_HEAD_RE`) редирект `>` больше не считается журнальной записью
сам по себе, поэтому команда вида `grep -c "x" logs/routing-log.jsonl
2>/dev/null` ДО денялась, а теперь проходит МОЛЧА; форма с ` 2>&1`
денается уже по классу редиректа. Правка БЕЗУСЛОВНА — НЕ стоит за
переключателем `PYC_DENY_ENABLED` (замер: 10 расхождений на 252 командах
при выключенном переключателе). Ветка «python -c/heredoc → БЛОК» в дереве
ЕСТЬ, но **на 2026-08-20 ВЫКЛЮЧЕНА**: `PYC_DENY_ENABLED = False`, класс
остаётся WARN; переключение — акт Lead, и тем же ходом правятся эта
строка и чек 25.

### Плюс три мелких правки

- **Легенда** (:13-32): добавить пункт `ГЕЙТ ФОРМЫ` (решение Р11(б)).
  Остальные восемь недостающих слов — строкой очереди.
- **Строка 44** (решение Р12): дописать `(литерал в контексте —
  «R6-ЗЕРКАЛО: line N attempt A без escalated»)` — карта называет слой
  внутренним именем кода `ESCALATION-слой (E1)`, которого в выводе НЕТ;
  это класс, уже починенный узлом B в чеке 11(в).
- **§8 «Сторож этого документа»** (решение Р14(а)): добавить пункт (в) —
  МАШИННЫЙ СЛОЙ `python tools/calibration_prepass.py --check-form`:
  КАЖДОЕ «чек N» этого файла обязано резолвиться в номер протокола
  (`validate_rules_backward`), а КАЖДЫЙ токен `rules:RC§N/token` шапок
  чеков — находиться словом внутри названной секции ЭТОГО файла
  (`validate_rules_forward`); плюс файл в `MECHANISM_PREFIXES`. Слой
  держит РАЗРЕШИМОСТЬ ссылок, не их СМЫСЛ.

---

## 5. КРАЯ ПОВЕДЕНИЯ

- **E-1 (байты)** — см. §2, главный край.
- **E-2 (разрешимость номеров чеков).** Любое `чек N` машинно обязано
  резолвиться. Новые строки называют чеки СТРОЧНЫМ «чек NN» намеренно:
  регекс регистрочувствителен, прописное `ЧЕК 34` в колонке режима под
  проверку не попадает. Существующие прописные вхождения НЕ переписываются.
- **E-3 (обратная сверка токенов).** Шапки чеков ссылаются на карту полем
  `rules:RC§N/token`; правки НЕ имеют права УДАЛЯТЬ токены `D-0096`,
  `F-49`, `R13`, `D-0050` из своих секций. При красном — откат правки, не
  подгонка протокола (чужой owns).
- **E-5 (пустой вход).** Если факт разошёлся с текстом драфта — строка НЕ
  пишется по памяти: докладывается расхождение. Негатив в карту — только
  с позитивным контролем той же формы, и контроль называется в отчёте.
- **E-6 (временной край).** Строка Д-8 описывает мир с
  `PYC_DENY_ENABLED = False`. ОБА мира названы; строка обязана нести
  пометку «на 2026-08-20 переключатель ВЫКЛЮЧЕН», иначе станет ложью молча.
- **E-7 (позиционный край).** Новые строки — В КОНЕЦ таблицы своей
  секции. **Инвариант:** `_split_rule_coverage_sections` режет файл по
  `^## (\d+)\. ` — строка, поставленная ПОСЛЕ заголовка «## Справочник…»
  или «## 8.», выпадет из секции 1 и порвёт `rules:RC§1/*`-резолюцию.
- **E-8 (генеративный край).** Правки Д-5 рождают новую сущность —
  ЯКОРЬ-ПО-ТЕКСТУ вместо пина по номеру. Носитель — сама строка карты;
  классификация — не новый механизм, а форма ссылки; владелец — этот
  файл; отдельного детектора не требует (свежесть уже сторожит чек 24).
- **E-9 (край таблицы).** Символ `|` внутри ячейки рвёт markdown-таблицу;
  экранировать как `\|` (прецедент — строка 38).

---

## 6. DoD

1. **Форм-гейт носителя (главный):** `python tools/calibration_prepass.py
   --check-form` → `FORM: OK (0 дефектов; 36 чеков, 58 шапок)`, exit 0.
   Он и есть механический слой: `validate_rules_backward` проверяет, что
   КАЖДОЕ `чек N` резолвится, `validate_rules_forward` — что каждый токен
   шапок находится ВНУТРИ названной секции. Сверка идёт В ОБЕ СТОРОНЫ.
   **Негативный контроль обязателен:** во ВРЕМЕННОЙ КОПИИ (не в боевом —
   гигиена п.7(г)) заменить любое `чек 24` на `чек 99` → обязана
   появиться строка о неразрешимой ссылке и exit 1.
2. **Канон:** `python -m pytest tools/ gateway/ -q`.
3. **Ключ-прогон** именованным скриптом (не heredoc, не `python -c` —
   гигиена п.4), исходник ПРИЛОЖЕН целиком, плюс негативный контроль
   заведомо отсутствующим ключом. Ключи: `GATE_A_HISTORY`,
   `check_gate_a_history_monotonic`, `ПОДЪЁМ №N`, `154000`, `57500`,
   `CLAUDE_WARN`; ОТСУТСТВИЕ `класс (б) чека 23` и `класса (б) чеком 23`;
   присутствие `шаг ОБЯЗАТЕЛЕН и АТРИБУТИВЕН`, `37500`, `37800`,
   `WINDOW_SECONDS`, `Р-О3`, `ОТКЛОНЕНИЯ ОТ ЛИСТ-МАРШРУТА`,
   `Финальное сообщение = ПОЛНЫЙ`, `--window-start`,
   `_tokens_outside_deploy_paragraph`, `_MANIFEST_SECTION_HEADER_RE`,
   `QUOTED_OWNS_WARN`, `md_regions`, `WRITE-QUOTED WARN`, `SKIP_RE`,
   `_read_stdin_bytes_deadline`, `OSLLM_STDIN_TIMEOUT`, `F-62`,
   `test_claude_binding_guard`, `Model here`, `require_nonempty`,
   `_READ_ONLY_HEAD_RE`, `PYC_DENY_ENABLED`, `ГЕЙТ ФОРМЫ`,
   `R6-ЗЕРКАЛО`, `validate_rules_backward`.
4. **Замер:** `python tools/corpus_growth.py` — строка по
   `docs/RULE_COVERAGE.md` дословно. См. §2.
5. Ни одна существующая строка не удалена.

---

## 7. ОТВЕТЫ НА ЗАПРОСЫ ФАКТОВ (добыты Lead)

- **З-1 (замер корпуса):** `docs/RULE_COVERAGE.md [corpus, строка
  реестра]: WARN | bytes=58210 records=56 bpr=1039 | Δbytes=4157
  Δrecords=1 Δbpr=56 | в полосе (95%..1058]`. **Карта уже в WARN.**
- **З-4 (размер ядра):** `CLAUDE.md` — 36998 Б при WARN 37500.
- **З-2, З-3** — не блокируют работу; исполнитель их не решает.

---

## 8. НАХОДКИ ДРАФТЕРА — вердикты Lead

**Свежие протухания карты, ПРИНЯТЫ (чинятся этим узлом или очередью):**
- **С-1** строка 44: карта называет слой `ESCALATION-слой (E1)` —
  внутреннее имя кода; в контекст печатается литерал `R6-ЗЕРКАЛО`.
  Читатель, ищущий по имени из карты, не найдёт ничего. → Р12(а).
- **С-2** строка 48: «19 файлов проводки» — в `MECHANISM_PREFIXES`
  сегодня по именам 21 путь. Число к посадке требует пересчёта. → очередь.
- **С-3** строка 49: обещает «вопрос к калибровке №8»; калибровка прошла,
  в отчёте `delegation.config` не упомянут НИ РАЗУ (греп; позитивный
  контроль `roles.lead` — 5 попаданий). Триггер выстрелил, ответа нет. →
  очередь.
- **С-4** строка 51: «121 тест» против 279 определений `def test_` в
  `tools/test_calibration_prepass.py` (витнесс узла B: 283 passed). →
  очередь.
- **С-5** строка 120: «50 пар» против 97 записей в
  `tools/parity_manifest.json`. → очередь.
- **Л-1** легенда: 9 слов в словаре против ≥18 используемых в таблицах.
  → Р11: `ГЕЙТ ФОРМЫ` сейчас, остальное очередью.
- **П-1** нет строки на D-0102 (глубина верификации разведки следует
  экзаменационному статусу привязки scout). → Р13(б), очередь.
- **П-2** §8 не называет машинного сторожа документа. → Р14(а), чинится.

**Находка 8.1 ОПРОВЕРГНУТА ЗАМЕРОМ Lead.** Драфтер утверждал, что
`tools/mechanism_gate.py:258` несёт `"tools\md_regions.py"` с ОБРАТНЫМ
слешем, из-за чего правка общего сканера регионов проходила бы commit-msg
без осевого блока. Проверка: строка 258 несёт `"tools/md_regions.py"` —
ПРЯМОЙ слеш, как у соседей (позитивный контроль той же формы: `:247`
owns_gate.py, `:295` calibration_prepass.py — та же форма). Коммит-гейт
сканер ПОКРЫВАЕТ. Класс промаха: утверждение о символе, сделанное
чтением без посимвольной сверки — тот же класс, что «негатив без
позитивного контроля», только в позитивную сторону.

**8.2 ПРИНЯТА, в очередь:** докстринг боевого `tools/dispatch_gate.py`
описывает несуществующий мир — первая строка живого файла говорит
«`dispatch_gate_md.py` — region-aware sibling of tools/dispatch_gate.py»
и «Живой dispatch_gate.py НЕ ТРОГАЕТСЯ», хотя файл ПОСАЖЕН. Буквальный
близнец находки t-533, для `owns_gate.py` уже починенной. Сиблинг того же
класса не подметён.

**8.3 ПРИНЯТА** — вошла в текст Д-3: `CLAUDE_BREACH` объявлен в трёх
местах прозы и не читается ни одной веткой кода.

**8.4 ПРИНЯТА как КЛАСС, в очередь:** три числовых протухания (С-2, С-4,
С-5) — один класс «число замера, вписанное в норму без владельца счёта».
Естественный ход — правило «в карте число сопровождается носителем счёта
либо не пишется вовсе». Решение Lead: правило заводить НЕ этим узлом,
но класс назвать в очереди — иначе следующая калибровка найдёт четвёртый
экземпляр.
