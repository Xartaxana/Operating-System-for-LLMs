# Драфт C2 (t-647, designer/opus): пин пары «реестр warn-слоёв ↔ чек 11(в)» (класс F-41)

Статус: ДРАФТ до критик-гейта (узел D петли) и приёмки Lead; развилки
НЕ решены. Написан под рекомендуемую форму (в)+(б); строки «[форма]»
меняются при ином выборе Р1.

## Спека

ИНВАРИАНТ: {name записей layers[] в tools/warn_layers.json} ==
{имена, извлечённые parse_check_11v_names из протокола}. Расхождение
в ЛЮБУЮ сторону падает в каноне, а не печатается в недельном отчёте.

1. [форма] Текст чека 11(в) (протокол :937-945) дополнить ЧЕТЫРЬМЯ
   именами с носителями: DOD-QUOTED, MANIFEST-QUOTED — к группе
   «— tools/dispatch_gate.py»; JOURNAL ECHO — к группе
   «— tools/journal_echo.py»; «Командная гигиена —
   tools/hygiene_gate.py» — новым сегментом ПЕРЕД «ПЕРЕЧЕНЬ ОТКРЫТ».
   Формат строго «ИМЯ, ИМЯ — tools/<файл>;» (парсер warn_density.py
   :581-613 режет по «;», отрезает « — tools/», удаляет «(...)»).
2. tools/warn_layers.json: listed_in_check_11v → true у DOD_QUOTED
   (:55), MANIFEST_QUOTED (:68), JOURNAL_ECHO_BASE (:214), HYGIENE
   (:268). Поле — ручная ТРЕТЬЯ копия факта (registry_listed в
   diff_check_11v:621 вычисляется и НЕ используется) — судьба Р4.
3. [форма] tools/test_warn_density.py:987-992 — тест, который сейчас
   ФИКСИРУЕТ расхождение как ожидаемое (assert in_reg_not_check ==
   {4 имени}), переписать в пин инварианта: обе стороны пусты +
   assert check_names is not None + assert непустоты ОБЕИХ сторон.
   Форма — буквальный порт кит-твина toolkit/tools/test_warn_density.py
   :730-739.
4. НЕГАТИВНЫЙ КОНТРОЛЬ: отдельный тест на СИНТЕТИКЕ в tmp_path —
   копия текста чека с удалённым живым именем при живом реестре →
   непустой in_reg_not_check, проверка падает. Живой PROCESS/*.md не
   портится (п.7(г)).
5. Правки 1+2+3+4 — ОДИН коммит (между 3 и 1 канон красный).

КРАЯ: К1 парсер не нашёл блок → None → diff молча ([],[]) → пин
вечнозелёный; тест падает по assert not None (чинить ли warn_density
— Р2). К2 пустой реестр/перечень → assert непустоты. К3 новый слой →
тест краснеет НАМЕРЕННО, имя дописывается тем же коммитом (вводимая
обязанность). К4 переименование name → обе стороны одним коммитом.
К5 конфликт 1×3 → один коммит, разделение запрещено. К6 toolkit/**
не трогается (перечень кита полный, пин уже стоит).

DoD (вывод = witness): узкий pytest test_warn_density -q зелёный;
python tools/warn_density.py --check → exit 0; канон зелёный.
Ключи: (i) новый пин И негативный тест видны в выводе; (ii) --check
без ДЕФЕКТА РЕЕСТРА по 4 записям; (iii) канон.

given (декларативно): warn_layers.json (20 записей, false ровно у 4);
warn_density.py :581-625 парсер+diff, :1366-1377 «НАХОДКИ О ЧЕКЕ»
(на exit-код НЕ влияет, main:1625), :1471-1516 run_check (сверки с
чеком НЕТ); test_warn_density.py :977-992; протокол :931-972;
кит-твин toolkit/tools/test_warn_density.py :723-739; прецедент
теста на живом PROCESS/*.md — tools/test_rule_coverage_bodies.py:36.
owns: PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md, tools/warn_layers.json,
tools/test_warn_density.py. non-goals: warn_density.py (кроме исхода
Р2), toolkit/**, доли/знаменатели, перенос чека в PROCESS/checks/.
handoff: дифф + witness трёх прогонов + перечень имён построчно.

## Четыре вопроса R10 — (а)/(в)/(г)

(а) Разово: 4 имени + 4 поля + ~20 строк теста. Впредь: автор нового
слоя платит правкой ДВУХ носителей одним коммитом (~1 мин);
обнаружение переносится с недельной калибровки на локальный прогон.
Ось 15: популяция = layers[] реестра — 20 (чтение 08-27), в чеке 16,
расхождение 4.
(в) Первичный — сам канон-тест; отказ детектора (None-парсер) закрыт
краем К1; второй канал — секция «НАХОДКИ О ЧЕКЕ» (полный прогон);
регистрация: строка «машинный пин: tools/test_warn_density.py::<имя>»
в теле чека (входит в правку 1).
(г) Замерено: .githooks/pre-commit pytest НЕ запускает → форма (б)
стоит на канон-прогоне (R11 самопрогон + witness приёмки) — явная
строка «держится дисциплиной» с названным детектором (в); усиление
fail-closed — Р5.

## Развилки (решает Lead; Р6-owns — оператор)

- Р1 форма (а) генерация / (б) тест-пин / (в) разовая правка.
  Рек.: (в)+(б). Основание «почему находка не сконвертировалась»:
  секция печатается только полным прогоном (не --check), на exit-код
  не влияет, а канон-тест :987-992 ПИНИТ расхождение как ожидаемое —
  машинный слой защищает протухший чек; чистая (в) не существует.
  (а) отклонена: вокруг перечня живёт проза-норма, генератор забрал
  бы владение md ради 4 имён.
- Р2 None-парсер: рек. (i) только в тесте; (ii) маркер в
  compute_run_defects → exit 1 (задевает кит-твин); (iii) как есть —
  отклонено.
- Р3 обязанность реакции на «НАХОДКИ О ЧЕКЕ»: рек. (i) строка-
  обязанность в теле чека («находка о чеке закрывается правкой чека
  в том же ходе»).
- Р4 судьба listed_in_check_11v: рек. (ii) запинить третьим assert
  (значение поля == факт присутствия имени в чеке).
- Р5 место исполнения: рек. (i) канон-тест; (ii) wiring_check
  pre-commit fail-closed — отдельное решение (расширение устава).
- Р6 кит: у кита ЛОЖНОЕ навигационное утверждение того же класса —
  toolkit/PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md:222 обещает у
  --check секцию сверки, которой нет в коде обеих сторон. Рек. (ii)
  отдельный узел. Расширение owns на toolkit/** — слово ОПЕРАТОРА
  (мораторий D-0074).
- Р7 куда лечь именам: рек. (i) к существующим группам носителей.

## Доложенные противоречия брифа (D-0096 п.5)

1. tools/derive_anchors.py НЕ существует (Glob-контроль; механизм
   якорей — toolkit/tools/install_parity.py --emit-anchors; CHK-маркеры
   читает tools/calibration_prepass.py) — given-дефект диспетчера.
2. docs/tasks/2026-08-25_queue8-closure.md:18 «пара 11(в)↔warn_layers
   ЗАКРЫТА узлом B, 14 имён» — протухла: 20/16/4 (ещё один F-41).
3. Кит-протокол :222 — ложное обещание (см. Р6).
