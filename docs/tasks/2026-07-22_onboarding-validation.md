# Валидация онбординга D-0090 — два полигона (DAG, D-0080)

Заказ: слово оператора 2026-07-22 «да, сделать валидацию на новом
проекте и на "живущем" D:\Dog». Процедура — по драфту
docs/tasks/2026-07-22_onboarding-architecture.md (чеканка D-0090).
Первая задача каждого полигона — evaluation, не build (D-0030).
Кит-артефакты (протокол, шаблон ledger) на момент старта НЕ
существуют (мораторий D-0074) — валидация гоняет процедуру
ВРУЧНУЮ по драфту; трение ручного прогона = прямой вход кит-батча.

Критерии успеха валидации (предрегистрация, до прогонов):
- V1: инвентаризация шага 0 заполняется по драфт-опроснику без
  до-изобретения вопросов (пробелы опросника = находки).
- V2: каждый кит-механизм получает статус в ledger без остатка
  «не знаю куда отнести» (остаток = дефект таксономии статусов).
- V3 (brownfield): ни одна установка слоя не требует изменения
  существующих носителей/потоков хоста (нарушение = дефект
  принципа 4).
- V4 (greenfield): полный комплект встаёт связками без ручного
  до-тюнинга предпосылок (ручной шаг = пробел манифестов).
- V5: расхождение процедуры с реальностью любого рода — находка в
  FINDINGS (класс F-51/F-35), не молчаливый обход.

## Ветка B — brownfield: D:\Dog (живущий чужой проект)

| Узел | Что | Ярус/исполнитель | Статус |
|---|---|---|---|
| B1 | Шаг-0 инвентаризация функций хоста (профиль, приёмка, журнал, агенты, очередь, риски) | scout-лист t-268, судья-приёмка (2 reject haiku → эскалация sonnet → accept v3) | DONE 07-22. ФАКТ, уточняющий профиль ветки: Dog = живущий деплой НАШЕЙ OS (c45f997, R1–R13, хуки, журнал) с доменными надстройками (вики-конвейер с LLM-ownership, telegram, 6 своих осей SIBLING_MAP) — ветка B валидирует АПГРЕЙД-онбординг AO3-класса, не чистый чужой brownfield; вопрос ветки C (полностью чужой хост) — у оператора |
| B2 | Карта эквивалентов: draft ledger по инвентаризации B1 (статусы по каждому кит-механизму) | Lead (сопоставление — Lead-класс; входы t-268 + t-269, второй — через исчерпанное R6-зеркало и failed-back) | DONE 07-22 — docs/tasks/2026-07-22_dog-adoption-ledger.md (первый ledger нормы D-0090; adopt-набор: policy refresh + wiring-check + critic-verdict + escape-allowlist; judge-приёмка = native-equivalent для вики через их LLM-lint + deferred для кода; гейт-свита deferred по Rule #1) |
| B3 | Слой 1 measure-first: план учёта поверх носителей Dog БЕЗ миграции; план предъявлен оператору внутри B2-ledger (касание разрешено словом 07-22) | Lead | DONE 07-22 (учёт у Dog уже стоял с c45f997 — measure-first выполнен установкой; план B3 = adopt-набор ledger'а) |
| B4 | Установка adopt-набора + замер трения (V3) | builder t-271, судья-приёмка (1 reject: грануляция ledger — вопрос-вверх разрешён координатором → accept v2); постановка Lead: escape_check активирован на их pre-commit, critic-схема staged до их экзамена (их правило 10), wiring-активация staged (харнесс отказал в записи чужого settings.json); коммит Dog c3203b2 через их гейты | DONE 07-22: их тесты 224=224 (дельты нет), чужие untracked нетронуты, домен нетронут |
| B5 | Вердикт ветки по V1–V3/V5 | Lead | **DONE 07-22, вердикт: АПГРЕЙД-ОНБОРДИНГ ПОВЕРХ ЖИВУЩЕГО ДЕПЛОЯ ВЫПОЛНЕН БЕЗ СЛОМА НАТИВНОГО.** V3 PASS: все установки аддитивны, носители хоста не мигрированы, их дисциплины уважены конструкцией (критик-роль — через ИХ экзамен: предпосылочное замыкание D-0090 сработало вживую; hook-конфиг — рукой хоста). V5-находки процедуры: (i) активация hook-конфига чужого проекта недоступна установщику по границе харнесса — онбординг-протокол кита обязан нести шаг «хост применяет hook-конфиг сам»; (ii) два spec-дефекта DoD диспатчера за день (нумерология «18», клейма про штаб) — кейсы R11 калибровке №4; (iii) кросс-репо форма escape-пинов подтверждена второй раз (после AO3) |

## Ветка G — greenfield: новый пустой проект

| Узел | Что | Ярус/исполнитель | Статус |
|---|---|---|---|
| G1 | Полигон: пустой каталог D:\Improving_AI\onboard_greenfield (место УТВЕРЖДЕНО словом 07-22 «ок») + git init | builder | DONE 07-22 (t-270) |
| G2 | Прогон процедуры: инвентаризация (пустая карта) → ledger «всё adopt» → установка полного комплекта по драфту | builder t-270, судья-приёмка (1 reject: недекларированная инверсия ledger/install → Finding C → accept v2) | DONE 07-22: 6 коммитов, 796 passed, гейты активны и проверены пробами |
| G3 | Замер: boot-бюджет свежего деплоя, число ручных шагов (V4), baseline-учёт с первого дня | доставлено сдачей t-270 | DONE 07-22: boot 40355/100000 (двойной замер); 5 ручных шагов; журнал полигона жив с первого коммита |
| G4 | Вердикт ветки по V1–V2/V4–V5 | Lead | **DONE 07-22, вердикт: ПРОЦЕДУРА ЖИЗНЕСПОСОБНА в greenfield.** V2 чист (0 остатка ledger). V4: 5 ручных шагов — все от пробелов кит-доков (clone-vs-подкаталог, момент hooksPath в Path A, headless-ветка вопроса контура, ручной Boot Report), вход кит-батча. V1/V5: 8 находок, из них несущие — (i) ДЕФЕКТ КИТА Finding B: preflight_quota.load_config глушит SessionStart-хук для ДЕФОЛТНОГО subscription-контура (батч мелочей + порт-очередь); (ii) Finding C: драфт-процедура неоднозначна о форме ledger (номенклатура-до vs файлы-после установки) — уточнить в кит-протоколе; (iii) экзамены онбординга структурно невыполнимы одной builder-сессией — онбординг-скилл кита обязан вести их отдельными диспатчами координатора (уточнение скилла, кит-батч) |

Ветка C (полностью чужой хост без нашего фреймворка): НЕ открывается
— слово оператора 07-22 «у меня нет сейчас ещё одного проекта, так
что пока не делаем»; триггер переоткрытия — появление кандидата.

## Выход DAG

Сводный вердикт обеих веток → вход кит-батча порт-очереди
(онбординг-протокол, ADOPTION_LEDGER.md, манифесты предпосылок —
исполняются по слову, D-0074) и §11 Limitations WP при ревью.
Статусы узлов двигаются тем же ходом, что их журнальные события
(D-0080); журнальные task_id вписываются в узлы по мере выдачи.
