# Спека: машинный слой FRESHNESS (п.5 чеклиста, промоция R11(n)) — драфт t-599, принят Lead 15:40

## ПОПРАВКА LEAD 17:5x — фикс-раунд по вердикту критик-гейта (ДВА блокера)

Полный вердикт — транскрипт критик-агента t-606. Решения:

- **Б1 (logs/ мёртв):** фильтр исключений параметризуется списком
  регексов-сканеров (по умолчанию given-пара — популяция чужого слоя
  НЕ меняется; freshness передаёт СВОИ три) — logs-кандидаты проходят
  owns/non-goals/run-line/деплой-подавление наравне с остальными и
  доходят до проверки файла. Батарея: logs/routing-log.jsonl:999999 →
  WARN, :100 → тишина; остальные шесть префиксов — регресс.
  Знаменатель оси 15 становится честным (популяция = предикат).
- **Б2 (ложный варн на кит-ссылки):** класс (а) получает ТЕ ЖЕ
  подавления, что класс (в): owns/non-goals/строка-прогона/абзац
  чужого деплоя (симметрия — критик показал: у (а) НИ ОДНОГО
  исключения). Носитель кит-ссылок — слова «кит»/«toolkit» в том же
  предложении/абзаце: если существующая деплой-механика
  (_load_deploy_markers/абзацное подавление Р1) их не покрывает,
  маркер «кит-»/«toolkit» добавляется в подавление КЛАССА (а)
  (только его — популяция (в) не трогается). Корпус критика — в
  тесты: «кит-чек 22(b)», «toolkit … check 22(d)» → тишина; голое
  «чек 22(b)» без кит-контекста → варн (законно: ссылка обязана
  нести маркер деплоя); «штабной чек 32(д)» → M1 (истинный позитив).
- **Ф5 тем же ходом:** порядок веток — СНАЧАЛА _freshness_check_exists
  (чека нет → M2), латиница M4 только для существующего чека.
- **Ф1 (ложные БЛОКИ охраны F2 — живые):** _WRITE_RETURN_TARGET_GUARD
  расширяется: цель-возврат распознаётся в любом падеже
  (отчёт|дайджест|доклад|ответ + окончания) и через ≤2 промежуточных
  слова («в свой отчёт», «в доклад краткий»); пять фраз критика → НЕ
  матч, прежние позитивы («внеси правку в tools/x.py», «добавь строку
  в X.md») → матч.
- **Ф2:** левая граница слова у якорного слова класса (а)
  ((?<![^\W\d_]) или \b) + тест «число строчек 30(щ)» → тишина.
- **Ф3:** кэш на вызов: {файл: line_count} и {(чек, буква): вердикт} —
  константы лимитов не меняются.
- **Ф4:** финальный witness перегоняется на сдаваемом дереве
  (узкий прогон + канон снимет координатор).

## РЕШЕНИЯ LEAD ПО РАЗВИЛКАМ

- Ф1(b): состав первого инкремента — классы (в) якорь файл:строка И (а) токен подпункта чека. Классы (б) цитаты и (г) идентификаторы — НЕ делаются.
- Ф2(a): класс (а) — ТОЛЬКО с якорным словом (чек/чека/чеку/чеке/чеки/check/CHK); голая форма «13(а)» не извлекается.
- Ф3(b): оракул подпунктов — дизъюнкция O1 (якоря `<!--CHK NN(х)|...-->`) ∪ O2 (литерал NN(х) где угодно) ∪ O3 (вхождение (х) в регионе чека NN от якоря CHK NN| до следующего верхнего). Найдено хоть одним → тишина. Чека NN нет вовсе → M2.
- Ф4(b): гомоглиф (латинская буква в скобках при кириллице протокола) → ОТДЕЛЬНОЕ сообщение M4 «буква латинская, токен не найдётся ни грепом, ни глазами; замени символ» — то же правило трёх, тот же префикс.
- Ф5(b): reachable — новый ИЗМЕРИМЫЙ вид `dispatch_prompt_freshness_token` в POPULATION_KINDS_MEASURED warn_density + предикат «tool_name ∈ {Task,Agent} И предфильтр слоя нашёл ≥1 кандидата», ИМПОРТОМ обоих регексов из dispatch_gate (не копией). Предикат — НАДМНОЖЕСТВО условия варна (иначе ДЕФЕКТ ПРЕДИКАТА exit 1).
- Ф6(b): POPULATION_RULE_VERSION → 2 (+ обновление ожидающих тестов).
- Ф7(a): файла нет вовсе → молчание (говорит GIVEN_PATH).
- Ф8(a): свой порог сводки 20/21, ПОКЛАССОВО.
- Ф9(a): правки чека 11(в) и RULE_COVERAGE — тем же коммитом/ходом.
- Ф10(a): один соло-билдер на все owns. Ф13(a): вызов — хвост warn_parts. Ф14(a): без кэша. Ф15(a): детектор — доля в чеке 11(в) + пин-тесты позитив/негатив в каноне.
- Ф12(a): ЧЕСТНАЯ ГРАНИЦА 1.5/4 исторических экземпляров — строкой в RULE_COVERAGE и в спеке; + ограничение «якорь В ПРЕДЕЛАХ файла, но не туда — не ловится».

## СОСТАВ (нормативно, из драфта)

- Класс (в): токен `<путь>.<ext>:<N>[-<M>]` → файл существует и читается и строк СТРОГО меньше max(N,M) → M3. СОБСТВЕННЫЙ регекс FRESHNESS_LINE_ANCHOR_RE с префиксами `(tools|gateway|PROCESS|docs|logs|\.claude|\.githooks)` (logs ВКЛЮЧЁН — общие GIVEN_*_RE НЕ трогать, их популяция чужая) + абсолютная win-форма; якорное двоеточие ПОСЛЕ обязательного .ext.
- Класс (а): FRESHNESS_CHECK_TOKEN_RE — якорное слово + ≤3 пробельных + (\d{1,2}) + опц. пробелы + \(([а-яa-z])\).

К1 `freshness_warn(payload) -> str` в tools/dispatch_gate.py, форма как given_path_warn: чистая, "" = нечего, наружу не бросает. К2 decide() не тронута НИ БАЙТОМ, exit_code не меняется. К3 вызов в main() в ХВОСТ warn_parts (после warn_write_quoted), свой try/except, только exit_code != 2. К4 матчер Task/Agent, иные — молчание без чтения диска. К5 предфильтр: нет кандидатов → "" ДО файловой системы (тест счётчиком open). К6 региональная осведомлённость через единый _safe_scan/_is_quoted; _safe_scan() is None → слой молчит ЦЕЛИКОМ. К7 класс (в): варн только когда файл существует, читается, строк меньше. К8 класс (а): варн только когда буква не найдена НИ ОДНИМ из трёх оракулов; оракул строится один раз на вызов. К9 дедуп: токен N раз → одно упоминание (normcase). К10 тексты M1-M4 по правилу трёх, общий байт-в-байт префикс `FRESHNESS WARN:`, провенанс (D-0096 п.5) в хвосте. К11 строка реестра warn_layers.json c reachable (Ф5). К12 имя в перечень чека 11(в). К13 строка RULE_COVERAGE с честной границей + детектором. К14 лимиты с тестами НА и ЗА.

Тексты M1 (подпункта нет: перечень факта ≤8 букв, действие «открой протокол, подставь фактическую букву»), M2 (чека нет вовсе: «ошибка в НОМЕРЕ, не в букве»), M3 (якорь за концом файла: «перечитай носитель, обнови номер либо сними его, оставив путь»), M4 (гомоглиф) — формы в драфте t-599, транскрипт агента; финальные формулировки билдера обязаны нести все три части правила трёх.

## ЛИМИТЫ (тест НА и ЗА): L1 токенов в сообщении на класс 20/21→сводка «первые 3», варн не гасится; L2 байт из одного файла 2 МиБ, больше → молчание; L3 промпт 300 000 симв., больше → слой молчит целиком без чтения диска; L4 файлов за вызов 8, сверх — молчание, сообщение НЕ упоминает непроверенные; L5 номер строки ≤7 цифр; L6 номер чека 1-2 цифры. Числа — оценка (не замер); менять = правка константы + граничных тестов.

## ПУСТЫЕ/ОТСУТСТВУЮЩИЕ (все молчание): payload не dict · нет tool_input · prompt не строка/пустой · cwd пустой (фоллбек os.getcwd()) · протокола нет/пуст/не читается · файл якоря каталог · оракул пуст (ни одного CHK) → молчание целиком. ИСКЛЮЧЕНИЕ: файл якоря 0 строк при `:1` → это M3 (0<1). `:0`/отрицательные → токен отброшен. Нечитаемая кодировка → errors="replace".

## ПОЗИЦИОННЫЙ ИНВАРИАНТ: ветки exit_code==2 недостижимы; порядок given→role→dod→manifest→write не меняется (пин-тест); один JSON на stdout, склейка "\n\n"; отказ слоя не мешает старым (try/except); число вызовов _safe_scan в чужих ветках не меняется.

## ВРЕМЕННОЙ КРАЙ: строка реестра — В ЭТОМ ЖЕ ходе (иначе слой вне замера, класс оси 15); якорь на файл, который диспатч САМ создаёт (путь в owns) — подавление переиспользованием owns-фильтра _filter_given_candidates (А-К1), второй не писать.

## БАТАРЕЯ A1-A17 (каждый пункт — тест): мохибейк; BOM/CRLF; NBSP/узкий пробел/zero-width внутри (а); гомоглиф (→M4); смешанный путь tools/тест_x.py:10; токен в фенсе/инлайн-коде/ёлочках/вложенных кавычках; незакрытый фенс (единая полярность — не цитата); токен в цитате И в прозе → проверяется; промпт 1МиБ → молчание, ровно L3 → скан; токен ×5000 → одно упоминание; 25 битых якорей → сводка; x.py:12:34 и D:\...:100; :0/:-5/:00012/:99999999; путь вне репо (_is_under_root); токен в owns/прогон-строке/не-целях → подавление А-К1/А-К2/А-К4; \x00 в prompt, нестроковый prompt; протокол без единого CHK → молчание.

## РЕЕСТР: {"id":"FRESHNESS","name":"FRESHNESS","carrier":["tools/dispatch_gate.py"],"symbol":null,"literal":"FRESHNESS WARN:","aliases":[],"hook_event":"PreToolUse","matcher":"Task|Agent","denominator":"Z1","listed_in_check_11v":true,"reachable":"dispatch_prompt_freshness_token"} (вид по Ф5(b)).

## Дано (D-0106): tools/dispatch_gate.py (несущие точки :1650-1697 фильтры, :1700-1786 given-слой, :1022-1060 _safe_scan/_region_at/_is_quoted, :2207-2271 main; ВНИМАНИЕ: файл сегодня правлен узлом K t-601 — строки могли сдвинуться, сверяй по именам функций) · tools/warn_layers.json · tools/warn_density.py (:216-289, :405-455, :1162-1175) · PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md (якоря CHK; чек 11(в) :896-942) · docs/RULE_COVERAGE.md (:8-11, :61) · docs/SIBLING_MAP.md ось 15 · docs/tasks/2026-08-25_warn-population-class.md (:165-199) · tools/test_dispatch_gate.py, tools/test_warn_density.py · эта спека.

## owns: tools/dispatch_gate.py, tools/test_dispatch_gate.py, tools/warn_layers.json, tools/warn_density.py, tools/test_warn_density.py, PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md (только перечень чека 11(в)), docs/RULE_COVERAGE.md.
## non-goals: кит-твин; классы (б)/(г); правка decide() и блок-вердиктов; ЧУЖИЕ warn-слои и их регексы/строки реестра; пороги чужих слоёв. База — ТЕКУЩЕЕ рабочее дерево (несёт непринятые правки узла K t-601 — это легальный базис, НИЧЕГО не откатывать, porcelain непуст законно).

## СУЖЕННЫЙ WITNESS (волна 2, R4; канон — координатор на сходе):
1. `python -m pytest tools/test_dispatch_gate.py tools/test_warn_density.py -q`
2. `python tools/warn_density.py --check`
3. живая проба ПАРОЙ в тестах: позитив (`чек 30(щ)` + `tools/dispatch_gate.py:999999` → оба сообщения, exit 0) и негативный контроль (`чек 30(д)` + `tools/dispatch_gate.py:100` → пусто, exit 0)
4. doc-witness R11(l): ключи ДОСЛОВНО до прогона («FRESHNESS WARN:» в реестре; FRESHNESS в 11(в); FRESHNESS в RULE_COVERAGE) + негативный ключ FRESHNESS-NOTAKEY → скрипт обязан отчитать провал; скрипт полным исходником.
5. `python tools/calibration_prepass.py --check-form`
Handoff: фактические L1-L4 если менялись; замеченные протухшие якоря — докладом; оракул (а) даёт варн на КОРРЕКТНЫЙ живой токен → СТОП и вопрос Lead.
