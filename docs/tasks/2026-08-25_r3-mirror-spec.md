# Спека: слой R3-ЗЕРКАЛО (протечка F7) — драфт t-604, принят Lead 16:16

## ПОПРАВКА LEAD 17:0x — фикс-раунд по вердикту критик-гейта (БЛОКЕР посадки; узел 1 верен)

Полный вердикт — транскрипт критик-агента (t-609). Решения:

- **Б1 (байтовый бюджет / недренирующий потребитель, ось карты :1003).**
  Замер критика: 4 строки accepted/builder → stdout 6835 Б при ёмкости
  пайпа 4096 Б (t-595); до-R3 максимум слоёв 5250 Б — канал уязвим и
  без R3. Фикс тройной, в сиблинге: (i) ПОРТ ДЕДЛАЙНА ЗАПИСИ —
  функция по образцу `_write_stdout_deadline` tools/session_context.py
  (тот же контракт: 5с / env-переопределение OSLLM_STDOUT_TIMEOUT,
  демон-поток, недописанность → немедленный тихий выход процесса с
  кодом 0 — у journal_echo exit ВСЕГДА 0, ветка не меняет этого),
  применяется к ФИНАЛЬНОЙ записи stdout в main(); провенанс-коммент
  называет источник (session_context, t-587) и ось карты; (ii) тексты
  M1/M2 СОКРАЩЕНЫ (ниже, дословно); (iii) потолок сегмента ПО БАЙТАМ:
  MAX_R3_BYTES = 2600 на json-провод (ensure_ascii) — при превышении
  строки сворачиваются в «+K more» раньше лимита строк; тест НА и ЗА.
  Новый эксперимент не нужен — свойство замерено t-595 (п.6(а)).
- **Ф3 → СИГНАЛ S5.** Голый токен `critic:t-NNN` в notes accepted-
  строки (регекс `\bcritic:t-\d{3,}\b`) глушит M1 и M2, но ТОЛЬКО
  если указанный t-NNN существует в файле как delegated(agent=critic)
  (множество critic_task_ids уже собрано — валидация бесплатна);
  токен на несуществующий вердикт НЕ глушит. Форма зеркалит
  узаконенный closes:t-NNN.
- **Новые тексты (заменяют старые M1/M2 дословно):**
  M1: `R3-ЗЕРКАЛО: line {N} accepted builder {T}: нет критик-входа под этим id и нет концессии - чек 2 прочтёт приёмку как самосертификацию; закрой: delegated(critic) по {T} / токен critic:t-NNN на покрывший вердикт / "critic: skipped, <причина>" (приёмщик строго выше)`
  M2: `R3-ЗЕРКАЛО: line {N} basis=critic по {T}, но delegated(critic) под ЭТИМ task_id нет - основание механически не прослеживается; закрой: токен critic:t-NNN на покрывший вердикт ЛИБО delegated-запись`
- **Ф4.** Приоритет КОДИФИЦИРОВАН: строка basis=critic без
  делегирования при наличии литерала концессии — ПРОТИВОРЕЧИВАЯ
  запись, M2 верен (тишина была бы прощением противоречия). Пины:
  этот случай + task_id=="" (пропуск строки) + task_id из пробелов.
- **Ф2.** Три постоянных теста НЕ утверждают на живом
  logs/routing-log.jsonl: срез журнала замораживается фикстурой
  (файл в tools/ или инлайн), живой проход остаётся разовым
  К6-witness'ом / неутверждающей пробой.
- **Посадка (узел 2, Lead) дополняется:** честная шапка (перечень
  слоёв ВКЛЮЧАЕТ R3-ЗЕРКАЛО, фраза «разница от живого» удаляется);
  строки честной границы №9 (бандл-паттерн: S5 — узаконенный выход)
  и №10 (S2/S5 не отличают цитату от утверждения — консервативное
  направление); пересверка диффа непосредственно ПЕРЕД байт-копией
  (ось :661); оси коммита: :1003 (покрытие journal_echo дедлайном,
  пара носителей дедлайна — кандидат унификации), :758 (закрыта Ф2).

## РЕШЕНИЯ LEAD ПО РАЗВИЛКАМ

- Р1(г): write-time WARN в journal_echo СЕЙЧАС; назначенный Р-О5 прогон чека 2 на Session End ОСТАЁТСЯ очередью; блокирующий гейт — по рецидиву. Решение о warn ЯВНОЕ, Р-О5 не отменён (Р-О5 отвергал гейт, warn ≠ гейт).
- Р2(а): сиблинг tools/journal_echo_r3.py + байт-копия Lead'ом на посадке (прецедент q503); тесты параметризованы целью R3_TARGET=live|sibling (прецедент F61_TARGET).
- Р3(а): basis:"judge" глушит; коллизия норм R3×R13(г) — ВОПРОСОМ ОПЕРАТОРУ (вносит узел Л).
- Р4(б): скан батча в ОБЕ стороны (предварительный проход new_lines за критик-делегированиями).
- Р5(а): ярус приёмщика при «critic: skipped» НЕ сверяется (M3 не строится); по рецидиву.
- Р6(а): ретро-события НЕ глушатся.
- Р7(а): литерал концессии — только `critic\s*:\s*skipped` (ignorecase).
- Р8(а): при used_fallback слой РАБОТАЕТ (семья TIER/WITNESS/ESCALATION).
- Р9(а): литерал слоя `R3-ЗЕРКАЛО: line `.
- Р10(а): строки в ядро CLAUDE.md НЕТ, достаточно RULE_COVERAGE.

## ПРЕДМЕТ И ЧЕСТНАЯ ГРАНИЦА

Слой судит ПОЛНОТУ ЗАПИСИ builder-приёмки (R3+D-0058), не размер диффа. Восемь строк «НЕ ловит» из драфта — ОБЯЗАТЕЛЬНАЯ часть строки RULE_COVERAGE: не измеряет объём; не судит законность концессии; не видит незаписанные приёмки; не видит незажурналированный критик-вход; литерал, не свободная формулировка; только agent=builder; слеп к записям не-Edit/Write-путём; не ретроактивен.

## ТРИГГЕР И СИГНАЛЫ

Триггер: строка echo_new_lines, dict, event=="accepted", agent=="builder". Тишина по строке при ЛЮБОМ из: S1 basis=="critic"; S2 notes матчит CRITIC_SKIP_RE = `critic\s*:\s*skipped` (i); S3 delegated(agent=="critic") с тем же task_id где угодно в файле (base_lines + ВЕСЬ текущий батч в обе стороны, Р4(б)); S4 basis=="judge". Состояние per-task_id тем же единственным линейным проходом, что _collect_escalation_events (:1431-1490) — прямой близнец по форме. Нового I/O ноль.

M2-детектор: basis=="critic" БЕЗ delegated(critic) по этому task_id во всём файле → «заявленное основание без делегирования» (основание: batch3-спека :173, подшаг чека 2).

## СООБЩЕНИЯ (правило трёх, ASCII-дефис, литералы статичны, динамика через _raw_sanitize/_ascii_sanitize по каналу)

M1 (ни одного сигнала): «R3-ЗЕРКАЛО: line {N} accepted builder task_id {T} без критик-входа и без пометки concession - R3 обязывает критика на диффах >100 строк / схеме / ядре / деньгах, а приёмка без входа выше исполнителя есть самосертификация; закрывает: делегирование критика по {T} ЛИБО строка "critic: skipped, <причина>" в notes (концессия только у приёмщика строго выше)».
M2 (фантомное основание): «R3-ЗЕРКАЛО: line {N} basis=critic по task_id {T}, но delegated(agent=critic) с этим task_id в журнале нет - основание приёмки ссылается на несуществующий вход, чек 2 прочтёт это как ложное основание; закрывает: записать делегирование критика по {T} ЛИБО снять поле basis».
M3 — НЕ строится (Р5(а)).

## ПОЗИЦИОННЫЕ ИНВАРИАНТЫ П1-П5 (каждый с пином): П1 никогда не блокирует, main() возвращает 0, логика НЕ в journal_validator.decide(); ни одна легальная форма accepted не валит коммит. П2 полная тишина на чистой записи: новый источник — в ТУ ЖЕ проверку истинности (:2160-2162). П3 расширение combine_context СТРОГО keyword-only (живой пин: шестипозиционный вызов test_journal_echo_escalation.py:431 остаётся побайтово прежним — прецедент t-447). П4 новый сегмент ПОСЛЕДНИМ содержательным, СТРОГО ПЕРЕД fallback_marker; заголовок build_context не меняется. П5 fail-open двумя слоями (построчный continue + внешний try/except в main()).

## ЛИМИТЫ (тест НА и ЗА): MAX_R3_LINES=5 (+«+K more»); MAX_MESSAGE_LEN=500 на динамический элемент (500 целиком / 501 и 10000 усечены); окно S3 без ограничения (первая строка файла ↔ последняя — тишина; тот же task_id при agent!=critic → warn); head_text=None на 200-строчном файле нарушений → ≤5 строк, exit 0.

## КРАЯ: task_id нет/не строка → пропуск строки; notes None → пустая; basis нет → S2/S3; basis=="queued-to-lead" → НЕ глушит; agent!=builder → тишина; повторный accepted → каждая строка независимо; ретро → не глушить; used_fallback → работает; пустой журнал/битый JSON/не-dict → пропуск без прерывания; чистая запись → полная тишина; коллизия R3×R13(г) → S4 глушит, вопрос наверх; BATCH CANON в witness → не влияет (сигналы, не witness).

## БАТАРЕЯ 1-14 (каждый — тест, всюду exit 0): «critic: skipped» в WITNESS (не notes) → warn ВСЁ РАВНО (S2 читает только notes); critic:skipped/Critic: Skipped/critic:   skipped → глушит; critic: skip / критик пропущен / без критика → НЕ глушит; task_id с   и ; DROP → санитайз; task_id 10000 симв → усечение; не-ASCII оба канала; JSON-не-объект → пропуск; обрезанный JSON → пропуск, следующая строка живёт; basis не строка → не сигнал; event/agent не строки → вне триггера; батч 200 нарушений → 5 + «+195 more»; T-500 vs t-500 → сравнение СТРОГОЕ → warn; пустой файл → тишина; не-журнальный payload → ранний выход.

## АКЦЕПТАНС УЗЛА 1 (билдер): К1 сигналы по-тестно (глушит + негативный близнец); К2 тексты дословно; К3 П1-П5 с пинами; К4 края+батарея; К5 лимиты НА/ЗА; К6 РЕТРО-ЗАМЕР на живом logs/routing-log.jsonl: помечены ВСЕ 11 task_id F7 (t-437 t-444 t-447 t-449 t-453 t-492 t-496 t-501 t-503 t-504 t-537), НЕ помечен ни один из 8 (t-509 t-516 t-525 t-529 t-532 t-535 t-536 t-538); полный список и ЧИСЛО печатаются (ожидается надмножество 11 — критерий слоя уже метода F7; каждое лишнее перечислить с вердиктом); К7 негативный контроль детектора: сломать S3 → замер краснеет, восстановить → зелень, обе половины в witness.

Имена: _collect_r3_events/_check_accepted_r3/_format_r3_line/build_r3_segment; кортежи (line_no, kind, task_id, extra), kind ∈ {"no_input","phantom_basis"}; line_no = len(base_lines)+idx+1; CRITIC_SKIP_RE константой; тест-файл tools/test_journal_echo_r3.py.

## Дано (D-0106): tools/journal_echo.py (образец-близнец :1348-1532, combine_context :762-854, main :2060-2214) · tools/journal_validator.py (BASIS_VALUES :304, JUDGE_BASIS_VALUE :305, матрица :532 — только чтение) · tools/warn_layers.json · tools/warn_density.py (:222, :240-252) · logs/routing-log.jsonl (образцы; только чтение) · PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md чек 2 :372-381 · docs/RULE_COVERAGE.md:44 · docs/task_reports/2026-08-20_calibration-8.md:184-208 (F7 дословно) · эта спека.

## owns УЗЛА 1: tools/journal_echo_r3.py (НОВЫЙ сиблинг), tools/test_journal_echo_r3.py (НОВЫЙ). БОЛЬШЕ НИЧЕГО — R2(б): живой journal_echo.py на активном hooksPath билдером НЕ трогается.
## non-goals: живой tools/journal_echo.py; tools/warn_layers.json (посадка узла 2, иначе --check красный — временной край); чек 11(в); RULE_COVERAGE; пин test_warn_density:416 (18→19 — узел 2); journal_validator.py; кит-твин.

## СУЖЕННЫЙ WITNESS УЗЛА 1:
`python -m pytest tools/test_journal_echo_r3.py -q` + `python -m pytest tools/test_journal_echo.py tools/test_journal_echo_escalation.py tools/test_journal_echo_noteslen.py tools/test_journal_echo_tsdrift.py tools/test_witness_echo.py tools/test_q503_echobase.py -q` (регресс соседей на непорченом живом файле) + вывод ретро-замера К6 + обе половины К7.

## УЗЕЛ 2 (Lead, ПОСЛЕ критик-гейта): байт-копия в живой journal_echo.py + фикс лживой шапки («SIBLING, НЕ ЖИВОЙ ПУТЬ» — F-41) и дублированного абзаца тем же касанием; строка реестра (id R3_ZERKALO, literal "R3-ЗЕРКАЛО: line ", reachable "journal_path", listed_in_check_11v true); имя в чек 11(в); правка строки R3 RULE_COVERAGE (+WARN-ХУК, не затирая ссылку на critic_snapshot); пин 18→19 в test_warn_density.py:416; DoD посадки: warn_density --check («слоёв в реестре: 19 (валидных: 19)») + полный канон + живой смок с откатом байт-копией (гигиена п.7).
