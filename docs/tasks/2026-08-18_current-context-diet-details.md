# CURRENT_CONTEXT diet — вынесенные подробности (2026-08-18)

Носитель подробностей пунктов очереди CURRENT_CONTEXT.md, вынесенных
диетой 2026-08-18 (реструктура бута C4). CURRENT_CONTEXT держит
строку-указатель на пункт; полное рассуждение/улика — здесь. Механизм —
docs/tasks/ DAG (D-0080, решение F9 критика t-479), НЕ новый класс
файла. Подтягивать секцию ТОЛЬКО когда оператор выбрал этот пункт.
Ничего не удалено — только перемещено (D-0038: «evidence is never
deleted, only relocated»). Указатель обратно: CURRENT_CONTEXT.md.

---

## d1 — Хвосты Phase 4, РЕШЁННАЯ часть (было L24-28)

Прочие хвосты Phase 4 решены 08-09/08-10: кодировка — класс «печатает
кириллицу без reconfigure потоков»; DOC_ONLY_EXTENSIONS — .yaml/.yml НЕ
добавлять, конфиг меняет поведение, doc-only освобождение открыло бы
обход зелёного прогона. (Живые три хвоста Phase 4 остаются в
CURRENT_CONTEXT: F2 SQL LIKE / строка-класс docs в леджере / экзамен-кита.)

## d2 — Триаж-критерий качества релиза (СТОЯЧАЯ НОРМА, было L73-80)

**Это НОРМА будущих релизов, не задача.** Заказ 08-09 исполнен релизом
v0.7.0. НАРРАТИВНАЯ ссылка (зовёт читателя за пределы кита —
D-NNNN/t-NNN/калибровки/даты/деплои/хеши/наши пути) снимается ВМЕСТЕ с
инлайном урока; СИНТЕТИЧЕСКАЯ ФИКСТУРА (самодостаточные данные теста)
легальна; потеря урока хуже оставленного номера. Вопрос «удали скобку —
знает ли читатель, что делать» есть вопрос применимости D-0100: критерий
релиза и критерий записи класса — одна форма.

## d3 — Класс «сторож не отличает цитату», решение (было L131-139)

РЕШЕНО 2026-08-16, осталась реализация. Общий СКАНЕР РЕГИОНОВ (fenced /
inline / цитата) — да; общая ПОЛИТИКА — нет. Сканер отвечает только «в
каком регионе этот кусок»; given-слой dispatch_gate решает сам
(инлайн-код НЕ глушить — пути там называют законно), парсер owns решает
сам (ему нужна не цитата, а отличие позиции декларации от прозы). Ровно
поэтому общее средство «на все три» отвергнуто как ложная экономия:
общим бывает разбор, а не вердикт. Ось 13 после посадки сканера
избыточна — снять.

## d5 — Порт фикса «фантомный root-only path-token», спека (было L196-204)

Порт → toolkit + Dog (батч D-0074, НЕ on-touch — мораторий).
is_path_like_token в toolkit/tools/dispatch_gate.py (~257-258) + мёртвая
пара _POSIX/_WINDOWS_ABS_RE в toolkit/tools/owns_gate.py (~324-325) несут
тот же дефект (голый «/» и «X:\» засчитываются путём → ложное OWNS
OVERLAP / перекрытие диска). Фикс штаба — t-476 (regex `^/+[^/\s]`,
`^[A-Za-z]:[\\/]+[^\\/\s]` + удаление мёртвой пары owns_gate). Ось
SIBLING_MAP ~285; детектор дрейфа parity_check --check (hq-drift). Dog —
та же правка (ось 11 D-0082).

## d6 — Обкатка Dog-пары корреляции, residual (было L212-213)

№1 закрыт t-340; №2 (D4b-резидуал) — записанное ограничение обеих
копий, фикса нет без рецидива.

## d7 — Порт NOTES LEN в кит, обоснование отступления (было L232-238)

ОСОЗНАННОЕ ОТСТУПЛЕНИЕ ОТ ОСИ 10 (08-16, механизм t-447). Ось требует
«кит-сиблинг тем же диффом»; здесь не так, причина: пороги выведены из
НАШЕГО распределения, а перенос числа через границу деплоя — та самая
ошибка, что вскрыла развилка символы/байты. Твин вдобавок поколением
старше (combine_context на 4 сегмента против наших 6). Порт минором:
либо с порогами хоста, либо с пометкой «числа — образец, не мера».

## d8 — Guard фабрикаций координатора (переименован 08-19; было «Adversarial-экзамен координатора», L251-253)

Верхняя граница F-28. Экзаменационная часть ЗАКРЫТА прогонами
LEAD_RANKING_EXAM v2 (08-18, немаркированный координационный эпизод:
opus 0/2, fable PASS) + CRITIC_EXAM №4 PASS — разовые проверки «потребует
ли координатор сверки» сделаны. ОСТАТОК — постоянный машинный сторож
класса pi_run_guard: детерминированная сверка load-bearing утверждений
координатора с артефактами (файл/журнал/прогон) + капканные кейсы в
поток. Частичные слои УЖЕ стоят и ловят: journal_echo WITNESS
(witness без зелёного прогона после правки), NEGATIVE_LINT (негатив
без контроля формы), claim_control_gate (негативы против леджера
поиска), dispatch_gate/owns_gate/main_gate. Полный guard — дорого
(сверка каждого утверждения, порог шума); триггер — рецидив
F-28-находки координатора в калибровке (утверждение без сверки,
прошедшее в решение мимо частичных слоёв).

## d9 — Батч «binding-agnostic пины тестов», энумерация (было L307-313)

В tools/test_dispatch_gate.py остались пины живых привязок — ~50
builder=sonnet (лейблы _builder_payload), critic/designer=opus
(:1328/:1331), judge=sonnet (:1160/:1342) — зелёные до перепривязки этих
ролей; лечение тем же _scout_families-классом (D-0102-фикс 08-16);
кит-твин toolkit/tools/test_dispatch_gate.py — тот же класс, релизным
батчем. (Копилка 08-16 пункт (е).)

## d10 — Очередь из находок №19 (было L343-346)

(а) упрочнение headless-протеза (деливерабл ограничен клеткой); (б)
M2-регресс t3 — чек-23 материал; (в) область given-path слоя в клетках —
по рецидиву; (г) порт эпох hygiene_gate/journal_echo в exam-кит
(отдельная оценённая задача).

## d11 — Standing-триггеры из закрывающего коммита Phase 2 (было L361-369)

Обязанность F-48/D-0082 — триггер в архиве не передан. (а) per-unit
worktree-изоляция (GSD, отвергнута Rule #1) — reopen: реальный
параллельный объём диспатчей + инцидент коллизии путей (класс D-0060);
(б) LLM-роутер-кандидаты (6 отвергнуты двумя волнами обзоров) — reopen:
≥100 размеченных примеров; (в) большая параллельность как workstream —
reopen: числа P1-класса (задачи ≥5 событий / ≥2 сессий) показывают
переполнение координационных артефактов.

## d12 — Eval plan stage 2 (снят 08-19, влит в план большого экзамена; было L370-372)

Цикл №1 07-13 (d90cd03). Словом Архитектора 08-19: minimum-n/pass^k +
numeric agreement ВЛИТЫ в план большого экзамена (носитель цепочки —
CURRENT_CONTEXT, блок экзаменов; смысл: статистическая надёжность
вердиктов нужна там, где прогонов много, — большой экзамен по
генераторам, не разовые точечные). Batch API (Anthropic Message
Batches: асинхронный пакет, −50% цены, до 24ч) — на своём триггере
«реплеи регулярны»: пока реплеи разовые, выигрыш половины цены редких
прогонов не окупает обвязку (диспетчер батчей: сабмит/поллинг/сшивка
в Runs log/частичные отказы — механизм со всеми вопросами R10);
триггер естественно выстрелит после перехода большого экзамена на
регулярные пакетные прогоны (после релиза+порта). NOT taken: per-PR
CI, bench-harness.

## d14 — Две оси-кандидата AO3 (было L406-413)

Все входящие AO3 по 2026-08-16 РАЗОБРАНЫ (тексты — архив
2026-08-16_boot-diet-extractions §4–§5). Живых остатка два, оба —
нерешённые кандидаты осей карты SIBLING_MAP:
- ось-кандидат «координатор закрывает цикл по снимку начала цикла» (их
  прецедент 08-03: три отсрочки готовой работы за день; у них закрыто
  правилом 8а + ре-скан 4а + чек 3а).
- ось-кандидат «защита объявлена тотальной, реализована частичной»
  (узкий except / приведение типа ВНЕ охраняемого читателя; исключение
  уходит наружу через уже взятый лок; порог ≥2 файлов выполнен их
  heartbeat_wrap + loop_lock).

## d15 — Класс «имя модели прибито как критерий», открытый остаток (было L421-439)

Владелец текста класса — реестр режимов отказа docs/SIBLING_MAP.md (три
носителя: протокол калибровки, savings_report, само ядро). Оба
кросс-пункта AO3 08-15, код-экземпляр и четыре экземпляра в ядре закрыты
08-16 (cc31cdd, 8fae98e, da54ec5, 5437d54; ответ в их носитель — их
1092a08, d65e303).

ОТКРЫТЫЙ ОСТАТОК В ЯДРЕ, требует отдельного решения (находка критика
t-446, F2в — до неё остаток жил только в сообщении коммита, то есть по
F-48 передан не был): секция CLAUDE.md «Role ≠ tier» несёт имена моделей
трижды и охраняется F-31 «do not compress» — привести её к привязке
нельзя молча, защита покрывает именно эту секцию; плюс колонка «Model
here» таблицы ярусов — второй носитель привязки рядом с
delegation.config.yaml, снятие = перестройка таблицы, а не сжатие истории.

ХВОСТЫ: (i) пометка разрыва базы в spend-аналитике — M14/D7 зовут ту же
функцию через алиас и стали ровно вдвое меньше (точное следствие, цены
отличаются ровно в 2 раза); (ii) кит-твин
toolkit/tools/savings_report.py остаётся на Fable-базе — релизной
очередью (D-0074), и там ветка «конфига нет» становится ОСНОВНОЙ, это
отдельное проектное решение; (iii) чек 6 кит-твина несёт дофиксовую
форму — тем же минором. Детектор класса (хвост чека 5) покрывает ТОЛЬКО
текст протокола, код-носители не покрыты — открыто честно.

ЗНАТЬ ПРИ ЧТЕНИИ ТРЕНДА 18(б): база контрфакта сменилась, точка №7
завышена (считалась по Fable вдвое дороже истины), №6 верна —
перепривязка попала внутрь её окна.
