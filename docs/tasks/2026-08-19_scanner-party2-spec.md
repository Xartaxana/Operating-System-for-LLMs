# Спека партии 2 сканера md_regions (драфт designer t-528, принят Lead 08-19)

Статус: принята; узел C (dispatch_gate) — builder t-529 + КРИТИК
обязателен (Р4а); узел D+E (owns_gate + mechanism_gate) — builder
t-530; параллельны (owns не пересекаются). Полный драфт —
транскрипт t-528; здесь — операционный носитель.

## РЕШЕНИЯ LEAD ПО РАЗВИЛКАМ (08-19)

Ф1 → (а) «СТРОЖЕ — ТОЛЬКО ЧЕРЕЗ WARN»: в блокирующем гейте
регион-фильтр меняет exit_code исключительно в сторону СНЯТИЯ
блока; направления «строже» — новыми WARN-слоями, decide() в этих
ветках не меняется. Ф2 → (а) DOD_QUOTED_WARN (повышение до блока —
по рецидиву после замера). Ф3 → (а) фильтровать write-половину
(класс F-59). Ф4 → (а) manifest-половину НЕ фильтровать +
MANIFEST_QUOTED_WARN. Ф5 → (а) маркер в inline_code — НЕ
декларация (расхождение с полярностью партии 1 — в докстринг
явно). Ф6 → (а) незакрытый фенс у owns_gate — декларация не
читается + QUOTED_OWNS_WARN. Ф7 → (б) SKIP_RE + TIER_LINE_RE
сейчас; осевые строки — после замера живого лога (детерминированный
скрипт, R8f, при посадке). Ф8 → (а) block_extra не сканируется
(строкой в докстринг). Ф9 → (а) красно-зелёная пара на git clone
--no-hardlinks в scratchpad + core.hooksPath. Ф10 → (а) два
диспатча: C solo+критик, D+E одним. Ф11 → (а) equivalence-run
обязателен как ключ. Ф12 → (а) фенсированный «## Ось N» в самой
карте (parse_axes) — строка очереди.

## Инварианты партии

И-0: любой отказ модуля (тройной try/except, scan=None; _safe_scan)
→ поведение живого файла байт-в-байт; отдельного пути кода нет.
И-1: scan() ≤1 раза за вызов, только после дешёвого предфильтра
(маркерный хит И символ из "`>~" в тексте; _no_markers_whole_text
даёт точный ответ — предфильтр корректность не теряет);
тест-счётчики. Полярность дефолта: переносится инвариант
«неоднозначность не расширяет зону молчания», НЕ буква «проза» —
вид региона зависит от полярности предиката потребителя (в
докстринг каждого сиблинга). Форма: сиблинги *_md.py + батареи
MODULE_UNDER_TEST с f61-fallback (образец test_negative_lint_md
:43-64); посадка байт-копиями — Lead.

## УЗЕЛ C — dispatch_gate (t-529, критик обязателен)

Ветки (замер t-528): B1 DoD-маркер (:721-723) блокирующая, маркер
ГАСИТ блок; B2-write (:730-732 = WRITE_INDICATORS_RE +
owns_declaration_has_path_token) — включает проверку 2; B2-manifest
(:737-741) блокирующая; B3 лейбл и W2 role-type — СТРУКТУРНЫЕ ПОЛЯ,
сканер НЕ применяется (§7, не-цель); W1 given-слой (:860-881) WARN.
Политика: W1 — путь проверяется, если хоть одно вхождение prose или
inline_code (бэктики — каноническая форма манифеста, НЕ гасить);
fenced/blockquote вхождения не проверяются; дедуп сохраняется;
B2-write — цитируемый глагол/декларация не включают проверку 2;
B1/B2-manifest — decide() ни байтом, вместо этого DOD_QUOTED_WARN /
MANIFEST_QUOTED_WARN (все совпадения в цитатах → warn).
Ключи C1-C9 (из драфта, обязательны): C1 четыре формы given-пути
(фенс нет-WARN / проза WARN / бэктики WARN / цитата нет-WARN); C2
«любое вне цитаты → проверяем»; C3 дискриминационная пара
цитируемого чужого манифеста (сегодня exit 2 → станет exit 0 +
MANIFEST_QUOTED_WARN; без фенса — exit 2 как сегодня); C4 DoD
только в фенсе → exit 0 как сегодня + DOD_QUOTED_WARN; C5 МАТРИЦА
равенства decide() сиблинг==живой на ≥20 формах (пины батареи + 9
живых образцов F-59 + канонический манифест) — допустимые
расхождения перечислены поимённо; C6 порядок WARN (given первым,
:1104) не тронут, новые в хвост; C7 ПОЗИЦИОННЫЙ ИНВАРИАНТ: exit 2
до вычисления любого WARN — на заблокированном диспатче ни один
новый слой не исполняется (тест: блок+WARN-условие → только exit 2,
stdout пуст); C8 И-0 три подмены; C9 И-1 счётчики.
Позиционные инварианты не-менять (адреса — замер t-528): все
регексы :529-555, is_path_like_token :576-617,
owns_declaration_has_path_token :654-692, BLOCK_MESSAGE_* :695-705,
приоритет 1→2→3 :708-747, given-регексы :775-786 (вкл. {0,300}),
порог 10/11 :788/:847-857, role_type_warn :997-1050, склейка :1104.
owns: tools/dispatch_gate_md.py + tools/test_dispatch_gate_md.py.

## УЗЕЛ D+E — owns_gate + mechanism_gate (t-530)

D (owns_gate): дыра фактическая (находка t-528, докстринг :457-468
лгал): разбираются однострочная декларация в фенсе, многострочный
блок в фенсе, цитата > — пишут владение в боевой sidecar; перехват
цитируемой декларации теряет реальный owns. Политика: декларация
только при position(маркер)==prose (по МАРКЕРУ, не по пути — бэктики
пути на настоящей строке живут, F-59-3); строка продолжения — тоже
prose, иначе четвёртое стоп-условие (после пустой строки и
секционного стопа, до токенизации); оба прохода (:858-872, :877-896)
одинаково; QUOTED_OWNS_WARN когда ВСЕ маркеры отфильтрованы.
Докстринг :457-468 переписать правдой (модуль построен, партия 2;
пин :360-367 покрывал 1 форму из 4). Ключи D1-D8 из драфта
(дискриминации D1/D2 красные на живом; D3 sidecar не растёт; D4
пин F-59-3 держится; D5 старый пин по двум причинам; D6 механика
реестра/окно/компакция/exit0 не тронуты + equivalence; D7 И-0
включая перехват; D8 счётчик). Позиционные инварианты: импорт
четырёх имён из ЖИВОГО dispatch_gate (:545-558, порядок посадки
безразличен), _EDGE_TRIM/_SECTION_STOP/CONTINUATION_LIMIT=40,
WINDOW_SECONDS :932, компакция 500/501, «сверка выше — запись
после» :1169-1183, exit 0 :1220.
E (mechanism_gate): скоуп Ф7(б) — SKIP_RE (:194) И TIER_LINE_RE
(:95): строка отказа/tier в фенсе или цитате НЕ засчитывается
(гейт fail-closed — фильтр к вердикту напрямую, асимметрия с
dispatch_gate осознанная, в докстринг); ALL-must-pass считает
только нецитированные tier-строки; осевые строки find_missing НЕ
фильтруются (Ф7, замер потом); block_extra не сканируется (Ф8).
Ключи E1-E8 из драфта (E1 skip-в-фенсе → блок, дискриминация
красная на живом; E4 tier-в-фенсе → «нет строки tier»; E6 живая
красно-зелёная пара на клоне — 4 прогона дословно по форме §4.3
драфта; E7 не-механизмный/merge → 0 вызовов scan; E8 И-0 с
фиксацией «цитированный skip снова глушит — объявленный фоллбек»).
Позиционные инварианты: порядок веток decide() hits→merging→skip→
fail-closed→axes; MECHANISM_PREFIXES :97-187 целиком; паттерны
:95/:189/:194 сохраняются — меняется только фильтрация найденного.
owns: tools/owns_gate_md.py, tools/test_owns_gate_md.py,
tools/mechanism_gate_md.py, tools/test_mechanism_gate_md.py.

## Края (5.1 драфта — таблица полярностей незакрытого фенса
ОБЯЗАТЕЛЬНА в докстрингах; 5.2 пустые входы; 5.3 конфликтные пары
(а)-(г) — поведение названо в драфте; 5.4 темпоральный: f61-fallback
резолвера обязателен — после посадки сиблинги снимаются; 5.5 новых
лимитов нет — пар границ нет, деградация monkeypatch'ем).

## DoD (§6 драфта): (1) батареи сиблингов зелёные; (2) дискриминации
КРАСНЫЕ на MODULE_UNDER_TEST=live (дословный хвост с именами); (3)
живые не задеты (шесть существующих батарей); (4) equivalence-run
существующих батарей на КОПИИ дерева с сиблингами под живыми
именами (отдельной командой, абсолютные пути — конфликт basename);
(5) узел E: живая красно-зелёная пара на клоне; (6) канон до/после;
(7) счётчики И-1. Не-цели: живые пути; md_regions.py; батареи
живых; SIBLING_MAP/CURRENT_CONTEXT (посадка Lead); toolkit;
escape_check/critic_verdict_check парсеры; боевой owns_registry.
Handoff: посадка байт-копиями (все три в неводе — механизменный
коммит + probe); SIBLING_MAP носители → ЗАКРЫТЫ + остаток Ф7(в)/Ф12
строками; CURRENT_CONTEXT снятие строк; кит-строка D-0074.
