# УЗЕЛ C — F-58, контент-пакет для посадки (builder, ПОСЛЕ посадки t-498)

Форма: ПАКЕТ ПРАВОК. Носители НЕ редактированы (запас CLAUDE.md до
храповика BREACH=36115 — 31 байт на момент замера 08-19; прямая
правка = born-breached). Посадка всех шести — ОДИН механизменный
коммит Lead после диеты W2c (Р10, спека узла C).

**РЕВИЗИЯ 2 (после вердикта критика fit_with_fixes):** семь фиксов
Lead применены — см. раздел «Дифф-лист фиксов 1–7» в конце файла.
Байт-дельта CLAUDE.md ПЕРЕСЧИТАНА: было 438 Б, стало **872 Б** (меняет
порог К4 спеки W2c — правка самого W2c не в моём owns).

Источник: docs/tasks/2026-08-19_f61-f58-remediation-spec.md, раздел
«УЗЕЛ C»; docs/FINDINGS.md «## F-58» (полный текст находки, до
статусной строки); живой материал t-503 (решения Lead по фиксам 1–7,
живой чат координатора).

## Акцептанс-ключи (дословно из DoD, зафиксированы ДО прогона)

Исходные четыре:
1. «входящем в собственный owns»
2. «пустой дифф после отката»
3. «не показывает АВТОРА»
4. «путь в owns»

Добавлены фиксом 2 (зафиксированы ДО этого прогона):
5. «НЕПОСРЕДСТВЕННО перед откатом»
6. «легитимный параллельный писатель»

Все шесть — литеральные подстроки, проверяются скриптом ниже по
ЦЕЛОМУ этому пакет-файлу (не по отдельному носителю). Распределены
избыточно: POLICY_FULL и RULE_COVERAGE несут ВСЕ ШЕСТЬ каждый в
одиночку (см. разделы 2 и 6 ниже).

## Шесть носителей — якоря перечитаны в актуальном дереве

| № | Носитель | Адрес в спеке | Адрес перечитан | Вердикт |
|---|---|---|---|---|
| 1 | CLAUDE.md п.7 | (без диапазона, «п.7») | строки 492–503 (последний пункт файла, 503 строки всего) | перечитан, совпадает |
| 2 | docs/POLICY_FULL.md | :585–595 | пункт 7 целиком строки 585–606 (диапазон спеки покрывал только начало пункта) | перечитан, слегка уехал (пункт длиннее диапазона спеки) |
| 3 | .claude/agents/builder.md | :93–98 | правило 10, строки 93–103 | перечитан, слегка уехал (+5 строк) |
| 4 | .claude/agents/critic.md | :84–91 | правило 14 «БЕЗОПАСНОСТЬ СОБСТВЕННОЙ ПРОБЫ», строки 83–99 | перечитан, слегка уехал (начало на 83, не 84; конец на 99, не 91) |
| 5 | PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md чек 25(в) | :824–831 | строки 1174–1190 | СИЛЬНО уехал (дрейф ~350 строк — файл вырос другими правками между замером спеки и этим чтением) |
| 6 | docs/RULE_COVERAGE.md §4 | :82–92 | секция «## 4. Командная гигиена», строки 84–94 (таблица 86–94) | перечитан, слегка уехал (+2 строки) |

Все шесть подтверждены greps с позитивным контролем той же формы.

Примечание к якорю №4 (FIX 7): в critic.md ДВА пункта с номером 14
(предсуществующий дубль) — ссылки ниже даются по фразе «БЕЗОПАСНОСТЬ
СОБСТВЕННОЙ ПРОБЫ», не по номеру; сам дубль НЕ чиню (Lead забрал
строкой очереди).

---

## 1. CLAUDE.md п.7 (команда гигиены)

Якорь (дословная существующая строка, последняя строка файла):
```
   covers this command class.
```
(строка 503; правило 7 — последнее в файле, реестрового блока пока
нет — это ПЕРВЫЙ амендмент, создаёт блок, п.1 CLAUDE.md).

### БЫЛО (ядро п.7 не редактируется — амендмент это ЗАПРЕЩАЕТ:
«amendment... never a paragraph in its core»; ниже — точка вставки)

Текущий конец файла (строки 492–503, дословно):
```
7. **Temporary corruption is rolled back by a BYTE COPY, never by
   `git checkout`** — that idiom wipes ANOTHER session's uncommitted
   changes to the file along with the corruption. For any
   mutation/red probe, at every tier: (а) take a byte copy BEFORE
   corrupting and restore FROM IT; (б) `git checkout`/`git restore` is
   legal only when `git status --porcelain -- <file>` was EMPTY before
   the corruption — check it, never assume; (в) the rollback's witness
   is the VERBATIM output of the comparison (hash or diff), not the
   word "restored"; (г) do NOT corrupt a live artifact at all when a
   function's verdict proves the same thing — corrupt a COPY of the
   tree instead. Detector: calibration check 25 — its transcript scan
   covers this command class.
```
(это последняя строка файла — за ней ничего нет)

### СТАНЕТ (добавляется ПОСЛЕ строки 503, ядро п.7 — БЕЗ единого
байта правки; первый амендмент = создание реестр-блока, форма по
образцу таблиц R1/R2/R4)

Ядро (492–503) остаётся БАЙТ-В-БАЙТ как в БЫЛО — ФИКС 2(а)/2(б) для
CLAUDE.md сознательно НЕ трогают core-текст (а)-(г): та же самая
шапка размера файла запрещает «paragraph in its core» для ЛЮБОГО
амендмента, включая уточнения; порча-таймингов и зеркало-принцип
внесены НИЖЕ, в реестровую строку (единственный разрешённый канал
роста). Добавляется, начиная СРАЗУ после строки 503 (т.е. с пустой
строки-разделителя, как между core-параграфом и таблицей у R1/R2/R4):
```

| п.7 | when | duty | src |
|---|---|---|---|
| д | discarding OWN uncommitted work in a path inside own declared owns, edited in the SAME session | legal; porcelain checked IMMEDIATELY before the rollback (baseline ≠ check); witness = owns declaration + diff BEFORE discard + EMPTY diff after | F-58 |
| е | owns undeclared, path outside owns, or edits not this session's | (б) unchanged: porcelain shows FACT, not AUTHOR — non-empty status outside own owns = a legitimate parallel writer (R4.d), rollback forbidden | F-58 |
```

Байт-дельта (точный подсчёт, UTF-8, включая ведущий перевод строки
разделителя и оба конца строк добавляемого блока): **535 байт**
(РЕВИЗИЯ 4, дожим Lead 08-19 после замера W2c-1: нетто диеты 517 Б
при пороге рев.3 526 Б — недобор 9 Б закрыт сжатием блока, не
добором диеты сверх P1; история: 438 Б рев.1 → 872 Б рев.2 после
фиксов критика → 557 Б рев.3 → 535 Б рев.4;
сжатие формой «две строки (д)/(е)» — строка RULE_COVERAGE «регистр
(д/е)» резолвится буквально; семантический чек-лист критика
сохранён полностью: тайминг IMMEDIATELY-before-rollback, зеркало
legitimate-parallel-writer + R4(d), «edits not this session's»
покрывает и старшие и параллельные правки, diff BEFORE discard в
свидетеле, литера (е)). Замер — scratchpad/measure_row_rev3.py,
вывод в журнале приёмки. Посадка требует диеты W2c-1 с нетто
≥526 Б (= 557 − 31 запаса).

Примечание формы: этот реестр-блок НЕ подпадает под машинный гейт
tools/test_norm_form.py — тот действует ТОЛЬКО на шесть КОНВЕРТИРОВАННЫХ
routing-правил (R1/R2/R4/R8/R11/R13, `RULE_HEADER_RE = r"^(R\d+a?)\.
\*\*"`); строка «7. **Temporary…» этому регексу не соответствует.
Проверено чтением tools/test_norm_form.py:64-65,75 и позитивным
контролем на его собственных примерах. Значит: ни ROW_BYTE_CAP=523,
ни CORE_MIN/MAX_LINES=3/8 на п.7 не действуют — формат таблицы взят
СТИЛИСТИЧЕСКИ (по указанию диспетчера), не из требования гейта.

---

## 2. docs/POLICY_FULL.md, пункт 7 (пара к CLAUDE.md, ось 4)

Якорь (дословный заголовок пункта, строка 585):
```
7. Временная порча откатывается БАЙТОВОЙ КОПИЕЙ, не `git checkout`
```

### БЫЛО (строки 585–606, дословно)
```
7. Временная порча откатывается БАЙТОВОЙ КОПИЕЙ, не `git checkout`
   (2026-08-05; порт правила AO3 08-02, подтверждённый живым кейсом
   этого репо: критик при R3-гейте портил боевую денежную таблицу
   logs/token_usage.xlsx и откатывал её checkout'ом — обошлось лишь
   потому, что файл был чист и бэкап снят). Для любой мутационной
   пробы на любом ярусе: (а) байтовая копия ДО порчи, восстановление
   ИЗ НЕЁ; (б) `git checkout`/`git restore` легален только при
   пустом `git status --porcelain -- <файл>` ДО порчи — проверяется,
   не предполагается (иначе сносятся чужие незакоммиченные правки,
   D-0060); (в) witness отката — дословный вывод сверки, не слово
   «восстановлено»; (г) боевой артефакт не портится вовсе, когда то
   же доказывает вердикт чистой функции или порча КОПИИ дерева
   (прецедент того же дня: проба пина судьи гонялась на испорченных
   копиях, боевой файл не тронут). Детектор — чек 25(в) калибровки.
   Твины: critic.md правило 14 (за экзаменом №3, PASS 08-05),
   builder.md п.10.
   [Ретро-примечание синхронизации 2026-08-05, Fable-ревью первой
   Opus-Lead сессии: п.7 вошёл в ядро коммитом aa4bbd2, зеркало этой
   секции догнало ядро ОТДЕЛЬНЫМ коммитом того же дня — осевой блок
   aa4bbd2 закрыл ось 4 парой «норма ↔ чек-детектор», упустив
   каноническую пару «ядро ↔ POLICY_FULL» (шапка ядра: same-commit
   discipline). Разрыв прожил ~1 час; класс — материал чека 24.]
```

### СТАНЕТ (РЕВИЗИЯ 2: правка (б) — тайминг проверки [ФИКС 2а];
вставка (д)/(е) с зеркалом и R4(d) [ФИКС 2б/2г]; исправлен прецедент
[ФИКС 6]; ссылка на critic.md по фразе [ФИКС 7]; остальное байт-в-байт)
```
7. Временная порча откатывается БАЙТОВОЙ КОПИЕЙ, не `git checkout`
   (2026-08-05; порт правила AO3 08-02, подтверждённый живым кейсом
   этого репо: критик при R3-гейте портил боевую денежную таблицу
   logs/token_usage.xlsx и откатывал её checkout'ом — обошлось лишь
   потому, что файл был чист и бэкап снят). Для любой мутационной
   пробы на любом ярусе: (а) байтовая копия ДО порчи, восстановление
   ИЗ НЕЁ; (б) `git checkout`/`git restore` легален только при
   пустом `git status --porcelain -- <файл>` ДО порчи — проверяется
   НЕПОСРЕДСТВЕННО перед откатом (замер на базлайне хода в зачёт не
   идёт, межвременной зазор), не предполагается (иначе сносятся
   чужие незакоммиченные правки, D-0060); (в) witness отката —
   дословный вывод сверки, не слово «восстановлено»; (г) боевой
   артефакт не портится вовсе, когда то же доказывает вердикт чистой
   функции или порча КОПИИ дерева (прецедент того же дня: проба пина
   судьи гонялась на испорченных копиях, боевой файл не тронут).
   (д) F-58 (2026-08-19): отбрасывание СОБСТВЕННОЙ незакоммиченной
   работы в пути, входящем в собственный owns, легально БЕЗ пустого
   porcelain — свидетель: декларация владения + дифф ДО
   отбрасывания (что именно отбрасывается) + пустой дифф после
   отката; (е) вне этого условия — путь вне owns, owns не объявлен,
   либо правка не этой сессии — старше, или из ДРУГОЙ сессии —
   действует (б) без изменений: porcelain показывает факт изменения
   и не показывает АВТОРА — и зеркально: непустой статус в пути, НЕ
   входящем в собственный owns исполнителя, читается как легитимный
   параллельный писатель, а не как сторонняя порча — откат такого
   пути ЗАПРЕЩЁН (сшивка с R4(d): «never touch another session's
   uncommitted paths»); декларация owns — основание, не суждение
   «M — моя» (прецедент 2026-08-09: воркер отбрасывал свою правку
   чужого файла, взятого в owns манифестом; обошлось лишь потому,
   что файл был в его owns и параллельные воркеры его не касались —
   обоснование было выводом, не проверкой). Детектор — чек 25(в)
   калибровки (третья ветка сверки с 2026-08-19: байт-копия /
   пустой porcelain / путь в owns исполнителя — без неё детектор
   метил бы легальный откат как нарушение).
   Твины: critic.md «БЕЗОПАСНОСТЬ СОБСТВЕННОЙ ПРОБЫ» (за экзаменом
   №3, PASS 08-05; ветка д/е F-58), builder.md п.10 (ветка д/е F-58).
   [Ретро-примечание синхронизации 2026-08-05, Fable-ревью первой
   Opus-Lead сессии: п.7 вошёл в ядро коммитом aa4bbd2, зеркало этой
   секции догнало ядро ОТДЕЛЬНЫМ коммитом того же дня — осевой блок
   aa4bbd2 закрыл ось 4 парой «норма ↔ чек-детектор», упустив
   каноническую пару «ядро ↔ POLICY_FULL» (шапка ядра: same-commit
   discipline). Разрыв прожил ~1 час; класс — материал чека 24.]
```

**Обязанность посадки (C3):** ядро CLAUDE.md п.7 и эта правка
POLICY_FULL — ОДНИМ коммитом (same-commit discipline, шапка ядра
CLAUDE.md; прецедент разрыва — тот самый ретро-примечание внутри
этого же пункта, ~1 час разрыва в 2026-08-05).

**НАХОДКА (не в scope правки, для отчёта Lead):** docs/POLICY_FULL.md
НЕ входит в MECHANISM_PREFIXES tools/mechanism_gate.py (проверено
чтением tools/mechanism_gate.py:97-182, позитивный контроль —
"docs/RULE_COVERAGE.md" и "docs/FINDINGS.md" там присутствуют,
"POLICY_FULL" — нет). docs/RULE_COVERAGE.md:49 сам это фиксирует:
«docs/POLICY_FULL.md — решается посадкой W2b». Следствие: коммит-msg
гейт НЕ потребует осевого блока конкретно за правку POLICY_FULL.md
(потребует за CLAUDE.md/RULE_COVERAGE.md/PROCESS/.claude/agents/ —
эти пять в неводе). Обязанность «ядро↔POLICY_FULL тем же коммитом»
держится ДИСЦИПЛИНОЙ, не машиной, до решения W2b.

Этот раздел содержит ВСЕ ШЕСТЬ акцептанс-ключей самостоятельно
(входящем в собственный owns / пустой дифф после отката / не
показывает АВТОРА / путь в owns / НЕПОСРЕДСТВЕННО перед откатом /
легитимный параллельный писатель).

---

## 3. .claude/agents/builder.md, правило 10

Якорь (дословная первая строка правила, строка 93):
```
10. Временная порча файла (мутационная проба, red-probe, «испорчу и
```

### БЫЛО (строки 93–103, дословно)
```
10. Временная порча файла (мутационная проба, red-probe, «испорчу и
   верну») откатывается БАЙТОВОЙ КОПИЕЙ, не `git checkout` (п.7
   командной гигиены CLAUDE.md, 2026-08-05): копия снимается ДО
   порчи, восстановление — из неё; `git checkout`/`git restore`
   легален только при пустом `git status --porcelain -- <файл>` ДО
   порчи, и это проверяется, а не предполагается — иначе ты сносишь
   незакоммиченные правки параллельной сессии вместе со своей порчей
   (D-0060). Witness отката — дословный вывод сверки, не слово
   «восстановлено». Боевой артефакт (журнал, реестр, денежная
   таблица) не портится ВОВСЕ, если то же самое доказывает вердикт
   чистой функции или порча КОПИИ дерева.
```

### СТАНЕТ (РЕВИЗИЯ 2: тайминг в исходном предложении [ФИКС 2а];
добавленный абзац расширен зеркалом/R4(d)/диффом-до [ФИКС 1/2б/2г])
```
10. Временная порча файла (мутационная проба, red-probe, «испорчу и
   верну») откатывается БАЙТОВОЙ КОПИЕЙ, не `git checkout` (п.7
   командной гигиены CLAUDE.md, 2026-08-05): копия снимается ДО
   порчи, восстановление — из неё; `git checkout`/`git restore`
   легален только при пустом `git status --porcelain -- <файл>` ДО
   порчи, замеренном НЕПОСРЕДСТВЕННО перед откатом (базлайн хода не
   годится — межвременной зазор), и это проверяется, а не
   предполагается — иначе ты сносишь незакоммиченные правки
   параллельной сессии вместе со своей порчей (D-0060). Witness
   отката — дословный вывод сверки, не слово «восстановлено».
   Боевой артефакт (журнал, реестр, денежная таблица) не портится
   ВОВСЕ, если то же самое доказывает вердикт чистой функции или
   порча КОПИИ дерева. Отдельно (F-58, 2026-08-19): отбрасывание
   СВОЕЙ незакоммиченной работы в пути, входящем в собственный owns,
   легально без пустого porcelain — свидетель: декларация владения +
   дифф ДО отбрасывания (что именно отбрасывается) + пустой дифф
   после отката; вне этого условия (путь вне owns, owns не объявлен,
   либо правка не этой сессии — старше, или из ДРУГОЙ сессии)
   действует правило выше без изменений: porcelain показывает факт
   изменения, а не показывает АВТОРА — и зеркально: непустой статус
   в пути, НЕ входящем в собственный owns, читается как легитимный
   параллельный писатель, не как сторонняя порча — откат такого пути
   ЗАПРЕЩЁН (R4(d): never touch another session's uncommitted
   paths).
```

---

## 4. .claude/agents/critic.md, правило «БЕЗОПАСНОСТЬ СОБСТВЕННОЙ ПРОБЫ» (номер 14 — предсуществующий дубль, ссылка по фразе, ФИКС 7)

Якорь (дословная первая строка правила, строка 83):
```
14. БЕЗОПАСНОСТЬ СОБСТВЕННОЙ ПРОБЫ (п.7 командной гигиены CLAUDE.md,
```

### БЫЛО (строки 83–99, дословно)
```
14. БЕЗОПАСНОСТЬ СОБСТВЕННОЙ ПРОБЫ (п.7 командной гигиены CLAUDE.md,
   2026-08-05; порт правила AO3, подтверждённый ЖИВЫМ кейсом — им
   был критик этого репо: при R3-гейте он портил боевую денежную
   таблицу logs/token_usage.xlsx и откатывал её `git checkout`).
   Твоя проба не имеет права стоить чужой работы. Порядок для ЛЮБОЙ
   мутационной пробы: (а) байтовая копия ДО порчи, восстановление ИЗ
   НЕЁ; (б) `git checkout`/`git restore` легален, только если
   `git status --porcelain -- <файл>` был ПУСТ до порчи — это
   проверяется, а не предполагается (иначе ты сносишь
   незакоммиченные правки параллельной сессии вместе со своей
   порчей, D-0060); (в) witness отката — дословный вывод сверки
   (хэш или дифф), не слово «восстановлено»; (г) боевой артефакт не
   портится ВОВСЕ, когда тот же вывод даёт вердикт чистой функции
   или порча КОПИИ дерева — а он даёт почти всегда. Нарушение этого
   порядка в собственном трейле — дефект вердикта, даже если в тот
   раз обошлось: цена ошибки здесь несимметрична. Тот же порядок —
   штатная категория находок при ревью ЧУЖОЙ пробы.
```

### СТАНЕТ (РЕВИЗИЯ 2: тайминг в (б) [ФИКС 2а]; вставка (д)/(е) с
зеркалом/R4(d)/диффом-до между (г) и «Нарушение…» [ФИКС 1/2б/2г];
остальное байт-в-байт)
```
14. БЕЗОПАСНОСТЬ СОБСТВЕННОЙ ПРОБЫ (п.7 командной гигиены CLAUDE.md,
   2026-08-05; порт правила AO3, подтверждённый ЖИВЫМ кейсом — им
   был критик этого репо: при R3-гейте он портил боевую денежную
   таблицу logs/token_usage.xlsx и откатывал её `git checkout`).
   Твоя проба не имеет права стоить чужой работы. Порядок для ЛЮБОЙ
   мутационной пробы: (а) байтовая копия ДО порчи, восстановление ИЗ
   НЕЁ; (б) `git checkout`/`git restore` легален, только если
   `git status --porcelain -- <файл>` был ПУСТ до порчи, замерено
   НЕПОСРЕДСТВЕННО перед откатом (базлайн хода не годится —
   межвременной зазор) — это проверяется, а не предполагается
   (иначе ты сносишь незакоммиченные правки параллельной сессии
   вместе со своей порчей, D-0060); (в) witness отката — дословный
   вывод сверки (хэш или дифф), не слово «восстановлено»; (г) боевой
   артефакт не портится ВОВСЕ, когда тот же вывод даёт вердикт
   чистой функции или порча КОПИИ дерева — а он даёт почти всегда;
   (д) F-58 (2026-08-19): отбрасывание СВОЕЙ незакоммиченной работы
   в пути, входящем в собственный owns, легально без пустого
   porcelain — свидетель: декларация владения + дифф ДО
   отбрасывания (что именно отбрасывается) + пустой дифф после
   отката; (е) вне этого условия (путь вне owns, owns не объявлен,
   либо правка не этой сессии — старше, или из ДРУГОЙ сессии)
   действует (б) без изменений: porcelain показывает факт изменения
   и не показывает АВТОРА — и зеркально: непустой статус в пути, НЕ
   входящем в собственный owns, читается как легитимный параллельный
   писатель, не как сторонняя порча — откат такого пути ЗАПРЕЩЁН
   (R4(d): never touch another session's uncommitted paths).
   Нарушение этого порядка в собственном трейле — дефект вердикта,
   даже если в тот раз обошлось: цена ошибки здесь несимметрична.
   Тот же порядок — штатная категория находок при ревью ЧУЖОЙ пробы.
```

**C4:** оба role-файла несут ОДНУ и ту же ветку (д)/(е), той же
формулировкой ключевых фраз (владения/диффа-до/диффа-после/АВТОРА/
таймингa/зеркала), что и CLAUDE.md/POLICY_FULL — твины синхронны.

---

## 5. PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md, чек 25(в)

Якорь (дословная первая строка подпункта, строка 1174 — АДРЕС
СИЛЬНО УЕХАЛ от спеки, было указано :824-831):
```
    (в) НЕБЕЗОПАСНЫЙ ОТКАТ ПОРЧИ (заведён 2026-08-05 как
```

### БЫЛО (строки 1174–1190, дословно)
```
    (в) НЕБЕЗОПАСНЫЙ ОТКАТ ПОРЧИ (заведён 2026-08-05 как
    зарегистрированный детектор п.7 командной гигиены): в
    транскриптах окна ищутся `git checkout --` / `git restore` РЯДОМ
    с мутационной пробой (порча файла, red-probe, «испорчу и верну»).
    Каждое попадание сверяется: снималась ли байтовая копия ДО порчи
    и был ли файл чист по `git status --porcelain` — если нет, это
    нарушение п.7 и находка НЕЗАВИСИМО от того, что в тот раз
    обошлось. Прецедент-эталон: 2026-08-05, критик при R3-гейте
    портил БОЕВОЙ logs/token_usage.xlsx и откатывал checkout'ом;
    потерь не было (файл был чист, бэкап снят, md5 сверен Lead), но
    идиома ровно та, что стоила инцидента AO3 08-02. Контроль формы
    поиска обязателен (гигиена п.6). Это
    зарегистрированный 10(в)-детектор секции «командная гигиена»
    CLAUDE.md: без него нарушения формы вызова не имели отдельного
    детектора отказа (первый замер — пилот t-106 на живой сессии:
    cd-префикс = 76% подозрительных вызовов, 2 heredoc-аппенда
    журнала, 3 MASKED-правила).
```

### СТАНЕТ (РЕВИЗИЯ 2: ветка (2) несёт тайминг [ФИКС 2а]; ветка (3)
требует ОБА witness-артефакта и явно исключает «зачтено по смыслу»
[ФИКС 2в/2г]; добавлено зеркало+R4(d) [ФИКС 2б]; остальное сохранено)
```
    (в) НЕБЕЗОПАСНЫЙ ОТКАТ ПОРЧИ (заведён 2026-08-05 как
    зарегистрированный детектор п.7 командной гигиены): в
    транскриптах окна ищутся `git checkout --` / `git restore` РЯДОМ
    с мутационной пробой (порча файла, red-probe, «испорчу и верну»).
    Каждое попадание сверяется ПО ТРЁМ ВЕТКАМ (третья заведена
    2026-08-19, F-58): (1) снималась ли байтовая копия ДО порчи и
    восстановление — из неё; (2) был ли файл чист по
    `git status --porcelain` ДО порчи, замеренной НЕПОСРЕДСТВЕННО
    перед откатом (замер на базлайне хода в зачёт не идёт —
    межвременной зазор); (3) входит ли путь в owns исполнителя
    пробы, причём правка — той же сессии (ветка (д) п.7 CLAUDE.md) —
    свидетель: ОБА артефакта обязательны — декларация владения И
    дословный пустой дифф после отката (плюс дифф ДО отбрасывания,
    что именно отбрасывается); отсутствие ЛЮБОГО из двух — находка,
    не «зачтено по смыслу». Ни одна из трёх не верна — нарушение п.7
    и находка НЕЗАВИСИМО от того, что в тот раз обошлось; БЕЗ
    третьей ветки детектор метил бы легальный откат СВОЕЙ работы как
    нарушение (класс «сторож, которого приучают игнорировать»).
    ЗЕРКАЛЬНО: непустой `git status --porcelain` на пути, НЕ
    входящем в owns исполнителя пробы, — НЕ основание для отката, а
    легитимный параллельный писатель (не сторонняя порча; сшивка с
    R4(d) «never touch another session's uncommitted paths») — сам
    откат такого пути этим же (в) ловится как нарушение, независимо
    от исхода. Прецедент-эталон: 2026-08-05, критик при R3-гейте
    портил БОЕВОЙ logs/token_usage.xlsx и откатывал checkout'ом;
    потерь не было (файл был чист, бэкап снят, md5 сверен Lead), но
    идиома ровно та, что стоила инцидента AO3 08-02. Контроль формы
    поиска обязателен (гигиена п.6). Это зарегистрированный 10(в)-
    детектор секции «командная гигиена» CLAUDE.md: без него
    нарушения формы вызова не имели отдельного детектора отказа
    (первый замер — пилот t-106 на живой сессии: cd-префикс = 76%
    подозрительных вызовов, 2 heredoc-аппенда журнала, 3
    MASKED-правила).
```

**C5:** третья ветка сверки (байт-копия / пустой porcelain / путь в
owns исполнителя) добавлена — без неё детектор МЕТИЛ БЫ легальный
откат СВОЕЙ работы (ветка (д) п.7) как нарушение (класс «сторож,
которого приучают игнорировать» — явно назван). ФИКС 2в/2г: свидетель
теперь ДВА обязательных артефакта, отсутствие любого — находка сама
по себе.

---

## 6. docs/RULE_COVERAGE.md, §4 «Командная гигиена»

Якорь (дословная последняя строка таблицы §4 перед заголовком §5,
строка 94):
```
| Разбивка находок permission-аудита по классам гигиены (инструмент замера, не сторож) | ЧЕК 25 | tools/permission_audit.py `--summary`, блок «По классам гигиены»: счёт по четырём классам (`2>&1`, `cd/Set-Location`, `python -c/heredoc`, «журнал мимо Edit/Write») по всем просканированным вызовам окна; baseline — числа окна калибровки №6 |
```
(следом — пустая строка 95, затем `## 5. Роли (frontmatter + правила
role-файлов)` на строке 96)

### БЫЛО
Строка 94 (выше) — последняя строка таблицы; новой строки за ней
нет.

### СТАНЕТ (РЕВИЗИЯ 2: строка несёт фиксы 1/2б/2г/7; добавлена
НОВАЯ строка таблицы сразу после строки 94, перед пустой строкой/
заголовком §5)
```
| F-58 (2026-08-19): CLAUDE.md п.7 ветка (д/е) — отбрасывание СВОЕЙ незакоммиченной работы в пути, входящем в собственный owns (правка той же сессии), легально без пустого porcelain — свидетель: декларация владения + дифф ДО отбрасывания + пустой дифф после отката; вне условия (owns не объявлен / путь вне owns / правка не этой сессии — старше, или из ДРУГОЙ сессии) действует (б) без изменений — porcelain не показывает АВТОРА, и ЗЕРКАЛЬНО: непустой статус вне owns исполнителя = легитимный параллельный писатель (сшивка R4(d): never touch another session's uncommitted paths), откат запрещён | ДИСЦИПЛИНА+ДЕТЕКТОР | CLAUDE.md п.7 регистр (д/е) + POLICY_FULL пара (same-commit); твины critic.md «БЕЗОПАСНОСТЬ СОБСТВЕННОЙ ПРОБЫ» (д/е), builder.md п.10; детектор — чек 25(в) калибровки, ТРИ ветки сверки (байт-копия / пустой porcelain, замеренный НЕПОСРЕДСТВЕННО перед откатом / путь в owns исполнителя), оба witness-артефакта обязательны |
```

**C6:** эта строка — тем же коммитом, что и остальные пять правок
(RULE_COVERAGE — ЖИВОЙ владелец картины «правило → сторож», строка
здесь протухшая = находка F-41 самим же документом, шапка
docs/RULE_COVERAGE.md:8-11).

**НАХОДКА (для отчёта):** docs/RULE_COVERAGE.md УЖЕ в
MECHANISM_PREFIXES (строка 147 tools/mechanism_gate.py) — правка
этого файла коммит-msg гейтом ТРЕБУЕТ осевого блока; в отличие от
POLICY_FULL.md (см. носитель №2 выше), для RULE_COVERAGE машинная
обязанность УЖЕ есть.

Эта строка содержит ВСЕ ШЕСТЬ акцептанс-ключей самостоятельно.

---

## Обязанности посадки (сводно, C3/C6)

1. Носители 1+2 (CLAUDE.md п.7 + POLICY_FULL.md п.7) — ОДНИМ
   коммитом (same-commit discipline, C3).
2. Носитель 6 (RULE_COVERAGE.md §4) — ТЕМ ЖЕ коммитом, что и
   остальные пять (C6, «строка тем же коммитом» — обязанность самого
   документа).
3. Носители 3/4 (builder.md/critic.md) — та же ветка (C4), тот же
   коммит логично (все шесть — один механизменный коммит по
   формулировке спеки узла C: «Коммит МЕХАНИЗМЕННЫЙ (все шесть путей
   в неводе/каталогах невода)»).
4. Носитель 5 (WEEKLY_CALIBRATION_PROTOCOL.md чек 25(в)) — тот же
   коммит (C5).
5. Путь docs/POLICY_FULL.md НЕ в неводе mechanism_gate (находка выше)
   — коммит-msg гейт не потребует осевого блока за этот файл
   конкретно, но пять из шести путей (CLAUDE.md, .claude/agents/
   builder.md, .claude/agents/critic.md, PROCESS/
   WEEKLY_CALIBRATION_PROTOCOL.md, docs/RULE_COVERAGE.md) — потребуют:
   коммит обязан нести осевой блок по вопросам D-0055 (а/б/в/г) плюс
   `tier: <model>`.
6. Байт-дельта CLAUDE.md = **872 байта** (РЕВИЗИЯ 2, пересчитана
   выше); посадка легальна ТОЛЬКО после диеты W2c освобождает этот
   объём без пробоя BREACH=36115 (запас на момент замера спеки — 31
   байт, born-breached запрещён); меняет порог К4 спеки W2c.
7. ФИКС 3: прогон PROCESS/CRITIC_EXAM.md ДО коммита посадки (правка
   critic.md — триггер 1 экзамена D-0071) + строка в его Runs log
   ТЕМ ЖЕ коммитом; учесть п.2а протокола экзамена — кэш определений
   агентов требует ЛИБО свежей сессии, ЛИБО inline-доставки нового
   текста critic.md с явной пометкой (иначе экзамен проверит СТАРЫЙ,
   ещё не посаженный текст правила).

---

## Замеченные аналоги / телеметрия (вне owns, не правились)

- **docs/POLICY_FULL.md отсутствует в MECHANISM_PREFIXES**
  (tools/mechanism_gate.py:97-182) — уже задокументированная,
  открытая находка (docs/RULE_COVERAGE.md:49: «решается посадкой
  W2b»); релевантна ЭТОМУ коммиту, так как один из шести правимых
  файлов — POLICY_FULL.md. Не чинил (не в owns, решение W2b — вне
  узла C).
- **norm_keys.json** (tools/norm_keys.json) на момент чтения НЕ несёт
  ни одной строки по командной гигиене (позитивный+негативный
  контроль) — темпоральный край спеки «после t-498 перечитать
  norm_keys.json… появятся — ключи правятся тем же ходом» подтверждён:
  сейчас появляться нечему, т.к. test_norm_form.py (RULE_HEADER_RE) в
  принципе не видит пункты «командной гигиены» (не R-правила) — этот
  класс структурно не может туда попасть, пока кто-то отдельным
  решением не переведёт секцию «Command hygiene» в ту же R-форму. Не
  мой scope, отмечаю для Lead.
- **Кит-твины (Р11)** — вне узла C по решению Lead (очередь релизного
  батча, строка по D-0082 при посадке узла C) — НЕ трогал, как
  указано в задаче.

**ФИКС 4 — три строки-вердикта по оси 3 SIBLING_MAP (класс «role-файл
несёт п.7-твин»), проверено чтением frontmatter каждого файла
(`tools:` строка):**

- **scout.md** — н-п: `tools: Read, Glob, Grep` (проверено чтением
  .claude/agents/scout.md:5) — ни Bash, ни Edit/Write; структурно НЕ
  МОЖЕТ выполнить ни порчу (Bash/git checkout), ни правку файла —
  норме отката/отбрасывания нечего охранять в этой роли.
- **designer.md** — н-п: `tools: Read, Glob, Grep` (проверено чтением
  .claude/agents/designer.md:5) — без Edit/Write (и без Bash) — та
  же причина, драфт спеки не пишет файлы напрямую.
- **judge.md** — н-п: `tools: Read, Glob, Grep` (проверено чтением
  .claude/agents/judge.md:5) — не мутирует дерево (ни Bash, ни
  Edit/Write) — вердикт/JSON-приёмка не создают и не правят файлы.

Контрольная пара (почему builder.md/critic.md — В СКОУПЕ, не н-п):
builder.md несёт `Bash, PowerShell, Edit, Write` (builder.md:5),
critic.md несёт `Bash` (critic.md:5, без Edit/Write, но Bash
достаточен для мутационной пробы) — обе роли структурно СПОСОБНЫ
породить порчу/откат, отсюда норма и её твины именно там.

---

## Witness (полный исходник скрипта + прогоны — R11l(ii), встроено в
пакет по замечанию критика)

Ключи (дословно, из раздела «Акцептанс-ключи» выше, зафиксированы ДО
прогона):
1. «входящем в собственный owns»
2. «пустой дифф после отката»
3. «не показывает АВТОРА»
4. «путь в owns»
5. «НЕПОСРЕДСТВЕННО перед откатом»
6. «легитимный параллельный писатель»

Скрипт (`check_keys.py`, полный исходник):
```python
"""DOC-DISPATCH WITNESS key-presence script (R11l/D-0052) for
docs/tasks/2026-08-19_f58-node-c-content.md (node C, F-58 remediation).

Keys (quoted VERBATIM from the DoD, fixed BEFORE this run):
  1. "входящем в собственный owns"
  2. "пустой дифф после отката"
  3. "не показывает АВТОРА"
  4. "путь в owns"
  5. "НЕПОСРЕДСТВЕННО перед откатом"
  6. "легитимный параллельный писатель"

Usage:
  python check_keys.py <path-to-md-file>

Exit code 0 = all six keys found; 1 = at least one missing.
"""
import sys

KEYS = [
    "входящем в собственный owns",
    "пустой дифф после отката",
    "не показывает АВТОРА",
    "путь в owns",
    "НЕПОСРЕДСТВЕННО перед откатом",
    "легитимный параллельный писатель",
]


def main() -> int:
    if len(sys.argv) != 2:
        print("usage: python check_keys.py <path-to-md-file>")
        return 2
    path = sys.argv[1]
    with open(path, "r", encoding="utf-8") as f:
        text = f.read()
    all_ok = True
    for key in KEYS:
        found = key in text
        status = "PASS" if found else "FAIL"
        if not found:
            all_ok = False
        print(f"{status}: {key!r}")
    print("RESULT:", "ALL KEYS PRESENT" if all_ok else "MISSING KEYS")
    return 0 if all_ok else 1


if __name__ == "__main__":
    raise SystemExit(main())
```

Прогон (i) — по реальному пакет-файлу (ЭТОМУ файлу, после всех
правок), дословный вывод — см. отчёт координатору (тот же скрипт,
тот же путь); экземпляр для повторяемости прогона зафиксирован здесь
байт-в-байт с исходником выше.

Прогон (ii) — НЕГАТИВНЫЙ КОНТРОЛЬ: копия этого файла с ВСЕМИ
вхождениями фразы «не показывает АВТОРА» вырезанными (замена на
плейсхолдер) — скрипт обязан покраснеть РОВНО на этом ключе, прочие
пять — зелёные; дословный вывод — см. отчёт координатору.

---

## Дифф-лист фиксов 1–7 (РЕВИЗИЯ 2, вердикт критика fit_with_fixes)

1. **ФИКС 1** (CLAUDE.md ядро реестра + все пять текстовых
   носителей): «or edits predate the session» / «правка не этой
   сессии» → «or the edits are not this session's — older, or
   another session's» / «правка не этой сессии — старше, или из
   ДРУГОЙ сессии» — применено в носителях 1 (реестр), 2 (е), 3
   (доп. абзац), 4 (е), 6 (строка RULE_COVERAGE).
2. **ФИКС 2** (все шесть носителей + чек 25(в)):
   - (а) тайминг «НЕПОСРЕДСТВЕННО перед откатом» внесён В (б)-ветку
     носителей 2/3/4 (существующий core-текст ЭТИХ файлов — там нет
     байт-бюджетного запрета) и в ветку (2) чека 25(в) (носитель 5);
     для CLAUDE.md (носитель 1) — тайминг внесён В РЕЕСТРОВУЮ СТРОКУ,
     core (а)-(г) НЕ тронут (тот же запрет «paragraph in core»,
     что и мотивировал форму амендмента всего узла C).
   - (б) зеркало-принцип + сшивка R4(d) — во ВСЕХ ШЕСТИ носителях
     (реестр CLAUDE.md, (е) POLICY_FULL/critic.md, доп. абзац
     builder.md, ЗЕРКАЛЬНО-абзац чека 25(в), строка RULE_COVERAGE).
   - (в) чек 25(в) ветка (3): «отсутствие ЛЮБОГО из двух — находка,
     не "зачтено по смыслу"» — внесено дословно.
   - (г) свидетель (д) расширен диффом ДО отбрасывания — во ВСЕХ
     носителях, несущих свидетеля (2/3/4/5/6); фраза «пустой дифф
     после отката» сохранена дословно как СУФФИКС нового текста.
3. **ФИКС 3**: handoff-чек-лист, пункт 7 — прогон
   PROCESS/CRITIC_EXAM.md + строка Runs log тем же коммитом, с
   оговоркой про кэш определений агентов (п.2а).
4. **ФИКС 4**: раздел «Замеченные аналоги» — три строки-вердикта
   (scout.md/designer.md/judge.md, все н-п) + контрольная пара
   builder.md/critic.md, все со ссылкой на проверенную строку
   `tools:` frontmatter.
5. **ФИКС 5**: реестровая строка CLAUDE.md — фолбэк помечен литерой
   «(е)» (было — безымянная часть duty-ячейки после тире); литеры
   сверены с твинами — POLICY_FULL/critic.md уже несли явные (д)/(е)
   с самой первой версии пакета, теперь совпадают буквально.
6. **ФИКС 6**: POLICY_FULL, прецедент 2026-08-09 — убрано ложное
   «файл был чист», заменено на «файл был в его owns» (файл БЫЛ
   грязным — это и есть находка F-58).
7. **ФИКС 7**: все ссылки на critic.md заменены с «правило 14» на
   «"БЕЗОПАСНОСТЬ СОБСТВЕННОЙ ПРОБЫ"» — носители 2 (Твины), 3
   (нет ссылки на critic.md), 6 (RULE_COVERAGE); заголовок раздела 4
   этого пакета тоже переименован.
8. **ПЛЮС (механический слой R11l(ii))**: скрипт-витнесс встроен
   ПОЛНЫМ исходником в раздел «Witness» этого файла (не только в
   отчёт координатору).

Байт-дельта CLAUDE.md пересчитана: **872 байта** (было 438 Б до
ревизии 2).

## Handoff-чек-лист посадки (для Lead)

1. Дождаться запаса байт от диеты W2c (≥872 Б под CLAUDE.md, плюс
   буфер по усмотрению координатора) — порог К4 спеки W2c меняется
   этой ревизией.
2. Применить шесть правок из этого пакета (разделы 1–6 выше) — ядро
   CLAUDE.md БЕЗ изменений, только новый реестр-блок п.7; остальные
   пять — точечные вставки «БЫЛО→СТАНЕТ».
3. Один механизменный коммит, несущий:
   - осевой блок (D-0055 а/б/в/г) + `tier: <model>` — гейт потребует
     его за CLAUDE.md/builder.md/critic.md/WEEKLY_CALIBRATION_
     PROTOCOL.md/RULE_COVERAGE.md (все пять в неводе);
   - явное перечисление ВСЕХ ШЕСТИ путей в теле коммита (включая
     POLICY_FULL.md, которого гейт формально не потребует —
     дисциплина, не машина).
4. Прогон PROCESS/CRITIC_EXAM.md ДО коммита посадки + строка в его
   Runs log ТЕМ ЖЕ коммитом (ФИКС 3, D-0071 триггер 1 — правка
   critic.md); свежая сессия ИЛИ inline-доставка нового текста
   critic.md с пометкой — иначе кэш определений агентов проверит
   старый текст.
5. После коммита — `python tools/escape_check.py` (pre-commit слой,
   должен пройти как часть самого коммита).
6. Канон: `python -m pytest tools/ gateway/ -q`.
7. Parity-проверка (при её наличии в текущем инструментарии
   посадки — не входила в мой манифест «дано», не проверял).
8. Строка по D-0082 для кит-твинов Р11 — при посадке узла C (слово
   Р11 спеки).
