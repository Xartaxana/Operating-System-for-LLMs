# Спека батча ремедиации очереди t-503 (драфт designer t-519, принят Lead 08-19)

Статус: принята; порядок Р0(в): N0 (scout, t-520) → N1‖N2 (builder,
t-521/t-522, параллельно) → N3 (после N0 и решения Р1) → П4
отдельным решением по В3/В4. В4 (A/B-зависание stdin) исполняет
КООРДИНАТОР детерминированной пробой (R8f) — у scout нет shell.
Полный текст драфта с обоснованиями — транскрипт t-519; здесь —
операционный носитель.

## РЕШЕНИЯ LEAD ПО РАЗВИЛКАМ (08-19)

Р0 → (в). Р1 → (г)-как-порядок (N0 меряет причину: если originalFile
исчез из payload — чинится база, не фолбэк), затем (б) + K15
диагностическая метка (выключается в фолбэке только TS DRIFT —
единственный корректностно-ложный слой; TIER/WITNESS/ESCALATION
остаются шумными-но-не-ложными). Р2 → (а) суффикс _q503. Р3 → (а)
батарея на узел. Р4 → (а)+(в): карантин для нераспарсиваемого
(имя НЕ оканчивается на .json), по-ключевая деградация для
«корень/секция кривые»; (б) отвергнут — замораживает предохранитель
consecutive_blocks. Р5 → (в) проверка файлов ДО записи конфига
(неоткатываемого состояния не возникает конструкцией) + (б)-строка
предупреждения как страховка. Р6 → (а) атрибуция по маркеру
liveness-probe-: с маркером = утечка (FAIL и внутри сессии, K11),
без маркера = LIVE-STATE-AMBIENT (не влияет на exit); текст чека
26(д) — handoff в W4, батч протокол не трогает. Р7 → (а) в этом
батче только разведка П4; фикс — следующим по В3/В4. Р8 →
каноническая формулировка класса: «чтение stdin до EOF на
не-TTY-канале БЕЗ EOF виснет; isatty-guard закрывает только
интерактивный случай и класс НЕ смягчает». Р9 → строки очереди
релизного батча кита заводятся при посадке (это учёт, не порт);
сами порты заморожены. Р10 → новые F-NN сейчас не заводятся: П2 —
остаток класса F-61, П1 — остаток F-57; по П4 решение после
N0-замера (реальная дыра у многих → F-62 при посадке фикса).

## Доклад свежести (t-519, несущее)

Узлы A/B F-61 и партия 1 посажены; сиблингов *_f61.py нет; адреса
живые: П1 journal_echo :1697-1827 (фолбэк :1825-1827, метка :1771,
NOTES LEN выключен в фолбэке :1976-1982; валидация :1901-1903 —
намеренная, НЕ трогать); П2 — ТРИ писателя _load_track: dod_track
:551-565, dod_gate :267-284, main_gate :172-185 (session_context и
journal_echo — читатели по самодекларации); П3 session_context
_try_hookspath_autofix :1011-1073 (ветка :1061-1071), живой пин
wiring :265-288 фиксирует отсутствие отката; П4 — 12 хуков, guard
есть у 3 (session_context, claim_control_gate, search_control_gate),
у 9 нет; попутная находка: search_control_gate :576-578 ссылается
на несуществующий hygiene_gate._read_stdin_bytes (протухшая
кросс-ссылка — чинится ходом П4-фикса или строкой очереди); П5
hook_liveness_probe (_LIVE_STATE_TARGETS :140-147, overall_ok
:1099-1108), класс уже задокументирован как «доброкачественный
флейк» в чеке 26(д) :805-808 («сторож, которого приучают
игнорировать»), текст чека — в руках W4 (CHK-26 тело). Цена Р1(а)
измерена: ~66 фолбэк-вызовов в 5 тест-файлах — потому (б), не (а).

## УЗЕЛ N0 — разведка (scout t-520; блокирует N3 и П4-решение)

В1: есть ли originalFile в РЕАЛЬНЫХ tool_response Edit/Write
транскриптов этого проекта (C:\Users\user\.claude\projects\
D--Improving-AI-Operating-System-for-LLMs\<uuid>.jsonl)? Критерий:
«в N последних записях ключ встречается K раз / ни разу» +
позитивный контроль той же формы (заведомый ключ structuredPatch/
filePath). В2: сколько строк «echo base: HEAD-diff fallback» в
транскриптах окна 08-18..08-19, на каких tool_name — единичные
(легитимный fail-open по 26(г)) или регулярные на рутинных
аппендах? В3: таблица по ВСЕМ 12 хукам settings.json — скрипт →
адрес чтения stdin → форма → isatty-guard есть/нет и где → тип
события → блокирующий ли → вердикт «дыра "читает до EOF на
не-TTY-канале без EOF" реальна/нет, почему» (формулировка класса —
Р8; стартовая карта в драфте — ПРОВЕРИТЬ, не наследовать; 12 строк,
ни одной «прочее»). DoD: 4 ответа (В4 — не его, у координатора) +
трейл + позитивный контроль на каждый негатив. Не-цели: правки;
«как чинить».

## УЗЕЛ N1 — П2, потеря чужих ключей трека (builder t-521)

Ключи: K1 класс ко ВСЕМ ТРЁМ писателям, перечисление с вердиктом,
вкл. ветку «корень не-dict» у каждого; K2 форма Р4(а)+(в) реализована
ОДИНАКОВО во всех трёх; K3 докстринг dod_track :111-122 приведён к
правде; K4 fail-open буквально (rc-контракты не меняются); K5
дискриминирующий тест «чужие ключи не исчезли» с красным
контр-прогоном на живом коде, обе половины дословно.
Края: файла нет → существующее поведение дословно; 0 байт → битый;
null/список/число/строка → корень не-dict → та же ветка, что битый;
секция чужая битая (gate_state=список: dod_gate :282-283 упал бы на
setdefault) → чинится только своя секция, чужая переносится как
есть ЛИБО ветка «битый» — реализовать по Р4(в), назвав; карантинное
имя НЕ оканчивается на .json (session_context глобит *.json — тест
на имя обязателен); карантин существует → уникализация mkstemp;
карантин невозможен (read-only) → существующий fail-open без нового
исключения; конфликтные пары 1 (ключи × предохранитель блоков — Р4
решает) и 2 (по-ключевая × нечитаемый текст — только для
распарсиваемого) — из спеки.
DoD: (1) базлайн канона ДО (2 известных тайминго-флейка probe —
легальны в базлайне); (2) контр-прогон КРАСНЫЙ Q503_TARGET=live
pytest tools/test_q503_trackkeys.py -q; (3) целевой зелёный;
(4) суженный: pytest tools/test_dod_track.py tools/test_dod_gate.py
tools/test_main_gate.py tools/test_f61_halfstate.py
tools/test_q503_trackkeys.py -q; (5) BATCH CANON — координатор.
Дано: dod_track :104-150/:551-610; dod_gate :39-73/:189-206/
:267-315; main_gate :49-50/:105-120/:164-217; session_context
:1480-1495/:1537-1600/:1634+ (READ-ONLY); test_f61_halfstate
:73-112 (резолвер — образец, env Q503_TARGET); FINDINGS :2602-2687.
owns: D:\Improving_AI\Operating-System-for-LLMs\tools\dod_track_q503.py;
...\tools\dod_gate_q503.py; ...\tools\main_gate_q503.py;
...\tools\test_q503_trackkeys.py.
Не-цели: живые пути (посадка Lead, механизменный коммит — все три в
неводе); critic_snapshot; session_context; lost-update гонка;
валидация session_id; общий модуль-хелпер (локальные копии);
settings.json; docs/**; кит-твины.
Handoff: посадка байт-копиями + проба живости PostToolUse Edit /
SubagentStop / Stop.

## УЗЕЛ N2 — П3+П5, честность самоотчёта (builder t-522)

П3 ключи: K6 форма Р5(в): проверка наличия файлов хуков ДО записи
git config (неоткатываемое состояние не возникает; при уже
записанном конфиге и отсутствующих файлах — строка предупреждения
оператору, форма (б)); K7 докстринг :1018-1030 к правде; K8 пин
wiring :265-288 перепинен ДВУХМИРНО (правка живого тест-файла
легальна — owns); K9 ветка «сама запись упала» (:1051-1059) не
меняется, пин :211-262 зелен без правок.
П3 края: чужой hooksPath (:1032-1039) не трогается никогда;
перечитка значения перед любой мутацией; отказ git при проверке →
существующий fail-open; detail 120 симв — тесты НА и ЗА границей;
пустой/отсутствующий .githooks → ветка «файлы отсутствуют» (теперь
ДО записи).
П5 ключи: K10 форма Р6(а) — атрибуция diff по маркеру
liveness-probe- (пути и содержимое); K11 ИНВАРИАНТ: настоящая
утечка (с маркером) остаётся FAIL и внутри сессии — тест с
искусственной утечкой обязателен; K12 новый вердикт
LIVE-STATE-AMBIENT в словарь VERDICTS (:78-95) и
format_human_report; overall_ok меняется ровно в ветке
live_state_diff; K13 пины probe :378-395 двухмирно, :352-374 без
правок.
П5 края: вне сессии поведение НЕ меняется (непустой diff = FAIL,
тест обязателен); внутри сессии diff пуст → OK без шума;
одиночные файлы без маркера в имени (critic_snapshot.json,
routing-log) → проверка содержимого на маркер пробы, нет → AMBIENT.
DoD: (1) базлайн канона ДО; (2) контр-прогон КРАСНЫЙ
Q503_TARGET=live pytest tools/test_q503_selfreport.py -q; (3)
целевой зелёный; (4) суженный: pytest
tools/test_session_context_wiring.py tools/test_session_context.py
tools/test_hook_liveness_probe.py tools/test_q503_selfreport.py -q;
(5) живой прогон python tools/hook_liveness_probe.py из сессии —
вывод дословно (ожидание: AMBIENT вместо ложного FAIL); (6) сторож
неизменности реального .claude/ (пробы на tmp-дереве).
Дано: session_context :1003-1073/:1076-1200/:78;
test_session_context_wiring :41-70/:184-288/:291-340;
hook_liveness_probe :1-103/:133-190/:1071-1148/:1167-1182;
test_hook_liveness_probe :330-420; чек 26(д) протокола :790-812
(ЧИТАТЬ, НЕ ПРАВИТЬ); FINDINGS :2657-2664; W4-спека :205-229
(граница).
owns: ...\tools\session_context_q503.py;
...\tools\hook_liveness_probe_q503.py;
...\tools\test_session_context_wiring.py (двухмирные пины);
...\tools\test_hook_liveness_probe.py (двухмирные пины);
...\tools\test_q503_selfreport.py.
Не-цели: живые session_context.py / hook_liveness_probe.py (посадка
Lead); ЛЮБОЙ текст PROCESS/** (чек 26(д) — handoff в W4);
CLAUDE.md/POLICY_FULL/RULE_COVERAGE/FINDINGS/SIBLING_MAP; прочие
каналы wiring_lines; таймауты/лимиты пробы; тайминго-флейки
(отдельный бэклог); кит-твины.
Handoff: посадка session_context — механизменный коммит (в неводе);
hook_liveness_probe ВНЕ невода (проверено с контролем) —
рекомендация оформить мех-коммитом добровольно, внесение в невод —
отдельным ходом НЕ этим батчем; строка в носитель W4: текст флейка
чека 26(д) заменить на семантику AMBIENT тем же ходом, что посадка.

## УЗЕЛ N3 — П1, фолбэк F-57 (builder; ПОСЛЕ N0 + В4 координатора)

Диспатчится после ответов N0 отдельным ходом Lead по Р1: если В1
покажет исчезновение originalFile из payload — постановка меняется
(чинится база, не фолбэк; вопрос наверх). Иначе — форма Р1(б)+K15:
ключи K14 (одна семантика фолбэка для пяти слоёв, документирована),
K15 (метка с кодом причины: tool вне Edit/Write / нет tool_response
/ нет ключа / не строка / не строгий аппенд), K16 (валидация
:1901-1903 не тронута), K17 (тишина на чистой записи сохранена),
K18 (перечисление 5 эхо-тест-файлов с вердиктом). Края и DoD — по
драфту t-519 (транскрипт); owns: journal_echo_q503.py +
test_q503_echobase.py.

## П4 — только разведка этим батчем (Р7(а))

Фикс — следующим батчем по В3/В4; края будущего фикса из драфта
(таймаут=лимит с тестами на/за границей; пара «таймаут ×
блокирующий гейт» — fail-open дыра enforcement, принять явно или
закрыть иначе; Windows select на пайпах не работает — форма
решения явная). Попутная протухшая кросс-ссылка
search_control_gate :576-578 — тем же будущим ходом.
