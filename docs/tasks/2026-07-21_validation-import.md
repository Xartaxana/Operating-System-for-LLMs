# Внедрение валидационного слоя — импорт из VALIDATION_RESEARCH (2026-07-21)

Источник: docs/research/2026-07-21_validation-research.md (внешняя
исследовательская сессия: индустрия + vibe-engineer + маппинг на OS;
оригинал — D:\Improving_AI\VALIDATION_RESEARCH_2026-07-21.md).

Несущие OS-факты отчёта spot-проверены Lead 07-21 до постановки
(R1, dispatch_skipped в журнале): journal_validator проверка 7
требует лишь «witness — непустая строка»
(tools/journal_validator.py:359-361) — дыра №1 реальна;
dod_track.py / session_context.py / dod_gate.py / main_gate.py
существуют и подключены в .claude/settings.json (SessionStart /
PostToolUse) — обе половины механизма №1 и носитель №2 на месте.

Статусы: pending / in-progress / done / blocked / queued.

## Узлы

### N0. Гейт плана (Lead + critic) — done 07-21 (N0a: trail в RELATED_WORK; N0b: вердикт t-256 — N1/N2 годны с правками, N3 годен, жёстких блокеров нет; ограничения внесены в узлы)

Два входа до первого кода (работы >30 мин, отчёт служит спекой):

- **N0a (Lead, D-0066):** точечный второй проход по vibe-engineer —
  только заимствуемые точки (wiring-integrity из run-quality.mjs;
  принцип evidence-v2 «перечитай и перехешируй, пересказ не
  принимается»; схемный read-only запуск ревьюера). Trail — в
  docs/RELATED_WORK.md. Без этого механизм из внешнего обзора не
  входит в очередь.
- **N0b (critic Opus, R3 critic-on-plan):** факты по trail (D-0046),
  осуществимость №1/№2/№4 архитектурным суждением, полнота класса
  по SIBLING_MAP, порядок волны.

### N1. №2 wiring-integrity гейт (builder Sonnet по спеке Lead) — done 07-21 (t-257: v3 принят с критик-вердиктом «годен с правками», F1 — граница 300 тремя тестами, F2 — ось 7 в порт-очередь; хардening stdout-глушения при exec_module; поставлен на живой путь Lead'ом, 113 passed пост-постановки)

Дыра: хуки settings.json умирают молча (python недоступен, файл
переименован, hooksPath не выставлен) — неотличимо от «всё прошло»
(класс F-7). Механизм: session_context.py на SessionStart проверяет
(а) core.hooksPath выставлен, (б) каждый command-файл из
settings.json существует и импортируется, (в) python в PATH;
расхождение → громкая строка в Boot Report (fail-loud). Первый в
волне: это детектор молчаливой смерти хуков — предусловие ценности
N2. Файл на активном hook-пути → D-0069: builder сдаёт контентом /
sibling-именем, ставит Lead при приёмке. Механизм → R10 четыре
вопроса в коммите; критик на дифф (enforcement-цепочка = core, R3).
Правки критика (t-256): чек отдаёт ГРОМКИЕ СТРОКИ-ДАННЫЕ (образец
boot_budget_lines), не исключения — fail-open инвариант
session_context сохраняется, свои под-сбои чек ловит В СТРОКИ; два
канала раздельно — core.hooksPath (git-гейты validator/
mechanism_gate) и settings.json (harness-хуки: exists + importlib
без side effects — top-level модулей чист, проверено критиком);
«python в PATH» — утверждение о PATH процесса (shutil.which), PATH
git-хуков в момент коммита — иной шелл, ограничение отражается
комментарием. Ход приёмки: отчёт builder t-257 принят по содержанию,
НО спека Lead дефектно продиктовала кириллические строки против
ASCII-инварианта файла (находка builder, вернул вопросом — штатно по
R2); решение Lead — инвариант сохраняем, строки в ASCII, доработка у
того же воркера. Находка класса в Lead-очередь: декларированный
ASCII-инвариант vs де-факто кириллица в stdout 5 файлов tools/
(journal_validator, permission_audit, mechanism_gate,
calibration_counts, savings_report) — расхождение документ/факт,
кандидат разбора на калибровке.

### N2. №1 witness × dod_track перекрёстная сверка — in-progress (builder t-258); depends N0, N1 (оба done)

Самое ценное (принцип evidence-v2; тот же класс находки, что
main_gate t-159). Дыра: witness в accepted — самодекларация
(проверка 7 — лишь непустота); текст из повествования или
устаревший прогон проходит. Механизм: journal_echo (момент записи)
сверяет witness нового accepted с реально наблюдавшимся
dod_track-прогоном. РЕШЕНИЯ Lead по вердикту t-256 (связывают
спеку):
- Критерий (B-N2-1): совпадение КОМАНДЫ (подстрочно); outcome —
  вторичный сигнал противоречия (determine_outcome дефолтит red на
  тихий вывод — по outcome не блокуем в одиночку). Трек хранит
  только {ts, tool_name, command, outcome, agent_id} — сверено
  Lead по dod_track.py:109; хеш/подстрока ВЫВОДА неосуществимы;
  расширение трека хешем вывода — НЕ в этой волне (отдельный вход
  + кит-ось).
- Fail-режим (B-N2-2): warn на записи; жёстче — ТОЛЬКО на
  ПРОТИВОРЕЧИИ within-session (команда witness шла red / не шла
  вовсе), НИКОГДА по отсутствию: батч-/кросс-сессионная/
  retro-приёмка (D-0079/D-0056b) легитимна и несопоставима →
  тихая note, не warn. Поиск по ВСЕМ agent_id (прогон
  builder-субагента лежит в том же <session_id>.json — эмпирика
  t-159).
- Оси (B-N2-3, в коммит-блок): ось 4 кит↔штаб — journal_echo (и
  dod_track при касании) в очередь порта тулкита под D-0074; ось 1
  AO3 — н-п (нет witness/track-инфры); ось 1 читающий близнец —
  calibration_counts.py + constants-match-тест синхронно, если
  добавятся схемные константы.
Батч мелочей в этот же диспатч (D-0081): устаревший маркер «НЕ
активирован» в докстрингах dod_track.py/dod_gate.py (активация
2026-07-16 состоялась — вердикт t-256). Тест-батарея R11 — на
границах и за ними. Критик обязателен (core).

### N3. №4 схемный вердикт критика + read-only запуск — pending; depends N0

Дыра: вердикт критика — свободный текст, «есть/полон» судится на
глаз. Механизм: critic-диспатч возвращает JSON по схеме (verdict /
blockers / class-completeness по SIBLING_MAP); инструментальный
набор read-only (без Edit/Write); fail-closed на невалидную схему —
по образцу runner'а vibe-engineer (Часть 2 п.7 отчёта). Средний
приоритет; depends N0, секвенс после N1, независим от N2 (единая
формулировка по t-256). Спека называет канал запуска: субскрипшн
critic-субагент со схемой в DoD-промпте vs gateway-вызов со
--json-schema — env-вопрос, решение при постановке.

### N4. №3 хеш-привязка постоянных исключений к D-решениям — queued

Отчёт сам ставит средний приоритет (DECISIONS_FULL
append-ориентирован, дрейф маловероятен). Не в этой волне; вход по
слову оператора.

## Правила прохода

- N1–N3 — механизмы (D-0065): четыре вопроса R10 + tier-строка в
  коммите, mechanism_gate. Приёмка — матрица D-0058; judge-приёмка
  неприменима (R13: механизмы вне leaf-класса).
- Статус узла двигается в одном ходе с журнальным событием (R4).
- «Не тащить» из отчёта (p0/AST-гейты, Node preload, release-цепочка)
  — решение принято, повторно не рассматривается без нового входа.
