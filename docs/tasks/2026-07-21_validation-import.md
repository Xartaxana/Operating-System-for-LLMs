# Внедрение валидационного слоя — импорт из VALIDATION_RESEARCH (2026-07-21)

Источник: docs/research/2026-07-21_validation-research.md (внешняя
исследовательская сессия: индустрия + vibe-engineer + маппинг на OS;
оригинал — D:\Improving_AI\VALIDATION_RESEARCH_2026-07-21.md).

Несущие OS-факты отчёта spot-проверены Lead 07-21 до постановки
(R1, dispatch_skipped в журнале): journal_validator проверка 7
требует лишь «witness — непустая строка»
(tools/journal_validator.py:359-361) — дыра №1 реальна;
dod_track.py / session_context.py / dod_gate.py / main_gate.py
существуют и подключены в .claude/settings.json (SessionStart /
PostToolUse) — обе половины механизма №1 и носитель №2 на месте.

Статусы: pending / in-progress / done / blocked / queued.

## Узлы

### N0. Гейт плана (Lead + critic) — pending; блокирует N1–N3

Два входа до первого кода (работы >30 мин, отчёт служит спекой):

- **N0a (Lead, D-0066):** точечный второй проход по vibe-engineer —
  только заимствуемые точки (wiring-integrity из run-quality.mjs;
  принцип evidence-v2 «перечитай и перехешируй, пересказ не
  принимается»; схемный read-only запуск ревьюера). Trail — в
  docs/RELATED_WORK.md. Без этого механизм из внешнего обзора не
  входит в очередь.
- **N0b (critic Opus, R3 critic-on-plan):** факты по trail (D-0046),
  осуществимость №1/№2/№4 архитектурным суждением, полнота класса
  по SIBLING_MAP, порядок волны.

### N1. №2 wiring-integrity гейт (builder Sonnet по спеке Lead) — pending; depends N0

Дыра: хуки settings.json умирают молча (python недоступен, файл
переименован, hooksPath не выставлен) — неотличимо от «всё прошло»
(класс F-7). Механизм: session_context.py на SessionStart проверяет
(а) core.hooksPath выставлен, (б) каждый command-файл из
settings.json существует и импортируется, (в) python в PATH;
расхождение → громкая строка в Boot Report (fail-loud). Первый в
волне: это детектор молчаливой смерти хуков — предусловие ценности
N2. Файл на активном hook-пути → D-0069: builder сдаёт контентом /
sibling-именем, ставит Lead при приёмке. Механизм → R10 четыре
вопроса в коммите; критик на дифф (enforcement-цепочка = core, R3).

### N2. №1 witness × dod_track перекрёстная сверка — pending; depends N0, N1

Самое ценное (принцип evidence-v2; тот же класс находки, что
main_gate t-159). Дыра: witness в accepted — самодекларация
(проверка 7 — лишь непустота); текст из повествования или
устаревший прогон проходит. Механизм: journal_echo (момент записи)
и pre-commit сверяют witness нового accepted с реально
наблюдавшимся dod_track-прогоном — совпадение команды +
подстроки/хеша вывода. Спека Lead решает: формат/срок жизни трека,
критерий совпадения, fail-режим (warn на записи / блок на
pre-commit), границы: пустой трек, прогон чужой сессии/субагента
(витнесс воркера против трека координатора!), устаревший прогон
после последней правки, retro-события. Тест-батарея R11 — на
границах и за ними. Критик обязателен (core).

### N3. №4 схемный вердикт критика + read-only запуск — pending; depends N0

Дыра: вердикт критика — свободный текст, «есть/полон» судится на
глаз. Механизм: critic-диспатч возвращает JSON по схеме (verdict /
blockers / class-completeness по SIBLING_MAP); инструментальный
набор read-only (без Edit/Write); fail-closed на невалидную схему —
по образцу runner'а vibe-engineer (Часть 2 п.7 отчёта). Средний
приоритет; после N1 (не блокирован N2).

### N4. №3 хеш-привязка постоянных исключений к D-решениям — queued

Отчёт сам ставит средний приоритет (DECISIONS_FULL
append-ориентирован, дрейф маловероятен). Не в этой волне; вход по
слову оператора.

## Правила прохода

- N1–N3 — механизмы (D-0065): четыре вопроса R10 + tier-строка в
  коммите, mechanism_gate. Приёмка — матрица D-0058; judge-приёмка
  неприменима (R13: механизмы вне leaf-класса).
- Статус узла двигается в одном ходе с журнальным событием (R4).
- «Не тащить» из отчёта (p0/AST-гейты, Node preload, release-цепочка)
  — решение принято, повторно не рассматривается без нового входа.
