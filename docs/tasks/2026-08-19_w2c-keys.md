# Ключ-карта W2c-1 / W2c-2 (досылка designer t-512b, принята Lead 08-19)

Владелец ключ-прогонов узлов W2c-1/W2c-2. Решения Lead по развилкам
досылки (Р10–Р15) — в шапке docs/tasks/2026-08-19_w2c-diet-spec.md.
Все ключи — ПОСТ-ФОЛД (контракт _fold_whitespace: {пробел,таб,CR,LF}+
→ один пробел; `|` не фолдится). ФОЛД ОБЯЗАТЕЛЕН: два живых
экземпляра класса — chek30-2 и «never invent project state»
(CLAUDE.md:8-9, сырой греп даёт ноль при живой норме). Классы
адресов: A — обязан остаться в носителе среза; A′ — пинит НОВЫЙ
текст замены; R — ретируется (обязан ИСЧЕЗНУТЬ, проверка с
позитивным контролем); P — контрольный ключ у другого носителя;
B — обязан ПОЯВИТЬСЯ в PROCESS/CALIBRATION_HISTORY.md; C — в
docs/RULE_COVERAGE.md. Ключ, найденный не по своему адресу, = провал.

## §1 W2c-1 — CLAUDE.md

Тексты замен (Р13(A), дословно):
- C2: `Grades (API contour) are accounting vocabulary, never rule words (ARCHITECTURE.md "Two Vocabularies").`
- C5: `Enforced at TWO points: the pre-commit validator and the journal_echo write-time warn (tools/journal_validator.py).`
- C6 (вместо мермейд-блока 431–436, две строки прозы):
  `While degraded (the window is opened by \`lead_degraded\` naming cause and scope): coordination and authorized tasks — yes; table statuses and gates — no; new DECISIONS → the Lead queue; acceptance per the Role ≠ tier matrix.`
  `Return is the DEFAULT at the task/session boundary → \`lead_restored\` + acceptance of the window (journal + diffs of ALL repos touched by the session, D-0044) in the event's notes; an empty window is noted explicitly.`
- C7 (замена последнего предложения п.6): `(F-55: the verdict sits at the END of an append-only entry — a truncated window shows the problem without its resolution).`
- C10 (Р15A): снять предложение `A repeat delegated on a CLOSED task is forbidden (D-0060).` — дубль мермейда жизненного цикла (`reopen forbidden (D-0060)` остаётся).
- C13 (Р15A): скобка `(never a block; silent in the hook's fallback base, which prints its own marker)` → `(warn, never a block)`.

Ключи (полный набор досылки):
- C1: c1-1 `This repo is the reference (dogfooding) deployment` A; c1-2 `never invent project state — retrieve it from the repository` A (фолд!); c1-3 `the repository overrides chat` A; c1-4 `This file holds only what must ALWAYS be in context` A; c1-5 `Full state recovery is BOOT.md` A; c1-6 `an amendment to a rule with a registry block is a ROW in that registry` A; c1-7 `Breach → boot-diet, CLAUDE layer` A; c1-8 `deploys:` в delegation.config.yaml P; c1-R `the pilot is D:\AO3_tests` R.
- C2: c2-1 `are accounting vocabulary, never rule words` A′; c2-2 `ARCHITECTURE.md "Two Vocabularies"` A; c2-3 `Policy rules speak ONLY these function names` A; c2-4 `the function→model binding is a deployment property` A; c2-5 `defaulting to fable when the file is absent` A (фолд); c2-6 `A RESERVE tier sits ABOVE the Lead binding` A; c2-7 `summoned only by the operator's word for the hardest cases` A (фолд); c2-R `never used in rules` R.
- C5: c5-1 `Enforced at TWO points` A′; c5-2 `tools/journal_validator.py` A; c5-3 `re-read the journal tail immediately before` A; c5-4 `never reuse a remembered id` A; c5-5 `A repeat \`delegated\` on a CLOSED task is forbidden` — ПЕРЕХОДИТ В R (C10 активирован Р15A); c5-5a НОВЫЙ КЛЮЧ A (заведён по фиксу 7 критика t-516 — адрес C10 обязан быть подпёрт ключом): `reopen forbidden (D-0060)` — мермейд жизненного цикла Journal; c5-6 `a missed event is fixed by a retro pair NOW` A — carrier_anchor, ТОТ ЖЕ АБЗАЦ, соседние предложения НЕ переформулировать; c5-7 `inserting lines into the past is forbidden` A (фолд); c5-8 `the SessionStart hook reads ONLY this token` A (фолд); c5-9 `never write the literal form with a live id in prose` A; c5-10 `Closure is reader-specific, not one fact` A (абзац C4 — вердикт НЕТ, целиком); c5-11 `or "judge" on a leaf-class dispatch` A — carrier_anchor.
- C6: c6-1 `While degraded` A′; c6-2 `naming cause and scope` A′; c6-3 `coordination and authorized tasks — yes` A′; c6-4 `table statuses and gates — no` A′; c6-5 `new DECISIONS → the Lead queue` A′; c6-6 `acceptance per the Role ≠ tier matrix` A′; c6-7 `Return is the DEFAULT at the task/session boundary` A′; c6-8 `diffs of ALL repos touched by the session, D-0044` A′; c6-9 `an empty window is noted explicitly` A′; c6-10 `Triggers: refusal of the Lead-binding model` A; c6-11 `check your actual model by the last visible signal` A; c6-12 `is a full Lead with margin — no window needed` A; c6-13 `a visible ascent is by itself PROOF of a window` A; c6-14 `A degradation crossing the session boundary must be the journal's last event` A; c6-R `stateDiagram-v2` R ПО ДИАПАЗОНУ СЕКЦИИ ДЕГРАДАЦИИ (в секции Journal остаётся — проверка по файлу даст ложный зелёный).
- C13 (ключи заведены по фиксу 8 критика t-516 — срез был вне
  прогона): c13-1 `(warn, never a block)` A′ (CLAUDE.md); c13-R
  `silent in the hook's fallback base` R (CLAUDE.md, с позитивным
  контролем формы); c13-2 `echo base: HEAD-diff fallback` A —
  НОСИТЕЛЬ: PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md чек 13(ж)
  (раскрытие фолбэк-базы перенесено туда решением Lead по фиксу 2
  критика — живой носитель вместо возврата байтов в ядро).
- C7: c7-1 `the verdict sits at the END of an append-only entry` A′; c7-2 `valid only after reading THROUGH that entry's status line` A; c7-3 `grepping the status FIELD itself` A; c7-4 `the presence of the entry or of its header inside a read window is NOT a check` A (фолд); c7-5 `an empty result is reported only after a positive control of the call` A; c7-6 `a control with a different pattern proves the pipe, not absence` A; c7-7 `unverified — mark explicitly "estimate, not verified"` A; c7-R `Append-only registries put the verdict at the END of a multi-section entry` R.

Контроль машинных пинов: 4 carrier_anchor (escape_allowlist) вне зон
срезов — но c5-6/:327-328 и c5-11/:318 в СЕКЦИИ правки C5, соседние
предложения не переформулировывать; escape_check обязателен.
norm_keys.json — только R1/R2/R4/R8/R11/R13 (:41-281), зоны срезов
вне; test_norm_form не задет; CLAUDE_WARN/BREACH — зона W2c-4, не
трогать; parity hq-drift ожидаем, объявить.

## §2 W2c-2 — протокол (D-серия + D17-D20/D9b)

Тексты замен (Р13(A)/Р12(A), дословно):
- d1-11 (концовка :664-667; АДРЕС ПЕРЕПИНЕН 08-19 по фиксу Ф2 критика t-525 — «§7» не резолвился, владелец = безномерная секция справочника): `а не снимок применения у владельца (docs/RULE_COVERAGE.md, «Справочник упомянутых чеков», запись «чек 26»): снимок может протухнуть — тогда прогон пересчитывает категорию (2) по факту текста.`
- D2-фикс (:729-730): `…НИ ОДИН из tools/*.py-скриптов этих хуков`.
- D4-пример: `пример дрейфа скоупа: v1-триггер t-177 ловил только класс «журнал мимо Edit/Write».`
- D14: `база контрфакта = привязка \`roles.lead\` с 2026-08-16 (t-444); точки ДО — по Fable, несопоставимы; разрыв базы помечается в notes.`
- D15: `разрыв базы помечается в notes с НАПРАВЛЕНИЕМ ошибки (завышена/занижена); известные разрывы — PROCESS/CALIBRATION_HISTORY.md.`
- D17-скобка: `(binding-aware формулировка 2026-08-16 по кросс-пункту AO3_tests 08-15; прежняя редакция — PROCESS/CALIBRATION_HISTORY.md)`
- D18-хвост: `для окон ДО 2026-08-16 — поправка в PROCESS/CALIBRATION_HISTORY.md.`
- D9-указатель: `известные поправки счёта — PROCESS/CALIBRATION_HISTORY.md`
- D19 (Р11A): снимается ТОЛЬКО атрибуция отмены (`отменён словом оператора 2026-08-10` → B); контраст «не «обкатано у нас»» остаётся.

Ф6 — D1 НЕ чистое иссечение, четыре обязательные точечные правки:
d1-11 выше; :628-630 самореферентная оговорка снимается вместе с
D5′; D2-фикс «перечисленных выше»; сжатый пример D4. D7 отдельно НЕ
исполняется (D7 ⊆ D1); долг очереди ремедиации чека 26 → строка в
CURRENT_CONTEXT.md (Р3A, класс P).

Ключи по срезам (класс в скобках):
- D1: d1-1 `Греп-попадание на имя гейта бывает ТРЁХ родов` (A); d1-2 `(2) НАЗВАН ПРЕДМЕТОМ ПРОВЕРКИ ЕГО ОТКАЗА/ЖИВОСТИ` (A); d1-3 `(3) НАЗВАН КАК ИНСТРУМЕНТ ЧУЖОЙ ПРОВЕРКИ` (A); d1-4 `РЕЖИМ ОТКАЗА, НЕ ФОРМА УПОМИНАНИЯ` (A); d1-5 `не отличает «нарушений не было» от «гейт мёртв, записей нет вообще»` (A); d1-6 `САМИ подпункты (б)/(в)/(г) ниже и тексты других чеков, ПЕРЕЧИТАННЫЕ ЗАНОВО` (A); d1-7 `резолюция по данным — законная форма опознания предмета` (A); d1-8 `ПОКРЫТЫ (молчание детектируется)` (C:172); d1-9 `ПОКРЫТЫ ЧАСТИЧНО` (C:173); d1-10 `НЕ ПОКРЫТЫ (детектора отказа/живости нет нигде в протоколе)` (C:183-184); d1-11 `прогон пересчитывает категорию (2) по факту текста` (A′).
- D2: d2-1 `сверить состав \`.claude/settings.json\` ПО ФАКТУ ФАЙЛА НА МОМЕНТ ПРОГОНА` (A); d2-2 `расхождение перечисления протокола с фактом = находка САМА ПО СЕБЕ` (A); d2-3 `состав сходится` (A); d2-4 `правка скрипта БЕЗ правки settings.json проходит мимо осевого блока` (A); d2-5 `СВЯЗЬ С F-56 (невод механизмов)` (A); d2-6 `tools/negative_lint.py` (P: .claude/settings.json).
- D3: d3-1 `блокирующий класс «журнал мимо Edit/Write» permissionDecision НЕСЁТ ПО ЗАМЫСЛУ` (A); d3-2 `его ИСЧЕЗНОВЕНИЕ есть регресс` (A); d3-3 `все ОСТАЛЬНЫЕ классы хука — warn-only` (A); d3-4 `появление permissionDecision у любого из них = регресс в обратную сторону` (A); d3-5 `была верна для v1 t-177 и ПРОТУХЛА после VG-5 2026-07-23` (B).
- D4: d4-1 `Живость hygiene_gate сверяется ЧЕКОМ 25 ПО ЕГО ФАКТИЧЕСКОМУ СКОУПУ НА МОМЕНТ ПРОГОНА` (A); d4-2 `скоуп расширяется решением по evidence, D-0063, и тогда же расширяется эта сверка` (A); d4-3 `находки аудита класса, ВХОДЯЩЕГО в текущий скоуп хука, при ПУСТЫХ warn-строках` (A); d4-4 `Правило сверки при любой редакции скоупа одно` (A); d4-5 `v1-триггер t-177 ловил только класс` (A′); d4-6 `на 2026-07-19 (калибровка №3, по чтению кода)` (B).
- D5′: d5-1 `КРИТЕРИЙ ЗАСЧИТЫВАНИЯ` (A); d5-2 `засчитывается (полностью или частично` (A); d5-R `не может служить своим же положительным контролем` (R).
- D14: d14-1 `База контрфакта = привязка` (A′; ПЕРЕПИНЕН 08-19 — косметика Ф6 критика t-525 подняла регистр начала предложения, перепин по правилу t-496); d14-2 `разрыв базы помечается в notes` (A′); d14-3 `вопрос контрфакта звучит «а если бы это делал Lead», и имя модели в нём не константа` (A); d14-4 `по ценам ПРИВЯЗКИ \`roles.lead\` НА МОМЕНТ ОКНА` (A); d14-5 `всё ещё держит \`FABLE_MODEL\` константой` (B).
- D15: d15-1 `с НАПРАВЛЕНИЕМ ошибки` (A′); d15-2 `известные разрывы — PROCESS/CALIBRATION_HISTORY.md` (A′); d15-3 `(в) цена за принятую единицу делегированной работы` (A); d15-4 `(г) API-контур: учётный итог по traffic_kind + real-доля` (A); d15-5 `№6 шла в окне 07-29T18:42..08-05T12:34` (B); d15-6 `брутто-экономия №7 ЗАВЫШЕНА, а не занижена` (B).
- D16: d16-1 `Секция отчёта «ПРОПУЩЕННЫЕ ОКНА»` (A); d16-2 `обязана быть пустой ЛИБО объяснённой в notes` (A); d16-3 `аудит СОСТОЯНИЯ НА СТАРТЕ прогона` (A); d16-4 `отсутствие чисел в notes \`calibrated\` видно следующему прогону и оператору (F-32)` (A); d16-5 `Калибровка ЗАПИСЫВАЕТ и свой собственный прогон в реестр run_units` (A); d16-6 `Это ЕДИНСТВЕННАЯ новая обязанность` (A); d16-7 `формулировка ДО этой правки утверждала` (B).
- D9/D9b: d9-1 `Целостность task_id (D-0060): дублей task_id между несвязанными задачами нет` (A); d9-2 `residual-детектор TOCTOU-окна кодового гарда AO3 log_append.py` (A); d9-3 `ts-честность (F-29): выборочная сверка ts событий окна с внешними часами` (A); d9-4 `расхождение порядка часов или немонотонность внутри сессии = нарушение` (A); d9-5 `Промоция в код — ts-sanity в объёме B1-валидатора (очередь)` (A); d9-6 `известные поправки счёта — PROCESS/CALIBRATION_HISTORY.md` (A′); d9-7 `Известный дубль — t-008` (B); d9-8 `во всех счётах чеков 3/13 идёт как ДВЕ задачи` (B); d9b-1 `события t-023..t-026` (B); d9b-2 `брать реальные времена из notes defect_found` (B).
- D17: d17-1 `ГРАНИЦА ДЕГРАДАЦИИ — ПРИВЯЗКА, А НЕ ИМЯ МОДЕЛИ` (A); d17-2 `Lead-активность на модели НИЖЕ \`roles.lead\` ТОГО деплоя, чей журнал читается` (A); d17-3 `при спорной строке — привязка на МОМЕНТ СОБЫТИЯ` (A); d17-4 `Модель, РАВНАЯ привязке, — штатный полный Lead, не деградация` (A); d17-5 `классифицируется вслух как ИСТОРИЯ` (A); d17-6 `Хоть одно попадание класса КРИТЕРИЙ` (A); d17-7 `Lead-активность на модели ниже Fable вне заявленного окна` (B).
- D18: d18-1 `для окон ДО 2026-08-16` (A′); d18-2 `(iv) КЛАУЗУЛА НЕЗАВИСИМОГО КОНТЕКСТА (D-0099` (A); d18-3 `Основания (iii)/(iv) читаются по привязке ТОГО деплоя, чей журнал читается, а не по имени модели` (A); d18-4 `Такая строка законна БЕЗ basis, и требование basis к ней = ложное срабатывание` (A); d18-5 `Льгота «critic: skipped» легальна только у принимающего выше исполнителя` (A); d18-6 `она отстала от D-0087 (2026-07-21) и D-0099 (2026-08-04)` (B).
- D19: d19-1 `каждый hq-drift и каждый файл штаба без кит-твина ЛЕГАЛЕН только если его класс явно помечен «кухня»` (A); d19-2 `прочий разрыв = НЕДОПОСТАВКА, находка калибровки` (A); d19-3 `обкатка у добровольцев` (A); d19-4 `отменён словом оператора 2026-08-10` (B).
- D20: d20-1 `Стоимостной кроссовер эскалаций (Update Rule 4 таблицы, исполняемая` (A); d20-2 `фактическая полная стоимость всех попыток` (A); d20-3 `Систематический минус по категории (дешёвый ярус с ретраями дороже) = evidence для правки границы делегирования` (A); d20-4 `Пустое окно (0 эскалаций) — явно` (A); d20-5 `правило 6 ССЫЛАЛОСЬ на калибровку, но исполняемого чека не было, класс F-15` (B). Форменный край: заголовок чека 21 укорачивается — маркер CHK 21 остаётся первой строкой после закрывающего ** (AM-1 П1), доказывается --check-form.
- Инварианты архива: h-1 указатель `PROCESS/CALIBRATION_HISTORY.md` ≥4 вхождений в протоколе (чеки 18/13/5/6); h-2 шапка архива: владелец + правило ведения (гейт (б)); h-3 все 14 B-ключей в архиве. Негативный контроль Р14(A): ДВА красных прогона — снятый ключ класса A и снятый ключ класса B.
