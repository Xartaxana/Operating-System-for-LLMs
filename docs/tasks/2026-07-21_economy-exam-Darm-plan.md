# D-плечо экономэкзамена — план прогонов (кандидат №1: статическая allocate-таблица)

Статус: ЗАПУЩЕН 2026-07-21 — R5 подписан, слово оператора
«подписываю R5, запускай D-прогоны» получено (D-0086).

## DAG задачи (правило 4а CLAUDE.md / D-0080; статусы двигаются с журналом)

| узел | ярус | статус |
|---|---|---|
| build: оркестратор D + манифесты D1 (Dsn/Dhk/C) + юнит-тесты | builder→opus | DONE (t-239 принят att.3: 45 passed; путь — 2×rejected, эскалация R6 на opus) |
| review: критик на оркестратор (>100 строк, R3) | critic | done ×2 (t-241 ДОРАБОТАТЬ 4 блокера; t-243 ДОРАБОТАТЬ 1 остаточный; att.3 — critic skipped под двумя трассами) |
| run1: прогон №1 (D + C) + judge-приёмка | конструкция | in_progress (запущен 11:51, прокси поднят; координаторная нагрузка окна — параллельный t-242, мерить cc_usage честно) |
| grade1: грейдинг табелем 6б | critic-класс | pending |
| run2 / grade2 | — | pending |
| run3 / grade3 | — | pending |
| verdict: медианы против ключей R4', строка гейт-отчёта, Runs log | Lead | pending | Рамка: R4' ROADMAP — гейт открывается взятыми D-ключами;
дизайн — docs/tasks/2026-07-20_routellm-evaluation-plan.md (раздел
«Дизайн эксперимента»); батарея и планки —
PROCESS/DEPLOYMENT_ECONOMY_EXAM.md (набор №1, табель 6б).

## Конструкция D (кандидат №1 — нулевой новый компонент)

Гипотеза: приз — УДАЛЕНИЕ ДОРОГОГО РЕШАТЕЛЯ (Lead-координатор ~85%
спенда) при сохранении качества; allocate/dispatch/приёмка — без
frontier-токенов.

1. **Allocate — статическая таблица** (категорийная лестница D-0085,
   пинуется здесь): t1 «калькулятор» (продукт с нуля,
   implementation) → sonnet; t2 «разведка click» (recon) → haiku;
   t3 «починка todo» (доработка кода, implementation) → sonnet.
   Никакого LLM-решателя на allocate: категория задачи → ярус по
   таблице.
2. **Dispatch — headless-клетки** tools/exam_runner.py, layout
   empty (без CLAUDE.md/агентов — конструкция D сама не несёт
   политики), suffix — штатный headless-протез эскалации (тот же,
   что у всех плеч; замер модели прокси обязателен, D-0083).
   Модель на манифест → сплит по ярусам (прецедент N18/18b):
   манифест D-haiku (t2) + манифест D-sonnet (t1, t3).
3. **Приёмка ВНУТРИ конструкции — судья, не Lead**: judge-sonnet
   (калиброван 13/13) по интент-ключам набора №1; вердикт
   accept/reject + краткий feedback. Стоимость судьи входит в цену
   плеча.
4. **Ретрай/эскалация — детерминированное зеркало R6**: reject →
   1 ретрай тем же ярусом (feedback судьи в промпт); второй reject
   → ярус вверх (haiku→sonnet→opus), максимум одна эскалация;
   reject после эскалации = задача не принята (плечо теряет очки).
5. **Финальный грейдинг** — штатная процедура табеля 6б (та же, что
   у A/B/C, вне стоимости плеча — как у всех плеч; судья-приёмщик
   п.3 — ВНУТРИ стоимости).

## Предрегистрированные ключи (числа запинованы 2026-07-21, до прогонов)

Вердикт — по МЕДИАНАМ ≥3 прогонов батареи №1 (дисперсия серии
×2.6–12.7):

- (а) **Деньги**: $/принятую D ≤ $/принятую C ТОГО ЖЕ ПРОГОНА
  (плечо C гоняется свежим в каждом прогоне — дословно по
  предрегистрации 07-20).
- (б) **Качество**: скаляр 6б D ≥ B_ref − 0.05 = **0.87**, где
  B_ref = 0.92 — медиана трёх точек АКТУАЛЬНОЙ политики (ядро
  D-0084; прогоны №15/№16/№17: 0.94/0.90/0.92). Качество не
  торгуется: провал (б) = проигрыш гейта при любых (а)/(в).
- (в) **Время**: wall-clock D ≤ B_ref = **18 мин** — медиана окон
  №15/№16/№17 (17/20/18; метод cc_usage min..max, правило
  атрибуции внешних простоев).

Зафиксированная интерпретация (Lead, до прогонов): ключи (б)/(в)
сверяются с ИСТОРИЧЕСКОЙ медианой B актуальной политики (гонять
свежие B-плечи не требуется — три точки ядра уже сняты 07-19);
ключ (а) — со свежим C того же прогона (буква предрегистрации).
Отступление от этой интерпретации после старта прогонов запрещено.

- Добавочная метрика D (из плана оценки): доля координационных
  токенов (судья+оркестрация против воркеров) — ожидание: падение с
  ~85% (чек 11) до ~цены судьи; меряется, ключом не является.

## Смета и порядок

3 прогона × (D + C) на батарее №1 ≈ $15–30 всего (D ~$2–4, C ~$2–3.5
за прогон) + судья-приёмка (~копейки) + грейдинг. Кандидаты №2/№3
(haiku-классификатор / causal_llm) в D-прогоны — ТОЛЬКО по
evidence-триггерам (статика провалила ключи качеством/misroute ИЛИ
трафик шире батареи); их скамейка на labeled_v2 — t-236.

Исполнение: builder собирает оркестратор (манифесты сплита,
judge-вызов, ретрай-петля — детерминированный скрипт) по спеке Lead;
прогоны — после подписи R5. Вердикты и evidence — SHADOW_EVALUATION_LOG
(строки прогонов) + Runs log экзамена + строка в гейт-отчёт; adopt/
reject решением с записью DECISIONS (шаг 4 плана оценки).
