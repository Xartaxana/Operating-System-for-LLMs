# Экзамен экономии — прогон №2 (релиз v0.2.0), операционный лист

Протокол: PROCESS/DEPLOYMENT_ECONOMY_EXAM.md (набор №1 пинован там же;
ключи приёмки плечам НЕ показываются). Повод: релизный снимок тулкита
v0.2.0 (тег v0.2.0, снимок 2200900, github.com/Xartaxana/Supervised-
Delegation; локальный toolkit/ staging заявлен идентичным, ось 7 в
синке) — триггер каденции «после каждого релизного снимка». Слово
оператора на прогон дано 2026-07-14 вечером. Сравнительная база —
прогон №1 (docs/tasks/2026-07-13_economy-exam-run1.md, релиз v0.1.0).
Спека этого прогона — opus-дизайнер (пилот, t-109); решения Lead по
развилкам спеки: источник шаблона — клон тега (R1), перезапись README
в B/t3 как в №1 (R2).

## ВАРИАЦИЯ ПРОТОКОЛА ЭТОГО ПРОГОНА (слово оператора 2026-07-14) + КОНФАУНД

Плечо B в №2 разворачивает спеки builder-диспатчей не Fable-сессией
напрямую, а через Opus-субагента (схема пилота дизайнера,
docs/tasks/2026-07-14_opus-designer-pilot.md). Инструкция доставляется
ДОБАВОЧНОЙ СТРОКОЙ к сообщению задачи плеча B (§ «Тексты задач»),
НЕ правкой релизного шаблона: шаблон v0.2.0 пилот-строки не содержит
(проверено грепом toolkit/CLAUDE.md — англ. релизная версия, пилот-
пометка есть только в корневом CLAUDE.md как эксперимент), и мораторий
D-0074 + честность «релизный шаблон» запрещают его править под экзамен.

КОНФАУНД (фиксируется явно): в №1 плечо B писало спеки само (Fable),
в №2 — через дизайнера. Поэтому дельта B (№2−№1) СМЕШИВАЕТ два эффекта:
(1) регрессию/прогресс релиза v0.1.0→v0.2.0 и (2) эффект opus-дизайнера
на стоимость/качество. Чистого прочтения релизной регрессии по B в
этом прогоне НЕТ — единственное релизонесущее плечо совпадает с
плечом-носителем конфаунда. A и C релиза не несут (§ «После прогона»,
разбор дельт). Прогон читается как первый замер связки
«деплой v0.2.0 + дизайнер» (слово оператора); финальная трактовка
вердикта — оператор.

## Полигон (готовится builder-диспатчем по спеке дизайнера, t-110)

- D:\Improving_AI\exam_release2\{A,B,C}\{t1,t2,t3} — девять песочниц.
  A — t1 пустой, t2 клон click, t3 todo-фикстура; C — идентично A;
  B — то же плюс файлы релизного шаблона v0.2.0 (без .git) в корне
  каждой из трёх песочниц.
- t2 всех плеч: клон pallets/click, пин b67832c2, одинаков во всех.
- t3 всех плеч: todo-фикстура (мастер docs/exam_fixtures/run1/todo_cli/,
  файлы todo.py + README.md; в B/t3 фикстурный README.md перезаписывает
  README шаблона — как в №1, README не boot-критичен).
- _src\ — эталонные клоны (click + шаблон v0.2.0 клоном тега; если
  github недоступен — копия toolkit/ staging с оговоркой в Runs log:
  контроль «staging==published» тогда отпадает). В сессии не открывать.

## Правила прогона

1. МОДЕЛЬ: Fable во всех девяти сессиях (сопоставимость с №1).
2. Одна сессия = один каталог = одна задача. Других работ в песочницах
   не вести. Сессию не продолжать после вердикта.
3. Порядок плеч — НОВАЯ ротация (не совпадает с №1; №1 был
   T1 A-C-B / T2 B-A-C / T3 C-B-A):
   - **T1 — B, A, C**
   - **T2 — C, B, A**
   - **T3 — A, C, B**
   (каждое плечо по разу в каждой позиции; порядок каждой задачи
   отличается от №1 — «нет любимого последнего плеча».)
4. Плечу A — только текст задачи. Плечу B — текст задачи + ДОБАВОЧНАЯ
   СТРОКА (opus-спеки). Плечу C — сначала канонический промпт, затем
   текст задачи, одним сообщением.

## Тексты задач (дословно из протокола, без ключей)

- **T1 (в каталоге t1):** «Напиши мне калькулятор»
- **T2 (в каталоге t2):** «В каталоге лежит клон репозитория click.
  Разберись, как в нём устроены группы команд и chain-режим, и подготовь
  отчёт с планом добавления алиасов команд: что менять, где, какие риски»
- **T3 (в каталоге t3):** «В каталоге маленький todo-менеджер.
  Пользователи жалуются: после удаления задачи номера съезжают и delete
  удаляет не то. Почини и сделай, чтобы этот класс багов больше не
  возвращался»

### ДОБАВОЧНАЯ СТРОКА к сообщению плеча B (все три задачи, дословно)

> Дополнение к процедуре для этой сессии (слово оператора 2026-07-14):
> точные спеки builder-диспатчей не пиши сам. По каждой нетривиальной
> спеке составь интент-бриф (намерение, DoD уровня результата, границы
> и уже принятые решения) и разверни его в точную спеку диспатчем
> opus-дизайнера (general-purpose, model=opus, лейбл «opus: спека …»)
> с ОБЯЗАТЕЛЬНЫМ блоком развилок. Спеку прими сам (сверка с брифом +
> вердикт по каждой развилке), затем запускай builder по принятой
> спеке. Однострочные правки — без дизайнера.

(Добавочная строка идёт ОТДЕЛЬНЫМ абзацем после текста задачи, тем же
сообщением. Она — вариация протокола, не часть недоопределённого
намерения задачи; конфаунд учтён выше.)

### Канонический промпт плеча C (дословно, перед текстом задачи)

> Используя воркфлоу-подход, запускай субагентами Sonnet и Opus там,
> где это возможно.

## UI-приёмка (правило протокола с 2026-07-14)

Задача, чей результат — UI (в наборе №1 это может быть T1-калькулятор,
если плечо выбрало GUI/web): принимается только ВОЖДЕНИЕМ UI —
скриншот/запись «до/после» как witness. Статическое чтение кода
недостаточно (прецедент C-T1 №1: визуальный дефект строки выражения
прошёл мимо кодовой приёмки).

## После прогона

1. **Приёмка** (Lead): каждый из девяти результатов — по пинованным
   ключам протокола (принят/не принят); ретраи по желанию плеча в той
   же сессии входят в стоимость. Ключи-интерпретации (напр. T1 «=6»)
   помечены «спор решает оператор»; уточняющий вопрос пользователю от
   плеча — плюс в вердикте. UI-результат — по вождению UI. Спорные
   вердикты — оператор.
2. **Учёт**: tools/usage_report.py по девяти проектам-песочницам
   (атрибуция по пути), API-расход плеча B из requests.db (если
   делегировал на API) в стоимость B. Заполнить строку Runs log
   протокола: $/принятую по плечам, делегированная доля, эскалации,
   мин/задачу. Проверить, что учёт видит workflow-агентов плеча C
   (урок F-42 №1 закрыт t-093 — сверить, что фикс держит).
3. **Окно экзамена** помечается в notes ближайшей калибровки (~07-18,
   D-0075): синтетическая нагрузка, песочницы вне рабочих репо; тренды
   чеков 11/18 читать с исключением exam_release2.
4. **Разбор дельт №2−№1**:
   - A: дельта = дрейф модели/харнесса (A инфраструктуры не несёт) —
     базлайн, который вычитается из прочих плеч.
   - C: дельта = дрейф модели/харнесса + поведение промпта; релиза C
     не несёт.
   - B: дельта = регрессия релиза v0.1.0→v0.2.0 + эффект opus-дизайнера
     (КОНФАУНД, см. шапку). Чистая релизная регрессия из B в этом
     прогоне невыделима без внешней оценки эффекта дизайнера —
     частично декомпозируется данными пилота дизайнера (стоимость
     opus-спеки vs Fable-спеки, метрика 2 пилота). Отметить как
     ограничение прогона.
   - B против C — вклад инфраструктуры; B против A — полная экономия
     (обе — с поправкой на конфаунд B и базлайн-дрейф A).

## Runs log

Строку внести в PROCESS/DEPLOYMENT_ECONOMY_EXAM.md по итогам, с
пометками: «вариация: opus-спеки в B (слово оператора)», «конфаунд B:
релиз+дизайнер», «одиночный прогон».

## ИТОГИ ПРОГОНА №2 (заполнено 2026-07-14 ~21:30; приёмка Lead по пинованным ключам)

Все девять сессий отработали 2026-07-14 вечером — окно
18:53:46..19:14:00 UTC (замер min/max ts по cc_usage; модель Fable,
сверка транскриптами); разведка результатов — три параллельных scout
(t-111..t-113), ключи гонял Lead сам. Учёт: usage_report (cc_usage,
после фиксов F-42/F-43) + разбор по-плечево скриптом по той же базе;
скорость — min..max ts транскрипта.

| Плечо | T1 калькулятор | T2 click | T3 todo | $ итог | мин/задачу (ср.) | делег. доля $ |
|---|---|---|---|---|---|---|
| A | $0.49 / 0.9м / ПРИНЯТ* | $1.56 / 3.5м / ПРИНЯТ** | $0.73 / 1.7м / ПРИНЯТ | $2.78 | 2.0 | 0% |
| B | $5.54 / 14.7м / ПРИНЯТ | $4.96 / 13.8м / ПРИНЯТ | $5.08 / 13.1м / ПРИНЯТ | $15.58 | 13.9 | 28% (36/25/23) |
| C | $2.26 / 7.3м / ПРИНЯТ | $3.85 / 10.0м / ПРИНЯТ | $3.66 / 11.9м / ПРИНЯТ | $9.77 | 9.7 | 59% (58/59/59) |

\* A-T1: web-калькулятор (единственный файл calculator.html), принят
вождением UI Lead (правило UI-приёмки): 5÷0=«Ошибка», −5+3=−2,
0,5×3=1,5 — чисто; 2+2×2=8 (immediate-execution, скобок нет) против
пинованного ключа «=6», помеченного «интерпретация: спор решает
оператор». ПРИНЯТ вердиктом оператора 2026-07-14 (право протокола;
мотивировка: промежуточный результат считается и показывается в
строке истории — «4 × 2 =» перед ответом 8 — интерпретация не только
легитимна, но и наглядна для пользователя; прецедент №1: C-T1). Тестов нет — качественное отличие от B/C
(39 и 41 тестов). Уточняющий вопрос пользователю не задан (снова НИ
ОДНО плечо не спросило). Ограничение witness: пиксельный скрин
недоступен (таймаут захвата харнеса) — вождение зафиксировано
текстовым слоем дисплея по шагам.
\** A-T2: файла-отчёта нет (в t2 только клон) — результат остался в
чате. ПРИНЯТ 2026-07-14 ~21:50 по предъявке чата оператором
(прецедент №1): Lead сверил 5 НОВЫХ несущих утверждений по клону
(Group core.py:1643; спуск default_map по info_name :387-392; запрет
необязательных Argument у chain-группы :1750-1755; ВТОРОЙ путь
_check_nested_chain в CommandCollection.get_command :2155-2156;
resilient-резолв shell_completion._resolve_context :599 + :613) —
все точны. Отчёт содержит риски, которых нет у B/C: семантика
default_map/info_name при канонизации, снапшот-тесты потребителей
хелпа, lazy/плагинные группы вне self.commands; проектное решение
противоположно B/C (динамический резолв по cmd.aliases вместо
кэшированного reverse-map — с аргументом о рассинхронизации кэша
через публичный self.commands и Group(commands=[...])). Evidence
по-прежнему умерло бы с сессией без предъявки — урок №1 в силе.
\*** Эскалаций в журналах B — 0; rejected — 0 (поправка спеки t1
внесена приёмкой, не реджектом). A/C журналов не ведут (по построению).

### Вложенный замер вариации (opus-спеки в B)

Добавочная строка исполнена всеми тремя B-сессиями: по 1
дизайнер-диспатчу на задачу (t1: 7 развилок; t2: F1–F10; t3: a–g),
спеки приняты sandbox-Lead'ом с вердиктами по развилкам; в t1 Lead
поверх спеки внёс своё решение (без __init__.py). Спека B-T2 сохранена
файлом docs/SPEC_t-002_click_aliases.md — Lead корня сверил 2
file:line-утверждения по клону, обе точны.

### Несущие наблюдения (одиночный прогон; конфаунд B помечен)

1. **B подорожало вдвое против №1** ($15.58 против $7.79 валом;
   $5.19/принятую против $2.60) при том же качественном профиле
   приёмки (3/3, журнал, witness). Конфаунд не разделяем этим
   прогоном, но состав удорожания виден в базе: сайдчейн-расход B
   вырос с ~$2.2 до $4.38 (дизайнер-цепочка scout→designer→builder→
   critic на каждой ~10-минутной задаче), и main-сессия дольше
   держит контекст (13.9 против 10.3 мин/задачу). На задачах этого
   размера дизайнер-звено себя не окупает — согласуется с уроком №1
   («выигрыш растёт с размером задачи») и метрикой 2 пилота
   дизайнера (цена спеки).
2. **A стала быстрее и дешевле** ($0.93/принятую при 3/3, 2.0
   мин/задачу против 6.8 в №1) — дрейф базлайна, но НЕОДНОРОДНЫЙ
   (разложение по cc_usage, вопрос оператора «умеют ли чистые плечи
   накапливать опыт»): носителя опыта у A/C нет (свежие песочницы,
   чата №1 не существует, модель между сессиями не учится) —
   ускорение раскладывается на (а) A-T1 9.3→0.9 мин = ДРУГОЙ выбор,
   меньше работы (один HTML-файл без тестов и демо-прогона, 4 хода
   против парсера с батареей, 6 ходов) — та же нестабильность
   выборов, что у C в п.3; (б) A-T2 6.1→3.5 и A-T3 5.1→1.7 мин при
   сопоставимом объёме (T2 даже больше ходов: 15 против 12) —
   похоже на честную пропускную способность инференса/харнеса в
   окно прогона; (в) поверх всего n=1 («never trust one run»).
   Дельты «№2−№1» по B/C читать ЗА ВЫЧЕТОМ этого дрейфа.
3. **C воспроизвёл свой профиль** ($3.26/принятую против $3.01; 59%
   делегирования против 66%) — но КАЧЕСТВЕННО вырос: все три
   деливерабла — файлы (в №1 T2 остался в чате), T1 — CLI-парсер с
   41 тестом и верной приоритетностью (в №1 — кнопочный UI,
   отклонённый ключом). Одиночные прогоны C нестабильны по
   архитектурным выборам — воспроизводимость не доказана.
4. **Артефактная дисциплина осталась дифференциатором B**: приёмка
   B прошла целиком по файлам и журналам без единого вопроса;
   приёмка A снова упёрлась в чат (T2 принят только после предъявки
   оператором, T1 — тестов нет). Урок №1 подтверждён вторым
   прогоном. При этом СОДЕРЖАТЕЛЬНО A-T2 — самый глубокий разбор
   рисков прогона (default_map-семантика, снапшот-тесты
   потребителей, lazy-группы): чистая модель думает не хуже, она
   хуже ПЕРСИСТИРУЕТ.
5. **Интерпретационный класс воспроизвёлся**: снова ни одно плечо
   не задало уточняющий вопрос по «напиши мне калькулятор»; A выбрал
   immediate-execution (как C в №1). Ключ-пометка «спор решает
   оператор» отработала штатно.
6. **Скорость (метрика 4), дельты №2−№1 по плечам**: A 6.8 → 2.0
   мин/задачу (−71% — дрейф базлайна, п.2); C 13.3 → 9.7 (−27% —
   быстрее даже без вычета дрейфа, но медленнее A и B-№1); B 10.3 →
   13.9 (+35% — ЕДИНСТВЕННОЕ замедлившееся плечо на фоне общего
   ускорения; wall-clock съедает последовательная дизайнер-цепочка:
   scout → opus-спека → приёмка спеки → builder → critic на каждой
   задаче). За вычетом дрейфа базлайна честная скоростная цена
   вариации ещё выше, чем сырые +3.6 мин/задачу. Оркестрация и в №2
   скорости не даёт — теперь на обоих инфраструктурных плечах.
