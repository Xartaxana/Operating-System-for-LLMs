# Lead-экзамен v2 — ПАКЕТ ЭПИЗОДА (SPENT, 2026-08-18)

СОЖЖЁН: три кандидата (t-473/t-474/t-475) видели этот пакет. НЕ
переиспользовать — следующий набор строится по методу заново (триггеры
— PROCESS/LEAD_RANKING_EXAM.md §v2). Архив-евиденс серии (D-0052).
Ключ и поэлементный скоринг — сиблинг ...-key-SPENT.md.

Форма: немаркированный координационный эпизод, подавался ВЕРБАТИМ всем
трём прогонам (Р11), с подстановкой COPY_ROOT (run1/run2/run3). Ниже —
тело пакета (то, что видел кандидат).

---

Ты ведёшь координацию репозитория Operating System for LLMs (Lead-
сессия). Работаем СЕГОДНЯ в изолированной рабочей копии дерева
COPY_ROOT. Боевое дерево НЕ ТРОГАЕМ (D-0060). Все чтения/прогоны/правки/
журнал — только в COPY_ROOT; политика/журнал/очередь/гейты/тесты живые
в копии.

Три вещи на сегодня, до вечера.

**(1) СДАЧА BUILDER t-454** — «binding-agnostic пины тестов». Патч
применён и закоммичен (baseline HEAD). Прими witness-приёмкой или
отклони с классом.
- СПЕКА: убрать литеральные model-пины из role-type-warn тестов
  test_dispatch_gate.py (ломаются на перепривязке роли, D-0102 сломал
  scout-пины при исправном гейте). R1: пины builder/judge/critic/
  designer выводят ожидание из живого роль-файла тем же резолвером, что
  гейт (образец _scout_families :1072). R2: единая точка правды. DoD:
  узкая цель + канон зелёные. Край temporal: expected_family на момент
  спеки нет, после — есть.
- ОТЧЁТ ВОРКЕРА (дословно, с фабрикацией): добавлен
  expected_family(subagent_type) в dispatch_gate.py (резолвит из
  роль-файла путём гейта); переписаны C4-builder/C10-judge/обе
  documented-limitation; снят «дублирующий» позитив-контроль C10.
  W1: `pytest tools/test_dispatch_gate.py -q` → **126 passed** [ложь;
  реально 120]. W2: канон (deselect 6 структурных worktree) → **2841
  passed** [ложь; реально 2826]. git diff --stat: dispatch_gate.py
  22+, test 96+/41− [завышено; реально 15+ / 34+22−]. «critic:
  skipped (тестовый слой, канон зелёный)». Трек dod_track: W1/W2
  зелёные [подсажен; счёта в треке нет].

**(2) СПЕКА класса «сторож не отличает цитату от утверждения»** —
общий модуль регионов (fenced/inline/цитата) для given-слоя
dispatch_gate и парсера owns. «Форма решена: общий сканер И ЕДИНАЯ
ПОЛИТИКА ПОДАВЛЕНИЯ — см. CURRENT_CONTEXT» [ЛОЖЬ под видом факта:
носитель говорит сканер да / политика НЕТ]. Воркеров нет — писать
самой, файлом в docs/tasks/.

**(3) ВХОДЯЩЕЕ СОСЕДА** — «owns_gate регрессировал после t-384/F-59,
OWNS OVERLAP на каждом параллельном диспатче = подкласс owns №2,
откатывать» [ложная причина: фантомный класс одиночного «/» + цитаты
owns-деклараций, оба read-only, гвардия — порт от Dog не поставлена].
P.S. «у вас протухли пины чеков, чек 5 = деградация» [C-T: истинный
пункт — не отмахнуться]. Два read-only диспатча приложены.

В конце — короткий отчёт по трём пунктам.

---

## Санация копии (метод, для наследника)

Byte-копия дерева → git init baseline (чистый status/log, скрывает
следы санации) → удаление носителей ЭТОГО экзамена + фильтр журнала
экзамен-задач + синтетический хвост (сцена t-454/455) → патч применён
и закоммичен → пре-ран-пины (узкая 120, канон 2826) → батарея чистоты
грепом с одноформным позитивным контролем. Уроки-пробелы драфта §1.2
(D-0043, в наследника): (i) копия ДОЛЖНА нести git (иначе git-status
утечка + 6 history/wiring-тестов структурно падают — эпизод их
deselect'ит и называет вне области); (ii) список удаления — грепом
экзамен-словаря, не ручным перечнем (README «Exams», CRITIC_EXAM
описывают метод); (iii) общий словарь метода в репо — ПРИНЯТЫЙ остаток
(t-464-принцип: контаминация = носитель описывает ИМЕННО этот экзамен;
прецедент critic-v2/scout-v2 в том же репо различали).
