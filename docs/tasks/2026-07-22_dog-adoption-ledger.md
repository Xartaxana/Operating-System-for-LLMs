# Adoption Ledger — деплой D:\Dog (первый ledger по норме D-0090)

Узел B2 валидации D-0090 (DAG 2026-07-22_onboarding-validation.md).
Составлен Lead по принятым инвентаризациям t-268 (профиль/функции/
риски) и t-269 (помеханизменная таблица 18). Режим по карте
инвентаризации: НЕ пустая → сопоставление; профиль ветки —
АПГРЕЙД-онбординг существующего деплоя (фреймворк установлен
c45f997, отставание = всё, начеканенное штабом после снимка).
Статусы: adopt / native-equivalent / deferred(триггер) / rejected.
Исполнение adopt-набора — узлы B3/B4 (план предъявляется оператору;
касание разрешено словом 07-22).

## Уже стоит (установка c45f997) — adopt, исполнено

journal_validator (pre-commit); mechanism_gate (commit-msg, осевой
блок); tier-верификация (session_context, SessionStart, BOOT-бюджет);
экзаменационные наборы (scout/critic/lead); калибровочные протоколы
(+ calibrated-события); session-handoff / boot-report; usage_report
(учёт cc_usage/requests); SIBLING_MAP (СВОЯ, 7 осей: 1≈наша ось 1 по
форме — не по членству [наша ось 7 осознанно исключает
пользовательские деплои], 2≈наша ось 8, 3–7 доменные).

## Дельта штаба после снимка — решения ledger

| Механизм штаба | Статус | Основание / триггер |
|---|---|---|
| Обновление ядра политики (D-0084 EN-ядро; R11a вопросы-вверх; манифест D-0073; батчинг D-0081; R12 каденция) | **adopt** (одним «policy refresh» батчем) | текст политики, предпосылок не требует; их доменные правила не трогаются (принцип 4 D-0090) |
| wiring-integrity чек | **adopt** | дёшев; предпосылка (SessionStart-хук) стоит; прецедент AO3 t-257 |
| critic-verdict машиночитаемая схема | **adopt** | critic-агент у Dog есть; прецедент AO3 t-259 |
| escape-allowlist | **adopt адаптированно** | у Dog есть свой DECISIONS.md (предпосылка закрыта); кросс-репо форма AO3 — прецедент для концессий, унаследованных от штабных решений |
| journal_echo (on-write валидация, witness×dod-сверка, ts-drift) | **deferred** | триггер: фикс defect_found t-263 (ts-drift замер врёт ~+31 мин) — дефектный детектор не экспортируется; после фикса → adopt (журнал Dog юн, on-write защита дешева) |
| dod_track / dod_gate | **deferred** | Rule #1: код-объём Dog мал (15 коммитов/2 мес; тесты только инфраструктура); триггер: регулярные builder-циклы кода |
| main_gate | **deferred** | тот же Rule #1 + известный doc-only-баг (разбор t-265; баг не экспортируется); триггер: фикс + рост код-потока |
| dispatch_gate / critic_snapshot | **deferred** | гейт-свита той же группы; триггер: builder-объём |
| R13 leaf routing / judge-приёмка | **native-equivalent (вики-контент) + deferred (код-листья)** | приёмку вики-контента нативно несёт их LLM-lint конвейер (функциональный аналог класса AO3 D1/F1); для код-листьев предпосылок нет (шлюз, судейский промпт, калибровка, $/замер) — без сетки basis judge = самосертификация F-22; BASIS_VALUES их журнала не расширять; триггер пересмотра (форма AO3): evidence дороговизны critic-входа на листах |

Rejected-строк нет: ни один механизм штаба не вреден Dog по
Rule #1 — отклонения выразились статусом deferred с триггерами.

## Механизмы Dog сверх номенклатуры штаба (фиксация соответствий)

pi_run_guard, preflight_quota, calibration_counts, savings_report —
штабные инструменты, приехавшие со снимком (не доменные);
доменные без штабного аналога: вики-конвейер (AGENTS.md, 4
воркфлоу, LLM-ownership контента), telegram-контур, command-center,
оси 3–7 их карты. Штаб их НЕ поглощает (принцип: носители хоста не
мигрируются); их дрейф — предмет ИХ карты и калибровки.

## Аудит ledger

До кит-батча (мораторий D-0074) детектор дрейфа этого ledger —
калибровка №4+ штаба (чек 30-окно уже несёт t-268/t-269) и
следующий порт-батч Dog; после кита — кит-копия калибровочного
протокола хоста (D-0090 (в)).

## План B3 (предъявляется оператору перед касанием)

Первый батч установки = adopt-набор ledger: policy refresh (ядро,
R11a/D-0073/D-0081) + wiring-check + critic-verdict схема +
escape-allowlist (адаптированный сид). Всё — warn-first, их носители
не мигрируются, их доменные оси/воркфлоу не трогаются. Слой учёта
у Dog уже стоит (usage_report) — measure-first выполнен установкой
c45f997; трение установки замеряется по V3, находки — в FINDINGS.
