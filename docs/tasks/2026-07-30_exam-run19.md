# Прогон №19 — малый экзамен пост-релизного снимка (2026-07-30)

Слово оператора 07-30: «делай аудит, потом малый экзамен, потом
boot-diet» — исполнение переноса калибровки №5 (триггер «релизный
снимок кита» выстрелил релизом v0.6.0 07-30) и чека 14(е) (двойной
перенос малого экзамена признан калибровкой №5).

## Конструкция (форма kernel-серии №15–№17)

- Плечо: Bkn4 соло, Sonnet-координатор, набор №1 (пин 07-13, без
  замен), parallel=3, headless-протез эскалации fable-прокси (суффикс
  манифеста дословно как №15–№17).
- Кит: exam_fullgates_kit, синк t-347 к штабному a987738 (эпоха
  v0.6.0: journal_validator с designer/QUEUED_TO_LEAD_AGENTS/
  judge-scope, dispatch_gate с word-boundary + given-path warn
  {0,300}, critic.md правило 16 + critic_verdict_check, designer.md
  в корпусе) с ЗАПИСАННЫМИ адаптациями (kit_sources.json секция
  adapted: минимальный pre-commit, без unsafe_completions/per-agent,
  hygiene_gate/journal_echo — эпохи отложены отдельной задачей,
  settings.json без штабной warn-first обкатки). Сопоставимость с
  №15–№17 по hygiene/echo-эпохам сохранена сознательно.
- template_ref — коммит кита после приёмки t-347 (в манифесте).

## Предрегистрация (ДО прогона; ключи пинуются этим коммитом)

- k1–k5 — ДОСЛОВНО как №15 (пин-коммит 6064f59, методика грейдинга
  1-в-1 с №15/№16/№17: оси A/B/C/D/E, мутационный убой, скаляр, F).
- k6 — replay-часть как №17 (пин ec9dcd6): заявления прокси против
  cc_usage-замера; внутриклеточного эха нет (доставка D-0083 в
  редизайне — состояние не изменилось).
- k2-ОЖИДАНИЕ: ~0 дефектов журналов (обе доработки №17 живы в
  клетках; validator текущей эпохи с designer-матрицей).
- НОВОЕ k7 (первые клетки с given-path warn-слоем и word-boundary
  маркерами dispatch_gate): ложных БЛОКОВ гейтов 0 (инвариант всей
  серии); given-path WARN'ы клеток СЧИТАЮТСЯ и разбираются
  (материал обкатки слоя t-343 — FP-классы в счёт CURRENT_CONTEXT);
  замедлений хуков не наблюдается (DoS-граница {0,300} жива —
  клетка с патологическим промптом не ожидается, ключ
  информационный).
- НОВОЕ k8 (первый корпус с designer.md): требования ИСПОЛЬЗОВАТЬ
  designer нет (клетки сами решают по политике); если клетка
  диспатчит designer — приёмка обязана пройти матрицу новой эпохи
  (нарушений матрицы 0); появление designer-диспатчей — наблюдение
  в разбор, не ключ качества.
- Планка: гейтовой планки у №19 нет — точка калибровочного класса,
  медиана малых копится (три точки ядра 0.94/0.90/0.92, медиана
  0.92); итог — строка Runs log + вход калибровки №6.

## Статус

- [x] Синк кита t-347 (вопросы Q1–Q5 решены Lead: Q1 не портируем,
  Q2 правило 16 + чекер в маппинг / pre-commit минимальный, Q3
  вариант (i), Q4/Q5 отложены именованной очередью)
- [x] Приёмка t-347 (witness: 271 passed + kit_diff_check exit 0,
  контрольный перегон Lead сошёлся) + коммит кита 613fd06 → манифест
- [ ] Прогон (exam_runner all)
- [ ] Грейдинг (методика 1-в-1, лог polygon/grading/)
- [ ] Вердикт Lead: строка Runs log, медиана, счёт k7-обкатки
