# 2026-08-09 — Промоция гигиены: warn → блок

Триггер: слово оператора «делай батч промоции гигиены» (после прямого
вопроса «почему его оставляем на калибровку? почему нельзя закончить
сейчас?», снявшего отсрочку). Носитель по D-0080. Журнал: t-370, t-372…
Класс: последний живой пункт находки (7) калибровки №6.

## Почему отсрочка была снята

Записанное основание отсрочки — «нужен замер окна при неизменном
скоупе, иначе нечего сравнивать». Инструмента замера не существовало;
батч раздела 1 его построил (разбивка `permission_audit` по классам
гигиены). Основание растворилось, а ссылка на отсрочку продолжала
повторяться по инерции — вопрос оператора это вскрыл.

Дожидаться объёма бессмысленно: решение упирается не в объём, а в
законность использования по каждому классу. За всю историю 3275 команд
с классом на 9799 вызовов; за окно неизменного скоупа — 9 на 84.
Обе цифры говорят одно: паттерн жив, warn его не останавливает.

## Что вскрылось перед решением — и изменило его

**Классификатор путает код с данными.** Проба Lead: оба попадания
блокирующего класса «журнал мимо Edit/Write» за сутки оказались
командами, которыми воркеры ТЕСТИРОВАЛИ классификатор — паттерны лежали
данными внутри тела `python -c` и heredoc. При живом блоке была бы
заблокирована проверочная работа критика и билдера.

Следствия: (1) маскировка вложенного — предусловие блока, не улучшение;
(2) исторические числа замера загрязнены на неизвестную долю.

**Два края, найденные драфтом:**
- п.2 гигиены ПРЕДПИСЫВАЕТ поднимать прокси из `gateway`, п.3 запрещает
  `cd`-префикс — блок запретил бы документированную форму. Решение:
  узкое исключение для `gateway`; отдельным вопросом в очередь — можно
  ли поднимать прокси без `cd` вообще.
- маскировка ОТКРЫВАЕТ дыру в живом блоке журнала (реальная запись
  внутри heredoc перестанет ловиться). Блок при write-намерении есть её
  КОМПЕНСАТОР, а не дополнение — оба обязаны встать одним ходом.

## Узлы

| узел | что | tier | статус |
|---|---|---|---|
| N1 | драфт спеки по интент-брифу Lead; 14 развилок возвращены списком | designer (opus) | DONE (t-370; нашёл оба края выше) |
| N2 | приёмка драфта + решения по развилкам (две рекомендации отклонены с обоснованием) | Lead (opus) | DONE |
| N3 | реализация — ПЯТЬ пересдач с эволюцией конструкции: маскировка+блоки+write-намерение → фиксы шести правок → ПРИНЦИП ОПРЕДЕЛЁННОСТИ (маска кавычек + << + якорь первого statement; парсер, список интерпретаторов и write-намерение удалены) | builder (sonnet) | DONE (t-372; узкий 277, канон 2255) |
| N4 | critic-гейты — ТРИ входа: 4 блокера и 27.1% потока под ложный блок → новый блокер в фиксе производительности + числа, изменившие вопрос (маскировка спасала 6/1126) → замер моей конструкции против альтернативы, альтернатива критика принята | critic (opus) | DONE (t-375/t-376/t-377; 4 дифференциала тождества, все 0 diffs) |
| N5 | включение выключателя — ход Lead (D-0069) | Lead (opus) | DONE (08-09 23:14) |
| N6 | ПРОБА ЖИВОСТИ из семи шагов | Lead (opus) | DONE (два живых DENY — cd-в-корень и newline-обход; канон исполнился; данные с warn; врепер тихий; путь отката доказан самим включением) |
| N6х | хвост включения: 13 пинов V4 перевёрнуты на дефолт диска; два subprocess-пина завязаны на диск явно — откат константы не пройдёт молча | builder (sonnet) | DONE (t-378; узкий 277, канон 2255 при True) |
| N7 | пост-промоционный базлайн живым инструментом | Lead (opus) | DONE (10187 вызовов; 2>&1 1195, cd 2097, python -c 885, журнал 80; для №7 — динамика deny-попыток после 08-09) |
| N8 | носители: RULE_COVERAGE (режим WARN→ЧАСТИЧНЫЙ БЛОК), SIBLING_MAP ось 11 (+строка hygiene_gate, долг B4), CURRENT_CONTEXT, чеканка с осевым блоком | Lead (opus) | DONE (этим коммитом) |

Зависимости: N1 → N2 → N3 → N4 → N5 → N6 → N7 → N8. Порядок N5→N6
обязателен: проба живости имеет смысл только на включённом гейте.

## Владение путями

- N3: `tools/hygiene_gate.py`, `tools/test_hygiene_gate.py`,
  `tools/test_permission_audit.py`.
- N5: `tools/hygiene_gate.py` (одна строка — константа).
- N8: `docs/RULE_COVERAGE.md`, `PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md`,
  `docs/FINDINGS.md`, `CURRENT_CONTEXT.md`.
- Вне владения: `toolkit/**` (мораторий D-0074), `.claude/settings.json`,
  `.claude/agents/**` (строка в builder.md — отдельной очередью).

## Рамки

Non-goal: общий markdown-модуль регионов для given-path и парсера owns —
отдельная задача очереди (класс тот же, поверхность другая). Non-goal:
ось 13 карты — условно, после того модуля. Non-goal: правка роль-файлов.

Принятая и записанная дыра: после маскировки реальная запись в журнал
внутри тела heredoc не ловится классом (г); её ловит только блок при
write-намерении. Второго слоя маскировки внутри python-кода не будет —
это парсинг Python, чего гейт делать не должен.
