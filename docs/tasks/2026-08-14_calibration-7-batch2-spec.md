> Восстановленный из транскрипта драфт спеки. Задрафчен воркером-designer как часть задачи t-438 (батч 2 калибровки №7), принят Lead'ом 2026-08-14.
> Источник: `C:\Users\user\.claude\projects\D--Improving-AI-Operating-System-for-LLMs\c199b28e-6a01-4d87-aaf2-478c18a5d223\subagents\agent-ae935cfcf4e40af8e.jsonl`, строка 105 (последнее assistant-сообщение агента).
> НЕ ИСПОЛНЕН: принятая, но не персистированная спека стоит в очереди ремедиации калибровки №8 узлом F — носитель хода `docs/tasks/2026-08-20_calibration-8-remediation.md`.
> Текст ниже приведён БЕЗ правок — дословное восстановление из транскрипта, разэкранирован только JSON. Устаревшие места (номера строк, ссылки на состояние файлов на 08-14, упоминания уже закрытых пунктов) читаются как ИСТОРИЧЕСКИЕ, а не как инструкция к слепому применению.

---

# ДРАФТ СПЕКИ (batch «калибровка №7: точность яруса, резерв, гигиена-блок, саморевизия протокола»)

Форма: R11 + пятипунктовый чеклист D-0096. Решения по развилкам — за Lead; ниже они возвращены списком, не закрыты.

---

## 0. КОНТЕКСТ-МАНИФЕСТ

### Дано (перечислением; всё прочитано мной в этот ход, пути абсолютные)

**Привязка и ярусы**
- `D:\Improving_AI\Operating-System-for-LLMs\delegation.config.yaml` — `roles.lead.subscription.model: claude-opus-5`; `reserve: claude-fable-5`; `critic: claude-opus-5`; `designer: claude-opus-5`; `builder: claude-sonnet-5`; `scout: claude-haiku-4-5`. Шапка называет трёх читателей (mechanism_gate / journal_validator / session_context) и правило «файла нет → все трое дефолтятся в fable».
- `D:\Improving_AI\Operating-System-for-LLMs\tools\mechanism_gate.py` — `LEAD_FAMILIES = ("fable","opus","sonnet","haiku")`; `lead_family()` — матч ПОДСТРОКОЙ без границ слова; `tier_declared_ok()` ветки 1–3 (точный id / вхождение семейства / семейство строго выше) + ветка 4 (лестница `ROLE_RANKS`, `_resolve_ladder_rank`); `MECHANISM_PREFIXES` (см. ниже).
- `D:\Improving_AI\Operating-System-for-LLMs\tools\session_context.py` — `_MODEL_TIER_SUBSTRINGS` (~стр. 169–176), `model_tier()` (~648–688), `model_line()` (~706–746), `open_degradation_window()` (~308–323).
- `D:\Improving_AI\Operating-System-for-LLMs\tools\journal_validator.py` — `EVENTS` (~269–273), `MODEL_REQUIRED_EVENTS`, `TIER_ORDER`, `AGENT_TIER`, ветка а2/3b матрицы D-0058 (~634–652).

**Гигиена**
- `D:\Improving_AI\Operating-System-for-LLMs\tools\hygiene_gate.py` — `V5_ENABLED = True` (стр. 484, включён ходом Lead 2026-08-09); `_collect_v5_signals()` (~1197–1241) — ЕДИНАЯ точка сигналов, ключи `journal/cd/cd_to_repo_root/redirect/redirect_certain/pyc`; `_decide_v5()` (~1244–1341) — фиксированный порядок deny «журнал → cd → 2>&1»; `_is_python_dash_c()` (~706) на `PY_DASH_C_RE=\bpython\s+-c\b` и `PY_HEREDOC_RE=\bpython\s+-\s*<<` (литерал `python`, НЕ `python3`/`py`); `_mask_quoted_segments()` (~796–811); принцип «deny требует определённости, неоднозначный вход деградирует в warn» (~954–995); `decide()` fail-open с видимым маркером (~1350–1395).
- `D:\Improving_AI\Operating-System-for-LLMs\tools\permission_audit.py` — `classify_hygiene()` (~509–578) читает `_collect_v5_signals` ПО ИМЕНАМ ключей; `HYGIENE_CLASS_LABELS` (4 метки, сравниваются с baseline чека 25); замер deny/warn-разбивки по корпусу сделан ОТДЕЛЬНЫМ скриптом (докстринг ~539–543, прецедент пересдачи 4), постоянного кода для него в репо нет.
- `D:\Improving_AI\Operating-System-for-LLMs\.claude\settings.json` — `PreToolUse / Bash|PowerShell → python tools/hygiene_gate.py` (хук живой, читает рабочее дерево новым процессом на каждый вызов).

**Политика и протокол**
- `D:\Improving_AI\Operating-System-for-LLMs\CLAUDE.md` — секция «Journal — logs/routing-log.jsonl»: перечень событий + таблица форм событий.
- `D:\Improving_AI\Operating-System-for-LLMs\docs\POLICY_FULL.md` — перечень событий (~442–450), «Роль ≠ ярус» (~452–507), «Деградация Lead» (~509–542). **Твин оси 4 подтверждён чтением: да, пункт 2 обязан править POLICY_FULL тем же коммитом — там живут И перечень событий, И раздел, симметричный которому вводится подъём на резерв.**
- `D:\Improving_AI\Operating-System-for-LLMs\PROCESS\WEEKLY_CALIBRATION_PROTOCOL.md` — чек 0 (стр. 17–24), чек 5 (97–111), чек 11 (размер вывода SessionStart-хука, 196–202), чек 12 (203–…, ТРОГАЕТСЯ ПАРАЛЛЕЛЬНЫМ t-437), чек 13(з) (стр. 301–304), чек 18 + 18(д) (443–499), чек 25 (643–700), чек 26 (702–…), секция «Завершение прогона» (1021–1034). **Последний номер чека — 32; новый чек получит номер 33.**
- `D:\Improving_AI\Operating-System-for-LLMs\docs\SIBLING_MAP.md` — оси 1..12 (осевой блок коммита обязан перечислить ДВЕНАДЦАТЬ осей); ось 4 «Парные документы» (пара `CLAUDE.md ↔ docs/POLICY_FULL.md`, пара «носители правил + tools/-гейты ↔ docs/RULE_COVERAGE.md» с правилом «новый механизм или смена РЕЖИМА срабатывания — строка карты покрытия ТЕМ ЖЕ коммитом»); ось 12 «Носители привязки модели роли».
- `D:\Improving_AI\Operating-System-for-LLMs\docs\RULE_COVERAGE.md` — раздел 4 «Командная гигиена», строка 82 (описывает V5 и явно говорит «WARN: … python -c/heredoc без изменений»); раздел 6 «Ярусы и деградация».
- `D:\Improving_AI\Operating-System-for-LLMs\.claude\skills\session-handoff\SKILL.md` п.2 — «нет `lead_degraded` без `lead_restored`».
- `D:\Improving_AI\Operating-System-for-LLMs\tools\calibration_counts.py` — секция 9 «Пары деградации lead_degraded/lead_restored (чек 5)» (стр. 443–468, вывод секции — 609).
- `D:\Improving_AI\Operating-System-for-LLMs\logs\routing-log.jsonl` — 6 событий `calibrated` (источник строки (2) пункта 4).

**Тестовая обвязка (все файлы существуют, проверено Glob)**: `tools\test_mechanism_gate.py`, `tools\test_session_context.py`, `tools\test_journal_validator.py`, `tools\test_hygiene_gate.py`, `tools\test_permission_audit.py`, `tools\test_calibration_counts.py`, `tools\test_parity_check.py`.

**Живая параллель (t-437)**: `docs\SIBLING_MAP.md`, `tools\test_sibling_map_liveness.py` (уже присутствует в рабочем дереве — чужое незакоммиченное состояние), `tools\conftest.py`, `tools\test_spend_report.py`, одна строка чека 12 протокола. Эти пути — НЕ owns этого батча; протокол правится ПОСЛЕ схождения t-437 и **перечитывается заново**: якорь правки — заголовок чека/секции, НИКОГДА не номер строки из этой спеки.

### owns (АБСОЛЮТНЫЕ пути записи)
`D:\Improving_AI\Operating-System-for-LLMs\tools\mechanism_gate.py`, `D:\Improving_AI\Operating-System-for-LLMs\tools\session_context.py`, `D:\Improving_AI\Operating-System-for-LLMs\tools\journal_validator.py`, `D:\Improving_AI\Operating-System-for-LLMs\tools\hygiene_gate.py`, `D:\Improving_AI\Operating-System-for-LLMs\tools\calibration_counts.py`, `D:\Improving_AI\Operating-System-for-LLMs\tools\test_mechanism_gate.py`, `D:\Improving_AI\Operating-System-for-LLMs\tools\test_session_context.py`, `D:\Improving_AI\Operating-System-for-LLMs\tools\test_journal_validator.py`, `D:\Improving_AI\Operating-System-for-LLMs\tools\test_hygiene_gate.py`, `D:\Improving_AI\Operating-System-for-LLMs\tools\test_permission_audit.py`, `D:\Improving_AI\Operating-System-for-LLMs\tools\test_calibration_counts.py`, `D:\Improving_AI\Operating-System-for-LLMs\CLAUDE.md`, `D:\Improving_AI\Operating-System-for-LLMs\docs\POLICY_FULL.md`, `D:\Improving_AI\Operating-System-for-LLMs\docs\RULE_COVERAGE.md`, `D:\Improving_AI\Operating-System-for-LLMs\PROCESS\WEEKLY_CALIBRATION_PROTOCOL.md`, `D:\Improving_AI\Operating-System-for-LLMs\.claude\skills\session-handoff\SKILL.md` (последний — только если развилка Р11 решена «да»)

### non-goals
- `toolkit\**` — мораторий D-0074; вся дельта уходит в порт-очередь строкой отчёта. Ожидаемый `hq-drift` в `tools/parity_check.py --check` по твинам `toolkit/tools/{journal_validator,session_context,hygiene_gate}.py` — ЛЕГАЛЕН, синк не делать.
- `D:\AO3_tests` — не трогать (кросс-деплой пункт — решение Lead, носитель CURRENT_CONTEXT.md).
- Журнал `logs\routing-log.jsonl` — НЕ писать (ведёт Lead); прошлые события не переписывать (D-0089).
- Не коммитить; не переключать выключатель пункта 3 (D-0069 — переключает Lead при приёмке).
- `tools\dispatch_gate.py` (`role_type_warn`, ось 12) — вне объёма; замеченные пробелы только в отчёт.
- Не расширять токен-набор класса (в) на `python3`/`py`/`pwsh`/`node` (развилка Р16).

### handoff
Отчёт билдера содержит: (1) дифф-сводку по пунктам; (2) витнесс канонического прогона дословно; (3) ПОЛНЫЙ исходник разового корпус-скрипта пункта 3 + его вывод + разбор каждого перевёрнутого warn→deny случая; (4) ГОТОВЫЙ ТЕКСТ трёх строк саморевизии (пункт 4) для вставки Lead'ом в notes события `calibrated` — билдер журнал не пишет; (5) перечисление сиблингов с вердиктом на каждого (D-0100); (6) список в порт-очередь (kit-твины) и список замеченных, но не чиненных аналогов; (7) числа размера вывода SessionStart-хука до/после (чек 11).

---

## 1. ПУНКТ 1 — точное поколение модели против привязки

**Цель.** Понижение поколения ВНУТРИ семейства (`claude-opus-4-8` при привязке `claude-opus-5`) перестаёт читаться как ярус Lead. Механика дыры подтверждена чтением: `lead_family()` матчит ПОДСТРОКОЙ, поэтому `session_context.model_tier("claude-opus-4-8")` даёт `Lead(bound)`, а `mechanism_gate.tier_declared_ok("claude-opus-4-8", "claude-opus-5")` даёт True веткой «вхождение семейства».

**Критерии приёмки**
1. **К1.1 — одна реализация.** В `tools/mechanism_gate.py` появляется ОДНА публичная функция сравнения декларации с привязкой, возвращающая ТРИ значения: `EXACT` (id совпал), `SAME_FAMILY_OTHER_ID` (семейство совпало, id — нет), `NO_FAMILY` (семейство не резолвится хотя бы с одной стороны). Второй реализации сравнения в `session_context.py`/`journal_validator.py` нет — они импортируют (контроль: число вызовов `lead_family(` вне `mechanism_gate.py` не растёт).
2. **К1.2 — регресс-пины легальных форм.** Каждый отдельным тестом: `tier: claude-opus-5` (точное) → как сегодня; `tier: opus` (голое семейство) → как сегодня; `claude-fable-5` → `top-reserve`; `claude-sonnet-5` → `builder-tier`; `claude-haiku-4-5` → `scout-tier`; чужая модель (`gemini-2.5-pro`) → `unknown`; конфига нет → статические метки. Ни одно тело существующего теста не правится (только аддитивные).
3. **К1.3 — видимость расхождения.** Для `SAME_FAMILY_OTHER_ID` носитель, выбранный развилкой Р1, выдаёт ОТЛИЧНЫЙ от сегодняшнего сигнал: метка `session_context` перестаёт быть `Lead(bound)`, и/или `mechanism_gate` печатает строку расхождения. Форма сигнала (отказ/warn) — развилка Р2; текст метки — Р5. Тест на КАЖДЫЙ выбранный носитель.
4. **К1.4 — границы подстроки.** Тесты: `claude-opus-5` (на границе — точное совпадение); `claude-opus-5-1` и `claude-opus-50` (за границей — тот же префикс, другой id, обязаны классифицироваться как `SAME_FAMILY_OTHER_ID`, а не как точное); `myopus-5` (за границей — подстрока «opus» есть, но это не член семейства: поведение ОБЯЗАНО быть названо в докстринге и запинено тестом, каким бы оно ни было).
5. **К1.5 — пустой/отсутствующий вход.** `model_id` пустой/из пробелов/None → сегодняшняя ветка «MODEL: not provided…» без изменений; `config_text=None`, битый YAML, отсутствующий файл, недоступный импорт `mechanism_gate` → fail-open в сегодняшнее поведение, без новой ветки отказа. Тест на каждый вход.
6. **К1.6 — чек 11.** Размер вывода `session_context.py` замерен ДО и ПОСЛЕ (форма чека 11: `echo '{}' | python tools/session_context.py`, байты и строки); оба числа в отчёте. Рост более чем на одну строку — повод вернуть форму метки на развилку.

**Края поведения**
- *Временной*: правка живая в момент СОХРАНЕНИЯ (`session_context.py` — SessionStart-хук). Мир «до»: метка `Lead(bound)` на любой opus-модели. Мир «после»: метка расходится. Артефакт, которого не было, — новая строковая метка; её создаёт ЭТА правка, момент — сохранение файла.
- *Позиционный (инвариант, не координата)*: если строгость ставится в `mechanism_gate.tier_declared_ok`, она НЕ МОЖЕТ жить внутри веток 1–4 — их задокументированный инвариант «лестница строго РАСШИРЯЕТ множество принимаемых деклараций, никогда не сужая». Легальные позиции: отдельный сигнал ДО/ПОСЛЕ функции, не меняющий её булев контракт. Инвариант, который позиция обязана сохранить: ни одна сегодня-принимаемая декларация не становится отказом иначе как через явно выбранную развилкой Р2 ветку.
- *Позиционный (journal_validator)*: если развилка Р1 решится «трогать и валидатор», новая проверка обязана стоять ПОСЛЕ энум-гейта формы `by` и ПОСЛЕ безусловного floor'а «ниже sonnet» — инвариант: floor остаётся безусловным, ветка `judge` остаётся первой, и новая проверка способна только ДОБАВИТЬ отказ, никогда не легализовать сегодня-нелегальное.
- *Генеративный*: новая функция + новая метка/строка — носители: докстринг модуля, тесты, строка `docs/RULE_COVERAGE.md` (смена режима сторожа), вердикт по оси 12 SIBLING_MAP («носители привязки модели роли» — карта сегодня фиксирует отсутствие периодической сверки как ОТКРЫТЫЙ ПРОБЕЛ; правка обязана либо закрыть эту строку, либо оставить с явным вердиктом). Механизмный путь: да (`tools/mechanism_gate.py`, `tools/session_context.py`, `tools/journal_validator.py` — все поимённо в `MECHANISM_PREFIXES`) ⇒ коммит несёт осевой блок по 12 осям и строку `tier:`. Parity-пара: kit-твины — в порт-очередь.
- *Конфликт требований*: «сделать понижение видимым» против «не ломать легальные случаи». Разрешается тем, что сигнал по умолчанию НЕ блокирующий (рекомендация Р2), а направление сравнения поколений НЕ вычисляется (Р4) — метка нейтральна: «id не равен привязке», без утверждения «ниже».

---

## 2. ПУНКТ 2 — маркер использования РЕЗЕРВНОГО яруса

**Цель.** Пара событий, симметричная деградации: подъём выше привязки перестаёт быть невидимым для чека 5.

**Критерии приёмки**
1. **К2.1 — ядро политики.** `CLAUDE.md`, журнальная секция: оба имени входят в перечень событий; если события несут типизированные поля (развилка Р10) — строка в таблице форм событий. Текст называет: триггер (слово оператора), что обязано быть в notes (причина + указание на слово оператора), обязанность пары (Р7), момент входа (Р8).
2. **К2.2 — твин оси 4 тем же коммитом.** `docs/POLICY_FULL.md`: (а) перечень событий приведён в соответствие; (б) добавлен раздел, симметричный «Деградация Lead», с обоснованием (D-0099: резерв ВЫШЕ привязки, вызов — слово оператора; замер окна 699 ходов/$489.83 против 327/$99.49 — как rationale, не как норма). Инвариант пары соблюдён: НОРМА только в ядре, RATIONALE только в твине.
3. **К2.3 — машинный слой.** `tools/journal_validator.py`: `EVENTS` пополнен обоими именами; базовые поля проверяются как у любого события; выбор Р10 отражён в `MODEL_REQUIRED_EVENTS`. Тесты: валидная строка проходит; ОПЕЧАТКА в имени (`reserve_engage`) падает с enum-сообщением (негативный контроль); строка без notes падает; `task_id` не требуется.
4. **К2.4 — чек 5.** В `PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md` чек 5 получает резервную половину: Lead-активность окна на модели ВЫШЕ привязки обязана быть покрыта окном `reserve_engaged`; открытое окно на границе сессии; notes без указания на слово оператора; **пустое окно отмечается явной строкой** («резервных окон нет»). Оговорка про непокрытые прошлые окна — по решению Р9.
5. **К2.5 — подметание сиблингов ПЕРЕЧИСЛЕНИЕМ (D-0100), вердикт на каждого**: `tools/calibration_counts.py` секция 9; `tools/session_context.py` `open_degradation_window`; `.claude/skills/session-handoff/SKILL.md` п.2; `docs/RULE_COVERAGE.md` раздел 6; `DECISIONS.md`/`docs/DECISIONS_FULL.md`; `docs/FINDINGS.md`; `toolkit/**`; AO3-деплой. Формат вердикта: применено / не применимо — почему / в очередь — с указателем. Проза «соседи проверены» приёмку не проходит.
6. **К2.6 — журнал не пишется.** Билдер не добавляет ни одной строки в `logs/routing-log.jsonl` и не создаёт ретро-событий; первое реальное употребление события — ход Lead после коммита.

**Края поведения**
- *Временной (обязателен)*: до правки валидатора строка `reserve_engaged` НЕВАЛИДНА — pre-commit отказывает сообщением enum'а, `journal_echo` предупреждает в момент записи. Спека предписывает: правка `EVENTS` и правка `CLAUDE.md`/`POLICY_FULL.md` — ОДИН И ТОТ ЖЕ коммит; легальное употребление начинается только после него. Мир «до» и мир «после» описаны, момент создания артефакта назван.
- *Пустые/отсутствующие входы (каждый обязан иметь названное ожидание)*: `engaged` без `released` до конца журнала; `released` без предшествующего `engaged`; два `engaged` подряд; `engaged` внутри открытого окна деградации. Ожидание по умолчанию (если Р7/Р11 не решены иначе): валидатор пар НЕ считает — это дословно сегодняшняя семантика для `lead_degraded`/`lead_restored`; судит чек 5.
- *Конфликт требований*: `CLAUDE.md` требует, чтобы деградация, переживающая границу сессии, была ПОСЛЕДНИМ событием журнала. Механически перенесённая симметрия сделала бы то же требование к открытому резервному окну — и вошла бы в конфликт с обычным хвостом журнала (`accepted`/`calibrated` последними). Спека РАЗРЕШАЕТ конфликт явно: требование «последним событием» на резерв НЕ распространяется; асимметрия документируется в твине одной строкой. Иное решение — развилка Р7.
- *Позиционный (инвариант)*: перечень событий существует в ТРЁХ носителях (`CLAUDE.md`, `docs/POLICY_FULL.md`, `journal_validator.EVENTS`). Инвариант позиции: три перечня совпадают ПО СОСТАВУ; порядок значения не несёт. Кандидат в машинный сторож этого инварианта — развилка Р12.
- *Генеративный*: два новых имени событий — носители перечислены в К2.5; механизмный путь: да (`CLAUDE.md`, `PROCESS/`, `tools/journal_validator.py`) ⇒ осевой блок + `tier:`; parity-пара kit — в очередь; твин оси 4 — обязателен (проверено чтением).

---

## 3. ПУНКТ 3 — класс `python -c`/heredoc: warn → БЛОК

**Цель.** Единственный класс с нулевым затуханием (7.66 → 7.81 на 100 вызовов) переводится в `permissionDecision: deny` по принципу определённости V5, без ложных блоков на легальной диагностике.

**Критерии приёмки**
1. **К3.1 — определённость отдельным сигналом.** В `_collect_v5_signals()` появляется НОВЫЙ ключ (напр. `pyc_certain`), вычисляемый ОДНОЙ функцией; `_decide_v5` использует для deny ТОЛЬКО его. Ключ `pyc` сохраняет сегодняшний смысл «класс сработал вообще» — **позиционный инвариант**: `permission_audit.classify_hygiene()` читает ключи ПО ИМЕНИ и сравнивает 4-меточный контракт с baseline чека 25; контракт возврата `classify_hygiene` не меняется, что запинено тестом.
2. **К3.2 — выключатель (D-0069).** Всё новое поведение стоит за модульной константой (`PYC_DENY_ENABLED = False` на сдаче), читаемой как global на каждый вызов. При выключенной константе вывод `decide()` байт-в-байт сегодняшний на ВСЁМ существующем наборе `tools/test_hygiene_gate.py` без правки тел старых тестов. Новые тесты включают её `monkeypatch`. **Билдер константу не переключает** — переключает Lead после критик-гейта и пробы живости (прецедент `V5_ENABLED`).
3. **К3.3 — замер ложных срабатываний на историческом корпусе.** Разовый скрипт (в постоянный код не входит, прецедент пересдачи 4) классифицирует ВСЕ Bash/PowerShell-команды транскриптов (источник: `permission_audit.snapshot_transcripts()` / `python tools/permission_audit.py --all --summary` как контрольная сумма корпуса) под сегодняшним и под кандидатным классификатором и печатает: размер корпуса; попадания класса (в); из них would-be-deny / would-be-warn / тишина; ПОЛНЫЙ дедуплицированный список команд, переворачивающихся warn→deny. **Скрипт прикладывается ПОЛНЫМ ИСХОДНИКОМ вместе с выводом**, и вывод несёт позитивный и негативный контроль: `python -c "print(1)"` попадает в класс, `python -m pytest tools/ gateway/ -q` — НЕ попадает. Каждый перевёрнутый случай получает ручной вердикт: «легальная диагностика (FP)» или «правка/запись (истинное срабатывание)». Порог приёмки по FP — развилка Р14.
4. **К3.4 — что блокируется и что остаётся легальным — НАЗВАНО В ТЕКСТЕ.** Докстринг раздела (в стиле файла, новая секция `v6`) и строка `docs/RULE_COVERAGE.md` формулируют границу дословно, включая: остаётся ли warn для неоднозначных форм (Р17), покрывается ли heredoc целиком или только с формой записи (Р13), какова замена для легальной разовой диагностики (Р15). Норма-источник (гигиена п.4 — про ПРАВКИ ФАЙЛОВ и журнал) цитируется, а не пересказывается.
5. **К3.5 — адверсариальная мини-батарея (интерактивная поверхность), вся зелёная.** `python3 -c`, `py -c`, `pwsh -Command`, `/usr/bin/python -c`, `mypython -c` (обязан НЕ срабатывать — пин `\b`), `PYTHON -C` (регистр), `python  -c` (двойной пробел), `python -c` внутри `git commit -m "..."`, внутри тела heredoc, CRLF-переводы строк, незакрытая кавычка, `python - <<'PY'` с продолжением после разделителя, эмодзи/не-ASCII в payload, команда, состоящая ровно из `python -c`, пустая команда, не-dict payload, не-Bash тул, битые байты stdin.
6. **К3.6 — носители тем же коммитом.** `docs/RULE_COVERAGE.md` строка 4-го раздела (сегодня буквально говорит «WARN: … python -c/heredoc без изменений» — после правки это ложь); чек 25 протокола (сегодня говорит, что решение warn→блок «ЭТИМ ЧЕКОМ НЕ ПРИНИМАЕТСЯ, оно калибровки №7» — после принятия формулировка устарела); чек 26 при необходимости; kit-твин — в порт-очередь.

**Края поведения**
- *Временной*: хук читает рабочее дерево новым процессом на КАЖДЫЙ вызов ⇒ поведение оживает в момент сохранения для всех параллельных сессий. Мир «до» = константа False (байт-в-байт сегодня), мир «после» = ход Lead (Edit константы) при приёмке. Момент создания «после» назван явно; билдер его не создаёт.
- *Позиционный (инвариант)*: порядок deny фиксирован «журнал → cd → 2>&1»; новый класс встаёт в этот порядок так, что `permissionDecisionReason` для КАЖДОЙ команды, которая денается сегодня, остаётся прежней строкой. Пин: команда, задевающая одновременно журнальный класс и класс (в), сохраняет `MSG_JOURNAL_BLOCK` как причину; новая причина добавляется в `additionalContext`, не подменяет.
- *Лимиты — тест НА границе и ЗА ней*: длина команды 100 КБ (на границе, бюджет времени `decide()` назван в отчёте числом) и 1 МБ (за границей — тот же порядок величины, отсутствие катастрофического бэктрекинга: утверждение линейности в докстринге обязано остаться верным); 500 statement'ов (на) и 5000 (за); 100 вложенных кавычковых пар (за).
- *Пустой/отсутствующий вход*: пустая строка команды, whitespace-команда, отсутствующий `tool_input`, не-строковая `command`, битый JSON на stdin → сегодняшний тихий пропуск без побочных эффектов; `permissionDecision` в fail-open-ветке не появляется НИКОГДА (существующий инвариант F1/F2).
- *Конфликт требований (назван, не разрешён молча)*: «блокировать класс» против «класс имеет законные применения, а норма п.4 — про правки файлов». Спека НЕ даёт билдеру угадывать: граница — развилка Р13, порог FP — Р14, путь отхода — Р15. До их закрытия пункт 3 не диспатчится.
- *Известное ограничение, назвать в докстринге*: класс ловит только литерал `python` — `python3`/`py`/`node -e` остаются вне блока (Р16, триггер повторного рассмотрения — живой обход через `python3` после включения).
- *Генеративный*: новый deny-класс и новый ключ сигнала — носители: докстринг, тесты, `RULE_COVERAGE`, чек 25; механизмный путь: да (`tools/hygiene_gate.py` поимённо в `MECHANISM_PREFIXES`) ⇒ осевой блок + `tier:`; parity-пара kit — очередь; сторож режима — чек 25/26.

---

## 4. ПУНКТ 4 — САМОРЕВИЗИЯ ПРОТОКОЛА последним шагом прогона

**Цель.** У калибровки появляется обязанность спрашивать «чего не хватает в самой калибровке».

**Критерии приёмки**
1. **К4.1 — норма в выбранном доме (Р18) с ТРЕМЯ строками.** Текст предписывает: последним шагом прогона в notes события `calibrated` пишутся три строки; каждая может быть пустой ТОЛЬКО явной строкой с названной формулировкой («впервые исполняемых чеков нет» и т.п.); молчание = отсутствие исполнения.
2. **К4.2 — строка (1)**: чек, исполнявшийся в этом окне ВПЕРВЫЕ, и что в его формулировке разошлось с реальностью при первом исполнении.
3. **К4.3 — строка (2) в ИСПРАВЛЕННОЙ форме (досылка Lead).** Норма обязана требовать ДВА факта на каждый названный чек: (а) сколько прогонов ПОДРЯД чек дал пустое окно (источник — notes предыдущих событий `calibrated`); (б) во что обходится прогон чека (один запрос/греп ≈ ноль против обхода документов/ручного разбора). Вывод — ОДИН ИЗ ТРЁХ: удешевить (реже раза в неделю / автоматизировать), переформулировать вопрос, оставить как есть с явной причиной. Текст обязан прямо говорить: **пустота сама по себе не признак; снятие чека — отдельное решение Lead, не автоматическое следствие пустоты.** Число названных кандидатов не ограничено одним; кандидатов нет — явной строкой.
4. **К4.4 — строка (3)**: находка окна, у которой ЧЕКА НЕ БЫЛО ВОВСЕ (пришла инцидентом/случайно), и следствие: новый чек / расширение существующего / явное «детектора не строим, <причина по Rule #1>».
5. **К4.5 — четыре ответа правила 10 записаны как СОДЕРЖАНИЕ механизма**: (а) цена — минуты калибрующего Lead раз в неделю, платит он же; (б) осевое перечисление по 12 осям — в сообщении коммита; (в) детектор — **чек 0**, и текст ЧЕКА 0 назван так, что отсутствие трёх строк ему видно (иначе детектор — пожелание, F-15/класс F-24); (г) слой — дисциплина с названным детектором (чек 0), машинного слоя не изобретаем.
6. **К4.6 — самоприменение доставлено готовым текстом.** Билдер отдаёт Lead'у блок из трёх строк для notes ЭТОГО прогона: (1) чек 18(д), первое исполнение, две неточности — команда для `unit_count` дана без оконных аргументов (число за всю историю) и `--window last` резолвится в ПРЕДЫДУЩЕЕ закрытое окно; (2) **чек 19 — четвёртый пустой прогон подряд, стоимость средняя, вывод: переформулировать вопрос**; **чек 13(з) — третий пустой подряд, стоимость ≈ ноль, вывод: оставить как есть, API-контур не поднимали**; (3) потеря заголовка оси карты — сторожа не было вовсе, следствие исполняется батчем t-437. Носитель доставки — Р24; билдер журнал НЕ пишет.

**Края поведения**
- *Граница критерия «три прогона подряд» — контроль на границе и за ней (доставлен досылкой Lead и обязан быть отражён)*: строка (2) этого прогона ОБЯЗАНА назвать чек 19 (серия 4) и чек 13(з) (серия ровно 3 — НА границе) и ОБЯЗАНА НЕ называть чек 16 и чек 32 (по два пустых, серия оборвана в №7 — ЗА границей). Это одновременно приёмочный контроль формулировки.
- *«Пусто» против «не исполнялся»*: спека требует, чтобы норма явно различала эти два состояния и чтобы «не исполнялся» НЕ засчитывался в серию. Механизм различения — развилка Р19.
- *Пустой/отсутствующий вход*: если notes прошлых `calibrated` не несут пер-чековых отметок, строка (2) обязана сказать это прямым текстом («данных за N прогонов нет: прошлые notes пер-чековых отметок не несут»), а не докладывать «кандидатов нет». Если прошлых событий `calibrated` меньше трёх — то же.
- *Конфликт требований (явный, разрешён спекой)*: пункт требует «строки записаны в notes ЭТОГО прогона», а non-goal запрещает билдеру писать журнал. Разрешение: билдер производит ГОТОВЫЙ ТЕКСТ, вставку делает Lead тем же ходом; приёмка пункта 4 проверяет наличие текста в отчёте, а не строки в журнале.
- *Временной*: новый чек 33 / новая строка секции «Завершение прогона» — артефакт, которого нет до правки. Мир «до»: notes без трёх строк легальны. Мир «после»: чек 0 следующего прогона видит их отсутствие. Момент создания — коммит правки протокола; первый прогон, обязанный исполнить норму, — сегодняшний (самоприменение).
- *Позиционный (инвариант)*: если норма ставится новым чеком, его номер 33 обязан быть СЛЕДУЮЩИМ за существующим максимумом (сегодня 32 — проверено), а нумерация чеков остаётся непрерывной; если норма ставится в «Завершение прогона», инвариант — перечень «что обязано быть в notes» остаётся ЕДИНСТВЕННЫМ местом этого списка (не заводить второй перечень).
- *Генеративный*: новая обязанность будущих прогонов ⇒ механизм (`PROCESS/` в `MECHANISM_PREFIXES`) ⇒ осевой блок по 12 осям + `tier:`; parity-пара — kit-копия протокола (`toolkit/PROCESS/...`, мораторий — Р23); твин оси 4 — `docs/RETRO_PATTERNS.md` (пара «бэклог ретро-паттернов ↔ действующие чеки»): внедрённый паттерн двигает строку бэклога ТЕМ ЖЕ коммитом — вердикт по этой паре обязателен.

---

## 5. DoD — точный проверочный прогон (его вывод станет witness)

Из корня репозитория, без `cd`-префикса и без ` 2>&1`:

```
python -m pytest tools/ gateway/ -q
```

Витнесс = дословный вывод (итоговая строка + список упавших, если есть). Дополнительно прикладываются ДОСЛОВНО:

1. `python tools/journal_validator.py --standalone` — живой контроль на РЕАЛЬНОМ журнале (новые события в `EVENTS` не ломают существующий файл). Ожидание: строка «STANDALONE MODE …, N строк» либо штатный no-op, exit 0.
2. `python tools/permission_audit.py --all --summary` — контрольная сумма корпуса для пункта 3 (блок «По классам гигиены»), ДО и ПОСЛЕ (с выключателем OFF числа обязаны совпасть — доказательство байт-тождества).
3. Разовый корпус-скрипт пункта 3: ПОЛНЫЙ исходник + вывод + ручные вердикты по перевёрнутым случаям.
4. Размер вывода SessionStart-хука по форме чека 11 (`echo '{}' | python tools/session_context.py` с подсчётом байт/строк; если в оболочке нет `wc` — число получено иным способом с НАЗВАННЫМ методом, а не «на глаз»).

Потолок объёма тестов (R11): ключи приёмки + адверсариальная батарея + границы; полный регресс сверх канонического прогона не требуется.

---

# РАЗВИЛКИ (22) — с ценой вариантов и моей рекомендацией

**Пункт 1**

**Р1. Где ставить точную сверку.** (а) только `session_context` — дёшево, ловит ровно живой случай 08-10 (метка сессии), но механизменный коммит с `tier: claude-opus-4-8` останется принятым; (б) `session_context` + `mechanism_gate` — покрывает и метку, и самодекларацию коммита, цена — две правки и риск ложного отказа на переходном коммите перепривязки; (в) все три — плюс `journal_validator`. **Рекомендация: (б).** `journal_validator` не трогать: его поля `by`/`model` — ЯРУСНЫЕ СЛОВА (haiku/sonnet/opus/fable), не model id; строгость там потребовала бы сменить словарь поля в `CLAUDE.md` — политическая правка, а не код, и она удорожает каждую журнальную строку.

**Р2. Режим строгости в `mechanism_gate`.** (а) отказ коммита при `SAME_FAMILY_OTHER_ID` — сильно, но ломает легальный переходный коммит перепривязки и любую новую, ещё не вписанную модель; (б) принятие + видимая строка расхождения в stdout гейта — стоит ноль, видно коммиттеру и калибровке (чек 8 уже судит правдивость tier-строк по транскриптам); (в) молчание. **Рекомендация: (б)** — тот же двухслойный принцип D-0063, что уже несёт этот гейт: код гарантирует форму, ярус выше судит правду.

**Р3. Модель, которой нет в конфиге вовсе.** (а) отказ; (б) warn/метка «вне лестницы»; (в) молчание (сегодня). Цена (а): экзаменационные прогоны на чужих моделях и любая новая модель ломают гейт до правки конфига. **Рекомендация: (б) в `session_context` (метка), (в) в `mechanism_gate`, если декларация резолвится лестницей.**

**Р4. Сравнение ПОКОЛЕНИЙ (выше/ниже), а не только «равно/не равно».** (а) парсить номер версии — даёт направление, но номер поколения не наше знание, парсер устареет на первом же нестандартном id; (б) не парсить: любое расхождение id при совпавшем семействе — нейтральная метка «id != binding». **Рекомендация: (б).** Направление судит человек/калибровка; иначе `claude-opus-6` (легальный подъём) читался бы как дефект.

**Р5. Текст новой метки `session_context`.** Влияет на чек 11 (размер вывода хука) и обязан быть ASCII ≤80 символов. Варианты: короткая (`Lead(bound:id-mismatch)`) против развёрнутой с обоими id. **Рекомендация: короткая** — сам id уже печатается в той же строке; развёрнутая растит бут-вывод.

**Пункт 2**

**Р6. Имена событий.** `reserve_engaged`/`reserve_released` (предложены Lead) против `lead_reserve_engaged`/`lead_reserve_released` (симметрия префикса с `lead_degraded`). Цена второго — длиннее, но перечень событий сам себя группирует. **Рекомендация: имена Lead** — короче, и «резерв» в этой ОС имеет ровно одно значение.

**Р7. Обязательна ли пара.** (а) `released` обязателен, открытое окно — нарушение (симметрия деградации); (б) `released` не обязателен машинно, открытое окно объясняет чек 5; (в) обязателен + требование «последним событием журнала» как у деградации. Цена (а)/(в): резерв вызывается на ОДНУ тяжёлую задачу, сессия часто просто кончается — норма будет систематически нарушаться и обесценится. **Рекомендация: (б).**

**Р8. Что считается входом на резерв.** (а) первое Lead-действие сессии (симметрия D-0056a); (б) начало сессии. Цена (б): событие пишется даже когда резерв ничего Lead-класса не делал. **Рекомендация: (а).**

**Р9. Ретро-форма для прошедших окон.** (а) не переписывать (склонность Lead, D-0089) — цена: чек 5 на прошлых окнах остаётся слепым, свидетельство — только слово оператора от 2026-08-14, и это надо ОДНОЙ строкой записать в чек 5, иначе следующий калибратор прочитает пустоту как нарушение; (б) ретро-пара «сейчас, с пометкой retroactive и фактическими границами» (легальная форма D-0089) — цена: одно событие, но задаёт прецедент ретро-разметки ярусов. **Рекомендация: (а) + обязательная оговорка в чеке 5.**

**Р10. Типизированные поля новых событий.** (а) только базовые; (б) + `model` (ТОЧНЫЙ id — единственная точка, где id резерва попадает в журнал машинно-проверяемо, прямая связка с пунктом 1); (в) + маркер авторизации («слово оператора»). **Рекомендация: (б)**, авторизация — в notes прозой (отдельного поля схема не заводит, `reason` в схеме нет).

**Р11. Машинная симметрия деградации.** (а) `tools/calibration_counts.py` секция 9 учит считать и резервные пары — дёшево, чек 5 её уже читает; (б) + строка SessionStart об открытом резервном окне (`session_context`) — цена: рост бут-вывода, за которым следит чек 11; (в) ничего, только текст. **Рекомендация: (а) сейчас, (б) в очередь с триггером «первое открытое резервное окно, пережившее сессию».** `.claude/skills/session-handoff/SKILL.md` п.2 — по этой же развилке (правится ли вечерняя проверка).

**Р12. Parity-сторож словаря событий** (`CLAUDE.md` ≡ `POLICY_FULL` ≡ `journal_validator.EVENTS`). (а) новый тест-файл — стоит один файл, зато закрывает ось 4 машинно И служит витнессом doc-части по R11 (ключи цитируются дословно, негативный контроль встроен); (б) разовая ручная сверка — ноль стоимости, ноль гарантии. Замечу: `tools/parity_check.py` для этого НЕ дом — он про хеш-паритет kit-твинов, не про содержание документов. **Рекомендация: (а).**

**Пункт 3**

**Р13. Граница «определённо запись/правка» против диагностики.** (а) КАВЫЧКИ как признак данных (симметрия с уже принятым решением по `2>&1`): если токен `python -c` / `python - <<` уцелел после `_mask_quoted_segments`, вызов реальный → deny; внутри кавычек → тишина. Цена: блокирует и легальную разовую диагностику — требует Р15 и нулевого FP по Р14; (б) кавычки + признак записи в payload — это ВОСКРЕШЕНИЕ удалённого `_has_write_intent` (часть 3 V5, снесена словом оператора как не окупающая сложность): цена — повтор уже отвергнутой конструкции; (в) deny только для heredoc-формы `python - <<` и для `-c` с редиректом, `-c` без записи остаётся warn — цена: оставляет ровно ту форму, что чаще всего и правит файлы одной строкой. **Рекомендация: (а)** — единственный вариант, согласный с принципом V5 и с уже принятым отказом разбирать код/данные; цена названа честно и компенсируется Р14/Р15.

**Р14. Порог ложных срабатываний для приёмки.** (а) НОЛЬ ложных deny на корпусе (планка, которую взяла замена `2>&1`: 1035 deny / 102 warn при нуле ложных); (б) ≤ N% с поимённым разбором; (в) «FP названы и объяснены». **Рекомендация: (а)** — ложный блок дороже любого warn (прецедент F-53-2: ложный блок на `grep -c ">"` подтачивал доверие к гейту).

**Р15. Break-glass.** (а) нет вовсе; легальная замена названа: правки — Edit/Write, расчёт/диагностика — именованный файл `python <path>` (в т.ч. в scratchpad); (б) снимающий маркер-комментарий в команде — цена: обесценивает блок в тот же день; (в) модульная константа отката, владелец — Lead (она и так есть по К3.2). **Рекомендация: (а) + (в).** (б) отвергнуть явно.

**Р16. Расширять ли токен-набор класса (в)** (`python3`, `py`, `pwsh -Command`, `node -e`). Сегодня ловится только литерал `python`. (а) расширить сейчас — растёт поверхность FP одновременно с промоцией в блок; (б) не расширять, замерить, поставить в очередь с триггером «живой обход через python3 после включения». **Рекомендация: (б).**

**Р17. Судьба сегодняшнего warn-текста класса (в).** (а) неоднозначные формы остаются warn с прежним текстом (полная симметрия с `2>&1`: deny / warn / тишина); (б) класс становится «всё или ничего». **Рекомендация: (а).**

**Пункт 4**

**Р18. Дом нормы.** (а) секция «Завершение прогона» — там уже перечислено, что обязано быть в notes; (б) новый чек 33 — норма попадает в исполняемый чек-лист, по которому идёт калибратор; (в) оба: чек 33 (РАБОТА по ревизии) + одна строка в «Завершении прогона» (носитель РЕЗУЛЬТАТА в notes). Цена (в) — две правки одного файла, зато и работа, и её след названы там, где их ищут. **Рекомендация: (в).**

**Р19. Как отличить «пусто» от «не исполнялся».** (а) дисциплина: каждый чек с пустым окном обязан писать явную строку (для многих чеков это УЖЕ норма — 1(в), 4, 9(б), 20, 21, 22, 25(г)), «не исполнялся» — тоже явной строкой, и в серию не засчитывается; (б) машинный счётчик серий в `tools/calibration_counts.py`. **Рекомендация: (а) сейчас** (прямо следует из ответа (г) Lead'а: машинного слоя не изобретаем), **(б) в очередь.**

**Р20. Форма строки (2).** (б1) один кандидат или все подходящие: **рекомендую все подходящие** (сегодня их два — 19 и 13(з); ограничение одним потеряло бы половину факта). (б2) два факта в ОДНОЙ строке на чек против двух отдельных строк: **рекомендую одну строку на чек** в формате «чек N: пусто M прогонов подряд; стоимость <≈ноль|средняя|высокая>; вывод — <удешевить|переформулировать|оставить, причина>» — так серия и цена не расходятся по разным местам notes.

**Р21. Машинный счётчик пустых прогонов подряд** (из досылки). (а) читать прошлые notes глазами — цена: минуты Lead, ровно та, что уже названа ответом (а) правила 10; (б) счётчик в `calibration_counts.py` — цена: код + его собственный детектор отказа. **Рекомендация: (а), (б) в очередь с триггером «третий прогон, где строке (2) потребовалось перечитать больше трёх прошлых notes».**

**Р22. Чинить ли формулировку 18(д)** (две неточности, названные строкой (1) этого же прогона) тем же коммитом. (а) да — иначе механизм заводится и в тот же день фиксирует незакрытую находку; цена — две фразы; (б) отдельной задачей. **Рекомендация: (а)** — это подметание внутри ТОГО ЖЕ артефакта (D-0100), файл уже открыт этой работой.

**Р23. Кит-строка** (`toolkit/PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md`): мораторий D-0074, дефолт «в очередь». **Рекомендация: в очередь** одной строкой порт-очереди (совпадает с дефолтом Lead).

**Кросс-пункты**

**Р24. Носитель готового текста трёх строк** для Lead. (а) только финальный отчёт билдера (эфемерно, но Lead вставляет тем же ходом); (б) блок в `CURRENT_CONTEXT.md`; (в) `docs/task_reports/`. **Рекомендация: (а)**, и если Lead хочет след на диске — (б), но `CURRENT_CONTEXT.md` вне owns билдера, это ход Lead.

**Р25. Число коммитов батча.** (а) один коммит на весь батч — один осевой блок и один `tier:`, но осевые вердикты четырёх разных механизмов сливаются в один список; (б) четыре коммита по пунктам — четыре честных осевых блока. **Рекомендация: (б).** Коммитит Lead (билдер не коммитит), поэтому решение прямо влияет на его ход.

---

# ЗАКРЫТЫЕ МНОЙ МЕЛОЧИ (технические, без проектного веса)

1. Имена новых функций/констант/ключей — на усмотрение билдера, при условии, что они задокументированы в докстринге в стиле файла.
2. Новая секция докстринга `hygiene_gate.py` называется `v6` (продолжение нумерации v2/v3/v4/V5).
3. Новые тесты живут в СУЩЕСТВУЮЩИХ `tools/test_*.py` соответствующих модулей; отдельный новый файл заводится только под parity-сторож (Р12), если он будет принят.
4. Тела существующих тестов не правятся; допускается только добавление комментария у поимённо инвертируемых пинов (прецедент «M9» V5).
5. Новые метки/сообщения — ASCII-safe и однострочные (класс критика t-043, `_ascii_sanitize`).
6. Тесты выключателя пункта 3 — через `monkeypatch.setattr(...)`, значение читается как модульный global на каждый вызов.
7. Корпус-скрипт печатает команды усечёнными до фиксированной длины и дедуплицированными; сам скрипт в постоянный код репо не входит.
8. Никаких новых зависимостей; всё на stdlib + уже используемом `yaml`.
9. Правки протокола якорятся по заголовку чека/секции, не по номеру строки (следствие живой параллели t-437).

---

# ДОЛОЖЕННОЕ (D-0043: замеченное чтением, скоуп не расширял)

1. **Расхождение брифа с носителем.** В `delegation.config.yaml` НЕТ формулировки «подстрока Claude-семейства — notation convenience и фолбэк при отсутствии конфига». Шапка говорит только: «Файл отсутствует → все трое дефолтятся в fable (поведение до D-0099, fail-closed)». Понятие семейства-по-подстроке живёт в `tools/mechanism_gate.py` (`LEAD_FAMILIES`, докстринг `lead_family()`, п.7 модульного докстринга) и в `tools/session_context.py` (`_MODEL_TIER_SUBSTRINGS`). Контроль формы: греп по тому же файлу, тем же тулом, тем же ключом `-i` шаблоном `удобств|notation|конвенц|фолбэк|подстрок|семейств` дал ноль; позитивный контроль той же формой (`лестниц|дефолт`) дал 6 совпадений — труба работает, отсутствие настоящее.
2. **Пункт 4 — тоже механизмный путь.** `PROCESS/` входит в `MECHANISM_PREFIXES` наряду с `CLAUDE.md`, `docs/SIBLING_MAP.md`, `docs/DECISIONS_FULL.md`, `.claude/settings.json` и поимённо `tools/{session_context,hygiene_gate,journal_validator,mechanism_gate,…}.py`. Значит осевой блок и `tier:` несут ВСЕ ЧЕТЫРЕ пункта, не только 1–3. (Вне невода: `tools/calibration_counts.py`, `tools/permission_audit.py` — их правка сама по себе осевого блока не требует, но в общем коммите требование действует.)
3. **Носители словаря событий шире брифа.** Кроме названных Lead'ом, `lead_degraded`/`lead_restored` живут в `tools/calibration_counts.py` (секция 9 + секция отчёта), `tools/session_context.py` (`open_degradation_window`, строка SessionStart), `.claude/skills/session-handoff/SKILL.md` п.2, `docs/RULE_COVERAGE.md` разд. 6, `docs/FINDINGS.md`, `DECISIONS.md`/`docs/DECISIONS_FULL.md`. Все внесены в подметание К2.5.
4. **`journal_validator` не имеет опоры для точного id** (пункт 1): поля `by`/`model` в схеме — ярусные слова, не model id; строгость там = смена словаря поля в `CLAUDE.md`.
5. **Позиционная связка пункта 3**: `permission_audit.classify_hygiene()` читает `_collect_v5_signals` по ИМЕНАМ ключей и сравнивает 4-меточный контракт с baseline чека 25 — любое переопределение смысла ключа `pyc` молча ломает сравнение с baseline калибровки №6.
6. **Два текста устареют в момент правки пункта 3**: `docs/RULE_COVERAGE.md` строка гигиены («WARN: … python -c/heredoc без изменений») и чек 25 («решение warn→блок ЭТИМ ЧЕКОМ НЕ ПРИНИМАЕТСЯ, оно калибровки №7»). Обе — в критерий К3.6.
7. **`tools/test_sibling_map_liveness.py` уже есть в рабочем дереве** (незакоммиченное состояние t-437) — путь не трогать (D-0060/F-23).
8. **Ось 12 SIBLING_MAP сама фиксирует открытый пробел**: «периодической сверки пары ВНЕ момента диспатча на сейчас нет». Пункт 1 — прямой кандидат частично закрыть эту строку; вердикт по оси 12 в осевом блоке обязателен.
9. **Не проверял чтением** (принял как замер Lead, помечаю честно): числа окна 2026-08-10 (сессия 01ffa76c, 12 ходов, $26.02), нормировки гигиены на 100 вызовов, счёты пустых окон чеков 19/13(з)/16/32 по трём прошлым `calibrated`, суммы 699/$489.83 против 327/$99.49. Из них проверено только существование 6 событий `calibrated` в журнале и существование подпункта 13(з) в протоколе.

---

# СЛЕД ЧТЕНИЯ

Прочитано целиком: `delegation.config.yaml`, `.claude/settings.json`, `tools/hygiene_gate.py` (стр. 1–783 и 784–1428, весь файл), `tools/journal_validator.py`, `tools/parity_check.py` (шапка).
Прочитано участками: `tools/mechanism_gate.py` (1–110, 110–150, 200–500), `tools/session_context.py` (300–340, 600–760), `tools/permission_audit.py` (500–579), `PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md` (1–80, 80–210, 443–572, 643–704, 1015–1035), `docs/POLICY_FULL.md` (430–550), `docs/SIBLING_MAP.md` (заголовки осей 1–12, ось 4 целиком, ось 12 целиком), `docs/RULE_COVERAGE.md` (75–104).
Грепы (с позитивными контролями там, где утверждается отсутствие): `lead_restored` по репо (26 файлов), `lead_degraded|lead_restored` по `tools/*.py`, `"event":"calibrated"` по журналу (6), заголовки чеков протокола (0..32), `hygiene_gate|python -c|heredoc` по `RULE_COVERAGE.md`, негатив+позитив по `delegation.config.yaml`.
Glob: `tools/test_*.py` (42 файла; все названные в манифесте существуют).

---

# ПРОВЕРОЧНЫЙ ПРОГОН ЭТОГО ДРАФТА (мой DoD)

- **Содержательных пунктов спеки: 4** (плюс манифест-раздел 0 и DoD-раздел 5).
- **Развилок: 22** (Р1–Р25 с пропуском — нумерация сплошная 1–25 без Р-дублей? нет: список содержит Р1–Р25 минус Р-номера, слитые в один пункт: фактических записей — 22; каждая несёт варианты, цену и мою рекомендацию).
- **Подтверждаю: каждый из четырёх пунктов брифа несёт минимум четыре именованных критерия приёмки** — фактически по ШЕСТЬ на каждый: К1.1–К1.6, К2.1–К2.6, К3.1–К3.6, К4.1–К4.6.
- Края названы явно в каждом пункте: временной, позиционный (с ИНВАРИАНТОМ, не координатой), генеративный (носитель + классификация «механизмный путь / parity-пара / твин оси 4»), пустой-отсутствующий вход, конфликт пары собственных требований; лимиты пункта 3 и границы критерия пункта 4 имеют контроль НА границе и ЗА ней; интерактивная поверхность (пункт 3) несёт адверсариальную мини-батарею.
- Ни одна развилка не закрыта молча; журнал не писался, файлы не правились, агенты не запускались.