> Восстановленный из транскрипта драфт спеки. Задрафчен воркером-designer как часть задачи t-441 (батч 3 калибровки №7), принят Lead'ом 2026-08-14.
> Источник: `C:\Users\user\.claude\projects\D--Improving-AI-Operating-System-for-LLMs\c199b28e-6a01-4d87-aaf2-478c18a5d223\subagents\agent-a39502cb1cb3d943b.jsonl`, строка 97 (последнее assistant-сообщение агента).
> НЕ ИСПОЛНЕН: принятая, но не персистированная спека стоит в очереди ремедиации калибровки №8 узлом F — носитель хода `docs/tasks/2026-08-20_calibration-8-remediation.md`.
> Текст ниже приведён БЕЗ правок — дословное восстановление из транскрипта, разэкранирован только JSON. Устаревшие места (номера строк, ссылки на состояние файлов на 08-14, упоминания уже закрытых пунктов) читаются как ИСТОРИЧЕСКИЕ, а не как инструкция к слепому применению.

---

# ДРАФТ СПЕКИ — калибровка №7, батч 3: кодификации в ядро (чеки 22/23/30/2)

Статус: ДРАФТ. Развилки НЕ закрыты (10 штук, раздел «Развилки»). Спека не готова к диспатчу, пока Lead не закроет развилки 1–7 (они меняют текст пунктов 3/4/5/6 и состав owns) — развилки 8–10 можно закрыть при приёмке драфта.

---

## 0. Предусловие исполнения (обязательный пункт, не преамбула)

**0.1. Параллель t-439.** Батч 2 (t-439) владеет теми же четырьмя markdown-носителями. Батч 3 стартует ТОЛЬКО после схождения t-439 (проверка — приёмка t-439 в журнале; сам журнал батч 3 не пишет).

**0.2. Перечитывание перед первой правкой.** Билдер обязан ПЕРЕД первой правкой прочитать заново целиком (не по памяти брифа и не по цитатам этой спеки):
- `D:\Improving_AI\Operating-System-for-LLMs\CLAUDE.md` — секции R2, R8, R11, «Journal — logs/routing-log.jsonl»;
- `D:\Improving_AI\Operating-System-for-LLMs\docs\POLICY_FULL.md` — хвост от заголовка `## R13 — Leaf routing` до конца файла;
- `D:\Improving_AI\Operating-System-for-LLMs\PROCESS\WEEKLY_CALIBRATION_PROTOCOL.md` — чеки 2, 22, 23, 30;
- `D:\Improving_AI\Operating-System-for-LLMs\docs\RULE_COVERAGE.md` — раздел «1. Маршрутизация».

**0.3. Якорение — по ЗАГОЛОВКАМ И ДОСЛОВНЫМ СТРОКАМ, номера строк запрещены.** Все адреса в этой спеке даны как текстовые якоря; номера строк, если встретятся, — справочные на момент драфта (2026-08-14, до схождения t-439) и НЕ являются адресом. Если якорь не найден (t-439 переписал формулировку) — билдер ОСТАНАВЛИВАЕТ пункт и возвращает вопрос, а не ищет «похожее место».

**Критерии приёмки п.0:** (а) отчёт билдера содержит строку «перечитаны заново: 4 файла» с перечислением; (б) ни одна правка не описана через номер строки; (в) при ненайденном якоре — пункт возвращён вопросом, а не исполнен по догадке.

---

## 1. R11: третий подкласс края — ГЕНЕРАТИВНЫЙ

**Носитель нормы:** `CLAUDE.md`, правило R11, абзац, начинающийся `Two sub-classes of that edge, both dispatcher defects when silent (2026-08-05, calibration #6, cases t-350 B1/B2): (i) TEMPORAL …`.

**Что сделать.** Заменить `Two sub-classes` на `Three sub-classes` и дописать третий подкласс ПОСЛЕ `(ii) POSITIONAL …handed to the performer as fact.`, тем же языком (EN), той же плотностью, той же формулой «класс → что обязана назвать спека → чей это дефект при молчании». Смысловой минимум третьего подкласса (формулировку пишет билдер, ключи — ниже):
- требование спеки САМО рождает артефакт / случай / ветку, которых до неё не было;
- спека обязана назвать (а) НОСИТЕЛЯ рождённого (файл/поле/событие — где он живёт), (б) его КЛАССИФИКАЦИЮ: механизмный путь или нет; есть ли у него ПАРНАЯ сущность (ось 4 / реестр / детектор); кто ВЛАДЕЛЕЦ;
- либо развилка явно передана вниз вопросом;
- молчание = дефект диспетчера (та же атрибуция чека 23), не догадка исполнителя.

**Ключи приёмки (дословно, до прогона):**
- `CLAUDE.md`: `Three sub-classes`, `(iii) GENERATIVE`, `(i) TEMPORAL`, `(ii) POSITIONAL` (два последних — граничный инвариант: старые подклассы не переписаны).
- `docs/POLICY_FULL.md`: `(iii) ГЕНЕРАТИВНЫЙ край`.

**Критерии приёмки:**
1. В `CLAUDE.md` присутствуют все четыре ключа, `Two sub-classes` отсутствует, `(iv)` в этом абзаце отсутствует (тест ЗА границей — см. п.7.3).
2. Тексты подклассов (i) и (ii) не изменены ни на символ (сверка: строки `(i) TEMPORAL — an` … `handed to the performer as fact.` совпадают с состоянием до правки; способ — диф в отчёте).
3. Третий подкласс называет все три обязанности (носитель / классификация / развилка вниз) — семантику судит приёмщик, ключи — только встреча.
4. Твин оси 4 существует и датирован (см. адрес ниже).

**Адрес твина оси 4:** `docs/POLICY_FULL.md`, существующая секция `## R11 — края поведения в спеке (2026-07-29, предложение оператора): rationale`, дописать ПОСЛЕ блока `ДВА ПОДКЛАССА КРАЯ (2026-08-05, калибровка №6)` — новый блок `ТРЕТИЙ ПОДКЛАСС: ГЕНЕРАТИВНЫЙ (2026-08-14, калибровка №7, чек 23 счёт 4)`, с прецедентами t-365 / t-367 / t-423 / t-369 из брифа, ценой и детектором (чек 23 следующего окна). Заголовок `## ДВА ПОДКЛАССА…` там не заголовок, а строка внутри секции — секцию НЕ дробить (развилка 8 про размещение твинов в целом).

---

## 2. Пятипунктовый чеклист D-0096, п.3: достаточность по ИНСТРУМЕНТАМ

**Носитель нормы:** `CLAUDE.md`, R11, фрагмент `(3) "given" enumerated AND sufficient — data, fixtures, paths NAMED, not implied;`.

**Что сделать.** Расширить п.3: достаточность корзины проверяется не только по ДАННЫМ, но и по ИНСТРУМЕНТАМ исполняющего яруса — факт, добываемый инструментом, которого у роли нет, диспетчер обязан ПОЛОЖИТЬ В КОРЗИНУ готовым, а не поручить к добыче; поручение такого факта = spec-дефект диспетчера (тот же класс чека 23), а не «исполнитель не справился».

**Проверенный факт основания (frontmatter `.claude/agents/*.md`, прочитан 2026-08-14):**

| роль | tools |
|---|---|
| scout | `Read, Glob, Grep` |
| designer | `Read, Glob, Grep` |
| judge | `Read, Glob, Grep` |
| critic | `Read, Glob, Grep, Bash` |
| builder | `Read, Glob, Grep, Edit, Write, NotebookEdit, Bash, PowerShell` |

Следствие, которое норма обязана сделать явным: git-история, вывод команды, состояние процесса/сети — недостижимы для scout / designer / judge ПО КОНСТРУКЦИИ; такие факты кладутся в «дано» текстом (вывод прогона, дословная выписка), либо задание переформулируется на доступный инструмент, либо маршрутизируется на ярус с Bash.

**Ключи приёмки:** `CLAUDE.md` — `TOOLS of the executing tier`, `put IN THE BASKET`; `docs/POLICY_FULL.md` — `достаточность корзины ПО ИНСТРУМЕНТАМ`.

**Критерии приёмки:**
1. Ключи присутствуют; нумерация чеклиста осталась 1–5 (тест: строки `(1) explicit question`, `(2) DoD inline`, `(3) "given"`, `(4) writing dispatch`, `(5) freshness` присутствуют все пять — граничный инвариант).
2. Правка живёт ВНУТРИ п.3, а не отдельным шестым пунктом (инвариант позиции — см. п.7.2).
3. Твин в POLICY_FULL называет фактический набор инструментов ролей (таблица выше) как основание, а не пересказ.
4. Норма не вводит нового ОБЯЗАТЕЛЬНОГО поля манифеста (см. развилку 10 — если Lead решит иначе, критерий меняется вместе с ключом).

**Адрес твина оси 4:** `docs/POLICY_FULL.md` — новая датированная секция в хвосте файла (после `## Витнесс доковой диспетчеризации …`), заголовок `## Чеклист D-0096 п.3 — достаточность по инструментам (2026-08-14, калибровка №7, чек 23): rationale`. Почему не в архивную часть: строки от `# CLAUDE.md — Operating System for LLMs (текст до среза, VERBATIM)` до `## R13 — Leaf routing` — ДОСЛОВНЫЙ снимок ядра на 2026-07-19 (заявлено шапкой файла), правка внутри него разрушает свойство архива.

---

## 3. R2: второе исключение — спека, продиктованная уже произведённым независимым контекстом

**Носитель нормы:** `CLAUDE.md`, R2, абзац от `The threshold counts the task's PRIMARY draft. A RESUBMISSION after a `rejected`…` до `…judged against the threshold on its own.`

**Что сделать.** ПОСЛЕ существующего края-пересдачи дописать ВТОРОЕ исключение той же формы и с той же жёсткостью границы: спека, содержание которой продиктовано УЖЕ ПРОИЗВЕДЁННЫМ независимым контекстом (мотив R2 — изоляция контекста — исполнен выше по цепочке).

**Формулировка границы — развилка 1; форма skip-события при исключении — развилка 2.** Билдер пишет текст ТОЛЬКО по закрытой развилке; при незакрытой — пункт не исполняется и возвращается вопросом (см. п.7.4).

**Проверенное основание (журнал, прочитан 2026-08-14):** `logs/routing-log.jsonl` содержит ровно 8 строк `"event":"dispatch_skipped","agent":"designer"` за всю историю (позитивный контроль формы греп-запроса пройден — тот же запрос находит образцы). Причины: t-394 «дословная транскрипция находок вердикта критика t-393»; t-409 «транскрипция кит-фиксов, УЖЕ принятых приёмками t-401/t-406/t-408»; t-429 «продиктовано вердиктом критика t-428 построчно»; t-423 «процедурная инстанциация кодифицированных D-0090/D-0091»; t-395 «реплицирует уже поставленную порт-спеку t-390»; три события батча раздела 2 (одно на спеку). Т.е. признак «ИМЕНОВАННЫЙ вышестоящий артефакт» в живых причинах присутствует в 5 из 8; в 3 из 8 (батч раздела 2) — ссылка на «первое событие», без task_id артефакта. Это существенно для выбора признака: узкая формулировка «есть task_id вышестоящего артефакта» НЕ покрыла бы три из восьми исторических случаев — они бы остались нарушением. Факт даётся Lead как вход развилки 1, решение не моё.

**Ключи приёмки:** `CLAUDE.md` — `DICTATED SPEC`; `docs/POLICY_FULL.md` — `ВТОРОЕ ИСКЛЮЧЕНИЕ R2: ПРОДИКТОВАННАЯ СПЕКА`. (Ключи фиксирует Lead при закрытии развилки 1, если формулировка потребует иных слов — ключи обязаны быть зафиксированы ДО прогона, R11 doc-witness (i).)

**Критерии приёмки:**
1. Исключение сформулировано ровно по варианту, выбранному Lead в развилке 1, и содержит все перечисленные в нём кумулятивные условия (счёт условий в тексте = счёт условий в решении).
2. Текст явно называет, ЧТО исключением НЕ является (антипример — «спека, где критик лишь высказался о направлении»); без антипримера исключение считается неисполненным.
3. Существующее первое исключение (пересдача под тем же task_id) не изменено ни на символ; порог `≥3 numbered items, or touches ≥3 files` не изменён (граничный инвариант).
4. Взаимная непротиворечивость с пунктом 4 доказана строкой в отчёте (см. п.7.5).
5. Твин оси 4 присутствует по адресу ниже.

**Адрес твина оси 4:** `docs/POLICY_FULL.md`, существующая секция `## designer — стоячая функция (промоция калибровкой №5, 2026-07-29): rationale`, дописать ПОСЛЕ блока `КРАЙ: ПОВТОРНАЯ ПОДАЧА ПОД ТЕМ ЖЕ TASK_ID (2026-08-09, слово оператора)` — блок `ВТОРОЙ КРАЙ: ПРОДИКТОВАННАЯ СПЕКА (2026-08-14, калибровка №7, чек 22 счёт 8)`, с восемью живыми причинами (перечислением task_id) и с ценой выбранной границы.

---

## 4. Чек 22: сузить «рецидив внутри сессии» до собственного мотива

**Носитель нормы:** `PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md`, чек 22, блок `РЕЦИДИВ ВНУТРИ СЕССИИ (2026-08-09, детектор нормы R8 «первый ход класса бесплатно», ретро от калибровки №6, чек 22 счёт 3)`.

**Что сделать.** Сузить область действия подпункта до его СОБСТВЕННОГО мотива: он говорит о классе причин САМОИСПОЛНЕНИЯ по льготе R8 («оператор ждёт» / «интерактивный запрос блокирует ход»), а не о любых skip-событиях; skip-события, у которых причина — не самоисполнение по льготе, а законный пропуск диспатча (по одному событию на артефакт, иначе их нечем записать), под правило рецидива НЕ попадают. Маркер повтора в notes сохраняется как дешёвый греп-слой — для класса, оставшегося в области действия.

**Проверенный факт (журнал):** три подряд события 2026-08-?? батча раздела 2 несут маркеры «(это первое из трёх)», «Второе skip-событие батча», «Третье skip-событие батча» — самодекларация СРАБОТАЛА, а по букве текущей формулировки второе и третье — нарушения сами по себе. То есть буква карает исправно работающую дисциплину; это и есть предмет сужения.

**Формулировка сужения — развилка 3.**

**Ключи приёмки:** `PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md` — `КЛАСС ПРИЧИН САМОИСПОЛНЕНИЯ`, `маркер повтора` (сохранён); `docs/POLICY_FULL.md` — `сужение рецидива чека 22`.

**Критерии приёмки:**
1. Оба ключа в протоколе присутствуют; фраза `ВТОРОЕ и последующие события ОДНОГО класса в ОДНОЙ сессии = НАРУШЕНИЕ САМО ПО СЕБЕ` либо переформулирована с явной областью действия, либо снабжена явным исключением — оставить её без квалификатора нельзя.
2. Оконный порог `>=3` (существующий отдельный сигнал «правило не соответствует практике») не тронут — правка не поглощает его (текст `порог по окну — ОТДЕЛЬНЫЙ сигнал` остаётся).
3. Строка «Пустое окно… отмечается явно» сохранена (граничный инвариант чека: пустой вход по-прежнему обязан отмечаться).
4. Сужение НЕ отменяет обязанность маркера для класса, оставшегося внутри области; это записано явной фразой.
5. Твин оси 4 присутствует.

**Адрес твина оси 4:** `docs/POLICY_FULL.md` — новая секция в хвосте `## Чек 22 — сужение рецидива до мотива R8 (2026-08-14, калибровка №7): rationale`. (Чек 22 — протокольный, но норма-источник R8 живёт в ядре: если сужение потребует правки текста R8 в `CLAUDE.md` — это выходит за состав, названный Lead; см. развилку 3, вариант В.)

---

## 5. Чек 30: утечка обязанности записывать причину отклонения от лист-маршрута

**Носитель:** `PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md`, чек 30 `**Leaf-routing D-0087 (механизм 2026-07-21).**`, подпункты (а)–(г).

**НАХОДКА, добытая чтением (несущая, к сведению Lead — она меняет диагноз пункта 5):** в файле `PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md` НЕТ ни одного вхождения `t-286`, `D-0094`, `отклонен` (греп по всему файлу; позитивный контроль той же формы тем же инструментом — `D-0087|лист-класс` найден в строках 961/970 — пройден). То есть детектор, НАЗВАННЫЙ ядром («A deviation … is legal ONLY with a recorded reason in the journal (t-286 form; the window detector is check 30)»), в собственном тексте шага сверки отклонений НЕ СОДЕРЖИТ: чек 30 проверяет живость R13, лист-класс приёмок с basis judge, экономику и сиблингов — но не «есть ли записанная причина у лист-задач, ушедших НЕ через D-конструкцию». При этом `docs/RULE_COVERAGE.md` (раздел 1, строка R13/D-0094) утверждает: «чек 30 метёт отклонения (форма t-286)» — утверждение реестра НЕ подтверждается текстом чека (класс F-41, протухшая строка реестра).

Следовательно причина утечки — не только «предположение»: у обязанности отсутствует шаг в детекторе. Спека обязана это признать явно (брифом задано) и не строить нового механизма.

**Что сделать — развилка 4** (варианты (а)/(б)/(в) из брифа + вариант (г), открывшийся находкой). Независимо от ветки развилки, ОБЯЗАТЕЛЬНО:
- в чек 30 добавляется подпункт-шаг сверки отклонений (иначе детектор остаётся немым при любой ветке);
- строка `docs/RULE_COVERAGE.md` про R13/D-0094 приводится в соответствие с фактическим текстом чека (при развилке 6 = «исключить RULE_COVERAGE из owns» — вместо правки в очередь явным пунктом handoff).

**Ключи приёмки:** `PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md` — `ФОРМА ПРИЧИНЫ ОТКЛОНЕНИЯ`, `t-286`; `docs/POLICY_FULL.md` — `лист-маршрут: форма причины отклонения`; при включённом RULE_COVERAGE — `docs/RULE_COVERAGE.md`: `чек 30(д)` (или номер подпункта, назначенный при закрытии развилки 4).

**Критерии приёмки:**
1. Новый подпункт чека 30 назван буквой, продолжающей существующий ряд (а)–(г) без переименования старых (позиционный инвариант: (а)–(г) сохраняют свои буквы и тексты).
2. Подпункт содержит ПУСТОЕ ОКНО явной строкой («лист-класcовых диспатчей вне D-пути нет» — форма, симметричная другим чекам).
3. Ветка развилки 4 отражена дословно: если выбрано исключение для рекон-диспатчей калибровочного класса — исключение названо в чеке И в твине, а не подразумевается.
4. Пограничный случай назван явно: разведка, чей дайджест служит входом СЕМАНТИЧЕСКОГО вердикта Lead, — либо внутри лист-класса, либо вне; молчание на этой границе = неисполненный пункт.
5. Твин оси 4 присутствует; строка RULE_COVERAGE либо исправлена, либо явно поставлена в очередь с пометкой F-41.

**Адрес твина оси 4:** `docs/POLICY_FULL.md`, существующая секция `## R13 — Leaf routing (D-0087, 2026-07-21): rationale` — дописать блок `ОТКЛОНЕНИЕ ОТ D-ПУТИ: ФОРМА ПРИЧИНЫ (2026-08-14, калибровка №7, чек 30)`.

---

## 6. Семантика поля `basis` в журнальной секции ядра

**Носитель нормы:** `CLAUDE.md`, секция `## Journal — logs/routing-log.jsonl`, строка таблицы событий `| accepted | task_id, model, by (TIER WORD…); + \`basis\`: "critic" or "queued-to-lead" when the acceptor is not strictly above, or "judge" on a leaf-class dispatch (R13/D-0087); + \`witness\`…|`.

**Что сделать.** Дописать прозой ПОД таблицей (таблица — однострочная сводка, длинное определение в неё не влезает без потери читаемости; позиционный инвариант — см. п.7.2) определение:
- `basis="critic"` означает ИМЕННО вердикт критика НА ЭТУ работу, с указанием task_id критик-диспатча в notes; ссылка на драфт дизайнера, на прошлый вердикт по смежной задаче или на «критик смотрел похожее» под этим значением незаконны;
- при приёмке СТРОГО СВЕРХУ поле `basis` не ставится вовсе (оно предназначено для случаев, когда приёмка НЕ строго сверху);
- прошлые строки не переписываются (D-0089) — норма действует вперёд; замеченные расхождения фиксируются заметкой в notes следующего события.

**Проверенные факты (прочитаны 2026-08-14):**
- `logs/routing-log.jsonl`: строка приёмки t-387 несёт `"by":"opus","basis":"critic"` при витнессе builder-прогона — форма из брифа подтверждена.
- `tools/journal_validator.py`: ветка (а) `ok_tier` легальна «ПРИ ЛЮБОМ basis (или без него)», ветка (а2) — лид-привязка принимает «БЕЗ basis»; ветка (г) проверяет лишь, что ярус критика строго выше agent. Существование критик-диспатча по задаче валидатор НЕ проверяет.
- Технический вход для развилки 5: `_harvest_line_into` уже копит `delegated_agents[task_id] -> set(agent)`, т.е. проверка «есть ли delegated с agent=critic по этому task_id» — однострочная по данным, которые уже собраны. Риск ложных срабатываний: критик-гейт иногда идёт СВОИМ task_id (прецедент t-428 «критик-гейт spend-батча»), тогда критик-вход по задаче есть, а agent=critic под тем же task_id — нет.

**Ключи приёмки:** `CLAUDE.md` — `a critic verdict ON THIS TASK`, `strictly-above acceptance carries NO basis`; `docs/POLICY_FULL.md` — `СЕМАНТИКА ПОЛЯ basis`; `PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md` (чек 2) — `basis="critic" без критик-диспатча`.

**Критерии приёмки:**
1. Оба ключа ядра присутствуют; строка таблицы событий не удлинена (инвариант формата таблицы) — определение живёт прозой под таблицей.
2. В чеке 2 протокола добавлен шаг сверки: `accepted` с `basis="critic"` без делегирования критика по этой работе = находка того же класса, что диффы >100 строк без критика.
3. Явная строка о неретроактивности (D-0089) присутствует; ни одна строка `logs/routing-log.jsonl` не изменена (журнал — не-цель).
4. Норма не требует ничего, что валидатор запрещает: формулировка проверена против веток (а)/(а2)/(г) `tools/journal_validator.py`, результат сверки — строкой в отчёте (сам файл НЕ правится, развилка 5).
5. Твин оси 4 присутствует.

**Адрес твина оси 4:** `docs/POLICY_FULL.md` — новая секция в хвосте `## Журнал: семантика поля basis (2026-08-14, калибровка №7, чек 2): rationale`. Почему не в секцию `## Журнал маршрутизации — logs/routing-log.jsonl` (она есть в файле): эта секция — часть ДОСЛОВНОГО архива до среза 2026-07-19, её правка разрушает свойство архива (см. п.2, тот же аргумент).

---

## 7. КРАЯ ПОВЕДЕНИЯ (обязательный раздел, R11)

**7.1. ВРЕМЕННОЙ край.** Артефакты, которых на момент написания спеки НЕТ и которые создаёт сама эта работа:
- четыре новые секции/блока в `docs/POLICY_FULL.md` — до правки их нет, после есть; создаёт ход билдера; их отсутствие ДО правки не является дефектом файла;
- файл теста-витнесса `tools/test_calibration7_batch3_keys.py` — до правки его нет; создаёт ход билдера; свойство R11 doc-witness (ii) «закоммичен тестом» ДО коммита не выполнено — до коммита оно замещается ПОЛНЫМ исходником в отчёте, коммит делает Lead (не-цель билдера);
- строки в `docs/RULE_COVERAGE.md` (при развилке 6 = «включить»);
- поведение в обоих мирах: если какой-либо ключ УЖЕ присутствует в файле (t-439 внёс похожий текст) — билдер НЕ дублирует норму, останавливает пункт и возвращает вопрос с цитатой найденного.

**7.2. ПОЗИЦИОННЫЙ край — инварианты позиций, не координаты:**
- п.1: третий подкласс идёт ПОСЛЕ (ii) и ДО предложения `A task with an INTERACTIVE surface…`. Инвариант: абзац о подклассах остаётся единым блоком края; блок `DOC-DISPATCH WITNESS` остаётся ПОСЛЕДНИМ блоком R11; `R11a` не сдвигается по смыслу и не затрагивается.
- п.2: расширение живёт внутри `(3)`. Инвариант: чеклист остаётся ПЯТИпунктовым, ссылки «пятипунктовый чеклист D-0096» по всему репо остаются истинными.
- п.3: исключение идёт ПОСЛЕ края-пересдачи и ДО строки `Pilot protocol and evidence:`. Инвариант: порог (≥3 пунктов / ≥3 файлов) остаётся ПЕРВИЧНЫМ правилом, исключения читаются как сужения, а не как новое дефолтное поведение.
- п.4: сужение живёт внутри блока `РЕЦИДИВ ВНУТРИ СЕССИИ`. Инвариант: оконный порог `>=3` остаётся ОТДЕЛЬНЫМ сигналом, сужение его не касается.
- п.5: новый подпункт идёт ПОСЛЕ (г). Инвариант: буквы (а)–(г) и их тексты неизменны; ни один существующий подпункт не переименован.
- п.6: определение — прозой ПОД таблицей событий, ДО блока `task_id issuance:`. Инвариант: mermaid-диаграмма жизненного цикла и строка `Open --> Closed: accepted (from above / basis critic / queued-to-lead)` остаются в силе — норма п.6 не меняет условий закрытия задачи, только семантику поля.

**7.3. ГЕНЕРАТИВНЫЙ край (самоприменение п.1 к этой спеке).** Сущности, которые рождает сама эта спека, с носителем и классификацией:
| рождаемое | носитель | классификация |
|---|---|---|
| третий подкласс края | `CLAUDE.md` R11 | механизменный путь (`CLAUDE.md` в MECHANISM_PREFIXES `tools/mechanism_gate.py`) → коммит требует осевого блока и строки `tier:`; парная сущность — твин POLICY_FULL; владелец — Lead |
| подпункт чека 30 | `PROCESS/…PROTOCOL.md` | механизменный путь (префикс `PROCESS/`); парная сущность — строка RULE_COVERAGE; владелец — Lead |
| тест-витнесс `tools/test_calibration7_batch3_keys.py` | новый файл в `tools/` | НЕ механизменный путь (в `MECHANISM_PREFIXES` перечислены конкретные `tools/*.py`, этого имени там нет и «tools/» целиком не входит); парной сущности нет; владелец — батч 3; живёт как обычный тест канона |
| ключи приёмки (таблица ключей) | текст этой спеки | не механизм; носитель — DoD диспатча; правило R11 doc-witness (i) требует их фиксации ДО прогона — их владелец Lead, билдер их не выбирает |
Лимиты, которые вводит спека, и их тесты: (граница) все перечисленные ключи присутствуют → тест зелёный; (за границей) удаление ЛЮБОГО одного ключа на КОПИИ дерева → тест обязан упасть с именем отсутствующего ключа; (за границей-2) появление `(iv)` в абзаце подклассов R11 или шестого пункта `(6)` в чеклисте D-0096 → тест обязан упасть (защита от «дописали лишнее»).

**7.4. Пустой / отсутствующий вход:**
- якорь не найден → СТОП по пункту, возврат вопросом (п.0.3), остальные пункты исполняются;
- развилка не закрыта Lead к моменту исполнения → пункт НЕ исполняется, возвращается вопросом; частичная сдача батча законна и не является реджектом-поводом;
- `docs/RULE_COVERAGE.md` исключён из owns (развилка 6) → пункты 4/5/6 сдаются без строк реестра, а недостающие строки перечисляются в handoff как очередь;
- файл-цель отсутствует или пуст (не должно случиться) → СТОП, никакого создания файла с нуля.

**7.5. Конфликтующие пары собственных требований:**
- **п.3 × п.4 (главный конфликт).** Оба про skip-события дизайнера. Требование совместимости, проверяемое явной строкой в отчёте: после обеих правок не должно существовать конфигурации, при которой одно и то же событие ОБЯЗАНО быть записано (R2) и одновременно является нарушением само по себе (чек 22). Если выбранные Lead ветки развилок 2 и 3 такую конфигурацию создают — билдер обязан ОСТАНОВИТЬ оба пункта и вернуть вопрос, а не «согласовать» тексты по своему разумению.
- **п.3 × R8 «Standing case: designer drafting (R2)».** Исключение в R2 сужает обязанность, названную в R8. Требование: если ветка развилки 2 = «событие не нужно», в R8 обязана появиться отсылка к исключению (иначе R8 и R2 расходятся); если ветка = «событие нужно с ref-токеном», R8 не трогается. Билдер исполняет ту ветку, что закрыта, и НЕ правит R8 по своей инициативе.
- **п.6 × матрица «Role ≠ tier».** Матрица говорит `basis` нужен, когда приёмщик не строго выше; п.6 добавляет «строго сверху — поле не ставится». Требование: тексты обязаны читаться как одно правило, а не два; проверка — приёмщик читает оба места подряд.
- **п.2 × R1.** Если факт недостижим инструментами scout, а достать его может только ярус с Bash, — норма п.2 НЕ отменяет R1 и не легализует самостоятельное чтение координатором без события; это сказано явной фразой либо явно передано развилкой (развилка 10).

**7.6. Адверсариальная мини-батарея.** Интерактивной поверхности (CLI/UI, принимающей пользовательский ввод) батч не создаёт: единственный исполняемый артефакт — тест, запускаемый pytest'ом без аргументов. Вместо батареи обязателен негативный контроль (п.8, R11 doc-witness (iii)) плюс тесты за границей из п.7.3. Если Lead закроет развилку 7 в пользу скрипта с аргументами командной строки — мини-батарея становится обязательной (пустой путь, несуществующий путь, путь-каталог, файл в иной кодировке), и это меняет DoD.

---

## 8. DoD — точный проверочный прогон, чей вывод станет витнессом

**Форма витнесса — по норме DOC-DISPATCH WITNESS (`CLAUDE.md`, R11, блок 2026-08-10), все три свойства:**
- (i) ключи процитированы ДОСЛОВНО из таблиц «Ключи приёмки» пунктов 1–6 ЭТОЙ спеки, зафиксированных ДО прогона; выбирать ключи постфактум запрещено;
- (ii) скрипт — не одноразовый файл в scratchpad, а тест в дереве: `D:\Improving_AI\Operating-System-for-LLMs\tools\test_calibration7_batch3_keys.py`, ПОЛНЫЙ исходник которого приложен к отчёту (коммит делает Lead — см. временной край 7.1);
- (iii) негативный контроль обязателен и выполняется НА КОПИИ во временном каталоге (`…\scratchpad`), живое дерево не портится (командная гигиена п.7(г)): тест `test_negative_control_missing_key_reports_failure` копирует `CLAUDE.md` во временный каталог, удаляет ОДИН ключ и проверяет, что функция-чекер возвращает НЕПУСТОЙ список отсутствующих ключей с именем ровно этого ключа.

**Прогоны (вывод каждого — дословно в отчёт, в этом порядке):**
1. `python -m pytest tools/test_calibration7_batch3_keys.py -q`
2. `python -m pytest tools/test_calibration7_batch3_keys.py -v -k negative` — имя негативного теста обязано быть ВИДНО в выводе (доказательство, что контроль реально прогонялся, а не «есть в файле»)
3. `python -m pytest tools/ gateway/ -q` — канонический прогон (диспатч соло, R4: соло-диспатч сохраняет канон)

**Приёмка батча состоится, если:** все три прогона зелёные; отчёт содержит полный исходник теста; отчёт содержит по одной строке-вердикту на каждый из пунктов 1–6 (исполнен / возвращён вопросом — с указанием незакрытой развилки); отчёт содержит строку совместимости п.3×п.4 (7.5); отчёт содержит перечисление замеченных аналогов без расширения скоупа (R9: билдер докладывает, не чинит).

---

## 9. Манифест контекста

**ДАНО (перечислением, читать самому, не по цитатам спеки):**
- `D:\Improving_AI\Operating-System-for-LLMs\CLAUDE.md` — R2 (якорь `The threshold counts the task's PRIMARY draft`), R8 (якорь `Standing case: designer drafting (R2)`), R11 (якоря `Two sub-classes of that edge`, `(3) "given"`, `DOC-DISPATCH WITNESS`), секция `## Journal — logs/routing-log.jsonl` (якорь — строка таблицы `| accepted |`).
- `D:\Improving_AI\Operating-System-for-LLMs\docs\POLICY_FULL.md` — шапка файла (свойство «дословный архив до среза 2026-07-19»), секции `## R13 — Leaf routing`, `## designer — стоячая функция`, `## R11 — края поведения в спеке`, `## Витнесс доковой диспетчеризации` (последняя секция файла).
- `D:\Improving_AI\Operating-System-for-LLMs\PROCESS\WEEKLY_CALIBRATION_PROTOCOL.md` — чек 2 (`**Правило 3 — critic на крупных диффах.**`), чек 22 (`**Повторяющиеся причины dispatch_skipped`), чек 23 (`**Повторы находок critic → кодификация`), чек 30 (`**Leaf-routing D-0087`).
- `D:\Improving_AI\Operating-System-for-LLMs\docs\RULE_COVERAGE.md` — раздел `## 1. Маршрутизация`, строка `R13/D-0094`, легенда режимов.
- `D:\Improving_AI\Operating-System-for-LLMs\docs\SIBLING_MAP.md` — `## Ось 4 — Парные документы (инвариант: не расходиться)`, пункт `CLAUDE.md (ядро политики, EN, только нормы — бут-путь) <-> docs/POLICY_FULL.md`.
- Готовые ФАКТЫ, положенные в корзину (их добыча инструментами билдера не требуется — самоприменение пункта 2 этой спеки):
  - набор инструментов ролей — таблица в пункте 2 (источник: frontmatter `.claude/agents/{scout,designer,judge,critic,builder}.md`, поле `tools`);
  - счёт и причины восьми designer-skip событий — пункт 3 (источник: `logs/routing-log.jsonl`);
  - результат грепа по чеку 30 (`t-286` / `D-0094` / `отклонен` отсутствуют, позитивный контроль пройден) — пункт 5;
  - ветки (а)/(а2)/(г) `tools/journal_validator.py` и наличие `delegated_agents[task_id]` — пункт 6;
  - `CLAUDE.md` и `PROCESS/` входят в `MECHANISM_PREFIXES` (`tools/mechanism_gate.py`), `docs/POLICY_FULL.md` и `docs/RULE_COVERAGE.md` — нет.

**OWNS (абсолютные пути, по одному на строку):**
owns:
D:\Improving_AI\Operating-System-for-LLMs\CLAUDE.md
D:\Improving_AI\Operating-System-for-LLMs\docs\POLICY_FULL.md
D:\Improving_AI\Operating-System-for-LLMs\PROCESS\WEEKLY_CALIBRATION_PROTOCOL.md
D:\Improving_AI\Operating-System-for-LLMs\docs\RULE_COVERAGE.md
D:\Improving_AI\Operating-System-for-LLMs\tools\test_calibration7_batch3_keys.py

(последние две строки зависят от развилок 6 и 7 — Lead фиксирует owns при закрытии развилок; owns нормативны, выход за них = нарушение)

**НЕ-ЦЕЛИ:** `toolkit/` (мораторий D-0074 — всё в порт-очередь); `D:\AO3_tests`; запись в `logs/routing-log.jsonl`; коммит; переписывание прошлых событий (D-0089); правка кодовых гейтов (`tools/journal_validator.py`, `tools/mechanism_gate.py`, `tools/dispatch_gate.py`, `tools/journal_echo.py` и прочая живая проводка — их правит батч 2, столкновение путей запрещено); правка `.claude/agents/*.md` (роль-файлы ссылаются на R11 целиком, отдельной правки не требуют); правка архивной части `docs/POLICY_FULL.md` выше заголовка `## R13 — Leaf routing`; правка `DECISIONS.md` / `docs/DECISIONS_FULL.md` (если Lead захочет решение D-XXXX под пункты 3/5/6 — это его ход, см. развилку 9).

**HANDOFF (что остаётся Lead после сдачи):**
- коммит с осевым блоком по ПЕРЕЧИСЛЕНИЮ и строкой `tier: <model>` (правятся `CLAUDE.md` и `PROCESS/` — механизм-префиксы, `tools/mechanism_gate.py` отвергнет коммит без блока);
- журнальные события батча;
- решение по очереди: машинный слой развилки 5 (если выбран), правка строки `docs/RULE_COVERAGE.md` (если реестр исключён из owns), кит-зеркало норм (`toolkit/`, D-0101/D-0074 — в порт-очередь);
- находка F-41 по строке RULE_COVERAGE о чеке 30 (пункт 5) — регистрация в `docs/FINDINGS.md` за Lead;
- прогон подметания R9/D-0100 по соседям внутри правленых артефактов — за приёмщиком.

---

## 10. РАЗВИЛКИ (10; не закрыты, решение за Lead)

**Р1. Признак второго исключения R2 (главная).**
- (А) ТРИ кумулятивных условия: (1) ИМЕНОВАННЫЙ вышестоящий артефакт с task_id (вердикт критика / принятая приёмка / принятый драфт), (2) содержание спеки — транскрипция/инстанциация этого артефакта, (3) нулевая форк-поверхность (в спеке нет проектных решений). Цена: жёстко и грепаемо (task_id виден); НО 3 из 8 живых случаев (батч раздела 2, ссылка «см. первое событие») формально не проходят — узость покупается признанием части практики нарушением.
- (Б) ОДНО условие: «дословная транскрипция именованного принятого артефакта». Цена: проще, но «дословная» неверифицируемо — самый вероятный кандидат в лазейку.
- (В) Условие через ОТРИЦАНИЕ: исключение действует, только если в спеке НЕТ ни одного проектного выбора, и это подтверждено пустым списком развилок в самой спеке. Цена: элегантно (сам факт «развилок нет» проверяем), но проверяем лишь постфактум — до написания спеки диспетчер не знает, есть ли развилки; на практике превращается в самооценку.
- (Г) Исключения НЕТ; вместо него порог R2 поднимается (напр. ≥5 пунктов). Цена: не решает проблему (эти спеки крупные), но и не открывает щели.
- **Рекомендация: (А)**, с одним смягчением: пункт (1) допускает ГРУППОВУЮ ссылку (одно событие называет артефакт, последующие события того же батча ссылаются на него) — тогда покрываются все 8 живых случаев без размывания признака. Обоснование: task_id — единственный машинно различимый якорь, ровно как в первом исключении (граница по task_id уже выбрана прецедентом 2026-08-09).

**Р2. Нужно ли skip-событие при втором исключении.**
- (а) НЕ нужно (симметрия с пересдачей). Цена: чек 22 теряет данные — исчезнет сам сигнал, по которому эта кодификация и родилась; следующая утечка станет невидимой.
- (б) НУЖНО, но с ОБЯЗАТЕЛЬНЫМ голым токеном ссылки в notes (напр. `dictated:t-NNN`), и такое событие НЕ считается нарушением ни по рецидиву (п.4), ни по порогу окна. Цена: одна строка на спеку; выигрыш — грепаемый реестр применений исключения и живой детектор.
- (в) Опционально. Цена: худшее из двух — данных нет, дисциплины нет.
- **Рекомендация: (б)** — исключение остаётся видимым, а чек 22 получает новый класс, который умеет отличать законное применение от лазейки. Побочный эффект: п.4 обязан явно вывести этот класс из-под рецидива.

**Р3. Форма сужения чека 22.**
- (а) Область действия сужается словами «правило говорит о классе причин САМОИСПОЛНЕНИЯ по льготе R8». Цена: минимальна, ровно мотив; риск — «самоисполнение» придётся определить (событие о пропуске диспатча тоже сопровождается работой координатора).
- (б) Список исключённых классов (перечисление: «одно событие на артефакт», «продиктованная спека»). Цена: перечисление протухает при новом классе.
- (в) Сужение + зеркальная строка в R8 ядра. Цена: расширяет состав батча на R8 (Lead состав не называл), зато норма и детектор перестают расходиться.
- **Рекомендация: (а) + (в)-минимум** — сужение в чеке 22 плюс ОДНА отсылочная фраза в R8; иначе детектор сужен, а норма (R8) по-прежнему читается расширительно.

**Р4. Форма закрытия утечки чека 30** (варианты брифа + вариант, открывшийся находкой п.5).
- (а) Уточнить, где живёт причина: голый грепаемый токен в notes события `accepted` (напр. `leafskip:<класс>`) + шаг сверки в чеке 30. Цена: один токен на приёмку лист-класса; выигрыш — дешёвый грепаемый слой.
- (б) Признать D-0094 неприменимым к рекон-диспатчам калибровочного класса и записать ИСКЛЮЧЕНИЕ. Цена: снимает 13 из 13 случаев окна одним движением, но выводит из-под D-конструкции целый класс — а именно он и был её основным потребителем; риск — исключение поглотит правило.
- (в) Оставить как есть, поднять только детектор. Цена: нулевая сейчас, но повторный рецидив через окно почти гарантирован (это уже второй раз: №6 — t-349, №7 — 13 случаев).
- (г) **новый вариант, открытый находкой:** причина утечки частично МЕХАНИЧЕСКАЯ — у чека 30 нет шага сверки отклонений вообще (`t-286`/`D-0094` в тексте протокола отсутствуют, позитивный контроль пройден). Тогда минимальный честный ход = добавить шаг в чек 30 + починить строку RULE_COVERAGE, а решение «токен или исключение» отложить до следующего окна, измерив на живых данных.
- **Рекомендация: (г) как обязательная база + (а) как форма.** Обоснование: пункт брифа требует признать предположение предположением; находка показывает, что как минимум одна причина утечки — фактическая и дешёвая в починке. Пограничный случай (разведка как вход семантического вердикта Lead) при этом обязан быть НАЗВАН в тексте чека, а не оставлен на суждение — иначе следующее окно даст тот же счёт.

**Р5. Нужен ли машинный слой под семантику `basis="critic"` (п.6).**
- (а) Только норма + чек 2. Цена: ноль кода; детектор — ручной проход окна.
- (б) WARN в `tools/journal_echo.py`: `basis="critic"` при отсутствии `delegated` с `agent=critic` по этому `task_id` → предупреждение в момент записи. Цена: строки кода малы (`delegated_agents[task_id]` уже собирается), но ЛОЖНЫЕ срабатывания реальны — критик-гейт нередко идёт СВОИМ task_id (прецедент t-428); плюс это правка живой проводки, а она — не-цель батча 3 (её сейчас правит батч 2).
- (в) Жёсткий FAIL валидатора. Цена: ломает запись при легальном критик-диспатче под другим id; преждевременно.
- **Рекомендация: (а) сейчас + (б) в очередь** с явным условием промоции по D-0063 (следующий рецидив), и с предварительным замером доли критик-диспатчей, идущих собственным task_id, — без этого замера (б) даст шум.

**Р6. Входит ли `docs/RULE_COVERAGE.md` в owns батча 3.**
- (а) Входит: файл несёт записанную обязанность («новый механизм или смена РЕЖИМА срабатывания существующего — строка/правка здесь ТЕМ ЖЕ коммитом, пара оси 4»); пункты 4/5/6 меняют режимы, а строка про чек 30 к тому же протухла. Цена: +1 файл к owns, ~3 строки правки.
- (б) Не входит, всё в очередь. Цена: коммит батча 3 нарушит записанную обязанность самого реестра; протухшая строка живёт дальше.
- **Рекомендация: (а)** — иначе спека сама создаёт F-41-класс, который сама же и обнаружила.

**Р7. Дом витнесс-скрипта.**
- (а) Тест в дереве `tools\test_calibration7_batch3_keys.py`, зелёный в каноне. Цена: +1 файл в `tools/`, зато свойство (ii) doc-witness выполняется идеально и ключи защищены от протухания навсегда (тест упадёт, если норму снесут).
- (б) Скрипт в scratchpad + полный исходник в отчёте. Цена: дешевле, но право дешёвого ре-рана держится на приложенном тексте; ключи не защищены на будущее.
- (в) Тест в дереве, но временный (Lead удаляет после приёмки). Цена: худшее из двух.
- **Рекомендация: (а).** Прецедент нормы — `test_findings_form` (POLICY_FULL, секция витнесса доковой диспетчеризации: «Лучшая форма уже отгружена тем же батчем»).

**Р8. Размещение твинов в `docs/POLICY_FULL.md`.**
- (а) ОДНА новая секция в хвосте «Калибровка №7, батч 3» со всеми шестью rationale. Цена: дёшево искать по дате; но rationale R11 и R2 отрываются от своих существующих домов (секции 767 и 671), где уже лежат предыдущие подклассы/края — следующий читатель R11 увидит два подкласса и не увидит третьего.
- (б) ПО ПРАВИЛАМ: п.1 → в секцию `## R11 — края поведения в спеке`, п.3 → в `## designer — стоячая функция`, п.5 → в `## R13 — Leaf routing`, п.2/4/6 → новые секции в хвосте (домов у них нет, а их архивные дома править нельзя). Цена: правки в четырёх местах файла, зато каждый rationale лежит там, где его ищут.
- **Рекомендация: (б).** Факт-основание: строки от `# CLAUDE.md … (текст до среза, VERBATIM)` до `## R13 — Leaf routing` — дословный архив среза 2026-07-19; секции `## Журнал маршрутизации`, `## Правила маршрутизации`, `## Роль ≠ ярус` лежат ВНУТРИ него и не могут принимать новые твины.

**Р9. Нужно ли решение D-XXXX под пункты 3/5/6.**
- (а) Нет: это уточнения существующих норм (R2/R13/журнал), дом — ядро + твин. Цена: ноль; риск — «второе исключение R2» по весу похоже на решение.
- (б) Да, одно решение на батч, с перечислением шести кодификаций. Цена: пара DECISIONS.md ↔ DECISIONS_FULL.md тем же коммитом (ось 4, счёт записей сверяет чек 12), +2 файла к owns.
- (в) Решение только под п.3 (исключение из нормы — самый весомый ход).
- **Рекомендация: (в)**, и это ход Lead, не билдера: исключение из R2 меняет обязанность будущих сессий, а прочие пять — уточнения формулировок уже принятых норм.

**Р10. Область действия правки п.2 (инструменты).**
- (а) Правило распространяется на ЛЮБОЙ диспатч, требования нового поля манифеста нет — диспетчер просто обязан проверить. Цена: ноль механики, детектор — чек 23 (атрибуция spec-дефекта).
- (б) Плюс обязательная строка «инструменты исполнителя: <список>» в манифесте пишущих диспатчей. Цена: строка на каждый диспатч + кандидат в WARN `dispatch_gate` (а это не-цель батча); риск карго-культа (маркер есть, проверки нет).
- (в) Правило только для read-only ролей (scout/designer/judge). Цена: уже; но builder с Bash тоже не имеет доступа, например, к чужому деплою — узость искусственная.
- **Рекомендация: (а).** Плюс явная фраза о взаимодействии с R1 (край 7.5): норма п.2 НЕ легализует самостоятельное добывание факта координатором без skip-события.

---

## 11. Закрытые мною мелкие технические выборы (без проектного веса)

1. Все адреса — текстовые якоря (заголовки/дословные строки); номера строк в спеке справочные и помечены как таковые.
2. Ключи приёмки — короткие различимые подстроки, на языке файла-цели (EN для `CLAUDE.md`, RU для POLICY_FULL/протокола/реестра).
3. Негативный контроль — на КОПИИ во временном каталоге сессии, живое дерево не портится (командная гигиена п.7(г); `git checkout` в этом батче не используется вовсе).
4. Чтение/запись файлов тестом — в UTF-8 явным указанием кодировки (файлы содержат кириллицу и типографские тире).
5. Заголовки новых секций POLICY_FULL — по образцу существующих: `## <тема> (2026-08-14, калибровка №7, <чек>): rationale`.
6. mermaid-диаграмма журнальной секции не трогается (п.6 не меняет условий закрытия задачи).
7. Роль-файлы `.claude/agents/*.md` не трогаются: п.1 роли designer ссылается на «форму R11 целиком», третий подкласс подхватывается автоматически.
8. Порядок исполнения пунктов — 1, 2, 6 (ядро), 4, 5 (протокол), 3 (ядро, зависит от развилок), твины и реестр — последними, одним проходом по POLICY_FULL.
9. Отчёт билдера — вердикт-строка на каждый пункт (исполнен / вопрос) плюс раздел «замеченные аналоги» без расширения скоупа (R9).

---

## 12. Самопроверка драфта (DoD, заданный брифом)

- **Пунктов:** 6 содержательных (1–6) + п.0 «предусловие исполнения» (перечитывание и якорение — брифом велено сделать явным пунктом) = 7 нумерованных требований; состав шести — ваш, п.0 процедурный.
- **Развилок:** 10 (Р1–Р10), каждая с вариантами, ценой и рекомендацией. Дополнительно 9 закрытых мелких выборов.
- **Каждый из шести пунктов несёт ≥3 именованных критерия приёмки:** п.1 — 4; п.2 — 4; п.3 — 5; п.4 — 5; п.5 — 5; п.6 — 5. ПОДТВЕРЖДАЮ.
- **Адрес твина оси 4 у каждого из шести:** п.1 → `docs/POLICY_FULL.md` `## R11 — края поведения в спеке…` (новый блок «ТРЕТИЙ ПОДКЛАСС»); п.2 → новая хвостовая секция «Чеклист D-0096 п.3»; п.3 → `## designer — стоячая функция…` (новый блок «ВТОРОЙ КРАЙ»); п.4 → новая хвостовая секция «Чек 22 — сужение рецидива»; п.5 → `## R13 — Leaf routing (D-0087…)` (новый блок «ОТКЛОНЕНИЕ ОТ D-ПУТИ»); п.6 → новая хвостовая секция «Журнал: семантика поля basis». ПОДТВЕРЖДАЮ.

---

## 13. Доклад Lead (D-0043 — замечено, скоуп не расширен)

1. **Расхождение реестра и детектора (F-41-класс, несущее для пункта 5):** `docs/RULE_COVERAGE.md` утверждает «чек 30 метёт отклонения (форма t-286)», а в `PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md` нет ни `t-286`, ни `D-0094`, ни `отклонен` (позитивный контроль греп-формы пройден на `D-0087|лист-класс`). Это меняет диагноз пункта 5 брифа: часть утечки — не дисциплина, а отсутствующий шаг детектора.
2. **Ограничение по факту брифа, снятое чтением:** брифом сказано «прочти оба существующих подкласса… это ядро, лишних слов там нет» — подтверждено; но брифом не сказано, что твин `## Журнал маршрутизации` в POLICY_FULL находится ВНУТРИ дословного архива до среза 2026-07-19. Это ограничивает п.6: его rationale физически не может лечь «рядом с нормой» в твине, только новой хвостовой секцией (развилка 8).
3. **Данные, уточняющие пункт 3:** из восьми designer-skip событий три (батч раздела 2) ссылаются на артефакт не своим task_id, а фразой «см. первое событие». Узкая формулировка исключения «с task_id вышестоящего артефакта» задним числом делает эти три нарушением — фактор выбора в развилке 1.
4. **Аналог, НЕ входящий в состав (в очередь Lead):** `toolkit/` содержит зеркала ролей и, вероятно, норм (`toolkit/.claude/agents/*.md` — прочитаны только имена); по D-0074 мораторий, по D-0101 кит-зеркало норм обычно идёт тем же коммитом. Здесь это конфликт двух записанных правил — разрешает Lead, я скоуп не расширяю.

**Прочитанное (след):** `CLAUDE.md` (R2 55–85, R8 168–199, R9 201–217, R11 246–316, журнальная секция 390–434, R13/basis-строки 346/401/426/476); `docs/POLICY_FULL.md` (1–60, 383–412, 671–843, перечень заголовков); `PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md` (перечень чеков, 65–126, 530–583, 961–1000); `docs/RULE_COVERAGE.md` (1–50); `docs/SIBLING_MAP.md` (ось 4, 100–129); `.claude/agents/{scout,designer,judge,critic,builder}.md` (frontmatter `tools`, плюс `scout.md` 1–15); `tools/journal_validator.py` (96–175, 288–306, 411–465); `tools/mechanism_gate.py` (MECHANISM_PREFIXES 97–171); `logs/routing-log.jsonl` (греп-выборки: `basis":"judge"` — 12 вхождений; `dispatch_skipped/designer` — 8 строк с notes; строки t-387/t-379). Негативы (`t-286`/`D-0094`/`отклонен` в протоколе) даны с позитивным контролем той же формы тем же инструментом.