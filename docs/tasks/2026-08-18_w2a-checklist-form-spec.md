# W2a — Спека реестровой формы чек-листа калибровки (драфт designer t-486 + амендменты t-489, приняты Lead 08-18; критик t-488 — fit_with_fixes, фиксы внесены)

Статус: драфт принят; критик-на-плане пройден (fit_with_fixes);
ПЯТЬ обязательных фиксов внесены амендментами t-489 (блоки AM-1..AM-10
в конце файла ЗАМЕНЯЮТ заякоренные секции драфта — при чтении
амендмент главнее). РЕШЕНИЯ LEAD по развилкам (08-18): Р1(A)
HTML-комментарий; Р2(A) индекс = вывод пре-пасса; Р3(A) шапки в M1,
перестановка M2; Р4(A) пилот 0/3/13/26/33; Р5(A) ретирование в M1;
Р6 СНЯТА (сторож norm_retire посажен 560a727, K4 перерешён в AM-4);
Р7(A) сайдкар-реестр logs/calibration_prepass.jsonl; Р8(A) без
композиции; Р9(A) builder отдаёт дерево, коммит с осями и tier — Lead;
Р10(A) строки ПУСТ дословно в notes; Р11(A) ключ deploys: в
delegation.config.yaml — заводится посадочным коммитом M1 (акт Lead);
Р12(A) наследование вплетённых маркеров, гибрид (C) — кандидат W2c.
Исполнение M1 — builder t-491. Ниже — драфт VERBATIM (ось 9), затем
амендменты VERBATIM.

---

## А. СПЕКА

### A0. Предмет и два слоя поставки

ОДИН механизм в двух слоях (D-0063): слой нормы — машиночитаемая шапка
у каждого чека + правило ведения в шапке чек-листа + группировка по
ИСТОЧНИКУ данных; машинный слой — детерминированный пре-пасс
tools/calibration_prepass.py (+ test), который решает ВСТРЕЧУ (какие
чеки имеют материал в окне) и печатает причины пропуска; смысловой
вердикт остаётся калибрующему Lead.

Инварианты Lead 1–4 проведены механически: номера не переиспользуются
(--check-form требует сплошного 0..N, ровно одна шапка на id);
ретирование — только с именованным сторожем И командой его живости в
поле статуса; предикат никогда не судит смысл; чек-лист — единственный
дом номеров (шапка ОБЯЗАНА повторять номер из текста, расхождение —
ошибка формы, не второй дом).

НЕ делается этой спекой: миграция сама (отдельные builder-диспатчи);
правка чеков по СМЫСЛУ; CLAUDE.md (W2b); детектор роста и стоячий чек
(W3); порт в toolkit/ (D-0074); коммит и журнал.

### A1. Форма машиночитаемой шапки чека

Носитель — однострочный HTML-комментарий ПЕРВОЙ строкой тела чека
(сразу под строкой жирного заголовка), с отступом тела:

```
<!--CHK 13(к)|src:журнал|pred:journal.parallel_groups|rules:RC§1/R4,RC§1/R11|status:живой|since:2026-08-09-->
```

Поля — фиксированный порядок, разделитель |, список значений через
запятую, пробелы внутри значений запрещены:

| поле | обяз. | значение |
|---|---|---|
| CHK <id> | да | <N> или <N>(<буква>); ОБЯЗАН совпадать с номером в тексте |
| src: | да | 1..4 значения из закрытого списка; ПЕРВОЕ — первичный источник, ключ группировки |
| pred: | да | ровно ОДИН предикат из закрытого списка (A3); дефолт при сомнении — always |
| rules: | да | 1..4 ссылки RC§<секция>/<токен> в docs/RULE_COVERAGE.md |
| status: | да | живой \| ретирован:<дата>;сторож:<id>;живость:<команда> \| деферред:<предикат-триггер> |
| since: | нет | дата посадки |
| note: | нет | короткая пометка без пробелов (напр. кандидат-ретирования(триггер:path.exists:tools/norm_retire.py)) |

Закрытый список src: журнал, журнал-AO3, git, git-AO3, cc_usage,
транскрипты, файлы, файлы-AO3, скрипт:<имя>, оператор.

Шапки подпунктов: подпункт получает СВОЮ шапку только когда его pred
или status отличаются от родительских (иначе наследует), и обязательно
если длиннее 10 строк. Преамбула чека принадлежит родителю: читается,
если жив хоть один подпункт; мёртв родитель и все подпункты — чек не
читается вовсе.

Лимиты формы (тест НА границе и ЗА ней): строка шапки ≤300 байт UTF-8;
значений src ≤4; шапок на id ≤1; длина id ≤12 символов.

Почему комментарий, а не видимая строка: рендер не засоряется,
человекочитаемую проекцию печатает пре-пасс. Альтернативы — Р1.

### A2. Группировка по ИСТОЧНИКУ данных

Правило первичного источника: первичен тот, ОДИН проход по которому
отвечает «есть ли материал в окне». Остальные — в src и в печатаемые
пре-пассом строки «СМЕЖНЫЕ» (указателем, без дублирования текста).

Порядок секций (= порядок прогона): §0 РЕТРО-КОНТУР (чек 0); §1 ЖУРНАЛ
(оба деплоя); §2 GIT-ДИФФЫ (оба репо); §3 ФАЙЛЫ И ЯКОРЯ; §4
ТРАНСКРИПТЫ/cc_usage/ДЕНЬГИ (самый дорогой, один раз); §5 ВНЕШНИЙ
ДЕПЛОЙ AO3 (целиком пропускается, если дерева нет).

Раскладка всех 34 чеков (src → секция → pred) — в драфте задана
таблицей полностью: 0→§0 always; 1→§4 (а manual:cc_usage, б
journal.event:delegated, в journal.event:rejected); 2→§2
git.diff_lines:>100; 3→§1 journal.event:rejected; 4→§4
journal.event:delegated; 5→§4 manual:транскрипты (+грепы файлов
always); 6→§1 journal.event:accepted; 7→§4 manual:cc_usage; 8→§2
git.any; 9→§3 always; 10→§3 always; 11→§4 (а/в always, б journal.any);
12→§3 always (скрипт:parity_check); 13→§1 по подпунктам (а
accepted,agent=builder; б defect_found; в rejected; г
failure_class=spec; д/е journal.any; ж manual:транскрипты; з
script:requests; и delegated; к parallel_groups; л
accepted,agent=builder); 14→§2 git.paths (а .claude/agents/*.md@OS,@AO3;
е toolkit/**; ж delegation.config.yaml,CLAUDE.md); 15/17/27/28/33→§5
path.exists:D:\AO3_tests; 16→§2 git.paths:docs/RELATED_WORK.md; 18→§4
script:savings_report; 19→§4 manual:cc_usage; 20→§1
journal.event:defect_found; 21→§1 journal.event:escalated; 22→§1
journal.event:dispatch_skipped; 23→§1 manual:вердикты-критиков; 24→§3
always; 25→§4 script:permission_audit; 26→§3 always
(скрипт:hook_liveness_probe); 29→§3 (а always, б journal.any); 30→§1
(а always, б journal.event:accepted); 31→§1 accepted,agent=builder;
32→§2 git.paths:toolkit/**.

Многоисточниковых чеков 21 из 34 — правило первичного источника и
«СМЕЖНЫЕ» указатели несущие.

### A3. Пре-пасс — tools/calibration_prepass.py

Имя по сиблингу calibration_counts.py (семья calibration_*). Вход:
протокол (шапки + номера строк), журналы OS/AO3, git log обоих репо за
окно (git -C из кода), существование путей. Транскрипты и cc_usage
пре-пасс НЕ читает сознательно (цена/разрешения) — такие чеки
объявляют manual: и всегда живы.

CLI: --window-start <ISO> [--window-end] [--protocol] [--journal ...]
[--ao3-root] [--json] [--check-form] [--require-all]. Коды выхода:
0 — прогон состоялся; 1 — только у --check-form (дефекты формы);
2 — IO/аргументы.

Закрытый список предикатов v1 (без композиции — Р8): always ·
journal.any · journal.event:<E>[,agent=<A>][,deploy=…] ·
journal.field:<f>=<v> · journal.parallel_groups · git.any ·
git.paths:<glob>[,…][@OS|@AO3] · git.diff_lines:><N> ·
path.exists:<путь> · script:<имя> (всегда жив, печатает команду) ·
manual:<почему> (всегда жив).

ИНВАРИАНТ ПРЕДИКАТА (несущий): предикат обязан быть НЕОБХОДИМЫМ
условием непустоты чека — никогда не гасит окно с материалом. Не
выражается списком → manual:. Нарушение = spec-дефект автора шапки;
детектор — сам прогон: пропуски в notes calibrated, поздний материал в
«пустом» чеке → defect_found.

Выход: шапка окна; КОНТРОЛЬ ИСТОЧНИКОВ (с позитивным контролем пути);
секции §0–§5; на чек — вердикт ЖИВ/ПУСТ/РЕТИРОВАН/ДЕФЕРРЕД + диапазон
строк + причина; ИТОГ с процентом строк к чтению (он же измеритель
гейта (а) фазы, печатается каждым прогоном); список ПУСТЫХ ОКОН
(обязательны явной строкой в notes); строки ПЕРЕВЗВЕДЕНИЯ деферредов с
истинным триггером → находка.

Краевые поведения пре-пасса:
1. Пустое окно — exit 0, баннер, все не-always/manual ПУСТЫ, список
   целиком (пустота обязана попасть в notes, иначе неотличима от
   пропуска).
2. Чек без шапки — ЖИВ (fail-closed); при --require-all — ошибка
   формы; флаг включается коммитом M2 (темпоральный край A8.1).
3. Предикат на несуществующий источник — чек ЖИВ (fail-closed),
   причина дословно; для §5 — строка «внешний деплой недоступен» С
   позитивным контролем той же формы (isdir по корню нашего репо
   рядом, гигиена п.6).
4. Непарсящаяся строка журнала — предупреждение + журнальные чеки ЖИВЫ.
5. Сломанный пре-пасс (крэш/отсутствует/exit 2) — чек-лист читается
   ЦЕЛИКОМ + строка в notes; правило живёт в шапке чек-листа (A7).
6. Деферред × живой предикат — СТАТУС СИЛЬНЕЕ; но истинный
   предикат-ТРИГГЕР деферреда → строка ПЕРЕВЗВЕДЕНИЕ → находка.
7. status:ретирован — чек не читается; печатается строка со сторожем и
   командой живости, и эта команда входит в обязанности прогона.

### A4. Критерий ретирования и кандидаты ПОИМЁННО

Ретировать можно только при ВСЕХ трёх: (i) норма в RULE_COVERAGE с
режимом БЛОК-ХУК или ГЕЙТ КОММИТА (WARN/ВСПЛЫТИЕ/ДИСЦИПЛИНА — нет);
(ii) чек не приносит данных сверх видимых гейту (ни транскриптов, ни
cc_usage, ни смыслового вердикта); (iii) существует ИМЕНОВАННЫЙ
детектор живости гейта — вписывается в поле живость:. Нет детектора —
чек не ретируется, а ПРЕВРАЩАЕТСЯ в проверку живости сторожа.

Остаётся (ловит ОБХОД), если хоть одно: (а) сверяет декларацию с
внешним свидетельством (чеки 4, 5, 6; 13(л)); (б) у гейта есть
легальный обход (--no-verify, «оси: не-механизм», break-glass,
fail-open) — чек аудирует пользование (8, 26(в)); (в) скоуп гейта уже
нормы (25); (г) вердикт семантический (9/19/21/23); (д) чек И ЕСТЬ
детектор отказа гейта (26, 13(ж), 11(б)).

Различающая фраза: гейт отвечает «форма соблюдена?»; чек остаётся,
когда отвечает на вопрос, который гейт задать НЕ МОЖЕТ — «правда ли
пропущенное, и не обошли ли его».

Кандидаты: K1 чек 13(а) witness-присутствие — РЕТИРОВАТЬ (сторож
journal_validator + journal_echo; живость pytest
test_journal_validator; остаточный риск в стаб: строка, не дошедшая до
коммита, гейта не встречает — правда витнесса за 13(л)). K2 чек 12
фрагмент про оси/test_sibling_map_liveness — ОФОРМИТЬ ретированным
(де-факто уже). K3 чек 30(а) фрагмент «"judge" в whitelist» —
РЕТИРОВАТЬ ФРАГМЕНТ (JUDGE_BASIS_VALUE + энум лист-классов в
валидаторе; остальное 30(а) остаётся — класс (а)). K4 чек 12 паритет
DECISIONS — НЕ СЕЙЧАС: сторож norm_retire.py на диске ОТСУТСТВУЕТ
(t-485 в полёте; позитивный контроль глобом 9/10) — статус живой +
note:кандидат-ретирования(триггер:path.exists); пре-пасс напечатает
ПЕРЕВЗВЕДЕНИЕ. K5 чек 13(в) — НЕ целиком: первая фраза ретируема
(FAILURE_CLASSES в валидаторе), остаток — детектор отказа
calibration_counts (класс (д)); расщепление — смысловая правка, вне
W2a. Остальные (2,3,6,8,9,19,21,22,23,25,26,29,31,13(л)) — ОСТАЮТСЯ по
классам (а)–(д).

Итог по объёму: чистых кандидатов три фрагмента (K1,K2,K3) ≈ 1 КБ.
Ретирование в W2a — не байтовый рычаг (см. A6).

### A5. Миграция — два диспатча

M1 «форма и инструмент»: пре-пасс + тест + правило ведения A7 + шапки
ПИЛОТНОЙ партии (0, 3, 13, 26, 33 — мультиисточник, эталон, гигант с
подпунктами, самоописательный, внешний деплой) + ретирование K1/K2/K3.
M2 «остальные 29 + секции»: шапки остальным, физическая перестановка в
§0–§5 ОТДЕЛЬНЫМ коммитом от шапок (ревьюабельность, кит-порт
содержимым), включение --require-all. Внутри диспатча: сначала ОДИН
чек сквозно, потом остальные.

### A6. Длина файла — честная оценка

Добавляется: 34 шапки ≈3.7 КБ + ~30 под-шапок ≈3.3 КБ + правило A7
≈1.2 КБ + секции ≈0.3 КБ = ≈ +8.5 КБ. Снимается: K1+K2+K3 ≈ −1.0 КБ.
НЕТТО ПО ФАЙЛУ: +7.5 КБ (115 → ~122.5 КБ, +6.5%). ФАЙЛ РАСТЁТ;
сокращается ЧТЕНИЕ: типичное окно ≈18% не читается, тихое — до ~30%,
плюс адресные диапазоны вместо линейного прохода. Против гейта (а)
«≤50% байт» W2a один НЕ БЕРЁТ: гиганты 26 (196 стр ≈19 КБ), 13 (122
стр ≈12 КБ), 18 (76 стр ≈7.5 КБ) = 27% файла, все always/script-живые;
их сокращение — смысловая диета (вне W2a). Это замер, не развилка;
решение о добавке скоупа — Lead.

### A7. Правило ведения (текст для шапки чек-листа)

> ФОРМА ЗАПИСИ (Phase 5, W2a). Каждый чек и каждый подпункт со своим
> предикатом несёт машиночитаемую шапку <!--CHK …--> первой строкой
> тела (поля и лимиты — tools/test_calibration_prepass.py). НОВЫЙ ЧЕК
> РОЖДАЕТСЯ СРАЗУ В ЭТОЙ ФОРМЕ: чек без шапки считается заведённым, но
> всегда живым (fail-closed), и помечается пре-пассом как дефект формы.
> Номера НЕ переиспользуются: новый чек — max+1; ретированный остаётся
> СТАБОМ со своим номером, именем сторожа и командой его живости. Чек,
> чей источник пре-пассу недоступен, объявляет pred:manual:<почему> и
> жив всегда — молчаливое угасание чека запрещено.
>
> ПРОГОН НАЧИНАЕТСЯ С ПРЕ-ПАССА:
> python tools/calibration_prepass.py --window-start <ts прошлого
> calibrated>. Его вывод целиком идёт в notes calibrated разделом
> «ПРЕ-ПАСС»; строки «ПУСТ» — обязательные явные отметки пустых окон.
> Пре-пасс решает ВСТРЕЧУ, НЕ смысл (D-0063). ЕСЛИ ПРЕ-ПАСС НЕ
> ОТРАБОТАЛ — чек-лист читается ЦЕЛИКОМ + строка в notes. Пропуск чека
> без строки причины — нарушение этого правила.

### A8. Края (R11)

1. ТЕМПОРАЛЬНЫЙ мир шапок: ДО M1 — шапок нет, --check-form зелен
   (проверяет присутствующие), все 34 живы; МЕЖДУ M1 и M2 — смешанный
   мир легален, --require-all выключен; ПОСЛЕ M2 — флаг включён, чек
   без шапки роняет канон. Переключает ИМЕННО M2.
2. ТЕМПОРАЛЬНЫЙ сторож-которого-нет — K4: живой +
   note:кандидат-ретирования(триггер); пустой сторож в живость:
   запрещён формой.
3. ПОЗИЦИОННЫЙ шапки: первая строка тела; ИНВАРИАНТ — ни одна строка
   тела не остаётся вне диапазона (иначе адресное чтение молча теряет
   норму); --check-form проверяет ПОКРЫТИЕ ВСЕХ СТРОК диапазонами.
4. ПОЗИЦИОННЫЙ перестановки: порядок меняется, номера — нет; инвариант
   — все внешние ссылки по номерам остаются валидными без правок;
   --check-form требует сплошного 0..N и уникальности.
5. Пустые входы: пустое окно (A3.1); протокол без чеков → exit 2
   «форма не распознана»; пустой журнал → предикаты ложны, контроль
   печатает «0 строк»; отсутствующий журнал → чеки живы; пустое
   значение поля → дефект формы.
6. Конфликт «один дом номеров» × шапка: авторитетен НОМЕР В ТЕКСТЕ;
   шапка повторяет; расхождение — дефект формы, не второй источник.
7. Конфликт «сокращаем чтение» × «норма не теряется»: инвариант
   необходимого условия + fail-closed + пропуск всегда печатается.
8. Конфликт деферред × предикат: статус сильнее; истинный триггер →
   находка. Порог PROCESS/README относится к ЭКЗАМЕНАМ — заимствуется
   форма статуса, не порог.
9. Группировка × порядок прогона: §0 первый; порядок секций = порядок
   прогона, печатается пре-пассом.
10. Лимиты — по два кейса на каждый (граница/за границей).

### A9. Как форма вмещает стоячий чек W3 (демонстрация)

Чек 34 садится без правки формы: src:скрипт:corpus_growth (вход чека =
вывод машинного слоя W3); status:деферред:path.exists:tools/
corpus_growth.py (можно посадить ДО постройки слоя — не спит тихо:
пре-пасс напечатает ПЕРЕВЗВЕДЕНИЕ при появлении скрипта — детектор
правила 10в по построению); подпункт (а) сужается git.paths по
корпусным артефактам (W3 не платится в окнах, где корпус не тронут);
rules:RC§9/… вынуждает строку в RULE_COVERAGE (--check-form красный без
неё) — механизм вынужден зарегистрироваться.

### A10. DoD диспатча M1 и проверочный прогон

Ключи: (1) пре-пасс + тест поставлены (поля A1, предикаты A3, exit
0/1/2); (2) вывод несёт окно/контроль источников с позитивным
контролем/секции/вердикты с диапазонами и причинами/ИТОГ с
процентом/пустые окна/перевзведения; (3) шапки ровно пилотной партии,
ретирование ровно K1/K2/K3; (4) правило A7 в шапке протокола; (5)
--check-form: поля и порядок; лимиты; совпадение id с текстом;
сплошность и уникальность; ПОКРЫТИЕ ВСЕХ СТРОК; резолвимость rules: в
обе стороны (каждая RC§ существует И каждое «чек N» из RULE_COVERAGE
резолвится в id протокола); энум status: и обязательность
сторож:/живость:/триггера; (6) адверсариальная батарея + по два кейса
на лимит; (7) отчёт: байты до/после + процент чтения на РЕАЛЬНОМ окне
(замер против оценки A6 — расхождение объясняется); (8) аналоги без
расширения скоупа.

Адверсариальная батарея (CLI): не-ISO даты; end<start; нулевое окно;
протокол отсутствует/не-UTF-8/CRLF; неизвестное поле; нарушенный
порядок полей; две шапки на id; id≠номер; шапка вне тела; --> внутри
значения; пробел в значении; вложенный <!--; неизвестные src/pred/
аргумент; rules: на несуществующую секцию; ретирован без живость:;
деферред без триггера; непарсящийся журнал; журнал отсутствует;
--ao3-root на несуществующий путь; не git-репо; строка >1 МБ; протокол
без чеков; дубль номера. Границы: 300/301 Б; 4/5 src; 1/2 шапки; 12/13
символов id. Потолок объёма: ключи + батарея + границы.

Проверочный прогон (вывод дословно = witness; без cd, без 2>&1):
```
python tools/calibration_prepass.py --check-form
python tools/calibration_prepass.py --window-start 2026-08-14T12:12:34
python -m pytest tools/test_calibration_prepass.py -q
python -m pytest tools/ gateway/ -q
python tools/parity_check.py --check
```
Ожидаемое: parity_check ОБЯЗАН показать hq-drift на паре протокола
(существовал ДО работы — notes №7) + новую позицию «штаб без кит-твина»
у calibration_prepass — обе законны, в порт-очередь, не находка.
Негативный контроль доковой формы — кейс сломанной шапки → exit 1.

### A11. Контекст-манифест

given: PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md (целиком; чеки 0…33 стр.
17–1159; «Завершение прогона» 1161–1175); docs/RULE_COVERAGE.md
(легенда 13–32; секции; справочник чеков 118–186); носитель фазы;
w1-retirement-spec (форма стаба — идейно не расходиться);
tools/calibration_counts.py (импортировать: load_journal,
analyze_journal, _in_window, parse_ts); tools/journal_validator.py
(276, 296–306, 594–610); tools/parity_manifest.json (729–734);
tools/mechanism_gate.py (97–113); PROCESS/README.md (52–94 — форма
deferred и перевзведение); docs/RETRO_PATTERNS.md (1–28);
logs/routing-log.jsonl (последний calibrated 2026-08-14T12:12:34);
D:\AO3_tests (в M1 только path.exists).

owns (M1): tools/calibration_prepass.py,
tools/test_calibration_prepass.py, PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md
(M2 — тот же третий путь; диспатчи последовательны).

non-goals: смысл чеков; docs/RULE_COVERAGE.md (ось 4 — Lead тем же
коммитом); CLAUDE.md (W2b); journal_validator/calibration_counts/
parity_manifest; порт в toolkit/; заведение чека 34 (W3); КОММИТ и
осевой блок; журнал; статус узла; объём пилотной партии.

handoff: отчёт → приёмка Lead. Долги Lead: (1) КОММИТ — PROCESS/ в
MECHANISM_PREFIXES, tier-строка builder-сессии рангом ниже lead будет
отвергнута гейтом → коммит с осями и tier: — акт Lead (Р9); (2) строка
RULE_COVERAGE + справочник чеков; (3) кит-порт (D-0074); (4) статус
узла + журнал.

## Б. РАЗВИЛКИ Р1–Р10 (рекомендации дизайнера)

Р1 носитель шапки: A HTML-комментарий (реком.) / B видимая строка / C
YAML-фенс (+20 КБ — перечёркивает A6). Р2 индекс: A нет, индекс =
вывод пре-пасса (реком.) / B генерируемая таблица (+2-3 КБ, риск
дрейфа). Р3 группировка: A шапки в M1, перестановка отдельным M2
(реком.) / B одним ходом (дифф >1000 строк, ревью слепое) / C
виртуальная (цель «рядом» только для гоняющего инструмент). Р4 пилот:
A 5 чеков 0/3/13/26/33 (реком.) / B секция §1 (10) / C все 34. Р5
ретирование K1/K3: A в M1 (реком. — демонстрация формы) / B после M2.
Р6 K4: A ждать norm_retire с note-триггером (реком.) / B ставкой на
t-485 (нарушает инвариант 2 на окно). Р7 сайдкар-реестр прогонов
logs/calibration_prepass.jsonl: A да, минимальный (реком. — машинный
счёт «ПУСТО ПОДРЯД» для саморевизии, вход W3) / B нет. Р8 композиция
предикатов: A нет, manual: (реком.) / B AND/OR (свой парсер и
батарея). Р9 кто коммитит: A builder отдаёт дерево, коммит с осями и
tier: — Lead (реком.; tier builder-сессии отвергнет гейт, «не-механизм»
был бы неправдой) / B builder с продиктованными осями (ложная
tier-декларация, класс F-36/F-29). Р10 notes: A строки «ПУСТ» дословно
в notes (реком.; ~2 КБ в бюджет 15000) / B ссылкой на файл (носитель
вне журнала, класс F-48).

## В. Мелочи, закрытые драфтом

Имена calibration_prepass/test_ (семья, тест под финальным именем —
прецедент W1 Б7); маркер <!--CHK … -->, регексп
^\s*<!--CHK\s+(\S+?)\|(.*?)-->\s*$; заголовок чека ^\s*(\d+)\.\s+\*\*;
подпункты прозой не парсятся — id несёт шапка; диапазон до следующего
заголовка или «## Завершение прогона»; CRLF→LF, лимиты в UTF-8-байтах;
вывод --check-form — строка на дефект, префикс FORM; git -C из кода;
порядок src значим, rules — нет; --window-end по умолчанию — системное
время запуска (печатается).

## Г. Замеченные расхождения и аналоги (D-0043)

1. norm_retire.py/test на диске ОТСУТСТВУЮТ (позитивный контроль:
   глоб 10 имён → 9; листинг tools/test_*.py 45 файлов без
   test_norm_retire) — временно (t-485 в полёте); спека, опирающаяся
   на них как живого сторожа, опирается на обещание — K4.
2. Обязанность «САМОРЕВИЗИЯ ПРОТОКОЛА» (заказ оператора 08-14) живёт
   ТОЛЬКО в notes calibrated 08-14 — в тексте протокола её нет (файл
   прочитан целиком). Класс F-48. Форма A1 её вмещает; заведение — ход
   Lead.
3. Аналог класса t-483: парсеры, пришпиленные к форме
   (test_findings_form, test_sibling_map_liveness); пре-пасс — третий
   такой читатель, но по КОММЕНТАРНОЙ шапке — смягчение класса;
   отметить в реестре режимов — ход Lead.
4. Кит-пара протокола уже в hq-drift (manifest 731–734; notes №7);
   порт-батч везёт файл СОДЕРЖИМЫМ (D-0091/D-0093).
5. Подозрение снято: чек 30(а) «"judge" в whitelist» НЕ протух
   (JUDGE_BASIS_VALUE:305, энум 594–610); BASIS_VALUES:304 — уже
   справка, не проверка. Ложной находки не завожу.

## Д. След чтения

Целиком: PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md (1175 строк, 4
захода), docs/RULE_COVERAGE.md, носитель фазы, w1-retirement-spec,
PROCESS/README.md, tools/calibration_counts.py. Точечно:
docs/RETRO_PATTERNS.md 1–60; mechanism_gate 90–119 + грепы
tier|rank|lead; journal_validator грепы констант (276, 296–306) и
judge (105–692); журнал строка последнего calibrated. Позитивные
контроли негативов: (i) глоб 10 имён → 9 найдено; листинг test_*.py 45
файлов; (ii) греп WEEKLY_CALIBRATION_PROTOCOL по tools/ — попадания
есть (комментарии/payload/манифест), ни одно не чтение файла; (iii)
глоб toolkit/PROCESS/*.md → 11 файлов, включая протокол. НЕ
ПЕРЕМЕРЕНО: 115 074 Б — число носителя фазы (у роли нет Bash); строки
и границы сверены чтением; байтовые оценки производны от 98 Б/строка —
ключ 7 требует фактических чисел от builder.

---
---

# АМЕНДМЕНТЫ t-489 (приняты Lead 08-18; заменяют заякоренные секции драфта)

Сверено чтением перед правкой: norm_retire.py/test на диске; чек 12
переписан (паритет делегирован в I3/I4) — все номера строк протокола
от чека 13 сдвинулись на +4 (чек 13 теперь со строки 312). Номера
строк критика датированы — подтверждение решения «диапазоны вычисляет
инструмент, номера строк не хардкодятся» (AM-1, AM-10).

## AM-1 — заменяет A1 (носитель, позиция, поля, лимиты)

Носитель — однострочный HTML-комментарий `<!--CHK …-->` НА СВОЕЙ
ОТДЕЛЬНОЙ СТРОКЕ.

П1. Позиция шапки: первая строка ПОСЛЕ строки с ЗАКРЫВАЮЩИМ `**`
жирного заголовка чека. Заголовок может быть многострочным — таковы
16 чеков из 34: 13, 14, 17, 18, 19, 21, 22, 23, 24, 25, 26, 27, 28,
31, 32, 33 (у чека 21 — четыре строки). Прежняя формулировка «первой
строкой тела сразу под строкой заголовка» ОТМЕНЯЕТСЯ (поставила бы
комментарий внутрь незакрытой пары `**`).

П2. Класс шире экземпляра (R9/D-0100, подметание подпунктов): маркер
`(х)` НАЧИНАЕТ строку у чеков 15, 17, 25, 26, 31 и большинства
подпунктов 11, 13(г…л); ВПЛЕТЁН В АБЗАЦ у 13(а)/13(б)/13(в)/14(а)/
18(а)/27(а)/28(а)/29(а)/30(а)/32(а). Правило: под-шапка ставится
ТОЛЬКО перед подпунктом, чей маркер начинает строку; вплетённый
подпункт под-шапки не получает и НАСЛЕДУЕТ предикат/статус родителя;
нормализация переносами в M1 не делается (развилка Р12 → решение
Lead: (A) наследование, гибрид — кандидат W2c).

П3. Правило --check-form (новое, обязательное): шапка не внутри
незакрытой пары `**` (счёт `**` от начала чека до строки шапки;
нечётное → FORM-ошибка); шапка занимает строку целиком; между
закрывающим `**` и шапкой нет других непустых строк, кроме случая
П2-наследования.

Поля (фиксированный порядок): CHK <id> | src: (1..4) | pred: (один)
| rules: (1..4) | status: (живой / ретирован:<дата>;сторож:<id>;
живость:<команда> / деферред:<предикат>) | cand: (НОВОЕ, опц.) |
since: (опц.) | note: (опц., свободная пометка).

Грамматика cand: (форма ОТДЕЛЬНОГО ПОЛЯ, не подграмматика note):
значение — предикат из закрытого списка A3, разбираемый тем же
парсером. --check-form требует: (i) cand: только при status:живой;
(ii) предикат резолвится; (iii) note:, содержащий подстроку «триггер»,
— ОШИБКА ФОРМЫ (триггер живёт ровно в одном поле). Поведение: cand:
не влияет на живость; истинный предикат → строка «ПЕРЕВЗВЕДЕНИЕ: чек
<id> — кандидат-ретирования, триггер истинен -> находка прогона».

Лимиты (тест на границе и за ней): строка шапки ≤300 Б UTF-8 (реальная
шапка-пример = 123 Б); значений src ≤4; полей ≤8; шапок на id ≤1;
id ≤12 символов.

## AM-2 — заменяет вывод A3; добавляет A3.8

Каждая строка ПУСТ несёт НАБЛЮДЁННЫЕ счётчики предиката:
`<предикат> -> <наблюдённое> из <объём материала>` («13(б) ПУСТ:
journal.event:defect_found -> 0 из 111 строк окна»). Голое «0» без
знаменателя запрещено (класс F-30).

A3.8. КОНТРОЛЬНЫЙ ПОЛНЫЙ ПРОЧИТ — именованный детектор инварианта
«предикат — необходимое условие» (без него инвариант — пожелание,
D-0049): (а) периодический триггер N=4 (каждый четвёртый прогон; цена
= один прогон «как раньше», верхняя граница ущерба от ложного
предиката — четыре окна); (б) событийный триггер сильнее: первый
прогон ПОСЛЕ любого коммита, тронувшего строки `<!--CHK` протокола
(пре-пасс сам смотрит git log); (в) обязанность контрольного прогона:
чек-лист целиком, каждый ПУСТ-вердикт сверяется с фактом, в notes
calibrated строка «контрольный прочит: N ПУСТ-вердиктов, расхождений
k» (ноль — тоже явно); расхождение = defect_found, failure_class=spec,
ref на приёмку шапки; предикат возвращается в always тем же ходом.
Пре-пасс печатает шапкой РЕЖИМ: обычный (до контрольного k прогонов) /
КОНТРОЛЬНЫЙ (триггер: …).

Строка для RULE_COVERAGE (посадка — ход Lead тем же коммитом M1):
«Phase 5 W2a: прогон читает только живые чеки; предикат — необходимое
условие непустоты | ГЕЙТ ФОРМЫ + ДИСЦИПЛИНА+ДЕТЕКТОР | форму держит
--check-form в каноне (test_calibration_prepass); отказ предиката
ловит КОНТРОЛЬНЫЙ ПОЛНЫЙ ПРОЧИТ (каждый 4-й прогон И первый после
правки шапок): строка в notes, расхождение = defect_found + возврат
предиката в always; отсутствие строки = чек не выполнен».

## AM-3 — заменяет предикат внешнего деплоя (фикс 5)

`deploy.exists:<имя>` вместо path.exists с абсолютным путём; в шапках
§5 — `pred:deploy.exists:ao3`. Резолюция по порядку, первый успех:
(1) CLI `--deploy <имя>=<путь>` (повторяемо); (2) ключ deploys.<имя>
в delegation.config.yaml — ТЕМПОРАЛЬНЫЙ КРАЙ: ключа сегодня НЕТ
(файл прочитан целиком), заведение — ход Lead посадочным коммитом M1
(решение Р11(A)); (3) env OSLLM_DEPLOY_<ИМЯ>; (4) не разрешено →
предикат НЕРАЗРЕШИМ → чеки секции ЖИВЫ (fail-closed) + строка «деплой
не сконфигурирован — секция читается целиком»; (5) имя разрешено,
путь отсутствует → чеки ПУСТЫ + позитивный контроль той же формы по
корню нашего репо (гигиена п.6); (6) захардкоженный деплой-путь в
исходнике инструмента = дефект, ловится тестом (греп по собственному
исходнику — негативный кейс).

## AM-4 — заменяет строки K2/K4/K5 таблицы A4 (мир «сторож жив»)

K2 — оформить ретированным подпунктом (сторож
test_sibling_map_liveness, живость pytest). K4 — ретирование ЛЕГАЛЬНО
СРАЗУ, cand: не нужно: norm_retire посажен 560a727, текст чека 12 уже
делегирует паритет в I3/I4; работа W2a — только форма
(status:ретирован;сторож:norm_retire(I3,I4);живость:python
tools/norm_retire.py --check). K5 — не ретировать, расщепление к Lead
(note: без слова «триггер» — AM-1 П3(iii)). Первого живого носителя
cand: в пилотной партии НЕТ (оба кандидата закрыты фактом); поле
вводится грамматикой + синтетикой; первый живой носитель — будущий
чек 34 (W3). Итог снятия ретированием: K1+K2+K3+K4 ≈ 0.7 КБ (было
«≈1 КБ» — чек 12 Lead уже сжал сам).

## AM-5 — заменяет край A8.2; добавляет край A8.11

A8.2: кандидат без живого сторожа → status:живой + cand:<предикат>
(грамматика AM-1); запись триггера в note: запрещена формой; мир
«сторож появился, поле не снято» — НЕ ошибка формы, а находка, ради
которой поле заведено (снятие cand: и перевод в ретирован — ход Lead
следующим прогоном). A8.11 (новый): ДИАПАЗОНЫ СТРОК ПРОТУХАЮТ —
номера строк не хранятся нигде (шапки/спека/notes), вычисляются
пре-пассом на каждом прогоне; прецедент: коммит 560a727 сдвинул все
строки от чека 13 на +4 между приёмкой драфта и амендментом.

## AM-6 — заменяет A6 ЦЕЛИКОМ (замер критика вместо оценок)

База ≈115.1 КБ (датирована: 560a727 добавил ~+0.4 КБ после замера).
Есть сейчас: гиганты 26+13+18 = 39 727 Б = 34.5%; всегда-живой пол
(строго always/manual/script) = 52 815 Б = 45.9%, с подпунктами
11/29/30 = 53.2%; пусто в РЕАЛЬНОМ окне (111 событий с 08-14) —
только чеки 16/20/32 = 3 659 Б = 3.2%. Добавляет форма: 34 шапки ×
123 Б = 4 182 + ~25 под-шапок ≈3 075 + правило A7 ≈1 200 + секции
≈300 = +8 757 Б; снятие ретированием ≈ −700. НЕТТО: +8.1 КБ →
~123.2 КБ (+7%).

Вывод прямо: (1) файл РАСТЁТ ~7%, байтового выигрыша у W2a нет;
(2) в busy-окне чтение почти не падает (3.2%, прежняя оценка «18%»
завышала в шесть раз — снята); (3) выигрыш структурный: адресные
диапазоны, пустые чеки без чтения, ИЗМЕРИТЕЛЬ в строке ИТОГ;
(4) гейт (а) ≤57.5 КБ формой НЕ БЕРЁТСЯ ПО ПОСТРОЕНИЮ: пустое окно
упирается в пол 61.3 КБ + шапки ≈2.5 КБ ≈ 63.8 КБ (+6.3 КБ над
гейтом), busy-окно ≈111 КБ; требование к W2c: для пустого окна снять
≥6.3 КБ из всегда-живого пола, для busy — порядка 50 КБ (три гиганта
39.7 КБ + часть прочего). W2a — предусловие W2c (адресность +
измеритель), не замена; (5) строка ИТОГ печатает абсолют и вердикт:
«ИТОГ: живых 31/34 · к чтению 111 402 Б из 115 149 Б (96.7%) · гейт
(а) ≤57 500 Б: НЕ ВЗЯТ (+53 902 Б)».

## AM-7 — дополняет ключ 5 и батарею A10

--check-form дополнительно: шапка не внутри незакрытого `**`; шапка —
строка целиком; под-шапка только у маркера с начала строки; cand:
только при живом + резолвится; note: без подстроки «триггер»;
deploy.exists резолвится по цепочке AM-3 или честно неразрешим; в
исходнике нет захардкоженного деплой-пути. Батарея +кейсы: шапка
между строками многострочного заголовка; шапка в конец строки с
текстом; под-шапка перед вплетённым маркером; cand: при ретирован;
cand: с неизвестным предикатом; note: со словом «триггер»;
deploy.exists без конфигурации (ЖИВ + строка) и на несуществующий
путь (ПУСТ + позитивный контроль); --deploy с пустым значением; ПУСТ
без знаменателя (тест формата вывода); контрольный режим по обоим
триггерам. Прогон дополняется командой:
python tools/calibration_prepass.py --window-start
2026-08-14T12:12:34 --control

## AM-8 — заменяет pred чеков 15/17/27/28/33 в A2

`pred:deploy.exists:ao3` у всех пяти.

## AM-9 — правки given A11

Добавить: delegation.config.yaml (ключа deploys НЕТ — шаг 2 резолюции
сегодня промахивается по построению); tools/norm_retire.py +
tools/test_norm_retire.py (живые сторожа K4). Убрать: строку про
каталог D:\AO3_tests — внешний деплой только через --deploy ao3=<путь>
в проверочном прогоне.

## AM-10 — вставка в A5

Номера строк нигде не фиксируются — ни в шапках, ни в отчёте builder
как якоря (только как замер), ни в notes. Единственный владелец
диапазонов — пре-пасс на момент прогона.

## Новые развилки (решены Lead 08-18)

Р11 носитель карты деплоев → (A): ключ deploys: в
delegation.config.yaml (читают уже три гейта, ось 2 «один носитель на
факт»); правка носителя — акт Lead, заводится посадочным коммитом M1.
Р12 вплетённые маркеры → (A): наследование, переносов в M1 нет;
гибрид (C: нормализация внутри гигантов 13/26) — кандидат W2c.

## Задето косвенно

A2 (pred пяти чеков), A4 (K2/K4/K5, снятие 1.0→0.7 КБ), A6 (заменён
целиком), A7 (формулировка позиции шапки — П1/П2 при посадке), A8
(край 2 переписан, +край 11), A9 (пример чека 34 — первый живой
носитель деферред; cand: не использует), A10 (+ключ 5, батарея,
--control), A11 (given), Р6 снята как беспредметная, гейт (а) — в
абсолюте, измеритель — строка ИТОГ с вердиктом.
