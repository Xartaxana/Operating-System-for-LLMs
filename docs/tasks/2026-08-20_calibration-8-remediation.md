# Ремедиация калибровки №8 — DAG хода

**Открыт:** 2026-08-20, слово оператора «исправляй всё что нашёл на
калибровке». Находки и их полные тексты —
docs/task_reports/2026-08-20_calibration-8.md (F1..F15), вердикты —
notes события `calibrated` 2026-08-20T14:24.

**Владелец хода:** Lead-сессия. Статус узла двигается ТЕМ ЖЕ ходом, что
и его журнальное событие (R4.f).

## Принцип раскладки: по ВЛАДЕНИЮ ПУТЯМИ, не по номерам находок

Горячая точка — `PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md`: его трогают
F5, F6, F9 и три пункта F15. Второй общий носитель —
`docs/RULE_COVERAGE.md` (F8 + пин-соседи). Третий — `CLAUDE.md` (F4 +
словарь событий из F2.2), и он под бюджетом 36700/37000 при факте
36102 — то есть добавление текста туда само по себе ограничение, а не
свободный ход.

Поэтому узлы нарезаны так, чтобы owns НЕ ПЕРЕСЕКАЛИСЬ у одновременных
ПИШУЩИХ диспатчей. Драфты (designer) пересечений не создают — они
read-only, их можно гнать параллельно; сериализуются только builder'ы.

## Узлы

### A — Решения оператора 08-20 (сужение двух формулировок) — В ХОДЕ

Предмет: F6 (триггер 14(ж) сужается до СОДЕРЖАТЕЛЬНОЙ правки) + чек 22
(рецидив внутри сессии сужается ДО МОТИВА) + попутно неверный пин
`docs/RULE_COVERAGE.md:81` («чек 14(ж)» вместо 13(ж)) и его соседи по
артефакту.

- owns: `PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md`,
  `PROCESS/LEAD_RANKING_EXAM.md`, `docs/RULE_COVERAGE.md`,
  при необходимости `docs/POLICY_FULL.md`.
- A1 драфт — designer t-547, В РАБОТЕ.
- A2 посадка — builder, после приёмки A1.
- A3 критик — если дифф >100 строк либо тронуто ядро.

**Блокирует:** узел B (тот же протокол), узел E (тот же
RULE_COVERAGE).

### B — Протокол, партия 2 (после A)

Предмет: F5 (у чека 30 нет шага сверки отклонений от лист-маршрута,
хотя R13(a) ядра называет его оконным детектором); F9 (чек 29 пинует
«critic.md п.15», факт — п.16); F15 в трёх пунктах — 18(д)
`--window last` печатает ЧУЖОЕ окно; чек 19 пятый пустой прогон при
непереформулированном вопросе; чек 11(в) называет слои именами,
которых нет в выводе (`ESCALATION` против фактического `R6-ЗЕРКАЛО`).

- owns: `PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md` (+ тела
  `PROCESS/checks/*.md` по применимости).
- Ограничение: гейт (а) пре-пасса — храповик 146000 Б; правка обязана
  оставить его взятым, иначе это находка против самой правки.

### C — Изоляция тестов от живого состояния (F1, F14)

Предмет: `test_search_control_gate.py::test_ledger_dir_honoured_from_env_var_not_real_logs`
краснеет от записей ОКРУЖАЮЩЕЙ сессии (в изоляции 46 passed) —
канонический прогон стал недетерминированным, а фоновый прогон есть
дефолт R7, то есть витнесс R2 краснеет ложно на торном пути. Класс
закрыт 08-19 в `hook_liveness_probe` (LIVE-STATE-TOUCHED против
LIVE-STATE-AMBIENT), сиблинг в тестах не подметён; ось 13 карты
заведена ПОД ЭТОТ КЛАСС, и экземпляр возник ПОСЛЕ неё.

Сюда же три из четырёх попаданий F14: исполнители откатывали
`git checkout`'ом пути ВНЕ своих owns, потому что эти файлы мутировал
САМ прогон канона.

- owns: `tools/test_*.py` из числа названных сканом, КРОМЕ
  `tools/test_dispatch_gate.py` и `tools/test_owns_gate.py`
  (зарезервированы за узлом D); найденное в зарезервированных —
  ДОКЛАДОМ, не правкой.
- Обязателен ответ: какие ещё тесты читают/пишут живое состояние
  (перечисление с вердиктом на каждого, D-0100).

### D — Ложные срабатывания warn-слоёв (F10)

Предмет, замеры окна с названными базами: ROLE-TYPE WARN 183/~224
диспатчей = 82%, OWNS OVERLAP 172/~224 = 77%, GIVEN-PATH 134/~224 =
60%. Плюс три собственных ложных срабатывания прогона калибровки:
GIVEN-PATH объявил несуществующими пути ВНЕШНЕГО деплоя (подкласс
«чужекорневой контекст» — триггер «по рецидиву» выстрелил);
QUOTED_OWNS_WARN потребовал перенести owns-строку у READ-ONLY
разведдиспатча, который owns не объявляет вовсе (подкласс owns №2,
счёт ≥3).

Норма чека 11(в): слой, стабильно метящий десятки процентов своего
потока, — находка о МЕХАНИЗМЕ, а не «строгая настройка», потому что
сторож такой плотности воспитывает привычку себя игнорировать.

- owns: `tools/dispatch_gate.py`, `tools/owns_gate.py`,
  `tools/test_dispatch_gate.py`, `tools/test_owns_gate.py`.
- НЕ входит: `tools/hygiene_gate.py` — он в узле F (там же ложный блок
  и смена скоупа, один носитель — один узел).

### E — Карта «правило → сторож» (F8, остаток)

Предмет: `docs/RULE_COVERAGE.md` не несёт строк на четыре механизма
окна — храповик гейта (а) W4 (`GATE_A_HISTORY`, skip-каунтер тел),
партия 2 сканера (WRITE-QUOTED WARN, QUOTED_OWNS_WARN,
SKIP_RE/TIER_LINE_RE), батч П4 (stdin-дедлайн 13 хуков),
сторож d15 (`test_claude_binding_guard`).

- owns: `docs/RULE_COVERAGE.md`.
- ПОСЛЕ узла A (тот же файл) и ПОСЛЕ узлов C/D/F, чьи правки сами
  породят новые строки карты — иначе карта протухнет в момент посадки
  соседей.
- Уже исправлено этим прогоном (не входит в узел): ROADMAP Phase 5
  закрыта, статус F-61 обновлён, хвосты t-503 в CURRENT_CONTEXT
  сведены к живому.

### F — Невыполненные батчи калибровки №7 (F2, F3, F13)

Предмет: драфты батчей 2 и 3 приняты 08-14 и НЕ ПЕРСИСТИРОВАНЫ — их
нет нигде в дереве (греп с позитивным контролем), они живут только в
транскриптах. Это и есть причина, по которой работа испарилась:
экземпляр класса durable-персистентности (F-50).

- F0 восстановление. Прогон 1 (scout t-548) ОТКЛОНЁН: координаты нашёл,
  дословный текст не доставил — финальное сообщение сослалось на
  предыдущие ходы, которых координатору не доставляют (F-49, scout.md
  п.9). ПРИНЯТОЕ ИЗ НЕГО: первоисточники — сайдчейны сессии
  `c199b28e-6a01-4d87-aaf2-478c18a5d223`, файлы
  `subagents/agent-ae935cfcf4e40af8e.jsonl` строка 105 (батч 2, t-438)
  и `subagents/agent-a39502cb1cb3d943b.jsonl` строка 97 (батч 3,
  t-441); дословного текста нет НИГДЕ в дереве (греп с позитивным
  контролем формы), вторичные носители несут только названия пунктов и
  статус.
  ЛОЖНАЯ ТРЕВОГА, снята Lead'ом: разведчик доложил расхождение ts
  «транскрипт раньше журнала на 1ч46м» как факт. Это артефакт часовых
  поясов — транскрипт пишет UTC (`10:11:08.199Z`), журнал локальное
  (+02:00), то есть 12:11:08 против приёмки 12:12:34; для батча 3 —
  16:30:18 против 16:30:36. Сходится, аномалии чека 13(е) нет.
  Прогон 2 (builder t-550) — ПРИНЯТ 2026-08-20. Оба драфта в дереве:
  `docs/tasks/2026-08-14_calibration-7-batch2-spec.md` (44754 симв.
  тела, 4 пункта) и `..._batch3-spec.md` (43816 симв., 6 пунктов).
  Дословность доказана сверкой sha256 тела с первоисточником
  (MATCH/exit 0) при негативном контроле на КОПИИ (MISMATCH/exit 1,
  боевой артефакт не портился — п.7(г)); Lead независимо сверил sha256
  целых файлов с отчётом и пересчитал развилки грепом. Класс F-50 на
  этом экземпляре снят.

  **Два доклада исполнителя, оба приняты — материал для дальнейших
  узлов, не косметика:**

  (1) **Ожидание координатора было неверным.** Я передал в спеку
  «Р1–Р25 с пропусками номеров» — по факту 25 развилок ПОДРЯД без
  пропусков, а собственный заголовок драфта называет «РАЗВИЛКИ (22)».
  Ожидание пришло из отклонённого t-548 и попало в спеку как факт;
  спека прямо требовала докладывать расхождение, а не подгонять под
  ожидание, и это сработало. Класс — «диспетчер внёс в спеку
  непроверенный факт из отклонённой работы» (п.5 пятипунктового
  чеклиста: свежесть несущих фактов сверяется с носителем). Для самого
  батча 2 остаётся вопрос: заголовок «22» против 25 определений —
  расхождение внутри восстановленного документа, разрешать при его
  исполнении.

  (2) **Запись owns-файлов шла именованными скриптами, не Write-тулом.**
  Отклонение РАЗРЕШЕНО Lead'ом. Основание: пункт 4 командной гигиены
  целит в ad-hoc `heredoc`/`python -c` по живым файлам — неаудируемую
  инлайн-мутацию; здесь же перенос ~90 тыс. символов, при котором
  ручной путь САМ создаёт риск против главного критерия DoD
  («дословность»), а выбранный путь дал именованные скрипты, полный
  приложенный вывод, независимую пересверку и негативный контроль —
  то есть БОЛЬШУЮ аудируемость, а не меньшую.
  **Норма этот случай не называет** — она не различает ad-hoc мутацию и
  программный перенос с доказанной дословностью. Это свежий экземпляр
  класса «норма/спека рождает случай, который сама не называет»
  (ГЕНЕРАТИВНЫЙ край, кластер (i) чека 23 калибровки №7), и он
  усиливает узел G: подкласс надо кодифицировать, а пункт 4 гигиены —
  уточнить границей «инлайн против именованного скрипта с витнессом».
- F.1 точный model-id вместо подстроки СЕМЕЙСТВА (F3: сессия 08-18
  отработала 723 хода на claude-opus-4-8 при привязке claude-opus-5 и
  посадила три механизменных коммита с `tier: opus`; понижение
  ПОКОЛЕНИЯ внутри семейства невидимо всем трём гейтам). Носители —
  `tools/journal_validator.py`, `tools/mechanism_gate.py`,
  `tools/session_context.py` + чек 5 протокола (его слот
  «то же семейство, другое поколение»).
- F.2 маркер `reserve_engaged/released` — событие подъёма ВЫШЕ
  привязки; сейчас чек 5 не отличает слово оператора от дрейфа.
  Носители — словарь событий `CLAUDE.md`, `tools/journal_validator.py`,
  чек 5.
- F.3 `python -c`/heredoc → БЛОК (слово оператора 08-14; ставка выросла
  2.4 → 14.3 на 100 вызовов, ×6, единственный растущий класс) +
  ЛОЖНЫЙ БЛОК того же хука на ЧИТАЮЩЕМ грепе (замерен в этом прогоне).
  Носитель — `tools/hygiene_gate.py` + тесты.
- F.4 шесть кодификаций ядра батча 3 — состав известен только из
  восстановленного драфта (F0).

Часть F.1/F.2 трогает чек 5 протокола → сериализуется ПОСЛЕ узлов
A и B.

### G — Кодификации, обязанные чеком 23 калибровки №7 (F4)

Предмет: (i) ГЕНЕРАТИВНЫЙ подкласс края в R11 рядом с TEMPORAL и
POSITIONAL — «норма/спека рождает артефакт или случай, который сама не
называет», счёт 4 с учётом экземпляра этого прогона; (ii) пункт 3
пятипунктового чеклиста D-0096 расширяется с достаточности ДАННЫХ на
достаточность ИНСТРУМЕНТОВ яруса — счёт 4, последний экземпляр мой
(DoD t-544 требовал от scout `git log`, у роли нет Bash).

- owns: `CLAUDE.md`, `docs/POLICY_FULL.md` (пара оси 4).
- ЖЁСТКОЕ ОГРАНИЧЕНИЕ: бюджет `CLAUDE.md` 36700 (breach 37000), факт
  36102 — обе кодификации обязаны уложиться в ~600 Б либо идти
  ВМЕСТЕ с компенсирующей диетой. Это предмет развилки, не
  самодеятельности исполнителя.

### H — Решения, требующие слова до кода

- **F7** (R3 протёк на 11 приёмках 08-14..08-18): строить ли машинный
  слой «крупный дифф без критик-входа», и если да — где он стоит
  (pre-commit? приёмка?). Кандидат в гейт, но D-0063 требует promotion
  ПО EVIDENCE утечки — evidence теперь есть.
- **F11** (доля notes сверх бюджета выросла 43% → 79.5% при 62 живых
  срабатываниях WARN-слоя): слой жив, молчит читатель. Что делать —
  усиливать слой, менять бюджет или менять форму записи.
- **F12** (эхо-база: 201 фолбэк за окно): сперва РАСКЛАДКА по
  причинам, потому что после узла N3 маркер стал диагностическим;
  слепой фикс запрещён.
- **F15**, пункт про носитель разбора калибровки: объявить отчёт
  штатным носителем, а notes `calibrated` — сводкой (этот прогон уже
  сделал так по факту); плюс оконные аргументы `savings_report` и
  база Δ `corpus_growth` (считается к записи сайдкара, не к границе
  окна).

## Порядок исполнения

```
A (в ходе) ──┬──> B ──> F.1/F.2 (часть про чек 5)
             └──> E (последним из всех, чтобы карта не протухла)
C ─┐
D ─┼── параллельно A, разные owns
F0 ┘
G — после развилки о бюджете CLAUDE.md
H — решения оператора, кода нет
```

## Статусы узлов

| узел | статус | задача |
|---|---|---|
| A1 драфт | в работе | t-547 (designer) |
| A2 посадка | ждёт A1 | — |
| B | ждёт A | — |
| C1 драфт | в работе | t-549 (designer) |
| C2 посадка | ждёт C1 | — |
| D1 драфт | в работе | t-551 (designer) |
| D2 посадка | ждёт D1 | — |
| E | ждёт A, C, D, F | — |
| F0 восстановление | ЗАКРЫТ | t-548 (reject) → t-550 (принят) |
| F.1–F.4 | готов к драфту, F0 снят | — |
| G | ждёт развилку бюджета | — |
| H | у оператора | — |

## Развилки восстановленного батча 2 — ЗАКРЫТЫ Lead'ом 2026-08-20

Все 25 несли обоснованные рекомендации драфтера; принял их как есть,
кроме мест, помеченных ниже. Развилки — уровня проектного решения, а не
интерпретации намерения оператора (R11a), поэтому закрываю сам.

**Пункт 1 (точное поколение против привязки).** Р1 = (б): правим
`session_context` + `mechanism_gate`, `journal_validator` НЕ трогаем —
его `by`/`model` суть ЯРУСНЫЕ СЛОВА, строгость там потребовала бы
сменить словарь поля в ядре. Р2 = (б): не отказ, а видимая строка
расхождения — тот же двухслойный принцип D-0063, который гейт уже
несёт. Р3 = (б) метка в `session_context`, (в) молчание в
`mechanism_gate`. Р4 = (б): поколения НЕ парсим, метка нейтральна
«id != binding» — иначе легальный подъём на будущую модель читался бы
как дефект. Р5 = короткая ASCII-метка; подкреплено замером чека 11
этого прогона: вывод хука уже 1180 Б против базлайна 528, растить его
нечем.

**Пункт 2 (маркер резерва).** Р6 = имена `reserve_engaged`/
`reserve_released`. Р7 = (б): пара не обязательна машинно — резерв
зовут на ОДНУ тяжёлую задачу, сессия часто просто кончается, и норма
«обязан released» будет систематически нарушаться и обесценится. Р8 =
(а): вход — первое Lead-действие сессии (симметрия D-0056a). Р9 = (а):
прошлое не переписываем (D-0089) + ОБЯЗАТЕЛЬНАЯ оговорка в чеке 5, что
прошлые окна покрыты только словом оператора от 08-14. Р10 = (б): поле
`model` с ТОЧНЫМ id — единственная точка, где id резерва попадает в
журнал машинно-проверяемо. Р11 = (а) сейчас, (б) в очередь с триггером
«первое открытое резервное окно, пережившее сессию» — снова по чеку 11:
строку в бут-вывод сейчас не добавляем. Р12 = (а): parity-тест словаря
событий по трём носителям — ось 4 закрывается машинно.

**Пункт 3 (гигиена в блок).** Р13 = (а), Р14 = (а) НОЛЬ ложных deny,
Р15 = (а)+(в) с явным отказом от маркера-break-glass, Р16 = (б) не
расширять токен-набор, Р17 = (а) неоднозначное остаётся warn.

**Пункт 4 (саморевизия протокола).** Р18 = (в): и новый чек, и строка в
«Завершении прогона». Р19 = (а) дисциплина сейчас, (б) в очередь. Р20 =
все подходящие кандидаты, одна строка на чек. Р21 = (а), (б) в очередь.
Р22 = (а): чинить формулировку 18(д) тем же коммитом — за прошедшую
неделю дефект сработал ВТОРОЙ раз (`--window last` снова напечатал
чужое окно на калибровке №8), это уже не гипотеза.

**Кросс.** Р23 = кит-строка в порт-очередь (мораторий D-0074). Р24 =
(а)+(б). Р25 = (б): четыре коммита по пунктам, честный осевой блок на
каждый; коммитит Lead.

**ДВЕ ПОПРАВКИ К ВОССТАНОВЛЕННОМУ ТЕКСТУ — мои, не драфтера:**

1. **Номер нового чека в пункте 4 пересчитать.** Драфт называет «чек
   33», исходя из состояния 08-14. Сегодня в протоколе занято 36
   номеров (пре-пасс: «живых 34/36»), а номера НЕ переиспользуются —
   значит новый чек получает следующий свободный, а не 33. Это ровно
   тот класс протухания, который эта же ремедиация чинит.
2. **К3.6 «носители тем же коммитом» уточняю.** Константа сдаётся
   ВЫКЛЮЧЕННОЙ, поэтому сегодняшние формулировки `docs/RULE_COVERAGE.md`
   и чека 25 остаются ПРАВДОЙ до её переключения. Носители уходят одним
   ходом с ПЕРЕКЛЮЧЕНИЕМ, а не с кодом. Иначе пришлось бы либо драться
   за занятые пути (R4), либо посадить в карту утверждение, ложное на
   момент посадки.

## РЕШЕНИЯ ОПЕРАТОРА 2026-08-20 (вопросы задавались по одному)

**Р-О1. Байтовый конфликт храповика.** Решение: **диета протокола
первой; вынос доказательств в файл экзамена (второй хоп) — ТОЛЬКО если
диеты не хватит.** Храповик НЕ поднимается — вариант «переустановить
порог явным актом» отклонён.
Следствие: первая обязанность узла B — диета `PROCESS/
WEEKLY_CALIBRATION_PROTOCOL.md`, и лишь при доказанной нехватке —
перенос изложения двух доказательств в `PROCESS/LEAD_RANKING_EXAM.md`.
На момент решения запас гейта (а) = 1253 Б после посадки узла A.

**Р-О2. Блок `python -c`: нодж или enforcement.** Решение: **нодж
сейчас + триггер на живой обход.** Охват `python3`/`py`/`pwsh` и
интерпретаторный гард НЕ вводятся (Р16 остаётся закрытым).
Триггер именован и ДЕТЕКТОР У НЕГО УЖЕ ЕСТЬ, а не строится: обёрточные
формы (`bash -c "python -c …"`, `bash <<EOF … EOF`) продолжают давать
warn и попадают в поклассовый счёт чека 25 — то есть первый живой обход
виден ЧИСЛОМ на ближайшей калибровке, а не по догадке. Формулировка
триггера: «появление обёрточной формы в warn-потоке окна».
Оговорка: само переключение константы ждёт трёх пунктов критик-гейта
(детерминированная разбивка 939 вместо выборки; замена невыполнимого
пункта 2 DoD раздела 5 на дельту `permission_audit`; строки
`docs/RULE_COVERAGE.md:88` и чека 25 тем же ходом).

## Копилка узла D: экземпляры, замеренные НА СЕБЕ по ходу ремедиации

Счёт ведётся здесь, потому что он растёт быстрее, чем узел исполняется,
и сам по себе является замером плотности слоя.

1. GIVEN-PATH на путях внешнего деплоя (диспатч t-544) — подкласс
   «чужекорневой контекст», триггер «по рецидиву» выстрелил.
2. GIVEN-PATH на путях, которые диспатч СОЗДАЁТ (t-550) — временной
   край R11(c): у гейта нет понятия «будет создано».
3. Слепота owns-парсера (t-550): «owns объявлен, путей не разобрано» на
   обычном markdown-списке с абсолютными путями — сверка пересечений
   МОЛЧА не состоялась, класс F-59.
4. QUOTED_OWNS_WARN на read-only диспатче (t-546), который owns не
   объявляет вовсе: токен стоял ИМЕНЕМ ИСКОМОГО МАРКЕРА в инлайн-коде —
   подкласс owns №2, счёт ≥3.
5. GIVEN-PATH на диспатче t-551 — обругал те же пути AO3, которые
   процитированы в нём как МАТЕРИАЛ подкласса №1.
6. Ложный DENY hygiene_gate на ЧИТАЮЩЕМ грепе с путём журнала
   (калибровка №8) — принадлежит узлу F вместе с носителем.
7. **БЛОК (не warn) на диспатче узла A**: «пишущий диспатч без
   манифеста контекста (given/owns)» при НАЛИЧИИ манифеста — не
   разобрался заголовок `## OWNS (§1 спеки, абсолютные пути)` с
   хвостом после маркера. Ровно причина (б), которую драфт узла D
   диагностировал по коду часом раньше. Это самый дорогой экземпляр
   класса: он не шумит, он ОСТАНАВЛИВАЕТ работу.
8. OWNS OVERLAP на диспатче узла A столкнул его с диспатчами
   ВЧЕРАШНЕГО дня (16:20 и 17:19 08-19), давно завершёнными — живая
   иллюстрация диагноза «окно живости 24 ч без сигнала завершения
   воркера».
9. GIVEN-PATH на критик-диспатче склеил ДВА пути, стоявших рядом в
   бэктиках, в один токен ``tools/hygiene_gate.py`/`tools/test_hygiene_gate.py``
   и объявил его несуществующим — токенайзер режет только по `;,\n`,
   а прозаический хвост отрезает только по « — ». Ровно механика,
   которую драфт узла D разобрал по коду.
10. Мой собственный промах формы при проверке узла A: грепнул ключи
    построчно, получил «2 из 4» и на секунду принял это за дефект —
    ключи ПЕРЕНОСЯТСЯ по строкам, и складывает их только keyrun с
    фолд-контрактом. Дефекта не было; экземпляр класса «негатив от
    промаха формы», на этот раз координаторский.

## Байтовая цена узла A — решение отложено до вердикта критика

Замер: гейт (а) ДО 139316 Б (запас 6684), ПОСЛЕ 143917 Б (запас
**2083**), ВЗЯТ. Дельта **+4601 Б** против рекомендованного спекой
потолка ≤2500 Б. Исполнитель не стал урезать сам — правильно, спека
это запрещала, — и доложил превышение.

Проблема не в самом превышении, а в остатке: **храповик не
поднимается никогда**, а в очереди этого же хода ещё правки ТОГО ЖЕ
протокола — шаг сверки отклонений чека 30 (F5), пин чека 29 (F9), три
пункта саморевизии (F15) и ЦЕЛЫЙ НОВЫЙ ЧЕК из пункта 4 батча 2. При
запасе 2083 Б узел B упрётся в гейт почти наверняка.

Поэтому вопрос вынесен критику отдельным пунктом: обязан ли
нормативный текст двух чеков быть такой длины, или часть его — РАЦИОНАЛ,
чьё место в `docs/POLICY_FULL.md` по стоячему правилу «норма в
чеке, рационал в твине». По его вердикту узел B получит явный байтовый
бюджет: либо нейтральный, либо НЕТТО-ОТРИЦАТЕЛЬНЫЙ с переносом
рационала в твин — тем же приёмом, каким шла диета W2c.

## ПОПРАВКА К ЧИСЛАМ F10 (внесена 2026-08-20, драфт t-551)

Доли 82/77/60% в записи калибровки ЗАВЫШЕНЫ на неизвестный множитель:
разведка считала ИМЕНА слоёв, которые живут также в исходниках,
тестах, отчётах и брифах, и одно реальное срабатывание попадает в
транскрипт минимум дважды. Счёт по ТЕЛАМ сообщений даёт 92/65/156.
Прошлое не переписывается (D-0089) — поправка внесена вперёд в
docs/task_reports/2026-08-20_calibration-8.md, находка F10.

Находка стоит: ложные срабатывания реальны и воспроизведены поимённо
(копилка выше). Но правильный вывод не «слои шумят на 80%», а «мы не
умеем воспроизводимо мерить собственный warn-поток» — и это делает
инструмент замера (кандидат `tools/warn_density.py`) предметом
очереди, а не факультативом.

**Диагнозы трёх слоёв оказались РАЗНЫМИ**, гипотеза брифа о единой
причине опровергнута замером:
- GIVEN-PATH — временной край; норма R2(b) САМА гарантирует
  несуществующий путь в owns (самоактивирующийся файл сдаётся под
  соседним именем), то есть слой обязан шуметь по конструкции;
- OWNS OVERLAP — половина массы это ФАНТОМНЫЕ пути из прозы
  канонической фразы `owns/non-goals/handoff` (подтверждено живым
  `logs/owns_registry.jsonl`), вторая половина — окно живости 24 ч
  без сигнала завершения воркера: слой отвечает на вопрос «тот же путь
  в пределах суток», а норма R4 спрашивает «одновременно ли»;
- ROLE-TYPE ложности НЕ ИМЕЕТ вовсе: он говорил правду о протухшем
  `model: haiku` в `scout.md`, и причина уже устранена перепривязкой
  D-0102. Шум был ПОВТОРОМ истины на каждом диспатче.
