# Спека ремедиации F-61 / F-58 (драфт designer t-502, принят Lead 08-19)

Статус: принят; решения Lead по развилкам — ниже; узел A — builder
t-503 (ПОСАЖЕН 08-19, 44ef907; флип статусов F-57/F-59 исполнен);
узел B — builder t-509 (08-19); узел C — builder t-511 (08-19,
форма ПАКЕТ без касания носителей: запас CLAUDE.md до BREACH 31 Б,
посадка одним механизменным коммитом после диеты W2c). Пакет ПРИНЯТ
08-19 ревизией 2 (критик fit_with_fixes, 7 фиксов внесены, живой
материал инцидента 7б покрыт: porcelain непосредственно перед
откатом + зеркало «легитимный параллельный писатель» + сшивка с
R4(d)); дельта CLAUDE.md 872 Б → порог К4 спеки W2c обновлён;
посадка ждёт W2c-1 (чек-лист в пакете, вкл. прогон CRITIC_EXAM). F-57/F-59 ИСКЛЮЧЕНЫ
из батча докладом свежести (см. §0) — обе материально исполнены в
дереве, статусы FINDINGS протухли (класс F-41, третий экземпляр за
08-19); флип статусов — акт Lead при посадке узла A.

## РЕШЕНИЯ LEAD ПО РАЗВИЛКАМ (08-19)

Р1→(а) F-57/F-59 вне батча, флип статусов ходом Lead; фолбэк-остаток
F-57 (ветка _resolve_echo_base вне Edit/Write возвращает
HEAD-базу) — строкой очереди. Р2→(а) одна батарея
tools/test_f61_halfstate.py с F61_TARGET. Р3→(а) гейты получают
ТОЛЬКО temp+replace, без внешнего fail-open (enforcement выше
косметики отказа). Р4→(а) mkstemp в той же папке, суффикс .tmp
последний и не .json — запинено тестом. Р5→(б) лимит длины stderr не
вводится, только санитизация (рамка Архитектора). Р6→(а)
session_context буферизует вывод и пишет одной записью+flush внутри
try («discards everything» становится правдой). Р7→(б): пробы
недоступности записи в батарее + строка RULE_COVERAGE + указатель
SIBLING_MAP; конфликт носителей разрешён формой дизайнера — узел A
несёт ДОСЛОВНЫЙ текст строки реестра в handoff, Lead сажает её тем
же коммитом, что фикс (после t-498). Р8→(а) A→B последовательно,
один файл батареи. Р9 — подтверждено НЕ-заведение атомарности
break_glass_ack (битый сайдкар = повторный показ, fail-safe); строка
в «Решения о НЕ-заведении» SIBLING_MAP — при посадке. Р10 — без
нового D (уточнение нормы п.7 в её тексте + пара POLICY_FULL + строка
RULE_COVERAGE). Р11 — кит-твины в очередь релизного батча, строка по
D-0082 при посадке узла C.

## §0. ДОКЛАД СВЕЖЕСТИ (сверено чтением дизайнером)

F-57 ИСПОЛНЕНА: journal_echo.py PAYLOAD-SCOPED ECHO BASE
(:1697-1771, main :1915-1947) — коллекторы питаются от
_resolve_echo_base(payload) (originalFile этого вызова, не git show
HEAD); валидация (:1901-1903) на HEAD-диффе намеренно. ОСТАТОК (край
фикса, не невыполненность): фолбэк-ветка (tool вне {Edit,Write},
кривой originalFile) возвращает кумулятивную HEAD-базу — строкой
очереди.

F-59 ИСПОЛНЕНА оба подкласса: dispatch_gate :532-536 \bowns\b удалён
из WRITE_INDICATORS_RE (08-10), owns_declaration_has_path_token
(:558-585); owns_gate :408-468 + backtick в _EDGE_TRIM_CHARS (:576) +
_first_raw_token (:741+), пин
test_f59_bold_marker_with_bulleted_continuation. ОСТАТКИ:
фенсированный блок — задокументированный не-цель; расхождение счёта
(FINDINGS 5 / CURRENT_CONTEXT 7) — зафиксировано, не разрешено.

F-58 ОТКРЫТА (подтверждено). F-61 ОТКРЫТА, экземпляров ЧЕТЫРЕ
писателя одного трека: dod_track._save_track (:519-523, write_text
без temp+replace, вызов :564 вне try), critic_snapshot
(_write_failure_snapshot :230 читает prior :258 ПОСЛЕ усечения
:300), session_context (ack внутри build_context_lines :1767 ДО
печати; fail-open в тот же stdout :1796), + dod_gate.py:273 и
main_gate.py:173 (тот же неатомарный write_text).

## УЗЕЛ A — F-61, писатели трека (builder t-503, сразу)

Акцептанс-ключи:
A1. Тотальный try в main() dod_track: любое исключение → ОДНА строка
stderr `dod_track.py: FAILED to save track (<Type>: <msg>)`; return 0
безусловен; _reconfigure_stderr_utf8() ДО try.
A2. Атомарная запись трека: mkdir(parents=True, exist_ok=True)
СОХРАНЯЕТСЯ ДОСЛОВНО (регресс-пин); запись в уникальный tmp В ТОЙ ЖЕ
папке + os.replace; имя tmp: суффикс .tmp ПОСЛЕДНИЙ и НЕ .json
(session_context.py:1616 глобит *.json и берёт stem —
<sid>.tmp.json читался бы как чужой трек).
A3. Guard не-dict корня И не-dict tool_input → return 0 молча
(образец journal_echo.py:1885); тот же guard в critic_snapshot
(:280-284) — сосед D-0100.
A4. critic_snapshot: prev-проекция {prev_ts, prev_tree_hash}
читается ДО рискованной записи, передаётся параметром;
_write_failure_snapshot файл больше не читает; обе записи атомарны
по A2; инвариант «отказный документ без ключа tree_hash» — буквально.
A5. Существующие пины test_critic_snapshot.py:289-291/296-315/317+
зелены БЕЗ правок (их правка = сигнал, что A4 сделан не так).
A6. Дискриминирующий тест атомарности: мок РЕАЛЬНО усекает файл и
лишь потом бросает; контр-прогон на непатченном коде ОБЯЗАН быть
красным; обе половины дословно в witness.
A7. dod_gate/main_gate — только A2 (Р3а), внешний try НЕ ставится.
A8. Детектор класса: проба «боевой артефакт недоступен на запись»
на КАЖДЫЙ пишущий хук батча (файл read-only; путь занят каталогом);
дословный текст строки RULE_COVERAGE — в handoff (сажает Lead).

Форма сдачи: СИБЛИНГИ tools/<name>_f61.py (живые пути не трогаются,
D-0069; посадка байт-копией — акт Lead; прецедент имени
session_context_b3.py). Батарея — ОДИН файл
tools/test_f61_halfstate.py, -k f61; цель через env F61_TARGET:
live → живые файлы (контр-прогон, красный до посадки); default →
сиблинг если существует, иначе живой. Существующие тест-файлы НЕ
трогаются (их SCRIPT жёстко на боевой путь — зелёный доказывал бы не
то; проверено: test_dod_track:31, test_critic_snapshot:50,
test_dod_gate:29, test_main_gate:25).

Края: stdin пуст → return 0 (существующее :545); session_id
отсутствует → return 0 (:552-556 дословно); cwd → "."; трека нет →
{"edits":[],"runs":[]}; каталога нет → mkdir; битый JSON → чистое
состояние (:505-511; потеря чужих ключей gate_state — строкой
очереди, не батч). Конфликтные пары: (1) fail-open × enforcement
гейтов → гейтам только A2; (2) атомарность × glob("*.json") → форма
имени tmp; (3) lost-update гонка двух сессий НЕ решается (не-цель,
докстринг :141-148), батч лишь не делает её разрушительной; (4) одна
строка stderr × рамка «не стричь пояснения» → санитизация без
усечения. Адверсариальная батарея: не-dict корень (список/строка/
число/null); tool_input не-dict; вложенность 200; payload 1МБ и 5МБ;
\x00 и \r\n; не-UTF8 (errors=replace уже стоит :542 — пин);
session_id с ../ фиксируется КАК ЕСТЬ (валидация не вводится —
строкой очереди); трек read-only; путь занят каталогом.

DoD (прогоны по порядку, вывод дословно): (1) базлайн ДО правок
python -m pytest tools/ gateway/ -q (после правок множество упавших
не шире базового — правило F-60); (2) контр-прогон КРАСНЫЙ:
F61_TARGET=live python -m pytest tools/test_f61_halfstate.py -q;
(3) целевой зелёный: python -m pytest tools/test_f61_halfstate.py -q;
(4) суженный по owns (R4, параллелен t-498): python -m pytest
tools/test_dod_track.py tools/test_critic_snapshot.py
tools/test_dod_gate.py tools/test_main_gate.py
tools/test_f61_halfstate.py -q; (5) канон — координатор после
схождения, меткой BATCH CANON.

Дано: FINDINGS F-61 (:2587-2672); f61-remediation-drafts (раздел
ОБЩЕЕ ЯДРО — как решённое); dod_track :141-148/:498-523/:533-565;
critic_snapshot :214-227/:230-265/:268-315; dod_gate :250-276;
main_gate :150-175/:398; session_context :1600-1666 (ЧИТАТЕЛЬ,
read-only); journal_echo :1885 (образец guard); test_critic_snapshot
:21-27 (chmod на Windows)/:289+; mechanism_gate :97-153 (пять путей в
неводе — факт для handoff); .claude/settings.json (read-only).

owns (АБСОЛЮТНЫЕ): D:\Improving_AI\Operating-System-for-LLMs\tools\dod_track_f61.py;
D:\Improving_AI\Operating-System-for-LLMs\tools\critic_snapshot_f61.py;
D:\Improving_AI\Operating-System-for-LLMs\tools\dod_gate_f61.py;
D:\Improving_AI\Operating-System-for-LLMs\tools\main_gate_f61.py;
D:\Improving_AI\Operating-System-for-LLMs\tools\test_f61_halfstate.py

Не-цели: живые пути четырёх хуков (посадка — Lead);
session_context.py и его тесты (узел B, занят t-498);
settings.json; FINDINGS/SIBLING_MAP/RULE_COVERAGE; гонка
lost-update; валидация session_id; потеря чужих ключей;
_try_hookspath_autofix; read_stdin_payload non-TTY; toolkit;
общий модуль atomic_write (локальная копия хелпера в каждом файле —
основание: общий модуль попал бы в транзитивное замыкание невода;
носитель конвенции dod_gate.py:411).

Handoff Lead: (1) посадка байт-копиями + проба живости с откатом
только из байт-копии (witness отката — дословный вывод сверки);
(2) коммит посадки МЕХАНИЗМЕННЫЙ (все пять путей в неводе поимённо);
коммит сиблингов — не механизм, строка «оси: не-механизм»;
(3) кейсы пробы живости: PostToolUse Edit (dod_track), SubagentStop
(dod_gate), Stop (main_gate), PreToolUse Task critic
(critic_snapshot) — rc=0, трек валиден, осколков *.tmp нет;
(4) очередь строками в носитель: фолбэк-остаток F-57; потеря чужих
ключей при битом JSON; _try_hookspath_autofix не откатывает
core.hooksPath; read_stdin_payload non-TTY зависание;
кит-твины; hook_liveness_probe отдельным кейсом; (5) флип статусов
FINDINGS F-57/F-59 — ход Lead.

## УЗЕЛ B — F-61 session_context (ПОСЛЕ посадки t-498)

B1 ack только после успешной выдачи, КОНСТРУКЦИЕЙ: ack'аются ровно
ключи строк, вошедших в lines[:MAX_LINES] и напечатанных;
позиционное «перенёс ниже» без фильтра — не принято. B2 одна запись
вывода + flush ВНУТРИ try. B3 fail-open в stderr (не в упавший
stdout), сам в try, return 0. B4 guard не-dict в read_stdin_payload.
B5 три пина канала — двухмирно; их адреса ПЕРЕЧИТЫВАЮТСЯ после
посадки t-498 (номера из t-469 протухли — не переносить памятью).
Края: сайдкар отсутствует/битый → повторный показ (fail-safe);
битый трек пропускается по-файлово; тесты НА границе (25 строк / 5
фактов) и ЗА ней (26/6 — сводная строка и НЕ-ack невошедших); отказ
канала на первой строке / в середине / на autoboot-блоке; оба канала
упали → rc=0 молча. Конфликт «discards everything» → Р6(а)
буферизация. Темпоральный: сиблинг строится от ПОСАЖЕННОЙ версии.
DoD: базлайн; контр-прогон -k session_context красный; целевой
зелёный; суженный (test_session_context*, test_f61_halfstate);
канон; сторож неизменности реального .claude/ (пробы на tmp-дереве).
owns: session_context_f61.py + test_f61_halfstate.py (дозапись).

## УЗЕЛ C — F-58 ветка отказа от своей работы (ПОСЛЕ посадки t-498)

Носители нормы (все шесть, greps с позитивными контролями):
CLAUDE.md п.7; POLICY_FULL :585-595 (пара, тем же коммитом);
.claude/agents/builder.md :93-98; critic.md :84-91; чек 25(в)
протокола (:824-831); RULE_COVERAGE §4 (:82-92). Кит-твины — порт,
очередь (Р11).
C1 ветка (д): отбрасывание СВОЕЙ незакоммиченной работы в пути
СВОЕГО owns легально; свидетель — декларация владения + пустой дифф
после отката. C2 граница явно: вне owns — прежний запрет; owns не
объявлен — прежний запрет; porcelain показывает факт, не АВТОРА —
основание owns-декларация, не суждение «M — моя». C3 пара
ядро↔POLICY_FULL одним коммитом. C4 оба role-файла — та же ветка.
C5 чек 25(в) — третья ветка сверки (байт-копия / пустой porcelain /
путь в owns исполнителя) — без неё детектор метит легальное как
нарушение (класс «сторож, которого приучают игнорировать»).
C6 строка RULE_COVERAGE тем же коммитом.
Края: приоритет (д)×(б) записан в тексте — (д) только при «файл в
СВОЁМ owns И правки этой же сессии», иначе (б); owns не объявлен →
(б); ТЕМПОРАЛЬНЫЙ БЛОКИРУЮЩИЙ: после t-498 измерить запас до
храповика — молча пробить запрещено, born-breached запрещён;
после t-498 перечитать norm_keys.json (сейчас строк гигиены нет —
проверено с позитивным контролем; появятся — ключи правятся тем же
ходом). DoD: DOC-DISPATCH WITNESS все три свойства (ключи: «входящем
в собственный owns», «пустой дифф после отката», «не показывает
АВТОРА», «путь в owns»); escape_check; канон; parity. Коммит
МЕХАНИЗМЕННЫЙ (все шесть путей в неводе/каталогах невода).

## След чтения дизайнера — в транскрипте t-502; несущие негативы
продублированы с контролями в тексте узлов выше.
