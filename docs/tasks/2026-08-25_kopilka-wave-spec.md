# Спека волны «копилка дня» (2026-08-25, слово оператора «доделай всё из копилки»)

Драфт Lead сам (dispatch_skipped designer — десять пунктов с готовыми
диагнозами из вердиктов дня; сборка). Носитель судьбы пунктов —
docs/tasks/2026-08-25_queue8-closure.md. Четыре параллельных билдера
(М2/V/RC/SE), owns дизъюнктны, witness сужены (R4), канон — координатор
на сходе.

## Решения Lead, принятые до диспатча

- **Р-С1 (порог длины варн-текста):** ХРАПОВИК 550 символов на
  СООБЩЕНИЕ (ось 14 — обоснование замером: снимок 2026-08-25,
  носитель счёта scratchpad/rc1_len_measure.py + вывод в этой спеке:
  максимум 513 (HYG_CD_NONROOT), второй 484 (SEARCH), хвост 130–305;
  550 = максимум + ~7% на правки хвостов без структурного роста).
- **Промоция п.2 чеклиста (маркер-warn):** НЕ строится этим ходом —
  кандидат остаётся взведённым (RULE_COVERAGE, строка R11), триггер
  R11(n)-рецидива п.2 не зафиксирован замером. Закрыто записью.
- **Правило «спека не предсказывает ЧИСЛО правленых тестов»**
  (рецидив недосчёта 4→9→13): числом владеет ДИФФ, спека задаёт
  ФОРМУ (каждый правленый существующий тест — поимённо с причиной и
  сохранением предмета пина). Носитель — роль-файлы designer/builder
  (правит Lead отдельным касанием, не эта волна).

## БИЛДЕР М2 — critic_snapshot, dod_gate, квадратичность, храповик Р-С1

- **М2-1 (аналог K5, F7 вердикта t-601):** tools/critic_snapshot.py
  пишет .claude/critic_snapshot.json и на диспатче, заблокированном
  dispatch_gate (гейт не спрашивается, main() всегда 0). Фикс тем же
  паттерном, что K5 в owns_gate: перед записью снимка —
  dispatch_gate.decide(payload) (импорт try-package/except-sibling);
  блок (exit 2) → снимок НЕ пишется; исключение → fail-open писать;
  собственный exit critic_snapshot не меняется. Края: (i) снимок —
  ПЕРЕЗАПИСЬ одного файла: пропуск записи сохраняет ПРЕДЫДУЩИЙ снимок
  (это и есть цель — блокированный вызов не должен затирать след
  последнего реального критика); (ii) не-критик диспатч → поведение
  как сейчас (ранний выход). Тесты: блок→снимок не тронут (старый
  цел), пропуск→новый записан, исключение→записан.
- **М2-2 (находка t-602):** tools/dod_gate.py не распознаёт one-off
  `python <path>.py` как verification-команду (регексп требует
  test/pytest). РЕШЕНИЕ: регексп НЕ ослаблять (любой скрипт-прогон =
  зелёная верификация — это дыра). Вместо этого: (а) в докстринг
  dod_gate — КОНВЕНЦИЯ обёртки, дословно:
  `<команда> && echo "verification test: <что проверено>"` (echo
  выполняется только при exit 0 — честный маркер); (б) пин-тест: две
  команды — `python docs/tasks/x.py` НЕ распознаётся,
  `python docs/tasks/x.py && echo "verification test: probe"` —
  распознаётся.
- **М2-3 (Ф3 вердикта t-605):** квадратичность
  _is_python_dash_c_certain на форме «повторённые опенеры
  `python - <<` без закрывателя» (замер: 0.074/1.165/8.363с на
  9.6/38/96КБ — предсуществующая). Фикс: 限 ограничить число
  обрабатываемых опенеров константой (например MAX_HEREDOC_OPENERS =
  64; сверх — ранний консервативный выход: pyc_certain=False,
  классификация U → старое WARN-поведение, deny НЕ срабатывает —
  назвать этот сдвиг тестом: до фикса гигантская форма могла бы
  денаиться, после — WARN; направление консервативно в сторону
  НЕ-блока, инвариант «deny не расширяется» цел). Перф-тест НА/ЗА:
  n=64 быстро; n=4000 — время не хуже линейного от размера,
  число приложить. I2-инвариант: все существующие deny-тесты зелены
  без правки тел.
- **М2-4 (Р-С1):** в tools/test_warn_messages.py заглушка
  test_c4_length_within_budget снимается: тест мерит len() КАЖДОГО
  текста батареи (константы и отформатированные образцы — тот же
  состав, что в замере) против WARN_TEXT_BUDGET_CHARS = 550;
  докстринг называет носитель счёта и снимок (максимум 513,
  2026-08-25). Граница: тест НА (текст ровно 550 — проходит,
  синтетикой) и ЗА (551 — падает, синтетикой через параметр-хелпер,
  не порчей живых констант).

owns М2: tools/critic_snapshot.py, tools/dod_gate.py,
tools/hygiene_gate.py, tools/test_hygiene_gate.py,
tools/test_warn_messages.py, tools/test_critic_snapshot.py и
tools/test_dod_gate.py (если существуют — Glob; нет — создать
test_critic_snapshot.py минимальный).
witness М2 (сужен): pytest поимённо всех owned-тестов + перф-числа
М2-3 + негативные контроли М2-1 (снимок цел после блока).

## БИЛДЕР V — journal_validator тексты + logs в given-префиксы

- **V-1 (R9-находка t-607):** тексты нарушений
  tools/journal_validator.py — по правилу трёх (что неверно / что
  сломается / действие повелительно; провенанс хвостом). Это
  BLOCK-тексты коммит-гейта (не warn-слой реестра — литералы реестра
  не задействованы, менять можно целиком, но НЕ МЕНЯТЬ смысл
  правил). Обход всех message-строк файла ПЕРЕЧИСЛЕНИЕМ в отчёте
  (правило/было/стало); пины тестов валидатора обновить с
  сохранением предмета каждого (перечислить).
- **V-2 (находка №4 t-599):** `logs` добавляется в
  _GIVEN_REPO_REL_PREFIX (tools/dispatch_gate.py:~1254) — журнал,
  самый цитируемый носитель, сейчас не проверяется given-слоем даже
  на существование. Края: популяция слоя GIVEN_PATH РАСШИРЯЕТСЯ —
  (i) запись GIVEN_PATH в tools/warn_layers.json: в reason строка
  «префикс-набор расширен logs 2026-08-25 (V-2); доли до/после
  несравнимы» (reachable остаётся unmeasured); (ii) тесты: given с
  битым logs-путём → warn; с живым logs-путём → тишина; (iii)
  сверить, что FRESHNESS-регексы (свои, уже с logs) не задеты; (iv)
  регресс полной батареи dispatch_gate.
owns V: tools/journal_validator.py, tools/test_journal_validator.py,
tools/dispatch_gate.py, tools/test_dispatch_gate.py,
tools/warn_layers.json.
witness V (сужен): pytest test_journal_validator + test_dispatch_gate
+ test_warn_density (реестр задет) + warn_density --check.

## БИЛДЕР RC — диета docs/RULE_COVERAGE.md

bpr 1013 при полосе (1005.1..1058] (замер corpus_growth 2026-08-25).
Цель: bpr ≤ 1000 (под 95%-полосой с запасом). Средство: РАЗВЁРТКИ в
тела docs/rule_coverage/RC-NN.md по УЖЕ СУЩЕСТВУЮЩЕМУ паттерну файла
(строки R13 и R7 уже несут «Развёртка — тело: RC-01.md#...»):
2-3 самые жирные строки (кандидат №1 — строка R11 пяти пунктов,
кандидат №2 — строка R3) сокращаются до ядра-ответа, развёртка — в
тело с якорем. ИНВАРИАНТЫ (нарушение = провал): (i) ни один из
токенов, которые сверяет prepass/кросс-чеки, не исчезает из ядра —
ДО правки собрать список грепом «D-0103» и всех «D-0NNN»/«RC§»/имён
чеков в правимых строках и проверить ПОСЛЕ (прецедент: потеря
«D-0103» дала 2 красных канона 08-25); (ii) смысл строки в ядре
остаётся ОТВЕТОМ (чек 24(б): ядро без тела должно отвечать), не
оглавлением; (iii) честные границы (1.5/4 и «якорь в пределах файла»)
остаются В ЯДРЕ дословно. witness: `python tools/corpus_growth.py`
строка RULE_COVERAGE (bpr ≤ 1000) + `python tools/calibration_prepass.py
--check-form` + pytest tools/test_calibration_prepass.py + грепы
токен-инвариантов до/после дословно.
owns RC: docs/RULE_COVERAGE.md, docs/rule_coverage/RC-*.md.

## БИЛДЕР SE — Session-End прогон чека 2 (исполнение Р-О5 + D-0110)

Новый tools/r3_integration_check.py (детерминированный, без
AI-суждения): (1) git log --numstat за окно (аргумент --since,
дефолт — последние 24ч) → коммиты с суммой изменённых строк >100;
(2) для каждого — поиск в logs/routing-log.jsonl критик-входа окна
(delegated agent=critic ЛИБО basis critic ЛИБО «critic:» в notes
принятых событий между предыдущим и этим коммитом — эвристика окна
по ts); (3) вывод: список коммитов без найденного критик-следа =
КАНДИДАТЫ для ручного вердикта (печать «кандидат, не вердикт — чек 2
решает»); кумулятив D-0110 НЕ автоматизируется (семантика «одной
темы» — за Lead), но печатается счёт «малых» коммитов окна (< 100
строк) как входные данные. Exit ВСЕГДА 0 (информатор, не гейт —
Р-О5: гейт по рецидиву). Проводка: ШАГ в
.claude/skills/session-handoff/SKILL.md (после шага 2а, номер по
месту): «прогнать python tools/r3_integration_check.py --since
<начало сессии>; кандидаты — в отчёт handoff». Тесты: синтетический
репо-фрагмент не нужен — тестировать функции разбора numstat и
поиска критик-следа на фикстурных строках; святое: скрипт только
ЧИТАЕТ git и журнал.
owns SE: tools/r3_integration_check.py (НОВЫЙ),
tools/test_r3_integration_check.py (НОВЫЙ),
.claude/skills/session-handoff/SKILL.md (один шаг).
witness SE (сужен): pytest нового файла + живой прогон скрипта на
реальном репо (вывод дословно — сегодняшние крупные коммиты обязаны
показать критик-след).

## Общее

Дано (D-0106): эта спека (перечни файл:строка — из вердиктов дня:
t-601 F7, t-602 находка, t-605 Ф3, t-607 R9, t-599 №4; свежесть —
вердикты сегодняшние); база — текущее рабочее дерево, порожние
porcelain-следы чужие, не откатывать. non-goals всем: кит-твины
(порт-очередь), чужие owns волны, журнал/реестры логов. Недостающее —
вопросами. Канон целиком НЕ гонять — снимет координатор (BATCH CANON).
