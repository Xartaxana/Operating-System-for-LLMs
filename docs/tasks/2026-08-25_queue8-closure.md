# Закрытие живой очереди ремедиации №8 — DAG хода (2026-08-25)

**Слово оператора:** «доделай Живую очередь ремедиации №8». Очередь —
CURRENT_CONTEXT «ЖИВАЯ ОЧЕРЕДЬ ремедиации»; сама ремедиация закрыта
08-21, это её хвосты. Владелец хода — Lead-сессия (fable, резерв).

## Судьба пунктов очереди (свежесть проверена носителями, t-597/t-598)

| пункт | вердикт | куда |
|---|---|---|
| Свежесть п.5 — машинный слой | СТРОИТСЯ | узел Св (t-599 драфт → билд) |
| Уточнение п.4 гигиены | СТРОИТСЯ | узел П4 (t-600 драфт → билд) |
| C-4 сиблинги *_md | РЕШЕНО LEAD: механизм жив (использован 08-25), лечится тихий фолбэк | узел K1 (t-601) |
| C-3 (из DAG ремедиации) | ПРОТУХ: обе половины посажены 08-20 (hook_liveness_probe:285-298; test_live_state_isolation:72-97 t-543) | закрыт записью |
| Число замера без носителя счёта | КОДИФИКАЦИЯ | D-0107, строка п.6; детектор — подпункт чека 24 |
| Посимвольное по выводу поисковика | КОДИФИКАЦИЯ | D-0108, строка п.6; детектор — класс-лист чека 25 |
| Классы кода: словарь глаголов / склейка бэктиков / маркер-проза owns / реестр блокированных | СТРОЯТСЯ | узел K2-K5 (t-601) |
| Пара оси 4 (чек 11(в)↔warn_layers) | ЗАКРЫТА узлом B 08-25: 14 имён совпадают буквально; открытый перечень объявлен и печатается машинно каждым прогоном (diff_check_11v) | закрыт замером t-597 |
| Граница «bpr=1005 / полоса 1005.1» | ЗАМЕРЕНА 08-25 (corpus_growth.py): пункт был про RULE_COVERAGE — уже WARN 1013/1058 (+476 Б boot-строки сессии); DECISIONS.md WARN 238/241 (запас 3); КАРТА OK 3597 (Δ −53 при +3 записях оси 15). Сторож — не гейт; реакция: новые индекс-строки решений короче среднего; RULE_COVERAGE-диета — очередь | закрыт замером; хвост в очередь |
| Вопрос без детектора (ядро строки карты без тела) | КОДИФИКАЦИЯ | подпункт чека 24, той же правкой что D-0107 |
| Порт-очередь ось 7 | НЕ ЭТОТ ХОД | релизный батч завтра (D-0074) |

**Живые остатки находок №8 (F7/F11/F12/F15-остаток)** — НЕ в этой
очереди (отдельный блок CURRENT_CONTEXT), этим ходом не трогаются.

## Эксперимент D-0105 (п.6(а)) — свойство среды, 15:35

Диспатч, заблокированный dispatch_gate (без DoD), не запустился, но
owns_gate записал строку — реестр 100→101 (owns_registry.jsonl:101).
Рантайм исполняет ВСЕ PreToolUse-хуки матчера независимо от блока
соседнего. Фикс — K5 (t-601): owns_gate сам прогоняет
dispatch_gate.decide перед регистрацией. Строка :101 остаётся
(append-only), тесты — на синтетике.

## ИТОГ СХОЖДЕНИЯ (2-я половина сессии, ~16:30-18:00)

Все узлы волны 2 ПОСАЖЕНЫ и ПРИНЯТЫ (журнал: t-601 basis critic,
t-602 концессия, t-605 basis critic, t-606 basis critic, t-607
концессия + посадка Lead, t-609 basis critic + посадка Lead байт-копией
с SHA-сверкой). Три критик-гейта вернули блокеры — ВСЕ закрыты
фикс-раундами тех же билдеров: K (fit_with_fixes, 7 находок), П4
(оси A/B классификатора — 12 атак теперь M/O), Св (Б1 logs +
Б2 кит-ссылки), R3 (байтовый бюджет 6835→3096 + дедлайн записи).
Посадочные коллатерали (класс «синтетика без маркеров», 3 экземпляра
— заведён в реестр карты) закрыты: 19 пинов текстов узла C обновлены,
10 фикстур R3, кейсы пробы. Узел Л: ядро 37771 (диета слоя тем же
ходом: mermaid+closure-абзац → POLICY_FULL), D-0107..0109 оба реестра,
чеки 19/24/25, реестр слоёв 20/20. Сиблинги удалены (шесть _msg +
journal_echo_r3), батареи перенаправлены на живые файлы.

## Узлы

| узел | статус | задача | owns |
|---|---|---|---|
| Скауты свежести | ПРИНЯТЫ по трейлу | t-597, t-598 | — |
| Св — слой свежести п.5 (промоция R11(n)) | драфт ПРИНЯТ 15:40; развилки решены: Ф1(b) (в)+(а), Ф2(a), Ф3(b), Ф4(b) M4, Ф5(b), Ф6(b) v2, Ф7(a), Ф8(a), Ф9(a), Ф10(a), Ф12(a) честная граница 1.5/4, Ф13(a), Ф14(a), Ф15(a); билд ПОСЛЕ t-601 (общий dispatch_gate.py), параллельно П4, witness СУЖЕН (test_dispatch_gate + test_warn_density + prepass) | t-599 → билд | dispatch_gate.py, test_dispatch_gate.py, warn_density.py, test_warn_density.py, warn_layers.json, PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md (11(в)), docs/RULE_COVERAGE.md |
| П4 — предикат pyc + амендмент п.4 | драфт ПРИНЯТ 15:40; развилки: F1(A) pyc_payload, F2 AST, F3(A)+чек 25 узлом Л, F4(A), F5(A)/F5b-нет, F6, F7 O, F8(A), F9 20000 не-гейт, F10 норма (строки а+б), F11 без расширения, F12 кит-твин → порт-очередь; билд ПОСЛЕ t-601 (оба гоняют tools/), параллельно Св, witness СУЖЕН (test_hygiene_gate + test_permission_audit) | t-600 → билд | hygiene_gate.py, test_hygiene_gate.py |
| K — мехбатч (K1-K5) | билдер в работе | t-601 | пять test_*_md, dispatch_gate.py, owns_gate.py, их тесты, warn_layers.json усл. |
| Л — посадка кодификаций | ждёт t-600 (текст п.4-строки) | D-0107/0108/0109: строки п.6(б,в)+п.4(а) ядра, DECISIONS+FULL, твины POLICY_FULL, подпункты чека 24 + класс чека 25, диета слоя CLAUDE.md тем же ходом (кандидат: mermaid-диаграмма журнала → POLICY_FULL, ось 4), CURRENT_CONTEXT — очередь №8 в закрытое состояние | CLAUDE.md, DECISIONS.md, docs/DECISIONS_FULL.md, docs/POLICY_FULL.md, PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md, CURRENT_CONTEXT.md |

## Кандидаты в очередь, рождённые этим ходом (в CURRENT_CONTEXT узлом Л)

- Кит-твин hygiene_gate отстал (F12 драфта t-600: нет PYC_DENY_ENABLED
  / _is_python_dash_c_certain; тот же класс — toolkit permission_audit)
  → порт-очередь релиза.
- `logs/` не входит в префиксы GIVEN_REPO_REL_PATH_RE — журнал, самый
  цитируемый носитель, не проверяется даже на существование (находка
  №4 драфта t-599; сосед ЧУЖОГО слоя).
- Промоция п.2 чеклиста (названные края) по лику — RULE_COVERAGE:61
  уже называет кандидата (находка №5 t-599).
- RULE_COVERAGE-диета: bpr 1013 в WARN-полосе (замер 08-25).
- Правка текста чека 25 под расхождение «счёт класса ≠ счёт варна»
  (F3 решения) — исполняет узел Л.
- Ретро-якорь отчёта №8: calibration-8.md:147 указывает RULE_COVERAGE:55,
  факт — :61 (находка №2 t-599; отчёт не правим — append-only культура,
  запись здесь и есть поправка вперёд).
- Аналог класса K5 (вердикт критика t-601, F7): tools/critic_snapshot.py
  перезаписывает .claude/critic_snapshot.json и на диспатче,
  заблокированном dispatch_gate (гейт не спрашивается, main() всегда 0);
  потребитель — протокол :670. В очередь — тот же фикс-паттерн, что K5.
- dod_gate: регексп verification-команды требует «test/pytest» —
  one-off python-прогоны невидимы (находка t-602).
- Тексты нарушений JOURNAL_ECHO_BASE живут в journal_validator.py —
  вне узла C; правило трёх для них — отдельный ход (R9-находка t-607).
- Р-С1 (числовой порог длины варн-текста): НЕ назначен решением Lead
  08-25 — число без замера было бы классом оси 14; заглушка-skip в
  test_warn_messages.py; замер длин по живому потоку — кандидат.
- SEARCH_RETURNED_NOTHING: узаконенное исключение C-К2 (провенанс
  вморожен в литерал; смена литерала дороже пользы) — решение Lead
  08-25, поимённое исключение в батарее.
- Квадратичность формы «повторённые опенеры python - << без
  закрывателя» в hygiene_gate (_is_python_dash_c_certain,
  ПРЕДСУЩЕСТВУЮЩАЯ: 0.07/1.2/8.4с на 9.6/38/96КБ; дифф t-605 множит
  ~×3) — известное ограничение, оговорка в перф-тесте; фикс — очередь
  (вердикт критика t-605, Ф3).
- Спека-дефекты Lead волны копилки (вердикт критика, материал чека 23):
  witness RC не покрыл тестовый набор собственных owns
  (test_rule_coverage_bodies — критик прогнал: 94 passed, отказа нет);
  witness RC предписывал corpus_growth без --registry — запись в
  logs/corpus_growth.jsonl вне owns и против собственных non-goals
  (конфликт пары требований R11(b)); атрибуция М2-2 dod_gate вместо
  dod_track (унаследована из t-602); снимок bpr 1013 протух к старту
  RC (факт 1064 BREACH — межузловая гонка той же волны).
- Рецидив класса «недосчёт перечня правленых пинов в спеке»
  (4→9→13 за один узел П4) — R11(n): кандидат машинного слоя
  «перечень правок существующих тестов сверяется с фактическим
  диффом тест-файлов».
- «Кластер (iii) чека 23» в тексте очереди — ссылка на таксономию
  ОТЧЁТА калибровки, не на подпункт протокола; пятым экземпляром
  класса не засчитан (решение Lead 08-25), как свидетельство — да.

## Волна по слову оператора «в параллели: 2, 4, 5, 6» (16:0x)

| пункт | судьба | основание |
|---|---|---|
| 4 — семь фиксов замерщика | **ПРИНЯТ 16:35** (t-602, концессия — предписания самого критика с его контролями; Фишер 0.5157 воспроизведён). Находки: CLEANED-популяция З2 = 0/0 — исторический корпус после чистки temporal сигнала не даёт, детектор (iii) переходит в режим «мерит новые данные»; dod_gate не распознаёт one-off python-прогон без «test» в команде (регексп verification-команды) — КАНДИДАТ ОЧЕРЕДИ | вердикт критика §4а/§5; результаты в manifest-form-probe-results.md |
| 5 F7 — слой R3 | дизайнер t-604 драфтит | — |
| 5 F11 | **ЗАКРЫТ ПЕРЕЗАМЕРОМ 16:1x**: по единой метрике (символы поля notes) доля >600 = 43% базлайн → 38.9% окно №8 (роста НЕТ); >800 = 7.3% окна; числа находки (медиана 869, 58.7%/79.5%) не сходятся ни с notes-символами (545), ни с длиной строки (758) — почти наверняка БАЙТЫ против СИМВОЛОВ. Экземпляр класса D-0107. Хвост: после окна >600 = 49.6% (n=139, драйвер — длинные приёмочные notes этой сессии; дисциплина Lead) | скрипт scratchpad/f11_notes_breakdown.py, вывод в этом файле |
| 5 F12 | **ЗАКРЫТ ЗАМЕРОМ 16:0x**: раскладка причин фолбэков по всем транскриптам — «not an append» 96, «no originalFile key» 9, «no tool_response» 4. Доминирует дисциплинарный сигнал (не-хвостовые правки журнала) — эхо работает как задумано, фикс-диспатч НЕ нужен, фолбэк остаётся диагностическим | греп по C:\Users\user\.claude\projects\...\*.jsonl |
| 5 F15 | (i) чек 19 — переформулировка в узле Л; (ii) corpus_growth baseline к границе окна и (iii) savings_report оконные аргументы — билдер М, волна 2 | тексты у t-603 |
| 5 Р-D1 | **РЕШЕНО**: вырожденный край HYGIENE — экземпляр класса, лечение на уровне РЕЕСТРА: достижимая популяция слоя = «непустая строковая команда Bash/PowerShell»; правка записи HYGIENE — патчем узла C (реестр в его посадке) | warn-population-class.md:104-110 |
| 5 Р-D2 | **РЕШЕНО**: наблюдение за населённостью встроенных типов закреплено за чеком 11(в) — расщепление ROLE-TYPE (узел A) делает долю BUILTIN-сообщения измеримой отдельно; новой машинерии не заводится | warn-population-class.md:74-81 |
| 6 клещи | OS-конец (регексп session_context.py:1630 + формы $CLAUDE_PROJECT_DIR) — билдер М, волна 2; кит-конец (свой экземпляр toolkit wiring_check:217 + нормализация settings) — ИМЕНОВАННОЙ строкой в порт-очередь завтра | архив §6 boot-diet-extractions |
| 6 allowlist reopen | кросс-чтущего скрипта НЕ существует (негатив с контролем t-603) — семантика ложится в докстринг правила 9г journal_validator (билдер М), пункт закрывается указателем | архив §2 |
| 6 d6 | живого действия НЕТ: «фикса нет без рецидива» — стоячее ограничение, закрыт записью | diet-details d6 |
| 6 D-0099 (а) | порт-очередь завтра; (б) — на триггере прогона набора; вопрос (2) — большой экзамен, у оператора | CURRENT_CONTEXT |
| 2 узел C | конфликта с живыми файлами НЕТ (пишет _msg-сиблинги, D-0069); прямую правку warn_layers.json забираю из его owns — патч-секцией отчёта, применяет Lead на посадке; сужённый witness = test_warn_messages.py; волна 2 | warn-class-fix-dag.md:325-417 |

**Волна 2 (после возврата t-601, всё параллельно, R4 сужённые witness, BATCH CANON на сходе — Lead):** П4 (hygiene_gate + test_hygiene_gate) · Св (dispatch_gate, test_dispatch_gate, warn_density, test_warn_density, warn_layers.json, протокол 11(в), RULE_COVERAGE) · C (шесть _msg-сиблингов + test_warn_messages.py; реестр — патчем) · М (спека 2026-08-25_wave2-misc-spec.md: session_context регексп клещей; corpus_growth baseline-ts; savings_report оконные аргументы; докстринг 9г валидатора; тесты все существуют — проверено ls 16:15) · **R3** (t-604: сиблинг tools/journal_echo_r3.py + tools/test_journal_echo_r3.py — только эти два owns; посадка в живой journal_echo.py + реестр + чек 11(в) + RULE_COVERAGE + пин 18→19 + фикс лживой шапки — узел 2, Lead, ПОСЛЕ критик-гейта). Owns всех пяти дизъюнктны. Узел Л — ПОСЛЕ Св (общий протокол): + чек 19 переформулировка, + правка текста чека 25 (F3), + подпункты чека 24, + НОВЫЙ ВОПРОС ОПЕРАТОРУ: коллизия норм «R3 требует критика на крупном диффе × R13(г) отдаёт лист судье — что старше, когда лист крупный?» (Р3 драфта t-604, R11a).

**Решение по Р-О5 (записано явно, не молчанием):** write-time WARN R3-ЗЕРКАЛО не отменяет и не заменяет решение Р-О5 от 08-20 — «отдельный дешёвый прогон чека 2 на Session End» остаётся очередью как назначенная форма, блокирующий гейт — по-прежнему на триггере рецидива. Warn ≠ гейт; Р-О5 отвергал гейт.

Приёмка узлов: K и билды Св/П4 — по witness (канон/сужённые по R4 при
параллели); Л — Lead-класс, политика. Бюджет ядра: 37723/37500 WARN,
breach 37800 — строки Л не влезают без диеты слоя; диета тем же ходом
Л (каденция boot-diet: дожим один раз, батчем).
