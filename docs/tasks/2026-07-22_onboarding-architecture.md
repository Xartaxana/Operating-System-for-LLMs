# Онбординг-архитектура: продукт поверх чужих агентных схем

Статус (обновлён 2026-07-22 вечером): ядро ОТЧЕКАНЕНО — D-0090
(развилки решены словом оператора 07-22), апгрейд-механика — D-0091
(ревью critic t-284, fit_with_fixes). Документ — носитель замысла,
evidence и аудита; живые нормы — в DECISIONS/ките/протоколах.
Открытым остаётся: ревью WP §4.3 Архитектором (его очередь) и
очередь следующей чеканки (дрейф врезки, см. §Аудит).
Повод: слово оператора 2026-07-22 «наша новая механика не подошла AO3;
многие проекты работают на агентах — придумать, как продукт будет
онбордиться на чужих проектах, не ломая их схемы, а только улучшая».

## Evidence (t-267, приёмка judge-basis; вторая проходка Lead по
## HANDOFF:205–233 — обязательна при чеканке, D-0066)

Прецедент AO3 (входящий батч os-inbox-0722, 4 решения их Lead):

1. D-0082 (escape-allowlist) — ПЕРЕНЯТЬ адаптированно; внутри решения
   зафиксировано собственное «признанное отличие» (reopen-семантика).
2. t-257 (wiring-integrity чек) — ПЕРЕНЯТЬ адаптированно
   (scripts/wiring_check.py + их SessionStart-хук).
3. t-259 (машиночитаемый вердикт критика) — ПЕРЕНЯТЬ адаптированно.
4. D-0087 (judge-приёмка лист-класса) — ПРИЗНАННОЕ ОТЛИЧИЕ, НЕ
   перенимать: (а) конвейерный лист уже принимают их гейты D1/F1 —
   AI-агенты на детерминированных триггерах rules.yaml (наш же
   паттерн D-0063: код гарантирует встречу, AI судит смысл);
   (б) неконвейерный лист — basis=critic; (в) предпосылок судейской
   механики нет (шлюз, промпт, калибровка, $/задачу-замер);
   (г) basis="judge" без калибровочной сетки = самосертификация
   (класс F-22). BASIS_VALUES жёсток (log_append.py:165).

Прочтение: 3/4 механизмов вошли АДАПТАЦИЕЙ к их носителям, 1/4
отображён на нативный эквивалент и отвергнут КАК КОПИЯ — с точным
называнием отсутствующих предпосылок. Это не сбой онбординга, а
образец зрелой реакции хоста. Проблема продукта: такая реакция
потребовала от хоста нашей же культуры (правила 10, F-22, Rule #1);
чужой проект её не воспроизведёт — процедуру сопоставления обязан
поставлять сам продукт.

## Диагноз

Сегодняшний онбординг тулкита — «копия штаба»: политика + роли +
журнал + гейты. Для проекта с живой агентной схемой копия либо
отторгается (как у AO3 — лучший случай), либо ломает нативные
механизмы (худший). Смежная находка уже есть: F-35 (онбординг не
собирает оси самого хоста). Новое (F-51): онбординг не умеет
ОТОБРАЖАТЬ наши механизмы на существующие эквиваленты хоста.

## Принципы (ядро замысла)

Онбординг = экзамен → карта эквивалентов → слоистая установка.

1. **Экзамен-инвентаризация хоста.** Первая задача онбординга —
   evaluation, не build (расширение D-0030/D-0070 с моделей на СХЕМУ
   хоста): по каждой ФУНКЦИИ (приёмка, журнал/учёт, эскалация,
   изоляция, калибровка) — чем хост её закрывает сегодня. Мост —
   словарь функций D-0062: мы спрашиваем «кто у вас принимает
   работу», не «где ваш judge».
2. **Карта эквивалентов (adoption ledger) — первоклассный артефакт.**
   По каждому механизму кита статус: adopt (перенять) /
   native-equivalent («признанное отличие»: функцию закрывает
   нативный механизм хоста, фиксируется соответствие) / deferred
   (нет предпосылок; триггер переоткрытия назван) / rejected
   (Rule #1 хоста). Блок AO3 — стихийный прототип; продукт даёт
   шаблон. Ledger аудируем калибровкой хоста (живость соответствий).
3. **Замыкание предпосылок.** Каждый кит-механизм несёт манифест
   предпосылок (расширение R10(в) на поставку): судья требует
   калибровочную сетку, гейты — хук-путь, учёт — носитель цен.
   Установка механизма без его предпосылок запрещена конструкцией
   онбординга: либо связка целиком, либо native-equivalent/deferred
   с записью в ledger. Ровно аргумент (в)-(г) AO3, превращённый из
   их зрелости в наше свойство поставки.
4. **Measure-first, warn-first; носители хоста не мигрируются.**
   Первый слой установки — только учёт/журнал-адаптер ПОВЕРХ их
   носителей (их журнал остаётся их носителем — D-0082-логика);
   гейты приходят в warn-режиме и промоутятся в block по ИХ
   evidence утечек (D-0063). «Улучшать, не ломая» = сначала показать
   числа на их трафике, потом предлагать enforcement.

## Два режима — одна процедура (слово оператора 07-22: «продукт
## могут поставить и в пустой проект как начало разработки»)

Вилка intake онбординга — результат того же шага 0 (инвентаризация):

- **Greenfield (пустой проект / без агентной схемы).** Инвентаризация
  даёт пустую карту функций → все статусы ledger = adopt → полный
  дефолтный комплект. Это вырожденный случай ТОЙ ЖЕ процедуры, не
  отдельный путь — и это сегодняшний фактический режим кита
  (копия штаба), он остаётся валидным. Особенности режима:
  (а) предпосылки замыкаются тривиально — связки ставятся целиком,
  конфликтовать не с чем; (б) лестница warn→block сжимается: ломать
  нечего, enforcement легален с первого дня (warn-режим остаётся
  дефолтом против ложных срабатываний самих гейтов, но промоция не
  ждёт «их evidence» — его ещё нет); (в) чужих данных/носителей нет —
  учёт и журнал ставятся нашими носителями и копят baseline С
  ПЕРВОГО ДНЯ (преимущество режима: цифры экономии с рождения
  проекта, не «до/после»); (г) ledger создаётся ДАЖЕ пустым-adopt:
  он — носитель БУДУЩИХ отличий (проект вырастет и начнёт
  расходиться со схемой — расхождение записывается статусом
  native-equivalent/deferred туда же, а не живёт молчаливым дрейфом);
  (д) экзамен моделей хоста (D-0070) обязателен и здесь — пустой
  проект не значит наши привязки моделей.
- **Brownfield (живая агентная схема).** Полная процедура принципов
  1–4 выше; прецедент — AO3/F-51.
- Смешанные случаи (проект без агентов, но с CI/тестами/журналами)
  ложатся той же вилкой: непустые строки инвентаризации получают
  сопоставление, пустые — adopt. Бинарной границы режимов нет —
  режим определяется картой, не декларацией.

## Следствия для тулкита — ИСПОЛНЕНЫ кит-батчем 07-22 (по слову
## оператора; статусы закрыты этим же днём, поздняя отметка — ниже)

- ~~Онбординг-протокол кита: шаг 0 «инвентаризация функций хоста»~~
  — исполнено: SKILL.md онбординга, шаги 1–2 (intake-вилка
  greenfield/brownfield по карте; F-35 закрывается там же).
- ~~Шаблон ADOPTION_LEDGER.md~~ — исполнено:
  toolkit/ADOPTION_LEDGER.template.md (+ поле снимок-ревизии,
  D-0091).
- ~~Манифест предпосылок в каждом кит-механизме~~ — исполнено:
  таблица PREREQUISITES онбординг-скилла.
- ~~Порядок слоёв установки~~ — исполнено: measure-first → warn →
  block в скилле и D-0090.

## Развилки — РЕШЕНЫ словом оператора 07-22 (при чеканке D-0090;
## строки закрыты поздней отметкой того же дня — сама задержка
## отметки и есть класс «статус не двинут тем же ходом», D-0080)

1. ~~Чеканить сейчас или после ревью WP?~~ — РЕШЕНО: чеканка
   сразу (D-0090 отчеканен 07-22); раздел в WP добавлен (§4.3),
   его ревью Архитектором — отдельная живая очередь.
2. ~~Ledger — норма или рекомендация?~~ — РЕШЕНО: НОРМА ПОСТАВКИ
   (текст D-0090).
3. ~~Полигон валидации?~~ — РЕШЕНО И ИСПОЛНЕНО: валидация на двух
   полигонах, обе ветки DONE с вердиктами (DAG
   docs/tasks/2026-07-22_onboarding-validation.md): G
   (onboard_greenfield, 796 passed, урожай Finding B/C) и B (Dog,
   апгрейд без слома нативного, 224=224).

## Аудит фактической поставки Dog (2026-07-22, вход ревью; F-52)

Повод: провал их холодного critic-экзамена на T-K3 → их critic.md
оказался снимком до dc2164f (без кит-правил 12–15). Аудит
(t-282 норматив / t-283 пофайловый дифф кита против Dog) показал:

- **Роль-файлы — категория-невидимка.** Все три роли урезаны против
  кита: critic без правил 12–15; scout без правила 9 (финальное
  сообщение) и с урезанным правилом 4 (контроль формы); builder без
  правила 7 и без клаузулы манифеста в правиле 1. Ни D-0090, ни
  онбординг-скилл (правит только model:-frontmatter), ни леджер Dog
  (17 позиций) не знают роль-файлов как артефакта поставки;
  INSTALL.md Path B несёт их только при установке.
- **Хост-леджер — неполное зеркало без версии** (уточнено ревью
  t-284: critic_snapshot из первоначального списка исключён — он в
  леджере Dog есть, поз. 16, честный deferred). Строк hygiene_gate
  и permission_audit в их леджере нет вовсе, хотя текущий шаблон
  кита их несёт (строки 79/83); строка Role profiles шаблона (73)
  уронена при зеркалировании; ревизии-версии, показывающей
  устарелость зеркала, нет. Честные deferred/split
  (dispatch/dod/main-гейты, journal_echo, judge-стек R13) — решены
  записью, недоставкой НЕ являются.
- **Обновления принятых механизмов без канала.** validator,
  session_context, PROCESS-протоколы, скиллы разошлись с китом;
  сверка сейчас ад-хок (прецедент: их t-002 против нашего батча
  мелочей — preflight_quota, «не накладывать дважды»).

Требования к механизму — ОТЧЕКАНЕНЫ D-0091 2026-07-22 (ревью
critic t-284 fit_with_fixes, правки вшиты; поправка ревью:
категории Role profiles/Skills/PROCESS в шаблоне уже были — гэп не
в категориях, а в версии, полноте зеркала и ре-диффе контента):
- поставка ВЕРСИОНИРУЕТСЯ: леджер хоста несёт снимок-ревизию кита,
  обновляемую установкой и каждым апгрейд-батчем;
- апгрейд = ре-инвентаризация ДЕЛЬТОЙ-ПО-РЕВИЗИИ (не полный
  рескан; платит хост, цена ограничена дельтой батча — Rule #1):
  новые/изменённые с записанной ревизии кит-механизмы, включая
  КОНТЕНТ роль-файлов (не только model:-frontmatter), скиллы,
  tools, PROCESS;
- каждая позиция дельты получает леджер-решение (хотя бы deferred;
  молчание — не решение); комплектность хост-леджера сверяется с
  номенклатурой ТЕКУЩЕГО шаблона — ловит уроненные строки;
- принятая правка роли проходит экзаменационный гейт хоста ДО
  коммита (critic — seeded-дифф, scout — golden set, builder —
  witness-решение кита);
- новый кит-механизм получает строку ШАБЛОНА леджера тем же
  коммитом (норма номенклатуры; внутрикитовая пара — ось 7
  SIBLING_MAP);
- детекторы: штаб-сторонний чек 32 калибровки (сборка порт-батча:
  дельта от записанной ревизии деплоя ↔ строки леджер-дельты в
  носителе-хэндоффе) + хост-сторонний чек 22 кит-протокола
  (полнота/дрейф леджера против шаблона).
Дрейф врезки (замечание ревью (б).2) — ОТЧЕКАНЕН тем же вечером:
D-0092 (adopt-строка = обязательство врезки; непрерывный детектор —
wiring-чек хоста читает леджер; оконный — чек 22(b); строка шаблона;
порт обобщённого wiring_check.py в кит — за релизным гейтом).

## Детекторы — ЗАРЕГИСТРИРОВАНЫ чеканками (R10(в))

Исполнение кандидатов этой секции: ledger-дрейф/полнота хоста —
чек 22 кит-копии WEEKLY_CALIBRATION_PROTOCOL (D-0091); штабная
сторона поставки — чек 32 штабного протокола (D-0091); полнота
манифестов предпосылок — чек 12 пары ось-7 при релизных снимках
(D-0090). Документ более не драфт-без-детекторов — нормы и их
детекторы живут в DECISIONS/протоколах, это носитель замысла и
evidence.
