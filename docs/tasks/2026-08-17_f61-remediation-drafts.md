# Ремедиация F-61 — два принятых PASS-драфта спеки (экзамен designer v2, t-468/t-469, 08-16)

Экзамен ЗАКРЫТ 22:16 (PASS+PASS, sonnet-контроль FAIL — различение в
обе стороны; журнал t-467..t-470, DAG B8). Оба драфта — живой рабочий
материал: СЛИЯНИЕ/выбор — Lead-ход следующего захода, затем builder.
Заморозка посадки F-61 СНЯТА. Ниже — нормативное содержимое обоих
драфтов (следы чтения и экзамен-мета опущены; полные тексты — вывод
агентов t-468/t-469, эфемерен). Третий источник деталей —
sonnet-контроль t-470 (его принятые сверх-находки внизу).

## ОБЩЕЕ ЯДРО (сошлись оба драфта — брать как решённое)

- Сдача sibling-файлами `*_f61.py`, живые пути хуков не трогаются
  (D-0069); размещение Lead байт-копией + liveness-проба с откатом
  (D-0093); откат только из байт-копии.
- Атомарная запись: mkdir СОХРАНЯЕТСЯ дословно (dod_track:520 —
  регресс-пин!); запись в УНИКАЛЬНО именованный tmp в том же
  каталоге; `os.replace`; суффикс `.tmp` ПОСЛЕДНИЙ и имя не
  оканчивается на `.json` (иначе session_context:1528
  glob("*.json") прочтёт осколок как трек).
- Тотальный try в main() (dod_track): одна строка stderr
  `<file>.py: FAILED to ... (<Type>: <msg>)`, rc=0 всегда;
  _reconfigure_stderr_utf8 ДО try.
- Guard не-dict payload И ВЛОЖЕННОГО tool_input (оба файла:
  dod_track + critic_snapshot:280-284 — сосед по D-0100, оба драфта
  включили; образец — journal_echo:1885).
- critic_snapshot: prev-ПРОЕКЦИЯ (prev_ts/prev_tree_hash) читается в
  main() ДО рискованной записи, передаётся параметром;
  _write_failure_snapshot snap не читает; инвариант-пины
  test_critic_snapshot.py:289-291, :296-315, :317+ зелёные без
  правок; atomic-запись ОБОИХ документов (успешного и отказного).
- session_context: ack ТОЛЬКО после успешной выдачи; одна запись
  вывода + явный flush ВНУТРИ try; fail-open-обработчик пишет в
  stderr (не в упавший stdout) под собственным try; return 0
  безусловен. Ack-инвариант держится КОНСТРУКЦИЕЙ (фильтр по
  фактически вошедшим в усечённый список строкам), не позицией.
- Дискриминирующий тест атомарности: мок, реально усекающий до
  броска (не json.dumps-до-открытия); контр-прогон на непатченном
  коде КРАСНЫЙ — обе половины дословно в witness.
- lost-update гонка параллельных PostToolUse НЕ решается — явный
  non-goal с цитатой докстринга :141-148; батч обязан лишь не
  превратить её в разрушительную (уникальный tmp).
- Канон: базовый счёт свежим прогоном ДО правок; после — множество
  упавших не шире базового; F-60-правило различения среды/дефекта.
- Кит-твины — очередь релизного батча (осознанное отступление от
  оси, прецедент NOTES LEN); строка в носитель по D-0082.

## ДРАФТ 1 (t-468) — отличительное

- Форма сдачи F1(а): 5 сиблингов + 5 НОВЫХ тест-файлов test_*_f61.py
  с константой MODULE_UNDER_TEST (посадка Lead = одно слово на файл);
  тест-файлы остаются в каноне навсегда.
- F3(а): dod_gate:273/main_gate:173 В БАТЧ (класс не закрыт без них —
  3 из 4+ писателей одного файла); карта получает +2 носителя.
- F6(б): детектор = юнит-тесты D1 в батче; кейс hook_liveness_probe —
  строкой очереди (probe non-goal «не трогать байты гейтов»,
  LIVE-STATE-TOUCHED риск, Windows chmod ненадёжен).
- F4(б): локальная копия хелпера в каждом файле (носитель конвенции —
  dod_gate:411 «Локальная копия, не импорт»).
- F8: лимит строки stderr 200/150-sanitize с тестами на границе и за.
- F2: канал warning — I(б) stderr всегда + best-effort stdout;
  II(б) чистая пара select_break_glass_lines + defer_ack, старые
  функции сохраняются (30 тестов зелёные).
- Края: MAX_LINES 25/[cap 5] границы; ENAMETOOLONG-мок; payload 1МБ/
  5МБ; вложенность 200; \x00 и \r\n в полях; session_id пустой.
- DoD: базлайн → контр-красный → суженный по owns (все test_* owns) →
  контр-зелёный → канон → pre-placement baseline пробы живости.
- Handoff: постановка с кейсами пробы 9/10/12 + dod_gate/main_gate
  (:908/:919), CRASH недопустим; флип FINDINGS/карты — после посадки;
  очередь: hookspath-autofix, read_stdin non-TTY.

## ДРАФТ 2 (t-469) — отличительное

- Форма F1(а): 3-5 сиблингов + ОДИН новый tools/test_f61_halfstate.py
  с env-переменной F61_TARGET (fixed→*_f61 по умолчанию / live→живые)
  — контр-прогон RERUNNABLE кем угодно; плюс §1.3.7 двухмирная правка
  трёх пинов канала (:363/:555/:592) на объединённый поток — зелена
  до И после посадки (временной край).
- F2(а) с критическим нюансом: гейты dod_gate/main_gate при включении
  получают ТОЛЬКО temp+replace, БЕЗ внешнего fail-open — они обязаны
  блокировать rc=2, иначе фикс гасит enforcement (конфликт-пара 5).
- F3(б)-анализ: общий хелпер tools/atomic_write.py попал бы в
  транзитивное замыкание проводки → test_mechanism_gate:942
  потребовал бы правки MECHANISM_PREFIXES (механизм в механизме) —
  поэтому локальная копия дешевле (сошлось с драфтом 1).
- MECHANISM_PREFIXES несёт все пять путей ПОИМЁННО → коммит посадки =
  механизм (осевой блок + tier), коммит сиблингов — нет
  (handoff-факт).
- A12: тест-сторож неизменности РЕАЛЬНОГО .claude/ репо при прогоне
  батареи; A9 граница MAX_LINES для ack; chmod-Windows семантика
  (пишется-нельзя/читается-можно) — из докстринга
  test_critic_snapshot:21-27.
- F8-доклад: _load_track на битом JSON теряет чужие ключи легально
  (то же последствие, другой триггер) — строка очереди по решению
  Lead. F9: session_id с разделителем пути — фиксировать как есть,
  валидация = новый лимит.
- Именование: прецедент session_context_b3.py/_d0076.py; батарея
  выбирается одним -k f61.

## ПРИНЯТЫЕ СВЕРХ-НАХОДКИ SONNET-КОНТРОЛЯ (t-470)

- Sibling-first индирекция SCRIPT в subprocess-смоках (test_dod_track
  SCRIPT захардкожен на боевой путь — без индирекции e2e-смок гоняет
  НЕПАТЧЕННЫЙ файл всю дистанцию: зелёный прогон доказывает не то).
- Развилка break_glass_ack.json: НЕ включать (fail-safe направление —
  битый sidecar = повторный показ, не потеря); строка в раздел
  «Решения о НЕ-заведении» карты.
- mkstemp(dir=..., prefix=path.name+".", suffix=".tmp") как схема
  ОС-уровневой уникальности (альтернатива pid+uuid драфтов 1/2).

## РАЗВИЛКИ К РЕШЕНИЮ LEAD ПРИ СЛИЯНИИ (расхождения драфтов)

1. Тест-форма: 5 файлов с MODULE_UNDER_TEST (д1) vs один
   test_f61_halfstate.py с F61_TARGET (д2, rerunnable — сильнее по
   духу негативного контроля) vs индирекция в СУЩЕСТВУЮЩИХ тестах
   (t-470). Склонение: д2-форма + sibling-first SCRIPT от t-470.
2. Гейты в батче: д1 даёт им ту же охрану, д2 — только temp+replace
   без fail-open (гейты обязаны блокировать). Склонение: д2.
3. Лимит stderr-строки: д1 вводит (200/150 + тесты), д2 не вводит.
4. Канал warning: д1 I(б) stderr+best-effort stdout; д2/t-470 —
   stderr-only с двухмирной правкой пинов. Склонение: д2.
5. tmp-схема: pid+uuid (д1/д2) vs mkstemp (t-470).
