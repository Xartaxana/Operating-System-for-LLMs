# Петля самоэволюции обвязки — итерация 2 (первая автоматическая)

Авторизация: СТОЯЧАЯ, слово оператора 2026-08-27 («запускать петлю
каждый день»); запуск — scheduled task `harness-evolution-loop`
08:08, оператора у клавиатуры нет (автономный прогон, границы
автономии — раздел «Посадка» протокола).

Окно замера: 2026-08-27T16:05 (старт итерации 1) → 2026-08-28T08:20.
Потолок кандидатов: 3. Ярус на входе (D-0056a): фактическая модель
`claude-opus-5` = `roles.lead` (delegation.config.yaml:29-31) — полный
Lead, окно деградации не требуется, посадка механизмов легальна.

Метрика итерации: рецидивы классов; сторожевые — warn_density,
канон, corpus_growth, spend, boot-бюджет.

## Узлы

| узел | этап | исполнитель | статус |
|---|---|---|---|
| A | 1. Сбор: canon, corpus_growth --check, wiring_check, warn_density, permission_audit, spend_report, calibration_counts, срез журнала окна ВКЛЮЧАЯ rejected/defect_found по failure_class (фикс дефекта спеки итерации 1) | скрипты (R8f), координатор | ИСПОЛНЕН — дайджест ниже |
| B | 2. Диагноз рецидивов (Q1-Q4) | scout (фон) | ИСПОЛНЕН t-654 — разбор ниже |
| C | 3. Драфт кандидатов (≤3) | designer | В РАБОТЕ: C1 (t-655), C2 (t-656); третий слот НЕ добирается — рецидивов ровно два, драфты ради числа протокол запрещает |
| D | 4. Адверсариальный гейт | critic | ИСПОЛНЕН t-657: fit_with_fixes оба, 4 блокера факта из 27, 16 фиксов, ОСНОВАНИЕ C2 ОПРОВЕРГНУТО замером |
| E | 5. Посадка прошедших гейт (автономно) / очередь «к оператору» | Lead | В РАБОТЕ: builder t-658 (объём сужен), нормативные тексты посажены Lead, очередь оператору — в CURRENT_CONTEXT |
| F | 6. Замер предсказаний итерации 1 | координатор | ИСПОЛНЕН — ниже |

## Этап 1 — дайджест сбора (2026-08-28 08:08-08:14)

**КАНОН** (`python -m pytest tools/ gateway/ -q`): прогон запущен на
старте итерации (протокол: красный канон → сначала канон). Результат —
в разделе «Канон» ниже.

**wiring_check:** WIRING: OK (git hooks: pre-commit, commit-msg; 12
hook-файлов импортируемы; skills casing 5 ok).

**corpus_growth --check:** ИТОГ corpus=1020603 (Δ−45339 — эффект
диеты итерации 1), порогов 18/19, WARN=1 BREACH=1.
- **НОВЫЙ BREACH: `DECISIONS.md` bpr=243 > порога 241** (bytes 27013,
  records 111, Δbytes +514, Δrecords +1). Пробой дала ОДНА индекс-строка
  D-0111 в 514 Б при среднем пороге 241.
- BREACH прошлого окна (POLICY_FULL bpr 8522>8027) СНЯТ диетой:
  Δbytes −62880, bpr 2705 — ниже 95% полосы.
- WARN: `PROCESS/checks/CHK-13.md` (1689, без движения).
- РОСТ ВГЛУБЬ (байты без новых записей): WEEKLY_CALIBRATION_PROTOCOL
  +97 Б/зап, RC-01 +32 Б/зап.
- Дефектов 0, выбывших 0.

**warn_density** (окно с 2026-08-27T16:00, `--no-sidecar`): calls
всего 11, tool_use в окне main-потока 126 (субагентский поток невидим:
519 из 645 — свойство транскрипта). Сработали: HYGIENE 4, GIVEN_PATH 2,
NEGATIVE_CLAIM 2, NOTES_LEN 1, WITNESS_ECHO 1, R3_ZERKALO 1. Дефектов 0.
- **Секция «НАХОДКИ О ЧЕКЕ»: «(сходится)»** — расхождений реестр↔чек
  11(в) НОЛЬ. Предсказание итерации 1 (посадка C2) ИСПОЛНЕНО.

**calibration_counts** (`--journal logs/routing-log.jsonl`; окно от
последнего `calibrated` = 2026-08-20): SPEC-RECIDIV (R11(n)) **5 spec
из 7 rejected (71.4%), ретро 0, ПОРОГ СРАБОТАЛ** (строки 1490/1525/
1553/1574/1580, все by=opus). Незакрытых задач нет. Непарсящихся строк
нет. False-accept по ярусам: builder 17/319, lead 6/15, scout 4/158.

**Срез журнала ОКНА ПЕТЛИ** (2026-08-27T16:05 → 08-28T08:14) —
ИСПОЛНЕНИЕ ФИКСА дефекта спеки этапа 1 итерации 1 (rejected/
defect_found теперь считаются):
- delegated 8, accepted 9, dispatch_skipped 1
- **rejected 0, defect_found 0, escalated 0** — по failure_class
  разбивка пуста (нечего разбивать).
- Оговорка честности: окно 16 часов и ОДНА сессия (посадка итерации 1),
  из них ~14 часов — простой. Нулевые счётчики — свойство ТОНКОГО ОКНА,
  а не доказанное улучшение.

**permission_audit:**
- за 1000 мин (включает дожизнь блока): 166 вызовов, 104 suspects;
  класс python -c/heredoc — 24 (suspects 24).
- за 810 мин (≈ с 18:18, блок активирован в 18:40:13 коммитом 8eb25b0):
  30 вызовов, 11 suspects; класс python -c/heredoc — **1** (suspects 1);
  cd/Set-Location 2 (suspects 0), 2>&1 — 1.

**spend_report --window last --compare prev:** окно то же, что в
итерации 1 — закрытое `shtab:calibrated:8` (08-14→08-20): текущее окно
не закроется до калибровки №9. НОВОГО СИГНАЛА НЕТ (граница метода,
не дефект). Полный прогон 112.5с (>60с, E11).

## Этап 6 — замер предсказаний итерации 1

Итерация 1 взвела три предсказания. Замер:

| предсказание | замер | вердикт |
|---|---|---|
| 0 новых расхождений реестр↔чек 11(в) | warn_density: «НАХОДКИ О ЧЕКЕ: (сходится)», 20 слоёв, все `11в=да` | **ИСПОЛНЕНО** |
| SPEC-RECIDIV против базлайна 5/7 | счётчик неподвижен: 5/7 (71.4%) — в окне петли НОЛЬ событий `rejected`, менять числу нечего | **НЕ ИЗМЕРЕНО** (окно тонкое; счётчик жив и печатает — см. находку слепого пятна, Q3 к t-654) |
| класс python -c/heredoc < 350 после активации блока | 1 экземпляр за 810 мин против 24 за 1000 мин; но популяция окна — 30 вызовов Bash/PS против 166 | **НЕ ИЗМЕРЕНО ЧЕСТНО**: направление верное, знаменатель на порядок меньше базлайна — числа несравнимы (п.6(б) гигиены: сравнение в одной метрике). Замер переносится на первое ПОЛНОЕ рабочее окно |

Сторожевые: канон — ниже; boot-бюджет 90542/100000 WARN, CLAUDE.md
37494/37500 (запас 6 Б — кандидат ближайшей диеты, унаследован);
corpus суммарно 1020603 (Δ−45339).

## Батч мелочей итерации 1 — сверка исполнения

| пункт | состояние на 08-28 |
|---|---|
| (а) owns_gate/dispatch_gate сиблингами в класс «один канал следа» | **ИСПОЛНЕН**: docs/SIBLING_MAP.md:987-988 несёт оба носителя-экземпляра |
| (б) permission_audit сыплет SyntaxWarning invalid escape ×13 | **НЕ ВОСПРОИЗВЁЛСЯ** в трёх прогонах этой итерации (вывод чист). Причина не установлена (вероятно кэш .pyc); в очередь наблюдения, не в кандидаты |
| (в) протухшая строка docs/tasks/2026-08-25_queue8-closure.md (пара 11(в)↔warn_layers «14 имён») | **НЕ ИСПОЛНЕН** — строка жива и протухла (реестр 20). Экземпляр F-41 в носителе хода |

## Этап 2 — диагноз (t-654, scout, приёмка по трейлу R1b)

Полный дайджест — транскрипт t-654. Главное:

### Q1 — класс «реакция на сторожевой WARN записана как ДИСЦИПЛИНА без носителя и без детектора»
**НОВОГО класса нет: порода уже зарегистрирована в карте осью 14**, и
данный прецедент — одна из её клеток. Дополнительных живых экземпляров
той же узкой формы (обещание прозой в носителе хода, без строки в норме
и без детектора, нигде не зарегистрированное) разведка не нашла.
Позитивный контроль формы: grep «короче среднего» по `docs/**/*.md` —
РОВНО ОДИН хит, сам прецедент (`docs/tasks/2026-08-25_queue8-closure.md`).
Соседние WARN проверены и отсеяны с основанием: CHK-13 несёт явный
non-goal «вынос бесполезен» (обещания не было); RULE_COVERAGE-диета —
открытая, видимая очередь, не молчащее обещание.
ЗНАЧИМОЕ СЛЕДСТВИЕ: ось 14 держит РОВНО ДВЕ непокрытые сторожем клетки
(`SIBLING_MAP.md:522-523`) — `bpr_max` и `WARN_TEXT_BUDGET_CHARS`. Первая
только что рецидивировала материально (BREACH DECISIONS.md). → кандидат C2.

### Q2 — класс «тест стоит на живом растущем носителе с порогом» (обход, отложенный итерацией 1)
**ОБХОД ЗАКРЫТ: уязвимых сиблингов нет.** Двенадцать кандидатов под
`tools/` и `gateway/` перечислены с вердиктом на каждый; ни один не
способен стать вакуумно-зелёным или красным ОТ РОСТА носителя:
- уже на синтетике: `test_dispatch_gate.py:3106-3140` (фикс итерации 1),
  `test_journal_echo_r3.py:859-914` (замороженная фикстура, снята 08-25
  ПО ТОЙ ЖЕ ПРИЧИНЕ — класс был опознан и починен ещё до итерации 1);
- самосознательный пин: `test_dispatch_gate.py:3143-3150` — при усадке
  журнала ниже капа тест УПАДЁТ явно, а не промолчит;
- не уязвимы по построению: проверки СУЩЕСТВОВАНИЯ, структурные сверки
  якорь↔указатель, ассерты на присутствие task_id в append-only журнале,
  `test_claude_ratchet_guard.py` (ожидаемое значение само измеряется);
- `gateway/`: обращений к живым растущим носителям с порогом нет
  (контроль формы: тот же запрос на `def test` дал 206 совпадений —
  инструмент исправен, пустота не промах вызова).
**Вывод: строка в карту НЕ заводится** — класс уже покрыт осью 5 и
починен в обоих известных экземплярах; заводить ось под класс без
живых непокрытых экземпляров — рост карты без носителя.

### Q3 — ГЛАВНАЯ НАХОДКА ОКНА: слепое пятно счётчика SPEC-RECIDIV
Механизм, посаженный итерацией 1 ВЧЕРА, видит меньшинство своего класса.
`tools/calibration_counts.py` сканирует исключительно `event=="rejected"`
(:191, :447, :476 — спот-проверено Lead). За 2026-08-20…08-27 разведка
выписала **14 самопризнаний дефекта диспетчера, живущих ВНЕ событий
`rejected`** — в notes событий `delegated`/`accepted` и в событиях
`defect_found`/`decomposable` (строки журнала 1523, 1529, 1566, 1569,
1573, 1634, 1636, 1639, 1648, 1654, 1661, 1689, 1721, 1722). Видимых
счётчику — 5. Его «5 из 7, 71.4%, ПОРОГ СРАБОТАЛ» описывает не класс, а
подвыборку.

**ПОПРАВКА ЧЕСТНОСТИ (внесена по §A5 драфта t-655, принята Lead):**
первоначальная формулировка этого узла считала «≥19 экземпляров, видны
5 → ~26%». Так писать НЕЛЬЗЯ: 19 стоит на классификации всех 14 как
экземпляров R11(n) (разведка перечитала не все) и на полноте перечня, в
которой она сама призналась обратном (узкий греп пропустил 2 из 14).
Печатать долю от заведомо неполного перечня — воспроизвести ровно тот
дефект, который кандидат чинит. **Честная форма: ≥14 экземпляров вне
поля зрения счётчика; измеритель неполон; доля НЕ вычисляется.**
Методическая оговорка разведки (важна для C1): её УЗКИЙ grep по формулам
самопризнания пропустил 2 из 14 — парафраз. Текстовый матч по
естественному языку заведомо неполон как измеритель.
Дом породы — ось 15 карты (популяция срабатывания ↔ знаменатель доли),
смежная ось 14 (сторож-декорация). → кандидат C1.

### Q4 — отсев одиночек (в кандидаты НЕ идут)
- `permission_audit` SyntaxWarning ×13 — не воспроизвёлся в трёх прогонах;
  нет второго экземпляра и нет устойчивости. Очередь наблюдения.
- CHK-13 WARN 1689 без движения — явный принятый non-goal, не обещание.
- РОСТ ВГЛУБЬ WEEKLY_CALIBRATION_PROTOCOL/RC-01 — те же ДВА носителя, что
  и в итерации 1: продолжение роста опознанного сигнала, спроектированного
  без порога намеренно, а не второй независимый экземпляр.
- `WARN_TEXT_BUDGET_CHARS` — под узкую форму Q1 не подходит (у него
  ОТКРЫТАЯ очередь, не молчащее обещание); но как клетка оси 14 идёт в C2
  по R9-обходу соседей внутри таблицы.

### Развилки разведки, поднятые наверх
1. Брать ли вторую непокрытую клетку оси 14 в тот же кандидат.
   **РЕШЕНО Lead:** брать — R9 требует обхода соседей ВНУТРИ артефакта
   прежде обхода карты, обе клетки лежат в ОДНОЙ таблице; designer'у
   затребован вердикт на каждую из четырёх клеток.
2. Форма исправления слепого пятна Q3 (расширить матч / обязать эмитить
   событие) — отдана designer'у C1 развилкой, с довеском Rule #1:
   обязанность эмитить рискует ОБУЧИТЬ НЕ ПРИЗНАВАТЬСЯ.

## Этап 4 — адверсариальный гейт (t-657, critic, независимый контекст)

Вердикт: **оба `fit_with_fixes`**, 16 фиксов поимённо, **4 несущих
блокера факта из 27 проверенных** утверждений драфтов. Полный текст —
транскрипт t-657. Что решило исход:

**1. Основание C2 ОПРОВЕРГНУТО ЗАМЕРОМ (главное).** Я просил критика
провести симметричную атаку по Rule #1. Он провёл её и выиграл:
`git log --follow -- tools/corpus_manifest.json` даёт 4 коммита
(cf309e1 завёл 12 артефактов с `bpr_max: null`; 05bfe88 перевёл 11
`null→N`; 12fe315 и 9cee6b9 добавили артефакты с НАЧАЛЬНЫМИ порогами).
**Подъёмов существующего числа — ноль. Снижений — ноль.** Позитивный
контроль формы: тот же запрос по `CLAUDE.md` находит три коммита.
Единственный прошлый BREACH (POLICY_FULL) был отвечен диетой, а не
подъёмом. Класс, ради которого предлагался сторож, **не рецидивировал
ни разу** — а этап 2 протокола петли требует рецидива. Сажать его
значило бы завести механизм под несуществующий класс, то есть ровно
«объявление вместо величины», за которое заведена ось 14.

**2. Настоящий живой дефект — ДРУГОЙ, и его нашёл гейт.** Процедура
bootstrap драфта C2 не исполнима: правило «замер из первой записи
сайдкара за 08-19» воспроизводит порог у **12 из 18**, а порог
`PROCESS/checks/CHK-26.md` = 18875 **не воспроизводится НИ ОДНОЙ из
21 записи** — все замеры за жизнь артефакта 14183 и 16490, а нужен bpr
16413-16414. Провенанс утрачен безвозвратно. Это порода с шестью
экземплярами, и она НЕ диагностировалась этапом 2 — значит, входит в
этап 2 итерации 3, а не сажается задним числом.

**3. Позиция по границе автономии подтверждена и УСИЛЕНА.** Я отдал
критику свою предварительную позицию по Р8 на атаку. Он пытался её
опровергнуть и не смог, добавив два основания к моему одному:
(i) сама норма, которую предлагал C2, требует у подъёма слова
оператора — петля, посадив её и тут же исполнив подъём, нарушила бы
механизм В МОМЕНТ ДЕБЮТА; (ii) единственный подъём порога этой породы
у брата подписан словом оператора 2026-08-20 (решение Р-D), обход =
отмена решения оператора, пункт (в) раздела «Посадка»; (iii) моё —
R11a, выбор формы результата. Все 20 развилок получили метку; к
оператору ушли Р8 и порт-пункты оси 7, остальные — петля решает сама.

**4. Обход класса не был завершён ни одним драфтом** — гейт нашёл
**пятый экземпляр оси 14**, который пропустили оба: пороги
`SPEC_RECIDIV_MIN_COUNT=3`/`MIN_RATIO=0.40`
(`calibration_counts.py:61-62`) — число от одного замера, с
провенансом в докстринге, и чек 13(г) САМ ссылается на «дисциплина оси
14». То есть претензия на монотонность здесь ЗАЯВЛЕНА, в отличие от
клетки 3. C2 сказал «соседей больше нет: строк ровно четыре» — верно
про СТРОКИ, неверно про ЭКЗЕМПЛЯРЫ.

**5. Эмпирика гейта безопасна.** Мутационные пробы велись на КОПИИ
дерева в scratchpad, `git checkout` не применялся, витнесс сохранности
приложен (`tools/` чист, константа на месте). Проба клетки 4 дала
находку сверх драфта: «лежачий полицейский» ДВУСТОРОННИЙ — снижение
550→520 роняет ТЕ ЖЕ два теста, что и подъём 550→800, поэтому
предлагавшаяся драфтом норма «снижение бесплатно» была бы машинно
ложной (C2-F3).

## Этап 5 — посадка (решения Lead)

**САДИТСЯ (автономно, builder t-658) — один класс, два измерителя:**
1. **C1, объём (А) — только D1**: строка популяции в блоке
   SPEC-RECIDIV. Текст задан мной дословно (развилка Р7). Основание
   сужения: D2 (маркер-токен) детектора НЕ ИМЕЕТ — его третий слой
   («0 экземпляров при ≥N диспатчей = находка») держится на N, которое
   остаётся буквой; а став числом, N родилось бы порогом от одной
   точки, то есть НОВЫМ экземпляром оси 14 тем же ходом. R10(в):
   без детектора — не механизм, а пожелание.
2. **C2-Р6** — знаменатель `порогов 18/19` → `18/18 · вне порога
   (boot-mirror): 1` + строка `БЕЗ ПОРОГА:`. **Обязателен по R9**:
   это ТОТ ЖЕ класс, что и п.1, и живёт он внутри самого измерителя;
   закрыть один и не закрыть другой = частичный фикс класса.

**НЕ САДИТСЯ:**
- **Сторож храповика `bpr_max`** — основание опровергнуто (п.1 выше),
  плюс C2-F1 блокирует критерий К2. В очередь итерации 3 с
  ПЕРЕПИСАННЫМ основанием (утраченный провенанс).
- **Клетка 4 в формулировке драфта** — норма «снижение бесплатно»
  машинно ложна (C2-F3). В очередь итерации 3.
- **A7 (развёртка 13(г) в `CHK-13.md`)** — измерено: запас файла
  **334 Б**, переносимый текст **1239 Б**; дословный порт ввёл бы
  ВТОРОЙ BREACH корпуса той же итерацией, которая на BREACH жалуется.
  В очередь с указателем: сперва диета `CHK-13.md` (он уже WARN).

**ПОСАЖЕНО ТЕКСТОМ Lead (мои owns, с билдером не пересекаются):**
- `PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md` 13(г) — **ПРОБЕЛ
  ПОПУЛЯЦИИ**: недосаженный вчера пункт принятой рекомендации Р5.
  Мой дефект посадки от 08-27, экземпляр измеряемого класса.
- `docs/SIBLING_MAP.md` — клетка 3 оси 14 (носитель поправлен:
  `bpr_max` живёт в манифесте, не в инструменте — C2-F4; вердикт
  «проверен, сторож не посажен сознательно» с замером); клетка 4
  (механика прежней записи была неверна — поправлена прогоном);
  **пятая строка** оси 14; **две строки** оси 15 + расширение её
  преамбулы (порода не ограничена warn-слоями; «реестра нет,
  измеритель — сам скрипт» вместо пустой колонки — C1-F6).
- `docs/RULE_COVERAGE.md` — строка W3 обновлена (19 артефактов вместо
  «12», 63 теста вместо «54» — экземпляр F-41, девять дней протухания
  при живом чеке 24) + честная строка «сторожа монотонности нет».
- `CURRENT_CONTEXT.md` — два вопроса оператору краткой формой + указатель
  сюда. САМОВСКРЫТИЕ: первая редакция этой записи весила 5.3 КБ и несла
  весь разбор — нарушение Maintenance Rule самого файла («пункту очереди
  — несколько строк: нерешённое + ВЛАДЕЛЕЦ полного текста») и удар по
  boot-бюджету (90542 → ~95817 из 100000). Поймано моим же замером,
  сжато до ~950 Б; разбор живёт здесь, где ему и место. Экземпляр
  R11(n)-класса в собственный счёт — и снова вне поля зрения счётчика.

### ОЧЕРЕДЬ ИТЕРАЦИИ 3 (петля решает сама, оператора не ждёт)
- **(а) ГЛАВНОЕ — порода «число с претензией на провенанс, чей
  провенанс утерян».** Замер гейта: правило «замер из первой записи
  сайдкара 08-19» воспроизводит порог у 12 из 18; шесть — нет
  (`CHK-26.md`, `CHK-18.md`, `CHK-13.md`, `FINDINGS.md`,
  `WEEKLY_CALIBRATION_PROTOCOL.md`, `RULE_COVERAGE.md`); у `CHK-26.md`
  (18875) провенанс не восстановим в принципе. Это вход этапа 2.
- **(б)** клетки 3 и 4 оси 14 + пятый экземпляр (пороги
  `SPEC_RECIDIV_*`) — у всех один вопрос «чем обоснована ВЕЛИЧИНА»;
  решать порознь значит применять правило избирательно.
- **(в)** машинный счёт пробела популяции SPEC-RECIDIV — только с
  РЕАЛЬНЫМ детектором; маркер-токен без него — пожелание (R10(в)).
- **(г)** пара оси 4: `PROCESS/checks/CHK-13.md` не несёт развёртки
  13(г); порт требует 1239 Б при запасе 334 Б → сперва диета файла.
- **(д)** класс «носитель числа-нормы вне невода коммит-гейта» — два
  экземпляра: `corpus_manifest.json` и `calibration_counts.py`.

**НОВОГО D-НОМЕРА НЕТ СОЗНАТЕЛЬНО.** Ход дочиняет уже принятое решение
(рекомендация Р5 от 08-27) и правит записи существующих осей — это
АМЕНДМЕНТ строками реестров, а не новое решение. Плюс прямая причина:
`DECISIONS.md` в BREACH, а реакция на него — развилка, ждущая
оператора; углублять пробой новой индекс-строкой, пока выбор не сделан,
значит предрешать его.

## Канон

Прогон `python -m pytest tools/ gateway/ -q` от 2026-08-28 08:08
(фоновый `by2s4mcim`), verbatim хвост:

```
4925 passed, 29 skipped, 34 warnings in 407.36s (0:06:47)
[exited with code 0]
```

**КАНОН ЗЕЛЁНЫЙ** — условие протокола «красный канон → сначала канон»
не сработало, итерация идёт дальше. Число совпадает с BATCH CANON
итерации 1 (4925/29) — регрессии за окно нет.

**КАНОН ПОСЛЕ ПОСАДКИ** (прогон `blcb3yvtr`, снят МНОЙ независимо,
уже ПОСЛЕ моих текстовых правок карты/протокола/RULE_COVERAGE):

```
4935 passed, 29 skipped, 34 warnings in 385.59s (0:06:25)
[exited with code 0]
```

+10 к базе — ровно число новых тестов (4 + 6). Совпал с каноном
билдера. ЗАЧЕМ ПЕРЕПРОГОН: витнесс билдера снимался, пока мои
текстовые посадки шли параллельно, а карту и протокол пинят
структурные тесты (`test_rule_coverage_bodies`,
`test_sibling_map_liveness`). Принять по витнессу, снятому ДО правок, —
ровно тот класс, который эта итерация чинит.

**ЗАМЕР КОРПУСА ПОСЛЕ ВСЕЙ ПОСАДКИ** (для очереди оператору — свежий):

```
ИТОГ: сумма байт corpus=1028779 (Δ-37163) · порогов 18/18 ·
      вне порога (boot-mirror): 1 · WARN=1 BREACH=1
РОСТ ВГЛУБЬ: PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md +147 Б/запись
```
Новых пробоев мои правки НЕ дали: BREACH по-прежнему один
(`DECISIONS.md` bpr 243 > 241 — предмет вопроса (1) к оператору),
WARN один (`CHK-13.md`). `RULE_COVERAGE.md` устоял в OK (bpr 1004 при
границе полосы 1005.1 — запас 1 байт, замерено; кандидат диеты).
Счётчик порогов печатается уже В НОВОЙ ФОРМЕ — механизм этой итерации
виден в собственном замере.

## ИТЕРАЦИЯ 2 ЗАКРЫТА 2026-08-28

**Дистанция:** 5 диспатчей (t-654 scout, t-655/t-656 designer,
t-657 critic, t-658 builder) при потолке протокола ~15; кандидатов 2
при потолке 3 — третий НЕ добирался, рецидивов было ровно два.

**Главный результат — отрицательный, и он ценнее посаженного:**
кандидат, выглядевший убедительно на этапах 2-3, был остановлен
гейтом на замере. Петля не посадила механизм под класс с нулём
рецидивов. Это первый случай, когда её собственный этап 4 сработал
против её же этапа 3.

**Второй результат — измеримый:** механизм, посаженный ВЧЕРА, за сутки
показал, что видит меньшинство своего класса (5 против ≥14). Его
починка — не новый механизм, а дочинка недосаженной половины уже
принятой рекомендации. Это ровно то, ради чего петля ежедневная.

**Предсказания для итерации 3 (метрика этапа 6):**
1. Строка популяции печатается на КАЖДОМ прогоне `calibration_counts`
   — проверяется дословным грепом вывода; её исчезновение обязано
   ронять канон (детектор C1-F1 посажен).
2. Счётчик порогов печатает `18/18 · вне порога (boot-mirror): 1`;
   форма `k/19` не возвращается.
3. Класс «дефект диспетчера вне события `rejected`» — считать
   экземпляры окна ВРУЧНУЮ (машинного счёта нет сознательно) и
   сравнить с ≥14 за окно 08-20…08-27. Ожидание НЕ «станет меньше»:
   ожидание — что число будет НАЗВАНО, а не потеряно.
4. Порода «утраченный провенанс» (6 экземпляров) входит в этап 2
   итерации 3 первым вопросом.

**Что осталось незакрытым СОЗНАТЕЛЬНО** (не забыто — записано):
сторож храповика `bpr_max`, клетка 4 оси 14, пятый экземпляр оси 14,
машинный счёт пробела популяции, пара оси 4 `CHK-13.md`, класс
«носитель числа-нормы вне невода». Все — в очереди итерации 3 выше.
