# RULE_COVERAGE — словесные политики и их сторожа (заведён 2026-07-28 словом оператора)

Карта «правило → что его сторожит → как реализовано». Грузится по
требованию (не boot-путь). Снимок-предшественник — enforcement-gap
аудит (task_reports/2026-07-24_enforcement-gap-audit.md); этот файл —
ЖИВОЙ владелец картины.

ОБЯЗАННОСТЬ ПОДДЕРЖАНИЯ: новый механизм или смена РЕЖИМА срабатывания
существующего — строка/правка здесь ТЕМ ЖЕ коммитом (пара оси 4
SIBLING_MAP); свежесть утверждений — чек 24 калибровки (документ в
списке владельцев). Протухшая строка здесь = находка F-41.

## Режимы срабатывания (легенда)

- **БЛОК-ХУК** — действие не произойдёт: хук харнесса возвращает блок
  до/вместо исполнения (exit 2 + stderr).
- **WARN-ХУК** — действие происходит, но координатор получает сигнал
  в контекст (additionalContext); суждение за ним (D-0063 warn-first).
- **ГЕЙТ КОММИТА** — сделать можно, закоммитить нельзя: pre-commit /
  commit-msg скрипт валит коммит.
- **ВСПЛЫТИЕ** — SessionStart печатает факт в контекст каждой новой
  сессии (не блокирует, но неизбежно видим).
- **ЧЕК N** — номерная проверка еженедельной калибровки; единственный
  дом всех номеров — чек-лист PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md
  («механизм → нарушение → как проверить»); расшифровка упомянутых в
  этом файле номеров — справочник в конце документа.
- **AI-СЛОЙ** — соответствие ПО СМЫСЛУ судит ярус выше (приёмка Lead,
  критик, судья); код гарантирует встречу, AI — вердикт (D-0063).
- **КОНФИГ** — нарушение исключено конфигурацией (харнесс не даст).
- **ЭКЗАМЕН** — регрессионный набор с закреплённым ключом.
- **ДИСЦИПЛИНА+ДЕТЕКТОР** — машинной встречи нет сознательно; отказ
  ловит НАЗВАННЫЙ чек (D-0064; без детектора — не механизм, D-0049).
- **ГЕЙТ ФОРМЫ** — детерминированная проверка СТРУКТУРНОЙ формы (не
  смысла), встроенная в канонический прогон/флаг проверки
  (`--check-form`/`--check`, `python -m pytest tools/ gateway/ -q`);
  держит РАЗРЕШИМОСТЬ и синтаксис, смысл — за AI-слоем/чеком (D-0063).

## 1. Маршрутизация (CLAUDE.md, ядро R1–R13)

| Политика | Сторож и режим | Как реализовано |
|---|---|---|
| R11/D-0054/D-0073: диспатч builder'а без DoD не уходит; пишущий — без манифеста given/owns не уходит; лейбл начинается с модели | БЛОК-ХУК | tools/dispatch_gate.py, PreToolUse на Task\|Agent: маркеры на ГРАНИЦАХ слова с 07-28 (\bDoD\b/\bwitness\b в DOD_MARKERS_RE, \bдано\b\|\bgiven\b, OWNS_WORD_RE=\bowns\b — класс ложных подстрочных маркеров вида «dod_gate.py в корзине»/«продано» закрыт t-335/t-336) / LABEL_MODEL_PREFIX_RE; exit 2 + причина в stderr — вызов субагента не состоится |
| R2/D-0052: builder-приёмка только с witness (дословный вывод прогона) | ГЕЙТ КОММИТА + WARN-ХУК | tools/journal_validator.py (pre-commit) валит accepted builder-работы без поля witness; tools/journal_echo.py WITNESS-слой предупреждает в момент записи строки |
| R3: критик на диффах >100 строк / схема / деньги; «critic: skipped» — только у принимающего строго выше | ЧЕК 2 + факт-фиксация + матрица | чек 2 сверяет коммиты окна с журналом критика; tools/critic_snapshot.py (PreToolUse) пишет tree_hash при каждом критик-вызове — правки ПОСЛЕ последнего снимка = «финал не ревьюился», неубираемый факт; концессию skipped валидатор пропускает только по матрице D-0058 |
| R4/D-0060: параллельные owns не пересекаются | WARN-ХУК + CLI приёмки | tools/owns_gate.py (PreToolUse, активация 07-28): sidecar logs/owns_registry.jsonl живых owns, пересечение → OWNS OVERLAP в контекст; tools/owns_verify.py — post-batch сверка фактических путей против декларации (exit 1 на выходе за owns). С 2026-08-19/20 у разбора декларации ТРИ уточнения режима: (а) декларация признаётся, только когда САМ МАРКЕР в ПРОЗЕ — не в фенсе, не в цитате, не в инлайн-коде (полярность СОЗНАТЕЛЬНО строже, чем у dispatch_gate); (б) третий исход `QUOTED_OWNS_WARN` — маркер найден, но ВСЕ вхождения цитированы: в сайдкар НЕ пишет; (в) заголовок и буллет распознаются наравне с жирным маркером. Окно живости owns-реестра — отдельной строкой ниже (Р-О3) |
| Р-О3 (решение оператора 2026-08-20): окно живости owns-реестра — 8 часов вместо 24; компромисс назван — фоновый воркер, запущенный раньше окна, ловиться перестанет, лечение (сигнал завершения воркера) — в очереди | WARN-ХУК + ЛИТЕРАЛЬНЫЙ ПИН В КАНОНЕ | `tools/owns_gate.py`: `WINDOW_SECONDS = 8 * 60 * 60`, граница включительна; одну константу делят проверка живости и компакция сайдкара; детектор решения — `tools/test_owns_gate.py::test_window_seconds_literal_pin_8_hours_ро3`, ЛИТЕРАЛЬНЫЙ ассерт в каноне (до фикса оба граничных теста ссылались символически, возврат на 24 ч прошёл бы с зелёным каноном — класс R10(в)); граничные тесты остаются символическими сознательно |
| F-61: пишущие хуки трека (dod_track/critic_snapshot/dod_gate/main_gate) не оставляют боевой файл в половинчатом состоянии при отказе записи на полпути; dod_track сверх этого не роняет диспатч на непредвиденном исключении | ДИСЦИПЛИНА+ДЕТЕКТОР | атомарная запись (mkstemp в той же папке + os.replace, суффикс .tmp последний/не .json — не подхватывается session_context.py glob("*.json")) во всех четырёх; dod_track main() тотальный try → одна строка stderr + rc=0 безусловен; critic_snapshot читает prev_ts/prev_tree_hash ДО рискованной записи (не после, как раньше); guard не-dict payload/tool_input в dod_track и critic_snapshot; dod_gate/main_gate несут только атомарную запись, без total-try (enforcement выше косметики отказа); детектор — tools/test_f61_halfstate.py (F61_TARGET=live/сиблинг), дискриминирующий мок реально усекает файл и лишь потом бросает |
| R5/D-0037: воркеры не плодят воркеров | КОНФИГ | у ролей scout/builder/critic в frontmatter tools нет Task/Agent (E4, 2105d4f) — харнесс не предоставит тул; правило в тексте ролей — дублирующий слой |
| R6/D-0045: после 2 reject — эскалация, не тихий ретрай | WARN-ХУК + ЧЕК 3 | tools/journal_echo.py ESCALATION-слой (E1) (литерал в контексте — «R6-ЗЕРКАЛО: line N attempt A без escalated»): delegated с attempt≥3 без escalated по той же задаче → сигнал в момент записи; чек 3 метёт окно |
| R7/D-0083: заявленный ярус воркера = фактическому | WARN-ХУК + ЧЕК 5 | journal_echo при записи строки с worker_ref agent:<id> меряет модели транскрипта воркера, MISMATCH → предупреждение; чек 5 сверяет Lead-сессии с окнами деградации |
| R1/R8: разведка — scout, skip только с событием; мелочи — батчем | ДИСЦИПЛИНА+ДЕТЕКТОР | чек 1 (самоизъятие), чек 22 (повторы skip-причин, baseline 16 — детектор батчинга D-0081); форма события держится validator'ом (notes обязателен) |
| R9/D-0043: чини класс — оси по карте, молчаливый пропуск сиблинга = нарушение | ГЕЙТ КОММИТА + AI-СЛОЙ | tools/mechanism_gate.py (commit-msg) требует осевой блок ПО ПЕРЕЧИСЛЕНИЮ на механизм-коммитах — встреча с вопросом гарантирована; полноту ПО СМЫСЛУ судит критик (правило 5: непокрытые оси = ДОРАБОТАТЬ) и чек 9 |
| F-56 закрыта (2026-08-10): невод механизмов покрывает ВСЮ живую проводку — 12 hook-команд settings.json, три скрипта .githooks поимённо, плюс транзитивное импорт-замыкание (tier_echo, preflight_quota); правка ЛЮБОГО из 19 файлов проводки теперь требует осевого блока | ГЕЙТ КОММИТА + САМОЗАМЫКАЮЩИЙСЯ ТЕСТ | tools/mechanism_gate.py MECHANISM_PREFIXES поимённо; tools/test_mechanism_gate.py: каждая command-строка живого settings.json и каждый скрипт гит-хуков покрыты (отсутствие/битость файлов роняет тест громко) + AST-замыкание импортов живой проводки против невода — обе оси класса F-56, дрейф ловится каноном |
| Phase 5 W1 (2026-08-18, тот же класс F-56): docs/FINDINGS.md (шапочные правила реестра + D-0100 + долг ретирования) и docs/RULE_COVERAGE.md (обязанность «строка тем же коммитом», картина сторожей) — носители обязанностей будущих сессий, добавлены в невод; их правка без осевого блока / строки «оси: не-механизм» не коммитится | ГЕЙТ КОММИТА | tools/mechanism_gate.py MECHANISM_PREFIXES; соседи класса, оставленные ЯВНО: docs/POLICY_FULL.md — решается посадкой W2b (туда потекут обоснования норм), delegation.config.yaml — носитель привязки, вопрос к калибровке №8; архивы/OPERATIONAL_NOTES/RETRO_PATTERNS — evidence и справочники, обязанностей не несут |
| Phase 5 W3 (2026-08-19): не-бут нормативный корпус не растёт без владельца числа; единица надзора — БАЙТ/ЗАПИСЬ, рост вглубь записей ловится дельта-правилом без порогов | ДИСЦИПЛИНА+ДЕТЕКТОР + ГЕЙТ ФОРМЫ | машинный слой tools/corpus_growth.py (манифест 12 артефактов, пороги ×1.15 от замера посадки, вердикты OK/WARN(95%)/BREACH, блок ВЫБЫЛИ против прошлого замера — анти-обход диетой-удалением; сайдкар logs/corpus_growth.jsonl); форма — `python tools/corpus_growth.py --check` в каноне (tools/test_corpus_growth.py, 54 теста: непустой файл с нулём записей = ДЕФЕКТ «регекс протух», позитивный контроль «фикстура 3/3» каждым прогоном); AI-вердикт — чек 34 калибровки (раскладка/диеты/пороги; отсутствие записи сайдкара за окно = чек НЕ ВЫПОЛНЕН); протухание этой строки — чек 24/F-41 |
| Phase 5 W2a (2026-08-18): прогон калибровки читает только чеки, живые в окне; предикат шапки чека обязан быть НЕОБХОДИМЫМ условием непустоты чека | ГЕЙТ ФОРМЫ + ДИСЦИПЛИНА+ДЕТЕКТОР | форму держит `python tools/calibration_prepass.py --check-form` в каноническом прогоне (tools/test_calibration_prepass.py, 121 тест): поля/лимиты шапки `<!--CHK …-->`, совпадение id с номером в тексте, сплошность 0..N, покрытие всех строк диапазонами, резолвимость rules:/cand:, позиция вне незакрытой пары `**`; ОТКАЗ предиката (чек помечен ПУСТ, а материал был) ловит КОНТРОЛЬНЫЙ ПОЛНЫЙ ПРОЧИТ — каждый 4-й прогон И первый прогон после любой правки шапок (пре-пасс определяет по git log): строка «контрольный прочит: N ПУСТ-вердиктов, расхождений k» в notes calibrated, расхождение = defect_found/failure_class=spec + возврат предиката в always; отсутствие строки в notes = чек не выполнен. Чеки без кодового гейта-владельца (0, 33) несут best-effort rules:-токены — класс «чек без owning-гейта» в очереди W2/W3 |
| Решение Р-D (2026-08-20): порог гейта (а) пре-пасса — ХРАПОВИК, не константа: снижение/плато легальны молча, ПОДЪЁМ — только объявленный (слово оператора + замер, пробивший прежний порог, формула `ceil(measured×1.05/100)×100`, причина); прежняя запись не переписывается (подъём ≠ аннулирование) | ГЕЙТ ФОРМЫ + ВСПЛЫТИЕ + ГЕЙТ КОММИТА + чек 34 | `tools/calibration_prepass.py`: история — `GATE_A_HISTORY` (подъём — блок `raised` с from/word/measured_breach/reason); сторож — `check_gate_a_history_monotonic()` в `run_check_form()`, `--check-form` печатает «ХРАПОВИК ГЕЙТА (а): …» и exit 1; ИТОГ печатает «ПОДЪЁМ №N словом оператора …» каждым прогоном; коммит-гейт — файл в `MECHANISM_PREFIXES`. ИСТОРИЯ: 57500 (аннулирован), 146000 (база 139042), 154000 (подъём №1, база 146664). Сиблинг без сторожа — `CLAUDE_WARN`/`CLAUDE_BREACH`, ось 14 SIBLING_MAP |
| R9/D-0100 (2026-08-09): подметание внутри артефакта — названный класс немедленно перечисляет соседей своего структурного блока/файла с вердиктом на каждого; находка с секцией «Класс.» несёт «Вопрос применимости.» | ДИСЦИПЛИНА+ДЕТЕКТОР | детектор — чек 9(б) WEEKLY_CALIBRATION_PROTOCOL.md + детектор формальности (вердикт «не применим» без указания отличия = отписка); машинный слой формы находок docs/FINDINGS.md — в очереди (Д2); реестр режимов отказа в карте симметрий + вход критика — в очереди (Д3, с прогоном CRITIC_EXAM) |
| R10/D-0055/D-0064/D-0072: механизм без 4 вопросов/tier не коммитится | ГЕЙТ КОММИТА + ЧЕК 8 | mechanism_gate: правка механизм-путей без блока «ось N: …» и «tier: <model>» → коммит отвергнут; «оси: не-механизм (<и почему>)» — единственный обход, и он виден; смысл ответов аудирует чек 8 |
| R13/D-0094: судья принимает ТОЛЬКО лист-класс; отклонение от D-пути — с записанной причиной | ГЕЙТ КОММИТА + ЧЕК 30 | journal_validator правило 11 (basis="judge" реджектит нелистовые/неизвестный by); чек 30 — оконный детектор отклонений, подпункт (д); дословность промпта — 30(а), judge_prompt_pin. Развёртка (провенанс F5, полная формула критерия «записанной причины») — тело: docs/rule_coverage/RC-01.md#rc1-r13-judge |
| R11 (5 пунктов диспетчера): вопрос/DoD+края/given/owns/свежесть | БЛОК-ХУК (форма) + WARN-ХУК (п.5) + ДИСЦИПЛИНА+ДЕТЕКТОР (края, п.2) + ЧЕК 23 (смысл) | форму держит dispatch_gate (см. выше); п.5 «свежесть» с 07-29 частично машинный: given-path warn-слой dispatch_gate (t-343, промоция пункта 5 пятипунктового чеклиста D-0096 чеком 23 по рецидиву t-338) сверяет существование НАЗВАННЫХ в диспатче путей под cwd — только additionalContext, блок-вердикты не трогает. С 2026-08-20 (узел D + вердикт t-554) у слоя ДВА СУЖЕНИЯ МОЛЧАНИЯ и одно расширение распознавания: (1) относительный путь освобождается маркером ВНЕШНЕГО деплоя только ПО ЛОКАЛЬНОСТИ (`_tokens_outside_deploy_paragraph()`) — КАЖДОЕ его вхождение в том же АБЗАЦЕ, что маркер, иначе путь проверяется как свой (прежняя глобальная форма гасила 21% потока, вкл. пути СВОЕГО корня); (2) путь, все вхождения которого в owns-СЕКЦИИ, не проверяется — диспатч его СОЗДАЁТ (временной край R11(c)); (3) секция манифеста распознаётся markdown-ЗАГОЛОВКОМ и БУЛЛЕТОМ через `_MANIFEST_SECTION_HEADER_RE` (14 и 15 из 237 промптов), owns-секция получила ВЕРХНЮЮ ГРАНИЦУ — прозаическая строка со словом owns больше не гасит проверку до конца промпта. П.2 с 07-29 требует НАЗВАННЫХ КРАЁВ ПОВЕДЕНИЯ (лимиты/пустые входы/конфликты требований — чек 23, абзац «шаг ОБЯЗАТЕЛЕН и АТРИБУТИВЕН» (якорь ПО ТЕКСТУ: подпунктов у чека 23 нет вовсе — прежний пин «класс (б)» протух, снят узлом E 2026-08-20), кейсы t-324/325/332/343; предложение оператора): слой дисциплинный, детектор — атрибутивный счёт того же абзаца чеком 23 + возвраты-вопросами исполнителей; маркер-warn — кандидат промоции по лику; содержательные промахи вскрываются реджектами/находками и с 07-28 ОБЯЗАТЕЛЬНО атрибутируются чеком 23 (spec-дефект диспетчера ≥2 → пункт чеклиста D-0096 тем же прогоном) |
| R8/D-0081: skip-причина класса-льготы (норма даёт послабление ОДИН раз — эталон R8(c) «оператор ждёт / интерактивный запрос блокирует ход») бесплатна только на ПЕРВЫЙ такой ход сессии; со второго — ДВЕ ВЕТВИ: НЕ блокирующая правка → batching (D-0081, R8.d) обязателен, самоисполнение = нарушение; БЛОКИРУЮЩАЯ правка → немедленный СОЛО-диспатч builder (R8.e), запись в батч НЕЗАКОННА (2026-08-09; сужено до мотива 2026-08-20) | ДИСЦИПЛИНА+ДЕТЕКТОР | чек 22, абзац «РЕЦИДИВ ВНУТРИ СЕССИИ»: рецидивом считается повтор КЛАССА-ЛЬГОТЫ, а не стоячей концессии (R1-самочтение, designer-самодрафт — их повторы называются числом и кормят порог >=3 и чеки 1/1(б), нарушением сами по себе не являются); арбитр — скан транскрипта сессии (границы сессии); дешёвый первичный проход — самодекларация маркером повтора («повтор класса») в notes второго-и-далее skip-события класса-льготы; отсутствие маркера у такого события = нарушение само по себе |
| R4/R11 п.4 чеклиста: параллельный узел сужает витнесс-прогон ПО OWNS (тестовые наборы ВСЕХ владеемых путей, не только своих файлов); ПОЛНЫЙ канон `python -m pytest tools/ gateway/ -q` — обязанность КООРДИНАТОРА после схождения ветвей, несётся в witness ПОСЛЕДНЕГО accepted батча с пометкой «BATCH CANON» (2026-08-09) | ДИСЦИПЛИНА+ДЕТЕКТОР | чек 13(к) «ОБЛАСТЬ ВИТНЕСС-ПРОГОНА ПАРАЛЛЕЛЬНОГО УЗЛА»: два признака в данных — (1) witness ДВУХ И БОЛЕЕ одновременных builder-приёмок несёт вывод ПОЛНОГО канона (обязан быть один, координаторский, помеченный); (2) witness параллельного узла не покрывает тестовые наборы всех путей своего owns |
| R7/D-0083 расширение: заявленный АГЕНТ (роль) диспатча = фактическому `subagent_type` транскрипта воркера, не только модель (2026-08-09, t-348) | WARN-ХУК + ЧЕК 4 | `tools/dispatch_gate.py` ЧАСТЬ B, `role_type_warn`: роль по `.claude/agents/*.md` против ярусa лейбла → WARN «ROLE-TYPE WARN»; ручная сверка чека 4 остаётся основой. ФЛАГ: у хука НЕТ зарегистрированного детектора отказа/живости — группа «НЕ ПОКРЫТЫ» чека 26. Развёртка (резолюция роли, классификация чека 26) — тело: docs/rule_coverage/RC-01.md#rc1-r7-role |
| R2: пересдача (`delegated` с `attempt`>=2 под ТЕМ ЖЕ `task_id` после `rejected`) — продолжение уже написанной спеки; designer-диспатч и `dispatch_skipped(agent=designer)` НЕ требуются, порог ≥3 пунктов/файлов на неё не проецируется (2026-08-09, слово оператора) | ДИСЦИПЛИНА+ДЕТЕКТОР | чек 1(в) «ИСКЛЮЧЕНИЕ "ПЕРЕСДАЧА = ПРОДОЛЖЕНИЕ СПЕКИ"»: детектор УТЕЧКИ исключения — переоформление продолжения под НОВЫМ task_id (тот же предмет/owns, соседние ts) без designer-диспатча/skip; детектор ОБРАТНОЙ утечки — skip-событие класса «пересдача» датой после 2026-08-09 (норма уже освобождает пересдачу без событий); пустое окно — «пересдач нет» |
| Phase 5 W2b-8 (2026-08-19): форма правил R1/R2/R4/R8/R11/R13 + персональный порог CLAUDE.md (`WARN=37500`/`BREACH=37800`, носитель `tools/session_context.py`) | ГЕЙТ ФОРМЫ + ВСПЛЫТИЕ | tools/test_norm_form.py в каноне: ключи tools/norm_keys.json в CLAUDE.md/POLICY_FULL; форма (ядро/строка/таблица/src/якорь) синтетикой; полнота converted∪exempted = R1..R13+R11a; session_context.boot_budget_lines() печатает `\| CLAUDE.md: <size>/<WARN>`, пробой строго `>` → OVER. Развёртка (история двух перевыводов порога, формула ceil, полнота исключений Р12(B)) — тело: docs/rule_coverage/RC-01.md#rc1-w2b8-form |
| Партия 2 (2026-08-19; класс SIBLING_MAP «сторож не отличает утверждение автора от цитируемого содержимого»): у трёх сторожей совпадение маркера засчитывается ПО РЕГИОНУ позиции, не голым regex; решение Lead — СТРОЖЕ ТОЛЬКО ЧЕРЕЗ WARN: в блокирующем гейте регион-фильтр двигает exit_code только в сторону СНЯТИЯ блока | WARN-ХУК ×2 + ГЕЙТ КОММИТА | Сканер `tools/md_regions.py`; потребители — dispatch_gate (WRITE-QUOTED WARN + DoD-цитаты), owns_gate (`QUOTED_OWNS_WARN`), mechanism_gate (`SKIP_RE`/`TIER_LINE_RE` только в прозе). И-0: любой отказ сканера — до-регионное поведение байт-в-байт. Ограничение (вердикт t-554 C-4, очередь Lead): сиблингов `*_md.py` физически нет, зелень доказывает поведение живого гейта, не дискриминацию |

## 2. Журнал маршрутизации

| Политика | Сторож и режим | Как реализовано |
|---|---|---|
| D-0053/D-0058: типизированные поля, матрица «приёмка только сверху», by-энум, пары-легальности | ГЕЙТ КОММИТА + WARN-ХУК | tools/journal_validator.py на pre-commit (валит коммит) И journal_echo VALIDATION в момент записи (PostToolUse Edit/Write — предупреждение сразу, до коммита); та же матрица в двух точках |
| D-0099: привязка функция→модель — параметр, а не константа кода; матрица приёмки binding-aware | ГЕЙТ КОММИТА + WARN-ХУК + ВСПЛЫТИЕ | носитель привязки — delegation.config.yaml (roles.*) в корне; читают трое: journal_validator (ветка «by == семейство привязки принимает agent-ярусы <= себя», ниже безусловного floor), mechanism_gate (tier-декларация ранга >= lead по лестнице конфига; строго выше — тоже легальна), session_context (метка Lead(bound)/top-reserve на SessionStart). Файла нет → все трое дефолтятся в fable (fail-closed, поведение до D-0099). Режим срабатывания сторожей не менялся — сменился ИСТОЧНИК их порога; строка заведена решением калибровки №6 (вопрос был явно отложен осевым блоком D-0099 до неё). Измерительные отчёты (tools/token_usage_stats.py) строк здесь НЕ получают: сторожами правил они не являются — сознательное решение той же калибровки |
| Append-only, прошлое не переписывается (D-0089), closes:-токены | ГЕЙТ КОММИТА + ВСПЛЫТИЕ | validator ловит вставки в прошлое/повторный delegated на закрытую (D-0060); SessionStart (tools/session_context.py) печатает OPEN DISPATCH по closes:-токенам — забытая задача видима каждой сессии |
| F-29: ts из системных часов, не из нарратива | WARN-ХУК | journal_echo TS-DRIFT: дрейф ts новой строки против системного времени → предупреждение (warn_stale) |
| D-0053 (продолжение): бюджет длины notes — 800 символов диспатчевому циклу, 15000 событию calibrated (его notes — объявленный владелец разбора прогона) | WARN-ХУК + ЧЕК 11(б) | journal_echo NOTES LEN (t-447, 2026-08-16): в момент записи строки сверяет длину notes с порогом ТИПА события, предупреждение с адресом «факт — в типизированное поле, разбор — к своему носителю»; никогда не блок; в фолбэк-базе хука слой молчит, но печатается маркер фолбэка, поэтому молчание отличимо от смерти. Детектор отказа самого слоя — чек 11(б) калибровки: окно с приростом журнала обязано дать либо срабатывание в транскриптах, либо явную строку «бюджет не превышался» с числом |
| Каденция D-0079: журнал не позже входа в долгое ожидание | ДИСЦИПЛИНА+ДЕТЕКТОР | детектор — чек 13 (полнота типизированных полей и хронология окна); гонка каденции задокументирована как причина sidecar-архитектуры owns_gate |
| Break-glass гейтов не выглядит успехом (t-325) | ВСПЛЫТИЕ | main_gate/dod_gate пишут unsafe_completions СПИСКОМ в трек; session_context всплывает неподтверждённые факты (ack только реально напечатанных, sidecar break_glass_ack.json, cap 5 + and-N-more) |

## 3. Сессионный цикл

| Политика | Сторож и режим | Как реализовано |
|---|---|---|
| Завершение main-хода с правками без СВЕЖЕГО зелёного прогона запрещено | БЛОК-ХУК | tools/main_gate.py (Stop): main-правки в dod_track без зелёного прогона ПОСЛЕ последней → exit 2, ход не завершится; предохранитель 2 блока подряд, третий — пропуск с unsafe-фактом (см. выше) |
| Субагент не сдаёт работу без прогона | БЛОК-ХУК | tools/dod_gate.py (SubagentStop) — та же логика по agent_id-срезу трека tools/dod_track.py (PostToolUse пишет edits/runs с разделением main/subagent) |
| Handoff: всё закоммичено/запушено, журнал закрыт (D-0050) | СКИЛЛ + ВСПЛЫТИЕ + ЧЕК 31 | skills/session-handoff вечером; Boot Report правило 6: DIRTY/UNPUSHED при старте = находка о пропущенном handoff; чек 31 — durable-персистентность принятых диффов за неделю |
| Boot: авто-впрыск директивы на свежем старте (D-0103), отчёт до работы, STOP до подтверждения | ВСПЛЫТИЕ + ДИСЦИПЛИНА + ДЕТЕКТОР | session_context.autoboot_lines впрыскивает AUTO-BOOT-директиву при source=startup/clear (не compact/resume; unknown→fire, fail-toward-boot); BOOT_REPORT_PROTOCOL правила 1–4 (STOP до авторизации, прецедент 2026-07-15); детекторы — autoboot-юниттесты (test_session_context_autoboot.py) + boot_path_files пин-тест + чек 13(ж) (маркер AUTO-BOOT на fresh-транскрипте); расхождение Current Task ↔ коммиты (D-0025) |
| Boot-бюджет ≤100КБ | ВСПЛЫТИЕ + СКИЛЛ | session_context меряет и печатает BOOT BUDGET (WARN на пробое); реакция — скилл boot-diet по слову оператора; чеки 10/11 |
| F-62 (батч П4, 2026-08-19): ни один хук не висит вечно на неинтерактивном stdin без EOF — `isatty()`-guard ловит только TTY и маскирует неинтерактивный случай (SessionStart раньше зависал на КАЖДОЙ сессии) | ДИСЦИПЛИНА+ДЕТЕКТОР (батарея в каноне) | Локальная копия `_read_stdin_bytes_deadline` (поток + `join(timeout)`, дефолт 10.0 с, env `OSLLM_STDIN_TIMEOUT` до 600 с, «0 = ждать вечно» НЕТ) в 14 носителях (13 хуков батча + `critic_verdict_check`). ПРИНЯТАЯ ДЫРА (В1, Lead 08-19, владелец текста F-62): таймаут FAIL-OPEN, проверка пропускается, дыра пред-существует; замер доставки — очередь В2. Детектор — `tools/test_p4_stdin_deadline.py`: K2/K5/K7 — sha256-пин региона + subprocess-пробы незакрытого пайпа. Кит-твины дыру несут — порт-очередь В11 |

## 4. Командная гигиена

| Политика | Сторож и режим | Как реализовано |
|---|---|---|
| Канонические формы, без cd-префикса, без 2>&1 | ЧАСТИЧНЫЙ БЛОК-ХУК + ЧЕК 25 | tools/hygiene_gate.py, константа `V5_ENABLED`: DENY на cd/Set-Location первым statement'ом (целью репо), на ` 2>&1` вне кавычек, на журнал мимо Edit/Write (кроме read-only головы). ФЛАГ: `PYC_DENY_ENABLED = False` (python -c/heredoc пока WARN); обход cd не первым statement'ом — принятая дыра в очереди. Развёртка (история включения V5, проба живости, полный разбор классов) — тело: docs/rule_coverage/RC-04.md#rc4-hygiene-v5 |
| п.6: негатив только с одноформенным позитивным контролем | WARN-ХУК + AI-СЛОЙ + ключи | tools/negative_lint.py (PostToolUse Task\|Agent): негатив-маркер без контроль-маркера в окне ±3 строк отчёта воркера → WARN (skip async-метаданных с 07-28); приёмка D-0046 обязана спот-чекать негативы; интент-ключи разведки несут форм-контроль (D-0094) |
| п.6 (машинные слои t-338, порт Dog-синка 07-29; ТРИ дополняющих среза одного правила с negative_lint — субагентские отчёты / прямые поиски / письменные утверждения): пустой поиск — сигнал в момент появления; негатив в политический файл — только после изучения субъекта в сессии | WARN-ХУК ×2 + леджер | tools/search_control_gate.py (PostToolUse Bash\|PowerShell\|Grep\|Glob\|Read): предупреждение на пустой поиск + посессионный леджер поисков И чтений logs/.search-ledger/ (gitignored, компакция >500); tools/claim_control_gate.py (PreToolUse Edit\|Write): негатив-утверждение о машинно-различимом токене (путь/dotted/UPPER_SNAKE, границы слова) без поиска/чтения субъекта по леджеру → WARN; warn-first обкатка (класс negative_lint/owns_gate), находки нумеруются в CURRENT_CONTEXT; отказ гейтов — чек 26, свежесть строки — чек 24 |
| Пермские концессии в allowlist привязаны к решению | ГЕЙТ КОММИТА | tools/escape_check.py (pre-commit): каждая escape-строка hash-pinned к авторизующему D-решению |
| п.6 подкласс «статус записи реестра» (F-55, решение Lead 2026-08-09: подкласс включён): статус структурированной записи — только по статусной строке/полю, окно чтения не дошедшее до неё сверкой не является | ДИСЦИПЛИНА+ДЕТЕКТОР | чек 25(а) (общий перечень находок-нарушений гигиены) + чек 25(г) «СВЯЗКА "УСЕЧЕНИЕ ОКНА РЕЕСТРА → СТАТУСНОЕ УТВЕРЖДЕНИЕ"» (адресный скан транскриптов: усечённое чтение реестра + последующее статусное утверждение без повторного чтения статус-поля); детектор дополняет, не заменяет инцидентный слой (вопрос оператора) |
| F-30 подкласс «относительное число без названной базы» (2026-08-09, ревью резервного яруса первой Opus-Lead сессии, п.(14в) остатков калибровки №6): относительная величина в отчёте оператору обязана называть базу сравнения | ДИСЦИПЛИНА+ДЕТЕКТОР | чек 25 категории (а) — общий перечень находок-нарушений гигиены аудита permission_audit |
| Разбивка находок permission-аудита по классам гигиены (инструмент замера, не сторож) | ЧЕК 25 | tools/permission_audit.py `--summary`, блок «По классам гигиены»: счёт по четырём классам (`2>&1`, `cd/Set-Location`, `python -c/heredoc`, «журнал мимо Edit/Write») по всем просканированным вызовам окна; baseline — числа окна калибровки №6 |
| F-58 (2026-08-19): CLAUDE.md п.7 ветка (д/е) — отбрасывание СВОЕЙ незакоммиченной работы в пути, входящем в собственный owns (правка той же сессии), легально без пустого porcelain — свидетель: декларация владения + дифф ДО отбрасывания + пустой дифф после отката; вне условия (owns не объявлен / путь вне owns / правка не этой сессии — старше, или из ДРУГОЙ сессии) действует (б) без изменений — porcelain не показывает АВТОРА, и ЗЕРКАЛЬНО: непустой статус вне owns исполнителя = легитимный параллельный писатель (сшивка R4(d): never touch another session's uncommitted paths), откат запрещён | ДИСЦИПЛИНА+ДЕТЕКТОР | CLAUDE.md п.7 регистр (д/е) + POLICY_FULL пара (same-commit); твины critic.md «БЕЗОПАСНОСТЬ СОБСТВЕННОЙ ПРОБЫ» (д/е), builder.md п.10; детектор — чек 25(в) калибровки, ТРИ ветки сверки (байт-копия / пустой porcelain, замеренный НЕПОСРЕДСТВЕННО перед откатом / путь в owns исполнителя), оба witness-артефакта обязательны |

## 5. Роли (frontmatter + правила role-файлов)

| Политика | Сторож и режим | Как реализовано |
|---|---|---|
| scout read-only, без Bash | КОНФИГ + ЭКЗАМЕН | tools: Read, Glob, Grep (сужение 07-28, P0 ревью); каждая правка scout.md → прогон PROCESS/SCOUT_GOLDEN_SET.md ДО коммита (Q3 негатив и Q4 отказ от суждения обязательные; contamination-чек; Runs log тем же коммитом) |
| builder: не изобретать требования, witness, финал целиком (F-49) | БЛОК-ХУКИ входа/выхода + ЧЕК 29 | dispatch_gate не пустит спеку без DoD (возврат вопросами не понадобится); dod_gate не выпустит без прогона; validator не примет без witness; полноту финалов метёт чек 29; экзамен-набора НЕТ — записанное решение (execution-based приёмка сильнее) |
| critic: вердикт со следом, fenced json-блок, независимое воспроизведение | FAIL-CLOSED проверка + ЭКЗАМЕН + ЧЕК 13 | вердикт без валидного json-блока не читается принятым — tools/critic_verdict_check.py прогоняется Lead'ом ДО приёмки; PROCESS/CRITIC_EXAM.md (D-0071, подсаженный дифф, ловушки: сфабрикованный witness, непроверенный негатив); false-accept rate — чек 13 |
| судья: калиброванная привязка, дословный промпт | ЭКЗАМЕН + ЧЕК 30(а) | сет D-0031 (13/13 обе формы, t-254); pinned JUDGE_SYSTEM_PROMPT — дрейф субагентного промпта ловит judge_prompt_pin/чек 30(а); хallюцинация судьи — кандидат отдельного детектора (очередь №5) |
| env-констрейнты Windows у пишущего яруса | КОНТЕКСТ РОЛИ | builder.md п.9 (07-28): PYTHONUTF8, пути, MAX_PATH, локали — в системном промпте каждого builder-диспатча |
| designer: драфт в форме R11, развилки возвращаются, в дерево не пишет | КОНФИГ + AI-СЛОЙ + ЧЕК 13(г) | .claude/agents/designer.md (промоция №5, 07-29): tools без Task/Agent/Edit/Write — плоскость и read-only машинным слоем; приёмка Lead каждого драфта; spec-дефекты принятых драфтов — failure_class=spec на дизайнере (чек 13(г)) |

## 6. Ярусы и деградация

| Политика | Сторож и режим | Как реализовано |
|---|---|---|
| D-0056: ярус сверяется на входе и выходе; окно деградации журналируется | ВСПЛЫТИЕ + ЧЕК 5 | SessionStart печатает MODEL-строку (или требование самопроверки D-0056a); чек 5 — внешняя сеть: фактические модели Lead-сессий из транскриптов против покрытия окон парами lead_degraded/restored |
| Приёмка только сверху (D-0058), включая незнакомый by | ГЕЙТ КОММИТА | journal_validator: матрица пар (6 веток B7, t-322) + отказ по by вне {haiku,sonnet,opus,fable} (t-323, порт семантики AO3) |
| Lead-экзамен v2 (PROCESS/LEAD_RANKING_EXAM.md, секция v2): триггеры — (1) перепривязка `roles.lead`/`roles.reserve`; (2) содержательная правка Lead-политики ядра; (3) правка носителя набора/ключей; (4) рост `failure_class=spec` на Lead-ярусе | ДИСЦИПЛИНА+ДЕТЕКТОР+ЭКЗАМЕН | триггеры 1/3/4 безусловны; триггер 2 сужен 2026-08-20 (F6 №8) — формосохраняющая правка закрывается строкой «форма: …» вместо переэкзамена; детектор — чек 14(ж). Развёртка (полный критерий ФОРМЫ/reflow-only, доказательство, аудит честности) — тело: docs/rule_coverage/RC-06.md#rc6-lead-exam-v2 |
| d15 (решение Lead 2026-08-20, t-543; класс SIBLING_MAP «имя модели прибито как КРИТЕРИЙ вместо резолюции привязки»): колонка «Model here» таблицы ярусов CLAUDE.md обязана сходиться с `roles.*` живого `delegation.config.yaml`, перепривязка видна КАНОНОМ (цена замерена: прошлая перепривязка прожила в протоколе 12 дней, нашёл её встречный деплой, не свой чек) | ГЕЙТ ФОРМЫ (канон) | `tools/test_claude_binding_guard.py`: `parse_tiers_table()` собирает строки ПО ФОРМЕ; `family_of()` резолвит семейство подстрокой регистронезависимо, сверка двоичная; МОЛЧАЛИВЫЙ НОЛЬ ЗАПРЕЩЁН — `require_nonempty` падает, если таблица не найдена/распознано 0 строк; литеральный em-dash — ЕДИНСТВЕННОЕ легальное пропускающее значение, дефис/пустая ячейка ПАДАЮТ |

## 7. Кит и парность (оси 4/7)

| Политика | Сторож и режим | Как реализовано |
|---|---|---|
| D-0074: staging только батчем по слову; дрейф видим | CLI + ЧЕК 12 | tools/parity_check.py + parity_manifest.json (50 пар, hash-базлайн): clean / hq-drift (легален, порт-очередь) / kit-drift (подозрителен) / both-drift; релизный батч обновляет базлайн --sync |
| D-0092/D-0093: обещанное поставкой имеет код; хуки исполнимы в индексе | ВСПЛЫТИЕ + CLI | SessionStart wiring-строка (хосты: ledger adopt-rows ↔ фактическая проводка; с t-338 включает skills-casing канал — регистр SKILL.md по git-индексу); кит: toolkit/tools/wiring_check.py (t-326, git-INDEX моды 100755 — F-53); чек 12 |
| Починка файла enforcement-цепи сверяется с её НАБЛЮДАТЕЛЕМ, не только с вылеченным симптомом (правило Dog-синка 07-29, их инцидент 07-25) | ГЕЙТ КОММИТА | tools/enforcement_probe.py в .githooks/pre-commit (t-338): staged ∩ цепь (динамический разбор hook-команд settings.json + .githooks, рекурсивно, --no-renames/quotePath=false) → запуск tools/wiring_check.py (fail-closed CLI над каналами session_context + skills-casing; AUTOFIX-факты не считаются warning'ами), не-OK/сбой наблюдателя → коммит отвергнут; выход из тупика — починка в РАБОЧЕМ дереве; задокументированный fail-open: сбой git при чтении staged-списка; отказ — чек 26 |
| Пары документов не расходятся (ядро↔POLICY_FULL, DECISIONS↔FULL, карта…) | ЧЕК 12 + ГЕЙТ | чек 12 (число записей, живость путей); mechanism_gate заставляет осевой блок, где ось 4 диктует вопрос о паре |

## Справочник упомянутых чеков (номера — из чек-листа PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md)

- чек 1 — правила 1/2/8: молчаливое самоизъятие main-сессий из
  делегирования; 1(б) — диспетчерский порог дизайнера (R2); 1(в) —
  исключение «пересдача = продолжение спеки» (2026-08-09) + детектор
  утечки/обратной утечки исключения.
- чек 2 — правило 3: критик на крупных диффах (коммиты окна против
  журнала критика).
- чек 3 — правило 6 / D-0045: два rejected на ярусе → эскалация.
- чек 4 — D-0042: самодекларация модели (поле model против
  фактических моделей cc_usage); расширена 2026-08-09 (D-0083,
  t-348) — поле `agent` журнальной строки против фактического
  `subagent_type` транскрипта воркера, детектор dispatch_gate ЧАСТЬ B
  дополняет, не заменяет.
- чек 5 — D-0039/D-0044/D-0056: деградация — окна и фактические
  модели транскриптов; граница деградации — привязка `roles.lead`,
  прежняя формулировка (по имени модели) —
  PROCESS/CALIBRATION_HISTORY.md.
- чек 6 — D-0037/D-0046/D-0058: каждый закрытый delegated несёт
  приёмку по матрице (четыре основания, вкл. клаузу независимого
  контекста D-0099); прежняя редакция (только основания i/ii) —
  PROCESS/CALIBRATION_HISTORY.md.
- чек 8 — правило 10: четыре вопроса на новых DECISIONS-записях.
- чек 9 — D-0043/SIBLING_MAP: классовая полнота закрывающих коммитов.
- чек 10 — F-8/D-0038: рост живых boot-файлов (счётчики строк).
- чек 11 — Rule #1 к самой машинерии (размер вывода хука и цена
  механики ретроспективно).
- чек 12 — D-0048: живость SIBLING_MAP и парных путей (в т.ч.
  parity-манифест, числа пар документов).
- чек 13 — D-0052/D-0053: доказательная приёмка и типизированные
  поля журнала (обе стороны; false-accept по ярусам); 13(к) —
  область витнесс-прогона параллельного узла (R4, 2026-08-09);
  известные разовые поправки счёта (13(д)/(е)) —
  PROCESS/CALIBRATION_HISTORY.md. Развёртки подпунктов 13(г)–13(л) —
  тело PROCESS/checks/CHK-13.md (Phase 5 W4-2, 2026-08-19); ядро и
  подпункты 13(а)/13(б)/13(в) остаются в протоколе целиком.
- чек 18 — Rule #1 итогом: экономический тренд (savings_report);
  18(д) — spend-отчёт (M4/M10/топ-стоков/run_units), прогон ТОЛЬКО
  оконными аргументами `--window-start <ts прошлого calibrated>
  --window-end <ISO момента прогона> --compare prev`: форма
  `--window last` ЗАПРЕЩЕНА как форма прогона калибровки — она
  резолвится в ПОСЛЕДНЕЕ ЗАКРЫТОЕ окно, а своё закрывается лишь
  записью `calibrated` в конце прогона, поэтому печатает числа
  ЧУЖОГО окна (назван №7, сработал повторно на №8, починен узлом B);
  `custom:`-окно и строка «н/д (окно пусто)» — НОРМА, не дефект; база
  контрфакта = привязка `roles.lead` с 2026-08-16 (t-444), известные разрывы —
  PROCESS/CALIBRATION_HISTORY.md. Baseline/контрфакт-история/
  процедурная часть 18(д) — тело PROCESS/checks/CHK-18.md (Phase 5
  W4-2, 2026-08-19).
- чек 21 — Update Rule 4 таблицы: стоимостной кроссовер эскалаций
  (полная стоимость попыток против оценки прямого диспатча).
- чек 22 — повторяющиеся причины dispatch_skipped; детектор
  батчинга D-0081 (baseline 16); рецидив КЛАССА-ЛЬГОТЫ ВНУТРИ ОДНОЙ
  сессии (R8 «первый ход класса бесплатно», 2026-08-09; сужен до
  мотива 2026-08-20 — стоячие концессии R1/R2(c) не в счёт) —
  нарушение само по себе независимо от порога окна.
- чек 23 — повторы находок критиков → кодификация; с 07-28
  ОБЯЗАТЕЛЬНАЯ атрибуция вердиктов (spec-дефект диспетчера | ошибка
  исполнителя | внешнее) с посадкой в чеклист D-0096.
- чек 24 — F-41: свежесть документов-владельцев (включая этот файл).
- чек 25 — permission/hygiene-аудит окна (волны запросов, промахи
  гигиены); 25(а) — общий перечень (вкл. F-30 подкласс
  «относительное число без базы» и F-55 подкласс «статус записи
  реестра»); 25(г) — адресная связка «усечение окна реестра →
  статусное утверждение» (2026-08-09).
- чек 26 — policy-as-code гейты штаба: (а) ПОСТАНОВКА — состав
  command-хуков `.claude/settings.json` сверяется ПО ФАКТУ ФАЙЛА. По
  критерию «обнаруживает ли проверка МОЛЧАНИЕ гейта, не только его
  ошибочное срабатывание» — ТРИ ГРУППЫ: ПОКРЫТЫ (молчание
  детектируется) — `session_context`, `critic_snapshot`, `dod_track`,
  `hygiene_gate`; ПОКРЫТЫ ЧАСТИЧНО — `dod_gate`/`main_gate`,
  `journal_echo`; НЕ ПОКРЫТЫ (детектора отказа/живости нет нигде в
  протоколе) — `dispatch_gate`, `owns_gate`, `claim_control_gate`,
  `search_control_gate`, `negative_lint`. Проба живости КОДА
  `hook_liveness_probe` (чек 26(д)) — ОТДЕЛЬНАЯ ось (живость кода, не
  ПРОВОДКИ/названности предмета проверки), снимок трёх групп НЕ
  заменяет. Свежесть снимка сторожит чек 24; ЕДИНСТВЕННЫЙ носитель
  снимка — этот справочник (с диеты W2c-2, 2026-08-19), в
  PROCESS/checks/CHK-26.md НЕ дублируется. Развёртка (критерий
  засчитывания рода греп-попадания, история уточнений 2026-08-09,
  полные развёртки (а)–(д)) — тело: docs/rule_coverage/RC-07.md#rc7-chk26
  (плюс PROCESS/checks/CHK-26.md, полный текст).
- чек 29 — F-49: полнота финал-сообщений воркеров; ЖИВОСТЬ
  ПРОВЕРЯЕТСЯ ПО ТЕКСТУ, не по номерам: четыре якоря — пункт,
  начинающийся словами «Финальное сообщение = ПОЛНЫЙ», в builder.md,
  scout.md, critic.md и подстрока «финальный отчёт ЦЕЛИКОМ» в
  BLOCK_MESSAGE tools/dod_gate.py; точечный lookup с позитивным
  контролем на каждый нулевой результат. Номера пунктов role-файлов
  НЕ пинуются сознательно — прежний пин «critic.md п.15» протух
  перенумерацией c3f8585 и снят узлом B 2026-08-20 (находка F9).
- чек 30 — leaf-routing D-0087/D-0094: отклонения от D-пути с
  причиной; 30(а) — дословность судейского промпта.
- чек 31 — F-50: durable-персистентность принятых деливераблов.
- чек 34 — Phase 5 W3, tools/corpus_growth.py: AI-вердикт калибровки
  (раскладка/диеты/пороги; отсутствие записи сайдкара
  logs/corpus_growth.jsonl за окно = чек НЕ ВЫПОЛНЕН); машинный слой
  — `python tools/corpus_growth.py` (замер, пишет сайдкар); `--check`
  — форма вывода в каноническом прогоне, сайдкар НЕ пишет (O2,
  a0e9c29).

## 8. Сторож этого документа

Сам файл сторожится: (а) чек 24 — RULE_COVERAGE в списке
документов-владельцев (владение: «какой код сторожит правило»),
протухшая строка = F-41-находка; (б) ось 4 SIBLING_MAP — пара
«носители правил + tools-гейты ↔ этот файл»: новый механизм / смена
режима срабатывания без строки здесь = дефект классовой полноты,
виден критику по карте и mechanism_gate'у по осевому блоку; (в)
МАШИННЫЙ СЛОЙ — `python tools/calibration_prepass.py --check-form`:
КАЖДОЕ «чек N» этого файла обязано резолвиться в номер протокола
(`validate_rules_backward`), а КАЖДЫЙ токен `rules:RC§N/token` шапок
чеков — находиться словом внутри названной секции ЭТОГО файла
(`validate_rules_forward`); плюс файл в `MECHANISM_PREFIXES`. Слой
держит РАЗРЕШИМОСТЬ ссылок, не их СМЫСЛ.
