# Sibling Map — карта осей симметрии (D-0043)

Инструмент правила «чини класс, а не экземпляр»: закрывая дефект, обход
аналогичных мест делается ПО ЭТОЙ КАРТЕ, а не сканом репозитория.
Карта намеренно мала и грузится только в момент закрытия дефекта (не
при boot — D-0038). Порядок работы:

1. Найди ось (оси), на которых лежит дефект — см. список ниже.
2. Проверь/почини парные места оси точечно; что не чинится сейчас —
   явно в очередь (CURRENT_CONTEXT.md) или журнал.
3. Класс шире карты или ось не находится → диспатчь scout (haiku) на
   точечный поиск с конкретным вопросом. Lead/critic НЕ перечитывают
   репозиторий руками (правило 1).
4. Появилась новая симметрия (новый деплой, новый парный документ,
   новая пара код/учёт) → добавь ось сюда В ТОМ ЖЕ коммите — это
   часть шага 3 D-0043.

critic проверяет классовую полноту фикса ПО КАРТЕ: «покрыты ли оси,
на которых лежит дефект» — ограниченный вопрос по диффу и карте, не
по всему коду.

## Ось 1 — Деплои routing MVP (правки политики/ролей/журнала — всегда парные)

| Это репо | D:\AO3_tests |
|---|---|
| CLAUDE.md (политика, правила 1–11, вокабуляр событий) | CLAUDE.md (то же + дисциплина команд конвейера) |
| .claude/agents/{scout,builder,critic,designer}.md (designer — стоячая с 2026-07-29) | те же три БЕЗ designer (признанное отличие: у AO3 роли нет) + 11 QA-агентов конвейера (c 2026-07-14 — exploratory-tester) |
| logs/routing-log.jsonl + tools/journal_validator.py + .githooks/pre-commit (enforce формата и матрицы D-0058, D-0069) + tools/calibration_counts.py (t-040 — читающий близнец той же схемы: гейт на записи ↔ счётчик чеков 3/13 на чтении; расхождение констант ловит test_schema_constants_match_journal_validator) | logs/routing-log.jsonl + scripts/log_append.py (enforce формата, by/basis и continuation/retry — порт 2026-07-10, AO3 коммит 7e6fa36; enforcement на записи, признанное отличие: reopen-семантика; счётного близнеца нет — журнал AO3 читает та же калибровка OS-скриптом) |
| .githooks/commit-msg + tools/mechanism_gate.py (enforce правила 10б, D-0055; + tier-декларация D-0072) | .githooks/commit-msg + scripts/mechanism_gate.py (карта читается из OS-репо; tier-парность портом 2026-07-12, их 0a0a8c6) |
| — | .claude/skills/qa-loop/SKILL.md (журналирование диспатчей) |
| .claude/skills/session-handoff (D-0050) | .claude/skills/session-handoff (порт t-021, коммит 0911cf6) |
| CURRENT_CONTEXT.md — носитель очереди OS (D-0082: кросс-деплойный пункт пишется в носитель ЦЕЛИ тем же ходом) | docs/HANDOFF.md «Открытые хвосты» — носитель очереди AO3 (та же обязанность их стороной) |
| tools/session_context.py, wiring-чек хуков (:880-970: hooksPath + индексные моды `git ls-files -s`, ловит и untracked) + tools/wiring_check.py (t-338: тонкий standalone CLI над КАНАЛАМИ session_context + новый skills-casing канал; fail-closed чекер — цель enforcement_probe в pre-commit; ТРЕТИЙ одноимённый модуль семейства) + toolkit/tools/wiring_check.py (кит-твин, ДРУГОЙ контракт: read-only универсальный аудитор хоста со своим --check, без session_context; casing-канал check_skills_casing портирован релизным батчем v0.6.0 2026-07-30) + toolkit/tools/enforcement_probe.py (кит-твин пробы, AST-equal порт, третий шаг кит-pre-commit — тот же релизный батч) | scripts/wiring_check.py, SessionStart-хук (os-port-0722; индексно-модовая проверка портирована 2026-07-28; признанное отличие: untracked-хуки НЕ warn'ит — их ловит is_file()-чек на самом клоне; casing-канал + режим --check + scripts/enforcement_probe.py в их pre-commit ПОРТИРОВАНЫ 2026-07-29, коммит AO3 d4ff7d4 — их наблюдатель без флага остаётся fail-open принтером, гейтит только --check) |

## Ось 2 — Контуры измерения (Rule #1, D-0032: учёт честен на обоих)

| API-контур (gateway) | Субскрипционный контур (Claude Code) |
|---|---|
| gateway/sqlite_logger.py -> requests | tools/usage_report.py -> cc_usage |
| учётные цены алиасов в gateway/config.yaml | PRICES_PER_TOKEN_USD в usage_report.py |
| traffic_kind (real/synthetic/replay/judge) | is_sidechain + agent_id/agent_type |
| evidence: Shadow Evaluation Log (docs/SHADOW_EVALUATION_LOG.md, D-0067) | evidence: routing-журнал + вердикты приёмки |
| gateway/metrics.py (дайджест) | tools/usage_report.py (отчёт) |
| детектор фабрикации F-14: tools/pi_run_guard.py (код, Pi/gateway-воркеры) | приёмка по следу + точечная сверка D-0046 (дисциплина, Claude Code-воркеры) |

Классические классы на этой оси: новая модель/цена (добавить в ОБА
учёта), новый вид трафика/атрибуции, правило «никогда молчаливый $0».
Подкласс F-38 (2026-07-13): одна МЕТРИКА — разные РАСКЛАДКИ ПОЛЕЙ
сторон: requests.prompt_tokens (litellm/anthropic) ВКЛЮЧАЕТ
cache_read/cache_creation; cc_usage.input_tokens ДИЗЪЮНКТЕН им.
Порт формулы через пару переводит семантику полей, не копирует
арифметику; сверка семантики — эмпирикой на живых строках, не
чтением кода (F-30 для critic'а тоже).
Подкласс ts-клоков (2026-07-22, находка t-261(ж), брат F-38 про
время): requests.ts — наивное ЛОКАЛЬНОЕ (sqlite_logger._base_row:
start_time.isoformat(), микросекунды) ↔ cc_usage.ts — UTC с 'Z' и
миллисекундами; на этой машине ~2ч разрыва. Любое сравнение/джойн
ts через пару обязан нормализовать формат И клок (эталон фикса:
_normalize_ts_to_local_naive в darm_orchestrator). Сиблинги
пройдены критиком t-261: savings_report и exam_runner чисты
(самосогласованные окна); gateway/metrics.py (substr(requests.ts)
vs date('now')-UTC, строки 194/203/313/373) — ЗАДИСПОЗИЦИОНИРОВАН
письменно: доброкачественно на day-granularity (расхождение лишь в
поздневечернем часовом поясе окна дня), фикс не требуется до
первого фактического расхождения в данных дайджеста.
Внутригейтвейный класс (t-018, critic F4): enforcement-события Guard
(budget_events, quota_events) <-> их нарратив в Ledger-дайджесте
metrics.py — новая таблица событий требует строки в дайджесте
(обе нарратятся с t-019; следующая enforcement-таблица обязана
получить строку дайджеста тем же коммитом).

## Ось 3 — Роли/ярусы (обязанность одной роли — проверь остальные)

scout / builder / critic / designer (штаб; designer промотирован
калибровкой №5 2026-07-29, у AO3/кита его НЕТ — порт по их
слову/релизом) + Lead-правила в CLAUDE.md + 10 QA-агентов AO3 (их
промпты — цикл изменений конвейера; правки туда ставятся в очередь,
если не по пути). Пример класса: «докладывай замеченные аналоги»
(D-0043), «не запускай субагентов» (D-0037).
ПРИЗНАННОЕ ОТЛИЧИЕ (2026-07-29, порт-батч d4ff7d4): E4-сужение tools
scout-роли (у нас Bash изъят по P0 07-28) на AO3 НЕ применено — их
scout.md п.6 сам предписывает device-проверки шеллом (Get-Device);
пересмотр — слово их Lead + прогон их golden set тем же коммитом.
builder-env: п.9 (мы) <-> п.10 (AO3, порт 07-29) — правка одной
стороны требует вопроса о другой.

## Ось 4 — Парные документы (инвариант: не расходиться)

- DECISIONS.md <-> CLAUDE.md обоих деплоев (решение принято — политика
  отражает; F-9/D-0042 были рассинхроном именно здесь).
- DECISIONS.md (индекс) <-> docs/DECISIONS_FULL.md (полные тексты,
  D-0051): число записей D-XXXX обязано совпадать (чек 12 калибровки);
  новое решение — обе стороны тем же коммитом.
- CLAUDE.md (ядро политики, EN, только нормы — бут-путь) <->
  docs/POLICY_FULL.md (обоснования/прецеденты/история, D-0084):
  правка нормы — в ядре, её rationale — сюда тем же коммитом; при
  расхождении выигрывает норма ядра; живость пары — чек 12.
- ROADMAP.md — единственный владелец фаз/гейтов; ARCHITECTURE.md и
  WHITE_PAPER.md ссылаются, не дублируют.
- ROADMAP.md (живые фазы/гейты, boot-путь) <->
  docs/task_reports/2026-07-15_roadmap-closed-phases.md (D-0078:
  закрытая фаза — полный нарратив закрытия в архив VERBATIM тем же
  коммитом, что закрыл фазу; в ROADMAP.md остаётся указатель-статус;
  живость пары — чек 12, дрейф — следующий boot-diet замер).
- BOOT.md <-> MEMORY_ARCHITECTURE.md (слои и список загрузки).
- docs/FINDINGS.md (наши наблюдения) <-> docs/RELATED_WORK.md (внешние
  приоры): факт заходит в один из двух, по происхождению.
- CURRENT_CONTEXT.md <-> docs/task_reports/ (D-0038: закрыто — в архив,
  указатель остаётся).
- PROCESS/SCOUT_GOLDEN_SET.md <-> .claude/agents/scout.md (D-0057:
  правка роли/модели яруса требует прогона набора тем же коммитом;
  ключи вопросов смотрят в живые файлы — verify-команды сверяются
  при каждом прогоне, живость — чек 14б). AO3-пара той же оси
  (порт 2026-07-10, AO3 коммит 72a16ad):
  D:\AO3_tests\docs\SCOUT_GOLDEN_SET.md <-> их
  .claude/agents/scout.md п.8.
- .claude/skills/next-task/SKILL.md <-> CLAUDE.md (D-0061: skill —
  порядок шагов, политика — правила; правило меняется только в
  CLAUDE.md, skill ссылается и не дублирует формулировок; при
  расхождении политика выигрывает).
- ARCHITECTURE.md «Two Vocabularies» <-> CLAUDE.md «Ярусы» обоих
  деплоев (D-0062: новая функция/грейд или смена привязки — мост и
  политика правятся вместе; грейд внутри правила маршрутизации —
  дефект словаря).
- ARCHITECTURE.md (полная спецификация, для людей) <->
  ARCHITECTURE_BOOT.md (boot-ядро, D-0067): правка секции полного
  файла требует вопроса «нужно ли ядру»; правка ядра — обратного;
  живость обоих путей — чек 12.
- DELEGATION_TABLE.md (статусы, boot-путь) <->
  docs/SHADOW_EVALUATION_LOG.md (лог прогонов, D-0067): новый прогон
  пишется в лог, смена статуса в таблице ссылается на строку лога
  (Update Rule 1).
- DELEGATION_TABLE.md (строки provisionally/production_validated) <->
  gateway/metrics.py VALIDATED_DELEGABLE_CATEGORIES (код-зеркало для
  R2 readiness, t-090): статусы двигает только калибровка, поэтому
  статусный ход обновляет frozenset ТЕМ ЖЕ коммитом; детектор —
  чек 12 (сверка множества с таблицей на каждом прогоне).
- PROCESS/LEAD_RANKING_EXAM.md (кейсы) <-> docs/FINDINGS.md
  F-28/F-30 (банк-источник): новая находка класса
  «непроверенный-вывод-как-факт» — кандидат в новый кейс экзамена;
  ревизия экзамена ссылается на находки-источники.
- Носители правил (CLAUDE.md ядро, role-файлы, PROCESS-протоколы) +
  tools/-гейты <-> docs/RULE_COVERAGE.md (карта «правило → сторож →
  режим срабатывания», заведена 2026-07-28 словом оператора): новый
  механизм или смена РЕЖИМА срабатывания существующего — строка/правка
  карты покрытия ТЕМ ЖЕ коммитом; свежесть — чек 24 (файл в списке
  владельцев), классовую полноту диффа по этой паре смотрит критик
  правилом 5.
- docs/RETRO_PATTERNS.md (бэклог ретро-паттернов) <->
  PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md (действующие чеки,
  2026-07-13): внедрение паттерна двигает его строку бэклога в
  секцию «Внедрено» тем же коммитом; триггеры бэклога сверяет чек 0
  каждого прогона; живость пары — чек 12.
- D:\Improving_AI\exam_fullgates_kit\tools (гейты policy-as-code —
  экзаменационный источник) <-> tools/{dispatch_gate,critic_snapshot,
  dod_track,dod_gate,main_gate,tier_echo,journal_echo}.py + test_*
  (штабные адаптации; порт t-159, активация 2026-07-16; tier_echo и
  journal_echo рождены в ШТАБЕ 2026-07-19 — в кит уходят синком
  подготовки прогона; tier_echo с пути СНЯТ 9e18347 — SubagentStop-
  канал кормит субагента, точка доставки в редизайне). Отличия штаба ЗАДОКУМЕНТИРОВАНЫ
  блоком «STAGING_HQ ВАРИАНТ» в докстринге каждого файла (снятие
  kit-исключения 2а, file_path в edit-записях, doc-only-правило
  .md/.json/.jsonl) — фикс/правка гейта на одной стороне требует
  вопроса «а на другой?» с поправкой на признанные отличия; живость
  пары — чек 12, отказ штабных гейтов — чек 26.
- Закрытие сессии <-> открытие сессии (D-0050):
  PROCESS/SESSION_PROTOCOL.md (Session End) + skills/session-handoff
  <-> BOOT.md + PROCESS/BOOT_REPORT_PROTOCOL.md. Проверка, добавленная
  на одной стороне, требует вопроса «а на другой?» (handoff проверяет
  вечером то, что Boot прочитает утром). AO3-пара той же оси (t-021,
  0911cf6): .claude/skills/session-handoff (вечер) <-> правило
  Session Start в docs/HANDOFF.md (утро, детектор пропуска).

## Ось 5 — Тестовая обвязка

tools/test_usage_report.py <-> gateway/test_*.py + gateway/conftest.py
(паттерн изоляции: tmp DB, conftest snapshot/restore litellm-колбэков;
путь уточнён калибровкой №2 — каталога gateway/tests не существует,
тесты лежат файлами в gateway/). Класс: «фикс затронул схему/учёт —
проверь изоляцию и фикстуры на обеих сторонах».

## Ось 6 — Внутренние оси AO3_tests (для сессий пилота)

Цепочка конвейера: .claude/skills/*/SKILL.md <-> state/rules.yaml <->
schemas/ (agent-output, transitions) <-> scripts/ (log_append,
board_sync, doctor и т.д.) <-> промпты 14 агентов. Класс на этой оси:
изменение формата/протокола в одном звене (событие журнала, поле
frontmatter, схема agent_output) проверяется по всей цепочке.

Под-оси (перенесены из AO3 HANDOFF 2026-07-08, правило поддержания
карты):

- `when`-условия state/rules.yaml <-> ручные метки/статусы в доках,
  которые правила читают. Класс: метка протухает, если её снятие не
  закреплено за конкретным агентом в том же ходе (прецеденты:
  needs-design в docs/01 §9; smoke_status-рассинхрон 2026-07-07).
- Один доменный паттерн в НЕСКОЛЬКИХ полях одной схемы:
  bug.schema.yaml дублирует паттерн bug-id в `id_pattern` и
  `regression_of` (потенциально — любое поле-ссылка на артефакт того
  же типа: duplicates, linked_bug). Правишь паттерн — пройди все
  поля схемы (найдено critic при ревью AT-BUG- 2026-07-08).
- Знание агента прозой в ТЕЛЕ артефакта <-> отслеживаемый артефакт,
  который читают правила: «Заметки для автоматизации» test-case
  описывали блокер, но конвейер видит только `bugs/`
  (`type: test_debt`) — блокеры жили заметками с 2026-07-02 и
  всплывали реактивно (прецеденты AT-BUG-004/005/006; смежный класс
  с под-осью про метки выше, но здесь протухает не метка, а само
  ЗНАНИЕ, никогда не становившееся машиночитаемым). Закрыто
  2026-07-09 в AO3: обязанность test-designer «блокер -> test_debt-баг
  у источника» + guard входа R14 (rules.yaml). Детектор отказа самого
  guard'а зарегистрирован 2026-07-09: чек 15
  PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md (якоря — разведка t-014).
- Пиксельная константа в кейсе/шаге без привязки к замеру среды
  (доклад strategist AO3 2026-07-29, внесено 2026-08-02): dpr
  2.625 vs 2.5, innerHeight 1800/2059/798, 20-dp хэндл, геометрия
  AT-BUG-030 — числовой литерал экранной геометрии в test-case/степе
  обязан нести привязку к замеру среды (какой AVD/вьюпорт его дал),
  иначе первый же другой профиль (API 26) даёт ложный красный.
  Первым споткнётся их TC-109.
- Писатель frontmatter/markdown-артефакта (два смежных класса, AO3
  2026-08-01/02): (а) байтовая точность EOL — `read_text`/`write_text`
  (newline=None) молча перегоняет окончания строк ВСЕГО файла, маскируется
  core.autocrlf; (б) граница frontmatter — regex `(?m)^field:` по всему
  тексту файла может зацепить строку в ТЕЛЕ артефакта, а слепая вставка
  без проверки существования поля плодит дубль YAML-ключа (PyYAML молча
  берёт последний — порча без падения). Образец правильной реализации
  обоих классов: их `scripts/gitlab_sync.py::writeback_gitlab_issue`
  (read_bytes/write_bytes + eol по факту + replace-or-insert строго в
  теле frontmatter). Известные носители остатка: их
  board_sync.approve_test_case/set_priority/set_severity,
  board_inbound._replace_field (оба класса), их HANDOFF «Открытые
  хвосты». Детектор дублей ключей frontmatter в их
  validate_frontmatter.py — в работе их стороной (2026-08-02).
- Фичевая трассабилити (внедрена AO3 2026-07-17, коммиты
  05fcd6f..afaf0de): docs/feature-registry.yaml (рукописный реестр,
  владелец test-strategist; правило гранулярности в шапке) <->
  поле `features:` frontmatter test-cases/** (валидатор
  scripts/validate_frontmatter.py: пустое/неизвестный id = ERROR) <->
  генераторы scripts/coverage_map.py (секции «Фичи → покрытие» /
  «Фичи без единого кейса» + детектор протухания реестра по
  inventoried_at_commit) <-> вход стратегии
  (.claude/agents/test-strategist.md: истина = код, владение
  реестром; триггер протухания реестра в state/rules.yaml). Класс:
  новая фича, дробление/переименование записи реестра, смена
  формата поля — правка по всей цепочке. Машинные детекторы держат
  три звена (валидатор — кейсы, coverage_map — протухание,
  rules.yaml — триггер стратега); ГРАНУЛЯРНОСТЬ записей машинно не
  ловится (поле features честно заполнено) — её смотрит чек 27
  калибровки.
- Персистентный Android-settings-стейт, выставляемый тестовой
  обвязкой (внесено 2026-08-12, критик-вход AT-BUG-064): настройки
  гостя, переживающие snapshot-boot эмулятора — `settings put global
  http_proxy` (их framework/core/mitm.py), `settings put system
  font_scale` и `cmd uimode night` (их framework/core/adb.py) —
  снимаются только in-process teardown'ом (try/finally); аварийное
  завершение между set и clear оставляет остаток, ломающий СЛЕДУЮЩУЮ
  сессию (прецедент: остаточный прокси, их AT-BUG-064 —
  ERR_PROXY_CONNECTION_FAILED на live без единого replay-теста).
  Класс: вводишь новую персистентную настройку гостя — обязателен
  fail-safe слой на подъёме сессии (образец: их
  `mitm.ensure_no_residual_proxy` + session-autouse хук conftest +
  перевзвод после device-liveness recovery), не только teardown.
  Известный остаток: font_scale/night mode без fail-safe — их
  AT-BUG-066 (заведён 2026-08-11).

| Это репо (штаб разработки ядра) | github.com/Xartaxana/Supervised-Delegation (публичная поставка) |
|---|---|
| toolkit/ — staging ядра (политика EN, роли, скиллы, PROCESS, tools, gateway-kit) | корень репозитория — релизные снимки staging'а |

Правило: изменение ядра рождается ЗДЕСЬ (полная rule-10 дисциплина),
в шаблон уходит релизной синхронизацией (снимок toolkit/ → пуш);
расхождение staging ↔ опубликованное — тот же класс дрейфа пар, что
ось 4. Живость — чек 12 (toolkit/ существует, remote доступен);
пользовательские деплои шаблона НЕ входят в эту ось (они — чужие
репозитории со своей политикой и журналом).

Внутрикитовая пара (D-0091, 2026-07-22):
toolkit/ADOPTION_LEDGER.template.md (номенклатура поставки) ↔
фактический состав кита (.claude/agents, .claude/skills, tools,
PROCESS) — новый кит-механизм получает строку шаблона ТЕМ ЖЕ
коммитом; уроненная строка хост-зеркала — класс F-52. Детекторы:
чек 32 штабной калибровки (дельта порт-батча ↔ леджер-строки
носителя цели) и чек 22 кит-протокола (хост: полнота/дрейф леджера
против шаблона).

Инструмент оси (VG-6, 2026-07-23): tools/parity_check.py +
tools/parity_manifest.json — 50 пар штаб↔staging (пересчёт t-328
07-28; число живёт чеком 12) под hash-базлайном
последнего синка; `--check` различает clean / hq-drift (порт-очередь,
легален по D-0074) / kit-drift (staging тронут вне порядка —
подозрителен) / both-drift (порт был, базлайн протух); релизный батч
кита обновляет базлайн `--sync`/`--init` тем же ходом. Разница,
запечённая в базлайн на `--init`, инструментом НЕ всплывает —
порт-долг живёт явными строками порт-очереди CURRENT_CONTEXT.
Живость пары манифест↔реальные пути — чек 12.

МОРАТОРИЙ D-0074 (приказ оператора 2026-07-12, после релиза
v0.1.0): эксперименты живут в ДВУХ деплоях (это репо + AO3);
toolkit-staging НЕ правится на каждом касании — порт в staging и
релизный снимок только БАТЧЕМ по проверенному у нас состоянию,
каждую синхронизацию открывает слово оператора. Дефолтный ответ
оси 7 в осевых блоках механизменных коммитов — «в очередь порта»
(порт-очередь тулкита в CURRENT_CONTEXT); её накопление и
staging↔published дрейф смотрит чек 12.

## Ось 8 — Экзаменационные наборы ярусов (D-0071, 2026-07-11)

| Инструмент | Ярус |
|---|---|
| PROCESS/SCOUT_GOLDEN_SET.md (D-0057) | scout |
| PROCESS/CRITIC_EXAM.md (D-0071, подсаженный дифф) | critic |
| PROCESS/LEAD_RANKING_EXAM.md (F-28/F-30, виньетки) | Lead-кандидаты |
| gateway/judge_calibration.json + PROCESS/JUDGE_CALIBRATION_PROTOCOL.md (D-0031) | judge |
| witness-приёмка каждой задачи (D-0052) — НАБОРА НЕТ по решению | builder |
| приёмка Lead'ом каждого драфта (метрики пилота, чек 13(г) failure_class=spec) — НАБОРА НЕТ по решению калибровки №5 (2026-07-29: выход роли принимается на каждой задаче, как у builder) | designer |
| PROCESS/DEPLOYMENT_ECONOMY_EXAM.md (A/B/C, заказ оператора 2026-07-13; regression-триггер — релизный снимок тулкита, детектор — чек 14е) | деплой/политика целиком |

Правило: изменение экзаменационной ДИСЦИПЛИНЫ (предрегистрация
ключей до диспатча, немаркированность, contamination-чек, триггеры
перепрогона, формат Runs log, кэш определений — п.2а
SCOUT_GOLDEN_SET) проверяется на переносимость по всем строкам оси;
новый ярусный набор входит в карту тем же коммитом. builder-строка
— записанное решение (execution-based приёмка сильнее набора), не
пропуск. Живость — чек 12 (пути) + чек 14 (regression-правило
правок role-файлов).

## Ось 9 — Durable-персистентность принятого деливерабла (F-50, 2026-07-22)

Приёмка и персистентность — разные события: witness/критик проходят
по РАБОЧЕМУ ДЕРЕВУ, durable-носитель наполняется широкими батчами
позже; в окне между ними дифф принятой работы может быть смыт
(широкий checkout/reset — прецедент AO3 AT-BUG-022/9eb15e4). Правишь
проверку персистентности на одном носителе — спроси «а на других?».

| Носитель принятого | Экземпляр класса | Что enforce'ит |
|---|---|---|
| Журнал (оба деплоя) | append-only строки событий | закрыт КОДОМ: journal_validator / log_append на пути коммита |
| Git-коммиты OS (широкие батчи сессий) | дифф принят, но не доехал до HEAD | session-handoff шаг 2а (вечер) + чек 31 калибровки (неделя); промоция в код-гейт — по первому рецидиву (D-0063) |
| Git-коммиты AO3 (гигантские батчи конвейера) | AT-BUG-022 | их фикс d4b612d: пост-коммит сверка деливераблов в /qa-loop + путь-ограниченные коммиты |

Проверка — по несущей сущности witness в HEAD, не по памяти сессии;
форензика споров — runtime-свидетели вне git (F-50, методичка).

## Ось 10 — База отбора «новых строк» эхо-хуков при записи (t-277/t-279, 2026-07-22)

tools/journal_echo.py эхо-слои (TIER / WITNESS / TS-DRIFT /
ESCALATION — страж R6-зеркала, t-317 Phase 4) <->
toolkit/tools/journal_echo.py (та же четвёрка слоёв: ts-drift и
warn_stale портированы t-316, ESCALATION — t-317 тем же батчем;
деферрал ts-drift РАЗРЕШЁН 2026-07-24). Инвариант: эхо-слой
оценивает ТОЛЬКО строки, добавленные
ТЕКУЩИМ tool-вызовом — payload-scoped через _resolve_echo_base
(источник tool_response.originalFile; fail-open → HEAD-дифф с
видимой пометкой FALLBACK_MARKER_TEXT), НЕ кумулятивный HEAD-дифф.
Кумулятивная база = класс дефекта t-277: незакоммиченная строка
переоценивается каждым PostToolUse (ts-drift — ложная растущая
STALE; tier/witness — пере-эхо). Контр-инвариант (НЕ трогать):
VALIDATION (decide() / JOURNAL ECHO) намеренно остаётся на
накопительной HEAD-дифф базе — форму незакоммиченных строк ловим до
коммита. Класс: новый эхо-коллектор в journal_echo ОБЯЗАН потреблять
echo_new_lines/echo_base_lines, не пересчитывать HEAD-дифф;
кит-сиблинг — тем же диффом. Детектор класса: чек 26(г) калибровки
(регулярные fallback-метки = originalFile исчез из payload).

## Ось 11 — Синк-пары Dog↔штаб (D-0082, заведена 2026-07-29 портом t-338)

Dog (D:\Dog) — деплой кита с двусторонним обменом механизмов; в ось 7
он не входит (не публичная поставка), но обмененные механизмы образуют
пары, расходящиеся ровно как ось 4. Правило: фикс класса в одной копии
пары требует вопроса «а в их копии?»; передача — по D-0082 в носитель
ЦЕЛИ при безопасном касании; молчаливое расхождение — класс F-9.

| Это репо | D:\Dog |
|---|---|
| tools/search_control_gate.py + tools/claim_control_gate.py (порт t-338 их t-021/t-022; правки ШТАБА поверх оригинала: word-boundary маркеры (F4 t-339), границы таблиц/заголовков (F5), bisect (F6), компакция >500 (F8), классификация поиска по строке команды, PowerShell-ветка) | tools/search_control_gate.py + tools/claim_control_gate.py (первоисточник; правки штаба им НЕ переданы — очередь передачи Dog в CURRENT_CONTEXT, при безопасном касании их носителя) |
| tools/negative_lint.py (первоисточник — штаб) | их входящее на синк 2 (записано их стороной 07-29) |
| tools/hygiene_gate.py (заимствование COMMIT_HEREDOC_RE из их копии документировано в докстринге; с 2026-08-09 штабная копия несёт V5 — блок cd-в-корень/2>&1-определённый по принципу «deny требует определённости», у них НЕ портировано) | tools/hygiene_gate.py (21215 Б, существование сверено критиком t-377; их копия — warn-режим; передача дельты V5 — по D-0082 при безопасном касании их носителя) |
| tools/journal_validator.py + tools/mechanism_gate.py + tools/session_context.py (штаб; с 2026-08-10 валидатор несёт фикс дыры B3 — self/foreign-retry с сигналами новой версии, невод расширен транзитивным замыканием) | те же три файла у Dog (существование сверено критиком t-386; их journal_validator:340 несёт ДОФИКСОВУЮ форму retry_ok — та же дыра B3; передача дельты — очередь Dog в CURRENT_CONTEXT, при безопасном касании их носителя). Строка заведена 08-10 по находке критика: пары существовали и расходились, карта их не индексировала — находка о полноте самой карты |

## Ось 12 — Носители привязки модели роли (D-0083, WARN-слой тип↔ярус, заведена 2026-08-09)

WARN-слой ЧАСТЬ B `tools/dispatch_gate.py` (role-type warn) ввёл
симметрию, которой в карте не было: роль-файл `.claude/agents/<роль>.md`
объявляет модель роли полем `model:` фронтматтера (короткое имя
семейства — haiku/sonnet/opus/fable), а `delegation.config.yaml`
объявляет ту же привязку в `roles.<роль>.subscription.model` (полный
id модели, D-0099). Два носителя одного факта, способные разойтись
молча.

| Читает роль-файл (`model:` фронтматтера) | Читает delegation.config.yaml (`roles.<роль>.subscription.model`) |
|---|---|
| tools/dispatch_gate.py ЧАСТЬ B (`role_type_warn`) — WARN на ПРОВЕДЁННОМ диспатче | tools/mechanism_gate.py (tier-декларация коммита), tools/journal_validator.py (lead-binding ветка матрицы), tools/session_context.py (метка яруса на SessionStart) |

Правило: правка модели роли на ОДНОЙ стороне (frontmatter `model:`
ИЛИ `roles.<роль>.subscription.model`) требует вопроса «а на другой?»
— кроме двух признанных асимметрий ниже.

Фактическое состояние на 2026-08-09 (проверено чтением обоих
носителей): семейства СОВПАДАЮТ по всем четырём ролям, присутствующим
в ОБОИХ носителях — scout (`model: haiku` ↔ `claude-haiku-4-5`),
builder (`model: sonnet` ↔ `claude-sonnet-5`), critic (`model: opus`
↔ `claude-opus-5`), designer (`model: opus` ↔ `claude-opus-5`).
Расхождений не найдено. Две ПРИЗНАННЫЕ асимметрии состава (не дрейф —
разный набор ролей по конструкции каждого носителя): (1)
`delegation.config.yaml` несёт `roles.lead` и `roles.reserve` —
привязку яруса САМОЙ координирующей сессии, которая никогда не
диспатчится Task-тулом, роль-файла в `.claude/agents/` для них нет и
не должно быть; (2) `.claude/agents/judge.md` объявляет `model: sonnet`
(подписочная форма судьи R13), но `delegation.config.yaml` НЕ несёт
`roles.judge` — судья намеренно живёт алиасами `gateway/config.yaml`
(комментарий самого файла: «здесь не дублируются, ось 2: один
носитель на факт») — API-контур, не subscription-привязка
координатора.

Проверяющий: живой детектор расхождения на момент диспатча — сам
`role_type_warn` (WARN, не блокирует); периодической сверки пары ВНЕ
момента диспатча (для ролей без свежих диспатчей в окне) на сейчас
нет — карта фиксирует это как открытый пробел, не как обязанность
существующего чека.

## Режимы отказа — индекс форм, не носителей (D-0100, заведён 2026-08-09)

Оси выше индексируют НОСИТЕЛЕЙ — родственные файлы/пары. Этот раздел
— второе измерение карты: РЕЖИМЫ ОТКАЗА, режущие поперёк
неродственных файлов, которые ничем, кроме формы дефекта, не связаны.
Правила раздела:

1. Класс входит только при счёте ≥2 в РАЗНЫХ файлах; экземпляры
   внутри одного файла — работа подметания R9, не реестра.
2. Если носители класса уже составляют ось карты — строка не
   заводится: ось покрывает, один дом на факт (D-0038).
3. Строка = класс + вопрос применимости + носители-экземпляры +
   указатель на находку/чек-владельца; полный текст живёт у
   владельца (FINDINGS / протокол / CURRENT_CONTEXT), здесь — только
   адрес.
4. Пустой реестр объявляется строкой «пусто на <дата>» — молчание
   неотличимо от смерти.
5. Читатель — критик при проверке классовой полноты диффа; вход в
   `.claude/agents/critic.md` ОТЛОЖЕН — заводится следующим касанием
   critic.md либо калибровкой №7.

### Класс: сторож/проверка читает только собственный след

Вопрос применимости: если этот механизм тихо перестанет работать —
что изменится в проверяемых данных? если ничего, детектора нет.

Статус: ЗАКРЫТ ЧАСТИЧНО с 2026-08-10. Детектор есть —
`tools/hook_liveness_probe.py` (t-387), прогоняется чеком 26(д):
заведомо триггерящий payload каждому гейту, след ОБЯЗАН быть,
молчание отличимо от «нарушений не было».

Носители (исходные): `dod_gate` + `main_gate` (чек 26(в) — сверяют
только СУЩЕСТВУЮЩИЕ события `gate_log`), `journal_echo` (чек 26(г) —
присутствие маркера вырожденного режима). Пополнены ПЯТЬЮ ранее
непокрытыми того же класса: `dispatch_gate`, `owns_gate`,
`claim_control_gate`, `search_control_gate`, `negative_lint` — их
отказ теперь отличим от штатной тишины независимым факт-триггером.

ОСТАТОК явно: смерть ПРОВОДКИ (регистрация в `.claude/settings.json`,
matcher, `core.hooksPath`) этим детектором НЕ покрыта — она за чеком
26(а) и `tools/wiring_check.py`; «закрыт частично», не «закрыт»,
пока два измерения не объединены одним прогоном.

Владелец разбора: чек 26 PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md
(трёхгрупповая сводка, установленный факт 4/3/5: покрыто с
детектором молчания / покрыто частично / не покрыто нигде) +
структурный критерий (детектору нужен НЕЗАВИСИМЫЙ факт-триггер, из
которого следует ожидание присутствия следа).

### Класс: сторож не отличает утверждение автора от цитируемого/вложенного содержимого

Вопрос применимости: разбирая текст, отличает ли сторож то, что автор
УТВЕРЖДАЕТ, от того, что он ЦИТИРУЕТ?

Носители: `hygiene_gate` (shell-поверхность, ЗАКРЫТ принципом
определённости 08-09 — блок cd-в-корень/2>&1 включён только там, где
deny определён однозначно), given-слой `dispatch_gate` + `owns_gate`
(markdown-поверхность, ОТКРЫТЫ — общий модуль регионов
fenced/inline/цитата не существует).

Указатель: очередь одной задачей в CURRENT_CONTEXT.md, секция «КЛАСС
«СТОРОЖ НЕ ОТЛИЧАЕТ ЦИТАТУ ОТ УТВЕРЖДЕНИЯ»» (общий markdown-модуль
регионов; CURRENT_CONTEXT — НЕ второй дом факта, полный текст живёт
там до переноса).

### Класс: git-subprocess без явной кодировки / quotepath (заведён 2026-08-10, F7 вердикта t-414)

Вопрос применимости: вызывает ли код git через subprocess с
`text=True` без `encoding=`, и разбирает ли его вывод ПУТИ без
`-c core.quotepath=false` (или `-z`)? Если да на любую половину —
экземпляр класса: на не-UTF8 локали значение приходит mojibake, а
не-ASCII пути приходят octal-quoted и разбор молча промахивается
(ложный пропуск или ложное срабатывание).

Носители-экземпляры (история): `wiring_check` обеих сторон +
`session_context` обеих сторон (закрыты t-410), `owns_verify` обеих
сторон — ОБЕ половины разом, ложное OUT-OF-OWNS доказано пробой
(закрыт фикс-батчем вердикта t-414). Образец правильной формы:
`enforcement_probe.py:216-230` (байты + явный decode + quotePath).
Чистые по AST-скану критика: journal_echo/journal_validator/
mechanism_gate/exam_runner/lead_replay (encoding есть, путей не
разбирают).

Указатель: вердикты t-407 (кит-инцидент cp1252) и t-414 (owns_verify
+ AST-скан поверхности); детектор нового экземпляра — этот вопрос
применимости у критика при любом диффе, добавляющем git-subprocess.

## Проверка самой карты (D-0048)

Карта — инструмент D-0043 и сама нуждается во внешней проверке;
собой её проверить нельзя (тот же класс, что F-15):

1. **Живость** — еженедельная калибровка (чек 12 протокола)
   проверяет, что каждый конкретный путь, названный в карте,
   существует в обоих репо. Переименовал/удалил файл — правь ось
   тем же коммитом.
2. **Полнота — по рецидивам.** Дефект класса, уже чинившегося по
   карте, всплыл в месте, куда карта не указывала → это находка О
   КАРТЕ (пропущенная или неверная ось), а не только о дефекте:
   фиксируется в FINDINGS, ось правится тем же фиксом.
3. **Рост.** Число строк карты считает калибровка (чек 10). Оси
   умирают симметрично рождению: симметрия исчезла (деплой закрыт,
   механизм удалён) → ось удаляется ТЕМ ЖЕ коммитом, что убил
   симметрию.

## Поддержание карты из второго репозитория

Карта одна — здесь (две карты сами стали бы расходящимися
«собратьями»). Сессия AO3, нашедшая новую симметрию: (а) если может —
отдельным коммитом в ЭТОТ репозиторий, сразу; (б) если нет — явная
строка в свой HANDOFF/журнал «новая ось для SIBLING_MAP: <что>», и
Lead OS-репо вносит её при следующем касании. Молчаливо не вносить
нельзя — это тот же класс нарушения, что и молчаливый пропуск (F-9).
