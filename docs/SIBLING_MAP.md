# Sibling Map — карта осей симметрии (D-0043)

Инструмент правила «чини класс, а не экземпляр»: закрывая дефект, обход
аналогичных мест делается ПО ЭТОЙ КАРТЕ, а не сканом репозитория.
Карта намеренно мала и грузится только в момент закрытия дефекта (не
при boot — D-0038). Порядок работы:

1. Найди ось (оси), на которых лежит дефект — см. список ниже.
2. Проверь/почини парные места оси точечно; что не чинится сейчас —
   явно в очередь (CURRENT_CONTEXT.md) или журнал.
3. Класс шире карты или ось не находится → диспатчь scout (haiku) на
   точечный поиск с конкретным вопросом. Lead/critic НЕ перечитывают
   репозиторий руками (правило 1).
4. Появилась новая симметрия (новый деплой, новый парный документ,
   новая пара код/учёт) → добавь ось сюда В ТОМ ЖЕ коммите — это
   часть шага 3 D-0043.

critic проверяет классовую полноту фикса ПО КАРТЕ: «покрыты ли оси,
на которых лежит дефект» — ограниченный вопрос по диффу и карте, не
по всему коду.

## Ось 1 — Деплои routing MVP (правки политики/ролей/журнала — всегда парные)

| Это репо | D:\AO3_tests |
|---|---|
| CLAUDE.md (политика, правила 1–11, вокабуляр событий) | CLAUDE.md (то же + дисциплина команд конвейера) |
| .claude/agents/{scout,builder,critic}.md | те же три + 10 QA-агентов конвейера |
| logs/routing-log.jsonl (формат событий) | logs/routing-log.jsonl + scripts/log_append.py (enforce формата) |
| .githooks/commit-msg + tools/mechanism_gate.py (enforce правила 10б, D-0055) | .githooks/commit-msg + scripts/mechanism_gate.py (карта читается из OS-репо) |
| — | .claude/skills/qa-loop/SKILL.md (журналирование диспатчей) |

## Ось 2 — Контуры измерения (Rule #1, D-0032: учёт честен на обоих)

| API-контур (gateway) | Субскрипционный контур (Claude Code) |
|---|---|
| gateway/sqlite_logger.py -> requests | tools/usage_report.py -> cc_usage |
| учётные цены алиасов в gateway/config.yaml | PRICES_PER_TOKEN_USD в usage_report.py |
| traffic_kind (real/synthetic/replay/judge) | is_sidechain + agent_id/agent_type |
| evidence: Shadow Evaluation Log (DELEGATION_TABLE.md) | evidence: routing-журнал + вердикты приёмки |
| gateway/metrics.py (дайджест) | tools/usage_report.py (отчёт) |

Классические классы на этой оси: новая модель/цена (добавить в ОБА
учёта), новый вид трафика/атрибуции, правило «никогда молчаливый $0».

## Ось 3 — Роли/ярусы (обязанность одной роли — проверь остальные)

scout / builder / critic (х2 деплоя) + Lead-правила в CLAUDE.md +
10 QA-агентов AO3 (их промпты — цикл изменений конвейера; правки туда
ставятся в очередь, если не по пути). Пример класса: «докладывай
замеченные аналоги» (D-0043), «не запускай субагентов» (D-0037).

## Ось 4 — Парные документы (инвариант: не расходиться)

- DECISIONS.md <-> CLAUDE.md обоих деплоев (решение принято — политика
  отражает; F-9/D-0042 были рассинхроном именно здесь).
- DECISIONS.md (индекс) <-> docs/DECISIONS_FULL.md (полные тексты,
  D-0051): число записей D-XXXX обязано совпадать (чек 12 калибровки);
  новое решение — обе стороны тем же коммитом.
- ROADMAP.md — единственный владелец фаз/гейтов; ARCHITECTURE.md и
  WHITE_PAPER.md ссылаются, не дублируют.
- BOOT.md <-> MEMORY_ARCHITECTURE.md (слои и список загрузки).
- docs/FINDINGS.md (наши наблюдения) <-> docs/RELATED_WORK.md (внешние
  приоры): факт заходит в один из двух, по происхождению.
- CURRENT_CONTEXT.md <-> docs/task_reports/ (D-0038: закрыто — в архив,
  указатель остаётся).
- PROCESS/SCOUT_GOLDEN_SET.md <-> .claude/agents/scout.md (D-0057:
  правка роли/модели яруса требует прогона набора тем же коммитом;
  ключи вопросов смотрят в живые файлы — verify-команды сверяются
  при каждом прогоне, живость — чек 14б).
- .claude/skills/next-task/SKILL.md <-> CLAUDE.md (D-0061: skill —
  порядок шагов, политика — правила; правило меняется только в
  CLAUDE.md, skill ссылается и не дублирует формулировок; при
  расхождении политика выигрывает).
- ARCHITECTURE.md «Two Vocabularies» <-> CLAUDE.md «Ярусы» обоих
  деплоев (D-0062: новая функция/грейд или смена привязки — мост и
  политика правятся вместе; грейд внутри правила маршрутизации —
  дефект словаря).
- Закрытие сессии <-> открытие сессии (D-0050):
  PROCESS/SESSION_PROTOCOL.md (Session End) + skills/session-handoff
  <-> BOOT.md + PROCESS/BOOT_REPORT_PROTOCOL.md. Проверка, добавленная
  на одной стороне, требует вопроса «а на другой?» (handoff проверяет
  вечером то, что Boot прочитает утром).

## Ось 5 — Тестовая обвязка

tools/test_usage_report.py <-> gateway/tests (паттерн изоляции:
tmp DB, conftest snapshot/restore litellm-колбэков). Класс: «фикс
затронул схему/учёт — проверь изоляцию и фикстуры на обеих сторонах».

## Ось 6 — Внутренние оси AO3_tests (для сессий пилота)

Цепочка конвейера: .claude/skills/*/SKILL.md <-> state/rules.yaml <->
schemas/ (agent-output, transitions) <-> scripts/ (log_append,
board_sync, doctor и т.д.) <-> промпты 13 агентов. Класс на этой оси:
изменение формата/протокола в одном звене (событие журнала, поле
frontmatter, схема agent_output) проверяется по всей цепочке.

Под-оси (перенесены из AO3 HANDOFF 2026-07-08, правило поддержания
карты):

- `when`-условия state/rules.yaml <-> ручные метки/статусы в доках,
  которые правила читают. Класс: метка протухает, если её снятие не
  закреплено за конкретным агентом в том же ходе (прецеденты:
  needs-design в docs/01 §9; smoke_status-рассинхрон 2026-07-07).
- Один доменный паттерн в НЕСКОЛЬКИХ полях одной схемы:
  bug.schema.yaml дублирует паттерн bug-id в `id_pattern` и
  `regression_of` (потенциально — любое поле-ссылка на артефакт того
  же типа: duplicates, linked_bug). Правишь паттерн — пройди все
  поля схемы (найдено critic при ревью AT-BUG- 2026-07-08).
- Знание агента прозой в ТЕЛЕ артефакта <-> отслеживаемый артефакт,
  который читают правила: «Заметки для автоматизации» test-case
  описывали блокер, но конвейер видит только `bugs/`
  (`type: test_debt`) — блокеры жили заметками с 2026-07-02 и
  всплывали реактивно (прецеденты AT-BUG-004/005/006; смежный класс
  с под-осью про метки выше, но здесь протухает не метка, а само
  ЗНАНИЕ, никогда не становившееся машиночитаемым). Закрыто
  2026-07-09 в AO3: обязанность test-designer «блокер -> test_debt-баг
  у источника» + guard входа R14 (rules.yaml). В очередь Lead
  OS-репо: чек еженедельной калибровки на отказ самого guard'а
  (WEEKLY_CALIBRATION_PROTOCOL) — из AO3-сессии не вносится.

## Проверка самой карты (D-0048)

Карта — инструмент D-0043 и сама нуждается во внешней проверке;
собой её проверить нельзя (тот же класс, что F-15):

1. **Живость** — еженедельная калибровка (чек 12 протокола)
   проверяет, что каждый конкретный путь, названный в карте,
   существует в обоих репо. Переименовал/удалил файл — правь ось
   тем же коммитом.
2. **Полнота — по рецидивам.** Дефект класса, уже чинившегося по
   карте, всплыл в месте, куда карта не указывала → это находка О
   КАРТЕ (пропущенная или неверная ось), а не только о дефекте:
   фиксируется в FINDINGS, ось правится тем же фиксом.
3. **Рост.** Число строк карты считает калибровка (чек 10). Оси
   умирают симметрично рождению: симметрия исчезла (деплой закрыт,
   механизм удалён) → ось удаляется ТЕМ ЖЕ коммитом, что убил
   симметрию.

## Поддержание карты из второго репозитория

Карта одна — здесь (две карты сами стали бы расходящимися
«собратьями»). Сессия AO3, нашедшая новую симметрию: (а) если может —
отдельным коммитом в ЭТОТ репозиторий, сразу; (б) если нет — явная
строка в свой HANDOFF/журнал «новая ось для SIBLING_MAP: <что>», и
Lead OS-репо вносит её при следующем касании. Молчаливо не вносить
нельзя — это тот же класс нарушения, что и молчаливый пропуск (F-9).
