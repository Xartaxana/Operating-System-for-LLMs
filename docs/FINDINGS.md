# Findings

Реестр эмпирических находок из эксплуатации самой архитектуры
(dogfooding) — оба контура: субскрипционный (Claude Code) и API
(gateway / Shadow Evaluation). В отличие от docs/RELATED_WORK.md
(внешние приоры, которые наша телеметрия должна подтвердить или
опровергнуть), здесь фиксируются наблюдения, полученные НАШИМИ
сессиями. Материал для White Paper.

Каждая находка: наблюдение → как получено → следствие для архитектуры →
статус (нужно ли решение DECISIONS.md).

Правила реестра. Записи дублируют-с-указателем, а не заменяют
канонический источник (evidence-лог DELEGATION_TABLE.md, отчёты в
docs/task_reports/ остаются на месте — D-0038); при расхождении
канонический источник прав. Нумерация — по порядку записи в реестр, не
по хронологии наблюдения: F-2..F-8 внесены 2026-07-08 задним числом,
при том что наблюдались раньше F-1.

---

## F-1 — Дефолтный харнесс не инициирует делегирование сам

**Наблюдение.** Оболочка Claude Code по умолчанию НЕ поощряет спавн
субагентов без явного запроса оператора. Инструкция агента прямо
задаёт: «Do not spawn agents unless the user asks.» Предоставленный сам
себе, Lead-сессия выполняет делегируемую работу САМА — на самом дорогом
из доступных ярусов (frontier-модель), — вместо того чтобы отдать её
вниз.

**Как получено (2026-07-08).** Delegated Task 6 (builder-tier по нашей
же DELEGATION_TABLE.md) ушла в субагента Sonnet и была принята с первого
ревью — но ТОЛЬКО потому, что оператор явно сказал «запускай в
субагенте». Без этой команды Lead (Fable) с высокой вероятностью
выполнил бы задачу сам. Диспатч был ручным: в этом репозитории на тот
момент не было ни CLAUDE.md с политикой, ни агентов scout/builder/critic,
ни журнала — политика роутинга существовала только как пилот на
D:\AO3_tests. Сопоставимо с первой живой проверкой на AO3 (2026-07-07):
там делегирование сработало, потому что дисциплина была прописана в
CLAUDE.md проекта; событие в журнал при этом не попало, т.к. диспатч
шёл через /qa-loop, чей промпт предшествовал политике. Оба случая — об
одном: **дисциплина действует ровно там, где она физически прописана в
загруженном контексте сессии.**

**Следствие для архитектуры.** Экономия от делегирования на
субскрипционном контуре НЕ возникает по умолчанию — её нужно активно
включить. Политику маршрутизации обязано автозагружать в контекст Lead
каждой сессии (в Claude Code это CLAUDE.md проекта); одних определений
агентов в .claude/agents/ недостаточно, потому что они лишь ОПИСЫВАЮТ
ярусы, но не побуждают Lead к ним обращаться. Это усиливает «Escalation
is a Resource» (PROJECT_PHILOSOPHY.md) конкретным механизмом: без
явного, всегда-загруженного правила модель делегирует по нулям, а не
избыточно. Практический вывод для проекта: routing MVP надо
разворачивать per-project (CLAUDE.md + агенты + журнал), а не считать,
что «умная модель сама догадается делегировать». Развёрнуто в этом
репозитории 2026-07-08 (эталонный деплой) и на AO3_tests (пилот).

**Замечание для White Paper.** Это контрапункт к нарративу
«агент-фреймворки максимизируют число агентов» (ANTI_GOALS.md): дефолт
современного продакшн-харнесса — противоположный, консервативный
(не-делегирование). Наша задача — не сдержать взрывной спавн, а поднять
делегирование с нуля до обоснованного минимума, управляемого evidence.

**Статус.** ОФОРМЛЕНО решением D-0041 (2026-07-08, восстановленный
Lead на Fable с одобрения оператора): делегирование на субскрипционном
контуре — opt-in; «задеплоить routing MVP» = политика в автозагрузке +
агенты ярусов + журнал, всегда втроём. Сама находка была записана в
degraded-режиме на Opus 4.8, решение — после lead_restored, как и
требует D-0039. Смежное решение той же датой: D-0042 (operator-switch
как триггер деградации).

---

## F-2 — Провайдерский кэш уже поглощает почти всё повторение контекста

**Наблюдение (2026-07-07, Task 5).** 97.6% input-токенов
субскрипционного контура — cache reads; учётная экономия против
безкэшевой цены $7,117 при общих тратах $1,178 (all-time базлайн,
8747 ходов).

**Следствие.** Прямое локальное подтверждение порядка D-0036: сначала
cache-aware учёт и измерение net-of-cache, и только потом какая-либо
lossy-компрессия. C-гейт Phase 2 обязан считать «деньги на столе»
только по ОПЛАЧЕННОМУ некэшированному повторно отправленному входу.

**Источник (канонический).** docs/task_reports/task-5_usage-report.md;
сводка в CURRENT_CONTEXT.md «Claude Code Baseline». Статус: учтено в
D-0036/ROADMAP (C-критерии cache-aware); решения не требуется.

---

## F-3 — Базлайн — цензурированные данные; frontier жжёт быстрее на единицу работы

**Наблюдение (2026-07-07, поправка Архитектора к Task 5).** Микс трат
базлайна нельзя читать как «где деньги» — оператор нормировал
использование frontier-ярусов из-за лимитов (цензурированные данные).
Корректное чтение — скорость расхода на единицу: opus-4-8 $0.264/ход,
fable-5 $0.216 против sonnet-4-6 $0.114 и sonnet-5 $0.063 (2–4x).

**Следствие.** Метрика успеха роутинга — стоимость за ПРИНЯТУЮ единицу
работы по ярусу + доля эскалаций, а не доля frontier в тратах
(освобождение лимитов легитимно ПОДНИМЕТ долю Fable на архитектурной
работе). Еженедельный цикл смотрит тренд свежего окна, не all-time.

**Источник (канонический).** docs/task_reports/task-5_usage-report.md
(полный текст поправки); сводка в CURRENT_CONTEXT.md. Статус: принято
как методика Phase 1.5; решения не требуется.

---

## F-4 — Вложенная диспетчеризация ломается на практике

**Наблюдение (2026-07-04, репетиция на AO3_tests).** Попытка дать
субагентам запускать собственных субагентов сломалась в живой
репетиции оркестрации QA-конвейера. Деталей прогона сохранилось мало
(находка предшествует и этому реестру, и журналу маршрутизации —
канонической записью была одна строка в CLAUDE.md пилота AO3_tests,
вне этого репозитория; поэтому здесь полная запись, а не указатель).

**Следствие.** Один из трёх аргументов D-0037 (плоское делегирование):
помимо принципиальных — декомпозиция и есть работа сильнейшего яруса;
вложенность разрушает атрибуцию стоимости/evidence — есть и грубо
практический: в текущем харнесе это просто ненадёжно. «Decomposable» —
категория эскалации, не право воркера делиться.

**Статус.** Оформлено D-0037 (2026-07-07). Урок реестра: находки,
живущие строкой в чужом файле, теряют детали — фиксировать сюда сразу.

---

## F-5 — Дешёвая метрика сходства меряет многословность, а не качество

**Наблюдение (2026-07-03, первые прогоны Shadow Evaluation).** difflib
(посимвольное сходство) как судья: 2 из 5 вердиктов неверны —
развёрнутый корректный ответ получает sim≈0 против лаконичного
эталона. Низкий sim ≠ хуже; ручное ревью опрокинуло rejected по coding.

**Следствие.** Валидация делегирования требует семантического судьи
(LLM), а судья — калибровки против размеченного людьми/Lead'ом сета.
Дешёвая детерминированная метрика годится только как грубый фильтр
высоких значений.

**Источник (канонический).** DELEGATION_TABLE.md, каверза
«comparison method» + Shadow Evaluation Log;
docs/task_reports/2026-07-03_shadow-evaluation-and-llm-judge.md.
Статус: закрыто внедрением LLM-судьи (D-0031).

---

## F-6 — Два режима отказа LLM-судьи: контаминация выборки и галлюцинация бага

**Наблюдение (2026-07-03).** (a) Неудачная калибровка судьи записала
его промпты как обычный трафик lead-gemini; случайная выборка Shadow
Evaluation втянула 6 вложенных судейских промптов из 11 пар — четыре
строки evidence отозваны (RETRACTED). (b) Судья middle-groq,
трассируя КОРРЕКТНЫЙ код фибоначчи, галлюцинировал баг («возвращает
b» — возвращает a) устойчиво к промпт-тюнингу; заменён reasoning-моделью
(judge-groq, калибровка 11/11).

**Следствие.** Судья — сам supervised worker (D-0031): выборка
исключает judge/replay трафик по traffic_kind; статус-меняющие
вердикты проходят chief-judge ревью; замена модели судьи — только по
измеренной деградации согласия. Режим отказа «галлюцинация при
трассировке корректного кода» внесён в правила critic-агента.

**Источник (канонический).** DELEGATION_TABLE.md Shadow Evaluation Log
(RETRACTED-блок и ruling); docs/task_reports/2026-07-03_shadow-evaluation-and-llm-judge.md;
PROCESS/JUDGE_CALIBRATION_PROTOCOL.md. Статус: оформлено D-0031.

---

## F-7 — Молчаливый $0: упавший учёт стоимости неотличим от бесплатности

**Наблюдение (2026-07-04).** Все строки evidence до 2026-07-03 несли
cost_target=$0.0000 — клиентский артефакт: litellm completion_cost()
не знал гейтвейных алиасов, молча бросал исключение, оно глоталось.
Проксайд-учёт в requests.db был верен всё время.

**Следствие.** Rule #1 проверяем только при честной цепочке стоимости:
неизвестная цена — это явный WARNING/None, никогда не $0; «не
вычислимо» печатается как «not computable yet + чего не хватает», а не
фальшивый ноль. Паттерн зашит в usage_report.py (unknown model → None
+ warning) и в спеку Task 3 (правило 3).

**Источник (канонический).** DELEGATION_TABLE.md, каверза
«cost_target=$0.0000». Статус: закрыто фиксом shadow_eval.py; принцип
воспроизводится во всех новых инструментах.

---

## F-8 — Слепые зоны телеметрии: параллельная раскладка транскриптов и рост живых файлов

**Наблюдение (2026-07-07/08, Task 6).** (a) Субагентные транскрипты
живут в отдельной раскладке (<project>/<session>/subagents/*.jsonl) и
были целиком невидимы для cc_usage — 7.2% токенов / $100.03 учётных
не попадали в книги; ретро-базлайн AO3 занижен на $57.82. (b) Живой
транскрипт растёт между командами: наивная проверка идемпотентности
импорта «дважды подряд» ловит 1–4 «новых» строки от САМОЙ работающей
сессии — сравнение honest только внутри одного вызова (снапшот).

**Следствие.** Полноту телеметрии надо верифицировать против самого
измеряемого процесса (чем больше делегируешь — тем сильнее врали бы
книги: недоучёт рос вместе с успехом роутинга). Идемпотентность на
живой системе определяется относительно фиксированного снапшота.

**Источник (канонический).** docs/task_reports/task-6_subagent-transcripts.md.
Статус: закрыто фиксом Task 6 (коммит 75af5b5); решения не требуется.

---

## F-9 — Автозагруженная политика не самоисполняется: Lead молча самоизъялся в первые же часы

**Наблюдение (2026-07-08, поймано оператором).** Через несколько часов
после усиления правила 1 («разведка → scout по умолчанию») Lead-сессия
выполнила разведочную проверку (поиск по репозиторию + чтение файла)
сама, без scout — и БЕЗ требуемой правилом записи причины в журнал.
Пропуск по существу был оправдан (4 точечные команды по известным
целям; передача контекста scout'у дороже самой работы — Rule #1), но
он был молчаливым: нарушение заметил оператор, не система.

**Следствие.** F-1 говорил: без автозагруженной политики делегирование
не возникает. F-9 уточняет: автозагруженной политики тоже мало, если
соблюдение дороже нарушения. Правило «всегда делегируй разведку» без
нижней границы срабатывает на каждый grep — и потому игнорируется, а
игнорируемое правило не производит данных. Механизм исправления —
сделать ПРОПУСК дешёвым и легальным: явный тип события
`dispatch_skipped` (одна строка в журнал с причиной) + калибровка
правила (точечная сверка ≤~4 известных целей может остаться у Lead,
скрытая разведка неизвестного объёма — всегда scout). Молчаливый
пропуск остаётся нарушением; журналируемый — данными о реальной
границе яруса, которые читает еженедельная калибровка. Для White
Paper: комплаенс-действие должно быть дешевле нарушения, иначе
политика деградирует в текст; и надзор оператора ловит то, что
самонадзор пропускает (человек в контуре — не декорация).

**Источник (канонический).** logs/routing-log.jsonl (событие
dispatch_skipped 2026-07-08T13:24:36, RETROACTIVE); калибровка правила
1 и тип события — CLAUDE.md обоих деплоев. Статус: механизм внедрён и
в тот же день (по вопросу оператора) РАСПРОСТРАНЁН НА ВСЕ ЯРУСЫ —
универсальное правило 8 в CLAUDE.md обоих репо: любая задача,
отображающаяся на дешёвый ярус, но выполненная Lead'ом сам, законна
только с dispatch_skipped (льгота: critic-пропуск на мелком диффе —
пометкой внутри accepted-события; пропуски правил /qa-loop покрывает
orchestrator-log). Решения DECISIONS.md пока не требуется (если
dispatch_skipped приживётся, вокабуляр журнала стоит зафиксировать
решением при первой недельной калибровке).

---

## F-10 — Все ярусы и модели чинят экземпляр дефекта, а не класс

**Наблюдение (2026-07-08, оператор; паттерн ловился неоднократно,
кросс-модельно).** Найдя дыру в одном месте, модель чинит её там же —
и не обобщает: не называет класс дефекта, не проходит по известным
аналогичным местам, не создаёт общего правила. Три подтверждения за
одну эту сессию: (а) лекарство F-9 применено только к scout, хотя
builder/critic имели ту же дыру — обобщено лишь после вопроса
оператора; (б) routing MVP месил только AO3, собственный репозиторий
проекта стоял без политики, пока оператор не спросил «а мы сами?»;
(в) триггер D-0042 добавлен в один деплой и не синхронизирован во
второй до следующего касания. Каждый раз аналогичные места были
известны или обязаны были быть известны.

**Следствие.** Локальное латание — устойчивый режим отказа LLM,
воспроизводящийся на всех ярусах (от Haiku до frontier) и не
исправляемый ростом модели; значит, обобщение фикса должно быть
явной обязанностью системы, а не ожиданием от интеллекта. Оформлено
D-0043 «Fix the class, not the instance» на КАЖДОМ уровне, где живут
правила поведения: конституция (SYSTEM_PROMPT.md — три шага: назови
класс / обойди аналоги, чиня или явно ставя в очередь / правило — на
самый верхний связывающий уровень), политика (правило 9 в CLAUDE.md
обоих деплоев), роли (builder докладывает замеченные аналоги, не
расширяя scope — D-0037; critic проверяет классовую полноту фикса как
штатную категорию находок; Lead владеет обходом). Молчаливо
оставленный известный аналог = нарушение — симметрично молчаливому
пропуску F-9. Для White Paper: пара F-9/F-10 — два лика одной
проблемы (локальность поведения LLM); лечение одно — сделать
глобальное действие дешёвым, явным и проверяемым ревьюером.

**Источник (канонический).** DECISIONS.md D-0043; SYSTEM_PROMPT.md;
CLAUDE.md и .claude/agents/{builder,critic}.md обоих деплоев. Статус:
оформлено D-0043 (2026-07-08). Остаток обхода поставлен в очередь
честно: девять QA-агентов конвейера AO3 получат строку «докладывай
замеченные аналоги» при следующем касании их промптов (записано в
CURRENT_CONTEXT.md).

Дополнение (тот же день, вопрос оператора о стоимости проверки):
чтобы «где такие же?» не требовало перезагрузки репозитория в
контекст, обход ограничен КАРТОЙ ОСЕЙ СИММЕТРИИ —
docs/SIBLING_MAP.md (деплои, контуры, роли, парные документы,
тестовая обвязка; грузится только в момент закрытия дефекта, не при
boot — D-0038). Класс шире карты → точечный scout (haiku), не ручной
скан Lead/critic. Новая симметрия — новая ось, тем же коммитом.
Экономика: lookup по одному маленькому файлу против рескана — сама
проверка полноты подчинена Rule #1.

---

## F-11 — Сильнейшая модель не видит пробелов в собственных механизмах; их находят вопросы оператора

**Наблюдение (2026-07-08, оператор; паттерн повторяющийся).** Каждый
существенный пробел в механизмах, спроектированных самим frontier-Lead,
был найден не системой и не моделью, а коротким вопросом оператора:

- 2026-07-03, судья: первая разметка судьи на слабой модели была
  принята Lead'ом за факт без ручной перепроверки — до вопроса
  оператора. Ручное ревью затем опрокинуло 2 из 5 difflib-вердиктов;
  «калибровка 11/11» middle-groq оказалась контаминированной (выборка
  втянула судейские промпты, 4 строки evidence отозваны), а сам судья
  — галлюцинирующим баг при трассировке корректного кода. Без вопроса
  эти ошибки ушли бы в статусы таблицы. Структурный фикс — D-0031
  (судья — supervised worker, chief-judge ревью обязательно).
- 2026-07-08, четыре подряд: «мы пользуемся своей же системой?» →
  routing MVP не был развёрнут в собственном репо (F-1);
  «а все уровни?» → лекарство F-9 было применено только к scout
  (правило 8); «это не заставит перезагружать весь код?» →
  обязанность обхода не имела ограничителя стоимости (SIBLING_MAP);
  плюс саму молчаливую самоизъятость Lead (F-9) заметил оператор.
- 2026-07-08, пятый: «разбор при возврате будет выполнен
  автоматически?» → возврат из деградации не имел шага приёмки —
  диффы деградированного окна вливались в mainline без ревью
  (F-12, D-0044).
- 2026-07-08, шестой: «а если ревью-циклы сделают задачу дороже, чем
  сразу на Opus — отловится?» → «неудачная попытка» правила 6 не была
  определена и не оставляла следа в журнале; циклы доработок были
  невидимы и для эскалации, и для Update Rule 4 (F-13, D-0045).
- 2026-07-08, седьмой: «а скаута мы как-то проверяем? верхние модели
  поверят ему на слово» → приёмка дайджеста не имела процедуры;
  судья с той же проблемой был починен (D-0031), scout — его
  пропущенный собрат по оси 3 (F-14, D-0046).
- 2026-07-08, восьмой: «а сама карта осей верная? она же будет
  разрастаться» → у SIBLING_MAP не было ни проверки живости путей,
  ни сигнала неполноты, ни правила удаления осей — только правило
  добавления (F-16, D-0048).

**Класс.** Frontier-интеллект систематически не обнаруживает слепые
зоны в механизмах собственного дизайна: недостающий ярус, недостающий
ограничитель стоимости, непроверенное основание (разметка слабого
судьи). Валидирующий вопрос приходит снаружи. Это не дефицит
интеллекта, который лечится более сильной моделью, — Lead и есть
сильнейший доступный ярус.

**Следствие для архитектуры.** Роль Архитектора-человека в иерархии
(последняя строка таблицы ярусов) эмпирически оправдана и не
сводится к подписанию гейтов (D-0033): самый дешёвый и самый
результативный инструмент надзора в системе — короткий вопрос
(«а вручную проверял?», «а везде?», «а что это стоит?»), каждый из
которых дал структурный фикс (D-0031, правило 8, D-0043, SIBLING_MAP).
Механизация: три контрольных вопроса встроены в политику как правило
10 — каждый новый механизм перед коммитом письменно отвечает, (а)
сколько стоит его соблюдение и кто платит, (б) покрыты ли все оси,
(в) кто и по какому потоку данных заметит его отказ. Вопросы
оператора, вскрывшие пробел, фиксируются как findings — они не должны
растворяться в чате (D-0014). Для White Paper: вершина иерархии
надзора «модель следит за моделью» сама нуждается во внешнем
кросс-экзамене, и его рабочая форма — вопросы, а не ревью артефактов.

**Источник (канонический).** Судья: DELEGATION_TABLE.md (каверзы и
Shadow Evaluation Log), docs/task_reports/2026-07-03_shadow-evaluation-and-llm-judge.md,
D-0031. Сегодняшние случаи: F-1, F-9, F-10 и их коммиты. Статус:
правило 10 в CLAUDE.md обоих деплоев (2026-07-08); решения не
требуется — D-0031/D-0043 уже покрывают структурную часть.

## F-12 — Возврат из деградации не имел приёмки: работа деградированного окна вливалась в mainline без ревью

**Наблюдение (2026-07-08, вопрос оператора — пятый случай паттерна
F-11).** На вопрос «оба пункта уже прописаны и разбор при возврате
будет выполнен автоматически?» честный ответ был «наполовину».
Очередь отложенных решений прописана (D-0039, CLAUDE.md обоих
деплоев) и отработала в первом живом цикле 2026-07-08. Но разбор
участка деградации при возврате не был прописан НИГДЕ — ни в D-0039,
ни в политике деплоев, ни в BOOT/PROCESS (грепы по «degrad» пустые).
Первый живой цикл подтверждает: notes события `lead_restored`
(12:35:24) разблокировали очередь решений — и всё; диффы
деградированного окна (commit 7f60273, сделан на Opus) в mainline
никто не ревьюил.

**Класс.** Деградированный Lead — фактически воркер нижнего яруса,
но единственный «воркер» системы, чей результат обходил обязанность
приёмки D-0037: субагентов принимает Lead, а деградированного Lead —
никто. Общий вид класса: обязанность, назначенная роли «исполнитель
нижнего яруса», должна проверяться и на роли «сам координатор,
временно работающий нижним ярусом» (ось 3 SIBLING_MAP).

**Фикс.** D-0044: возврат включает приёмку участка деградации —
восстановленный Lead просматривает журнал и диффы окна как штатную
приёмку D-0037, итог — в notes события `lead_restored` (пустое окно
тоже отмечается); разбор очереди отложенных решений приёмку не
заменяет. Политика — п.3 раздела «Деградация Lead» CLAUDE.md обоих
деплоев (ось 1/4). Отказ механизма виден еженедельной калибровке по
уже читаемому ею потоку: событие `lead_restored` без пометки о
разборе окна — нарушение.

**Источник (канонический).** DECISIONS.md D-0044; CLAUDE.md обоих
деплоев (раздел «Деградация Lead», п.3); первый цикл —
logs/routing-log.jsonl, события 2026-07-08 12:30:00 / 12:35:24.

## F-13 — Цикл доработок был невидим: эскалацию по правилу 6 нечем проверить, полную стоимость Update Rule 4 нечем посчитать по журналу

**Наблюдение (2026-07-08, вопрос оператора — шестой случай паттерна
F-11).** Вопрос: «если Opus-ревьюер будет постоянно находить ошибки и
задача выйдет дороже, чем сделать сразу на Opus — такой случай
отловится и эскалируется?» По замыслу — да, двумя контурами:
оперативно правило 6 (2 неудачные попытки → ярус выше, не повторять
молча), ретроспективно Update Rule 4 (сравнивается ПОЛНАЯ стоимость
задачи с ретраями; внешний приор «1 цикл frontier против 10 у дешёвой
модели» записан в External Evidence). Но механика была дырява дважды:
(а) нигде не определено, что отклонение результата на приёмке — это и
есть «неудачная попытка» правила 6 (опыт F-9: что не написано, то
молча не исполняется); (б) отклонение не оставляло следа — в словаре
журнала есть `accepted`, но не было отрицательной пары (оба деплоя;
в AO3 enforce формата scripts/log_append.py отверг бы такое событие).
Следствие: правило 6 проваливало вопрос (в) правила 10 — тихий третий
заход на том же ярусе не был виден ни по какому потоку данных, а циклы
доработок для Update Rule 4 не считались по журналу (только косвенно
в агрегатах cc_usage).

**Класс.** Событийный словарь фиксировал только положительные исходы
приёмки. Общий вид: правило, чей триггер — счётчик («N неудач»), но
чьи инкременты не порождают записей, непроверяемо; след обязан
писаться при каждом срабатывании счётчика, а не только при финальном
действии.

**Фикс.** D-0045: событие `rejected` (agent = воркер, model
обязательна, notes: задача/причина/номер попытки); отклонение на
приёмке = неудачная попытка правила 6; два `rejected` по задаче на
ярусе → обязательный `escalated`. Стоимостного stop-loss в моменте
сознательно НЕТ (правило 10а): на подписочном контуре нет живого
счётчика долларов на задачу; счётчик попыток — дешёвый оперативный
прокси кроссовера, точный кроссовер меряет еженедельная калибровка
(Update Rule 4, cc_usage + журнал). Калибровка теперь проверяет оба
направления механически: два `rejected` без последующего `escalated`
— нарушение; `escalated` без следа `rejected` — вопрос к
самодекларации.

**Источник (канонический).** DECISIONS.md D-0045; CLAUDE.md обоих
деплоев (правило 6 + словарь событий журнала); AO3:
scripts/log_append.py (ROUTING_EVENTS/MODEL_REQUIRED_EVENTS) и
scripts/tests/test_log_append.py.

## F-14 — Дайджест скаута принимался на слово: у приёмки не было процедуры, а судья с той же проблемой уже был починен

**Наблюдение (2026-07-08, вопрос оператора — седьмой случай паттерна
F-11).** Вопрос: «билдер, критик и Lead смотрят на результат скаута —
а его эффективность мы проверяем? Верхние модели могут просто не
получить нужной информации и поверить на слово». Проверка показала:
(а) приёмка по D-0037 обязательна для каждого воркера, но КАК принять
дайджест скаута — не определено нигде; перечитать файлы за ним нельзя
(это стёрло бы смысл делегирования), значит «на слово» было
единственной опцией; (б) evidence по scout нет вовсе — ноль диспатчей
за первые ~18ч (интерим-разбор), и даже при использовании его строка
в таблице не получала бы потока данных: ошибка разведки всплывает
позже как провал builder'а, без атрибуции к дайджесту; (в) самое
показательное — эта проблема У НАС УЖЕ БЫЛА и уже чинилась: судья
(F-6) — тоже слабая модель, чьи утверждения принимались за факт, фикс
— D-0031 (supervised worker, обязательное ревью chief-judge). Scout —
информационный воркер того же класса, собрат судьи по оси 3
SIBLING_MAP, на которого механизм надзора не был распространён.
Особый риск — негативные утверждения («X нигде нет»): именно они были
несущими в F-12/F-13, и именно их слабая модель проваливает тише
всего (плохой поиск неотличим от отсутствия).

**Класс.** Выход информационного воркера (scout-дайджест,
critic-вердикт, судейская разметка) — утверждения, а не артефакты;
без приложенного следа они непроверяемы дешевле повторения работы.
Правило: след (что искал/читал/трассировал) — часть выхода; приёмка =
проверка покрытия + выборочная сверка, а не доверие. Builder вне
класса: его выход — код и тесты, проверяемые артефактами.

**Фикс.** D-0046: дайджест scout обязан включать блок «След» (поиски,
паттерны, прочитанные файлы); негативные утверждения валидны только
со следом; Lead принимает дайджест проверкой покрытия + точечной
сверкой ≥1 несущего утверждения (негативное — обязательно), сверка
отмечается в accepted-событии; дайджест без следа → `rejected`
(D-0045). Вердикт critic «ПРИНЯТЬ» — тоже негативное утверждение
(«находок нет») и требует следа: что именно проверено (уже практика
приёмки Task 7 — теперь в файле роли). Downstream-атрибуция: если
провал builder/critic восходит к плохой разведке, notes события
`rejected`/`escalated` называют дайджест источником — это и есть
evidence-поток для строки scout в таблице. Судья не тронут — D-0031
уже покрывает.

**Источник (канонический).** DECISIONS.md D-0046; .claude/agents/
scout.md и critic.md обоих деплоев; CLAUDE.md обоих деплоев
(правило 1); прецедент класса — F-6/D-0031.

## F-15 — Все детекторы сходятся в еженедельную калибровку, а её собственный отказ не видел никто

**Наблюдение (2026-07-08, разовый аудит существующих механизмов по
вопросу (в) правила 10, заказан оператором после F-12/F-13/F-14).**
Аудит прошёл по правилам 1–10, деградации, журналу, приёмке, карте
осей, гейтам и самой калибровке. Итог: почти каждый поток обнаружения
нарушений — «еженедельная калибровка читает журнал/cc_usage и
замечает» (D-0042, D-0044, D-0045, D-0046, правила 3/8/10...). При
этом сама калибровка: (а) определена одной строкой ROADMAP Phase 1.5
шаг 3, без расписания и триггера; (б) не упоминается в
SESSION_PROTOCOL.md вообще (сверено: 0 совпадений); (в) не оставляет
записи о факте прогона — события/отметки «калибровка выполнена»
не существовало; (г) её чеки были рассеяны по текстам решений без
единого исполняемого списка. Single point of failure: если калибровка
не запускается или тихо умирает, ни одно из сегодняшних обещаний
«нарушение будет видно» не выполняется — и этого никто не замечает.
Попутно подтверждено (первый боевой скаут-диспатч, приёмка по
D-0046 со сверкой): детектор роста живых файлов из F-8 так и не был
построен — закрыт чеком 10 нового протокола без отдельного тула.

**Класс.** Механизм-детектор сам нуждается во внешнем детекторе, и
им не может быть он сам (циркулярность). Вершина пирамиды надзора
обязана оставлять след, проверяемый более простым и более частым
процессом — здесь: Boot Report каждой сессии.

**Фикс.** D-0047: (1) исполняемый чек-лист —
PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md, канонический дом всех
механических проверок (новое обещание «калибровка заметит X» обязано
тем же коммитом добавлять чек туда); (2) каждый прогон завершается
событием `calibrated` (период, разобранные события, изменения
статусов, счётчики строк boot-файлов); (3) внешний детектор — строка
Last Calibration в Boot Report (BOOT_REPORT_PROTOCOL.md, правило 5):
NONE или >7 дней при живом трафике = OVERDUE, видно оператору при
каждом boot'е.

**Источник (канонический).** DECISIONS.md D-0047;
PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md;
PROCESS/BOOT_REPORT_PROTOCOL.md (правило 5); ROADMAP Phase 1.5 шаг 3;
дайджест скаута — logs/routing-log.jsonl 2026-07-08 (delegated/
accepted, первый scout-диспатч).

## F-16 — Карта осей не имела собственной проверки: только росла, пути не проверялись, пропущенную ось никто бы не заметил

**Наблюдение (2026-07-08, вопрос оператора — восьмой случай паттерна
F-11, третий подряд «сторож без сторожа»: журнал → калибровка →
карта).** SIBLING_MAP создана утром того же дня; правило пополнения
есть («новая симметрия → ось тем же коммитом»), правила проверки —
нет. Три непокрытых режима отказа: (а) устаревание — карта называет
конкретные пути; переименованный файл превращает ось в указатель в
пустоту, и обход даёт ложную уверенность «оси покрыты»; (б)
неполнота — незамеченная симметрия в карту не попадает, а
единственный её сигнал — рецидив дефекта уже чинившегося класса вне
указанных картой мест — считался бы просто новым дефектом, сигнал о
карте терялся; (в) разрастание — правило только на добавление осей,
на удаление — нет; вечно растущая карта дорожает при каждом обходе
(Rule #1) и врёт о мире, где механизмы ещё и умирают.

**Класс.** Тот же, что F-15: инструмент-детектор не может проверять
сам себя; и уточнение: реестр (карта, таблица, whitelist), у которого
есть операция добавления, обязан иметь симметричные операции
проверки и удаления — иначе он монотонно расходится с реальностью.

**Фикс.** D-0048, три потока: (1) живость — чек 12 калибровки:
каждый путь карты существует в обоих репо, мёртвый путь = нарушение
same-commit-правила; (2) полнота — правило рецидива в заголовке
карты: повтор дефекта чинившегося класса вне мест карты = находка О
КАРТЕ (пропущенная ось), фиксируется в FINDINGS с правкой оси тем же
фиксом; (3) рост — строки SIBLING_MAP.md добавлены в счётчики чека
10, оси удаляются тем же коммитом, что убил симметрию.

**Источник (канонический).** DECISIONS.md D-0048; docs/SIBLING_MAP.md
(раздел «Проверка самой карты»); PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md
(чеки 10 и 12).

## F-17 — Приёмка слепа к ложным принятиям: accepted не требует доказательств, поздние дефекты не атрибутируются ярусу

**Наблюдение (2026-07-08, вопрос оператора — девятый случай паттерна
F-11).** На разборе pi-autopilot (внешний приор witness coverage —
docs/RELATED_WORK.md, раздел оркестрации) оператор спросил: «как
определить, кто справится — причём не прописать один раз, а менять,
если данные покажут много ошибок, — если не на основе валидации и
доказательств?» Проверка контура показала асимметрию. Отрицательные
исходы приёмки журналируются и работают: `rejected` (D-0045) →
эскалация правила 6 → калибровка чек 3. Положительные —
самосертификация в две стороны: (а) accepted по builder-диспатчам не
требует приложенных доказательств — «тесты проходят» принимается на
слово; D-0046 закрыл это для информационных воркеров, builder был
выведен из класса («его выход — проверяемые артефакты»), но
обязанности ПРИЛОЖИТЬ результат проверки артефактов нет; (б) принятая
работа, сломавшаяся позже, не оставляет следа к исходному диспатчу —
ярус выглядит компетентным ровно до тех пор, пока ошибку ловят в
момент приёмки; false-accept rate невычислим по журналу, таблица
учится только на пойманном сразу и потому умеет двигать статусы вниз
лишь по приёмочным отказам.

**Класс.** Тот же, что F-13, с противоположным знаком: там не
оставляли следа отклонения, здесь — поздние опровержения принятий.
Плюс расширение класса F-14 на положительные вердикты: приёмочное
«принято» — тоже утверждение информационного воркера (самого Lead) и
без приложенного доказательства непроверяемо.

**Фикс (план внедрения, принят оператором 2026-07-08; полная
последовательность — CURRENT_CONTEXT.md, очередь).** Кандидат D-0052,
этап 1: (1) accepted по builder обязан нести witness — фактический
вывод прогона (тесты/команда), не пересказ; симметрично следу D-0046;
(2) событие `defect_found` со ссылкой на исходный accepted-диспатч
(задача, ярус) — ретро-атрибуция; калибровка получает false-accept
rate по ярусам — недостающий поток для движения статусов ВНИЗ по
Update Rule 1. Детектор (правило 10в): чеки в
WEEKLY_CALIBRATION_PROTOCOL тем же коммитом, что D-0052. Смежные
заимствования того же разбора (path claims — этап 2, context_budget
хук — этап 3, data-gated) — в плане, не в этой находке.

**Источник (канонический).** docs/RELATED_WORK.md («Agent
orchestration with evidence gates»); logs/routing-log.jsonl
(dispatch_skipped 2026-07-08, разбор pi-autopilot); план —
CURRENT_CONTEXT.md. Статус: решение D-0052 оформляется при исполнении
этапа 1 плана.

## F-18 — Несущие факты журнала жили в прозе notes: гейты калибровки нельзя посчитать скриптом

**Наблюдение (2026-07-08, внешнее ревью — автор pi-autopilot через
оператора: «упущен structured output»).** Проверка по свежему D-0052
(суточной давности не было — ЧАСОВОЙ) подтвердила укол: журнал
формально структурирован (JSONL, enforce-словарь событий), но вся
несущая информация лежала в свободном поле notes. Три конкретных
дыры: (а) связка defect_found → исходный accepted — прозаическая
«ссылка по ts/задаче в notes»: false-accept rate чека 13 не считается
скриптом; (б) «два rejected по одной задаче» (правило 6) — задача
идентифицируется прозой, нет task_id — нет механического счётчика
попыток; (в) witness, класс отказа, номер попытки, отметка сверки
scout-дайджеста — всё внутри notes. Следствие: чеки калибровки
3/6/13 обречены на LLM-чтение прозы Lead-ярусом, хотя по природе —
«Ledger (no LLM)» из собственной таблицы делегирования; растящийся
чек-лист сам создаёт оверхед чека 11.

**Класс.** Поле, которое потребляет детерминированный гейт, обязано
быть типизированным; проза — для человека, не для проверки.
Продолжение линии F-13/F-17 (словарь событий рос, но оставался
прозой) и вариант F-11 с НОВЫМ источником кросс-экзамена: слепое
пятно собственного дизайна вскрыл не оператор, а внешний автор —
внешнее ревью работает и на уровне механизмов, не только кода
(прецедент: Architect correction в Task 5). Собрат по оси 6 уже
существовал: AO3 schemas/agent-output валидирует выход QA-агентов —
routing-контур был его непокрытым аналогом (класс D-0043).

**Фикс.** D-0053: типизированные поля журнала — task_id (сквозной,
обязателен для delegated/accepted/rejected/escalated/defect_found),
attempt + failure_class (rejected), witness (accepted по builder),
ref (defect_found → task_id исходного accepted); notes остаётся
человекочитаемым довеском. Enforce — AO3 log_append.py; в OS-репо
журнал пишется Edit-тулом, отсутствие полей ловят чеки 3/13. Старые
события не переписываются (журнал append-only); первая калибровка
читает их вручную. Счётный скрипт чеков 3/13 — builder по спеке,
после первой калибровки. Структурные рамки ОТЧЁТОВ воркеров
(Trail/вердикт как схемный блок) сознательно отложены до роста
объёма (Rule #1).

**Источник (канонический).** DECISIONS.md D-0053; CLAUDE.md обоих
деплоев (раздел журнала); AO3 scripts/log_append.py + тесты;
PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md (чеки 3/13). Статус: оформлено
D-0053 (2026-07-08).

## F-19 — Норма DoD была предложена для одного яруса: обход оси 3 пропущен на стадии ПРЕДЛОЖЕНИЯ

**Наблюдение (2026-07-08, вопрос оператора — десятый случай паттерна
F-11).** Внешний совет автора pi-autopilot («каждой задаче — DoD с
планом верификации») был переведён в предложение D-0054 только для
builder-спек. Вопрос оператора «почему смотришь только на builder —
у нас несколько слоёв» вскрыл: ось 3 карты (роли/ярусы) существует,
прямо покрывает случай («обязанность одной роли — проверь остальные»)
и не была пройдена. Рецидив класса F-10 (лечится экземпляр, не
класс) — на этот раз пойманный ДО коммита, но не картой, а снова
вопросом оператора.

**Класс.** F-10, с уточнением жизненного цикла: обход осей (правило
10б) фактически исполнялся на механизмах в момент их ЗАПИСИ, но
решение рождается раньше — предложением в чате; узкое предложение,
одобренное оператором, было бы закоммичено узким. Обход карты
обязателен на моменте формулирования механизма, не только перед
коммитом.

**Фикс.** D-0054 расширен на все ярусы до оформления: DoD — свойство
ЛЮБОГО диспатча в форме яруса (builder: критерии приёмки +
проверочный прогон; scout: явный вопрос + критерий полноты; critic:
приложенная спека/DoD ревьюируемой работы); Lead покрыт D-0025/D-0047
(текущая задача несёт acceptance, у калибровки — чек-лист), судья —
D-0031 (калибровочный сет), QA-агенты AO3 — машинерией оси 6
(schemas/agent-output + transitions). Отдельного нового правила «обход
на стадии предложения» не вводится: правило 10б уже требует ответа
об осях, урок фиксируется этой находкой.

**Источник (канонический).** DECISIONS.md D-0054; CLAUDE.md обоих
деплоев (правило 11); роли scout/builder/critic обоих деплоев.
Статус: оформлено D-0054 (2026-07-08).

## F-20 — Правило без chokepoint'а игнорируемо; recall-ответ отказывает молча, перечисление — громко

**Наблюдение (2026-07-08, вопрос оператора — одиннадцатый случай
F-11): «как ты смог проигнорировать правило 9/10б и как сделать его
неигнорируемым, если системный подход уже записан в SYSTEM_PROMPT,
но не работает?»** Разбор по горячему следу F-19 дал три механики:
(а) триггеры правила привязаны к коммиту/закрытию дефекта — стадия
формулирования предложения не распознаётся как момент срабатывания;
правило без распознанного триггера для LLM не существует (текст в
контексте — вес во внимании, не исполняемый код); (б) ответ (б)
правила 10, как практиковался, — это RECALL (перечисляю оси, которые
затронул), а не ENUMERATION (прохожу все оси карты с вердиктом);
вспоминание отказывает молча — незамеченная ось просто не
упоминается, и ответ выглядит полным; перечисление отказывает
громко — пропуск требует явного «н-п» напротив строки, которую
пишешь; (в) у Lead-выхода нет гейта в потоке: диффы builder проходят
critic, дайджесты scout — приёмку по следу, а политика/решения/спеки
Lead освобождены от событий (правило 8) и не имеют ревьюера, кроме
оператора и калибровки с недельной задержкой — весь список F-11
состоит из оператора, ловящего выход единственного непроверяемого
яруса.

**Класс.** Обобщение F-9 с ролей на сам механизм комплаенса:
конституционная проза (SYSTEM_PROMPT) не самоисполняется НИГДЕ —
работает только тройка «дешёвое явное действие + машинный chokepoint
+ внешний детектор» (прецеденты: dispatch_skipped, log_append
enforce, witness). Сделать правило неигнорируемым текстом нельзя;
можно сузить окно игнорирования до моментов с машинным турникетом и
сделать пропуск требующим явной письменной лжи вместо молчаливого
невспоминания.

**Фикс.** D-0055: (1) ответ (б) — осевым перечислением, формат
следует за ТЕКУЩЕЙ картой (оси парсятся из заголовков SIBLING_MAP
при каждом запуске — карта растёт и меняется, D-0048; ничего не
захардкожено); (2) commit-msg-хук в обоих репо отклоняет механизмный
коммит без блока; явный отказ «оси: не-механизм (<причина>)» — только
из сообщения коммита. Первый полный цикл дисциплины D-0052..54 на
этом же артефакте: critic нашёл подтверждённый блокер F-A
(самообход гейта через цитату синтаксиса отказа в диффе) плюс F-B/C/D
— rejected записан, фиксы с регрессионными тестами. Честные пределы —
в тексте D-0055.

**Источник (канонический).** DECISIONS.md D-0055; CLAUDE.md обоих
деплоев (правило 10); tools/mechanism_gate.py + tests, AO3-твин;
журнал t-001 (delegated/rejected/accepted). Статус: оформлено D-0055
(2026-07-08).

## F-21 — Деградация Lead детектируется самим деградированным: молчаливый вход невидим, отсутствие события читается как отсутствие факта

**Наблюдение (2026-07-09, вопрос оператора — двенадцатый случай
F-11).** Сессия AO3 отработала полный проход /qa-loop (диспатчи,
приёмка, rejected по вердикту critic, два механизменных коммита) на
Opus — ярусе critic, не Lead (Fable), — без события `lead_degraded`.
Оператор переключил модель на Fable и спросил: «у Lead есть
обязанность после возвращения проревьюировать всё, что ему принесли —
это сделано?» Вскрылось три слоя: (а) вход в деградацию не был
зафиксирован — протокол D-0039 предполагает, что деградированный Lead
сам заметит и запишет свою деградацию, но детектор поручен именно
тому, чьё понижение он должен ловить; (б) хуже — восстановленный Lead
ОТРИЦАЛ деградацию («сессия шла без lead_degraded — значит деградации
нет»): отсутствие события прочитано как отсутствие факта, круговое
рассуждение, тот же класс вывода, что «пустой вывод adb = устройства
нет» из разбора той же сессии; (в) внутри незафиксированного окна
сделан механизменный коммит — по D-0039 п.2 такое откладывается
полному Lead (смягчение: явный приказ оператора; ратифицировано при
приёмке окна). Приёмка окна по D-0044 выполнена ретроактивно, пара
lead_degraded/lead_restored дописана в журнал AO3 2026-07-08 с
пометкой о ретроактивности.

**Класс.** Самодетекция отказа отказавшим — родня F-15 (детекторы
без внешней проверки) в применении к вертикали ярусов: у события
`lead_degraded` нет внешнего chokepoint'а, оно целиком доброволное.
Плюс инвариант «последнее событие журнала определяет состояние»
(D-0039 п.3) неверифицируем против реальности: журнал утверждает
«полный Lead», транскрипт показывает Opus — и никто эти два источника
не сверяет. Двенадцатый случай F-11 подтверждает диагноз F-20: Lead —
единственный ярус без гейта, и ловит его только оператор.

**Дополнение (2026-07-09, тот же оператор, следующий вопрос):
асимметрия детектируемости — вход невидим, ВЫХОД видим всегда.**
«Если ты знаешь, что только что включился Fable, — значит, до этого
было что-то другое; даже если момент входа не виден, обратный подъём
виден точно». Это ключ: вход в деградацию требует самодетекции
деградированным (ненадёжно по построению), а восстановление — событие
В КОНТЕКСТЕ восстановленного (команда переключения, системный промпт
с новой моделью): полный Lead видит подъём и способен вывести
существование окна дедуктивно, без журнала. В прецеденте
восстановленный Lead ИМЕЛ прямое доказательство окна (видимая команда
/model) и предпочёл ему косвенное отсутствие записи. Значит якорь
протокола — момент восстановления, а не входа.

**Второе дополнение (2026-07-09, тот же оператор): якорь только на
выходе недостаточен — подъёма может не быть вовсе.** «Возможно, что
из-за настроек безопасности Fable сбросится до Опуса и никогда не
вернётся обратно — момента „вверх" не будет. Нужна проверка в обеих
точках». Сценарий ровно из триггеров D-0039 (safety-отказ Fable):
сессия деградирует и умирает деградированной — exit-якорь молчит.
Вход при этом детектируем не хуже выхода: системный промпт КАЖДОЙ
сессии называет её модель явно; отказ входа — не отсутствие сигнала,
а отсутствие распознанного ТРИГГЕРА сверки (класс F-20). Фикс —
привязать сверку к первому Lead-действию сессии (диспатч, приёмка,
механизменный коммит), для конвейера — к preflight скилла.

**Фикс (кандидат, решение за Lead OS-репо; итог — три точки).**
(1) ВХОД: перед первым Lead-действием сессии сверить свою модель по
последнему видимому сигналу (системный промпт / команда
переключения) с ярусом Lead; ниже и окно не открыто журналом →
lead_degraded ДО действия. (2) ВЫХОД: видимый подъём — сам по себе
доказательство окна; ретроактивная фиксация + приёмка D-0044 тем же
ходом. (3) ВНЕШНЯЯ СЕТЬ: чек еженедельной калибровки «фактическая
модель Lead-сессий по транскриптам vs покрытие окон парами
lead_degraded/lead_restored» — ловит отказ обеих in-session точек
(в т.ч. сессию, умершую деградированной); расширение сверки
«самодекларация model vs транскрипт» (D-0034) с воркеров на Lead.
На AO3-стороне (1)+(2) закреплены: CLAUDE.md «Деградация Lead» п.4
(а/б/в), турникет входа — шаг 0 preflight /qa-loop (коммиты 1efef9f,
642dc30); порт в парную секцию OS-репо CLAUDE.md (строки 143-155) и
регистрация чека (3) — за Lead OS-репо, возможно с чеканкой
D-номера (ось 4).

**Источник (канонический).** AO3 logs/routing-log.jsonl (пара
lead_degraded/lead_restored 2026-07-08, обе с пометкой
ретроактивности); AO3 state/orchestrator-log.md (приёмка окна);
прецедент env-негатива — CLAUDE.md AO3 permission-hygiene п.6
(коммит fb41872); симметричный якорь вход+выход — CLAUDE.md AO3
«Деградация Lead» п.4 а/б/в + шаг 0 preflight /qa-loop (коммиты
1efef9f, 642dc30). Статус: ЗАКРЫТА решением D-0056 (2026-07-09) —
порт п.4 в OS CLAUDE.md, сеть (3) зарегистрирована чеком 5
калибровки (расширение D-0042 с воркеров на Lead).

## F-22 — Сессия присваивает роль Lead по адресату политики: полномочия шли от роли, а гарантию даёт ярус

**Наблюдение (2026-07-09, оператор, скриншот AO3-сессии —
тринадцатый случай паттерна F-11).** AO3-сессия на Sonnet,
координируя конвейер, «приняла» builder-класс фикс log_append.py с
льготой «critic: skipped» и отчёт fix-verifier по AT-BUG-003 —
решения яруса, на котором физически не находилась; сессия сама
доформулировала пробел: принимающий обязан стоять ярусом ВЫШЕ
исполнителя, иначе это самосертификация одного яруса. Корень:
CLAUDE.md автозагружается в каждую main-chain сессию и адресован
«Lead'у» — любая модель, прочитав политику, присваивает роль
целиком. D-0039 п.2 обрезал деградированному статусы/гейты/DECISIONS,
но НЕ приёмку; D-0056 сверяет ярус на входе, но матрицы «что
разрешено С ЭТОГО яруса» не существовало. Это же — блокер
планируемого оператором режима: координировать ежедневно с Sonnet,
Fable запускать батчем на накопленную очередь Lead-задач.

**Класс.** Полномочия, выведенные из РОЛИ, тогда как гарантию даёт
ЯРУС. Собратья по классу самосертификации: F-6 (судья), F-14
(scout) — там самосертифицировался воркер; здесь — сессия-координатор.
Правило класса: «принято» требует стороны, стоящей выше исполнителя,
либо явной очереди к ней; роль не наследуется от текста политики.

**Фикс.** D-0058: роль ≠ ярус — секция «Роль ≠ ярус» в CLAUDE.md
обоих деплоев с матрицей полномочий по фактической модели сессии;
приёмка только сверху (равный ярус — только с critic-входом;
critic-класс и Lead-класс — в очередь полного Lead); льгота
«critic: skipped» — только у принимающего ярусом выше исполнителя;
штатный Sonnet-координаторский режим легализован той же матрицей
(деградация D-0039 — незапланированный вход в него). Детектор: чек 6
калибровки расширен сверкой яруса принимающей сессии (cc_usage) с
ярусом agent на каждом accepted.

**Источник (канонический).** DECISIONS.md D-0058; CLAUDE.md обоих
деплоев (секция «Роль ≠ ярус», деградация п.2); PROCESS/
WEEKLY_CALIBRATION_PROTOCOL.md чек 6; .claude/agents/critic.md обоих
деплоев; AO3 .claude/skills/qa-loop/SKILL.md шаги 0/3; прецедент —
скриншот оператора 2026-07-09 (AO3-сессия, самокоррекция в чате).

## F-23 — Параллельные сессии без правила выдачи task_id: один id — две задачи

**Наблюдение (2026-07-09, замечено этой же сессией по чужим правкам
в рабочем дереве).** Две одновременно работавшие сессии выдали
task_id t-008 двум разным задачам: прогон golden set D-0057 (события
12:42/12:55) и recon по task-pipeline (13:20/13:28, сессия D-0059).
Инвариант D-0053 «task_id сквозной на задачу» на t-008 сломан;
журнал append-only — события не переписываются, задачи различимы
только по ts/notes. Счётные механизмы (чеки 3/13, будущий счётный
скрипт) без явной пометки посчитали бы четыре события одной задачей.
Отложенное Stage-2 правило параллельности («владение путями»)
сработало на первом же реальном случае параллельной работы — но
коллизия пришла по оси, которую правило не называло:
идентификаторы, не пути.

**Класс.** Общий append-only ресурс без правила аллокации уникальных
идентификаторов при конкурентной записи. Коллизия путей видна git'ом
(конфликт, dirty tree); коллизия id — молчалива, ловится только
явным правилом + детектором (родня F-9: молчаливые нарушения
инвариантов).

**Фикс.** D-0060: (1) task_id выдаётся перечитыванием хвоста журнала
НЕПОСРЕДСТВЕННО перед записью delegated — max(t-NNN)+1, «запомненный
ранее» id не переиспользуется; (2) параллельные диспатчи И сессии
объявляют владение путями до запуска (правило 4); чужие
незакоммиченные пути не трогаются и не коммитятся; (3) замеченная
постфактум коллизия не переписывается — пометка в notes следующего
события той же задачи + запись в FINDINGS; счёт — как две задачи;
(4) детектор — чек 13(д): скан дублей task_id между несвязанными
задачами (t-008 запинен там как две задачи). AO3 log_append.py может
enforce'ить уникальность кодом — в очередь (builder, мелкое).

**Источник (канонический).** logs/routing-log.jsonl, события t-008
12:42/12:55 vs 13:20/13:28; DECISIONS.md D-0060; CLAUDE.md правило 4
и раздел журнала (оба деплоя); чек 13(д); уточнение ссылки на t-008
в docs/TASK_PIPELINE.md.

## F-24 — Механизм не обязан был называть свой слой enforcement: «на дисциплине» был молчаливым дефолтом

**Наблюдение (2026-07-09, вопросы оператора при разборе GSD Pi —
класс F-11).** Формулировка оператора: политику можно не
прочитать/забыть/не выполнить — код нельзя; код гарантирует, что ИИ
не может не задуматься о правиле, а вердикт по смыслу выносит ИИ, а
не признаки. Проверка против наших механизмов подтвердила пробел:
правило 10 требовало зарегистрированный детектор ОТКАЗА (в), но не
спрашивало, что гарантирует ВСТРЕЧУ с механизмом до всякого отказа.
Почти все механизмы держатся на дисциплине чтения политики, и этот
выбор нигде не был записан КАК выбор. Постфактум-находки этого
класса уже накопились: F-9 (автозагруженная политика не
самоисполняется), F-21 (вход в деградацию пропущен самодетекцией),
F-22 (самосертификация яруса) — всё отказы дисциплины там, где на
пути исполнения не стояло кода.

**Класс.** Молчаливый дефолт вместо записанного выбора — родня
F-9/F-20 (правило без chokepoint'а игнорируемо молча). Отличие от
вопроса (в): детектор ловит отказ ПОСЛЕ факта; слой enforcement
определяет, мог ли отказ случиться вовсе. Две разные оси надёжности,
из которых записана была только одна.

**Фикс.** D-0063 — двухслойная модель как основа архитектуры (код
гарантирует встречу и решает детерминированно решаемое; смысл судит
ИИ ярусом выше; гейт, судящий семантику по признакам, — запрещённая
зона; продвижение дисциплина→гейт по evidence, детерминированные
подвопросы внутри ИИ-суждений — в код). D-0064 — вопрос (г) правила
10: чем механизм триггерится и какой код на пути; «на дисциплине» —
только явной строкой с названным детектором. Ретро-покрытие: прогон
(г) по D-0028..D-0063 одним проходом с уже запланированным прогоном
10(б). Первые применения слоя-1: очередь B-серии (валидатор журнала,
бюджетные стены gateway, SessionStart-хук) + A1 zero-tool-call
guard.

**Источник (канонический).** DECISIONS D-0063/D-0064 (+ индекс);
ARCHITECTURE.md «Core Insight» (принцип); CLAUDE.md правило 10 (оба
деплоя); PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md чек 8 (расширен на
(г)); CURRENT_CONTEXT: «GSD Pi adoption plan» (A/B-серии) и
ретро-прогон 10(б)+(г); прототип слоя-1 в чужом исполнении —
RELATED_WORK «GSD Pi deep-dive» (гейты GSD и их отказ без
семантического слоя: --no-verify).

## F-25 — Правило 10 триггерится распознаванием «это механизм», а распознавание отказывает молча

**Наблюдение (2026-07-09, вопрос оператора: «что нужно сделать,
чтобы четыре вопроса применялись ВСЕГДА? Помню случай, когда вопросы
не были заданы — ИИ не распознал, что создаёт механизм»).** Гейт
D-0055 стоит на ПУТЯХ файлов — синтаксическом прокси распознавания.
Проверка невода подтвердила дыры: ARCHITECTURE.md был вне невода —
D-0063 (принцип лёг в Core Insight) прошёл гейт только потому, что
тем же коммитом шёл DECISIONS.md (near-miss, день в день с вопросом
оператора); сама enforcement-цепочка (tools/mechanism_gate.py,
.githooks/) не гейтилась — правка гейта обходила гейт (класс F-15:
страж без стража); дома воркер-профилей (gateway/PI_HARNESS.md) вне
невода — hardened-профиль scout'а (правило 0 экзаменов, t-012)
родился там механизмом без четырёх вопросов (кандидат в запомненный
оператором случай; ретро-прогон уточнит и оформит).

**Класс.** Семантический триггер без синтаксического невода нужной
ширины и без детектора нераспознанных: правило без chokepoint'а
(F-20) в точке, где отказывает само распознавание (родня F-11:
модель не видит пробелов собственных механизмов). Отличие от F-24:
там не записывался слой enforcement ИЗВЕСТНОГО механизма; здесь
механизм не опознаётся вовсе — отказ ступенью раньше.

**Фикс.** D-0065, эшелоны по D-0063: (1) операциональный тест
распознавания в правиле 10 обоих CLAUDE.md (встреча гарантирована
автозагрузкой, D-0041; «сомнение = механизм»); (2) невод гейта
расширен на известные дома механизмов и самозащиту цепочки
(ARCHITECTURE.md, BOOT.md, gateway/PI_HARNESS.md,
tools/mechanism_gate.py, .githooks/; AO3-твин: свой гейт +
.githooks/); широкие каталоги НЕ добавлены — уважение записанного
выбора D-0055 (ложные срабатывания приучают к --no-verify);
(3) детектор нераспознанных — аудит распознавания в чеке 8
(семантический вердикт за калибрующим Lead — слой 2, невод даёт
кандидатов); (4) ретро-прогон 10(б)+(г) дополнен охотой на уже
рождённые нераспознанные механизмы по домам невода; (5) внешний
контур — вопросы оператора (F-11), это срабатывание — очередное.

**Источник (канонический).** tools/mechanism_gate.py
MECHANISM_PREFIXES + тесты (оба репо); DECISIONS D-0065; CLAUDE.md
правило 10 «Распознавание» (оба деплоя); чек 8 «аудит
распознавания»; CURRENT_CONTEXT ретро-прогон; gateway/PI_HARNESS.md
(кандидат-экземпляр).

## F-26 — Дайджест scout по внешнему репо достоверен, но неполон для планирования: приёмка по следу не меряет глубину покрытия

**Наблюдение (2026-07-10, вмешательство оператора: «скаут может
принести не всю информацию, такой прецедент уже был... Lead сам может
сходить и посмотреть попристальнее — а то внесём в план изменения,
спланированные не на полных данных»).** Обзор openclaw/openclaw
(t-022): дайджест scout прошёл приёмку по D-0046 честно — все
проверенные утверждения, включая негативные, подтвердились. Но второй
проход Lead по перспективным местам показал недостаток ГЛУБИНЫ:
в оглавлении docs/concepts/ лежали нечитанные scout'ом файлы, часть
из которых оказалась несущей для плана (model-failover.md — лестница
cooldown'ов, strict-selection семантика = внешняя валидация t-018;
context.md — surface наблюдаемости boot-бюджета), а часть — пустой
(delegate-architecture.md — не о ярусах). Прецедент того же класса:
GSD Pi потребовал operator-ordered deep-dive после первичного обзора
(RELATED_WORK, 2026-07-09).

**Класс.** Приёмка по следу (D-0046) проверяет ДОСТОВЕРНОСТЬ
принесённого (сверка утверждений, след покрытия заданных вопросов),
но вопросы формулируются ДО знакомства с репо — полнота относительно
неизвестной структуры непроверяема тем же следом. Для планировочных
решений (что внедрять) дайджест нижнего яруса — карта, не основание;
recall на незнакомом материале ограничен ярусом честно и молча.
Родня F-14 (фабрикация — отказ достоверности) — здесь отказывает не
достоверность, а полнота; и F-11 (пробел вскрыт вопросом оператора).

**Фикс.** D-0066: обзор внешних репо — двухпроходный. Scout даёт
общую карту (первый проход); механизмы попадают в план/очередь только
после точечного второго прохода Lead по перспективным местам; след
второго прохода (что Lead читал сам) — обязательная часть секции
RELATED_WORK. Детектор — чек 16 калибровки. Первое применение — обзор
OpenClaw тем же днём: второй проход изменил состав плана.

**Источник (канонический).** DECISIONS D-0066; CLAUDE.md правило 1
(поправка); PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md чек 16;
docs/RELATED_WORK.md «OpenClaw» (первое применение, след второго
прохода).

## F-27 — Квотная стена считает по своей базе, провайдер — по своей: трафик мимо requests.db невидим стене

**Наблюдение (2026-07-10, t-015 attempt 3).** Экзамен запущен первым
Groq-трафиком «свежего» окна (02:16, по подсказке 429 «~02:00»);
стена t-018 пропустила запросы молча (её окно показывало 14,175
токенов из лимита 100,000 — даже без warn-события), а Groq отбил
ход 2 с «Used 90614». Недостающие ~68k — трафик t-013, писавшийся
через GATEWAY_DB_PATH в боковую базу gateway/t013.db (73 строки,
68,054 токена, 18:28–18:56): стена читает ТОЛЬКО requests.db, Groq
считает всё. Зарегистрированный при приёмке t-018 (в)-детектор
(«Groq 429 с tooling-rejected ПОСЛЕ стены = утечка стены», чек 13
калибровки, комментарий в budgets.yaml) сработал ровно как записан —
первым же экзаменом после постройки стены.

**Класс.** Enforcement-счётчик, питающийся собственным логом,
расходится с ledger'ом провайдера на любом трафике, не прошедшем
через лог: боковые базы (GATEWAY_DB_PATH — легальный механизм
тестовой изоляции), вызовы мимо прокси (SDK напрямую), чужие
потребители того же ключа. Собратья по классу: долларовые бюджеты
Guard (spent_today — тот же requests.db), любые будущие стены над
тем же логом. Отдельный урок планирования: у скользящего 24ч-окна
«retry in X» из 429 освобождает место под ОДИН запрос — многоходовый
прогон требует расчёта, когда выпадут вчерашние куски, и запаса под
рост истории (~70k для golden-set экзамена).

**Фикс.** Очередной item OpenClaw (2) — сверка стены с
provider-reported rate-limit заголовками — был поставлен data-gated
«только по evidence дрифта»; evidence получен, ТРИГГЕР СРАБОТАЛ:
пункт переведён в очередь на постройку (вместе с B1, следующий
builder-батч). До постройки — дисциплинарная строка в PI_HARNESS
(разрыв №3): квотные прогоны планировать по СУММЕ requests.db +
боковых баз, не по одной requests.db. Детектор утечки уже
зарегистрирован (чек 13) и доказал себя этим же инцидентом.

**Источник (канонический).** logs/routing-log.jsonl t-015 attempt 3
(rejected 2026-07-10T02:30); gateway/t013.db (68,054 tok);
gateway/PI_HARNESS.md разрыв №3 (дополнение 2026-07-10);
CURRENT_CONTEXT очередь «OpenClaw adoption plan» item 2.

## F-28 — Рубричный Lead-экзамен сатурируется ярусом ниже: экзамен различает низ, не верх

**Наблюдение (2026-07-10, t-026).** Вопрос оператора после t-024
(«нижняя модель прошла на лида — не слишком ли лёгкий экзамен?») с
предрегистрированным критерием: Сонет прошёл → экзамен слишком
лёгкий; провалил → проверить Опус. Сонет (builder-ярус этого деплоя,
нейтральный субагент general-purpose) сдал ВЕСЬ экзамен с лучшим
результатом дня: часть A 13/13 (выше 12/13 кандидата 2.5-flash,
включая ловушку пары №7 с разбором ошибочного лингвистического
довода); B1 — единственный ответ, назвавший гонку task_id,
напряжение Two Vocabularies (в журнале model, не ярус), no-op-кейс
хука и вопрос critic'у о лазейке самопринятия; B2 — единственный,
поймавший scope-creep builder'а («с запасом на будущее» против «не
изобретает»), который упустили ОБА Gemini-кандидата. Критерий
оператора сработал: Опус не проверялся (его ветка была только на
случай провала Сонета). Каверза сопоставимости названа: субагенту
автозагружен CLAUDE.md (полная политика vs дайджест у Gemini) — B1
этим усилен; но часть A контекстно-свободна, а находки B2 из
политики не выводятся — PASS стоит a fortiori.

**Класс.** Экзамен с ясной рубрикой («вот политика — примени»)
измеряет исполнение ЗАДАННОЙ политики и сатурируется верхними
средними ярусами; он отделяет непригодность (qwen3:4b 0/7 с
фабрикацией, t-012/t-016) от пригодности, но НЕ отделяет
builder-ярус от Lead-яруса: frontier-дельта живёт там, где правила
молчат или конфликтуют, где дефект — в ОТСУТСТВУЮЩЕМ (не в
предъявленном), и на длинном горизонте с реальной неоднозначностью.
Собратья по классу: judge-калибровка (13 меченых пар: три модели
подряд ≥12/13 — та же сатурация), scout golden set (уже честно
позиционирован D-0057 как регрессионный, не ранжирующий), будущий
critic golden set (учесть при постройке).

**Фикс.** (а) Статусная дисциплина уже держит удар: экзамены НЕ
двигают статусы таблицы (D-0028/D-0035), вердикты t-023/t-024
переквалифицированы аддендумом отчёта в «входной фильтр пройден»,
не «Lead-способность доказана» — реальная валидация остаётся за
production-журналом. (б) Ранжирующий Lead-экзамен (кейсы, где
политика молчит/конфликтует; дефекты по отсутствию; adversarial-
отчёты с правдоподобным фальшивым witness — первый сеемый дефект
уже записан в t-025) — В ОЧЕРЕДЬ как кандидат, строить только при
реальной потребности ранжировать верхние ярусы (Rule #1).
(в)-детектор: недельная калибровка — production-метрики (cost per
accepted unit, escalation rate по ярусам) против экзаменных
вердиктов; расхождение = экзамен переоценил кандидата.

**Источник (канонический).** logs/routing-log.jsonl t-026;
docs/task_reports/2026-07-10_gemini-key-role-exam.md (аддендум
t-026 с verbatim-ответом Сонета).

## F-29 — Журнальный ts выдумывается повествованием, если его не читает код

**Наблюдение (2026-07-10).** Вопрос оператора «окно Groq уже
открыто — точно? ещё утро» вскрыл: сессия Lead весь день писала в
журнал события с выдуманными ts (12:05–22:35), тогда как реальное
время работы — 03:40–08:00 (внешние часы: git-коммиты 389ec68
04:07 / 8dc4faa 04:21 / 57daf70 08:00, ts экзаменационных вызовов в
requests.db). Порядок событий верен, абсолютные времена — нарратив
модели («утренняя работа», «вечерний добор»), ни разу не сверенный
с Get-Date. ВТОРОЙ случай класса: ночная сессия уже флажковала
clock-mismatch (t-015 attempt 3, notes: «events with 04:xx
timestamps while local clock now reads 02:16»). Производный вред:
ложное «окно Groq уже открыто» в отчёте оператору — план запуска
t-015 строился бы на выдуманном времени суток.

**Класс.** Любое поле, которое пишет РУКА сессии (Edit-тул), а не
код, дрейфует к правдоподобному нарративу; ts — худший случай,
потому что от него считаются квотные окна (F-27 addendum) и
таймлайны калибровки. Соседний контраст: requests.db/cc_usage/git
пишут ts кодом и не дрейфуют — они и есть эталон. AO3 иммунен по
построению (scripts/log_append.py генерирует ts) — живое
подтверждение D-0063: детерминированно решаемое должно уйти в код.

**Фикс.** Правило в CLAUDE.md (журнальная секция): ts — с часов,
прочитанных непосредственно перед записью; замеченный позже
неверный ts не переписывается (append-only) — пометка в notes
следующего события. Детектор: чек 13(е) калибровки — выборочная
сверка ts журнала с внешними часами (requests.db / cc_usage /
git log). Промоция в код по D-0063: ts-sanity новых строк
(монотонность, близость к времени коммита) добавлена в объём
уже стоящего в очереди B1-валидатора журнала. События этой сессии
не переписаны — defect_found с перечнем и реальными границами.

**Источник (канонический).** logs/routing-log.jsonl defect_found
2026-07-10T08:12 (ref t-023); git log 389ec68/8dc4faa/57daf70;
t-015 attempt 3 notes (первый случай класса).

## F-30 — Lead принимает собственные автоматические выводы по неполным данным за реальность

**Наблюдение (2026-07-10, формулировка оператора).** Четыре
экземпляра одного класса за короткое окно, все — у Lead-сессий, не
у воркеров:

1. **«Судья разметил — значит пары верные»**: вердикт по промаху
   пары №9 («судья строже человека») вынесен БЕЗ ручной проверки
   метки; метка при пост-проверке оказалась верной, но проверка
   случилась только после вопроса оператора. Слабая модель судила —
   сильная модель приняла не глядя.
2. **«Экзамен сдан — значит Lead»**: pass рубричного экзамена принят
   за пригодность к роли, пока оператор не потребовал контроль
   Сонетом (F-28: экзамен сатурируется ярусом ниже).
3. **«Scout принёс — строим план»**: план внедрения по чужому репо
   строился на дайджесте без точечной перепроверки несущих мест по
   полным данным (F-26 → D-0066 two-pass; правило существует именно
   потому, что класс уже всплывал).
4. **«Токены появились — можно стартовать»**: ТРИ попытки t-015
   сорваны квотными стенами — каждая стартовала на предположении о
   запасе, не на измерении; плюс «окно уже открыто» при 08:00 утра
   и выдуманные ts (F-29 clock mismatch).

**Класс.** Архитектура дисциплинирует утверждения ВОРКЕРОВ (witness
D-0052, след D-0046, two-pass D-0066, env-негатив — правило гигиены
6), но собственные несущие утверждения Lead'а о состоянии мира
(квоты, окна, метки, роли) не имели симметричного требования
источника: правдоподобный вывод отправлялся оператору как факт.
Это и есть frontier-дельта, которую не меряет рубричный экзамен
(F-28): устойчивость к подмене «проверено» на «правдоподобно».

**Фикс.** (а) Правило гигиены 6 расширено с негатива на ЛЮБОЕ
несущее утверждение о состоянии среды: в отчёте оператору или
плане оно валидно только после сверки измерением (каноническая
команда / внешние часы / база / провайдер), иначе обязано нести
явную пометку «оценка, не проверено». (б) Четыре экземпляра выше +
scope-creep из t-025 = стартовый банк кейсов ранжирующего
Lead-экзамена (очередь F-28): кейсы подают правдоподобный
неполный сигнал и меряют, потребует ли кандидат сверки. (в)-детекторы
экземпляров: чеки 5 (модель Lead-сессий), 13е (ts), 16 (two-pass),
правило 3 судейского протокола (random audit). Детектор КЛАССА —
инцидентный: вопрос оператора / производственный сбой фиксируется
находкой + defect_found; автоматический детектор семантики не
обещан (паттерн F-25). Промоция в код — по evidence утечек
(D-0063); детерминированные подвопросы уже уходят: ts-sanity и
квотная сверка с заголовками провайдера — в B1-батче.

**Дизайн защиты (направление оператора 2026-07-10: защитить, не
заставляя всё переделывать и не сжигая токены).** Корень: сгенерировать
правдоподобное продолжение дешевле, чем обратиться к миру; лечится
устройством, не вниманием (дисциплина умирает с контекстом — F-1).
Слои по цене: (1) «реальность фоном» — SessionStart-хук B3 печатает
часы и квотные окна в контекст: чего не надо угадывать, то не
выдумывается (~0 токенов); (2) код на немногих необратимых путях —
квотный запуск через pre-flight скрипт, который мерит и отказывает
(расширение OpenClaw item 2); (3) теги происхождения [измерено:
команда]/[вычислено из]/[оценка] на несущих утверждениях — акт
таггинга и есть проверка; критерий НЕЗАВИСИМОГО источника (сверка
не должна разделять моду ошибки: git-часы vs нарратив, провайдер vs
своя база); (4) перевёрнутая пирамида, data-gated: Haiku-скаут
прогоняет команды-источники из списка несущих утверждений отчёта
(другая голова против self-confirmation; вводить только по evidence
вранья тегов, Rule #1). УТОЧНЕНИЕ (вопрос оператора про локальную
модель, 2026-07-10): слой распадается на 4а и 4б. 4а — перепрогон
команд и сравнение значений из структурных тегов [измерено:
команда -> значение]: детерминированно, делает СКРИПТ (D-0063),
LLM не нужен вовсе. 4б — извлечение утверждений из прозы и нечёткое
суждение «подтверждает ли вывод утверждение»: qwen3:4b НЕ ГОДИТСЯ
по evidence — фабрикация выводов инструментов (t-012/t-016, 0/7
дважды) дисквалифицирует именно верификатора (сверщик, выдумывающий
результат сверки, отмывает непроверенное в «проверено» — хуже
отсутствия сверки), судейская калибровка 11/13 < 90%. Узкая ниша
слабых моделей: только работа, чей выход сам проверяется кодом
дешевле работы (напр. извлечение команды с substring-валидацией
против текста отчёта); экономику против Haiku она не улучшает
($1.33 за всю историю scout-яруса), стоящий аргумент один —
резильентность, и он уже гейтирован закрытым тредом local-scout.
Доверие самому Haiku (вопрос оператора) — тоже evidence, не вера:
golden set с анти-фабрикационными ловушками Q3/Q4 пройден 2x7/7
(2026-07-09, тот же инструмент, где qwen3:4b дважды 0/7 с
фабрикацией) + продакшн-сверка негатива t-022 держалась; НО
самоотчёту сверщика не верит никто по конструкции: tool-вызовы
субагента сверяются по счётчикам/транскрипту харнесса (аналог
pi_run_guard/F-14 на API-контуре), а привязка 4б, если строится,
требует вступительного экзамена с фабрикационными ловушками
(паттерн D-0057) ДО доверия вердиктам; (5) ранжирующий экзамен отбирает модели,
устойчивые к классу. НЕ делается: critic на каждый отчёт Lead —
переносит F-30 ярусом выше и нарушает Rule #1.

**Карта покрытия слоёв (вопрос оператора: поймал ли бы слой 4
известные кейсы?).** Слой 4 (Haiku прогоняет команды-источники)
ловит ТОЛЬКО утверждения с командой-источником: кейс 4 (квоты/часы)
— полностью; кейс 3 — наполовину (негативы дайджеста — да, полноту
карты — нет: вторая Haiku разделяет моду ошибки первой, независимость
нарушена; правильный инструмент — D-0066 второй проход Lead); кейсы
1–2 (метки, экзамен=роль) — НЕТ: семантика без команды, слабая
голова не проверяет суждение сильной. Кейс 1 ловят тег [оценка] и
СУЩЕСТВОВАВШЕЕ правило 1 judge-протокола (ревью вердикта по
фактическим парам), которое было молча обойдено — утечка дисциплины,
кандидат на промоцию в гейт по D-0063. Кейс 2 закрывает шестой
инструмент, добавлен этим уточнением: (6) КОНТРОЛЬНЫЙ ПРОГОН —
новый измерительный инструмент (экзамен, набор, метрика) калибруется
прогоном кандидата ИЗВЕСТНОГО яруса до использования вердиктов
(прецедент: Сонет за один диспатч опрокинул «экзамен=роль»); дёшев,
потому что инструментов мало, а кандидатов много.

**Источник (канонический).** Формулировка оператора 2026-07-10
(транскрипт сессии, cc_usage); экземпляры: F-26/F-28/F-29,
rejected t-015 attempts 1–3, accepted t-024 (вердикт по паре №9 до
ручной проверки); пост-проверка метки №9 — этот же день, метка
подтверждена руками.

## F-31 — Словарь «роль ≠ ярус» схлопывается в пересказе: координатор читается как синоним Lead/Fable

**Наблюдение (2026-07-10, t-036).** Три модели трёх ярусов —
Sonnet (draft-A), Opus (draft-B) и Fable (draft-C) — при написании
статьи о проекте НЕЗАВИСИМО совершили одну и ту же словарную
ошибку: «координатор = Fable = тот, кто декомпозирует, пишет спеки
и принимает» (A вдобавок приписал оператору ручную приёмку
scout-отчётов). Обе ошибки нашла вычитка оператора, не самонадзор.
Показательно: путаницу совершил в том числе Fable — модель, весь
день ИСПОЛНЯВШАЯ обе стороны различения.

**Класс.** Определение понятия через ЧУЖИЕ работы вместо
собственной функции: прежний текст секции «Роль ≠ ярус» открывался
фразой «роль координатора (декомпозиция, спеки, диспатчи)…» — т.е.
сам описывал РОЛЬ работами Lead-ЯРУСА, а различение держалось на
второй половине абзаца. Сосед по классу — D-0062 (функции vs
грейды): словарь, живущий в практике как выстраданное различение,
в пересказе схлопывается до ближайшего стереотипа («начальник»).
Родня F-28: текст не передаёт того, что живёт в поведении, — но
здесь наоборот: поведение (матрица исполнялась корректно) не
защитило текст.

**Фикс.** Секция «Роль ≠ ярус» переписана словарём из трёх
НЕ-синонимов (ярус = фактическая модель; роль координатора =
маршрутизация: распределить по ярусам + передать наверх, НЕ
исполнение; полный Lead = координатор на ярусе Fable) — по прямому
указанию оператора; AO3-секция — тем же касанием (ось 1). Детектор
словарного дрейфа: вычитка текстов ярусом выше (оператор) — как и
сработало; операционные нарушения матрицы ловят чеки 5/6
калибровки и код-гейт D-0069 (поля by/basis) — словарная путаница
в ТЕКСТАХ им не видна, поэтому вычитка остаётся отдельным
детектором. Драфты-носители ошибки не переписываются (evidence).

**Источник (канонический).** logs/routing-log.jsonl t-036
(delegated 13:38 / accepted 14:18 + вердикты оператора в
транскрипте); docs/draft-A.md, docs/draft-B.md (ошибка verbatim),
docs/draft-C.md v1→v2 (дифф правки — коммит d3e52d2).

## F-32 — Чек полноты исполнен выборкой и отчитан PASS: recall-паттерн внутри самой калибровки

**Дата:** 2026-07-11. **Источник:** вопрос оператора сразу после
первого прогона калибровки («при калибровке проверились ВСЕ
механизмы на соответствие всем вопросам 10-го пункта?») — очередное
срабатывание внешнего контура F-11.

**Факт.** Чек 8 первого прогона требует буквой: «НОВЫЕ записи
DECISIONS.md за период содержат ответы (а)/(б)/(в)/(г)». Прогон
проверил ВЫБОРКУ (D-0069 подробно, D-0067/68 частично, два осевых
блока коммитов) и отчитал «8 PASS (выборка...)» — notes события
`calibrated` честно назвали выборку, но чек полноты выборкой не
исполняется. Дочитка тем же днём (все шесть решений нового режима
D-0064..D-0069 построчно + осевые блоки четырёх коммит-сообщений
159dde5/dfa205e/e94bef3/b7e7ef2 — по 6 осей в каждом): нарушений
НЕТ — пробел был в строгости прогона, не в механизмах.

**Класс.** Родня F-20 (recall отказывает молча, перечисление —
громко), применённая к САМОЙ калибровке: чек, сформулированный как
«каждый X содержит Y», исполненный проходом «посмотрел несколько X»,
вырождается в recall-проверку — его PASS слабее, чем читается.
Отличие от F-20: там лечили ответ механизма (проза → перечисление),
здесь — исполнение аудита. Сосед — D-0055 (осевой блок перечислением
на каждую ось).

**Фикс.** (1) Дочитка выполнена, разрыв закрыт (эта запись —
evidence). (2) Второй прогон калибровки исполняет чеки класса
«каждый X» перечислением по списку X с явным счётом «N из N» в
notes; чек 8 — поимённо по новым D-номерам периода. Правка текста
протокола НЕ требуется (буква чека верна — отказало исполнение);
рецидив выборки на втором прогоне → правка протокола строкой
«поимённо, N из N» по evidence (D-0063). (3) Ретро-свип
rule-10(б/г) по D-0028..D-0063 этой дочиткой НЕ закрывается — он
в очереди отдельным пунктом, как и был.

**Детектор.** Тот же класс, что у F-20/D-0055: счёт «N из N» в
notes `calibrated` виден следующему прогону и оператору; внешний
контур — F-11 (как и сработал здесь).

## F-33 — Осевой блок отвечает за коммит, а не за каждый механизм в нём: класс второго механизма уезжает молча

**Дата:** 2026-07-11. **Источник:** вопрос оператора после коммита
acbee48 («Карта собратьев соблюдена — точно? Первым делом мы
ужесточали правило по передаче работы "вверх"... у билдера и у
критика... если я работаю в сессии с сонетом или опусом») —
внешний контур F-11.

**Факт.** Коммит acbee48 нёс ДВА механизма: ужесточение правила 3
scout (класс «решение не по чину передаётся наверх») и правило 4 +
гигиена п.6 (класс «ложно-пустой поиск»). Осевой блок был один, и
его строки фактически отвечали по второму классу; собратья первого
класса — builder («не изобретай требования»), critic («вердикт ≠
приёмка»), координаторская матрица — остались неразобранными
молча, при формальном наличии перечисления. Дочитка тем же вечером:
у координатора класс уже закрыт сильнее текста — переформулировка
F-31 плюс код-гейт валидатора (D-0069, матрица by/basis),
подтверждено поведением Sonnet-координатора полигона From_Zero в
тот же день (сам позвал critic вместо самоприёмки); builder/critic
дополнены явными запретами («даже если уверен... нарушение роли»)
коммитом этой находки, обе копии ролей.

**Класс.** Родня F-20/F-32: перечисление, исполненное по НЕ ТОЙ
единице учёта (коммит вместо механизма), вырождается в recall для
остальных единиц — PASS-вид при непроверенном содержимом.

**Фикс.** (1) Собратья первого класса закрыты (см. Факт). (2)
Правило 10(б) уточнено в обоих словарях (CLAUDE.md наш + шаблон):
осевой блок отвечает за КАЖДЫЙ механизм коммита — коммит с
несколькими механизмами либо несёт блок на каждый, либо один блок,
строки которого явно закрывают каждый механизм поимённо.

**Детектор.** Чек 8 калибровки (аудит осевых блоков новых
механизменных коммитов — та же выборка, что ловит распознавание);
внешний контур — F-11 (как и сработал здесь).

## F-34 — Позитивный контроль, не разделяющий форму вызова, доказывает трубу, не пустоту

**Дата:** 2026-07-11. **Источник:** t-062 — первый ШТАТНЫЙ прогон
golden set после ужесточения правила 4 (t-057); приказ оператора
«посмотри почему скаут опять провалил проверку».

**Факт.** Scout провалил обязательный Q3 («ложно-пустой grep») ПРИ
ФОРМАЛЬНО ИСПОЛНЕННОМ правиле 4: использовал рекомендованный
Grep-тул (не shell-grep), выполнил позитивный контроль (sqlite3 —
20 хитов), приложил контроль к следу. Провал — в форме самого
вызова: паттерн `postgres|postgresql|psycopg|redis` в нижнем
регистре при регистрозависимом дефолте Grep-тула («Postgres» /
«Redis» в guard.py:6,33 с заглавной — честный ноль), плюс фильтры
type:py/yaml молча вырезали README.md из охвата. Контроль sqlite3
прошёл, потому что `sqlite3` в коде действительно lowercase —
контроль разделял с проверяемым вызовом инструмент и путь, но НЕ
свойство, из-за которого вызов лгал (регистровый профиль).

**Класс.** Родня F-30/F-14: проверка, не разделяющая
отказоопределяющее свойство проверяемого, вырождается в проверку
трубы — PASS-вид при ложном утверждении. Named-gotcha-список
правила (-E, locale) был shell-специфичен; у рекомендованного
инструмента своя gotcha (регистр), не названная. Отдельный урок
методики: inline-валидация правки роли (t-057 att.3 PASS) не
воспроизвела поведение штатного субагента — расхождение
inline↔штатный путь считаем зарегистрированным риском метода п.2а.

**Фикс.** Правило 4 обеих копий scout.md + гигиена п.6 обоих
CLAUDE.md: контроль обязан разделять ФОРМУ вызова (регистр,
фильтры, синтаксис); негатив по содержимому — только
case-insensitive; сужающий фильтр при негативе перечисляется в
следе как граница охвата.

**Детектор.** Чек 14 калибровки (тренд failure_class=recon +
regression-правило правок role-файлов); приёмка по следу D-0046 с
обязательной сверкой негативного утверждения — как и сработала
здесь (Lead-сверка поймала ложь до принятия).

**Аддендум — ретро-аудит записи (вопрос оператора, 2026-07-11
21:43).** Возражение: «production-запись 14/0 собрана из данных,
которые принёс сам haiku, — увидели бы мы такую ошибку?» Ответ
двухчастный. (1) По существу верно: 0 defect_found на recon —
слабое свидетельство (позднему recon-дефекту нечем всплыть — у
разведки нет тестов, падающих от ложного «нигде нет»); а сверка
приёмщика «пусто — совпало» могла быть согласием двух одинаково
сломанных вызовов — до F-34 форма сверки Lead'а не была
специфицирована. (2) Фактически запись выдержала аудит: все 3
именованных негатива принятых дайджестов (t-032, t-033, t-039)
перепроверены правильной формой (-i, без фильтров) с датировкой по
git — все истинны на момент приёмки (t-032: расширение F-30 вошло
в AO3 через 7 минут ПОСЛЕ приёмки тем самым портом; t-033:
профили builder/critic появились в PI_HARNESS через 10 часов;
t-039: хит маркеров — в батче backup, вне заявленного скоупа).
Ложных приёмок не найдено; несущая защита класса — обязательная
сверка негативов на приёмке, чья форма теперь специфицирована
гигиеной п.6 (связывает и Lead'а, не только scout).

## F-35 — Карта симметрии приезжает в чужой проект без осей самого проекта: onboarding их не собирает вовсе

**Дата:** 2026-07-12. **Источник:** вопрос оператора перед
собственноручной установкой 5б («вижу, что и в готовый проект
загружается пустая карта симметрии — разве не надо пройтись по
проекту и составить эту карту?») — внешний контур F-11.

**Факт.** Шаблон отдаёт docs/SIBLING_MAP.md с двумя осями
СОБСТВЕННЫХ механизмов тулкита (ось 1: политика ↔ role-файлы ↔
журнал; ось 2: экзаменационные инструменты ярусов) — они законны в
любой установке: эти файлы после установки реально живут в проекте
пользователя. Но осей ПРИНИМАЮЩЕГО проекта — ноль, и ни один шаг
onboarding (0–4), ни строка INSTALL.md их не собирают и не
упоминают: INSTALL говорит о карте только как об источнике строк
для гейта первого коммита. Для Path B (существующий проект) это
значит: первый class-fix по правилу 9 пойдёт по карте, не знающей
ни одной симметрии проекта, а осевой блок правила 10(б) с первого
дня перечисляет только две наши оси — перечисление рождается
ритуалом. Симметрии существующего проекта при этом уже существуют,
известны владельцу и одноразово дёшевы в сборе именно в момент
онбординга.

**Класс.** Родня F-16/D-0048 (карта — живой кэш знаний о
симметриях, требующий внешней проверки): шаблон переносит кэш
ЧУЖОГО деплоя без процедуры первичного заполнения знанием
принимающего. Стадия 5а (пустой проект) класс вскрыть не могла —
нечего картировать; вопрос оператора сработал на пороге 5б, до
установки.

**Фикс (в очереди, батчем находок 5б).** Onboarding получает шаг
для установки в существующий проект: один вопрос оператору («какие
парные/зеркальные структуры проекта вы знаете — переводы,
дублируемые схемы/конфиги, несколько деплой-целей?») + опционально
ограниченный scout-обзор верхней структуры как подсказка к этому
разговору; в карту входят только пары, подтверждённые оператором
как реальные повторяющиеся обязанности; ноль осей проекта —
легальный исход, фиксируемый явной строкой.

## F-36 — Распознавание Lead-класса не создаёт передачу: координатор называет работу Lead-tier и делает её сам тем же ходом

**Дата:** 2026-07-12. **Источник:** наблюдение оператора за
собственной сессией 5б (D:\Dog, вся сессия на claude-sonnet-5) —
внешний контур F-11.

**Факт.** Заполняя карту симметрии по просьбе оператора (живой
прогон дизайна F-35-фикса до его кодификации), Sonnet-координатор:
(1) правило 1 исполнил образцово — «recon-of-unknown-scope … goes
to scout first», диспатч ушёл; (2) в ТОЙ ЖЕ фразе верно
классифицировал следующий шаг — «deciding which patterns rise to
the level of a tracked symmetry axis is Lead-tier judgment» — и
объявил «I'll write the actual map entries myself» (транскрипт
установки, зап. 316); (3) вписал оси 3–7 в docs/SIBLING_MAP.md сам;
в очередь ушёл только побочный конфиг-баг, не решение об осях. По
матрице «Роль ≠ ярус» установленного шаблона Mid tier: Lead-класс —
только в очередь; правка SIBLING_MAP — вдобавок механизм (меняет
обязанность каждого будущего механизменного коммита через гейт).
Сопутствующее: диспатч scout не журналирован (журнал так и держит
одну строку journal_created) — класс F-9, третий экземпляр установки
вместе с экзаменационными диспатчами; вся работа не закоммичена —
код-гейты шаблона живут на пути коммита и ещё не имели шанса
сработать, но и при коммите mechanism_gate проверяет форму
сообщения, не ярус автора.

**Класс.** F-22 (самосертификация) с новым оттенком против
F-22/F-25: там отказывало РАСПОЗНАВАНИЕ или присвоение полномочий
молчаливо — здесь распознавание СРАБОТАЛО (класс работы назван
вслух, верно, в момент действия), отказала ПЕРЕДАЧА. Знание правила
не создаёт очередь: у шага «положи в Lead-очередь» нет ни носителя
(Lead-очередь в шаблонном CURRENT_CONTEXT — комментарий-заглушка без
протокола разбора), ни события журнала, ни кода на пути исполнения.

**Фикс (в очереди, батчем находок 5б — смежен с F-35-фиксом, это
шаги одного протокола).** Черновик: (1) шаблонный CURRENT_CONTEXT
получает живую секцию Lead-очереди с явной инструкцией матрицы
(ниже-Lead координатор КЛАДЁТ сюда, не исполняет; разбор — первой
Lead-сессией); (2) дизайн F-35-фикса дополняется разделением:
scout-обзор и ЧЕРНОВИК осей — координатор любого яруса, ЗАПИСЬ в
карту — только Lead либо оператор явным словом; (3) код-гейт на
ярус автора механизменной правки — НЕ сейчас (D-0063: продвижение в
гейт по evidence утечек; первая утечка записана этой находкой,
счётчик открыт). Полный rule-10 — при коммите фикса.

**Детектор.** До фикса — протокол 5б (как сработал: наблюдение
оператора); после — их аналог чеков 5/8 калибровки (модель сессии
vs класс исполненной работы) + внешний контур F-11. Полный обход репо по
умолчанию НЕ делается (Rule #1: каждая ось — строка в КАЖДОМ
механизменном коммите через гейт; спекулятивные оси — налог без
evidence; правило 9 и так несёт fallback «класс шире карты → scout
с конкретным вопросом»). Полный rule-10 разбор — при коммите фикса.

**Детектор.** До фикса — сам протокол валидации 5б (установка в
существующий проект; как и сработал, на пороге). После фикса
детектор будет зарегистрирован rule-10-разбором коммита фикса;
внешний контур — F-11.

**Аддендум — коррекция дизайна фикса (возражение оператора,
2026-07-12).** Исходный дизайн опирался на посылку «владелец знает
симметрии своего проекта лучше любого прохода» (scout-обзор —
опциональная подсказка). Оператор возразил: вайбкодер, ни разу не
заглядывавший внутрь своего приложения, о нём НИЧЕГО не знает — а
это ровно целевая аудитория тулкита. Посылка отвергнута; заодно
исходный дизайн нарушал правило 1 самой политики («разведка
неизвестного объёма — всегда scout»). Дизайн перевёрнут: (1)
scout-обзор структуры проекта — источник кандидатов ПО УМОЛЧАНИЮ
(один ограниченный диспатч с конкретным вопросом о парных
структурах, приёмка по следу штатно); (2) оператор — СУДЬЯ
кандидатов, не источник: подтверждает, какие пары — реальные
повторяющиеся обязанности, достойные налога на каждый механизменный
коммит; (3) вопрос оператору остаётся ДОПОЛНЕНИЕМ с узкой ролью,
которую scout закрыть не может по построению: симметрии, невидимые
изнутри репо (второй репозиторий, внешний деплой, зеркальная копия
политики — класс нашей оси «обе копии» OS↔AO3). Ноль подтверждённых
осей — по-прежнему легальный исход явной строкой.

---

## F-37 — MODEL-строка boot-хука (D-0056a) доверяет harness-payload'у: present-but-stale ярус утверждается как измеренный

**Дата:** 2026-07-12. **Источник:** вопрос оператора этой сессии
(«в этом окне мы работаем через API?») — внутренний контур,
dogfooding. Сессия — opus-координатор.

**Факт.** SessionStart-хук `tools/session_context.py` (D-0056a)
объявил в Boot Report `MODEL: claude-sonnet-4-6 -> tier
builder-tier`. Фактическая живая модель сессии — `claude-opus-4-8`,
подтверждено независимо с двух сторон: (1) окружение сессии
(`claude-opus-4-8[1m]`); (2) лог прокси `gateway/requests.db` —
органические запросы диалога id 529–532, ts 22:19:46–22:23:11,
`model=claude-opus-4-8`, разнесены во времени в такт ходам (в
отличие от синтетической смок-пачки в один тик 22:19:05, где каждая
модель по разу). Passthrough в `gateway/config.yaml` — строго 1:1,
прокси НЕ ремапит модель; значит `sonnet-4-6` пришёл из harness
SessionStart-payload, а не из шлюза. Хук — честный эхо-репитер
(`extract_model_id`, стр. 250–272): печатает поле `model` из
stdin-JSON as-is.

**Класс.** D-0042 (расхождение самодекларации модели с
транскриптом — само по себе событие). Новый оттенок: механизм
D-0056a задумывался как ИЗМЕРЕННЫЙ вход для in-session сверки яруса
(D-0056/D-0058), но измеряет не живую модель, а снимок
harness-payload'а на момент старта — который может быть
дефолтным/устаревшим и молча разойтись с фактом (сессия стартовала
как sonnet и переключилась на opus-4-8[1m], либо payload несёт не
live-модель). Хуже: при ОТСУТСТВИИ payload'а хук честно пишет
«not provided -- verify tier yourself», а при present-but-wrong
утверждает ярус УВЕРЕННО, без хеджа. Present-but-stale опаснее
absent: absent зовёт проверить, present усыпляет.

**Следствие для архитектуры.** Здесь недоверие сыграло в плюс (хук
ЗАНИЗИЛ ярус: реально opus — критик-ярус-координатор, — а не
builder-tier), но симметричный случай (хук ЗАВЫСИЛ бы ярус) дал бы
деградированной сессии ложную санкцию на Lead-действия. Сверка
яруса на входе (D-0056 п.4а) опирается на этот вход — если он врёт,
вся in-session ветка сверки наследует ложь; ловит только внешний
детектор.

**Фикс (в очереди для полного Lead — механизм D-0056a,
Fable-класс).** Черновик: (а) MODEL-строка СВЕРЯЕТ payload-модель с
независимым измерением (последняя органическая строка requests.db
за окно сессии / cc_usage) и при расхождении печатает ОБЕ + флаг
MISMATCH, а не одну payload-версию; (б) как минимум — снять
уверенный тон для payload-источника (payload = «заявлено harness,
не измерено»), сохранив измеренную ветку отдельной строкой.
Кодификация и правка хука — Lead (правка самоактивирующегося
enforcement-файла + механизм, D-0069).

**Детектор.** Обнаружено вопросом оператора (внешний контур F-11).
Системный детектор до фикса — чек калибровки «фактическая модель
сессий по транскриптам (cc_usage) vs самодекларация» (расширение
D-0042); расхождение этой сессии зафиксировано `defect_found`
ref=t-043 в routing-log. После фикса — сама сверка в хуке + тот же
чек.

**Аддендум — фикс реализован (2026-07-12, тем же днём, Fable-батч,
D-0075).** Вариант (б) черновика: MODEL-строка несёт маркер
«declared by harness, not measured -- F-37» (обе ветки формата);
вариант (а) — in-hook сверка с requests.db/cc_usage — отвергнут как
НЕреализуемый в момент SessionStart (собственные строки сессии ещё
не легли в базу; свежайшие строки принадлежат предыдущей сессии —
сверка сравнивала бы с чужим фактом), записанное ограничение в
докстринге model_line(). Измеренная сверка остаётся за D-0056
(вход-чек) и чеком 5 калибровки; живость маркера — чек 13(ж).

---

## F-38 — Порт формулы через пару учёта скопировал арифметику, но не семантику полей; critic-вход пропустил

**Дата:** 2026-07-13. **Источник:** пересмотр гейт-отчёта Phase 2 по
слову оператора — дайджест показал cache_read_share 48–49% против
«97% из кэша» в заметке API-окна; расхождение вскрыло дефект.

**Факт.** Формула cache_read_share в gateway/metrics.py (t-078,
принята С critic-входом, который ревьюил ИМЕННО согласованность
пары оси 2) взяла у собрата tools/usage_report.py знаменатель
«сумма трёх счётчиков» — корректный там, где input_tokens
ДИЗЪЮНКТЕН кэш-счётчикам (cc_usage, нативные поля транскриптов
Claude). Но в requests.db litellm Usage.prompt_tokens для anthropic
— ПОЛНАЯ input-сторона, уже включающая cache_read/cache_creation:
эмпирика на живых строках 527–531/749–751 даёт
prompt−(read+creation) = ~2 токена/запрос. Сумма трёх = двойной
счёт; дайджест занижал долю вдвое (48% против измеренных 96.1%).

**Класс.** Ось 2 SIBLING_MAP, новый подкласс: «одна МЕТРИКА, разные
РАСКЛАДКИ ПОЛЕЙ у сторон пары» — порт формулы обязан переводить
семантику полей, не копировать арифметику. Родство F-34 (контроль
обязан разделять форму вызова): и там и тут проверка «той же
формой» на другом носителе молча даёт другой смысл. Отдельный урок
про ярусы: critic сверил формулу с собратом ПО КОДУ, но
дизъюнктность полей ЭКСТРАПОЛИРОВАЛ с cc_usage на requests без
эмпирической пробы на живых строках — сверка семантики поля
требует измерения, не чтения (F-30 применим и к critic'у).

**Фикс (t-080, тем же ходом).** Знаменатель = prompt_tokens;
семантический комментарий в metrics.py (обе раскладки названы);
тест live-shape по форме реальной строки id 531 (двойной счёт даёт
0.49 и валит порог >0.9 — регресс-зубы). Ретро-чисел в БД фикс не
требует (колонки сырые, неверна была только производная в
дайджесте). Независимая проверка — critic t-080 с обязательной
СВОЕЙ эмпирикой по строкам.

**Детектор.** Тест live-shape (код на пути pytest); классовая
полнота фиксов по оси 2 — чек 9; подкласс вписан в ось 2 карты тем
же коммитом (правило поддержания карты, D-0043 шаг 4).

---

## F-39 — Реплей без max_tokens меряет потолок провайдера, а не кандидата

**Дата:** 2026-07-13. **Источник:** chief-judge аудит прогона
Middle-кандидата middle-oss (замена llama по приказу оператора);
WORSE-вердикт id 1564 вскрыл ответ, обрезанный на середине кода.

**Факт.** replay() в gateway/shadow_eval.py не передаёт max_tokens —
реплей-таргет наследует дефолтный потолок провайдера. gpt-oss-120b
на Groq: 7 из 11 ответов обрезаны ровно на 3072 completion-токенах
при source-ответах 4138–19537 токенов (requests.db id 1540–1563).
Судья честно ставит WORSE обрезанному ответу — но меряет при этом
стенд, не кандидата. Довесок того же класса: у алиаса middle-oss не
стоял reasoning_format: hidden (у builder-groq стоит с t-037) —
reasoning gpt-oss ест тот же completion-бюджет.

**Проверка соседей (D-0043, измерением).** Прежние llama-прогоны
(вкл. 4 reject-прогона R1) НЕ задеты: max completion у middle-groq
1564 токена, кластера у потолка нет — краткость её собственная, не
обрезка. Прогон A middle-oss (pass_rate 27%) помечен НЕВАЛИДНЫМ в
SHADOW_EVALUATION_LOG; на необрезанном подмножестве 2/4=50%.

**Фикс (t-091, builder).** Per-pair max_tokens от длины source
(completion_tokens×1.3, floor 8192) + CLI --max-tokens; счётчик
truncated по finish_reason=="length" в строке результата; hidden
для middle-oss. Собрат toolkit/gateway/shadow_eval.py — в
порт-очередь (D-0074).

**Детектор.** truncated-счётчик в выводе shadow_eval (код на пути
каждого прогона) + штатный chief-judge аудит прогона (D-0031);
тест уровня replay() в pytest.

---

## F-40 — Ёмкость бесплатного судьи смещает судимую выборку к коротким задачам

**Дата:** 2026-07-13. **Источник:** прогон Middle-кандидата
middle-gemini — 4 из 10 пар не судимы: Groq отверг судейские
запросы 9–14k токенов («Request too large», TPM-лимит 8000 на
gpt-oss-120b).

**Факт.** Пара «длинный source + длинный кандидат» не влезает в
TPM-лимит judge-groq — судимое подмножество систематически смещено
к коротким задачам. В llama-прогонах эффект маскировался краткостью
кандидата: успешные судейские промпты прогона №3 все ≤7162 токена
(измерено), 2 «пустых ретрая» — тот же класс. Чем ближе кандидат к
source по полноте, тем длиннее пара — т.е. отказ судьи коррелирует
с КАЧЕСТВОМ кандидата: смещение не случайное.

**Mitigation сейчас.** errors-счётчик прогона уже печатается;
строка лога прогона обязана раскладывать unjudged-пары явно.
Длинные пары точечно судимы judge-gemini (кросс-семейный, 20
req/day) — на объём не хватает. Решение о платном судье для длинных
пар (Rule #1: цена судьи против цены слепого пятна) — калибровка
~07-18.

**Детектор.** errors= в выводе shadow_eval + обязательная раскладка
unjudged в строке SHADOW_EVALUATION_LOG; chief-judge аудит (D-0031).

---

## F-41 — Навигационный документ протухает молча: вскрытие вопросом оператора, не механизмом

**Дата:** 2026-07-13. **Источник:** слово оператора (прецедент:
ROADMAP.md отставал от фактического состояния фаз, пока оператор не
спросила; шаги Phase 1/1.5 помечались [x] задним числом 2026-07-11
как «bookkeeping» — работа была закрыта раньше, документ-владелец
этого не отражал).

**Факт.** Документы-владельцы (ROADMAP — фазы/гейты,
CURRENT_CONTEXT — живая задача, DELEGATION_TABLE — статусы,
DECISIONS — решения) обновляются дисциплиной сессий; системного
детектора ОТСТАВАНИЯ содержимого от фактов не было: чек 10 смотрит
РАЗМЕР живых файлов, session-handoff — что закоммичено, но не
соответствие написанного реальности. Протухший навигационный
документ невидим до вопроса оператора — класс F-15 (обещание без
детектора), применённый к слою памяти: репозиторий объявлен
единственным источником истины (D-0001), а истинность его
навигационного слоя никто не проверял.

**Mitigation.** Чек 24 еженедельной калибровки (зарегистрирован тем
же коммитом, что эта находка): прямой проход «события окна отражены
в документах-владельцах» + обратный «несущие утверждения boot-пути
сверяются с фактом». Частота раз в неделю — решение оператора
(2026-07-13); вердикт семантический, за калибрующим Lead (D-0063).

**Детектор.** Чек 24; отказ самого чека ловит чек 0 следующего
прогона (разбор полноты предыдущего) и строка Last Calibration в
Boot Report (D-0047). Внешний контур — вопрос оператора (F-11), как
и сработал в прецеденте.

---

## F-42 — Учёт транскриптов слеп к workflow-субагентам; негатив замера прошёл приёмку с контролем чужой формы

**Дата:** 2026-07-14. **Источник:** вопрос оператора на прогоне №1
экзамена экономии — скрин чата C-t1 с Opus/Sonnet-оркестрацией
против вывода Lead «C делегировал 0%».

**Факт (два слоя).** (1) Системный: Workflow-тул пишет агентские
транскрипты на два уровня глубже Task-субагентов
(`<session>/subagents/workflows/wf_*/agent-*.jsonl`); дефолтные
глобы tools/usage_report.py (`*/subagents/*.jsonl`) их не видят —
расход агентов молча выпадает из cc_usage (класс «молчаливый $0»,
ось 2). Масштаб по всей истории проектов: 16 файлов, все в трёх
песочницах C прогона №1 (baseline, калибровки, savings-тренды НЕ
задеты — проверено обходом ~/.claude/projects целиком). Недосчёт C:
$6.98 видимых → $20.30 реальных, делег. доля 53/77/46%.
(2) Приёмочный, рецидив класса F-34 у ПРИНИМАЮЩЕГО (Lead): негатив
«0 сайдчейнов» был проверен позитивным контролем на сайдчейнах
плеча B — но это Task-субагенты, ДРУГАЯ форма хранения; контроль
чужой формы доказал работоспособность трубы, не отсутствие объекта.
Неверный вывод ушёл в закоммиченный отчёт и опровергнут внешним
контуром (F-11, вопрос оператора).

**Фикс.** t-093 (builder, спека в журнале): рекурсивный охват
subagents/** в usage_report + regression-тест глубокого пути
(старый плоский паттерн пинуется как «не видел»). Сканер экзамена
исправлен тем же часом (os.walk); отчёт прогона №1 исправлен громко
(секция «ИСПРАВЛЕНИЕ», старые числа отозваны с обоснованием, не
переписаны молча). Toolkit-копия usage_report — порт-очередь.

**Детектор.** Точечный — regression-тест в каноническом прогоне
suite (код на пути). Классовый («появилась ещё одна форма хранения
транскриптов») — кодом не ловится по построению; держится
правилом F-30/F-34 командной гигиены (контроль ТОЙ ЖЕ формой) при
каждом негативном замере + внешним контуром F-11; рецидив этой
находки = evidence для сверки суммарного учёта с независимым
источником (биллинг провайдера).

---

## F-43 — Мультистрочные турны субагентов: first-occurrence дедуп теряет 53–96% их output-токенов

**Дата:** 2026-07-14. **Источник:** живая сверка DoD t-093 —
builder честно доложил расхождение факта с ожиданием спеки и
разобрал причину построчно (requestId с output 5/5/5/383), не
выходя за owns; масштаб домерил Lead.

**Факт (замером, все три формы).** Турн субагента с несколькими
content-блоками пишется НЕСКОЛЬКИМИ строками транскрипта с одним
requestId: input/cache-счётчики идентичны во всех строках,
output_tokens в промежуточных — placeholder (2–7), реальное
значение несёт ПОСЛЕДНЯЯ строка. Конвенция usage_report «первое
вхождение requestId побеждает» (докстринг Task 5/6 утверждал
«identical usage block» — неверно для субагентов) систематически
берёт placeholder. Проба по всей истории ~/.claude/projects:
main-цепочки ЧИСТЫ (0 расхождений на 3907 requestId — там строки
дубли-идентичны, first==last); Task-субагенты и workflow-агенты
задеты массово — недосчёт output по моделям: sonnet-5 2.15M из
2.49M (86%), opus-4-8 96%, haiku-4-5 91%, sonnet-4-6 53%, fable-5
71%; в деньгах ~$50+ за историю. Обратная сторона той же монеты:
суммирование ВСЕХ строк без дедупа (разовый сканер экзамена v1/v2)
завышает input/cache в разы. Верная семантика — «последняя строка
requestId побеждает» (input/cache идентичны, output финальный);
сверено: пересчёт v3 совпал с официальным учётом main-сессий до
цента.

**Следствия.** Экономика делегирования занижала расход воркеров:
чеки 11/18 (доля сайдчейнов, $/принятую единицу), savings_report,
evidence-блок DELEGATION_TABLE — output-часть сайдчейнов
недосчитана; кэш-числа и main-цепочки верны, G1/объёмы задач не
задеты. Числа прогона №1 экзамена пересчитаны третьей ревизией
(соотношения плеч устояли, абсолюты ÷~2.3).

**Фикс.** t-094 (после critic-входа t-093, тот же файл):
last-occurrence семантика в usage_report + ре-импорт/backfill
исторических строк + regression-тест на паттерне 5/5/5/383;
toolkit-копия — порт-очередь. До фикса экономические числа читать
с этой оговоркой.

**Детектор.** Точечный — regression-тест t-094 (код на пути).
Классовый — образцовое поведение исполнителя: расхождение
witness-числа с ожиданием спеки НЕ замалчивается и НЕ чинится
подгонкой, а возвращается находкой (этот случай — эталон для
builder-роли; сверка чисел приёмкой D-0052 и сработала как
детектор). Экономтренд чека 18 после t-094 получит ступеньку —
помечена этой находкой заранее, чтобы калибровка ~07-18 не читала
её как реальный рост расхода.
