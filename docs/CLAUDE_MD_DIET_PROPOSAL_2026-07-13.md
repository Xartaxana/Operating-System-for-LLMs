# Предложение глубоких срезов CLAUDE.md — 2026-07-13 (ждёт решения Architect)

Статус: ПРЕДЛОЖЕНИЕ Lead (Fable) по запросу оператора; глубокие
срезы операционных домов — только решением Lead+Architect (D-0068).
До одобрения CLAUDE.md не трогается.

## Замер (2026-07-13, boot-diet round 5 закрыт на 99 854/100 000 — запас 146 байт)

CLAUDE.md = 30 857 байт, по секциям:

| байт | секция |
|---|---|
| 810 | заголовок |
| 1 367 | Ярусы |
| 13 613 | Правила маршрутизации (правило 10 — 4 287; правило 11 — 2 394; правило 1 — 1 541) |
| 5 613 | Журнал маршрутизации |
| 3 230 | Роль ≠ ярус |
| 2 907 | Деградация Lead |
| 3 317 | Командная гигиена |

## Принцип резки

Автозагруженная политика — единственный роутер (D-0041, F-1):
режем НЕ обязанности, а их ОБОСНОВАНИЯ. Три класса текста в файле:

- **Оперативный** (что делать на пути исполнения) — не трогаем.
  Урок F-31: формулировки несущие, пересказ их схлопывает.
- **Рационале/история/прецеденты** — уже записаны в своих домах
  (docs/DECISIONS_FULL.md — полные тексты решений; docs/FINDINGS.md
  — находки). В политике остаётся ссылка (D-XXXX/F-XX). Это не
  перенос, а снятие ДУБЛЯ — перед каждым срезом сверить, что дом
  реально содержит вырезаемое (иначе сначала дописать дом).
- **Процедурная детализация, чью встречу гарантирует КОД** (D-0063):
  commit-гейт (правило 10) и journal_validator (журнал) стоят на
  пути исполнения и сами инструктируют текстом ошибки. Полная
  процедура нужна в момент отказа гейта, не в каждом контексте.

## Срезы по секциям (кандидаты; ~-8.2К суммарно, 30.9К -> ~22.7К)

### 1. Правило 10 «Четыре вопроса» — 4 287 -> ~1 300 (-3.0К)

Оставить: сами четыре вопроса (а-г) одной строкой каждый;
распознавание механизма («любая правка, добавляющая обязанность
будущих сессий или машинную проверку; сомнение = механизм»);
факт гейта (осевой блок перечислением + строка tier:, отказ ниже
lead-привязки) и «на дисциплине — только явной строкой с
(в)-детектором».

Срезать (дома: D-0055, D-0063, D-0064, D-0065, D-0072 в
DECISIONS_FULL; F-20/F-25/F-33 в FINDINGS): объяснение, почему
перечисление а не проза; блок-на-каждый-механизм (F-33) — сжать до
одной строки; описание невода гейта и эшелонов распознавания;
историю продвижения в код-гейт; «вопросы оператора фиксируются как
findings» (дубль D-0014). Гейт сам не даст пропустить: его текст
ошибки ведёт к mechanism_gate.py и решениям.

Риск: сессия, пишущая механизм, увидит краткую форму и узнает
детали только от гейта — по построению D-0063 это штатный путь.

### 2. Журнал маршрутизации — 5 613 -> ~3 200 (-2.4К)

Оставить: формат строки-примера; базовые поля; типизированные поля
одним перечнем (task_id/attempt/failure_class/witness/ref/by/basis
— по одной строке); выдача task_id перечитыванием хвоста; ts с
системных часов; список событий; факт pre-commit валидатора.

Срезать (дома: D-0053, D-0056б, D-0060, D-0069 полные тексты;
F-23/F-29 в FINDINGS; сам валидатор — исполняемая спецификация с
громкими ошибками): нарратив находки 5а; процедуру ретро-пары
(валидатор + D-0056б держат); историю коллизий и «не переписывать»
детали (append-only остаётся строкой); развёрнутую матрицу
by/basis с уроком t-064 (валидатор отклонит неверное сам и скажет
почему); перечисление детекторов-чеков.

Риск: воркер/координатор узнаёт тонкость формата от валидатора на
коммите, а не заранее — цена = один отбитый коммит, громко (уже
случалось и сработало: находка 5а).

### 3. Правило 11 DoD+манифест — 2 394 -> ~1 300 (-1.1К)

Оставить: DoD обязателен в каждом диспатче, форма по ярусам (по
строке на builder/scout/critic); манифест: «дано» всегда,
owns/non-goals/handoff на пишущем, декларативен по чтению /
нормативен по записи; воркер возвращает диспатч без DoD/манифеста
вопросами.

Срезать (дома: D-0054, D-0073): объяснения скобками («стартовая
корзина; её достаточность — ответственность Lead», «телеметрия
качества спек»), ссылку на покрытие Lead-задач и судьи чужими
механизмами.

### 4. Правило 1 разведка — 1 541 -> ~1 100 (-0.4К)

Оставить: маршрут по умолчанию, калибровку до ~4 целей с
dispatch_skipped, двухпроходный обзор внешнего репо (D-0066) одной
строкой, приёмку по следу с негативной сверкой.

Срезать: развёрнутое описание второго прохода (дом D-0066) и
«дайджест без следа -> rejected» дубль (это уже в D-0046-строке).

### 5. Командная гигиена п.6 — 3 317 -> ~2 700 (-0.6К)

Оставить: обе нормы целиком (env-негатив после позитивной сверки;
несущее утверждение о среде только после измерения; позитивный
контроль пустого поиска той же формой вызова).

Срезать: историю порта из AO3 и родословную (fb41872, F-21
addendum), «класс ловился 4 раза...» (дом F-34 в FINDINGS),
примеры алтернаций сжать до одной строки.

### 6. Мелочь по всем секциям (-0.7К суммарно)

Датированные пояснения в заголовках секций, повторные расшифровки
уже введённых терминов, двойные ссылки на одно решение в одном
абзаце. Точечно, без изменения формулировок норм.

## Что НЕ режем (по риску, не по размеру)

- **Роль ≠ ярус** — целиком (F-31: три модели схлопнули секцию в
  пересказе; текущий текст — результат починки). Допустим только
  срез преамбулы-нарратива (~-0.3К), сами три определения и
  матрица — неприкосновенны.
- **Деградация Lead** — сверка в обеих точках (D-0056) с
  объяснением «поодиночке недостаточны»: это несущая логика, её
  сокращение — класс F-21.
- Ярусы, правила 2-9 (кроме мелочи п.6) — уже плотные.

## Порядок исполнения (после одобрения)

1. Каждый срез — сверка, что дом содержит вырезаемое; если нет —
   сначала коммит в дом (evidence не удаляется, D-0038-принцип).
2. Правка CLAUDE.md одним коммитом: механизменный путь — осевой
   блок правила 10 + tier: fable + четыре вопроса в сообщении.
3. Верификация: (а) critic-диспатч «дифф не потерял ни одной
   обязанности» с DoD-списком норм до/после; (б) прогон golden set
   scout (D-0057 — правка политики затрагивает и его контекст);
   (в) детекторы дрейфа — ZCRT-класс (D-0022) + чек 10 калибровки
   (рост/усушка живых файлов) + чек 8 (аудит распознавания).
4. Порт урезанных формулировок в AO3/toolkit НЕ автоматический:
   их CLAUDE.md — свои деплои; в порт-очередь при расхождении.

## Ожидаемый итог

CLAUDE.md ~22.7К (-8.2К); boot-бюджет ~91.7К/100К — запас ~8К
вместо 146 байт. Следующий резерв при новом росте — ROADMAP 17.3К
(закрытые фазы 0/1/1.5 сжать до строк закрытия с указателями) —
отдельным предложением, если понадобится.
