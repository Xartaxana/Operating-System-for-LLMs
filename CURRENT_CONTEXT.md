# Current Context

## Maintenance Rule (D-0038)

LIVE state only. A queue item's budget — a few lines: the undecided
decision + the OWNER of its full text. Retellings of closures, of
other documents, or of another deploy's implementation belong to
their owners; evidence is never deleted, only relocated. A pointer to
closed work is scaffolding, not a resident: once the archive index
carries the entry, the pointer here is a duplicate and the next diet
removes it. Procedure — the boot-diet skill (D-0068). Per-item detail
offloaded 2026-08-18 → docs/tasks/2026-08-18_current-context-diet-details.md
(pull a section only when that item is chosen).

## Current Milestone

**Phase 5 — Нормативный корпус — ЗАКРЫТА 2026-08-20; оба гейта
зелёные** (носитель хода — docs/tasks/2026-08-18_phase5-norm-corpus.md):
W0-W3 (2026-08-18), W2c целиком (диета CLAUDE.md/протокола + архив
CALIBRATION_HISTORY + стабы K1/K3/K5 + пороги 36700/37000 (ПЕРЕВЫВЕДЕНЫ
08-20 → 37500/37800, ниже) словом
Архитектора), W4 целиком (body/bodypred + skip-каунтер + сторож +
вынос тел CHK-26/13/18 + храповик: ГЕЙТ (а) ВПЕРВЫЕ ВЗЯТ, порог
146000 от факта, 57500 аннулирован). Верификации словом Архитектора:
W4-V — потерь 0/0/0; W2c-V — потерь 0 по шести срезам вкл.
ретро-скоуп W1+bf8eb0c/634a3a8; четыре фикса W2c-V посажены
(CLAUDE.md:12 порог, статус F-56, пара протокол↔архив в оси 4,
обратные указатели 12(в)/21). Недобор К3 (1295 Б) принят словом
2026-08-20. Пол пустого окна 80083→67258 Б. Хвост фазы в очередях
карты/CURRENT_CONTEXT (Ф12, _region_at, О2 corpus_growth --check,
Р6-машина секций). Владелец фаз — ROADMAP.md; plan of record —
docs/UNIFIED_PLAN_2026-07-07.md.

ХВОСТЫ Phase 4 (не гейт), живых ДВА: (1) строка-класс справочных
docs/*.md в леджере — порт-очередь (D-0074); (2) экзамен-кита
отдельным заходом — тестов нет у СЕМИ инструментов кита из
четырнадцати (t-374). Хвост «SQL LIKE exam_runner» ВЫЧЕРКНУТ 08-19:
закрыт ещё 08-16 (t-449, 842db30, 8 граничных тестов) — строка
пережила закрытие и диету, вскрыта повторным диспатчем t-501
(протухший пункт = класс F-41, материал стоячего чека W3). Решённые
хвосты (кодировка, DOC_ONLY_EXTENSIONS) → детали d1.

## Current Task (Authoritative, D-0025) — не назначена

Живые рамки: ТУЛКИТ-МОРАТОРИЙ D-0074 (toolkit/ — батчем по слову);
AO3-очередь — в ИХ docs/HANDOFF.md (D-0082); «haiku сохраняем» СНЯТО
08-16 → D-0102 (scout=Sonnet, приёмка по экзам-доверию R1, haiku не
привязан). Активные гейты и их эпохи — docs/RULE_COVERAGE.md
(единственная карта «правило → сторож»).

**ХВОСТ Lead-экзамена v2:** D-0083-сверка ИСПОЛНЕНА 08-19 (t-499) с
НАХОДКОЙ: оба «opus»-прогона (t-473/474, вердикт «opus 0/2») шли
фактически на claude-opus-4-8 — привязанная claude-opus-5
способностной половиной НЕ ИЗМЕРЕНА; fable PASS подтверждён (t-475
MATCH). Стоячая формула (слово оператора 08-19: «пока прогонов на
claude-opus-4-8 хватит» — перепрогон на opus-5 СНЯТ): «fable PASS
измерен; opus-4-8 0/2; opus-5 способностной половиной не измерен —
сознательно принято». Хвост ЗАКРЫТ. Носитель —
docs/tasks/2026-08-18_lead-exam-v2.md (узел B6). Порт класса
«фантомный /» → пункт очереди ниже.

## Routing MVP — LIVE on both deployments

Оба деплоя живы (pilot D:\AO3_tests, reference — этот репо). Разборы
калибровок — notes событий `calibrated`, не этот файл; №8 08-20
(полный разбор — docs/task_reports/2026-08-20_calibration-8.md),
следующая ~08-27 (staleness — Boot Report).
**docs/OPERATIONAL_NOTES.md — справочник ПО ТРЕБОВАНИЮ, не на
boot-пути.** Читать ПЕРЕД: подъёмом прокси и правкой gateway/ (§2);
работой с судьёй, traffic_kind, статусами делегирования (§1); спором
о метрике успеха и доле frontier (§3).

## Remaining Lead-tier Queue (живое)

- **РЕМЕДИАЦИЯ КАЛИБРОВКИ №8 — ЗАКРЫТА 2026-08-21.** Тринадцать узлов,
  ВОСЕМЬ критик-гейтов (два вернули блокер), два коммита: 786cf81 (37
  файлов) и 9cee6b9 (17 файлов). Канон 4245 passed, 29 skipped.
  Владельцы полного текста: docs/tasks/2026-08-20_calibration-8-remediation.md
  (DAG, решения оператора, оба блокера с их атаками) и
  docs/task_reports/2026-08-20_calibration-8.md (находки F1..F15).
  Спеки узлов персистированы отдельными файлами в docs/tasks/ (узлы B,
  E, I, подъём храповика, сторож порогов ядра, тела карты).

  **ГЛАВНОЕ, ЧТО ОСТАЛОСЬ ЗНАНИЕМ, а не строкой отчёта:** оба блокера
  вскрыли ОДИН класс — механизм, который выглядит работающим и не
  работает. Первый: пункт нормы про коэффициент существовал только как
  текст, кода за ним не было. Второй: знаменатель измерителя считал
  популяцию, физически неспособную дать числитель. Отсюда ОСЬ 14
  (docs/SIBLING_MAP.md): у порога с претензией на монотонность сторож
  обязан проверять не факт объявления подъёма, а ВЕЛИЧИНУ и СВЯЗНОСТЬ
  его обоснования — иначе объявление становится ритуалом, а сторож
  декорацией. Оба экземпляра породы закрыты, формы РАЗНЫЕ намеренно.

  **ЖИВАЯ ОЧЕРЕДЬ ремедиации (владелец текста — носитель хода):**
  - **Свежесть несущих фактов — МАШИННЫЙ СЛОЙ СОЗРЕЛ.** Кластер (iii)
    чека 23 калибровки №7: п.5 чеклиста существует с t-343 и протёк
    ЧЕТЫРЕЖДЫ (t-368, t-438 и дважды в окне №8, один раз — моё «два
    кластера» вместо трёх). R11(n) даёт промоцию «по следующему
    рецидиву», рецидив наступил. Частичный слой есть: given-path warn.
  - **Уточнение п.4 командной гигиены:** норма не различает ad-hoc
    инлайн-мутацию и именованный скрипт с приложенным исходником и
    негативным контролем. За вечер трижды дала ложное предупреждение на
    чистом вычислении.
  - **C-4, решение уровня Lead:** сиблинг-инфраструктура `*_md` мертва
    — пяти модулей нет, контр-прогон дискриминации ЗЕЛЁН там, где шапка
    файла требует красного. Сливать батареи или восстанавливать сиблинги.
  - **Класс «число замера в норме без владельца счёта» — ЧЕТЫРЕ
    экземпляра** (19 файлов проводки против 21; 121 тест против 279;
    50 parity-пар против 97; «12 артефактов манифеста» против 15).
    Правило-кандидат: число в норме сопровождается носителем счёта либо
    не пишется вовсе.
  - **Класс «посимвольное утверждение по выводу поисковика».** Драфтер
    прочёл прямой слеш как обратный и объявил живой дефект гейта;
    опровергнуто чтением. Правило-кандидат: утверждения о символе
    валидны только по Read.
  - Классы кода в очереди: «маркер в негативном контексте на строке
    прямого извлечения» (исказил `logs/owns_registry.jsonl:72`);
    «склейка двух бэктик-путей в один given-токен»; «словарь
    write-глаголов из четырёх форм — общий потолок трёх слоёв»;
    реестр владений регистрирует ЗАБЛОКИРОВАННЫЙ диспатч, которого нет.
  - **ПОРТ-ОЧЕРЕДЬ, ось 7:** `owns_gate.py` и `dispatch_gate.py`
    портируются **ТОЛЬКО ВМЕСТЕ** — жёсткий импорт
    `marker_immediately_followed_by_slash`, фолбэка нет, порт одного
    убьёт хук кита на импорте. Плюс: ни измеритель, ни тела карты в кит
    не поставляются.
  - **Пара оси 4 — ДОЛГ:** реестр литералов `tools/warn_layers.json` ↔
    перечень слоёв чека 11(в); `PROCESS/**` был не-голом узла.
    Туда же: чек 11(в) называет два слоя именами, которых в печатаемом
    тексте НЕТ, и живых слоёв больше, чем в его перечне.
  - **ГРАНИЦА КАРТЫ ТОНКАЯ:** после выноса тел `bpr=1005` при полосе
    1005.1 — запас 0.1. Следующая строка карты вернёт WARN.
  - **Вопрос, который сегодня не задаёт НИ ОДИН детектор:** достаточен
    ли ответ в ядре строки карты БЕЗ открытия тела. Кандидат —
    подпункт чека 24.

  **ЖИВЫЕ ВОПРОСЫ К ОПЕРАТОРУ:** (1) должен ли внешний файл-указатель
  засчитываться как given-манифест `dispatch_gate` — мой диагноз блока
  был опровергнут измерением: причина не в owns, а в отсутствии слова
  given; (2) наследник Lead-экзамена прицеплен к БОЛЬШОМУ ЭКЗАМЕНУ
  словом оператора, триггер 2 взведён и записан строкой Runs log
  (PROCESS/LEAD_RANKING_EXAM.md) — долг остаётся: способностная граница
  Lead/резерв на текущем тексте R11 не измерена.

  **ЖИВЫЕ ОСТАТКИ находок №8** (остальные закрыты узлами): F7 — R3
  протёк на 11 приёмках, машинный слой отсутствует, кандидат «крупный
  дифф без критик-входа»; F11 — доля notes сверх бюджета 43%→79.5% при
  живом WARN-слое; F12 — эхо-база 201 фолбэк за окно, шаг = раскладка
  по причинам; F15 — остаток саморевизии (чек 19 пятый пустой прогон;
  Δ corpus_growth считаются к записи сайдкара, не к границе окна;
  savings_report без оконных аргументов).

  **ЗАМЕР ПЛОТНОСТИ WARN-СЛОЁВ — ИНСТРУМЕНТ ЕСТЬ** (`tools/warn_density.py`,
  сайдкар `logs/warn_density.jsonl`). Окно №8, честная база: диспатчевый
  слой 22.9%, гигиена 4.4%, эхо витнесса 1.9%, длина заметок 1.5%,
  поиск 0.8%. **Прежние 82/77/60% были завышены; но и моё «завышены
  втрое» НЕВЕРНО** — базы несопоставимы, слои и окна разные, послойно
  множители 11.9 / 5.0 / 2.7. Граница носителя печатается каждым
  прогоном: субагентский поток структурно невидим (7734 из 9757 вызовов
  окна), и это свойство транскрипта, а не дефект инструмента.

- **Phase 5: живые остатки** (носители — спеки 08-19): W2c в
  исполнении (спека принята: W2c-1 диета CLAUDE.md — builder t-516;
  W2c-2/3 протокол — после него; W2c-4 пороги; W2c-V гит-сверка —
  слово Архитектора 08-19; развилка Р1 гейта (а) — У АРХИТЕКТОРА);
  посадка узла C (после байтов W2c-1; тем же коммитом перенумерация
  дубля «14» critic.md + прогон CRITIC_EXAM); партия 2 сканера
  (dispatch_gate с критиком; owns_gate) — очередь по приоритету;
  живой замер №8 (~08-21); Р2(C) SessionStart-строка сайдкара —
  после №8. Узел B, партия 1, батч мелочей (пин 35, инварианты Д1)
  — ПОСАЖЕНЫ 08-19 (89b6142, 1e15980; канон 3309).

- **Калибровка №7 (08-14) и boot-diet (08-16) исполнены** (разборы —
  notes `calibrated` + архив 2026-08-16_boot-diet-extractions). ЖИВОЕ:
  вопрос большого экзамена 14(е) — три релиза без прогона.

- **Триаж-критерий качества релиза — стоячая НОРМА будущих релизов** →
  детали d2 (нарративная ссылка снимается с инлайном урока; синтет-
  фикстура легальна; потеря урока хуже оставленного номера).

- **F-54 РЕШЕНА 08-16, строка «решения нет» протухла — снята 08-19**
  (пятый экземпляр класса F-41 за день, материал чека 34): класс
  применим к нам как шум warn-потока; вместо оси — эвристика
  проектирования в правиле 10 + чек 11(в) меряет долю срабатываний
  каждого warn-слоя. Владелец полного текста — docs/FINDINGS.md F-54.

- **F-55 РЕШЁН 08-09** (подкласс включён: статус реестра валиден только
  после чтения ДО статусной строки / grep статусного поля). Дом — п.6
  гигиены ядра + твин POLICY_FULL; детектор — чек 25(а)/25(г).

- **Калибровка №6 исполнена 08-05** (разбор — notes calibrated
  08-05T12:34; кодификации 4de6178..f768821). ЖИВОЕ: замер экономии
  перепривязки — на калибровке после 14.09.

- Пункт «глубокие срезы boot-бюджета» СНЯТ 08-05 (бюджет восстановлен
  полными проходами, 130014→90930, порог 100000 стоит); кандидаты —
  архив 08-05 §boot-бюджет на случай пробоя иной природы.

- **Находки калибровки №6 ЗАКРЫТЫ целиком 08-09** (носители —
  docs/tasks/2026-08-09_section1-early-codification.md, _hygiene-
  promotion.md, t-358..t-378). ЖИВОЕ, три очереди V5: (а) позиционный
  обход cd не первым statement'ом (24 живых) — триггер «рецидив»; (б)
  можно ли поднимать прокси без cd (тогда п.2 гигиены упростится); (в)
  порт V5 в кит и Dog. Динамику deny мерить на №8.

- **F-61 — узлы A и B ПОСАЖЕНЫ** (A: 44ef907 08-19, четыре писателя
  трека; B: 1e15980 08-19, session_context — ack после выдачи
  конструкцией, буферизация, stdout→stderr-фолбэк, санитизация; 5
  пинов перепинено двухмирно, критик t-509 fit_with_fixes исполнен,
  канон 3309 зелёный). ЖИВОЕ после сверки калибровки №8 (08-20, F8):
  из шести хвостов handoff t-503 пять ЗАКРЫТЫ в том же окне —
  фолбэк-остаток F-57 (N3, t-523), потеря чужих ключей при битом JSON
  (N1, t-521/t-531), hookspath_autofix (N2, t-522), read_stdin
  non-TTY (батч П4, t-535), live_state_diff (классифицирован AMBIENT
  тем же N2); ЖИВЫ только кит-твины (мораторий D-0074).
  Спека — docs/tasks/2026-08-19_f61-f58-remediation-spec.md; узел C
  (F-58) ПРИНЯТ пакетом рев.3, посадка после W2c-1.

- **F-56..F-59 — очередь ремедиации; владелец текста docs/FINDINGS.md**
  (невод/протухание journal_echo/ветка отката своей работы/слепота
  парсера owns). Покрытие детекторов гейтов 4/3/5 — снимок у
  владельца (справочник RULE_COVERAGE, запись чека 26; критерий — в
  самом чеке 26) + реестр режимов отказа карты.

- **Класс «сторож не отличает цитату» ЗАКРЫТ ЦЕЛИКОМ 08-19**:
  md_regions t-507 + партия 1 t-510 + партия 2 t-529/t-530 посажены
  (dispatch_gate B1/B2/W1 + WRITE-QUOTED WARN с критиком; owns_gate
  prose-предикат; mechanism_gate SKIP_RE/TIER_LINE_RE). Ф7(в) закрыт
  замером посадки (250 осевых коммитов, 0 в фенсах); остаток в
  очереди карты: Ф12 (фенс «## Ось N» ломает parse_axes),
  квадратичный _region_at. Класс «посадка байт-копией замещает
  рационал» заведён в карте (рецидив ×4; восстановление t-533 сделано,
  t-534 в ходу).

- **Батч П4 (stdin-дедлайн) ПОСАЖЕН 08-19** (t-535, критик, F-62
  закрыта в этом деплое; 14 файлов: 13 хуков + wiring_check;
  эскалация os._exit по эмпирике K7 — класс в реестре карты).
  ОЧЕРЕДИ посадки: В2 — замер фактической доставки payload
  инструментовкой; В9(б) — gate_log setdefault-append (носитель
  q503); В9(в) — critic_verdict_check stdin-дедлайн (:191); В10(б)
  — probe-лимит времени hook_liveness_probe; Ф2 критика t-535 —
  экземпляр класса «неинъектируемый дефолт пути» (K5/K12 батареи
  против реального корня, break_glass латентно); расширение вопроса
  класса карты :614 на stdin= (или отдельный класс — заведён);
  subprocess.run без stdin= по остальной кодовой базе — по
  применимости. Кит-учёт В11: 13 твинов + critic_verdict_check
  несут дыру — релизным батчем кита.

- **Входящее от Dog 08-16 отработано в тот же день** (полный текст —
  git c350bd1/8b2bc45, D:\Dog\docs\OUTBOUND_TO_KIT.md, архив §6). Итоги:
  (1) недовоз был при УСТАНОВКЕ (ec4e6f0), не в снимке — ~14 блоков
  политики слиты в их CLAUDE.md, коммит Dog 37100b3, их чеки 20/1(в)
  ожили; (2) installer-parity — в копилке ниже; (3) F-A..F-I приняты —
  DAG docs/tasks/2026-08-16_dog-delivery-scout-exam.md (t-454..t-457,
  итог D-0102), ключи docs/tasks/2026-08-16_scout-exam-v2-draft.md;
  протухшие строки SIBLING_MAP :303-305/:588-593 чинить С переаудитом
  ловушек Q6/Q4 (реестр в файле драфта); граница DOG: их critic-exam НЕ
  переделан. (4) «авто-CLAUDE.md субагента = снимок» ПРОВЕРЕН 08-19
  (t-500): ПОДТВЕРЖДЕНО — снимок, свежеспавненный воркер нёс
  формулировку R8 до правки 95b27a5; правило-посреди-сессии обязано
  цитироваться в диспатче явно (норма подтверждена измерением, узел
  закрыт).

- **Порт-очередь кита исполнена релизом v0.7.0** (t-390..t-395, критик-
  гейт t-393 GO); не вошедшее → named-узлы следующего минора (см. ниже).
  **ОЧЕРЕДЬ ПЕРЕДАЧИ DOG (D-0082, при безопасном касании их
  носителя):** их tools/journal_validator.py:340 несёт дофиксовую форму
  дыры B3; дельты B3/невода/дискриминатора path-токена — передать
  фактом и вопросом; плюс классы v0.7.0 (guard sibling-импорта, 9в
  retry) тем же касанием. [handoff-долг D-0082/F-48 — держится полным до
  передачи, не сжимать]

- **D-0099 хвосты (перепривязка Lead→Opus), живых два из шести:** (а)
  порт-очередь кита (lead-binding ветки journal_validator/mechanism_gate/
  session_context → toolkit-твины + roles.reserve/designer в образец);
  (б) ревизия марки «Lead (Fable)» в PROCESS-экзаменах — при следующем
  прогоне каждого набора; (в) замер эффекта — на калибровке после
  14.09. Способностная половина ЗАКРЫТА прямым замером 08-18 (opus 0/2,
  fable PASS). Ход — docs/tasks/2026-08-18_lead-exam-v2.md; посадка v2 —
  PROCESS/LEAD_RANKING_EXAM.md §v2.

- **Порт фикса «фантомный root-only path-token» → toolkit + Dog** (батч
  D-0074) → спека d5. Фикс штаба — t-476 (коммит 714ce91); детектор
  дрейфа parity_check --check.

- **Порт правила 9 journal_validator ЗАКРЫТ 08-10** (обе ветки). ЖИВОЕ:
  allowlist их reopen-формы в кросс-чтущем скрипте калибровки (наш
  валидатор их семантику не принимает, D-0060 — ядро). Текст — архив §2.

- **Обкатка Dog-пары корреляции** → детали d6 (№1 закрыт t-340; №2
  D4b-резидуал — ограничение обеих копий).

- **Клещи `$CLAUDE_PROJECT_DIR` — одна задача из двух половин** (наш
  t-423 08-13 + встречный Dog 14.08; полный текст — архив §6):
  wiring_check `_HOOK_COMMAND_RE` не парсит форму
  `python "$CLAUDE_PROJECT_DIR/..."` → enforcement_probe рубит коммит;
  кит settings.json нормализует в плоскую форму → старт не из корня
  ловит ERROR хука. Чинить оба конца одним батчем (фикс regex снимает
  половину). Класс «чекер диктует форму артефакта» — счёт 1.

- **Named-узлы следующего минора кита** (мораторий D-0074) — владелец
  docs/tasks/2026-08-10_kit-v0.8.0-full-mirror.md: owns_verify;
  correlation-пара + negative_lint + claim/search_control_gate;
  exam_fullgates + designer в AGENT_TIER (t-346); designer-exam-gen по
  слову; hook_liveness_probe → кит; из батча 08-05 — п.7 гигиены/
  подклассы края R11/строка D-0099 RULE_COVERAGE.

- **Guard фабрикаций координатора** (переименован словом Архитектора
  08-19 из «Adversarial-экзамен координатора»: экзаменационная часть
  закрыта прогонами LEAD_RANKING v2 08-18 + CRITIC_EXAM №4; остаток —
  постоянный машинный сторож, не экзамен) → детали d8; триггер —
  рецидив F-28-находки координатора в калибровке.

- **OWNS_GATE и GIVEN-PATH слой активны** (warn-first; owns_verify — шаг
  приёмки D-0095; история — архив 08-05). ЖИВОЕ: подкласс owns №2 (счёт
  3) — следующим батчем; given-path «чужекорневой контекст» — по рецидиву.

- **Вотчдог зависших воркеров НЕ строится** (Rule #1), триггер
  rejected/tooling к окну №8. Онбординг D-0090 — живые остатки у Dog.
  Trigger-gated кандидаты калибровок — docs/RETRO_PATTERNS.md (чек 0);
  ждут большой экзамен, critic-lite точка 2, gemini-кросс-аудит.

- **WP v0.2.4** (08-18: +§6.3.1 характер моделей; коммит 8c5be97;
  ROADMAP тем же ходом; заметка — docs/MODEL_CHARACTER_MEMO.md;
  ARCHITECTURE/ARCHITECTURE_BOOT проверены, правок не потребовали).
  Слово Архитектора 08-18: WP — живой документ, обновляется по ходу
  работы, читается по желанию с возвратом правок; НИЧЕГО не блокирует
  и очередь не держит.

- **Релизы кита: текущий v0.8.1** (публичный 1d9cbd5, снимок 1aa9085);
  мораторий D-0074. Владелец батчей — kit-v0.8.0-full-mirror DAG.
  **РЕШЕНИЕ 08-16: релиз КОПИТСЯ** (один заход, без микро-релизов) — всё
  «портом минором» ниже в один батч. **СРОК назначен словом
  Архитектора 08-19: большой релиз — ПОСЛЕ калибровки №8 (~08-21) и
  разбора всех её вопросов; в релизный заход входит порт на все
  проекты; большой экзамен — после релиза+порта.** **КОПИЛКА 08-16:** (а) installer-
  parity — docs/tasks/2026-08-16_installer-parity-spec.md; (б) порт от
  Dog (dod_track «фон≠прогон», owns_gate /-guard, verdict-блок critic.md);
  (в) scout/critic-exam-gen rewrite — dog-delivery-scout-exam DAG узлы
  B6/B7; (г) designer/judge-exam-gen — узел B8; (д) порт D-0102 в кит;
  (е) батч «binding-agnostic пины тестов» → детали d9; (ж) ПАРТИЯ
  Phase 5 (08-19): norm_retire+тест+шапочные правила ретирования;
  calibration_prepass+тест+протокол-с-шапками СОДЕРЖИМЫМ;
  corpus_growth+манифест+тест+чек 34; md_regions+тест; f61-фиксы
  четырёх хуков; test_norm_form+norm_keys; суффикс CLAUDE_WARN
  session_context; CLAUDE.md конверсии (hq-drift пары); (з)
  ПОПОЛНЕНИЕ 08-19: узел B session_context + партия 1 region-aware
  сторожей + md_regions в неводе + ветка (д/е) F-58 шести носителей
  + перенумерация critic.md (14→15/16/17) + диета W2c-1 + .gitattributes
  eol=lf + батч q503 по мере посадки. (и) ПОПОЛНЕНИЕ 08-20 (Phase 5
  закрыта): партия 2 сканера (3 гейта) + батч П4 (13 хуков +
  wiring_check + батарея) + W4-1a/1b/2/3 (body/skip-каунтер/сторож/
  тела CHK-*/храповик) + пороги ЯДРА **37500/37800** (НЕ 36700/37000 —
  перевыведены 08-20, портировать актуальные) + подъём храповика гейта
  (а) до 154000 с блоком провенанса и расширенным сторожем; ТУДА ЖЕ строка A12:
  тела PROCESS/checks/*.md — ВНЕ kit-parity (discover_pairs берёт
  PROCESS/*.md нерекурсивно) — внести в parity_manifest/discover при
  релизном батче; кит-учёт В11 (13 стдин-твинов + critic_verdict_check
  несут дыру F-62 до порта). КРОСС-ЗАПИСЬ
  AO3/Dog отложена (их репо заняты); передаётся при безопасном
  касании.
  ЖИВОЕ: конвергенция wiring_check двух сторон; латентная pytest-коллизия
  одноимённых модулей штаб/кит.

- **Экзамены — решения Архитектора 08-19** (кодификация — ближайшим
  механизменным касанием PROCESS/README + формы поставки кита,
  строка очереди): (1) форма поставки — ПРАВИЛО «КОГДА ЧТО»:
  источник экзаменов ВЕЗДЕ генераторы; фиксированный набор легален
  только как ЗАМОРОЖЕННЫЙ СНИМОК генератора, пришпиленный к эпохе
  привязки (сравнимость между прогонами), перегенерация при
  ребайндинге/пересборке набора — снимок живёт эпоху, не вечно
  (ответ на «их все будут знать»); (2) БОЛЬШОЙ ЭКЗАМЕН — ПО
  ТРИГГЕРУ: после релиза, чей батч менял экзамен-релевантные
  поверхности (роли, привязки, наборы, судья), Lead ПРЕДЛАГАЕТ
  прогон оператору; отклонение = вопрос снят до нового триггера;
  первое предложение (08-19, хвост 14(е)) ОТКЛОНЕНО словом
  Архитектора с назначенной ЦЕПОЧКОЙ: калибровка №8 → разбор ВСЕХ
  её вопросов → БОЛЬШОЙ РЕЛИЗ (копилка D-0074 + порт на все
  проекты — снятие моратория) → большой экзамен. 14(е) закрыт.
  Большой экзамен НЕСЁТ влитый остаток eval-плана stage 2 (слово
  Архитектора 08-19): minimum-n/pass^k + numeric agreement — форма
  вердиктов многопрогонная, не единичная.
  Прежние остатки живы: (i) перевзведение в wiring_check кита тем же
  батчем; (ii) прогон генераторов на пустом репо; (iii) обратный
  hq-drift генераторов — порт, заморожен.

- **Очередь из находок №19** → детали d10 (headless-протез / M2-регресс
  t3 / given-path в клетках / порт эпох hygiene_gate,journal_echo).

- **Spend-мониторинг v1 ЗАКРЫТ 08-13** (DAG docs/tasks/2026-08-13_spend-
  analytics.md, t-416..t-429; `python tools/spend_report.py --window
  last --compare prev`). Триггерная очередь, тексты в носителе: OTel-
  приёмник / GPT-Codex-адаптер / кит-порт spend-слоя / класс «агрегат по
  ключу N:1».

- Evidence-gated residuals — 9 пунктов на триггерах, VERBATIM:
  docs/task_reports/2026-07-16_evidence-gated-residuals.md.
- **Standing-триггеры Phase 2** (F-48/D-0082, триггер в архиве не
  передан) → детали d11 (worktree-изоляция / LLM-роутер / большая
  параллельность — все reopen-по-числам).
- **Eval plan stage 2 СНЯТ как отдельная строка** (слово Архитектора
  08-19): minimum-n/pass^k + numeric agreement ВЛИТЫ в план большого
  экзамена (цепочка выше — там один прогон не различает, там они и
  нужны); Batch API остаётся на своём триггере «реплеи регулярны»
  (детали d12).
- NOT adopted — владелец docs/RELATED_WORK.md («NOT adopted» ~473 +
  OpenClaw survey).

## КРОСС-ДЕПЛОЙНОЕ (D-0082) — живое

- **НЕ ПЕРЕДАНО В AO3 (слово оператора 08-16 «их репо не трогать»),
  явная строка по правилу 3а handoff:** (1) их класс «защита объявлена
  тотальной, реализована частичной» ЗАМЕРЕН у нас — 3 попадания в 3
  файлах, F-61, ось карты заведена; (2) метод-урок к нему: вопрос
  «держится ли гарантия» есть СУЖДЕНИЕ о путях кода, и ярус поиска
  сводит его к «есть ли общий try/except» — у нас scout вернул 0 в 16
  файлах и был отклонён, critic по 4 файлам вернул 3. Обоим деплоям это
  полезно до того, как они пошлют тот же вопрос разведчику. (3) СВЕРКА
  08-16 (заказ оператора, класс DOG): установочного недовоза у AO3 НЕТ
  (они не зеркалят кит, отличия ведутся сознательно и задокументированы
  «признанное отличие от OS»; входящие от нас разобраны, реверс-якоря
  наших чеков 15/33 в их дереве живы); найден класс ПРОТУХШИХ ПИНОВ
  номеров наших чеков: «чек 5 калибровки OS» в их CLAUDE.md (4в, 8а,
  шапка гигиены, п.8), next-task:99 и HANDOFF:468 указывает на наш чек 5
  (деградация), тогда как дома́: чек 4 (D-0042 самодекларация/тип), чек
  25 (hygiene/байт-копия транскрипт-скан), чек 1 (самоизъятие); «чеки
  0/5 (F-48)» в их 4б — F-48 у нас живёт в чеке 32. Их же кросс-пункт
  08-15 чинил наши чеки 5/6 (binding-aware) — канал рабочий, пины
  поправить встречным кросс-пунктом при безопасном касании; их правка —
  решение их Lead. ТАКЖЕ: их master ahead 11 (в т.ч. наши кросс-коммиты
  1092a08, d65e303) — не запушен, их сессии виднее. Передать всё при
  безопасном касании их носителя. [handoff-долг D-0082/F-48 — держится
  полным до передачи, не сжимать]

- Все входящие AO3 по 08-16 РАЗОБРАНЫ (тексты — архив §4–§5); живых
  остатка два (нерешённые оси-кандидаты карты) → детали d14.

- **Класс «имя модели прибито как критерий вместо резолюции привязки»** —
  владелец текста docs/SIBLING_MAP.md; закрытое 08-16 (cc31cdd, 8fae98e,
  da54ec5, 5437d54). Открытый остаток (критик t-446 F2в: секция «Role≠
  tier» F-31 + колонка «Model here») + хвосты (i-iii) + trend-note 18(б)
  → детали d15.

## Archive (D-0038)

docs/task_reports/README.md — единственный владелец описаний архива.
