# Current Context

## Maintenance Rule (D-0038)

LIVE state only. A queue item's budget — a few lines: the undecided
decision + the OWNER of its full text. Retellings of closures, of
other documents, or of another deploy's implementation belong to
their owners; evidence is never deleted, only relocated. A pointer to
closed work is scaffolding, not a resident: once the archive index
carries the entry, the pointer here is a duplicate and the next diet
removes it. Procedure — the boot-diet skill (D-0068).

## Current Milestone

ВСЕ ОБЪЯВЛЕННЫЕ ФАЗЫ ЗАКРЫТЫ, новая не открыта; идут штатные операции
и очередь ниже. Владелец фаз, гейтов и подписей — ROADMAP.md; plan of
record — docs/UNIFIED_PLAN_2026-07-07.md.

ХВОСТЫ Phase 4 (не гейт), живых три: (1) F2 критика —
неэкранированный SQL LIKE (метасимвол `_` в слагах, класс t-126,
локализован exam_runner.py:92-156); (2) строка-класс справочных
docs/*.md в леджере — порт-очередь (D-0074); (3) экзамен-кита
отдельным заходом — предмет оказался классом: тестов нет у СЕМИ
инструментов кита из четырнадцати (переразведка t-374). Прочие
хвосты решены 08-09/08-10 (кодировка — класс «печатает кириллицу без
reconfigure потоков»; DOC_ONLY_EXTENSIONS — .yaml/.yml НЕ добавлять,
конфиг меняет поведение, doc-only освобождение открыло бы обход
зелёного прогона).

## Current Task (Authoritative, D-0025) — не назначена

Живые рамки: ТУЛКИТ-МОРАТОРИЙ D-0074 (toolkit/ — батчем по слову);
AO3-очередь — в ИХ docs/HANDOFF.md (D-0082); «haiku сохраняем» в силе;
старая очередь on-touch/evidence-gated. Активные гейты и их эпохи —
docs/RULE_COVERAGE.md (единственная карта «правило → сторож»).

## Routing MVP — LIVE on both deployments

Оба деплоя живы (pilot D:\AO3_tests, reference — этот репо). Разборы
калибровок — notes событий `calibrated`, не этот файл; №7 08-14,
следующая ~08-21 (staleness — Boot Report).
**docs/OPERATIONAL_NOTES.md — справочник ПО ТРЕБОВАНИЮ, не на
boot-пути.** Читать ПЕРЕД: подъёмом прокси и правкой gateway/ (§2);
работой с судьёй, traffic_kind, статусами делегирования (§1); спором
о метрике успеха и доле frontier (§3).

## Remaining Lead-tier Queue (живое)

- **КАЛИБРОВКА №7 ИСПОЛНЕНА 08-14, BOOT-DIET ИСПОЛНЕНА 08-16** (оба
  хвоста слова оператора 08-10; разборы у владельцев — notes
  `calibrated` и архив 2026-08-16_boot-diet-extractions). ЖИВОЕ из
  копилки №7: вопрос большого экзамена 14(е) — три релиза без прогона.

- **ТРИАЖ-КРИТЕРИЙ КАЧЕСТВА РЕЛИЗА — стоячая норма будущих релизов**
  (заказ 08-09 исполнен релизом v0.7.0): НАРРАТИВНАЯ ссылка (зовёт
  читателя за пределы кита — D-NNNN/t-NNN/калибровки/даты/деплои/хеши/
  наши пути) снимается ВМЕСТЕ с инлайном урока; СИНТЕТИЧЕСКАЯ
  ФИКСТУРА (самодостаточные данные теста) легальна; потеря урока хуже
  оставленного номера. Вопрос «удали скобку — знает ли читатель, что
  делать» есть вопрос применимости D-0100: критерий релиза и критерий
  записи класса — одна форма.

- **F-54 (кросс-пункт AO3): нужна ли ось эвристик агрегированного
  ОПЫТА другим деплоям и штабному словарю?** (оракулы корректности
  слепы к таким дефектам; полный текст и прецедент — docs/FINDINGS.md
  F-54). Решения нет.

- **F-55 РЕШЁН 08-09: ДА, подкласс включён** — «статус записи реестра
  валиден только после чтения ДО статусной строки либо grep'а по
  статусному полю». Дом нормы — п.6 командной гигиены ядра + твин
  POLICY_FULL; детектор — чек 25(а) и новый адресный подпункт 25(г).
  Статус в docs/FINDINGS.md переведён.

- **КАЛИБРОВКА №6 ИСПОЛНЕНА 08-05** (полный разбор 32 чеков — notes
  события calibrated 08-05T12:34; чек 5 впервые по якорю конфига —
  чист; кодификации того же дня — коммиты 4de6178..f768821). ЖИВОЕ:
  замер экономии перепривязки — на калибровке после 14.09.

- Пункт «глубокие срезы boot-бюджета» СНЯТ 08-05 (слово оператора):
  бюджет восстановлен полными проходами владения БЕЗ резки
  операционных домов (130014 → 90930), порог 100000 стоит; разбор
  кандидатов остаётся в архиве 08-05 §boot-бюджет на случай
  будущего пробоя иной природы.

- **НАХОДКИ КАЛИБРОВКИ №6 — ЗАКРЫТЫ ЦЕЛИКОМ** (досрочно 08-09 словом
  оператора, включая (7) — промоция гигиены warn→блок, V5 в проде).
  Владельцы хода: docs/tasks/2026-08-09_section1-early-codification.md
  и ..._hygiene-promotion.md, журнал t-358..t-378.
  ЖИВОЕ, три очереди V5: (а) позиционный обход cd не первым
  statement'ом (24 живых) — триггер «рецидив»; (б) можно ли поднимать
  прокси без cd (если да, п.2 гигиены упростится); (в) порт V5 в кит
  и Dog. Динамику deny мерить на №8, базлайн в носителе промоции.

- **F-61 — батч фиксов трёх хуков (`dod_track`, `critic_snapshot`,
  `session_context`), не исполнен осознанно.** Три однострочных фикса
  названы; не ставятся с ходу, потому что все три — самоактивирующиеся
  хуки живого пути, а `session_context` бежит на КАЖДОМ старте сессии
  (D-0069), и у класса пока НЕТ детектора — он заводится вместе с
  фиксом. Разбор и формы — docs/FINDINGS.md F-61; ось карты заведена.

- **F-60 — канон недетерминирован**, бьёт по инструменту приёмки:
  витнесс — наше единственное доказательство. Ремедиация ВЫБРАНА
  (три класса, три лечения), исполняется t-453. Разбор —
  docs/FINDINGS.md F-60.

- **F-56..F-59 — очередь ремедиации; владелец текста docs/FINDINGS.md**
  (невод против подключённого; протухание строк journal_echo; ветка
  отката собственной работы; слепота парсера owns двумя формами).
  Сюда не пересказывать. Смежный факт покрытия детекторов гейтов
  4/3/5 — владелец чек 26 протокола + реестр режимов отказа карты.

- **КЛАСС «СТОРОЖ НЕ ОТЛИЧАЕТ ЦИТАТУ ОТ УТВЕРЖДЕНИЯ» — РЕШЕНО
  2026-08-16, осталась реализация.** Решение: общий СКАНЕР РЕГИОНОВ
  (fenced / inline / цитата) — да; общая ПОЛИТИКА — нет. Сканер
  отвечает только «в каком регионе этот кусок»; given-слой
  dispatch_gate решает сам (инлайн-код НЕ глушить — пути там называют
  законно), парсер owns решает сам (ему нужна не цитата, а отличие
  позиции декларации от прозы). Ровно поэтому общее средство «на все
  три» и было отвергнуто как ложная экономия: общим бывает разбор, а
  не вердикт. Ось 13 после посадки сканера избыточна — снять.

- **ВХОДЯЩЕЕ ОТ DOG 2026-08-16 — ОТРАБОТАНО в тот же день (сессия
  разбора, слово оператора).** Полный текст: git c350bd1/8b2bc45 и
  `D:\Dog\docs\OUTBOUND_TO_KIT.md`. Итоги: (1) их центральное
  утверждение «батч не привёз правила» неверно — правила БЫЛИ в
  снимке v0.8.1 (toolkit/CLAUDE.md@1aa9085); недовоз случился при
  УСТАНОВКЕ (ec4e6f0 host-restoration сохранил их старый
  политблок), а ставили мы — сверка парности была нашей
  обязанностью. Недостающие ~14 блоков политики слиты в их CLAUDE.md
  с сохранением их адаптаций, ответы на их вопросы записаны в их
  CURRENT_CONTEXT — коммит Dog 37100b3; их чеки 20/1(в) ожили;
  остальные 28 расхождений — host-ahead/адаптации, довоза не
  требуют. (2) Механизм installer-parity — в релизной копилке ниже.
  (3) Их девять выводов об экзаменах (F-A..F-I) приняты в работу:
  переделка скаут-экзамена + вывод по Haiku — DAG
  docs/tasks/2026-08-16_dog-delivery-scout-exam.md (t-454 драфт в
  фоне); граница DOG: их critic-exam НЕ переделан, лекарства нет —
  только метод. (4) Их находка «авто-загруженный CLAUDE.md субагента
  = снимок на старт сессии, не живое чтение» — НЕ проверена на нашем
  харнессе; проверка — узел DAG'а; до проверки: правило, посаженное
  посреди сессии, считать НЕ дошедшим до воркеров без цитаты в
  диспатче (безопасная сторона).

- **ПОРТ-ОЧЕРЕДЬ КИТА — ИСПОЛНЕНА релизом v0.7.0 08-10** (t-390..t-395,
  критик-гейт t-393 GO). НЕ ВОШЛО, named-узлы следующего минора:
  R2 designer-роутинг + калибровочный 1(в); чек hygiene/permission_audit
  в кит-протоколе; critic_snapshot/main_gate свежего поколения +
  test_critic_snapshot; пара toolkit/docs/SIBLING_MAP.md в
  parity_manifest.
  **ОЧЕРЕДЬ ПЕРЕДАЧИ DOG (D-0082, при безопасном касании их
  носителя):** их tools/journal_validator.py:340 несёт дофиксовую
  форму дыры B3; дельты B3/невода/дискриминатора path-токена —
  передать фактом и вопросом; плюс классы v0.7.0 (guard
  sibling-импорта, 9в retry) тем же касанием.

- **D-0099 хвосты (перепривязка Lead→Opus 5), живые три из шести:**
  (а) порт-очередь кита (батч D-0074): lead-binding ветки
  journal_validator/mechanism_gate/session_context → toolkit-твины +
  roles.reserve/designer в образец toolkit/delegation.config.yaml +
  инструкция перехода; (б) ревизия марки «Lead (Fable)» в
  PROCESS-экзаменах — при следующем прогоне каждого набора, не скопом;
  (в) замер эффекта: окно 14.08–14.09 token_usage.xlsx против
  14.06–14.07 — на калибровке после 14.09; (д) прогон
  PROCESS/LEAD_RANKING_EXAM.md на claude-opus-5 вместо постоянного
  операторского override. ИСПОЛНЕНЫ: (г) — оба деплоя ответили и оба
  перепривязались (AO3 08-15, Dog 14.08); (е) — 08-05.

- **ПОРТ ПРАВИЛА 9 journal_validator — ЗАКРЫТ 08-10, обе ветки.**
  ЖИВОЕ из него одно: allowlist их reopen-формы в КРОСС-ЧТУЩЕМ
  скрипте калибровки (наш валидатор их семантику не принимает,
  D-0060 остаётся ядром). Текст — архив
  2026-08-16_boot-diet-extractions §2.

- **ОБКАТКА DOG-пары корреляции**: №1 закрыт t-340; №2 (D4b-резидуал)
  — записанное ограничение обеих копий, фикса нет без рецидива.

- **КЛЕЩИ `$CLAUDE_PROJECT_DIR` — ОДНА задача из двух половин** (наша
  находка критика t-423 08-13 и встречная Dog 14.08 — это ОДИН класс,
  здесь сведены; входящее Dog целиком — архив
  2026-08-16_boot-diet-extractions §6). Половина первая:
  `toolkit/tools/wiring_check.py` `_HOOK_COMMAND_RE` не парсит
  рекомендованную Anthropic форму `python "$CLAUDE_PROJECT_DIR/tools/x.py"`
  (только плоскую `python tools/x.py`), а enforcement_probe на этом
  fail-closed рубит коммит. Половина вторая: поэтому китовый
  `settings.json` нормализует хук-команды в ПЛОСКУЮ форму — кит толкает
  адоптера на менее cwd-устойчивый вариант, и сессия, стартовавшая не
  из корня, ловит ERROR хука (блок, не WARN) на `Task|Agent` и `Stop`.
  У Dog принято как есть со standing-констрейнтом «стартовать из
  корня». ЧИНИТЬ ОБА КОНЦА ОДНИМ БАТЧЕМ: фикс regex'а снимает половину
  класса. Класс «чекер диктует форму артефакта» — счёт 1, в реестр
  карты при рецидиве. Заодно проверить наш штабной твин тем же
  вопросом (какой формы наши хуки в settings.json).

- **ПОРТ NOTES LEN В КИТ — ОСОЗНАННОЕ ОТСТУПЛЕНИЕ ОТ ОСИ 10** (08-16,
  механизм t-447). Ось требует «кит-сиблинг тем же диффом»; здесь не
  так, причина: пороги выведены из НАШЕГО распределения, а перенос
  числа через границу деплоя — та самая ошибка, что вскрыла развилка
  символы/байты. Твин вдобавок поколением старше (combine_context на
  4 сегмента против наших 6). Порт минором: либо с порогами хоста,
  либо с пометкой «числа — образец, не мера».

- **NAMED-УЗЛЫ СЛЕДУЮЩЕГО МИНОРА КИТА** (мораторий D-0074; сюда же
  живой остаток ревью Codex — (в) дизайн режимов телеметрии):
  (а) owns_verify; (б) correlation-пара + negative_lint +
  claim_control_gate/search_control_gate (штабные хуки, в ките
  отсутствуют); (в) exam_fullgates — кит-сторона гейтов (+designer в
  AGENT_TIER корпуса, вердикт t-346); (г) designer-exam-gen только по
  слову; (д) hook_liveness_probe → кит (у кита свой состав
  settings.json, 9 хуков против наших 12); (е) из батча 08-05 — п.7
  гигиены, подклассы края R11, строка D-0099 RULE_COVERAGE в
  кит-твины.

- **ADVERSARIAL-ЭКЗАМЕН КООРДИНАТОРА** (верхняя граница F-28, класс
  pi_run_guard) — дорого, по слову оператора. NEGATIVE_LINT активен
  (кандидат в кит, обкатка).

- OWNS_GATE и GIVEN-PATH слой активны (warn-first; owns_verify —
  шаг приёмки D-0095; история обкаток — архив 08-05). ЖИВОЕ: подкласс
  owns №2 (счёт 3) — следующим батчем; given-path «чужекорневой
  контекст» — по рецидиву.

- Вотчдог зависших воркеров — НЕ строится (Rule #1), триггер
  rejected/tooling к окну №8. Онбординг D-0090: живые остатки у Dog в
  ИХ носителе. Trigger-gated кандидаты калибровок — владелец бэклога
  docs/RETRO_PATTERNS.md (читает чек 0); по слову ждут большой
  экзамен, critic-lite точка 2, gemini-кросс-аудит.

- **WP v0.2.3 ГОТОВ К ЧИТКЕ АРХИТЕКТОРА** (обновлён 08-16, коммит
  8c5be97; ROADMAP тем же ходом). ARCHITECTURE и ARCHITECTURE_BOOT
  проверены и правки не потребовали — имён моделей и устаревших
  версий в них нет. ЖДЁТ: собственно читка; плюс решение Архитектора,
  открывать ли новую фазу — ROADMAP фиксирует, что ни одна не открыта,
  но фазу не предлагает (это подпись, не запись файла).

- РЕЛИЗЫ КИТА: текущий v0.8.1 (публичный 1d9cbd5, снимок штаба
  1aa9085 — ревизия для D-0091-леджеров); прошлые v0.8.0/v0.7.0/v0.6.0.
  Мораторий D-0074 в силе. Владелец хода батчей — DAG
  docs/tasks/2026-08-10_kit-v0.8.0-full-mirror.md.
  **РЕШЕНИЕ ОПЕРАТОРА 2026-08-16: релиз КОПИТСЯ.** Недостающее
  ставится в кит ОДНИМ заходом, когда наберётся; отдельных микро-
  релизов не делаем. Следствие для очереди: всё «портом минором» ниже
  копится в один батч и не требует отдельных решений.
  **В КОПИЛКУ 08-16 (разбор входящего DOG, слово оператора):**
  (а) механизм INSTALLER-PARITY: установка/апгрейд кита сверяет
  парность правил и проверок ПО ФАЙЛАМ после host-restoration +
  протокол слияния дельты toolkit/CLAUDE.md (PATCH_PROTOCOL о
  CLAUDE.md молчит; класс — недовоз ec4e6f0 у DOG, ставили мы);
  (б) порт ОТ DOG: dod_track «фоновый запуск ≠ прогон» (их фикс
  размещён и отревьюён — наша очередь с 08-14 закрывается их
  замером), owns_gate guard одиночного `/` в is_path_like_token,
  verdict-блок (их critic.md rule 17) в toolkit critic.md — код
  critic_verdict_check в ките уже есть, доки в роли нет;
  (в) scout-exam-gen rewrite (F-A..F-I) + critic-exam-gen тем же
  классом + ключ вне дерева (их D-0005) — через DAG
  docs/tasks/2026-08-16_dog-delivery-scout-exam.md, узлы B6/B7.
  **КРОСС-ЗАПИСЬ В AO3 И DOG ОТЛОЖЕНА ТЕМ ЖЕ СЛОВОМ** — их репо
  сейчас заняты (у AO3 живая сессия, 28 грязных путей на 08-16).
  Накопленное им передаётся при безопасном касании их носителей
  (D-0060/D-0082), не раньше. ЖИВОЕ:
  конвергенция архитектур wiring_check двух сторон — в очередь;
  наблюдение о латентной pytest-коллизии одноимённых модулей
  штаб/кит (канонформы границу не пересекают).

- **ЭКЗАМЕНЫ И КИТ — три живых пункта** (находка «раскол экзаменов»
  ОТОЗВАНА 08-14: генераторы в ките ЕСТЬ, недопоставки нет; разбор —
  архив 2026-08-16_boot-diet-extractions §3):
  (1) ФОРМА ПОСТАВКИ неединообразна: LEAD_RANKING/DEPLOYMENT_ECONOMY
  едут содержимым, critic/scout — генераторами; нигде не записано как
  решение — назвать правило либо привести к одной форме.
  (2) ОБРАТНЫЙ hq-drift: генераторы есть у кита и отсутствуют у штаба
  — кандидат порта кит→штаб, направление, которого парити-манифест не
  описывает.
  (3) **РЕШЕНО оператором 2026-08-16: порог.** Мало материала →
  экзамен ОТЛОЖЕН (не отменён и не подменён образцом). Форма легла в
  существующий слот леджера `deferred(<триггер>)`; экзамен получил
  пререквизит «материал» из трёх частей (прогоняемый набор со
  стабильным счётом; родня ≥2 файлов; ≥20 исходных файлов — приор, не
  замер). Норма — PROCESS/README. ОСТАЁТСЯ РАБОТОЙ: (i) перевзведение
  в wiring_check кита (детектор: строка `deferred`, чей триггер стал
  истинным); правило едет в кит ТОЛЬКО тем же батчем, иначе отгружаем
  пожелание; (ii) прогнать оба генератора на пустом репо и увидеть
  фактическое поведение — сейчас утверждается отсутствие ОПИСАННОЙ
  ветки, а не наблюдённое.

- ОЧЕРЕДЬ ИЗ НАХОДОК №19: (а) упрочнение headless-протеза (деливерабл
  ограничен клеткой); (б) M2-регресс t3 — чек-23 материал; (в) область
  given-path слоя в клетках — по рецидиву; (г) порт эпох
  hygiene_gate/journal_echo в exam-кит (отдельная оценённая задача).

- **SPEND-МОНИТОРИНГ v1 — ЗАКРЫТ 08-13**; владелец хода — DAG
  docs/tasks/2026-08-13_spend-analytics.md, журнал t-416..t-429.
  Инструмент: `python tools/spend_report.py --window last --compare
  prev` (+`--dashboard`). ТРИГГЕРНАЯ ОЧЕРЕДЬ, полные тексты в
  носителе: (а) OTel-приёмник — триггер: рецидив RUNS_WITHOUT_UNITS на
  калибровке (дисциплина реестра не прижилась → замена кодом, D-0063)
  ЛИБО скилл-разрез вне пятёрки ЛИБО слово; (б) GPT/Codex-адаптер по
  факту GPT-подписки (§8а носителя); (в) кит-порт spend-слоя минором
  (D-0074); (г) класс-кандидат «агрегат по ключу N:1» — в реестр карты
  при втором экземпляре.

- Evidence-gated residuals — 9 пунктов на своих триггерах, список
  VERBATIM: docs/task_reports/2026-07-16_evidence-gated-residuals.md.
- STANDING-ТРИГГЕРЫ ИЗ ЗАКРЫВАЮЩЕГО КОММИТА PHASE 2 (07-23,
  обязанность F-48/D-0082 — триггер в архиве не передан):
  (а) per-unit worktree-изоляция (GSD, отвергнута Rule #1) — reopen:
  реальный параллельный объём диспатчей + инцидент коллизии путей
  (класс D-0060); (б) LLM-роутер-кандидаты (6 отвергнуты двумя
  волнами обзоров) — reopen: ≥100 размеченных примеров;
  (в) большая параллельность как workstream — reopen: числа
  P1-класса (задачи ≥5 событий / ≥2 сессий) показывают переполнение
  координационных артефактов.
- Eval plan stage 2 — цикл №1 07-13 (d90cd03); остаток:
  minimum-n/pass^k + numeric agreement; Batch API по триггеру
  «реплеи регулярны»; NOT taken: per-PR CI, bench-harness.
- NOT adopted (чтобы не пересуживать) — владелец списка и обоснований
  docs/RELATED_WORK.md («NOT adopted», строка ~473 + OpenClaw survey).

## КРОСС-ДЕПЛОЙНОЕ (D-0082) — живое

- **НЕ ПЕРЕДАНО В AO3 (слово оператора 08-16 «их репо не трогать»),
  явная строка по правилу 3а handoff:** (1) их класс «защита
  объявлена тотальной, реализована частичной» ЗАМЕРЕН у нас — 3
  попадания в 3 файлах, F-61, ось карты заведена; (2) метод-урок к
  нему: вопрос «держится ли гарантия» есть СУЖДЕНИЕ о путях кода, и
  ярус поиска сводит его к «есть ли общий try/except» — у нас scout
  вернул 0 в 16 файлах и был отклонён, critic по 4 файлам вернул 3.
  Обоим деплоям это полезно до того, как они пошлют тот же вопрос
  разведчику. ТАКЖЕ: в их дереве лежат ДВА моих коммита (1092a08,
  d65e303 — кросс-пункт 08-16 и поправка масштаба), НЕ запушенные.
  Передать при безопасном касании их носителя.

Все входящие AO3 по 2026-08-16 включительно РАЗОБРАНЫ; тексты — архив
2026-08-16_boot-diet-extractions §4–§5. Живых остатка два, оба —
нерешённые кандидаты осей карты:

- **ось-кандидат «координатор закрывает цикл по снимку начала цикла»**
  (их прецедент 08-03: три отсрочки готовой работы за день; у них
  закрыто правилом 8а + ре-скан 4а + чек 3а).

- **ось-кандидат «защита объявлена тотальной, реализована частичной»**
  (узкий except / приведение типа ВНЕ охраняемого читателя; исключение
  уходит наружу через уже взятый лок; порог ≥2 файлов выполнен их
  heartbeat_wrap + loop_lock).

**КЛАСС «ИМЯ МОДЕЛИ ПРИБИТО КАК КРИТЕРИЙ ВМЕСТО РЕЗОЛЮЦИИ ПРИВЯЗКИ» —
владелец текста реестр режимов отказа docs/SIBLING_MAP.md** (три
носителя: протокол калибровки, savings_report, само ядро). Оба
кросс-пункта AO3 08-15, код-экземпляр и четыре экземпляра в ядре
закрыты 08-16 (cc31cdd, 8fae98e, da54ec5, 5437d54; ответ в их
носитель — их 1092a08, d65e303).
ОТКРЫТЫЙ ОСТАТОК В ЯДРЕ, требует отдельного решения (находка критика
t-446, F2в — до неё остаток жил только в сообщении коммита, то есть
по F-48 передан не был): секция CLAUDE.md «Role ≠ tier» несёт имена
моделей трижды и охраняется F-31 «do not compress» — привести её к
привязке нельзя молча, защита покрывает именно эту секцию; плюс
колонка «Model here» таблицы ярусов — второй носитель привязки рядом
с delegation.config.yaml, снятие = перестройка таблицы, а не сжатие
истории.
ХВОСТЫ: (i) пометка разрыва базы в spend-аналитике — M14/D7 зовут ту
же функцию через алиас и стали ровно вдвое меньше (точное следствие,
цены отличаются ровно в 2 раза); (ii) кит-твин
`toolkit/tools/savings_report.py` остаётся на Fable-базе — релизной
очередью (D-0074), и там ветка «конфига нет» становится ОСНОВНОЙ, это
отдельное проектное решение; (iii) чек 6 кит-твина несёт дофиксовую
форму — тем же минором. Детектор класса (хвост чека 5) покрывает
ТОЛЬКО текст протокола, код-носители не покрыты — открыто честно.
ЗНАТЬ ПРИ ЧТЕНИИ ТРЕНДА 18(б): база контрфакта сменилась, точка №7
завышена (считалась по Fable вдвое дороже истины), №6 верна —
перепривязка попала внутрь её окна.

## Archive (D-0038)

docs/task_reports/README.md — единственный владелец описаний архива.
