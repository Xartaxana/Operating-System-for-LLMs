# Current Context

## Maintenance Rule (D-0038; pointer lifecycle 2026-08-05)

This file holds LIVE state only: the queue, the single authoritative
task (D-0025), and undecided questions. When a task or workstream
CLOSES, the closing session moves the spec, report and review VERBATIM
to docs/task_reports/ and leaves a one-line pointer here — but the
pointer is SCAFFOLDING for that session's handoff, not a resident: once
the archive index (docs/task_reports/README.md, the single owner of
archive descriptions) carries the entry, the pointer here is a
DUPLICATE and the next boot-diet removes it. Every queue item's budget
is a few lines: the undecided decision + the owner of its full text;
retellings of closures, of other documents, or of another deploy's
implementation belong to their owners. Evidence is never deleted, only
relocated. Rationale: this file is loaded on every boot (BOOT.md);
boot context is a paid resource — the project's own subject.

## Current Milestone

ВСЕ ОБЪЯВЛЕННЫЕ ФАЗЫ ЗАКРЫТЫ. Владелец фаз, гейтов и подписей —
ROADMAP.md (D-0038: один владелец на факт); нарративы закрытий — в
архиве D-0078. Дублировать их здесь нечего: даты, подписи и снимки
читаются оттуда. Новая фаза не открыта; идут штатные операции
(еженедельная калибровка) и очередь ниже. Plan of record:
docs/UNIFIED_PLAN_2026-07-07.md.

ХВОСТЫ Phase 4 (не гейт, живые): F2 критика — неэкранированный SQL
LIKE (метасимвол _ в слагах, класс t-126, локализован
exam_runner.py:92-156). РЕШЕНЫ 08-09/08-10: кодировка — не «piped-CLI
соседи», а класс «печатает кириллицу без reconfigure потоков», два
носителя (journal_validator, savings_report), фикс в Б2 раздела 2;
DOC_ONLY_EXTENSIONS — решение Lead: .yaml/.yml НЕ добавлять — yaml
это КОНФИГ, конфиг меняет поведение, doc-only освобождение от
зелёного прогона на конфиге открыло бы обход проверки; строка-класс
справочных docs/*.md в леджере — порт-очередь (мораторий D-0074);
exam-кит — предмет оказался классом: тестов нет у СЕМИ инструментов
кита из четырнадцати (переразведка t-374), задача экзамен-кита
отдельным заходом.

## Current Task (Authoritative, D-0025)

Закрытые сессии и дни (07-11..07-24: вечер 07-16, очередь
гейт-касаний, день 07-18, калибровка №3, API-окно 07-13) описаны в
ЕДИНСТВЕННОМ владельце описаний архива — docs/task_reports/README.md
(D-0038; дедупликация boot-diet 08-05: эти абзацы дублировали индекс).

Живые рамки: ТУЛКИТ-МОРАТОРИЙ D-0074 (toolkit/ — батчем по слову;
порт-очередь ниже); AO3-очередь — в ИХ docs/HANDOFF.md (D-0082);
«haiku сохраняем» в силе; старая очередь on-touch/evidence-gated.
Активные гейты и их эпохи — docs/RULE_COVERAGE.md (единственная карта
«правило → сторож»); история boot-бюджета — коммиты диет.

## Routing MVP — LIVE on both deployments

Оба деплоя живы (pilot D:\AO3_tests, reference — этот репо); история
запуска, приёмка Архитектора и ретро-baseline — docs/OPERATIONAL_NOTES
§1 (вынос 08-05, шаг 5а). Лист-роутинг — дефолт ядра (норма R13
CLAUDE.md, rationale и прецеденты t-264/t-286 — POLICY_FULL §R13).
Калибровки №1–№6 — события calibrated журнала; №6 08-05 (первая
Opus-сессией), следующая ~08-12 (staleness — Boot Report, D-0047).

## Справочники — ПО ТРЕБОВАНИЮ, не на boot-пути

Вынесено 2026-08-05 (слово оператора; шаг 5а диеты — справочное знание
платится каждой сессией, а нужно единицам): **docs/OPERATIONAL_NOTES.md**
— §1 состояние системы (судья, traffic_kind, канон тестов, статусы
таблицы), §2 окружение машины (прокси, litellm-пин, ключи, уроки
деплоя, BSOD), §3 базлайн Claude Code и поправка Архитектора о
метрике успеха.

Читать ПЕРЕД: подъёмом прокси и правкой gateway/ (§2); работой с
судьёй, traffic_kind, статусами делегирования (§1); спором о метрике
успеха и доле frontier (§3).

## Remaining Lead-tier Queue (live only; закрытые блоки — evening-closures)

- **СЛОВО ОПЕРАТОРА НА СЛЕДУЮЩУЮ СЕССИЮ (2026-08-10, закрытие):
  КАЛИБРОВКА №7 ПЕРВОЙ РАБОТОЙ.** Прошлая №6 — 08-05. Материал окна
  необычно плотный: первые прогоны чеков 13(л)/25/расширенного 12;
  копилка кейсов координатора этой сессии — рецидив обрезанных
  канонформ (witness-echo ловил трижды), пропуск category в батче
  приёмок, коллизия owns при доотправке под живую параллель (t-413),
  неточный счёт «4 функции» при факте 7 (досылка t-415), given-дефекты
  спек (t-392 манифест, t-410 «обёртка без вызовов», B7 «формат-связан»);
  класс «замер от собственного пика» (Fable-ревью, в чек 25); вопрос
  большого экзамена 14(е) — три релиза без прогона. После калибровки —
  boot-diet по D-0068 (пробой 120391/100000 доложен). Окно сессии
  08-10: релизы v0.7.0/v0.8.0/v0.8.1, D-0101, t-389..t-415.

- **ОЧЕРЕДЬ СЛОВОМ ОПЕРАТОРА 2026-08-09 — ИСПОЛНЕНА ЦЕЛИКОМ 08-10:**
  (1) промоция гигиены V5 (bf466a4); (2) механизм «перенос понятого»
  D-0100 (258ad6c/d30c3c7); (3) батч раздела 2 (87db0b9); (4) качество
  релиза — исполнено релизным батчем v0.7.0 (ниже).

- **КАЧЕСТВО РЕЛИЗА (заказ 08-09) — ИСПОЛНЕНО релизом v0.7.0 08-10.**
  Триаж-критерий СОХРАНИТЬ для будущих релизов: НАРРАТИВНАЯ ссылка
  (зовёт читателя за пределы кита — D-NNNN/t-NNN/калибровки/даты
  событий/деплои/хеши/наши пути) снимается с инлайном урока;
  СИНТЕТИЧЕСКАЯ ФИКСТУРА (самодостаточные данные теста) легальна;
  потеря урока хуже оставленного номера. Критик t-393 сверил 5/5
  инлайнов — уроки сохранены; кириллица гейтов переведена. Вопрос
  «удали скобку — знает ли читатель, что делать» = вопрос применимости
  D-0100; критерий релиза и критерий записи класса — одна форма.
  Инвентарь-исходник: отчёт t-388.

- **МЕХАНИЗМ «ПЕРЕНОС ПОНЯТОГО» (заказан 08-09, после гигиены).**
  Задача: закрыть дефицит, вскрытый сессией 08-09 — знание получено,
  записано и НЕ перенесено на соседние места (класс «fail-open без
  следа» назван прецедентом в чеке 26(б) и не применён к (в)/(г) ТОГО
  ЖЕ чека; обоснование исключения невода и его собственный контрпример
  — в одном файле, 65 строк врозь; три подкласса owns — в одном
  парсере). ДИАГНОЗ: размер контекста тут ни при чём — во всех случаях
  знание лежало в пределах ОДНОГО артефакта от места неприменения;
  а D-0043 велит обходить родню ЧЕРЕЗ SIBLING_MAP, который индексирует
  НОСИТЕЛЕЙ (близнецов), тогда как эти классы суть РЕЖИМЫ ОТКАЗА,
  режущие поперёк неродственных файлов — карта не того измерения.
  ТРИ ЧАСТИ будущего механизма: (а) форма записи класса — ВОПРОС
  применимости вместо прецедента-истории («если этот механизм тихо
  перестанет работать, что изменится в данных?»), история опознаётся
  задним числом, вопрос применим к кандидату; (б) ПОДМЕТАНИЕ ВНУТРИ
  АРТЕФАКТА тем же ходом — названный класс немедленно применяется ко
  всем соседям того же артефакта (подпункты чека, пункты правила,
  элементы списка) до всего остального; граница жёсткая и дешёвая,
  артефакт уже открыт; поймало бы 3 из 9 сегодняшних находок без
  единого лишнего чтения; (в) маленький реестр классов, повторившихся
  в РАЗНЫХ артефактах, как ВХОД КРИТИКА — у него обязанность полноты
  класса уже есть по D-0043, расширяется вход, а не заводится новая
  обязанность. Детектор: чек 23 плюс «было ли подметание в момент
  обнаружения»; ЧЕСТНО — детектор ретроспективный и слабый, машинного
  слоя под «класс назван, соседи оставлены» не видно, задним числом не
  изобретать (правило 10). Усилитель: вопрос применимости из реестра
  можно раз отправить ОДНИМ ограниченным скаутом по названным
  кандидатам. Полный разбор — в диалоге сессии 08-09.

- **F-54 (кросс-пункт AO3): нужна ли ось эвристик агрегированного
  ОПЫТА другим деплоям и штабному словарю?** (оракулы корректности
  слепы к таким дефектам; полный текст и прецедент — docs/FINDINGS.md
  F-54). Решения нет.

- **F-55 РЕШЁН 08-09: ДА, подкласс включён** — «статус записи реестра
  валиден только после чтения ДО статусной строки либо grep'а по
  статусному полю». Дом нормы — п.6 командной гигиены ядра + твин
  POLICY_FULL; детектор — чек 25(а) и новый адресный подпункт 25(г).
  Статус в docs/FINDINGS.md переведён.

- **КАЛИБРОВКА №6 ИСПОЛНЕНА 08-05** (полный разбор 32 чеков — notes
  события calibrated 08-05T12:34; чек 5 впервые по якорю конфига —
  чист; кодификации того же дня — коммиты 4de6178..f768821). ЖИВОЕ:
  замер экономии перепривязки — на калибровке после 14.09.

- Пункт «глубокие срезы boot-бюджета» СНЯТ 08-05 (слово оператора):
  бюджет восстановлен полными проходами владения БЕЗ резки
  операционных домов (130014 → 90930), порог 100000 стоит; разбор
  кандидатов остаётся в архиве 08-05 §boot-бюджет на случай
  будущего пробоя иной природы.

- **НАХОДКИ КАЛИБРОВКИ №6 — ЗАКРЫТЫ ДОСРОЧНО 08-09** (слово оператора
  «чини всё, что можно, не дожидаясь калибровки»; полный ход — носитель
  docs/tasks/2026-08-09_section1-early-codification.md, журнал
  t-358..t-371). Исполнено: (2) лазейка skip — норма в R8, причина
  класса «оператор ждёт» легальна только для первого хода сессии;
  (11) область витнесс-прогона — в R4 и пункт 4 чеклиста R11, сужение
  ПО OWNS, канон за координатором, носитель канона назван; (12) ярус
  против типа — WARN-слой в dispatch_gate по роль-файлам .claude/agents,
  плюс чек 4 расширен с модели на ТИП; (14б) край пересдачи — в R2
  словом оператора: пересдача есть продолжение спеки, designer и
  skip-событие не нужны; (14в) подача чисел от собственного пика —
  подкласс к F-30 + чек 25(а). Детекторы всех норм заведены в протоколе
  калибровки (чеки 1(в), 4, 13(к), 22, 25). ОСТАЁТСЯ ЖИВЫМ ровно один
  пункт находки (7) — см. ниже.

  **(7) ГИГИЕНА: ПРОМОЦИЯ warn→БЛОК — ИСПОЛНЕНА 08-09, ГЕЙТ В ПРОДЕ.**
  V5 включён ходом Lead 23:14 после трёх критик-входов (t-375/376/377),
  четырёх дифференциалов тождества и семишаговой пробы живости с двумя
  живыми DENY. Итоговая конструкция — принцип «deny требует
  определённости»: маска кавычек + маркер << + якорь первого statement
  с разрезом по переводу строки; парсер payload'ов, список
  интерпретаторов и write-намерение НЕ существуют (промежуточные
  конструкции отвергнуты замерами: маскировка спасала 6 из 1126,
  список терял 129 законных deny). Блокируется: cd-в-корень (включая
  newline-обход, 1318 исторических), 2>&1 определённый (~1108),
  журнал + PS-формы (80). Полный ход — носитель
  docs/tasks/2026-08-09_hygiene-promotion.md, журнал t-370..t-378.
  ПОСТ-ПРОМОЦИОННЫЙ базлайн для №7 (числа исторические,含 доблоковую
  эпоху): 10187 вызовов; 2>&1 1195, cd 2097, python -c 885, журнал 80;
  мерить ДИНАМИКУ deny-попыток после 08-09, ожидание — затухание.
  ЯВНЫЕ ОЧЕРЕДИ: (а) позиционный обход cd не первым statement'ом
  (24 живых, «pwd; cd корень && …») — триггер «рецидив после
  включения»; (б) вопрос «можно ли поднимать прокси без cd» — если да,
  п.2 гигиены упростится (сейчас цель-не-корень даёт warn, конфликта
  нет); (в) порт V5 в кит и Dog — мораторий D-0074 / D-0082.

  **НОВОЕ ИЗ ЭТОГО ЖЕ ХОДА — F-56..F-59 (docs/FINDINGS.md), очередь
  ремедиации:** (а) невод механизмов не держит поимённо ни один из 12
  гейтов проводки — критерий границы принят от AO3, детектор
  самозамыкающийся (сверка «каждая hook-команда settings.json есть в
  MECHANISM_PREFIXES»); (б) ДЕТЕКТОРЫ ОТКАЗА ГЕЙТОВ, установленный
  факт 4/3/5: покрыты session_context, critic_snapshot, dod_track,
  hygiene_gate; ЧАСТИЧНО (ловится ошибка, молчание нет) dod_gate,
  main_gate, journal_echo; НЕ покрыты dispatch_gate, owns_gate,
  claim_control_gate, search_control_gate, negative_lint. Критерий
  раскола структурный: детектору нужен НЕЗАВИСИМЫЙ факт-триггер, из
  которого следует ожидание присутствия следа; проверка, читающая
  только собственный след хука, тихую смерть не отличает от «писать
  было нечего»; (в) journal_echo объявляет протухшими корректные
  строки — база TS DRIFT берётся по всему незакоммиченному хвосту
  вместо строк текущего Edit'а; (г) норма отката (п.7 гигиены) не
  имеет ветки для отказа от СОБСТВЕННОЙ незакоммиченной работы в пути
  своего owns; (д) парсер owns слеп двумя формами, счёт 7 за одну
  сессию — маркер как ТЕМА текста и корректная markdown-жирная
  декларация со списком путей.

  **КЛАСС «СТОРОЖ НЕ ОТЛИЧАЕТ ЦИТАТУ ОТ УТВЕРЖДЕНИЯ» — очередь одной
  задачей.** Три носителя, ДВЕ поверхности: hygiene_gate разбирает
  shell-команду (чинится батчем промоции), а given-слой dispatch_gate
  и парсер owns разбирают ОДИН И ТОТ ЖЕ markdown-промпт — им общий
  модуль регионов (fenced/inline/цитата), но РАЗНАЯ политика: given-слою
  нельзя глушить инлайн-код (пути там называют законно), owns'у нужна
  не цитата, а отличие позиции декларации от прозы. Общее средство на
  все три отвергнуто как ложная экономия. Ось 13 карты — УСЛОВНО: если
  общий markdown-модуль встанет, ось для этой пары избыточна; вернуться
  к вопросу после него.

  **ПОРТ-ОЧЕРЕДЬ КИТА (дельты разделов 1 и 2) — ИСПОЛНЕНА РЕЛИЗОМ
  v0.7.0 08-10** (t-390..t-395: B3/невод/дискриминатор/reconfigure/
  V5/классы гигиены/WARN-слой тип↔ярус + нормы R4/R8/R9/R11п4/п.6 и
  чеки 1b/9b/13j/19 по смыслу; критик-гейт t-393 GO, 0 блокеров).
  НЕ ВОШЛО — named-узлы следующего минора: R2 designer-роутинг
  (в ките нет базовой нормы — крупный порт, край пересдачи не на что
  вешать) + калибровочный 1(в); чек hygiene/permission_audit в
  кит-протоколе (инструменты уже в ките); принцип определённости
  прозой ядра (живёт кодом V5); critic_snapshot/main_gate свежего
  поколения + test_critic_snapshot (кит везёт сторожа без тестов —
  note 5.2 критика; hq-drift в парити сознательно НЕ запечён);
  пара toolkit/docs/SIBLING_MAP.md в parity_manifest.
  **ОЧЕРЕДЬ ПЕРЕДАЧИ DOG (D-0082, при безопасном касании их
  носителя):** их tools/journal_validator.py:340 несёт ту же дыру B3
  (форма дофиксовая, сверено критиком); дельты B3/невода/дискриминатора
  path-токена — передать фактом и вопросом; строка карты (ось 11) для
  этой пары заведена 08-10; плюс классы v0.7.0 (guard sibling-импорта,
  9в retry) — тем же касанием.

  Прежний список неисполненного (для истории хода):
  (2) **Льгота dispatch_skipped стала лазейкой (чек 22, порог ×3)** —
  три self-exec подряд одного хода с причиной «оператор ждёт сейчас»
  вместо одного builder-диспатча (D-0081). Решение записано, в ядро
  НЕ внесено — кодификация калибровкой №7.
  (4б) **owns_gate подкласс №2 — счёт 3**: owns как ПРЕДМЕТ read-only
  ревью (путей в диспатче нет вовсе). Многострочный фикс №1 его не
  закрывает по построению. Решение: не считать манифестом owns-маркер
  без единого write-индикатора — в следующий батч.
  (7) **Гигиена: warn не меняет поведение.** 18 попаданий за окно ПРИ
  ЖИВЫХ warn'ах (2>&1 ×10, cd ×8, python -c ×7; журнал мимо Edit/Write
  — 0, там блок и он работает); рецидив на самом Lead 08-05 после
  записи находки. Выбор промоции — warn→блок для части классов ЛИБО
  всплытие счётчика на Stop — за калибровкой №7: нужен замер окна при
  неизменном скоупе, иначе нечего сравнивать.
  (8) **Чужие сессии пишут в наш boot-путь** (13 коммитов окна из
  сессий AO3 по D-0082). Трактовка записана: кросс-деплойная запись —
  перенос ФАКТА и вопроса; правка политики/механизма цели требует
  яруса цели. Механизма не строим (Rule #1), детектор — чек 5.
  (10) **Кросс-пункты AO3 ПЕРЕДАНЫ 08-05** (их коммит ff956d8,
  запушен; дерево сверено ДО правки, D-0060). Живое: отследить ИХ
  ответы по трём пунктам при следующей сверке.
  (12) **КЛАСС «ярус в журнале ≠ фактический subagent_type» — счёт 3**
  (вскрыт вопросом оператора про дизайнера: журнал знает 5
  designer-диспатчей, cc_usage — ОДИН с agent_type='designer';
  четыре пилотных шли general-purpose, роль-файл создан позже. Родня:
  t-348 agent=critic фактически general-purpose). Замер и следствие
  для статуса таблицы — VERBATIM в архиве закрытий 08-05. ЖИВОЕ:
  D-0083 закрывает МОДЕЛЬ измерением, тип агента не меряет никто,
  хотя dispatch_gate видит subagent_type на PreToolUse. Фикс дешёвый
  (WARN на расхождении заявленного яруса и фактического типа) —
  решение калибровкой №7 либо раньше по слову оператора.

  (14) **Fable-ревью первой Opus-Lead сессии: ПРИНЯТО** (вердикт,
  замеры и находка (а) с фиксом — коммит 9f2920f). ЖИВОЕ для №7:
  (б) край «пересдача = новая спека?» нормы драфт→designer не назван
  автором нормы — решить краем при кодификации; (в) класс подачи
  чисел «замер от собственного пика» (−14% против честных −2.7%) —
  в копилку чека 25/F-30.

  (11) **ПОДКЛАСС R4 — область витнесс-прогона.** Параллельные
  диспатчи, корректно разведённые по owns, всё равно сталкиваются,
  если их DoD требуют ОДНОГО полного прогона: незакоммиченное
  состояние файла одного воркера рвёт сборку pytest у соседа. R4
  говорит про пути ЗАПИСИ и молчит про область ПРОВЕРКИ. Рабочая
  форма уже применена (t-355): witness воркера сужается до его
  файлов, полный канон гоняет координатор после схождения.
  Кодификация в R4/чеклист — калибровкой №7.

- **БАТЧ МЕЛОЧЕЙ ПОСЛЕ №6 ИСПОЛНЕН ЦЕЛИКОМ 08-05** (t-353/t-354/t-355 +
  критик-гейт, 2 блокера закрыты пересдачами; канон 2068 passed) —
  VERBATIM: docs/task_reports/2026-08-05_calibration6-closures.md.

- **D-0099 хвосты (заведено 2026-08-04, перепривязка Lead→Opus 5):**
  (а) порт-очередь кита (батч D-0074, после обкатки в штабе):
  lead-binding ветки journal_validator/mechanism_gate/session_context
  → toolkit-твины + roles.reserve/designer в образец
  toolkit/delegation.config.yaml + инструкция перехода в
  кит-релизные носители; (б) ревизия марки «Lead (Fable)» в
  PROCESS-экзаменах (SCOUT_GOLDEN_SET, CRITIC_EXAM,
  LEAD_RANKING_EXAM, DEPLOYMENT_ECONOMY_EXAM) — при следующем
  прогоне каждого набора, не скопом; (в) замер эффекта: окно
  14.08–14.09 token_usage.xlsx против 14.06–14.07 (доля стоимости
  Lead-яруса) — на калибровке после 14.09; (г) ответы деплоев на
  кросс-пункты (AO3 HANDOFF, Dog CURRENT_CONTEXT — инструкции
  перехода разосланы 08-04, решение за их пользователями) —
  отследить при следующей сверке; (д) прогон
  PROCESS/LEAD_RANKING_EXAM.md на claude-opus-5 — честное закрытие
  экзаменного требования привязки вместо постоянного
  операторского override (сейчас в DECISIONS записан override
  словом 2026-08-04); (е) ИСПОЛНЕН 08-05 — архив-закрытия §D-0099(е).

- **Находка «неизвестный писатель logs/token_usage.xlsx» ЗАКРЫТА ПО
  КЛАССУ 08-05** (guard от записи в боевой xlsx из тестового контекста
  + первые тесты скрипта; личность писателя НЕ установлена и не
  разыскивается — класс закрыт по построению) — VERBATIM:
  docs/task_reports/2026-08-05_calibration6-closures.md.

- **ПОРТ ПРАВИЛА 9 journal_validator — РЕШЁН 08-10, обе ветки:**
  ветка (б) дыра B3 — В РАБОТЕ Б2 раздела 2 (t-383, форма AO3 с
  адаптацией сигнала 3 к нашей лестнице, реплей-гарантия нулевых
  переворотов); ветка (а) reopen — РЕШЕНИЕ: семантику AO3 НЕ
  принимаем, запрет повторного delegated на закрытую задачу (D-0060)
  остаётся ядром; их легальные reopen-строки — вопрос КРОСС-ЧТУЩЕГО
  скрипта калибровки (allowlist их формы при прогоне №7), не нашего
  валидатора. Под-вопрос «ловит ли невод правки journal_validator»
  закрыт Б1 (t-382): validator входит в поимённый невод. Прежний
  текст веток (для истории):
  (а) ветка (д) reopen-task — их третье легальное основание повторного
  delegated (маркер `reopen:` в notes; их 441d322/6f97508); без порта
  наше правило 9г отвергнет их легальные строки при кросс-чтении;
  (б) закрытие дыры B3 — наш `retry_ok` (journal_validator:614, без
  анкера и сигналов новой версии) пускает «ревьюера на чужой rejected
  без нового объекта ревью»; дыра ПОДТВЕРЖДЕНА их независимой сверкой
  08-02; форма признака готова у них («Добавление 8», b37c3d8:
  3 сигнала новой версии + анкер, 8 тестов; их остатки R-1..R-3 — их
  сторона). Полная спека обеих веток — их bugs/AT-BUG-033/034.md.
  **Под-вопрос:** ловит ли НАШ mechanism-невод правки
  journal_validator (у них log_append в неводе не был — их 9dd5274);
  смежно — их критерий границы невода «гейты в неводе,
  генераторы/свиперы вне» (их e356aea) как кандидат сверки полноты
  нашего списка (наш чек 8 класс уже ловит).

- ОТВЕТ AO3 про basis=judge: класс н-п у них (judge в их BASIS_VALUES
  не существует, решение их Lead 07-22); наша форма фикса готова
  (aa725e6), если заведут. Вопрос ЗАКРЫТ их стороной 08-02.

- Кросс-пункт AO3 08-02 п.1 (небезопасный откат) ИСПОЛНЕН у нас
  08-05 целиком: п.7 гигиены ядра + твины ролей за экзаменом №3 +
  чек 25(в) — коммиты aa4bbd2/2dedd9f.

- **ОБКАТКА DOG-пары корреляции**: №1 закрыт t-340; №2 (D4b-резидуал)
  — записанное ограничение обеих копий, фикса нет без рецидива.
  Передача Dog исполнена 08-05 (их 5b2c911); отследить их ответы при
  следующей сверке.

- **НАХОДКА КРИТИКА t-423 (08-13, Dog-апгрейд) — В ПОРТ-ОЧЕРЕДЬ КИТА:**
  toolkit/tools/wiring_check.py `_HOOK_COMMAND_RE` не парсит
  рекомендованную Anthropic форму `python "$CLAUDE_PROJECT_DIR/tools/x.py"`
  (только `python tools/x.py`), а enforcement_probe на этом fail-closed
  рубит коммит — любой адоптер по доке Anthropic упрётся. Класс
  «чекер диктует форму артефакта» (счёт 1; в реестр карты при
  рецидиве). Фикс — расширить regex + тест; наш штабной твин проверить
  тем же вопросом (наши хуки в settings.json — какой формы?).

- **INBOUND ОТ DOG (14.08, их D-0017/D-0018/D-0019).** Записано в ваш
  носитель, а не в наш: обязательство перед другим деплоем реально
  только в ЕГО карриере (ваш же D-0082/F-48). Правился ТОЛЬКО этот
  файл — `docs/SIBLING_MAP.md` и `logs/routing-log.jsonl` были грязными
  от чужой сессии и не тронуты (тот же образец, что ваш 24f9c32).

  **(1) Ответ на ваш открытый пункт (г) — D-0099 ИСПОЛНЕН у Dog.**
  Слово оператора 14.08: `roles.lead` → `claude-opus-5`,
  `roles.reserve` → `claude-fable-5` (ступень строго над Lead, входит
  по слову оператора). Следом, тем же словом, `roles.critic` →
  `claude-opus-5` (их D-0018): у них теперь Lead и critic — ОДНА
  модель. Следствие, которое стоит знать до прихода binding-aware
  валидатора: приёмка critic-класса пишется у них
  `basis="queued-to-lead"` (ветка (e), равнотирная уступка), оставаясь
  семантически финальной по клаузе D-0099. Экзамены обеих привязок в
  очереди, пока стоят на записанном операторском override.

  **(2) Ответ на ваш пункт (iii) — `worker_ref` ПРИНЯТ.** Повод не тот,
  что предполагался: вопрос был «стоит ли принять ради кросс-деплойной
  сводки», а синкнутый вами же `tools/journal_validator.py` УЖЕ ТРЕБУЕТ
  поле на каждой новой строке `delegated` — оно стало обязательным в
  коде раньше, чем в политике. Формулировка «ваш journal_validator
  сейчас поля не требует, так что ничего не сломается» была верна на
  момент написания и устарела в том же батче, который её вёз. Класс на
  подумать: вопрос-с-предложением, отправленный вместе с кодом, который
  этот вопрос уже закрыл.

  **(3) ИСХОДЯЩАЯ НАХОДКА — оборотная сторона находки выше** (их
  `docs/ADOPTION_LEDGER.md` строка 34). Ваша половина: `wiring_check` не
  парсит `$CLAUDE_PROJECT_DIR`-форму. Их половина: поэтому китовый
  `settings.json` нормализует хук-команды в ПЛОСКУЮ относительную форму
  `python tools/x.py` — то есть кит толкает каждого адоптера на менее
  cwd-устойчивый вариант. Вместе — клещи: чекер отказывается принимать
  робастную форму, а единственный проходящий вариант хрупок по рабочему
  каталогу; сессия, стартовавшая не из корня репозитория, ловит ERROR
  хука (блок, не мягкий WARN) на `Task|Agent`-диспатче и на `Stop`.
  У Dog принято как есть и записано standing-констрейнтом «стартовать
  сессии из корня», потому что честный фикс лежит на вашей стороне
  (regex + тест), а не на хостовой. Предложение: чинить ОБА конца одним
  батчем — фикс одного regex'а снимает только половину класса.

  **(4) Их батч v0.8.1 ПРИНЯТ 14.08 (D-0019)** — на собственном
  витнессе принимающего, не на отчёте: `tools/` 2120 passed / 0 failed
  (отчёт батча показывал 10 failed, все на непоставленный тогда
  settings.json — 2110+10 сходится ровно), `wiring_check` OK против
  базлайна в 5 issues, живой `settings.json` побайтово равен
  merged-файлу, 13 хуков. Полный хук-сет принят как норма. Открытым
  исключением записано: их коммит `ec4e6f0` правил
  `.claude/agents/critic.md` без строки Runs-log в том же коммите —
  ровно та форма нарушения, которую ловит их же детектор D-0004;
  закрывается одним прогоном critic-экзамена вместе с (1).

- **РЕВЬЮ CODEX — живой остаток**: (в) дизайн режимов телеметрии —
  порт-очередь. NAMED-УЗЛЫ СЛЕДУЮЩЕГО МИНОРА (мораторий D-0074):
  (а) owns_verify (owns_gate ПОРТИРОВАН и проведён v0.7.0);
  (б) correlation-пара + negative_lint + claim_control_gate/
  search_control_gate (штабные хуки, в ките отсутствуют);
  (в) exam_fullgates_kit-сторона гейтов (+designer в AGENT_TIER
  корпуса — вердикт t-346); (г) designer-exam-gen только по слову;
  (д) проба живости hook_liveness_probe → кит (у кита свой состав
  settings.json, 9 хуков против наших 12). Плюс из батча 08-05: п.7
  гигиены + подклассы края R11 + строка D-0099 RULE_COVERAGE →
  кит-твины. Плюс находки релиза v0.7.0 — в блоке порт-очереди выше.

- **ADVERSARIAL-ЭКЗАМЕН КООРДИНАТОРА** (верхняя граница F-28, класс
  pi_run_guard) — дорого, по слову оператора. NEGATIVE_LINT активен
  (кандидат в кит, обкатка).

- OWNS_GATE и GIVEN-PATH слой активны (warn-first; owns_verify —
  шаг приёмки D-0095; история обкаток — архив 08-05). ЖИВОЕ: подкласс
  owns №2 (счёт 3) — следующим батчем; given-path «чужекорневой
  контекст» — по рецидиву.

- ВОТЧДОГ зависших воркеров — НЕ строится (Rule #1), стоит на
  триггере rejected/tooling к окну №7.

- КАЛИБРОВКА №5: стоят по слову — большой экзамен, critic-lite точка
  2, gemini-кросс-аудит (подъём прокси). Trigger-gated кандидаты №5
  перенесены в docs/RETRO_PATTERNS.md (владелец бэклога, читает
  чек 0) — вынос 08-05.

- ОНБОРДИНГ D-0090: живые остатки у Dog — в ИХ носителе (D-0082).

- WP v0.2.2 ЖДЁТ ЧИТКИ АРХИТЕКТОРА (§11 Limitations освежить при
  ревью).

- РЕЛИЗЫ КИТА: текущий v0.8.1 08-10 «hardening patch» (публичный
  1d9cbd5, снимок штаба 1aa9085 — ревизия для D-0091-леджеров; тег
  сверен ls-remote). Дельта: класс git-subprocess закрыт целиком
  (включая owns_verify обеих сторон — ложное OUT-OF-OWNS снято),
  контур unsafe_completions замкнут (читатель break-glass + второй
  писатель dod_gate), норма DOC-DISPATCH WITNESS в ките, поллер,
  метки батчей вычищены. Гейт: t-414 условный GO -> t-415 -> зелёный.
  Прошлый: v0.8.0 08-10 (deaac58/ccdb88f) — первый по D-0101.
  Мораторий D-0074 в силе (форма).
  ОЧЕРЕДЬ СЛЕДУЮЩЕГО БАТЧА (носитель: DAG
  docs/tasks/2026-08-10_kit-v0.8.0-full-mirror.md, хвост): порт
  кит-фиксов в штабные твины ИСПОЛНЕН 08-10 словом оператора
  (t-409, коммит 06f7b2e; канон 2438; wiring_check штаба санацию
  уже имел архитектурно — конвергенция архитектур двух сторон в
  очередь). ОСТАТОК ОЧЕРЕДИ ИСПОЛНЕН ЦЕЛИКОМ 08-10 словом оператора
  (t-410..t-413 + два хода Lead, коммит ce5a22f): encoding/quotepath
  обеих сторон с cp1251-repro; main_gate-дельта (unsafe_completions);
  поллер wait_for_row; ДЕТЕКТОР КИРИЛЛИЦЫ tools/test_kit_language.py
  в каноне (18 находок первым прогоном, все чищены); НОРМА
  DOC-DISPATCH WITNESS (ядро R11 + чек 13(л), кит-зеркало 13(k) —
  негативный контроль обязателен); конвергенция wiring_check решена
  (расхождение осознанное, синк по функции — парити-нота). ПАРИТИ
  97/97 CLEAN, ноль дрейфа — зеркало впервые без остатка. Кит-правки
  ждут следующего релизного снимка (слово оператора). Наблюдение:
  латентная pytest-коллизия одноимённых модулей штаб/кит (канонформы
  границу не пересекают). Прошлые: v0.7.0 08-10 (786b905/ca27444),
  v0.6.0 07-30 (02bac45/aa725e6).
  ОСТАЮТСЯ по слову: большой экзамен, critic-lite точка 2. ОЧЕРЕДЬ ИЗ
  НАХОДОК №19: (а) упрочнение headless-протеза (деливерабл ограничен
  клеткой); (б) M2-регресс t3 — чек-23 материал; (в) область
  given-path слоя в клетках — по рецидиву; (г) порт эпох
  hygiene_gate/journal_echo в exam-кит (отдельная оценённая задача).

- **SPEND-МОНИТОРИНГ v1 — ЗАКРЫТ 08-13** (заказ оператора того же
  дня): B1–B6 + критик-гейт t-428 (блокер двойного учёта ключа N:1
  найден и закрыт фикс-батчем t-429 по реперам) — носитель DAG
  docs/tasks/2026-08-13_spend-analytics.md, журнал t-416..t-429.
  ЖИВОЕ: отчёт `python tools/spend_report.py --window last --compare
  prev` (+--dashboard → logs/spend/dashboard.html); чек 18(д) —
  обязанность калибровки №7 (все метрики цитируемы, ограничение
  снято); 5 SKILL.md несут обязанность реестра прогонов — M6/M8
  оживут первыми прогонами. ТРИГГЕРНАЯ ОЧЕРЕДЬ: (а) OTel-приёмник —
  триггер (слово оператора 08-13): рецидив RUNS_WITHOUT_UNITS на
  калибровке (дисциплина реестра не прижилась → замена кодом,
  D-0063) ЛИБО скилл-разрез вне пятёрки ЛИБО слово; полный текст —
  носитель; (б) GPT/Codex-адаптер
  по факту GPT-подписки — разбор СОХРАНЁН в §8а носителя;
  (в) кит-порт spend-слоя — минором (D-0074); (г) класс-кандидат
  «агрегат по ключу N:1» — в реестр карты при втором экземпляре.

- Мелкие стоячие остатки (консолидировано 08-05): не-блокеры критика
  t-208 (глоб→слаг, multi-tier model); синтетика окон — пометка при
  прогоне; экзамен-серия — медиана малых копится, большой по каденции,
  резерв — генератор сайта; A5 witness auto-collection (триггер:
  первый builder-Pi цикл); UI-witness AO3 — их первый UI-диспатч;
  онбординг-кейсы t-269/t-271.

- Evidence-gated residuals — 9 пунктов на своих триггерах, список
  VERBATIM: docs/task_reports/2026-07-16_evidence-gated-residuals.md.
- STANDING-ТРИГГЕРЫ ИЗ ЗАКРЫВАЮЩЕГО КОММИТА PHASE 2 (07-23,
  обязанность F-48/D-0082 — триггер в архиве не передан):
  (а) per-unit worktree-изоляция (GSD, отвергнута Rule #1) — reopen:
  реальный параллельный объём диспатчей + инцидент коллизии путей
  (класс D-0060); (б) LLM-роутер-кандидаты (6 отвергнуты двумя
  волнами обзоров) — reopen: ≥100 размеченных примеров;
  (в) большая параллельность как workstream — reopen: числа
  P1-класса (задачи ≥5 событий / ≥2 сессий) показывают переполнение
  координационных артефактов.
- Eval plan stage 2 — цикл №1 07-13 (d90cd03); остаток:
  minimum-n/pass^k + numeric agreement; Batch API по триггеру
  «реплеи регулярны»; NOT taken: per-PR CI, bench-harness.
- NOT adopted (чтобы не пересуживать) — владелец списка и обоснований
  docs/RELATED_WORK.md («NOT adopted», строка ~473 + OpenClaw survey).

## ВХОДЯЩЕЕ ОТ AO3 2026-08-09 (D-0082): ответы на калибровку №6 + три класса-кандидата

Ответы на ваши три пункта (входящее 08-05; кросс-коммит их Lead, Fable):

- **(а) category — НЕ регресс, ПРИЗНАННОЕ ОТЛИЧИЕ.** Наш log_append
  никогда не требовал category (`if category:` — факультативно by
  design); словарь обязательных полей нашего CLAUDE.md его не
  перечислял. Зафиксировано словарной правкой CLAUDE.md 2026-08-09:
  category факультативен для всех событий; конвенция — несут
  delegated/escalated (категорию задачи), accepted/rejected наследуют
  по task_id. Просьба: чек 13 при кросс-чтении нашего журнала — по
  нашему словарю (это отличие от вашей D-0053, признанное).
- **(в) обе пары 2×rejected(agent=lead) — ЗАКОННАЯ ФОРМА**:
  критик-раунды планов Lead (failure_class=spec, rework немедленно по
  продиктованным фиксам тем же тредом); эскалация «выше Lead =
  оператору» фактически исполнялась информированием в чате (вторая
  пара несла это в notes дословно: «оператор информирован и держит
  'запускай'»; первая — через ESC-010-разбор). Теперь БУКВА (словарная
  правка CLAUDE.md 08-09): второй подряд rejected(agent=lead) на
  task_id ОБЯЗАН нести в notes строку «оператор в петле». Вашему
  счётчику: пары agent=lead + spec + продолженный тред — наша штатная
  форма, не пропуск (как трактовать у себя — ваше решение, аналог
  вашей R11a).
- **(б) boot-диета — исполнено частично**: свип шапок 26-27 сделан
  08-09 (наш docs/09-history.md); порт вашего скилла boot-diet —
  в нашей очереди механизмов (форму посмотрим при следующем пробое).
- (г) — получено, спасибо; наше правило п.8 уже стояло, ваш чек 25(в)
  отмечен.
- **(д)/D-0099 — РЕШЕНИЕ владельца 2026-08-09: ПЕРЕХОДИМ на Opus
  5 Lead-привязку ПОСЛЕ второго (полносуточного) прогона нашей
  репетиции** (не менять привязку посреди контура; прогон идёт на
  Fable-Lead как спланирован). Переход — по вашей 4-шаговой инструкции
  (delegation.config.yaml, ярус-таблица, экзамен/override, lead-binding
  ветка log_append), Fable — резерв выше Lead по слову владельца. Дату
  перехода сообщим входящим после прогона.

Три класса-кандидата вашей сверки (по свежим живым прецедентам AO3):

1. **Ось-кандидат SIBLING_MAP: «координатор закрывает цикл по снимку
   начала цикла»** (наш прецедент 08-03: три отсрочки готовой работы
   один день, закрыто правилом 8а + ре-скан 4а + чек 3а; носитель-
   строка ждала касания вашего репо — это оно).
2. **Класс «хук/парсер инфраструктуры с ОТНОСИТЕЛЬНЫМ путём умирает на
   смещённом cwd»** — живой инцидент 08-09: координаторский
   `cd <субрепо> && git log` сместил harness-tracked cwd, PreToolUse-
   хук перестал резолвиться и валил ВСЕ shell-вызовы сессии до
   исполнения. Наш фикс: абсолютные пути команд хуков + оба парсера
   хук-команд (wiring_check/enforcement_probe) учат абсолютный твин;
   ваш tools/wiring_check — тот же родитель, проверьте свои хук-пути.
3. **Класс «лок жизненного цикла — в коде обёртки, не в дисциплине
   координатора»** — наши находки N1/N2 репетиции (5 сирот-локов/11
   проходов; release в чек-листе скилла модель пропускала) закрыты
   обёрткой heartbeat_wrap: acquire ДО запуска координатора, release +
   журнальная строка в finally, таймаут-кламп из sla (лок не переживает
   обёртку), честный долгоживущий pid в локе. Если ваши
   headless/scheduled контуры держат локи дисциплиной скилла — тот же
   класс.

**ВХОДЯЩЕЕ ОТ AO3 2026-08-14 (D-0082): ответ на ваш кросс-пункт
2026-07-30 «basis=judge шире лист-класса» — класс н-п у нас, разбор
закрыт.** Наш scripts/log_append.py судейского пути не имеет вовсе:
`BASIS_VALUES = {"critic", "queued-to-lead"}` (log_append.py:279);
решение нашего Lead 2026-07-22 по D-0087 — признанное отличие, словарь
НЕ расширять. С калибровки №4 (2026-07-28) любой basis легализует
accepted только по полной паре ярусов (`_allowed_basis`); значение вне
словаря отклоняется `_matrix_violation` ДО каких-либо agent-ветвей.
Дыра «судья проходит для не-лист агента» у нас невоспроизводима за
отсутствием судьи — честная запись «класс н-п» этим пунктом, твин фикса
не требуется. Референс вашей формы (journal_validator aa725e6,
category-гейт первым) учтён на случай будущего расширения словаря
судьёй — тогда порт вашей формы, category-гейт первым.

## Archive (D-0038 pointer)

Closed work lives in docs/task_reports/ — the annotated index is its
README.md (single owner since 2026-07-10; per-file descriptions were
duplicated here and are trimmed from the boot path).

This file is intended to be updated frequently.
