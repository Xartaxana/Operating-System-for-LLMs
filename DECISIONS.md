# Decisions Log (index)

One line per decision — the operative statement. Full texts
(rationale, history, rule-10 answers): docs/DECISIONS_FULL.md,
point-read on demand (D-0051). A new decision adds both the index
line and the full text in the same commit; entry counts must match
(calibration check 12).

- D-0001 — Git is the single source of truth.
- D-0002 — English is canonical; Russian is a synchronized translation.
- D-0003 — Engineering over Perfection.
- D-0004 — Project = White Paper + Architecture Spec + Reference Implementation.
- D-0005 — Kernel independent of any specific LLM.
- D-0006 — Project memory is layered.
- D-0007 — Every document reachable from repository navigation.
- D-0008 — Every Patch leaves the repository consistent and navigable.
- D-0009 — Repository content overrides chat history.
- D-0010 — BOOT.md defines the canonical loading sequence.
- D-0011 — SYSTEM_PROMPT.md defines permanent behavioural rules.
- D-0012 — Every commit improves the repository as a knowledge system.
- D-0013 — Recurring practices are documented as repository protocols.
- D-0014 — No important knowledge remains only in chat history.
- D-0015 — Repository stores both knowledge and engineering processes.
- D-0016 — One commit = one conceptual change.
- D-0017 — Validate Before Elaborating.
- D-0018 — Infrastructure Before Features.
- D-0019 — Patch Format v2; large documents modified incrementally.
- D-0020 — Human assistance is a fallback mechanism.
- D-0021 — The repository is executable engineering memory.
- D-0022 — Success criterion: Zero Context Recovery Test.
- D-0023 — Every boot produces a standardized Boot Report.
- D-0024 — Phase 0 completes only after a passed ZCRT.
- D-0025 — Exactly one current engineering task at all times.
- D-0026 — Direct git commits are standard; patches are fallback.
- D-0027 — Supervision split Guard/Ledger/Analyst; supervision must cost less than it saves (Rule #1).
- D-0028 — Delegation driven by DELEGATION_TABLE.md: estimated up front, refined by Shadow Evaluation; no long measurement phase.
- D-0029 — Router deferred until Phase 1 telemetry shows what to route.
- D-0030 — Prefer existing open source; custom code only where none fits or it is the contribution.
- D-0031 — The LLM judge is a supervised worker: chief-judge audits, calibration set, upgrade only on measured degradation.
- D-0032 — Accounting prices, not cash prices; free tiers are cash discounts, not zero cost.
- D-0033 — Phase gates are telemetry-computable; gate report + Architect signature; first task of a new workstream is an evaluation, never a build.
- D-0034 — Claude Code subscription is the real Lead; transcripts are first-class telemetry, accounted at API list prices.
- D-0035 — Four-state delegation statuses: estimated / provisionally_validated / production_validated / rejected.
- D-0036 — Phase 2 workstream 2 is Context Management Evaluation; cache-aware accounting first, provider caching before lossy compression.
- D-0037 — Flat delegation: workers never spawn workers; decomposition/specs/acceptance belong to the coordinator; "decomposable" is an escalation category.
- D-0038 — CURRENT_CONTEXT.md holds live state only; closed work is archived to docs/task_reports/; boot context is a paid resource.
- D-0039 — Lead degradation is explicit, scoped, reversible: lead_degraded/lead_restored; no table/gate/DECISIONS changes while degraded.
- D-0040 — Workers are dispatched in the background by default; Lead availability is the scarcest resource.
- D-0041 — Subscription-contour delegation is opt-in: policy must auto-load (CLAUDE.md); agents alone are insufficient (F-1).
- D-0042 — Operator-initiated downward model switch is a lead_degraded trigger; calibration cross-checks declared models vs cc_usage.
- D-0043 — Fix the class, not the instance: name the class, sweep siblings per SIBLING_MAP, rule at the highest binding level.
- D-0044 — Restoration from degradation includes acceptance of the degraded window (journal + diffs) in lead_restored notes.
- D-0045 — Rejections are journaled (`rejected`, model required); two on one task at one tier make escalation mandatory.
- D-0046 — Information workers are accepted by trail, not trust: scout Trail block, spot-checked claims; critic's ACCEPT carries its own trail.
- D-0047 — Weekly calibration leaves a `calibrated` run record; checklist in PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md; Boot Report watches staleness.
- D-0048 — The sibling map is verified externally (liveness, recurrence rule) and shrinks as well as grows.
- D-0049 — Rule 10(c) is a lifecycle invariant: every mechanism has a REGISTERED failure detector, or it is not a mechanism.
- D-0050 — Session close is checked symmetrically to open: session-handoff skill at Session End; the next Boot Report detects skipped handoffs.
- D-0051 — Boot diet: DECISIONS.md is this index; full texts in docs/DECISIONS_FULL.md; CLAUDE.md operative-only; CURRENT_CONTEXT archived per D-0038.
- D-0052 — Acceptance is evidence-based both ways: builder-accepted carries a witness (actual run output); `defect_found` retro-attributes late defects to the original dispatch; rejected notes carry a failure-class word.
- D-0053 — Load-bearing journal facts are typed fields, not prose: task_id links a task's events; attempt/failure_class on rejected, witness on builder-accepted, ref on defect_found; notes is human-readable surplus.
- D-0054 — Every dispatch carries a tier-shaped DoD: what "done" means and how acceptance will verify it; a worker returns a DoD-less dispatch with questions before starting work.
- D-0055 — Rule 10(b) is answered by enumeration over the CURRENT map's axes and enforced by a commit-msg hook: a mechanism commit without the axis block is rejected; explicit skip line only from the commit message.
- D-0056 — Lead tier is verified at both ends and externally: entry check before the first Lead action of a session, visible ascent proves a degradation window regardless of the journal, weekly calibration sweeps actual Lead-session models vs window coverage.
- D-0057 — Scout recon is regression-tested: a golden set with pinned keys (negative-claim and judgment-refusal cases mandatory) runs before any scout.md edit or tier-model swap; builder exempt (execution-based acceptance), critic set queued; detector is calibration check 14.
- D-0058 — Role ≠ tier: a session's authority comes from its actual model, not from the policy's addressee; acceptance only from above (an equal tier accepts only with a critic input or queues to the full Lead), the critic-skip concession belongs only to an acceptor above the worker; the planned Sonnet-coordinator mode is the same matrix.
- D-0059 — Phase 2 gains a third gated workstream: externalize the task pipeline (intake/scope/DAG generate/allocate) from the Lead's head into explicit artifacts, artifacts before code; order fixed (task brief → DAG artifact → allocate column → code last); rationale preserved in docs/TASK_PIPELINE.md; decomposition authority stays with the Lead (D-0037).
- D-0060 — Parallel-session discipline: task_id novelty is checked against the journal tail at write time (OS scheme: t-NNN max+1; AO3: descriptive ids, novelty + reopen flag enforced by log_append.py — the id scheme is a deployment convention, the no-silent-reuse invariant is what ports); parallel dispatches and sessions declare owned paths and never touch another session's uncommitted paths; a later-found collision is never rewritten — flagged in the next event's notes and counted as two tasks; detector is check 13(д).
- D-0061 — "Do the next priority task" is a command, not a chat phrase: the /next-task skill sequences tier check (D-0058 matrix, runnable from any tier), priority pick from CURRENT_CONTEXT (D-0025), background dispatch with DoD, evidence acceptance and rolling commits; the skill orders steps while CLAUDE.md stays the only source of rules; invoking it is work authorization after boot.
- D-0062 — Two vocabularies, one bridge: scout/builder/critic/Lead are canonical FUNCTION names — the only vocabulary policy rules speak; intern/junior/middle/senior are model GRADES for accounting and the delegation table; a deployment ports the policy by binding functions to models, an unbound function is a recorded difference; the bridge table lives in ARCHITECTURE.md "Two Vocabularies".
- D-0063 — Two-layer enforcement: code on the execution path guarantees the ENCOUNTER with a rule and decides only the deterministically decidable (presence, shape, typed fields); the verdict on fulfillment IN MEANING belongs to an AI tier above the performer and is never inferred from syntactic proxies; promotion from discipline to gate follows journal evidence of leaks (Rule #1), and deterministic sub-questions inside AI judgments move into code; principle text in ARCHITECTURE "Core Insight".
- D-0064 — Rule 10 gains question (г): every mechanism states its enforcement layer — what triggers it and what code stands on the execution path; «held by discipline» is legal only as an explicit line naming the (в) detector; promotion to a gate stays evidence-driven (D-0063); the queued rule-10(б) retro sweep is extended to ask (г) of all decisions in the same pass; detector is calibration check 8 (class F-24).
- D-0065 — Recognition defense-in-depth: rule 10's own trigger is the semantic call «this is a mechanism», so it is held by echelons — a recognition test in rule 10 (autoloaded; doubt = mechanism), the gate's net widened to known mechanism homes and the enforcement chain itself (ARCHITECTURE.md, BOOT.md, PI_HARNESS, the gate, .githooks — both repos), a recognition audit in check 8 (calibrating Lead judges candidates), and retro backfill via the queued sweep; 100% by code alone is explicitly not promised (F-25).
- D-0066 — External-repo surveys are two-pass: scout delivers the general map (D-0046 acceptance unchanged); a mechanism enters the plan/queue only after the Lead's own targeted second pass over the promising spots, whose trail is a mandatory part of the RELATED_WORK section; detector is calibration check 16 (class F-26).
- D-0067 — Boot diet round 2: the boot path loads ARCHITECTURE_BOOT.md (session-sized operative core) while the full ARCHITECTURE.md stays authoritative for humans and is point-read on demand; DELEGATION_TABLE's Shadow Evaluation Log relocates verbatim to docs/SHADOW_EVALUATION_LOG.md with a pointer (new runs append there); both pairs are registered on SIBLING_MAP axis 4 and their liveness is watched by calibration check 12.
- D-0068 — Boot-budget breach response is an executable skill (boot-diet), the single owner of the procedure: measure and name the grower, archive-sweep every boot file that has an archive home, verify archiving correctness (verbatim, pointers, index) and condensed↔full pair convergence, deduplicate ownership, re-measure; deep cuts of operative homes only by Lead+Architect decision; the session-handoff budget check triggers the skill and does not duplicate its steps.
- D-0069 — Journal format and the D-0058 acceptance matrix are enforced by code on the commit path: a pre-commit validator checks NEW routing-log lines only (append-only prefix, D-0053 typed fields, ts monotonicity and no narrative-future, task-id novelty with continuation/retry branches); new accepted/rejected carry `by` (acceptor model self-declaration), with `basis` = critic / queued-to-lead as the recorded exceptions; a self-activating enforcement file is never placed on the execution path by its builder — the Lead places it at acceptance (t-031 precedent).
- D-0070 — Toolkit workstream (Phase 3): the system becomes an installable tool for OTHER projects — core packaged apart from this dogfooding repo (users never see our tasks/plans), the user binds models to roles in one step, models are EXAMINED at onboarding, delegation then runs itself with minimal-attention notifications; stage order fixed (inventory eval → operator packaging decisions → core spec → template skeleton → validation on a fresh AND an existing project → public wrap); first task is an evaluation, never a build (D-0030/D-0033); intake: docs/TOOLKIT_INTAKE_2026-07-11.md.
- D-0071 — The critic tier gets an exam: a seeded-defect diff dispatched as an ordinary unmarked review (a trap-in-flow, not a vignette), with the independent-reproduction pair (fabricated witness / unverified negative) mandatory, hallucinated blockers and ACCEPT-on-seeded-diff failing by construction; ships in the toolkit (critic-exam-gen at onboarding — a user's critic is unexamined by construction) and lives here as PROCESS/CRITIC_EXAM.md (regression trigger: critic role-file edit or model swap); builder stays set-less by recorded design (execution-based acceptance beats a one-time set).
- D-0072 — Mechanism commits declare their tier and code checks the declaration: the commit-msg gate requires a `tier:` line on every axis-block commit, rejects declarations below the deployment's lead binding with a queue-to-Lead instruction, and calibration reconciles declarations against transcripts; promoted to a code gate on the FIRST recorded leak by explicit operator decision (F-36; the toolkit ships to other users, whose leaks we will not see) — a recorded exception to the evidence-threshold default of D-0063, not its repeal.
- D-0073 — Dispatch context manifest: every dispatch names its injected "given" basket next to the DoD; a writing dispatch adds owns/non-goals/handoff (optional maxConcurrent on a parallel fan-out); the manifest is declarative on reads (reading past the basket is a report line, not a violation) and normative on writes; a writing/parallel dispatch with no manifest is returned by the worker like a DoD-less one.
- D-0074 — Toolkit moratorium after v0.1.0: experiments live in the two dogfooding deployments (this repo + AO3); toolkit staging and the published template change only in verified batches, each sync opened by the operator's word; the default axis-7 answer in mechanism commits becomes "queued for port" (the port queue lives in CURRENT_CONTEXT, watched with staging↔published drift by calibration check 12).
- D-0075 — traffic_kind is honest by construction: the 'real' default stays (organic passthrough clients cannot tag), so every NON-organic generator self-tags synthetic/replay/judge in its payload; mislabeled rows are corrected loudly by Lead decision (exact ids + rationale recorded), never silently; the SessionStart MODEL line is a harness declaration, not a measurement, and says so (F-37); detectors are calibration checks 13(з)/13(ж)/5.
- D-0076 — The journal records accomplished facts, never intentions (F-44): delegated/escalated are written AFTER the dispatch call returns, same turn; new delegated lines carry a typed worker_ref field (validator-enforced — the handle exists only after launch); open dispatches are reconciled at both session boundaries (SessionStart hook line + session-handoff check) and swept for staleness by calibration check 13(и).
- D-0077 — Questions route UP, work routes DOWN; the USER is the apex of the hierarchy (above Lead): underspecified requirements are user-level questions no tier may absorb; the skip concession is directional (downward only); coordinator self-executed work passes the same acceptance as a builder diff; headless environments get a proxy-escalation prosthesis only as an explicit environment clause (exam protocol), never as general policy.
- D-0078 — Boot diet round 11: ROADMAP.md deep-cut (Lead+Architect decision, boot-diet step 6). Closed phases (0, 1, 1.5, 3) move VERBATIM to the standing archive home docs/task_reports/2026-07-15_roadmap-closed-phases.md; a status pointer stays per phase; live Phase 2 gates (R1–R5/C1–C3/P1–P3, deferred section, gate decision 07-13) stay in ROADMAP.md untouched. Future phase closures follow the same move in the closing commit. Pair registered on SIBLING_MAP axis 4; drift detectors — calibration check 12 (pair liveness) and the boot-budget measure (SessionStart hook / handoff check 4 / next boot-diet).
- D-0079 — Journal batch cadence: events are appended in a stage-boundary batch (one tail Edit), but never later than entering a long wait (background dispatch with no other work, pre-idle, handoff) — facts in session memory die with the session; delegated/escalated still only after the dispatch call returns (D-0076 fact discipline unchanged, cadence relaxed by operator's word 2026-07-16; per-event writes were the top coordinator cost in exams 5-10).
- D-0080 — Task-graph carrier adopted as standard (markdown DAG in docs/tasks/ for tasks >=5 events or >=2 sessions) and the delegation-boundary audit gains an executable loop: budgeted boundary probes (2-3 expected rejects/week, tagged in delegated-notes), shadow-replay of Lead's dispatch_skipped work on the API contour, and a Sonnet-coordinator exam on a NATURAL batch only — Architect signature on calibration #2's (в)/(г) candidates.
- D-0081 — Batching of small work: a non-blocking builder-class edit is never self-executed piecemeal by the coordinator — it accumulates in the session's list and goes to builder as ONE batched dispatch at a stage boundary ("батч мелочей" marker); self-execution with a skip event is legal only for edits BLOCKING the current move (the reason must name the blockage). Decided same-day on both halves of the evidence (capability: shadow-replay 5/8 equivalent; overhead: exams/check 11 — switching dominates, a batch amortizes it); calibration #3 VERIFIES by data (check 22 vs baseline 16) and may roll back.
- D-0082 — A cross-deploy queue item exists only in the TARGET deploy's boot-read carrier (OS: CURRENT_CONTEXT.md; AO3: docs/HANDOFF.md), written in the same move; own journal notes / FINDINGS are not a carrier — an item living only there is NOT handed over (F-48 promotion, second recurrence; encounter: session-handoff check, detectors: calibration checks 0/5).
- D-0083 — A tier-REQUIRING decision point closes by MEASUREMENT: the SubagentStop hook (tier_echo) prints the worker's actual model on the execution path; a mismatch with the requested tier is resolved BEFORE using the result as that tier's word (promoted on first recorded leak — run №15-t3 proxy substitution, operator word; external net: calibration check 5).
