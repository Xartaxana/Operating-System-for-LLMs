# Decisions Log (index)

One line per decision — the operative statement. Full texts
(rationale, history, rule-10 answers): docs/DECISIONS_FULL.md,
point-read on demand (D-0051). A new decision adds both the index
line and the full text in the same commit; entry counts must match
(calibration check 12).

Строка ретированного решения несёт инлайн-суффикс `[РЕТИРОВАНО
<YYYY-MM-DD>: <причина>; полный текст — docs/DECISIONS_ARCHIVE.md]`
(образец формы уже стоит на D-0011). Число строк и МНОЖЕСТВО ID
индекса равны множеству заголовков `## D-NNNN` в
docs/DECISIONS_FULL.md — держит tools/test_norm_retire.py.

- D-0001 — Git is the single source of truth.
- D-0002 — English is canonical; Russian is a synchronized translation.
- D-0003 — Engineering over Perfection.
- D-0004 — Project = White Paper + Architecture Spec + Reference Implementation.
- D-0005 — Kernel independent of any specific LLM.
- D-0006 — Project memory is layered.
- D-0007 — Every document reachable from repository navigation.
- D-0008 — Every Patch leaves the repository consistent and navigable.
- D-0009 — Repository content overrides chat history.
- D-0010 — BOOT.md defines the canonical loading sequence.
- D-0011 — SYSTEM_PROMPT.md defines permanent behavioural rules. [РЕТИРОВАНО 2026-08-18: поглощён CLAUDE.md (преамбула + R9) и PROJECT_PHILOSOPHY.md, файл SYSTEM_PROMPT.md удалён как дубль; полный текст — docs/DECISIONS_ARCHIVE.md]
- D-0012 — Every commit improves the repository as a knowledge system.
- D-0013 — Recurring practices are documented as repository protocols.
- D-0014 — No important knowledge remains only in chat history.
- D-0015 — Repository stores both knowledge and engineering processes.
- D-0016 — One commit = one conceptual change.
- D-0017 — Validate Before Elaborating.
- D-0018 — Infrastructure Before Features.
- D-0019 — Patch Format v2; large documents modified incrementally.
- D-0020 — Human assistance is a fallback mechanism.
- D-0021 — The repository is executable engineering memory.
- D-0022 — Success criterion: Zero Context Recovery Test.
- D-0023 — Every boot produces a standardized Boot Report.
- D-0024 — Phase 0 completes only after a passed ZCRT.
- D-0025 — Exactly one current engineering task at all times.
- D-0026 — Direct git commits are standard; patches are fallback.
- D-0027 — Supervision split Guard/Ledger/Analyst; supervision must cost less than it saves (Rule #1).
- D-0028 — Delegation driven by DELEGATION_TABLE.md: estimated up front, refined by Shadow Evaluation; no long measurement phase.
- D-0029 — Router deferred until Phase 1 telemetry shows what to route.
- D-0030 — Prefer existing open source; custom code only where none fits or it is the contribution.
- D-0031 — The LLM judge is a supervised worker: chief-judge audits, calibration set, upgrade only on measured degradation.
- D-0032 — Accounting prices, not cash prices; free tiers are cash discounts, not zero cost.
- D-0033 — Phase gates are telemetry-computable; gate report + Architect signature; first task of a new workstream is an evaluation, never a build.
- D-0034 — Claude Code subscription is the real Lead; transcripts are first-class telemetry, accounted at API list prices.
- D-0035 — Four-state delegation statuses: estimated / provisionally_validated / production_validated / rejected.
- D-0036 — Phase 2 workstream 2 is Context Management Evaluation; cache-aware accounting first, provider caching before lossy compression.
- D-0037 — Flat delegation: workers never spawn workers; decomposition/specs/acceptance belong to the coordinator; "decomposable" is an escalation category.
- D-0038 — CURRENT_CONTEXT.md holds live state only; closed work is archived to docs/task_reports/; boot context is a paid resource.
- D-0039 — Lead degradation is explicit, scoped, reversible: lead_degraded/lead_restored; no table/gate/DECISIONS changes while degraded.
- D-0040 — Workers are dispatched in the background by default; Lead availability is the scarcest resource.
- D-0041 — Subscription-contour delegation is opt-in: policy must auto-load (CLAUDE.md); agents alone are insufficient (F-1).
- D-0042 — Operator-initiated downward model switch is a lead_degraded trigger; calibration cross-checks declared models vs cc_usage.
- D-0043 — Fix the class, not the instance: name the class, sweep siblings per SIBLING_MAP, rule at the highest binding level.
- D-0044 — Restoration from degradation includes acceptance of the degraded window (journal + diffs) in lead_restored notes.
- D-0045 — Rejections are journaled (`rejected`, model required); two on one task at one tier make escalation mandatory.
- D-0046 — Information workers are accepted by trail, not trust: scout Trail block, spot-checked claims; critic's ACCEPT carries its own trail.
- D-0047 — Weekly calibration leaves a `calibrated` run record; checklist in PROCESS/WEEKLY_CALIBRATION_PROTOCOL.md; Boot Report watches staleness.
- D-0048 — The sibling map is verified externally (liveness, recurrence rule) and shrinks as well as grows.
- D-0049 — Rule 10(c) is a lifecycle invariant: every mechanism has a REGISTERED failure detector, or it is not a mechanism.
- D-0050 — Session close is checked symmetrically to open: session-handoff skill at Session End; the next Boot Report detects skipped handoffs.
- D-0051 — Boot diet: DECISIONS.md is this index; full texts in docs/DECISIONS_FULL.md; CLAUDE.md operative-only; CURRENT_CONTEXT archived per D-0038.
- D-0052 — Acceptance is evidence-based both ways: builder-accepted carries a witness (actual run output); `defect_found` retro-attributes late defects to the original dispatch; rejected notes carry a failure-class word.
- D-0053 — Load-bearing journal facts are typed fields, not prose: task_id links a task's events; attempt/failure_class on rejected, witness on builder-accepted, ref on defect_found; notes is human-readable surplus.
- D-0054 — Every dispatch carries a tier-shaped DoD: what "done" means and how acceptance will verify it; a worker returns a DoD-less dispatch with questions before starting work.
- D-0055 — Rule 10(b) is answered by enumeration over the CURRENT map's axes, enforced by a commit-msg hook; explicit skip line only from the commit message.
- D-0056 — Lead tier is verified at both ends and externally: entry check before the first Lead action, a visible ascent proves a window regardless of the journal, calibration sweeps transcripts vs window coverage.
- D-0057 — Scout recon is regression-tested: a golden set with pinned keys runs before any scout.md edit or tier-model swap; builder exempt by recorded design.
- D-0058 — Role ≠ tier: authority comes from the session's actual model; acceptance only from above; the critic-skip concession belongs only to an acceptor above the worker.
- D-0059 — Phase 2 gains a third gated workstream: externalize the task pipeline (intake/scope/DAG/allocate) into artifacts, order fixed, artifacts before code; decomposition authority stays with the Lead.
- D-0060 — Parallel-session discipline: task_id novelty is checked against the journal tail at write time; parallel work declares owned paths; a found collision is never rewritten.
- D-0061 — "Do the next priority task" is a command (/next-task skill): tier check, priority pick, background dispatch with DoD, evidence acceptance; invoking it is work authorization.
- D-0062 — Two vocabularies, one bridge: functions (scout/builder/critic/Lead) are the only rule vocabulary; grades are accounting vocabulary; deployments bind functions to models.
- D-0063 — Two-layer enforcement: code guarantees the ENCOUNTER and decides the deterministic; an AI tier above judges meaning; promotion to gates follows journal evidence of leaks.
- D-0064 — Rule 10 gains question (г): every mechanism names its enforcement layer; «held by discipline» is legal only with a named detector.
- D-0065 — Recognition defense-in-depth: the trigger «this is a mechanism» is held by echelons (recognition test, widened gate net, calibration audit, retro backfill); doubt counts as a mechanism.
- D-0066 — External-repo surveys are two-pass: scout maps; a mechanism enters the plan only after the Lead's own targeted second pass with a recorded trail.
- D-0067 — Boot diet round 2: the boot path loads ARCHITECTURE_BOOT.md; the Shadow Evaluation Log relocates to docs/SHADOW_EVALUATION_LOG.md; both pairs on axis 4.
- D-0068 — Boot-budget breach response is an executable skill (boot-diet), the single owner of the procedure; deep cuts of operative homes only by Lead+Architect decision.
- D-0069 — Journal format and the acceptance matrix are enforced by a pre-commit validator; a self-activating enforcement file is never placed on the path by its builder — the Lead places it at acceptance.
- D-0070 — Toolkit workstream (Phase 3): the system ships as an installable tool for other projects; models are examined at onboarding; stage order fixed; first task is an evaluation, never a build.
- D-0071 — The critic tier gets an exam: a seeded-defect diff dispatched unmarked; independent-reproduction traps mandatory; retriggered by role edits and model swaps; builder set-less by recorded design.
- D-0072 — Mechanism commits declare their tier and the commit-msg gate checks the declaration; calibration reconciles declarations against transcripts.
- D-0073 — Dispatch context manifest: every dispatch names its "given" basket; writing dispatches add owns/non-goals/handoff; declarative on reads, normative on writes.
- D-0074 — Toolkit moratorium after v0.1.0: staging and the template change only in verified batches opened by the operator's word; default axis-7 answer is "queued for port".
- D-0075 — traffic_kind is honest by construction: non-organic generators self-tag; mislabeled rows are corrected loudly by Lead decision; the hook MODEL line is a declaration, not a measurement.
- D-0076 — The journal records accomplished facts, never intentions: delegated/escalated are written after the dispatch call returns and carry a typed worker_ref.
- D-0077 — Questions route UP, work routes DOWN; the USER is the apex: no tier absorbs user-level questions; the skip concession points only downward.
- D-0078 — Boot diet round 11: closed ROADMAP phases move VERBATIM to the standing archive home; status pointers stay; future closures follow the same move.
- D-0079 — Journal batch cadence: events land as one tail append at stage boundaries, never later than entering a long wait; D-0076 fact discipline unchanged.
- D-0080 — Task-graph carrier adopted as standard (markdown DAG in docs/tasks/ for tasks ≥5 events or ≥2 sessions) plus an executable delegation-boundary audit loop.
- D-0081 — Batching of small work: non-blocking builder-class edits accumulate into ONE batched dispatch at a stage boundary; piecemeal self-execution only for edits blocking the current move.
- D-0082 — A cross-deploy queue item exists only in the TARGET deploy's boot-read carrier, written in the same move; an item living only in own notes is NOT handed over.
- D-0083 — A tier-requiring decision point closes by MEASUREMENT: the worker's actual model is measured on the execution path; a mismatch is resolved before the result is used.
- D-0084 — CLAUDE.md is the policy KERNEL (EN, norms-only); rationale and precedents live verbatim in docs/POLICY_FULL.md; the kernel wins on divergence.
- D-0085 — API-contour reference bindings and toolkit defaults mirror the subscription ladder; Opus-as-builder rejected by Rule #1; rebinding requires a new evidence line.
- D-0086 — Router gate criteria revised to the coordination framing: R2' = coordination overhead ≥25% (measured ~85%), R4' = pre-registered D-arm keys; original wording preserved in DECISIONS_FULL.
- D-0087 — Leaf routing adopted (kernel rule R13): a leaf may run allocate ladder → worker → calibrated-judge acceptance (`basis: "judge"`, leaf-class only) → deterministic R6 mirror; graphs keep the Lead loop; two judge forms (gateway alias / subscription subagent, 13/13 equivalence).
- D-0088 — One architecture: the task path is single and forks by SIZE at intake (leaf vs graph); contours are demoted to supply-and-measurement channels; replay and judge calibration stay API-side.
- D-0089 — Journal retro discipline formalized: the past is never rewritten; gaps are closed by retro pairs written NOW (formerly the dangling label "D-0056b").
- D-0090 — Onboarding is mapping, not copying: intake inventories the host's functions (D-0062 vocabulary) and decides the mode (greenfield full-kit / brownfield equivalence) by the inventory map; the adoption ledger (adopt / native-equivalent / deferred / rejected) is a DELIVERY NORM; a mechanism installs only with its prerequisites closed; layers go measure-first → warn → block on host evidence; host carriers are never migrated.
- D-0091 — Upgrade re-inventory (F-52): the host adoption ledger records the kit snapshot revision; an upgrade batch re-inventories by REVISION DELTA — role-file CONTENT included, every delta item gets a ledger decision (at least deferred), adopted role edits pass the host's exam gate before commit; host-ledger completeness is checked against the current template's row nomenclature; a new kit mechanism adds its template row in the same commit; detectors: staff calibration check 32 (port-batch delta ↔ handoff ledger lines) + kit-protocol check 22 (host ledger drift/completeness).
- D-0092 — Wiring drift (review t-284 remark): an adoption-ledger row with status adopt must reconcile with the deployment's ACTUAL wiring (hooksPath entries, settings hooks, script presence); continuous detector — the host's SessionStart wiring check reads the ledger's adopt rows, not only the configured hooks; window pair — kit-protocol check 22(b); the wiring-integrity check gets its own template row; porting a generalized tools/wiring_check.py into the kit is queued behind the release gate.
- D-0093 — Sealed delivery of the enforcement chain (F-53): a carrier changing an EXECUTABLE control-chain file ships the full target content, never a delta line (a delta cannot express the file's invariants); delivery/activation ends with a liveness PROBE — a known-invalid input must be rejected, then rolled back ("hooksPath set + file present" is not liveness); hook executability is wiring — the wiring check reads git-INDEX modes of the required hooks (100644 = silently dead gates on Linux clones), never the filesystem bit.
- D-0094 — Leaf routing is the DEFAULT path (MAY→default on the clean check-30 audit of calibration #4): a leaf-class task goes allocate ladder → worker → calibrated-judge acceptance → deterministic R6 mirror with no per-session operator word; a coordinator deviation is legal only with a recorded reason (window detector — check 30); recon-leaf intent keys carry the negative-form-control criterion (hygiene p.6); judge scope limits are unchanged — leaf-class only, never mechanisms/policy/integration wholes.
- D-0095 — Deterministic script runs are environment operations, not skippable dispatches (check-22 evidence: the class repeated ×12 in one window): launching/collecting an exam runner, construction orchestrator, validator or health check needs no dispatch_skipped event; the trace duty stays in the run's own carrier (Runs log / construction events); a run with no carrier trace is still a violation; in doubt the old skip-event form is the safe default; detector — check 22 (the class must VANISH from skip statistics; its return = a wording leak).
- D-0096 — The dispatcher's pre-send five-point checklist (check-23 evidence: the "spec/basket did not name the data" class recurred against a live D-0073): question/completeness criterion; DoD inline with the exact run; "given" sufficient with data NAMED; owns/non-goals/handoff on writes; freshness of load-bearing facts against their carrier, not memory. A miss exposed by a reject/finding = dispatcher spec-defect (check-23 case); machine-layer promotion on the next recurrence (D-0063); detectors — checks 13(г) and 23, already registered.
- D-0097 — Kit release is an executable skill (kit-release), the single owner of the release procedure: named gate before the word, staging-vs-port-queue inventory (reconcile, don't apply twice), pre-release small-work batch with a critic gate and a Lead control rerun on the final tree, snapshot assembly with explicit deletion review and hook exec bits set in the CLONE's index (100644 = silently dead gates, F-53), tag+push, post-release duties (snapshot revision for host ledgers, carriers, cross-items) in the same move; invoked only on the operator's release word (invocation = authorization); step-failure detectors are the pre-registered externals (checks 32(г), 14(е), kit check 22, check 24 reverse pass).
- D-0098 — Phase 4 Hardening-for-Delivery: for the DELIVERED product the default flips from evidence-driven promotion (D-0063 "on leak evidence") to PROACTIVE hole-closure — potential enforcement gaps are hunted and closed before any future user can hit one; D-0063's leak-evidence default stays for staff-internal mechanisms; every proactive closure still needs a named detector or gate (a wish is not a mechanism); the phase's entry evaluation is the 2026-07-24 enforcement-gap audit.
- D-0099 — Lead rebound Fable→Opus 5 (operator's word, cost motive: Fable 60.4% of API-equivalent cost at 30.4% of tokens, 14.06–14.07 window); the binding materializes in delegation.config.yaml (roles.lead) and code gates resolve it (journal_validator lead-binding branch: by == binding family accepts agent tiers <= itself, independent-context clause for critic/designer output; mechanism_gate accepts tier declarations strictly ABOVE the binding; session_context labels by binding); Fable = reserve tier ABOVE Lead, summoned only by the operator's word; escalations above Lead go to the operator (R11a unchanged); F-22 risk on peer-accepted reviews consciously accepted, watched by the calibration defect_found stream. Amendment same day: the gate's RANK SOURCE is the exam-validated role ladder of the config (scout < builder < critic < lead, optional roles.reserve above lead) — exact model-id match ranks by rung, the Claude-family substring stays as the notation convenience and the no-config fallback; ladder validity is held by onboarding exams, not the gate.
- D-0100 — Transfer of the understood to adjacent places: a class finding carries an APPLICABILITY QUESTION (a candidate test, not a history); a named class immediately sweeps its own artifact's neighbors as an enumeration with verdicts, above a threshold — scout routing; classes with ≥2 instances in DIFFERENT artifacts enter the failure-mode register in the symmetry map as the critic's input.
- D-0101 — Kit = mirror of the staff branch minus development traces (operator's word 2026-08-10, retiring the road-tested-only port filter): every rule, check, tool, hook and process doc ships; only development kitchen stays behind (kit-release skill, staff parity tooling, live keyed configs, real-traffic corpora, staff history logs); one staff calibration of a mechanism suffices for shipping; road-testing moves to volunteer hosts; D-0074 keeps its FORM (kit edited only by release batches on the operator's word) while its content filter changes; detector — check 12 extended: any hq-drift not explicitly marked "kitchen" at calibration = under-delivery, a finding.
- D-0102 — Scout rebound Haiku→Sonnet (operator's word 2026-08-16; evidence: exam v2 on DOG's F-A..F-I design tests from 8 real journal incidents — haiku 2/10 twice with systematic carrier-substitution fabrication, sonnet 10/10 on the same pre-registered keys; wall-clock ×3 consciously accepted). Acceptance trust follows EXAM STATUS, not model name: a binding holding a current exam pass is accepted by trail coverage alone — mandatory Lead re-verification lifted, the digest itself still carries same-form controls for negatives (worker duty); currency voids on rebinding / role-file edit / set rebuild / defect_found, strict form returns until re-pass. The scout FUNCTION is kept: its motive is context isolation and the digest contract, not model price (same motive logic as R2/R8 designer).
- D-0103 — Full boot auto-fires on a fresh session start via the SessionStart hook (operator opted in 2026-08-18, amends D-0038's request-only default). session_context.py injects an AUTO-BOOT directive (read Layer A+B of BOOT.md, produce a Boot Report, STOP) when the hook source is startup/clear — or absent/unknown, fail-toward-boot; NOT on compact/resume (the compact summary already carries state, so re-injecting the paid boot there is the exact waste D-0038 guards). Boot ≠ work authorization (BOOT_REPORT_PROTOCOL rule 4) — the STOP stays, the operator still greenlights the first task. Enabled by the boot restructure that made the load ~2 layers (~8KB orientation + state), so paying it every fresh session is cheap. Detector — autoboot unit tests (tools/test_session_context_autoboot.py) + boot_path_files pin-test; hook liveness — calibration check 13(ж) [was mis-cited 14(ж) until 2026-08-25 — F10, designer t-590].
- D-0104 — AUTO-BOOT goes HYBRID (operator's word 2026-08-25, refines D-0103's delivery form): the SessionStart hook PRINTS Layer A verbatim (five orientation files, hardcoded _LAYER_A_FILES with a pin-test against BOOT.md) instead of only directing the session to read them — D-0103's directive-only form leaked the same day it was probed (a session answered the operator with Layer A unread). Layer B stays a pointer (operator: "слой А обязателен и меньше по объёму", 8279 B vs 37848 B). Form: transliteration to ASCII with a LOUD warning on any unmapped character (operator: "главное чтобы смысл сохранился"); 16384 B threshold is a WARN, never a block (operator: "не останавливать впрыск, а сказать оператору"); the closing line carries counted files/lines/bytes-emitted — truncation is detected by the RECEIVER (no dependency on the undocumented harness stdout ceiling); a write DEADLINE (default 5 s, env OSLLM_STDOUT_TIMEOUT, os._exit(0) on expiry before any further I/O) caps the newly-measured hang: a single >4096 B write blocks forever on a non-draining consumer (critic's probe; kit release next day made the machinery immediate rather than trigger-gated). Detectors — check 13(ж) CLOSING literal «--- END BOOT LAYER A:»; batteries test_session_context_layer_a.py (B1-B27) in canon; emitted-bytes ≠ on-disk on live files (tautology was rev-1's defect). Built as sibling (D-0069), landed after critic control t-595.
- D-0105 — Experiment-before-machinery for unknown environment properties (2026-08-25, F-63, hygiene amendment 6(а)): a design decision standing on an UNKNOWN environment property first MEASURES it with one throwaway experiment; building machinery instead of measuring is legal only when the measurement does not port to the target environment (kit/Dog/AO3) or the experiment is destructive — and then the machinery's cost is written up as the cost of NOT measuring. Precedent: 2026-08-25 session spent a day (reference agent on wrong tier, half-tautological truncation detector, write-deadline machinery) around a question one fresh restart would have answered; the machinery survived only because next-day kit release made the measurement non-portable — the amendment's own clause, spoken after the fact. Core row: hygiene п.6 block (first amendment creates the block); twin: POLICY_FULL; detector — check 25 (mechanism commit whose spec cites an environment property as "unknown/undocumented/probably" without a measurement line).
- D-0106 — Given-manifest norm (А) (operator's word 2026-08-25, R11 amendment q): "given" is fulfilled by a MARKED declaration whose content is either an inline enumeration OR a pointer to a persisted spec itself carrying the enumeration — ONE hop, chains illegal by letter; a bare "read the whole X" without the marker is NOT a manifest; owns stays STRICTLY inline (owns has a machine consumer — owns_gate writes the ownership registry from prompt text; given has none). Decided by the reserve tier in two rounds (carrier docs/tasks/2026-08-25_given-manifest-decision.md): stands on three constructional legs (one-carrier-per-fact D-0038, owns/given consumer asymmetry, sufficiency judged above per D-0063); both empirical probes were demolished by the critic and carry no weight either way; a dead pointer fails LOUD and CHEAP, a stale inline copy fails SILENT and EXPENSIVE. Block message of dispatch_gate rewritten to state the norm (node A landing, t-596); detector — given_path_warn (pointer is a path token) + check 23 attribution of spec defects.
- D-0107 — Numbers carry their count carrier (2026-08-25, hygiene row 6(б)): a measured number cited in a norm/report names the file/command that produced it or is not written; comparisons only within one metric. Six instances (four from №8; F11 bytes-vs-chars; the Lead's own same-day core-size estimate). Detector — check 24(а); twin POLICY_FULL.
- D-0108 — Symbol claims only by Read (2026-08-25, hygiene row 6(в)): character/case/slash-direction claims are valid only by Read of the carrier; search output is not a basis. Detector — check 25 class list; twin POLICY_FULL.
- D-0109 — Hygiene п.4 rows а/б + pyc predicate narrowing (2026-08-25): named committed script with attached output is legal; pure -c/heredoc compute is out of п.4 — M/P/O classifier gated on pyc_certain (U = old text), deny path untouched, asymmetry pinned; built t-605 under critic gate (alias/receiver blocker fixed). Twin POLICY_FULL.
